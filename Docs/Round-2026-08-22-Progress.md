# Round 2026-08-22 — Progress Ledger

Two-track parallel round: **Track A** (accessibility, all 5 batches of
`Docs/Accessibility-Review-2026-08-22.md` §7) and **Track B** (food search, items 1–13 of
`Docs/Food-Search-And-Community-Database-Research-2026-08-22.md` §30).
Orchestrated per `Docs/Next-Round-Prompt-Parallel-2026-08-22.md`. Resume from this file alone.

## Setup (2026-08-22)

- Base: `main` == `origin/main` == `b259f3d`. Note: prompt expected 3 untracked `Docs/*2026-08-22*`
  files; there are 4 (the extra is `Next-Round-Prompt-Parallel-2026-08-22.md`, the round prompt itself).
  Proceeded — not tree corruption.
- Worktrees: `../fernlet-wt-a11y` → branch `claude/a11y-2026-08-22`; `../fernlet-wt-food` →
  branch `claude/food-search-2026-08-22`. Both untracked reports copied into each worktree's `Docs/`.
- DerivedData: `/tmp/dd-a11y` and `/tmp/dd-food` (never shared).
- Dedicated simulators (avoid contention with other sessions' booted sims):
  `Fernlet-A11y` = BBB73DF2-0E2E-4B2F-B437-58DBF12D563B,
  `Fernlet-Food` = B2A0DAC3-B9AC-4018-BC4B-C23D53AEA2D7 (both iPhone 17, iOS 26.5).
- Budget: no token target set for this round (`budget.total` null). ~14.9M session tokens at start.

## Standing rules (from the round prompt — binding)

- Track B owns `App/Fernlet/FoodView.swift`. Track A items T1-4 (FoodView:2266), T2-4 (MealRow),
  T2-14 (Food search announcements) → `DEFERRED-TO-RECONCILE`.
- Track A owns `FernletKit/Sources/FernletUI/`; Track B consumes, never edits — B files change
  requests here in the ledger.
- Merge order: A → main first, then B rebased, then reconciliation batch.
- Item 13: regenerated `FoodCatalog.sqlite` stays UNCOMMITTED pending owner LFS decision.
- 1.7(a) only; 1.7b (score-first) NOT authorized. Items 14–17 out of scope (deferred).
- No new network destinations, no new SPM deps, no hosted-UGC anything (Q1 settled: no).

## WIND-DOWN (2026-08-23): owner reports 2% weekly / 5% Fable plan usage remaining.
No new agents after the two in flight (item 9 implementation, food; chip-growth review, a11y).
Whatever they return is RECORDED, not cycled further: chip growth commits only if the review
pre-clears it or proves the recipe-sheet anomaly; item 9 lands as implemented-uncommitted with
its report in its ledger row (review = next session's first food move, per the 5b/A5 handoff
pattern). Then: final report + fresh continuation prompt. Items 10–13, 1.7(b),
confidentBindScore, Q2 provenance recovery, StoragePrivacyUITests fix, endgame gauntlet + merges
ALL DEFERRED to the next session.

## AI track opened (2026-08-23, owner-directed at wind-down)

Owner decisions: BYOK = pre-GA groundwork now (no bespoke pre-27 path); on-device routing =
DIAGNOSE FIRST then decide; scope = recipe creation + NL meal logging/portions + workout
suggestions; planning docs only + the one diagnosis increment. Plan doc:
`Docs/AI-Alternate-Track-Plan-2026-08-23.md`. Overlaps folded into the continuation prompt
(items 10/12 + endgame device session). Increment D (read-only bypass diagnosis, Opus) DONE — full
report persisted as `Docs/AI-Bypass-Diagnosis-2026-08-23.md` (owner's 10-min device checklist
inside). HEADLINE: `aiStatus = .off` is the APP default (SettingsModel.swift:61), onboarding never
mentions it — the features are INVISIBLE, not bypassed; meal logging is already AI-FIRST once
enabled; recipe creation has NO AI feature at all; workout Adjust is the only visible affordance
AND the app's only device-eligibility signal. Recommendation (owner to decide): explicit
affordance + discoverability + two honesty fixes, NOT default-flip (kills CI's grip on the shipped
path). CRITICAL for E2: AI-authored recipes CAN auto-commit without review today
(FernletStore.swift:2165 path) — pin first. `.deep` tier exists with zero dispatch sites, built
for exactly E1/E3. Worktree verified untouched.

## Increment ledger

| # | Track | Item IDs | Commit | Tests run | Notes |
|---|---|---|---|---|---|
| H1 | — (owner interjection 2026-08-23) | Home visuals: photo frames bigger + Customize quieter | **4bd4d20** on `claude/home-visuals-2026-08-23` (worktree `../fernlet-wt-home`, DD `/tmp/dd-home`, sim Fernlet-Home B53294A4-75F7-43FA-8062-7476726B5870) | build 0 warnings EXIT=0; scanners 0/0 density 0.682; before/after screenshots orchestrator-verified; no geometry test exists (WeightedPhotowallOrderingTests covers ordering only) | Implemented (Opus), HomeView.swift only. Tiles 57×50 → 104×92 (exact pre-FLOW-18 print, overlap restored), strip 92→126 + 10pt lift; Customize+Body-signals chips drawn ~30pt trailing-aligned, 44pt hit target/label/identifier/AX-size branch preserved (tap 7pt above pill proven). FLOW-18 fold cost ~12pt (disclosed). MERGE NOTE: HomeView.swift will conflict trivially with A-track's Home edits — merge this branch LAST, after A and B, resolve by hand. Owner visual sign-off pending (screenshots sent). |
| 0a | A | §6 Inspector probe (4 grouping questions) | n/a (read-only) | throwaway AX-walk probe | DONE (Opus). Verdicts below. Worktree left clean. |
| 0b | B | Item 1 (57-query corpus test) | **c6cb9c2** | FoodSearchCorpusTests 12/12; full FernletTests 2700/2700 EXIT=0 | Implemented (Opus): Tests/FernletTests/FoodSearchCorpusTests.swift. Report's 11 zero-result + 15 wrong-top-1 reproduce EXACTLY; mozzarella bind = 58 vs 250 confirmed. Vintage = composition proxy (catalog has NO vintage metadata; user_version=2 + per-data_type counts + 118,317 total, cross-checked vs loaded bundle). Adversarial review (different Opus): pipeline-fidelity/discrimination/arithmetic/parallel-safety/P10/vintage claims SURVIVED independent 6-mutation attack; 6 should-fix confirmed → fix pass dispatched to implementer: (F1) 31 "defensible" queries were outcome-selected → replace with neutral pre-registered mix, measure honestly; (F2) chickpeas pinned defensible but returns DRY row (2.2× kcal) → re-verdict; (F3) brandSource test vacuous when catalog missing → add #require; (F4) dump test zero-assertion; (F5) expectations in 9 places → single source + partitioned dump; (F6) resolver path candidates(for:) (searchPhrases + PreparedDishHeuristic reorder) uninstrumented → add resolver-surface bank. Notes kept: locale tie-break latent (tr_TR green), user-items absence documented, method-level -only-testing runs ZERO tests + green banner (never use). |
| 9b | B | Item 9 (1.9 history-first ranking) | **IMPLEMENTED, UNCOMMITTED** over 9aa090a (review = next session's FIRST food move) | new suites 21/4; corpus+adjacent 143/12 ZERO corpus/bank flips; walls 228/12; scanners 0/0 density 0.688; strict build 0 warnings; self-mutation M1/M2/M3/M5/M6 each red-then-reverted | Implemented (Opus). §26's prescription built exactly (history tier = TOP key of FoodItemSearch.ranksAhead, weight round(1000·log(1+count)·exp(−days/21)); comparator-not-reorder is measured load-bearing: Greek Yogurt cold rank 39→1 across 3 data-type tiers + past limit:6). THREE DIVERGENCES: (1) DERIVED not persisted — DiaryStore.recentMeals (50-meal ≈19-day window, inside τ band) via didSet + init publish (resetDiary empties the live catalog in the same statement — no relaunch hazard, no second call site); NO new persisted surface (historyIsDerivedNotStored grep-wall pins the 2 derivation sites + fails on any minted defaults key; PrivacyWipeCoverage gets a PROSE note deliberately not a table row — table is machine-parsed, rows need a wipeManifest token a derived surface lacks); (2) TYPED QUERIES ONLY, keyed on stripsStopwords — resolver pool + candidates stay cold (M2 shows the real cost: 3/18 pool rows rewritten if narrowing removed); (3) scaledWeight public for direct pinning. PRECEDENCE: correction WINS (statement over inference), structural (history re-ranks inside FoodItemSearch, promotingCorrection prepends after), aCorrectionOutranksTheHistoryTier pins compose-order. Gates: scoredResults has NO history parameter (signature-level exclusion); history reorders only rows that passed FTS + both 1.8 floors (historyNeverAdmitsARowTheGateOrTheFloorsRejected @weight 3000); 1.7a interaction pinned (promoted dish still sunk for bare-ingredient query). 0 new strings. Disclosed: spm-wall-check.sh IGNORES env DerivedData/destination (touched shared DD on first run — script fix worth doing); decay evaluated at publish not query (membership time-independent); didSet double-derivation per appendMeal (O(50)×2); comparator resolves weight once per row (tuple refactor in scoredMatches = named reviewer target); no-foodItemId components contribute nothing (name-fallback considered, rejected, documented). REVIEW TARGETS for next session: the tuple refactor, the stripsStopwords narrowing key, didSet/init publish seam pair, precedence composition, the prose-not-row wipe decision, decay staleness. |
| 8b | B | Item 8 (1.10 local correction memory) | **9aa090a** (final: structural no-dot-line rule replaced the modifier name list — reviewer's e2 evasion re-planted red with the over-run NAMED by swallowed modifier lines; reviewer pre-cleared commit on exactly this change; orchestrator pre-commit scanners independently 0/0 density 0.687) — was: in review | new suites 17 (37 w/ corpus); corpus batch 96; meal batch 88; wall batch 159 — all EXIT=0; scanners 0/0 density 0.687; spm-wall-check PASSED | Implemented (Opus), uncommitted over f83d327. THREE NAMED DIVERGENCES from §26's prescription: (1) storage = device-local UserDefaults sidecar `fernlet.foodSearchCorrections.v1` (cap 200 + R3 read-clamp), NOT tags-on-FoodItem — implementer argues the report's mechanism can't work (picks are read-only catalog rows; mirroring into foodItems = catalog data in the SYNCED blob + `index(for:)` unions without id-dedup → duplicates); report's "no disposition row" conclusion therefore wrong; (2) promotes `results(for:)`/`candidates` only, NEVER `scoredResults` (a correction must not push a quick-log past the review gates); (3) alias key = Replace-field text at pick (not meal.name) → query self-heals on search/typeahead/resolver-pool, NOT on re-quick-log of the same sentence. Write at Save only; "Add an item" deliberately not a write site. Wipe: resetAll → clearAll + inline setSearchAliases([:]) (in-memory copy); disposition row + PersistedSurfaceWipeBoundaryTests entry; plant-wipe-prove-gone test; one-writer structural test. Warmed-state bank separate from corpus (mozzarella/costco-slice/chiken-breast pin cold AND warm); corpus 6/24/27 zero flips. 0 new UI strings. Disclosed risks: Replace only appears on low-confidence components (narrower reach); broad-key ("cheese") rank-1 blast radius in resolver pools; per-device by design. Review DONE (Opus): 0 BLOCKER / 2 HIGH / 5 MED / 5 LOW. SURVIVED: anti-report argument ALL THREE LEGS (BundledFoodSource has no write API; foodItems = synced blob; index(for:) unions WITHOUT id-dedup — §26 row 1.10's mechanism is unimplementable and its "no disposition row" claim wrong; round prompt already anticipated the persisted surface); wipe wiring 4/4 red mutations incl. the boundary-suite row; firewall held over 24 measured cold/warm resolveMeals (zero flips); corpus determinism (corpus never constructs a store); bounds/concurrency (same NSLock, 400 interleaved ops clean); normalized-floor symmetry. REFUTED → fix pass in flight via SendMessage to implementer: (F1 HIGH) ordering guard = file positions not control flow — reviewer's write-at-pick mutation passed GREEN; (F2 HIGH) foodSearchCorrectionDefaults=.standard read AT INIT → process-global leak into un-injected test stores (proven, persists in sim container); (F3) one-writer scan sweeps only 2 App files; (F4) firewall proof pins a surface no gate reads — real protection = deterministicIngredients' alias-free re-filter + bindConfidence's carries floor, must be pinned as load-bearing; (F5) AI tier stamps .high ungated from the contaminated pool — floors bypassable when AI on (argued not measured; fix = gate AI picks or seam-doc + residual); (F6) prefill one-tap mints wrong-name one-word keys ("Cheese" et al. exist as exact row names) → skip when searchText==initialText; (F7) permanent/invisible/unremovable → Settings clear-corrections row (OWNER-VETOABLE addition, new strings join unsynced set); (F8-F12 LOW) displacement doc, 2 undocumented ungated consumers (fallbackMicronutrients, RecipeWebImporter.estimateMacros), ~87B not ~60B, "never synced" overstates (rides device backups), digit-keys can't fire as sub-phrases (no action). FIX PASS DONE (all 12): H1 → brace-counted Save-closure extraction + storage-free FoodSearchCorrectionDraft behavioral leg (compiling defeat variant proven red); H2 → init-param `let` + uniqueFoodSearchCorrectionDefaults threaded through both helpers; M4 → real firewall pinned (reviewer's chiken-breast probe: promoted row IN pool, never bound, never .high) + deterministicIngredients load-bearing doc; M5 PREFERRED option → retrievalGatedConfidence caps AI-tier .high→.low on minimumBindScore failure via shared bestSubPhraseScores (wiring source-evidenced only — model can't run in sim → on-device validation flag); M6 prefill-skip (apostrophe asymmetry pinned); M7 "Forget corrected searches" Privacy&data row (moss-not-terracotta, count onFirstAppear; +5 strings → unsynced set now 22 module + 7 app; 3 ride DestructiveConfirmation's pre-existing String fields). Corpus/bank zero flips; scanners 0/0 density 0.687; spm-wall PASSED. CLOSURE VERIFY DONE: 7/8 CLOSED (four by red mutations: DEFEAT-B ambiguous-marker throw, M3 third-file plant, M4 canonical-simplification break, M5 gate-off), RETURN-FOR-FIX on H1 ONLY — reviewer's DEFEAT-E compiles and passes green (stray `{` inside a string literal blinds the brace counter; write relocated to .onDisappear fires on cancel); prescription = string-literal-aware counting + dismiss()-present + no-view-modifier-token + literal size cap. Also: M5's "no-op on uncorrected devices" claim measured FALSE (admission scores description sub-phrases, gate scores split-item-name sub-phrases; 1/15 ordinary plans demote, ALL deservedly — safe direction; doc softened + multi-item test ordered); M4's test discriminates the canonical break but NOT a weaker fall-back-on-empty degradation (recorded honestly); DEFEAT-A' reachability-blindness class recorded no-action; forget-row IA placement (inside exportDataCard) = OWNER judgment call; helper-doc orphaning + displacement-key attribution fixes ordered. NARROW H1 FIX IN FLIGHT → targeted re-verify → commit. |
| 5bb | B | Item 5b (1.14 plausibility + completeness gate) | **f83d327** (2026-08-23: fix pass 2 — CFR regime derivation nutrient-aware, 6.75→8.75, RoundingRegime enum no-default; polyol 2.0 restated as engineering choice; no-scan completeness via static seams, red-if-reverted proven; closure verify re-ran mutations independently, F4/F5/F2 CLOSED, verdict COMMIT; its catalog-sync gate OVERRIDDEN by the standing single-post-merge-sync rule; verifier also found+fixed-in-A5-scope: sync-string-catalogs --check WRITES on stale trees (pipefail kills the restore) — its earlier app-catalog attribution to the fix pass was its own tool side effect, retracted; F7/F9-F11/F13/F14 unreconstructable from the record, noted in commit) | 175/11 owning+walls, 89/5 adjacent, 46 new tests, scanners 0/0 density 0.685, strict wall build passed | Implemented (Opus): NutritionPlausibility (5 checks, authorities cited as engineering facts, thresholds DERIVED from 21 CFR 101.9 roundings — 15 kcal floor computed; fibre-exclusion lower edge); absent-vs-zero load-bearing (optional-typed NutritionFacts, missing≠0, rule (c) stands down without all 3 macros); fires at CustomIngredientUpsert seam + label scanner + live BarcodeNotFoundView dialog (Rescan/Remember anyway/Cancel — never blocks); Evenepoel ceilings outer-guard-only w/ Warning; mesh clean. DEFERRED NAMED: 2 FoodView one-line adoptions → reconciliation; **20 catalog keys unsynced — ORCHESTRATOR runs sync-string-catalogs once post-merge (parallel-track conflict avoidance)**; barcode completeness surface awaits FoodView half. Review (Opus, authorities verified at PRIMARY SOURCES): 14 findings incl. (F1) band hull swallowed lost-decimals on high-fibre foods; (F4) 15 kcal floor misread rounding regimes (±2.5/±0.25, regime-wrong); (F5) superseded (c)(1)(i)(C) text + polyol blindness (sugar-free false positives); (F6) LIVE REGRESSION — (j)(4) exemption wired at the screen the proviso excludes, disabling the pre-existing nudge; (F3) §40.9 breach ("is fine" clearance + claim term); (F8) ceilings under a contradiction title; (F2) completeness surfaced nowhere + rememberFood still ?? 0; (F12) covered-destination-dismiss trap on Rescan. FIX PASS DONE: alternative-targets band (discriminating hull-mutation pin added); regime-aware per-value slack derivation (6.75/13.5 pinned); polyol 2 kcal/g split; exemption removed from the barcode screen (nudge restored) + water honestly labeled heuristic; advisory/contradiction partition w/ dynamic dialog title; pendingLabelScan flag (order-independent); "Still missing" line in both dialogs (named-then-stored, schema question ledgered); "and N more"; 243/14 battery + 9/9 mutation harness; UI hook infeasibility note names the exact follow-up (FERNLET_UI_TEST_OPEN_BARCODE_NOT_FOUND). **UNCOMMITTED — closure verify + commit = next session's first food task.** Catalog keys now 21, still deliberately unsynced. [CORRECTED by closure verify 2026-08-23: actual unsynced scope = 22 FernletDomainModel keys (13 nutrient.* + 9 nutritionCheck.*) + 2 app-target keys ("These numbers don't add up", "Worth a second look"); `sync-string-catalogs.sh --check` exits 1 until the single post-merge sync.] NEW LEDGER: schema-optionality question (absence representable to disk). |
| 5b7 | B | Items 5+6+7 (1.8 dual floor + 1.7a guarded demotion + 1.6 position stopwords — ONE coupled unit) | **809e25e** | corpus re-baselined 16 flips; audit re-pins ×3; full suite 2745/248 (1 attributed flake) | Implemented (Opus). **HEADLINE: 13/27/17 → 6/26/25 of 57 (70%→56% failure), zero corpus regressions, §29 calzone case passes BY CONSTRUCTION.** Measurement-forced divergences from §26: 1.8 = NAME floor not score floor (score floor deletes stem-reached correct answers); 1.6 = position rules not flat list; phraseScore dual-eval; 1.7a score guard. Tier-2 .high hardcode removed (burger-and-fries REFUTED as a confidence fix — it's wrong-VARIETY relevance, pinned still-committing); tomato soup never reaches tier 2 anymore. addResolvedMeals = architectural, written up at seam. Search/resolver divergence 10→6, asserted. One disclosed audit regression: chicken-noodle-soup's "pasta noodle cooked" binds nothing (template file fenced — one-string fix to authorize in fix pass). Remaining 6 zero-results = brand-index (item 13's 2.3), typo, partial-match (item 16 deferred) classes. §31 mozzarella promise REFUTED under (a), pinned → owner-question section. Review (Opus, independent A/B harness on the shipped catalog; all 57 pins re-verified live): headline/§29/guard/tier-2/fences/mozzarella-refutation SURVIVED. 3 HIGHs → fix pass in flight: (F1) name-floor justification REFUTED — both floors are complementary (score floor added changes 1 corpus row for the better, fixes `eggs` leading with 6 negative rows; the cited blocker was a 4-row synthetic fixture); (F3) floor deletes canonical category-reached rows (`burger` loses Hamburger,NFS, keeps BURGER KING Chicken Strips) → name-OR-category carriage; (F2) §29 hazard REAPPEARS on the resolver surface (searchPhrases sub-slices before searchTokens; `two slices of pizza` lost every sliced-pizza candidate — corpus structurally blind there) → strip top-level only + resolver-bank pins. (F4) carrier negation ("no bun" → pickle relish top-1) + missing carriers (salad/dip/rings/bits); realized 1.7a leverage was 2 flips vs §26's promised 15 — honest number after fix. (F5) floor raises top-1 scores → weak binds present confident; population measurement required, owner calibration question if material. Fence lifted for ONE DishTemplates string (dead "pasta noodle cooked"). Doc fixes ×8 + stopword-set freeze pins. FIX PASS + CLOSURE: reviewer RETRACTED its own F3 prescription after replaying it (calzone resurrected via category; substring carriage is right); both-floors dominant in the 4-design replay; all 57 pins + 32 resolver-bank entries at 0 mismatches on the independent build; F5 reproduced to the row. **VERDICT: SHIP** w/ 2 conditions: calibration risk named in commit (mine); retrieval/scorer token asymmetry fixed (micro-fix in flight — SQLiteBundledFoodSource.candidates strips unconditionally while the scorer keeps; ODR-tier truncation arithmetic recorded: cap=600, food_id ASC, grows with item 13's 2.3). New headline verified: **6/24/27 of 57 (70%→53%), 20 moved, 0 regressed.** |
| 4b | B | Item 4 (1.5 unmatched brand/retailer tokens) | **a3962e4** | audit 30/30; corpus untouched; full suite 2735/248/0; scanners 0/0 | Implemented (Sonnet). GATE MET: costco → .low + unmatchedItems ["Costco"] + review, decomposition intact; "cheese pizza" stays confident. Terms reused (retailerTerms hoisted; FoodBrandLexicon.matchedChainTokens twin over the same 43-entry chains set); Q7 pinned not decided; MealResolutionService zero changes (items 2-3 threading carries it). Note: additive method added in fenced FoodItemSearch.swift (no layout/normalized() change claimed). Review: gate survived end-to-end (both layers, variants, exact-match immunity proven); fenced-file diff additive-only confirmed; Q7 discipline held. Confirmed fixes → fix pass in flight: (F1 HIGH) apostrophe spellings defeat the gate — `wendy's burger` auto-commits while `wendys burger` reviews; 12/43 chains canonically possessive; fix scoped to the NEW matchedChainTokens only (Q7 untouched); (F3) chips show mangled canonical tokens ("Mcdonalds") → surface user-typed text verbatim (also dissolves the F8 doc contradiction); card body copy brand-inappropriate → item 10 FoodView work; (F4) meaningfulTokens.ignored is a hand-split second copy of retailerTerms → derive programmatically; (F6) per-meal token stamped before brand fold (misleading "Food match" on the addResolvedMeals no-review path) → stamp from folded quality; (F5) 3 documentation-tests pass with the feature deleted → make discriminating; (F2 pin-not-fix) chipotle-pepper/green-chilis food-word class forces review — §26's own named risk, pinned + ledgered as a Q7-tied trade; (F9) deterministic tie-break. LEDGERED for items 5b/1.14: "5 guys burger" → leading 5 parses as count, ×5 scaling, 2240 kcal auto-commit (under the 4000 gate; "5 guys" not in the chains lexicon). FIX PASS DONE: possessive collapse in matchedChainTokens (12 chains pinned both spellings, Q7 untouched); verbatim-typed chips via span machinery (self-caught bug disclosed + fixed); ignored set derived from retailerTerms; meal stamp from folded quality; discriminating tests; deterministic tie-break. Full suite 2738/248/0. Closure verify: ALL CLOSED — smart quotes (U+2019) proven working at mechanism level; span machinery survived independent mutation + edge battery; fenced file byte-proven additive. Micro-fix in flight: pin U+2019/U+2018 forms (working-but-unguarded) + pre-map Lm apostrophes (U+02BC/U+02BB defeat the gate via exotic keyboards) inside matchedChainTokens. Cosmetic residuals ledgered: trailing-apostrophe chains chip without the apostrophe; sub-word containment ("supersonic") = F2/Q7 class. Reviewer holds item 10 to the FoodView brand-chip copy fix. |
| 3b | B | Item 3 (1.3 template data audit + 1.4 unit-word parsing) | **5ee31f0** | audit 20/20; corpus 0 flips; full suite 2884/0/3 (after discarding a 776-test signal-kill wave from ANOTHER session's concurrent xcodebuild — verified via xcresulttool, retried clean) | Implemented (Sonnet): all 58 wrongFood/noCatalogRow binds repaired (mozzarella → real mozzarella @360); confident templates 2→14; sashimi defaultCount 1→5; 1.4 count parsing (singular own-unit mention → 1) scoped entirely inside DishTemplateLexicon — zero 1.6 overlap. **FLAG: "costco cheese pizza slice" auto-commits AGAIN (now with correct data, 1 slice) — "costco" disclosure deferred to item 4 (fix 1.5); item 4 is GATED on flipping this to review.** 4 item-13 dependencies pinned defensible (ramen noodles/egg, pizza crust, curry sauce). Review verdict: repairs largely real (24/24 replays matched; all plausibility totals independently verified sane; displayName NOT dropped — done well). Confirmed fixes → fix pass in flight: condensed tomato soup (1.9× kcal at .high where HEAD reviewed); hamburger bun SCORE-SELECTED (mixed-grain@300 vs correct plain@240 — chosen only to clear the floor); glutinous-rice miss across 8 templates (26% undercount, biggest leverage skipped); tortilla/bread/patty better rows at higher scores; fried rice double-counts (dish row + separate egg) → AUTHORIZED collapse to single component; sashimi defaultCount REVERTED (second consumer: defaultRecipeServings → "Serves 5" recipes; conflation ledgered as owner question); audit portion-shape table added (reviewer PROVED gramsPerUnit 10× and defaultCount 2→9 mutations pass silently); costco test must state cut-point truth (~390 kcal ≈ half a real slice, review sheet lost vs HEAD — acceptable ONLY with 1.5 landing same round). Ledgered: componentGramBounds token-intersection cross-match (verbose strings widened AI-tier gram bands; aiStatus off by default) + normalized() now load-bearing for count math (items 5/6/7 must not touch it) + word-number parsing residual ("three slices" → 2). FIX PASS DONE: soup→prepared row @1379; bun→PLAIN row @2050 via exact-name phrasing (no honesty sacrifice); rice→long-grain @2170 (5 more templates confident); tortilla/bread/patty upgraded; fried rice COLLAPSED to single-component (§27 pattern); sashimi reverted; portion-shape audit test (non-vacuity proven); costco test pins 388 kcal ≈ half-real-slice + not-a-stopping-point; Localization-Plan updated for displayName. Confident set 19/30, all kcals sane; full suite 2890/0/3 (reviewer's scope: 2731/248/0). Closure verify: 9/11 CLOSED, crust partial-by-design-disclosed, gram-bounds ledgered; reviewer's independent mutation proved the portion wall non-vacuous (and found the aliasOverride gram axis open). Micro-fix in flight: pin the one override gram; per-alias fried-rice variant rows where they exist; per-element shape-pin failure output; comments (isComposite dead field, tortilla tie, long-grain-for-sushi note). Sashimi/nigiri 33/45-kcal single-piece auto-commit = disclosed owner data question. |
| 2b | B | Item 2 (1.1 lexicon confidence + 1.2 bind floor) | **8c55627** | DishTemplateBindAuditTests 12 (new); corpus 14/14 untouched; MealBuilder/persistence 40; walls 53 — all EXIT=0 | Implemented (Opus). Hardcoded `.high` replaced by derived confidence (.high only if ≥1 bound, 0 dropped, all ≥250; else .low; .medium unreachable — needsReview is `==.low`, changing it would re-route the SAFER AI tier → OWNER FLAG). Failed binds drop the component (recorded), resolve nil only if none survive. Tester's query + "cheese pizza": identical decomposition, now .low → review, stamped roughEstimate. 92-component audit: 46 bind WRONG food, 8 no row, 8 dropped by floor (§26's "minimumBindScore=1 is a no-op" is wrong in this tier — it bites 8×), 10 confident-but-WRONG (floor can't catch — item 3 must). Only sashimi+smoothie still auto-commit; tomato soup + oatmeal now resolve nil. Interim state until item 3: template tier ≈ always-review (real delta corrected by review: 17/29 newly-reviewed; 8 were ALREADY review-routed by the 4000-kcal gate). Review confirmed 5 fixes → fix pass dispatched: (F1) per-item nil destroys correct multi-item decompositions and still auto-commits ("smoothie and tomato soup" case); (F4) dropped components invisible — thread into existing unmatchedItems card; (F5) merged meal token = parts[0] → word-order-dependent persisted token; (F3) audit doc overstates delta; (F7) alias-override rows mixed into a verdict resolve() never exercises (item-3 trap). DEFERRED WITH PAPER TRAIL: tier 2's highConfidenceResolution ALSO hardcodes .high — "tomato soup" relief arrives at item 5 (fix 1.8), which must also examine FernletStore.addResolvedMeals committing .low resolutions without review (AI-retry path). Items 3+12 briefs get: sashimi 28g bluefish auto-commit; ramen 46k-kcal serving-unit mismatch (`1.0 cup` servingSize handed gram units — 2.4/2.5 territory); templates carry no display name (unmatched chips show raw search terms). FIX PASS DONE: F1 keeps bindable items + records unresolved text (narrowing: unknown-to-lexicon items still nil the whole description — deliberate, pinned); F4 unmatched threading (burger → names patty+lettuce); F5 pessimistic token fold; F3 corrected to 17+2+2+9; F7 audit re-keyed by description (96 rows/30 descriptions). Full FernletTests 2721/248 green. Closure verify: F1(narrowed)/F4/F5/F7 closed, F2 deferral accepted — but fix pass introduced NEW-3 (pessimistic fold relabels recipe+foodMatch merges; doc claim false; test covered only the working case) + NEW-1 (duplicate ForEach ids on merged unmatched lists) + NEW-2b (dropped preparation prefix misnames the missing food) + F3 off-by-one (prose 8 vs test 9). F1's narrowing justification REFUTED empirically: "burger and fries" → tier 2 auto-commits Burger King burger + chili fries at .high with unmatchedItems==[] — residual filed against fix 1.8 alongside tomato soup (same root cause). Orchestrator ruling on NEW-2a: component search terms are food-name DATA (same class as catalog FoodItem.name, rendered untranslated app-wide), not UI copy — documented as such; real display names = item 3. Final fix round in flight. |
| 6a | A | Chip 44pt growth (T1-9 second half, owner-accepted) | in review | scanners 0/0/0 density 0.684; clean build 0 warnings; ScreenAppearanceUITests 30/2 — the 2 fails are the OPEN ANOMALY | Implemented (Sonnet), 3 files uncommitted over ba6d561: ChipButtonStyle.Chip.body `.frame(minHeight:44)` + `.contentShape(Rectangle())` (drawn capsule stays ~34pt — visual unchanged, target grows), WorkoutLocationSetupView redundant fernletTapTarget removed, 2 ratchet baselines re-harvested with justification (Sheet·Workout exercise.search clip; Sheet·Workout-suggestion "ANYTHING ELSE?" clip — that sheet's hand-budgeted 437pt medium detent is now broken by the accepted growth, FLAGGED as detent-policy, not fixed). Screenshots ×4 screens ×2 sizes clean. ANOMALY RESOLVED by review: PROVEN PRE-EXISTING — pristine ba6d561 fails the 2 recipe probes byte-identically (Control 1 + Control 2); "(49)" = textField = the sub-44pt `TextField("Qty"...)` at FoodView.swift:2046, whose `.hitRegion` audit finding FLAPS non-deterministically (0/8 recent runs) — this FALSIFIES UXScreenProbe.swift:339-340's "not one non-DT finding has ever failed to reproduce". Implementer's "zero ChipButtonStyle in RecipeSheet" claim CORRECT (the FoodView chips are in RecipeDetailView/RecipeRowLogControl, not RecipeSheet); only its causal attribution was wrong. REVIEW VERDICT: RETURN-FOR-FIX, short list, core mechanism SURVIVED (modifier order right; zero chip-overlap measured incl. 3-row FlowLayout; capsule 32.3pt unchanged; spread +9.28pt; both workout baseline removals proven causally by single-variable Run A/B — stronger than implementer's evidence; mood chips now 44.0pt, superseding the 31→33 ruling favourably). NEXT-SESSION FIX LIST (tiny, precise): (1) MoveView.swift:1873 detent doc now false ("ANYTHING ELSE?" sliced mid-glyph; exercise.search placeholder fully hidden behind Save bar — scroll-recoverable, not blocks) → update comment or re-budget; (2) FoodView.swift:2527-2531 FOOD-08 "without a drag" comment now false ('Post-workout' chip at y=744 under Save bar) — FENCED → goes to the RECONCILIATION LIST, not this branch; (3) SEPARATE attributed change: delete the 2 recipe baseline entries citing the Qty field + hitRegion flap AND correct the falsified determinism claim at UXScreenProbe.swift:339-340; (4) optional: F2 wording (42.25pt measured under sheets' 0.9602 centre-scale — pre-existing transform; claim should read "44pt in the view's own coordinate space"), F3 minWidth note (style guarantees minHeight only; short-label chip could be <44pt wide, no wall), F4 comment rationale. UNCOMMITTED pending that fix pass (wind-down: recorded, not cycled). |
| 5a | A | A5 (FINAL): T2-2/6/7/9/10/11/12/17/18/20/22 + §4.2 + Q2 + §4.5 wall + ANL declaration | **ba6d561** (61 files, +8622/−518; narrow pass: identity() label normalisation w/ collapse audit 136→135 + baseline-digit guard; DT-excuse fraction documented (5-6/18 Home, once 23/23 Cycle, DT=46/135 identities); tap-through ceiling doc'd both places; observationRow→LocalizedStringKey surfaced+fixed a 2nd same-class instance, +2 keys synced; audit Code=-56 timeout under load FLAGGED not fixed (XCTSkip recommendation, later round); orchestrator pre-commit: 3 scanners independently 0/0/0 density 0.684) | AccessibilityBoundaryTests + audit ratchet 25 screens + all walls green; clean build 0 warnings; scanners 0/0/0 | Opus + sub-agent fan-out. §4.2: Color(light:dark:lightHC:darkHC:) in the same provider, zero churn, approved hexes recomputed (slate HC 6.90, mossFill 7.22, moss reuses mossInk 5.54); goldenrod HC REFUSED with measurement (midnight ink 2.89 on deepened fill). T2-7: the 0.46 threshold WAS the bug — WCAG crossover 0.1791 + fit-against-harder-surface → 81.1%→0% failure over 512 sampled backgrounds (deliberate visual change for custom-bg users, documented). §4.5 wall: A1-A5 rules 0/385 + 9 allowlisted; tokenizer hole found+fixed (string literals erased, blinding A2 to interpolated rawValues); runtime audit THROWS chain, 119 real issues frozen at ratcheted per-screen ceilings. ANL: Dark Interface + Reduced Motion (enumeration) + DWC (greyscale screenshots) declared; Captions/AudioDesc blank-verified; 4 rows undeclared w/ named blockers. PREMISES REFUTED: Q2 toggles were never unreachable (real bug = selection state, fixed via .contain+.isSelected, probe evidence); wizard-PIN probe IMPOSSIBLE (XCUITest ignores accessibilityHidden — proven; the grep-wall is the ONLY enforcement, recorded). NEW FINDINGS for A5's review: MilestonesView.headline is a LIVE localization bug; DeleteEverythingSheet collapses 5 claims to 1 at AX5 (legally sensitive); gate re-lock on background/tab-leave in 3 routes (device check); .ignore-on-Button mints a traitless second element; sync-string-catalogs.sh aborts after first stale target. CLAUDE.md wall addition = owner action (agent correctly refused). REVIEW DONE (Opus, 2026-08-23): 1 BLOCKER + 4 HIGH + 15 MEDIUM + 9 LOW. SURVIVED exactly: every hex/ratio recomputed to the digit; T2-7 crossover derivation (0.179129) + 0/512 + 0/32768; tokenizer-hole fix; 119-baseline honesty; Q2 premise; T2-9 (artifact-level); T2-11/20/22; RM/Captions/AudioDesc declarations; all 5 self-reported findings real+triaged. REFUTED: (B1) runtime ratchet red-in-suite/green-isolated (Home 25 vs 19 — container-state nondeterminism) + count-only + no staleness; (H2) DWC declared while JournalMonthCell six-category hue-only (1.004:1 greyscale); (H3) DeleteEverythingSheet AX5 collapse unfixed in-scope; (H4) grep-wall missed 13/15 planted evasions (announcement channel = worst) + Swift port not whitespace-equivalent to Python; (H5) T2-18 temp-unit contradiction (.uk locale; user-selected unit ignored on read-back); (M6) T2-7 sim numbers 81.1/36.1 don't reproduce (82.0/35.2) and no artifact; (M8) fitted() drops the contrast axis (IC users get 4.5 where default gives 6.90); (M12) sync-string-catalogs --check ALSO violates never-writes (pipefail exits before restore — worse than self-reported); (M13) ContentView:172 force-light vs Dark Interface declaration; (M14) T2-14 non-food surfaces unimplemented; (M15) Siri shortcuts no-op-as-success; (M17) T2-17 dropped weekday info entirely; (M19) ConnectView albumCellLabel (fence doesn't apply); (M20) T2-18 fork duplicates PeriodTrackerStore helpers + drifted. FIX PASS DONE (fresh Opus, 31 files + 4 new): B1 root cause was a 4TH hypothesis — `.dynamicType` audit UNDER-REPORTING (Cycle 23→0, Home 18→12 on one binary; no other category ever flaked) → identity-based baselines (frame excluded), absences excused ONLY for that category, appearances always fail, 22 baselines re-harvested full-suite (136 identities), AuditRatchetBoundaryTests staleness wall, ScreenAppearance in-suite ×3 green; evasion recatch 1/15→15/15 + Swift/Python drift alarms (hardening surfaced real pre-existing SessionChatPanel.swift:123, allowlisted); DWC journal FIXED for real (6 SF symbols, bark-inked, DWC-off byte-identical); adaptive-ink-simulation.py committed artifact reproduces reviewer's replay to the digit (82.0/82.0/35.2/0.0), AdaptiveInkBoundaryTests 512-real-UIKit, fitted() contrast axis added (476/512 improve, 0 regress); sync-script `|| true` + trap restore proven on harness (1/8→8/8), --check EXIT=0 after a11y-key sync (xcstrings diff carries an Xcode-vs-xcstringstool formatting-only component; semantic = 84 added/11 removed); force-light DELETED with staleness proof + live sim check both directions; LockGate tap-through probe now PASSES FOR REAL (gate survives covered tap); Siri no-ops + a 5th unnamed path fixed (needed nonisolated under default-MainActor); temp unit via PeriodTemperatureUnit.regionDefault incl. edit-seed; T2-14 non-food count-announce; WipeClaim custom content; weekday on both grids; all LOWs. NEW RESIDUALS disclosed: LogWaterIntent Siri-visible rename ("Log a bottle of water"→"Log water") = OWNER-VETOABLE; CookingLiveActivityIntents has the identical no-op defect (out of scope, unfixed); LogPeriodSheet.swift:385 observationRow(String) loc-wall violation unassigned; PeriodTemperatureUnit selection @State not persisted (durable pref would need disposition row); CyclePhase.title .capitalized render. Manual list += paged-TabView covered-tap page change; .accessibilityValue on .contain container spoken?; AX5 custom-content announce check; lock-gate TOUCH-BLOCKING question (closure N3: the tap-through test's sensitivity is unproven — reviewer disabled hit-testing and it still passed; only observability is proven). CLOSURE VERIFY DONE (original reviewer): 27/29 CLOSED, verdict RETURN-FOR-FIX (NARROW) — reviewer pre-cleared COMMIT after: (N1 HIGH) identity baseline pins wall-clock/seed-derived labels ("SUNDAY, AUGUST 23" daily, "August 2026" monthly, "2 entries"/"2 of 8" in the non-excused category) → intermittent dated reds, normalise in identity(); (N2) DT-excuse doc must carry the measured fraction (5/18 excused on one passing run); (N3) tap-through ceiling doc; (N4) JournalView 1.25:1 claim is 1.3046/1.4205 measured; (N5) stale 119-count + broken DocC link in UXScreenProbe; + fold in LogPeriodSheet observationRow String→LocalizedStringKey. Reviewer's own mutations: DT-excuse CANNOT mask an appearance (M2 red), rename-a-probe red both levels, revert-to-count pinned. Every recomputed number reproduced exactly (476/0/36, 82.0/35.2/0.0, 1.0028, 4.9318, 1.4974). Evasion adjudication: baseline 2/15 caught (one accidental), now 15/15 — immaterial. NARROW FIX IN FLIGHT → then ORCHESTRATOR COMMITS A5 (no further verify round). |
| 4a | A | A4: §4.0 + T2-1 + T2-8 (non-FoodView) + T2-16 + T2-19 | **7a115ad** | FernletTests 2713/249; spm-wall-check PASSED; catalogs --check ×8; scanners 0/0 density 0.681 | Implemented (Opus). **§4.0 = option (i)**: catalogs for FernletUI(12)/ProximityKit(20)/AppServices(5), argued from the already-false owns-no-copy invariant, 3 no-call-site strings, the FernletLockUI precedent; caller-supplied discipline KEPT for parametric components. 19 violations fixed (17 beyond the report's 2). DestructiveConfirmation forked (26 sites); 15/16 String a11y members forked; WorkoutWeekCell was speaking a frozen rawValue aloud → displayName. SheetTextEditor placeholders were NEVER localized (verbatim overload) — fixed. 4 new wall rules (E1/E2/E3/F) + harvest-evidence pins, non-vacuity proven (E3's own first draft caught). One honest revert: a11yValue clobbered placeholder-as-value. Deferred, named: MilestonesView 24-sentence headline class, spliced-lowercase prose sites, .capitalized-on-rawValue class, FoodView sites. Review (Opus): §4.0 decision VERIFIED SOUND end-to-end (fr-translation injection resolved through Bundle.module; no Package.swift edit needed); ProximityKit 20 + AppServices 5 keys ALL clean display copy (no wire/token contamination); module bundles currently EMPTY (all-new English catalogs emit nothing — defaultValue covers; catalog-file test is the guard). 11 findings → fix pass in flight: (F1 HIGH latent) wall rules missed 8/8 planted evasions incl. the exact SheetSaveBar parameter-default shape §4.0 is about → harden for measured shapes + honest ceiling; (F2) rule F covers 7 not ~18 members, ~9 of the round's own forks unprotected → forked-member type pins; (F3) paletteName launders 16 spoken names via Text(verbatim:); (F4) new keys ship English in %@ slots (displayNoun/flowLabel/MoveGoalSummary/stored-vs-displayLabel/ItemSlot/built-ins) + a FALSE translator comment; (F5) 3 of 7 .textCase swaps inert (String receivers) + Turkish SİDE İTEM on frozen strings; (F6) pluralization test passes by construction; (F7) DestructiveConfirmation docs contradict themselves ×4; (F9) 2 surviving rawValue renders + 2 intensity.capitalized; F11 smalls. LEDGERED (F8): uncatalogued display copy in FernletScoring (~24)/HealthKitGateway (~10)/PrivateHealthStore (flowLabel) — a named deferred class, same silent shape §4.0 fixed elsewhere. FIX PASS DONE: rules hardened over logicalLines (8/8 evasions closed, honest ceiling written); 21 forked-member pins; 16 palette names keyed; flowLabel via typed-case app-target fork (sealed module stays clean); 4 inert textCase sites forked + record.category ruled frozen-classifier; plural variations added AND pinned; DEFERRED NAMED: LocationTemplate bakes English into persisted WorkoutLocation.name (schema+migration). FernletTests 2719/250; catalogs ×8 w/ variations surviving re-sync. Closure verify: SHIP verdict — 9/11 closed with discriminating proofs; final micro-fix in flight (7th rawValue render; flow-picker title; LocationTemplate deferral written in-tree; F8 ledger home; multi-line-container evasion (d) — square-bracket joining; passesBundle accepts bundle:.main → require .module in package source). Next-round recorded: typealias spelling, wrapped-colon shape, debug-inspector rawValue, coin-pill test binding. CaptureProtection flake co-signed as tracked-named-flake, not proven. |
| 3a | A | A3: T1-2 (FernletAnnouncer), T1-7, T1-10, T2-3, T2-5, T2-13, T2-15 | **26e4b89** | full suite 2703/249; both schemes 0 warnings; new FernletAnnouncerTests(6)+FernletDismissalWindowTests(4); scanners 0/0 density 0.683 | Implemented (Opus). Announcer: zero literals in package (LocalizedStringResource surface + resolved: escape hatch), 13 sites, one-seam grep-wall; 3 of §4.4's catch arms REFUTED (real alerts — double-speak); T2-3 focus moves DROPPED per report's own reproducibility rule (sortPriority band shipped; needs manual VO pass); T1-10 companion per probe verdict (single leaf, value=Thriving, pet default + Edit custom action). FLAGGED BY IMPLEMENTER: first allowlist hole in LocalizationBoundaryTests (announcer's String(localized:)). Review: allowlist mechanics SURVIVED, but the LocalizedStringResource overload itself is an unguarded channel — reviewer's planted package literal built clean, passed the wall, resolved to a raw key at runtime (HIGH, latent; all 13 shipping sites correct today). Fix pass in flight: mechanical closure (package callers must use `resolved:` — new grep-wall rule), ProximityRecipeShareSheet's 1.4s whole-sheet dismissal adopted (the missed worst case), .success adopted at the durable-write site + .increase absence documented, 7 dead toast guards restructured (strand-on-nil hazard), wrong-ceremony spinner copy, path-not-filename wall compare, honest sortPriority doc. FoodView createdNotice toast + bare ProgressView → reconciliation list (disclosed). FIX PASS DONE: wall closed with the reviewer's own probes proven red (both forms); F2 = stretched window NOT nil — sheet owns the recipe radio, never-closing = privacy regression (advertising forever); F6 fixed at the API (stranding branch unrepresentable); F9 preview labelled (first element, headerless screen); .success at 3 durable-write arms; full suite 2707/249, both schemes 0 warnings, density 0.682. Closure verify in flight. |
| 2a | A | A2: T1-1, T1-3, T1-4(4/5 sites), T1-5, T1-6, T1-8(+lock glyphs), T1-9(tab-bar half + QuickMoodRow migration), NEW-1 ChipButtonStyle isEnabled | **886a352** | 328 tests green (unit 265 + UI 63); scanners 0/0 density 0.681 | Implemented (Sonnet), 31 files. Recomputed inks match approved values. 20 text swaps (not ~31 — hand-classified; First Aid tiles judged icon-only — reviewer to settle). Residuals flagged: SettingsSheet's DeletingEverythingOverlay call site lacks covered-content hiding (5th T1-4 site); runtime AX probes skipped (sim MCP declined in agent session). Review (Opus, with synthetic .isModal harness + live AX probes): T1-1/T1-5/T1-6/T1-8 + token math + walls VERIFIED empirically; full suite 2692+162 green. Confirmed fixes → fix pass in flight: (F1 HIGH regression) Reduce Transparency branch paints selected tab pill parchment-on-parchment 1.00:1; (F2) equipmentCard (the named T1-5 control) still bare; (F3) 8 more moss small-text sites incl. same-screen inconsistency (First Aid call was CORRECT, report wrong); (F4) ConnectionSuccessOverlay .isModal proven no-op in fullScreenCover → accessibilityHidden fix; (F5) non-compact tab bar shrank 6pt for zero gain; (F6) weekday-initials hiding reverted (shipped T2-17's removal half without its naming half); (F9) wrong comment rationale; (F10) DisposableCameraView exemplar itself ungated on RM. Rulings: F7 residuals were mis-scored conservative (.isModal on overlay-presented covers hides unaided — SettingsSheet site NOT a gap); F8 QuickMoodRow 31→33pt ruled unification-not-growth (recorded for owner). Pre-existing StoragePrivacyUITests predicate false-match attributed to baseline via isolated worktree → spawned as separate task chip. FIX PASS DONE (33 files): F1 → mossFill/onMoss pill under RT (measured 4.67:1 / 5.36:1, existing tokens); F2 equipmentCard traited; F3 7 swaps done; F4 accessibilityHidden on FriendsView content; F5 non-compact padding restored; F6 weekday hiding reverted; F9/F10/F7 comments+gate. 293 unit + 40 UI green. Closure verify: 9/11 CLOSED (F1 ratios reproduce to the digit; F4 gating complete by construction; normal path byte-identical); F8 = commit-note wording (chips 31→33 = unification ruling); F9 micro-fix in flight (reviewer's side-by-side harness proved .combine DOES absorb unlabelled glyphs → the 2 Milestones hidings are inert, remove + correct comment). Reviewer's non-blocking notes ledgered: ConnectView "Connected" small-text moss sits on material-over-scrim (needs own measurement, not a reflex swap — A5's contrast pass); default non-compact tab label = moss@3.74 on parchment pill (pre-existing; fold into §4.2/T2-6 in A5); RT-mode inverted pill treatment = designer glance for owner. PrivacyDataSettingsUITests 1/7 chained-run failure attributed to load (7/7 standalone). |
| 1a | A | T0-1…T0-7 + T2-21 (dark launch) + AssistiveAccess key | **59fde3b** | FernletLockTests 20; lock/duress/crypto 101; cooking/period/intimacy 59; worry/capture/walls 81; CaptureProtectionUITests 6; ScreenAppearanceUITests 25 — all EXIT=0 | Implemented (Opus), 12 files, uncommitted. Key deviations (implementer-stated): T0-4 groups only the pacer, NOT all of runningScreen (keeps "End early" reachable); T0-7 caveat saves stay open + bar flips to Done (residual: post-write edits discarded on Done); T0-3 also covers biometric failure arms; sensoryFeedback closure form (avoids buzz on attempt start). AX-walk evidence: locked gate = 0 gated leaves + accessibilityViewIsModal true; landscape keypad scrollable (content 541 vs 393). Scanners 0/0, density 0.682, catalogs in sync, SUPPRESS_WARNINGS=NO clean. Residual for A2 (T1-8): lock overlay glyphs focusable, one speaks raw "lock.shield". Adversarial review (different Opus) returned 1 BLOCKER + should-fixes → fix pass dispatched to implementer: (F1 blocker) shared FernletNumericPad enlarged but ScrollView shipped to only 1 of 5 hosts — passcode-creation/change/biometric-verify/duress hosts clip at AX5; (F2) 2 of 3 focus writes fire before target exists (T0-4 inert if unlanded); (F3) savedWithCaveat pins draft guard off forever — post-caveat edits silently discarded; (F4) ScreenChanged skipped, cursor stranded on gate engage; (F5+F18) .isModal unverified per-leaf + T0-1 has zero automated coverage (all UI suites bypass the lock); (F6) biometric user-cancel announced as failure; (F9) overlayIsUp third hand-copy of pinned condition; + doc/copy/format-key cheap items. Survived attack: no duress oracle, no token drift, walls clean, T0-6 wiring correct, T0-4 pacer-only deviation endorsed. Recorded no-action: launch-screen dark flash when app pinned Light w/ OS Dark (inherent); .updatesFrequently semantics → manual VoiceOver pass list. FIX PASS DONE (14 files + LockGateAccessibilityBoundaryTests): shared fernletLockPadPage() on 4 hosts + type-cap on the safeAreaInset host, AX5 rendered 11/11 keys on all 5; focus via onChange; F3 closed by FREEZING fields post-caveat (deliberate deviation — re-arming Save would DUPLICATE the sealed entry since logEvent mints a new externalUUID per call); ScreenChanged on rising edge; .isModal probe-verified (15/15 overlay leaves); user-cancel LAErrors filtered from announcements; guards deleted (density 0.681 holds); parameterized announce keys. Unbypassed UI probe judged infeasible (no hook mints real credentials) → grep-wall + discovery test instead. Closure verified by original reviewer: ALL CLOSED (F3 freeze deviation CONFIRMED correct — logEvent/insert are pure appends, re-arming Save would duplicate; reviewer withdrew its prescription); F18 partial → micro-fix in flight (per-file pad/wrapper count pin — a 6th pad inside an existing file slipped the file-level wall). Carried to A2: ChipButtonStyle must read \.isEnabled (frozen chips render full-contrast inert — NEW-1); unbypassed wizard-PIN UI probe IS feasible via pure taps (reviewer demonstrated the path) → later batch's UI-test sweep. Manual list += keyboard-avoidance pass on wrapped alphanumeric passcode steps (NEW-2). |

## Deferred / blocked

- Track B items 14–17: out of scope this round (per round prompt §1).
- A: T1-4's FoodView.swift site, T2-4, T2-14 → reconciliation batch (FoodView ownership rule).
- **Uncatalogued display copy in non-UI modules** (A4 review F8): FernletScoring (~24 literals, e.g.
  "Built around …"), HealthKitGateway (~10 incl. user-visible statusMessage), PrivateHealthStore
  (flowLabel — feeds the cycle-calendar key via the caller). Same silent class §4.0 fixed in the UI
  modules; bare String returns from switches, invisible to rules E/F. A named post-round item.
- **A4 copy-volume classes, named**: MilestonesView.headline (24 drawn+spoken sentences); ~20
  spliced-lowercase prose sites; .capitalized-on-rawValue class (PeriodTrackerStore ×4 et al.,
  T2-18 neighbourhood); Text(signal.value.capitalized) derived-signal renders.

## OWNER DECISIONS — 2026-08-23 (these supersede the open-questions list below)

1. **Option 1.7(b) score-first ranking: AUTHORIZED.** Implement as a measured increment (after
   item 13), gated on the corpus + review battery; it is the only path to the mozzarella-class
   fixes. §30 row 17's sign-off requirement is now satisfied.
2. **`confidentBindScore`: RE-DERIVE the value** against the floored search path — as a measured
   increment AFTER 1.7(b) lands (1.7b re-ranks and would shift scores again; calibrate once).
3. **§37 Q7 (brand-token boundaries): stays TBD.** Owner guidance recorded: "chipotle chicken
   taco" is genuinely ambiguous both ways (pepper vs restaurant) — the answer may come from other
   parts of the natural language entered, i.e. context disambiguation, not the lexicon alone.
   Future design direction, not this round.
4. **Item 13's regenerated `FoodCatalog.sqlite`: NEVER COMMITTED — stays local due to size.** No
   LFS, no blob. Item 13 commits code + generator only; tests stay green against the COMMITTED
   catalog, the regenerated binary is verified locally with its expected vintage documented.
   CONSEQUENCE (flagged to owner): fresh clones/CI/archive builds ship the committed catalog;
   placing the new binary is a deliberate owner release step. Distribution mechanism (e.g. the
   branded catalog's ODR pattern) = future decision.
5. **§37 Q2 (curated file): REVISIT.** New task for the next session: search the old session
   transcripts (~a month back) that generated `CuratedSurveyFoodItems.json` and the food catalogs,
   to recover the curation criteria before deciding.
6. **§37 Q6 (provenance token): defer until item 13 reports** whether reuse suffices. ✔ as recommended.
7. **§37 Q5 (web-lane consent prompt): yes, future round**, framed as explicit privacy consent. ✔
8. **Ingredient schema optionality: future round** with the freeze/park decode-compat pattern. ✔
9. **Portions: future data-design item.** Owner context recorded: the real driver is natural
   language → portions ("4 pieces of salmon nigiri" — the app struggled to turn that into a recipe
   and convert 4 pieces to the right number of servings). Design the portion-count field around
   that conversion, not just the sashimi default.
10. **§37 Q9 (Syndigo/Nutritionix email): CLOSED — no paid data, ever.** Owner ruling
    (2026-08-23): Fernlet uses only publicly available data. No vendor email, no commercial
    licensing questions, permanently. This is a STANDING DATA-SOURCING PRINCIPLE, not a one-off:
    the research memo's entire paid tier (Nutritionix/Syndigo, NCCDB, any commercial option) is
    off the table; adopted sources stay the free/public/redistributable set (USDA FNDDS, BLS,
    Ciqual, Canadian Nutrient File — all already the plan).
11. **Chip 44pt growth (T1-9 second half): ACCEPTED.** Small A-track increment after A5 commits.
    QuickMoodRow's 31→33pt unification stands (not vetoed).
12. **`.medium`/needsReview semantics: keep as-implemented** (non-high template → .low). CLOSED.
13. **`[foodMatch, recipe]` merged stamp: leave as-is.** CLOSED.
14. **CLAUDE.md accessibility-wall entry: NO.** The wall stays enforced by scanner + tests without
    a CLAUDE.md paragraph. CLOSED.

## Open owner questions (historical — superseded by the decisions above where they conflict)

- §37 Q2 (replace CuratedSurveyFoodItems.json with FNDDS) — FNDDS lands additive in item 13; no delete without owner.
- §37 Q5 (web-lane consent prompt) — out of scope (item 15).
- §37 Q6 (FoodItemSource provenance case) — frozen-token decision; do not mint unilaterally.
- §37 Q7 (queryContainsBrandToken token boundaries).
- §37 Q9 (Syndigo/Nutritionix email) — owner action, not ours.
- **NEW (item 2): `.medium` semantics.** `MealResolutionConfidence.needsReview` is `self == .low`, so a
  `.medium` template resolution would still auto-commit (the exact bug class). Implementer mapped all
  non-high template results to `.low` rather than change the shared property, because
  `needsReview = (self != .high)` would also re-route the AI decompose tier's `.medium` results — the
  one tier the research calls SAFER. Recommendation: keep the mapping as implemented; revisit
  needsReview semantics only as a deliberate cross-tier decision, not inside this round.
- **NEW (items 5+6+7 fix pass): `confidentBindScore = 250` needs recalibration — measured, material.**
  The floor removes bad rows, which removes the low score that used to flag the survivor as weak:
  confident top-1s went 28 → 38 of 57, and SIX of the ten newly-confident are WRONG top-1s now
  presenting as confident. The constant was calibrated against a floorless search path. Pinned in
  `confidentBindPopulationIsPinnedForCalibration`; deliberately NOT retuned — the right value is a
  measurement-driven owner decision (options: raise the constant, or make it phrase-quality-aware).
- **NEW (items 5+6+7): §31's mozzarella/cheese-pizza promise NEEDS OPTION 1.7(b) — the argument, recorded per the round prompt §4.** Measured: under option (a), `Mozzarella Cheese` scores 1870 but sits two data-type tiers below survey rows; NO reordering that leaves the comparator alone can surface it — top-1 merely moved from one fried-stick row (58) to another (369). §31 attributes the fix to "1.7a, 1.8" but §9's own counterfactual describes data-type-as-tiebreak, i.e. option (b). Same cap applies to `cheese pizza`, `broccoli`, `chickpeas`, and the `burger and fries` chili-fries bind. Pinned in-test as unreachable-under-(a). The 1.7b decision (re-ranks every search surface) remains yours; the coupled unit's 70%→56% was achieved without it.
- **NEW (A2): T1-9's chip-density change needs design sign-off.** 44pt targets on ChipButtonStyle's
  36 call sites cannot be achieved inside makeBody without growing the layout box — chip rows spread
  ~10pt, a visible density change (report §3 T1-9). Recommendation: accept it — the chips are the
  app's selection vocabulary (food, mood, workout filters, period flow, intimacy) and sub-44pt
  targets there hit AssistiveTouch/Switch Control/tremor users hardest; the tab-bar half (unambiguous)
  is already in A2. If approved, it is a small follow-on increment; a before/after screenshot can be
  produced on request via the simulator.
- **NEW (item 2): `[foodMatch, recipe]` merged-stamp order dependency.** Folding a provenance stamp
  against an estimate grade is undefined by `MealConfidence`'s own semantics (it is a provenance enum,
  not a confidence ladder). Pre-existing behavior (parts[0]) deliberately left untouched and pinned in
  a test. Settling it is an owner semantics call: what should a merged meal built from one recipe part
  and one catalog-match part display as its provenance? Recommendation: leave as-is until a real user
  complaint; the interactive path restamps `.reviewed` anyway.
- **NEW (item 2): interim always-review state.** Until item 3 (template data audit) lands, 27 of 29
  templates route to review because the template DATA is bad (46/92 components bind to the wrong
  food). This is honest but a UX regression if shipped alone — items 2 and 3 should reach main
  together, which the branch structure already guarantees.

## §6 probe verdicts (2026-08-22, empirical, via in-app UIAccessibility walk — NOT XCUITest tree, which over-reports)

- **Q1 HealthBar (HomeView `HealthBar`): NOT a bug** — `.accessibilityLabel` on the HStack of shapes
  forces element creation; "Care score 90 percent" is a real focusable element. Item closed. Polish
  residuals only: no trait/value (state should be `.accessibilityValue`), 8pt-tall focus frame.
- **Q2 Hygiene card: REAL, upgrade** — outer `Button` + `LazyVGrid` destroys custom-action promotion:
  ONE element for the whole card, `customActions=[]`, all 8 toggles 100% unreachable. Controlled matrix
  shows a non-lazy layout (e.g. the `FlowLayout` `QuickMoodRow` uses) restores promotion (cheap fix);
  thorough fix = `.contain` + explicit open button. Card is not a default Home widget (priority input).
  → scheduled into A5.
- **Q3 SavedSpaceRow (WorkoutLocationSetupView): downgrade to polish** — Edit/Delete survive as
  VoiceOver custom actions on the row. Nothing unreachable. Residual: add a hint that actions exist.
  Do NOT apply the Q2 pattern here.
- **Q4 general rule: parent `.accessibilityLabel` OVERRIDES children under default/.combine/.ignore
  (and propagates onto every descendant element under default — duplicate reads); only `.contain`
  preserves child labels, and then the parent's label/hint lands on a non-focusable container (silently
  lost).** Companion on Home carries outer "Fernlet companion", no value, no traits; inner state label
  is dead code. T1-10 fix is unambiguous: delete inner label, move state to `.accessibilityValue` on
  the outer element, add `.isButton` trait + a custom action for the long-press edit path.
- Method note for later batches: **XCUITest's tree is not the VoiceOver element set** — it descends
  into collapsed subtrees. Verify grouping claims with a UIAccessibility-hierarchy walk
  (isAccessibilityElement == leaf), not `app.debugDescription` alone.

## Item 1 final state (committed c6cb9c2)

- All 6 review findings fixed. **Honest measured baseline at b259f3d: 13 zero-result, 27 wrong-top-1,
  17 defensible of 57 — 70% failure** (40/57 fail outright; 61% of returning queries wrong). The
  report's 46% was outcome-selection; its named 11/15 lists still reproduce exactly and stay pinned.
- Corpus rows carry a pre-registered `FoodSearchCorpusShape` (query-shape panel, not outcome-picked);
  5 queries were written down blind before first run; 2 of those are zero-result §34 never named
  (`grilled salmon fillet`, `quest protein bar`).
- **Resolver surface instrumented (30-entry bank) and divergent from search**: 6 queries return
  nothing in search but a full resolver pool (2 with the RIGHT food); `peanut butter` RESOLVES to
  "Egg omelet or scrambled egg, made with butter" (phrase splitter searches `butter` alone + dish
  demotion promotes it); `chicken burrito bowl` → "Ramen bowl, NFS"; `bowl of oatmeal` → branded
  sugary breakfast bowl. Strong direct evidence for items 2–7.
- `chickpeas` re-verdicted wrong-top-1 (dry row, 2.2× kcal); `chobani greek yogurt` → apricot row
  (silent flavour substitution) = wrong. 14 tests, green also under tr_TR; full suite 2703 green.
- Independently re-verified by orchestrator before commit (14/14, EXIT=0).

## Findings from item 1 worth keeping (not yet acted on)

- **The report's 33% wrong-top-1 rate is a floor.** The implementer probed ~100 extra everyday
  queries while reconstructing Appendix A's unlisted 31 "defensible" queries; roughly half had a
  wrong top-1 (`white rice`→gluten-free crackers, `whole milk`→ricotta, `peanut butter`→PB&J
  sandwich, `salmon`→breaded nuggets, `spaghetti`→spaghetti squash, `avocado`→sushi roll, etc. —
  classic data-type-priority failures). Decision pending review: add these as a clearly-labeled
  second bank beyond Appendix A, to strengthen the 1.6/1.7a/1.8 joint measurement.
- **Stale doc comment**: `FoodCatalogSchema.userVersion` claims the shipped file "is still v1
  content (no UPC data)"; it is v2 with 50,000 GTIN rows. Fix in a later B increment that touches
  shipping code (item 1 deliberately touched none).
- §9's named queries (mozzarella, protein bar, cheerios, chili(s), cheese pizza slice, black
  coffee, pizza dough crust) pinned in dedicated tests OUTSIDE the 57 to keep §34 arithmetic intact.
- Re-baselining after deliberate fixes: env-gated `TEST_RUNNER_FOOD_CORPUS_DUMP=1` dump prints
  corpus literals for paste-back.
- Log-reading note: this repo's suites emit Swift Testing `✔ Test run with N tests` lines, NOT
  `passed on 'Clone` — count executed tests from the ✔ summary, verify EXIT=0.

## Flags (not work items)

- (pending) Branded ODR catalog capability-vs-binary mismatch paragraph — to be recorded when Track B
  reaches the relevant increments.

## Manual verification owed (device/VoiceOver passes no agent can fully substitute)

- A1: real-VoiceOver pass on unlock (failure spoken? focus survives?), one full breathing session
  (does `.updatesFrequently` + value-change re-speak behave as the spec assumed? — its documented
  semantics are a polling hint), one cooking walk, worry-box release stopwatch with VO running.
- A1: AX4/AX5 check of all five numeric-pad hosts on the smallest supported device (iPhone 11 floor).
- A1: keyboard-avoidance behavior on the wrapped ALPHANUMERIC passcode steps (SecureField inside
  GeometryReader+ScrollView is known-fiddly; no defect found, but untested by any automated pass).
- A3: VoiceOver reading order on sheets after the SheetHeader sortPriority band (title before
  Cancel, all 16 sites) — XCUITest cannot model sorted traversal; needs a real VO pass.

## Reconciliation batch (grows during the round)

- ~~T1-4 @ FoodView.swift:2266~~ **RESOLVED by A2 + harness proof**: `CaptureAnalyzingOverlay` (shared
  component) got `.isModal`, and the A2 reviewer's synthetic harness proved `.isModal` on an
  overlay/ZStack-presented cover hides covered content unaided. One-minute verify of FoodView's
  presentation shape during reconcile; no edit expected.
- T2-4 MealRow custom content
- T2-14 search-result announcements (Food surfaces)
- **NEW (A3): FoodView `createdNotice` toast → FernletDismissalWindow adoption** (the 9th toast,
  ~:5989 at 886a352) and **FoodView's bare `ProgressView` (~:3317)** — both disclosed A3 gaps under
  the FoodView ownership rule.
- **NEW (chip-growth review F1): FoodView.swift:2527-2531 FOOD-08 doc comment** — states "type, slot,
  Save, without a drag"; the accepted chip growth puts 'Post-workout' at y=744 under the Save bar
  (measured), so the comment is now false. Fenced → fix the comment (or re-budget) at reconciliation.
- **NEW (A5 review M11): FoodView Smart Invert sites** — `Image(uiImage:)` without
  `accessibilityIgnoresInvertColors` at FoodView.swift:3103 (just-taken meal photo), :4665
  (MealPhotoThumb), :5093 (recipe photo) at the A5-review line numbers. The a11y branch adds a
  scanner rule + allowlists these 3 with the fence as reason; reconciliation removes the allowlist
  entries when fixing the sites.
- **NEW: FoodView moss→mossInk macro-row swaps** (`:1018/:1121/:3741/:4192/:4313/:4747/:5858` at
  b259f3d — locate by symbol): A2 inked BarcodeScanView's P/C macro tokens; FoodView's identical
  rows still raw moss (3.74:1). Same one-word swaps.
