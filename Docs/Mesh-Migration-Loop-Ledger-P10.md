# Mesh Migration Loop Ledger — P10

**Phase:** P10 (the companion `BGAppRefreshTask`), with P8's device gate and P9's MC cutover open · **Prompt:** [Next-Round-Prompt-Mesh-P10-2026-09-20.md](Next-Round-Prompt-Mesh-P10-2026-09-20.md)
**Started:** 2026-09-20 · **Closed:** 2026-09-21 · **Iteration:** 8 (notation `10.<item>.<tier>`; iteration 8 is items 8, 9, item 9's verify fixes and this close-out) · **Tree at seed:** `main` = `b9c02f7` (the P9 close-out); P9 BUILT at tier 1/1b/2, §15 NOT RUN, 9.4-LATER blocked
**Worktree:** `.claude/worktrees/admiring-moser-43ae1d` on `claude/practical-dijkstra-4b75c1`; main fast-forwarded after each iteration.

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 0 | The device gate (§15.1–§15.4, device plan A–F) + the two owed re-runs + P9's two device rows | 3 | — | blocked (owner) | | no devices in hand on 2026-09-20 (the only install is the owner's phone) — P10 is NOT gated by it |
| 1 | `FernletStoreAccess` move + the dead `install(store:)` | 1 | — | done | `437eaf3` + fix `f940026` | verify found 6 (inner `install(_:)` born callerless too — deleted; `load()` documented; unused SwiftUI import; FileIndex date + ExchangeIntentService row; F6 → item 2/4 note below). main ff'd to f940026 |
| 2 | The background-refresh import wall (§16.4) | 1 | — | done | `5688903` + fix `9e934ba` | wall = `BackgroundRefreshBoundaryTests` (s3-grep step, name pin 3→4): directory walk over `App/Fernlet/CompanionRefresh/`, import zero-list, **68** call-spelling needles (36 app-declared, each pinned as still declared), positive identifier exactly once, stripper table cell. Verify: F1 private funnels unreachable → 27 internal doors added; F2 interpolation stripped → kept. PERMITTED for item 4: `FernletStoreAccess.shared/.load`, `todayKey`, `companionState`, `companionThought`, `publishWidgetSnapshot()`, `widgetSnapshotMirror`, WidgetBridge/WidgetSnapshot/WidgetSnapshotMirror. main ff'd to 9e934ba |
| 3 | Identifier + plist (`fetch`) + the scheduling seam | 1 | 2 | done | `6698ac0` + fix `b3bf24a` | seam = `CompanionRefreshScheduling` / `CompanionRefreshTaskHandle` / `CompanionRefreshRequest` + `SystemCompanionRefreshScheduler` / `SystemCompanionRefreshTaskHandle`; `CompanionRefreshCoordinator.shared` (`registerAtLaunch()` in `FernletApp.init()`, `appDidEnterBackground()` on the `.background` edge outside the `.ready` guard, `taskWasDelivered(_:)`, `taskDidExpire()`, in-memory `pendingRequest` slot, `maxEdgeSubmissionsPerLaunch` 64 — edge only, tail exempt; submit-then-complete; refusals → `FernletAuditLog` `companionRefresh.*`). Verify found 7 (floor slid on every edge; lifetime cap; 4 pins that could not fail; DocC Topics). Wall now 83 needles (clock/persistence family added; `FileManager`, `UserDefaults`, `Timer`, `Task.detached`, `SecItem…` are needles). main ff'd to b3bf24a |
| 4 | The handler pipeline + the diff rule + exactly-once | 1 | 3 | done | `b0983b5` + fix `ec1bcda` | `CompanionRefreshPipeline` (values, step trace, outcomes incl. `.scoringContextUnavailable`, `.widgetActionsPending`, `.publishedDespitePendingActions`, `.writeFailed`) + `CompanionRefreshWiring` (production bindings: `FernletStoreAccess.shared.load()`, `refreshCurrentDayIfNeeded(now:)`, `companionState`, `currentWidgetSnapshot()`, `publishIfContentChanged`); `contentEquals` on the app's `WidgetSnapshot` copy in `WidgetBridge.swift` (6 content / 1 metadata, `Mirror` pin); late-attach `FernletStore.attachHealthKitServiceIfMissing` → `HealthSyncCoordinator` (Bool, refuses once the sync exists); coordinator holds the pipeline `Task` with an ML1 row. F1 took the FALLBACK (bridges need ContentView's `@State` PeriodTrackerStore + lock env, and the stress bridge carries a HealthKit fetch) → `hasCompleteScoringContext` gates the publish. Wall 84 needles / 37 app-declared. main ff'd to ec1bcda |
| 5 | P9 residuals: 83 ungated cells + the P9-touched suites left as honesty rows, five unscoped counts + ~43 unscoped `.count(of:)` reads, the restart branch, item 8's residuals | 1 | — | done | `bfb1fe4` + fix `288f501` | verify: 3 — F1 ratchet cell records a FALSE reason for `mesh.promotion.refusedExistingMesh` (fires only with a mesh held; most scopeable, left as "unscopeable"); F2 `heldMeshAuditContext` doc claims totality over no-mesh callers but misses `refuseKeyAdvertisementFrame` on the restore path; F3 no FileIndex/Verifiability row for the self-test script; + `heldBackSetFull == 1` moved onto CI unscoped. Measurement reproduced (1172/135). fixed: promotion-refusal + held-back-set doors got `held`, ratchet 20→17, helper doc lists five no-mesh callers, FileIndex/Verifiability rows. main ff'd to 288f501. Facts: (a) 17 names (14 + tier 3) → mesh-batteries MEASURED `1172 tests in 135 suites`, floor 1048→1172, name pin 118→135; (b) 7 doors `held`, 13 reads scoped + 9 re-spelled, ratchet 42→20 (two `== 1` named next); (c) `Scripts/run-gated-suites-selftest.sh` green + red ×2, on the enforcement self-test step; (d) nothing taken — CI's Simulator device is unpinned (the prerequisite under all three UI residuals) | measure the floor at the commit; `ProximityRecipeShareCapTests` is the one to check first |
| 6 | 9.4-LATER — the MC→QUIC cutover | 1 | owner | blocked (owner) | | D-4.1 hold / D-4.3 cut over; patches in Docs/Mesh-P9-Item4-Design-2026-09-20.md appendices |
| 7 | P9-3-A — the lock parks the 1:1 radios | 1 / 1b | owner | **answered 2026-09-22 — FIXED on the owner-calls launcher** | | product call; the owner chose *make it work*, and the fact was retired rather than re-projected (23 040 → 11 520 rows) |
| 8 | What a Simulator can show of a refresh | 2 / 1b | 3, 4 | done | `6cae3e8` | Lane E in the runbook (22 min of 90): registration ACCEPTED (`companionRefresh.registered` ×2); edge submission REFUSED 3/3 `BGTaskSchedulerErrorDomain Code=1` (P8's wall extends to `BGAppRefreshTask`, measured); `_simulateLaunchForTaskWithIdentifier:` REFUSED ("No task request … has been scheduled"), expiration likewise; iOS never delivered in a 10-min soak. `earliestBeginInterval` carried exactly (bonus row). `runFinished` has never been emitted on any machine. lldb traps: `-a true`, app must be FOREGROUND. FINDING: `FernletAuditLog` context is `privacy: .private` → on a device `submitRefused <private>` (error/trigger redacted) — owner installs the private-data profile before the device rows | timeboxed 90 min; record the verdict either way |
| 9 | P10 acceptance battery + CI lines | 1 | 1–5 | done | `790c92f` + fix `da3bac1` | 5 suites `MeshP10{SchedulingSeam,HandlerPipeline,DiffRule,ImportWall,Honesty}AcceptanceTests` / 26 cells (5/6/5/5/5); mesh-batteries MEASURED `1198 tests in 140 suites` — green at floor 1198 after the fix; name pin 135→140; battery pin 53→58; digests unmoved. The line's one red was a LATENT TEST DEFECT (`LocalizedStringKey ==` on an interpolated key is not a value comparison; flips with codegen) at `MeshRoutedHeartTests.swift:816` — fixed by `String(describing:)`; CI comment corrected; `permitted.count == 6` by name; diff-rule temp dirs cleaned (96 stale swept). Verify F2 (D-10.4.5's circular FileIndex home) → close-out. main ff'd to da3bac1 |
| 10 | Close-out: §17.2 BUILT, §15.5 added, §28 the handoff, the next launcher, memory | 1 | 0–9 | done | — see the close-out commits (this ledger's own commit in the worktree, plus the plan blob committed index-only in the primary) | draft → adversarial verify (57 corrections, applied in place) → apply. The drafts were re-based on `da3bac1` first: §17.2.3 finding 9 is DIAGNOSED and FIXED rather than environmental, and the mesh line's acceptance evidence is the GREEN `Test run with 1198 tests in 140 suites passed`. D-10.4.5 got a real plan home (§28.3) and the honesty suite's needle moved off `Docs/FileIndex.md` onto the plan, which is what item 9's verify F2 asked for; the FileIndex row lost its "tracked home" sentence in the same commit. New launcher `Docs/Next-Round-Prompt-Device-Round-2026-09-21.md` with its own FileIndex row — the `Next-Round-Prompt-Mesh-P*.md` glob is NOT widened, because widening it would also swallow the superseded 2026-08-20 prompt |

## Blocked on owner
- **P10's device rows (Lane E, by name):** (1) a cold background launch by iOS; (2) a refresh granted and launched on iOS's own schedule — everything after `taskWasDelivered`: the tail submission, every pipeline outcome, the WidgetKit reload, exactly-once completion; (3) the real conformer's expiration handler under a genuine budget; (4) the 15-min floor honoured; (5) Background App Refresh off in Settings; (6) Low Power Mode; plus the two audit events that need an accepted submission to exist (`edgeFoundARequestAlreadyPending`, `deliveryAbsorbed`). Prerequisite: the private-data logging profile, or the context stays `<private>`.
- Devices for item 0 (two minimum, three for partitions, four for the topology row); Lane D is the cheapest first run. Every §15 row and device-plan row A–F is NOT RUN as of 2026-09-20 (P9 ledger "Blocked on owner", read once at iteration 1).
- D-4.1 / D-4.3 (item 6) and D-4.4; P9-3-A (item 7).
- Carried from plan §26.4 / §27.4 (read them; do not re-list here).

### Disposition at close (every line above ends here)

| Blocked line | How it ends |
|---|---|
| **P10's own device rows** (Lane E's six phone-only rows + the two audit events that fall with them) | **CARRIED into plan §15.5** as eight named rows, each `NOT RUN — owner's device, 2026-09-21`, with the cost stated: the mechanism exists end to end and **no machine has ever emitted `companionRefresh.runFinished`**. Prerequisite recorded on every row — the private-data logging profile, or the audit context reads `<private>` on exactly the row where no debugger can attach (§17.2.3 finding 2) |
| **Devices for item 0** (§15.1–§15.4, the device plan's A–F, the two owed re-runs, P9's two rows) | **CARRIED unchanged** to plan §15 and to §28.2's tier 3, third phase running, with its cost: §15.3's soak still **decides the degraded ladder**, which stays unchosen. No row's status or date moved in P10 — §15's table is untouched and §15's status paragraph says so in its own sentence |
| **D-4.1 / D-4.3** (item 6, 9.4-LATER) and **D-4.4** | **CARRIED** to §28.3 with its default — **hold (D-4.1)**, unchanged for a second phase — and its cost: QUIC still has no first-meeting stranger admission, so a cutover ships a build where two phones that have never met cannot found a mesh. The patches have been ready since P9; the prerequisite is a design |
| **P9-3-A** (item 7) | **CARRIED** to §28.3, default unchanged from §27.3 (leave the policy alone, surface the reason). Cost: two shipped radios are dead for anyone with the app lock configured, and nothing tells them why. **TAKEN 2026-09-22: neither default — *make it work*.** The `!appLockEngaged` leg, the input fact and its projection are all retired; the policy imports no `FernletLock` |
| Plan §26.4 / §27.4's carried list | **CARRIED unchanged** into §28.4, which adds only P10's own three one-liners: the private-data profile, pinning CI's Simulator device, and `@Suite(.serialized)` on `AppIntentsTests` |
| The one red on the board at item 9 | **RESOLVED in `da3bac1`**, and it was not environmental: a latent `LocalizedStringKey ==` defect at `MeshRoutedHeartTests.swift:816` that codegen exposed when item 9 added files to the module. The mesh line is green at 1198 |

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| The first iteration | item 1; item 0 `blocked (owner)` — no devices | 2026-09-20 it.1 |
| Whether P10 may touch the mesh | no, and the wall lands in the first refresh commit (item 2) | 2026-09-20 it.1 |
| The scheduling seam | its own protocol + production conformer, copied from `MeshContinuationScheduling` | 2026-09-20 it.1 |
| "Reload only on change" | diff the meaningful fields, pinned as a table; one cell: `computedAt`-only difference does NOT reload | 2026-09-20 it.1 |
| P10's battery names | `MeshP10<Clause>AcceptanceTests`, gated on the mesh step in the declaring commit | 2026-09-20 it.1 |
| 9.4-LATER | hold (D-4.1) | 2026-09-20 it.1 |
| P9-3-A | surface the reason, no policy-row change (deferred to owner; item 7 stays blocked) | 2026-09-20 it.1 |
| New persisted surface | none | 2026-09-20 it.1 |
| **D-10.4.1 cold-launch store poisons the foreground** (`FernletStoreAccess` caches process-wide; background callers pass `healthKitService: nil`, the scene passes a real one — pre-existing via App Intents, amplified to every ~15 min by the refresh) | Fix in `FernletStoreAccess`, not the handler: `load(healthKitService:)` LATE-ATTACHES a non-nil service to a cached service-less store through a `FernletStore` seam (tier-1 tested). If a clean late-attach is infeasible, fallback = the handler does NOT build a store when the cache is empty (completes with a named `.noStore` outcome, re-submits; a suspended-not-terminated process still has its cache) and the finding is recorded with its cost. | 2026-09-20 it.3 |
| **D-10.4.2 a refresh between a widget tap and the next foreground republishes the app's lower count** | NO queue drain — §17.2 limits the handler to its listed steps. If undrained widget actions exist, the handler SKIPS the publish (named outcome), so the widget's optimistic state is never regressed. | 2026-09-20 it.3 |
| **D-10.4.4 ambient CloudKit mirroring** — the day roll's flush and ledger reconciles write through Core Data, whose production container mirrors to the user's private CloudKit DB on its own schedule | ACCEPTED as ambient: §17.2 forbids a CloudKit FORCE-sync, not persistence the store already owns; the handler asks for no sync. Recorded for the close-out's findings and the privacy copy check. | 2026-09-21 it.4 |
| **D-10.4.5 foreground after-hook still reloads unconditionally** (`WidgetSnapshotMirror.publish(_:)`; only the handler's `publishIfContentChanged` diffs) | DEFERRED to the close-out as a one-line follow-up decision; §17.2 scopes the diff to the refresh handler. | 2026-09-21 it.4 |
| **D-10.4.6 scoring bridges on a cold wake** (F1) | The handler NEVER publishes a lower-fidelity score. Preferred: attach the same period/stress bridges the foreground attaches, through a store-level door outside the walled directory (only if their construction is UI-free and respects the sensitive-feature fail-closed gate). Fallback: when either adjustment is ENABLED and its bridge is absent, skip the publish with a named outcome; when both are off the identity IS the app's number, publish. | 2026-09-21 it.4 |
| **D-10.4.7 mirror installed by a background-only process** (F6) | ACCEPTED: a later background save (a Siri water intent) reloading the widget is the foreground's behaviour and arguably the pre-existing gap; documented in the wiring and the close-out. | 2026-09-21 it.4 |
| **D-10.4.8 queue-skip across midnight** (F7) | The skip applies only while the mirror's previous `dateKey` is still today's; if the day advanced, publish regardless (a widget a tap behind beats a blank one), named outcome. | 2026-09-21 it.4 |
| **D-10.4.3 acquire path** | (a) `FernletStoreAccess.shared.load()` with defaults — `healthKitService` defaults nil through `FernletStore.load` into a lazy coordinator, so construction touches no Health gateway; `.deviceLocked` refuses before building. The apply agent must trace `refreshCurrentDayIfNeeded(now:)` and the companion recompute for any Health/Cloud/mesh call the wall cannot see (they live inside the store). | 2026-09-20 it.3 |

## Surprises worth not re-deriving
- **10.9 verify (FIXED in `da3bac1`):** `LocalizedStringKey ==` is NOT a value comparison when the key is interpolated — two structurally identical keys can compare unequal depending on codegen of unrelated code in the module. Compare `String(describing:)`. A green test of that shape is green by accident.
- **10.9 verify — close-out MUST preserve these strings verbatim** (the P10 honesty suite needles them): plan — `**15.1 Radio matrix:**`, `**15.2 Partition walks:**`, `**15.3 Progress soak:**`, `**15.4 Wi-Fi Aware evaluation`, `blocked (owner)`, `9.4-LATER`, `P9-3-A`, `P10's own device row` (single occurrence at plan:5623 in §27.2 — do NOT rewrite §27.2 without keeping it); plan — `D-10.4.5` (the close-out moved this needle off `Docs/FileIndex.md` onto §28.3, where the decision now lives); runbook Lane E (lines ~2256–2373) — `companionRefresh.registered`, `BGTaskSchedulerErrorDomain`, `Code=1`, `has never been emitted on any machine`, and the eight row sentences verbatim.
- **10.8 lane:** `_simulateLaunchForTaskWithIdentifier:` needs a PENDING request, and the Simulator refuses every submission — so the SPI cannot drive a handler whose submission was refused. A Simulator proves registration and the request's shape, nothing after.
- **10.8 lane:** lldb on a Simulator process: `expr -a true …` or the call never returns; a backgrounded app is SIGSTOPped and the expression dies with it — attach in the foreground.
- **10.5 fix:** `-only-testing:FernletTests/MeshP9AcceptanceTests` ran ZERO tests and reported green — the file exists, the struct does not (the ratchet cell is in `MeshP9HonestyAcceptanceTests`). Struct, never file; the gate script exists for this.
- **10.5 verify:** a "why this stays process-wide" bullet is a claim about the EMITTER's guard — read the guard's branch, not its name; an emitter in the `else` of `currentMesh == nil` holds a mesh.
- **10.5 apply:** `spm-wall-selftest.sh` ran last → DerivedData holds NO `FernletTests.xctest`; the next agent must `build-for-testing` before any test run.
- **10.5 apply:** the launcher's item 5(d) claim that no workflow names `AuditRatchetBoundaryTests` is stale since P9 item 9 — the launcher doc is left as written; the close-out's next launcher must not carry it.
- **10.4 fix:** a scoring bridge can be un-attachable in the background for a structural reason (it lives in a view's `@State` and needs the lock environment) — then the honest handler REFUSES to publish rather than publishing the identity.
- **10.4 fix:** a cell that compares a `Date` across an ISO-8601 file round trip reds on precision alone — compare decoded whole values.
- **10.4 verify:** a "deterministic recompute" can be deterministic and still not be the APP's number — `score` reads bridges that only a view attaches. Trace every input of a recomputed value back to who attaches it.
- **10.4 verify:** the store's own day roll publishes and reloads; a diff taken AFTER the roll sees nothing. Capture `previous` before any step that can publish.
- **10.4 apply:** `App/FernletWidgets/WidgetSharedModels.swift` is NOT a member of the Fernlet target (pbxproj exception sets) — the app has its own copy in `WidgetBridge.swift`; a patch to the shared file would be dead code for the app.
- **10.4 apply:** a nil `healthKitService` was never "no gateway" — `WorkoutHealthKitSync` fell back to a SECOND `HealthKitService()` instance (`HealthSyncCoordinator.swift:64`). The late-attach seam `attachHealthKitServiceIfMissing` refuses once the sync exists; its `internal` getter had to become a wall needle (85 now).
- **10.4 apply:** `AppIntentsTests.pendingIntentSheetRoundTripsAndIsConsumedOnce` flaked once under load — pre-existing (11 non-serialized tests draining a shared `UserDefaults.standard` token); fix = `@Suite(.serialized)`; candidate for item 5(b) or the close-out list.
- **10.5 draft:** the launcher's item 5 row overstated the ungated set — six of seven P9-touched suites were gated by the 9.9 fix, and the a11y ratchet suite already sits on s3-grep. Re-parse the yml before believing a residual list.
- **10.3 fix:** with the pending-slot rule, 64 ACCEPTED edge submissions per launch is unreachable; only a refusal storm reaches the cap. A cap cell must be driven through the shape that can reach it.
- **10.3 verify:** `BGTaskScheduler.submit` REPLACES a pending same-identifier request — a submission on every background edge is a floor that slides with use. Submit on the edge only when nothing is known-pending (in-memory), and never draw the handler tail from an edge budget.
- **10.3 verify:** a cell that compares an app constant to itself (`registered == [CompanionRefresh.taskIdentifier]`) cannot see drift — pin literals in the test.
- **10.3 apply:** a seam that holds the launch handler's `self` WEAKLY makes `_ = Coordinator(...)` in a test a deallocated subject — two draft cells were green over a coordinator that never ran. Bind the subject.
- **10.3 apply:** submit-then-complete — after `setTaskCompleted` iOS may suspend in the same breath; the order is observable only AT the completion (fake handle `atCompletion` hook), counts alone cannot see it.
- **10.3 apply:** cell (a) of the BackgroundTasks home-set lives in `MeshContinuationTaskHostWallTests`, not `…TaskHostTests` — a selector on the latter never runs it.
- **10.2 verify:** a call-spelling wall must list the spellings another file CAN speak — check each needle's access level (`private` funnels are unreachable from the walled directory, so pinning them proves nothing; pin the `internal` doors that lead to them).
- **10.2 verify:** a stripper that blanks string literals must keep `\( … )` interpolation bodies, or a call inside a log string is invisible.
- **10.3 draft:** `MeshContinuationTaskHostTests:569` cell (a) asserts SET equality over the homes of `import BackgroundTasks` / `BGTaskScheduler` under App/ — a second BGTask seam reds it on arrival; the same commit must widen that set.
- **10.1 verify (F6):** `FernletStoreAccess.swift` imports `HealthKitGateway` and `load(healthKitService:…)` takes a HealthKit service — the item-2 wall walks only `App/Fernlet/CompanionRefresh/`, so the handler's acquire step must call `load()` without constructing a HealthKit service itself; item 4 must show how a cold-process acquire gets its `healthKitService` argument without the handler touching HealthKit (or refuse to build a store when the cache is empty — plan §17.2: "acquire the existing store safely").
- **10.1 verify:** the doc-coverage scanner matches type declarations only (`DECL_RE`); an undocumented METHOD is invisible to it. Document members by hand.
- **10.1 verify:** five of the ten app-target walls the launcher lists (`PersistedSurfaceWipe`, `PrivacyWipeCoverage`, `DeleteAllData`, `MemoryLifecycle`, `PrivacyPolicyParity`) sit on NO workflow step — a candidate for item 5 or 9.
- (seeded from the P10 launcher §7's lesson list — read it there; do not copy it here.) The ones that bite a refresh handler first: **diff the meaningful fields, not the `Equatable`**; **a grep wall strips whole-line comments only**; **a suite name that matches neither half of the selector predicate is protected by nothing**; **every P8/P9 adversarial verify found something real (17/17)**.

## Next item
**None — P10 is CLOSED, and with it the plan's phases.** The phase ends on `main` (**not pushed**)
with §17.2 BUILT at tier 1 and 1b, tier 2 **measured negative**, §15.5 added and NOT RUN, §28 written
as the device-round handoff, and every line of "Blocked on owner" dispositioned in the table above.

**Resume from `Docs/Next-Round-Prompt-Device-Round-2026-09-21.md`, in a FRESH session** — a `/loop`
does not compact between phases. It is deliberately **not** an eleventh phase: one page, two entry
conditions (phones in hand, or the owner's D-4.3), and a stop condition that says to write the
one-page "still blocked" note and stop if neither holds.
