# Mesh Migration Loop Ledger — the owner's calls, built

**Round:** not a phase — two build items the owner decided in the device round, plus the two product findings
salvaged from the superseded P7 branch. The launcher is
[Next-Round-Prompt-Owner-Calls-2026-09-22.md](Next-Round-Prompt-Owner-Calls-2026-09-22.md); the round it follows is
[Mesh-Migration-Loop-Ledger-Device-2026-09-22.md](Mesh-Migration-Loop-Ledger-Device-2026-09-22.md) (item 3, the five calls).
**Started:** 2026-09-22 12:54 EDT. **Tree at seed:** `main` = `3397cc2`; HEAD's CODE byte-identical to `d88062c`
(`git diff --stat d88062c..HEAD -- . ':(exclude)Docs'` empty — every commit since touched `Docs/` only).
**Worktree:** the primary checkout, `main` directly. The owner's held working copies were never staged or written:
`App/Fernlet/Localizable.xcstrings`, the migration plan, `Docs/FernletSpecificationV3.md`, `Docs/ImplementationPlan.md`,
`Docs/Mesh-Migration-Loop-Ledger-P6.md`, `Docs/Next-Round-Prompt-Mesh-P7-2026-09-12.md`, `Docs/RemainingWork-2026-08-20.md`,
the xcuserdata plist, and the untracked `Docs/RemainingWork-2026-09-14.md`. Plan edits land as index-only blobs.
**Session note:** the desktop app restarted twice mid-round; each restart killed the in-flight background verifier and one
test run, and nothing committed was lost.

## Entry condition — the soak
| Condition | Found | Consequence |
|---|---|---|
| The 6 h soak of 2026-09-22 read out | **Not run yet** — the device round scheduled it for the EVENING of 2026-09-22; this round started at 12:54. The device round's scratch (`soak/`, `readout.sh`) is not on disk: both of that session's scratchpads (`933b433c…`, `d49fdbba…`) are empty | Item 0 stays OWED. Items 1–3 were built anyway, deliberately: the soak runs on the build INSTALLED on the phone (`MBO.Fernlet 1.0 (1)`, installed by the device round, code = `d88062c`), and a tree change does not reach an installed app. **Do not install anything from `4d1fa0b` or later on the phone before the soak has run.** The soak's scripts must be rewritten from the runbook's *Lane B* row when it runs |

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`.

| # | Item | State | Commits | Notes |
|---|---|---|---|---|
| 0 | The soak's read-out | **blocked (owner) — not yet run** | — | Scheduled for the evening of 2026-09-22 on the phone's installed (`d88062c`-code) build; see the entry condition |
| 1 | P9-3-A — make it work | **done** | `4d1fa0b` · plan blob `ef3b511` | The app-lock FACT is retired, not re-projected: no leg in `presenceState`/`recipeShareState`, no `Input.appLockEngaged`, no projection, no feed, and `ProximityRunPolicy.swift` imports no `FernletLock`. Product 23 040 → **11 520** (3 × 5 × 4 × 3 × 2⁶), every derived count moved with it. Red once against the restored old table (the P7 clause failed on the count and `agrees`; the new cell failed five ways — the first draft's `inactiveIsForeground` was a harness artifact, corrected by the verify). The `ContentView` lock-state edge survives as the view's duress feed and listener reconciliation — NOT a gate re-entry pass, which the first draft claimed (an unchanged gate is a no-op push); view-edge count stays 7. Two funnel rows now pin `selectedTab = .personal`, because `allowNearbyRecipeShares` defaults to TRUE and the lock had been what kept a unit test from starting a real recipe listener |
| 2 | Option 1b's name deferral | **done — the first build LEAKED; fixed after the verify** | `e83ec82` · record `f8687d5` (plan blob inside) · verify fixes (next commit) | One send-side gate, `disclosedDisplayName` ("" until `confirmPeerIdentity()`), read by all FIVE coordinator send sites — the introduction alone was not enough: a pre-commit heartbeat's ack carried the name. The receive side ignores any name on an introduction (so an older peer is shown by fingerprint too); the name is adopted from the first verified post-commit envelope, once, from the verified key only. No new frame and no wire-shape change. Subscribers: the mesh roster and the recipe picker. The join screen shows `FingerprintText` until the dwell or the tap. Red once: 9 issues across the five cells. **Lane C OBSERVED on two Simulators** (runbook *Lane C — Option 1b*): each side's `peers=[…]` is the other's fingerprint at `awaitingProximityCommit` and its name at `connected` |
| 3a | The cold-start nag | **done — the first build did NOT stop it in the app; fixed after the verify** | `b277888` · verify fixes (next commit) | `MeshSessionContext.endingPresented` (additive, no schema bump — `localTermination`'s precedent), inside the sealed file so delete-all and a new session already cover it; the presentation answers `.nothing` for a marked ending; `acknowledgeSessionEndingPresented()` is the card's `.onAppear`. Neither launcher sketch as written: reaping loses the rejoin bar, and narrowing to a rejoin-bar hit silences EVERY ending at launch (removal, ceiling). The pin — two launches over one sealed `ownDeparture` context — reads ONE presentation and `rejoinRefusal == .ownDeparture`; red once it read `[.youLeft, .youLeft]` |
| 3b | The nameless, modeless context | **skipped (inert — taken with the resume-accept door)** | — | Nothing on main mints a descriptor from a restored context (`promoteToMesh` is the only mint, and no resume-ACCEPT door exists), so the missing name/mode can reach no surface yet. The v3 → v4 bump, its decode-compat cell and any wipe row belong to the commit that builds that door (P7 item 5's 1b half) |
| 3c | The branch's resume-card UI suite as a template | **not built (not asked)** | — | Commit `85e79ea` still resolves in the local object store (`git cat-file -t 85e79ea` → `commit`; `Tests/FernletUITests/ProximityResumeCardUITests.swift` and the `FERNLET_MESH_RESUME_PRESENTATION` hook readable via `git show`). Unreferenced objects go at the next `git gc` — the owner's call whether to pin them before then |
| 4a | `recordError(domain:)` typed domain | **skipped (blocked by a held file)** | — | Its display names are new localized strings, and the app's string catalog (`App/Fernlet/Localizable.xcstrings`) is one of the owner's held working copies — a sync would have to run in a clean worktree and land as an index-only blob. Left priced |
| 4b | `MeshLinkTable.links` bounded | **done** | `04b1d99` | TWO growth paths found, not one: `noteClosed(_:)` kept an idle record for every closed tunnel, including inbound tunnels from never-browsed peers (a pending key no `forget` ever reaches); and the cache eviction reaped counters but never the link map. Both legs lossless (an idle record = no record). Red once: 40 records, 96 instead of 32 |
| 4c | Wipe-effect value tests ×3 | **done** | `4d9820b` | `PrivacyWipeIdentityEffectTests`: RAM key gone, no keychain row a fresh instance can load, re-provision from the keychain. `ProximityRecipeShareManager` gained the `identity:` seam (a test on the default identity would wipe the test host's real one). Red once: exactly the two no-op doors failed |

## Verification
| Item | Verifier | Result |
|---|---|---|
| 1 + 2 | one blind Opus pass over `4d1fa0b`…`f8687d5`, own worktree and DerivedData | **Item 1: no BLOCKER** — counts, behaviour and the radio-start hazard all confirmed; 1 FIX (the lock edge is NOT a gate re-entry pass — corrected everywhere) + NOTEs (the red-once record's `inactiveIsForeground` was a harness artifact; stale 23 040 / signature text in FileIndex, two test headers, two store comments — all corrected). **Item 2: 1 BLOCKER** — the mesh manager's `sendEnvelopeCore` named this device on six broadcasts and the QR ceremony to UNCOMMITTED slots (fixed at that one door, per slot, payload names included); **5 FIX** — `SealedIntroductionTests` left red and on no CI line (fixed), the roster subscriber could append a refused stranger (now rename-only), the key check / subscribers / bounded adopted name / friend-mode gate untested (four cells added), the "every in-field peer is unaffected" claim false for older builds (corrected, cost recorded); NOTEs fixed: `send(_:)` undoing a mid-transfer adoption, audit rows persisting "" / "A friend", an early presence heart filed under the fingerprint (now the kept friend's name), the recipe picker's fingerprint flicker. Left as residuals: the QR sheet interpolates the fingerprint into prose (new copy needs the held catalog), a one-sided commit keeps a friend under the fingerprint (inherent: the peer never disclosed) |
| 3 | one blind Opus pass over `b277888` (own worktree, own `iPhone 17e`, three real cold starts with a scratch launch hook) | **1 BLOCKER** — the acknowledgement never wrote the mark on a real launch: opening Friends runs `startJoin()`, which nils `restoredSessionContext` before the card's `.onAppear`, and the guard keyed on it returned early; the card showed on all three cold starts while every unit pin stayed green (both "launches" ran on one manager, no `startJoin()` between). Fixed: keyed on `lastSessionRestoreOutcome` (the value the card is drawn from), the early return audited — validated end to end by the verifier before it was written. **3 FIX** — the pin's bar half proved nothing (now a FRESH manager per cold start, bar re-derived from the file each time); the view's `.onAppear` was unpinned (now in `SessionResumeCopyTests`); the "one writer" rule was false in three docs (restated with the narrow exception). NOTE taken: a newly written ending clears a stale mark. **3b's skip confirmed.** NOTE for the owner, pre-existing: `startJoin()` also clears `offersForegroundResume`, so P7 item 5's resume-OFFER card is probably gone before its only surface renders — a future resume-accept door inherits that |

| fixes + 4b + 4c + CI | one blind Opus RE-verify of `04b1d99`, `4d9820b`, `1cbf5e3`, `a60b940`, `65c9432`, `c97474d` (a separate `git clone` under the home directory, its own Simulator, the primary untouched during another session's merge) | **No BLOCKER.** Re-measured the mesh line itself: 1222 tests in 141 suites, 0 failed. 10 mutations, 6 caught. **3 FIX, all fixed on `31fafd6`** (after the reconnect session's merge): (1) a vote from a peer that has not committed this device arrived with both names blanked and read " asked to remove ." with a live Second button — now filled on receipt from the descriptor, the roster, or the fingerprint; (2) a blanked admission request made the admitter prompt "A friend wants to join" and write "A friend" into the descriptor, which spread — the join request now goes to COMMITTED slots only (every trigger holds one; the harness door already documented that contract); (3) two claimed pins did not exist — the subscriber's rename-only WIRING (a source-needle cell now) and the door keyed on the SLOT not the `fingerprint:` argument (a QR-ceremony cell now; that mutation had survived 1 416 tests). NOTEs fixed: the subscriber comment's participant-list overclaim; `MeshLinkTable`'s "lossless" made exact (`noteDialFailed` tells absent from idle, and no path reaches it after either removal). Red once: four mutations, each caught by its own new cell |

## Residuals and owner decisions (after all three verifies)
- **Joining before a name is known — a protocol decision, not taken.** The requester now asks committed slots only, which removes every blanked request. Whether the descriptor should ever hold a member whose name the admitter never learned (and fill it later, monotonically) is left to the owner; today it cannot arise from this build.
- **The recipe radio names this device to anyone in range, by design** — its Bonjour TXT carries the display name for the picker (Home/Food/Move tabs), and with item 1 it now runs for people who have a lock configured (it was parked for them before). "A bystander learns no name" is true of the MESH radio only.
- **Pre-existing, found by the verifies, not changed here:** presence hearts auto-commit before the friend check and the sealed-introduction check binds only the key-agreement key, so a tag-replaying forger who knows both public KA keys receives a named heartbeat; the descriptor gossips every member's name to any committed peer before admission; `startJoin()` clears `offersForegroundResume`, so P7 item 5's resume-OFFER card likely never renders; a contrived single-slot `rejoinBar` path can let a stale launch card mark a later ending shown; a failed-then-succeeded expiry write-back shows that expiry twice.
- **Unpinned but correct (NOTE):** `ProximityCoordinator.send(_:)`'s restore, the audit-name helper, the recipe picker's advertised label.
- **CI:** `ProximityCoordinatorTests`, `SealedIntroductionTests`, `HeartShareTests`, `MeshHostPinTests`, `MeshSessionResumePresentationTests`, `PrivacyWipeIdentityEffectTests` and the reconnect session's `MeshReturningMemberReseatTests` ride no CI line; several use fixed sleeps, so gating them wants a de-flake first.
- **A flaky cell ON the mesh line:** `MeshKeyAdvertisementDeliveryTests.twoDoorsFiringForOnePeerInOneTurnWriteOneFrame` failed under load in this round's first measurement and twice in a row on one build in the re-verify's clone, then passed on rebuilds — its outcome depends on which of two concurrent tasks runs first (its own comment says so). A CI red from it is not a regression.
- **Test infrastructure:** a worktree under `/tmp` breaks `RepoRoot`-relative path stripping (the `/private` symlink) — `MeshKeyAgreementFoldTests` and `LocalizationBoundaryTests`/`PersistedSurfaceWipeBoundaryTests` cells go red there only. Measure in a clone under the home directory.
- **4a** stays priced (its strings need the owner's held catalog). The QR sheet still interpolates the fingerprint into prose (same reason).

## Gates, as read
- Item 1: 51 tests in 6 suites green (both run-policy suites, the funnel, both P7 clauses, P7 honesty); power-of-10 0 violations; doc coverage 0.
- Item 2: `ProximityCoordinatorTests` 41/41; 294 tests in 16 neighbouring suites; the four boundary walls green but for the held-xcstrings red below; `Scripts/spm-wall-check.sh` WALL CHECK PASSED (warnings are errors).
- Item 3: 66 tests in 9 session/restore/store/wipe suites; with five boundary walls 166/167 across 14 of 14 named suites.
- No full-suite run (the owner's standing rule).

## Pre-existing reds in the primary checkout — NOT caused by this round
1. `LocalizationBoundaryTests.countBearingKeysCarryPluralVariations` — the held `App/Fernlet/Localizable.xcstrings` lost the
   `%lld friends connected` plural variation (its comment is `isCommentAutoGenerated`). HEAD's committed catalog has it.
2. The plan-needle cells of `MeshP9HonestyAcceptanceTests` and `MeshP10HonestyAcceptanceTests` — the held plan on disk is a
   4 535-line older copy (HEAD: 6 478 lines) that contains no `P9-3-A`, no §15/§27 sections.
Both come from the owner's held working copies; a verification in a clean worktree reads the committed files.

## Concurrent work seen
`claude/mesh-reconnect-fixes-2026-09-22` (`f210db6`, one commit, its own worktree) also edits
`FernletKit/Sources/ProximityKit/Mesh/MeshNetworkManager.swift` (+146) and the runbook. Items 2 and 3 touch the same file in
different functions; whichever merges second should expect a textual merge, not a semantic one. Its two booted Simulators
(`Fernlet Reconnect A/B`) were left alone.
