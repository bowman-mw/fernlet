# Mesh Migration Loop Ledger — P8

**Phase:** P8 (background continuation), preceded by the P7 gauntlet · **Prompt:** [Next-Round-Prompt-Mesh-P8-2026-09-18.md](Next-Round-Prompt-Mesh-P8-2026-09-18.md)
**Started:** 2026-09-18 · **Closed:** 2026-09-19 · **Iterations:** {{UNKNOWN: not tracked after item 1 — the header was left at 2 and never bumped}} · **Tree at seed:** `main` = the P7 close-out (fast-forwarded 2026-09-18, `292ea0e`); P7 UNBUILT until item 0

**Environment of this session:** a Mac with Xcode 26 and the iOS 26.5 SDK, the owner's primary checkout. Concurrent sessions hold `App/Fernlet/Localizable.xcstrings`, `xcuserdata/**` and several `Docs/*.md` uncommitted; every commit below is by explicit pathspec, and the catalog and plan edits land as index-only blobs (the held working copies are untouched).

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 0 | Build and run P7: the gauntlet, the red-onces, the floor and pin re-measured, the catalog sync, the UI suite, the full suite; §13 → BUILT | 1 + 1b | — | done | item 0 log | eight commits (`cb34622` `4b5a2d8` `32b2d5c` `a29197b` `90ba678` `45c8449` `b7606d2` `4b46f6b` + the flip); owed to the owner: the device re-run, the three eyeballs, two a11y ratchet baselines, the DayRecord assert family |
| 1 | P7 item 6 carried: gate the drain cells, measured | 1 | 0 | done | `c758bd9` | one warm bundle at `92f0b8e`, three runs: 316 / 153 s → with the drain suite **359** / 157 s (+43, inside the 157–160 s noise) → with the fourteen 606 / 168 s (+247 cells, +11 s). Floor 316 → 359 (measured); floor red-once refused at 316 (`only 316 test(s) ran but the floor is 359`, exit 1). The fourteen PRICED, not gated — the owner's call: gating all of them costs 11 s; the plan's "would roughly double the step" (§12.3 finding 14) is refuted. No static pin (behaviour suite, the locked-device precedent). Adversarial verify: SHIP, one comment mislabel fixed in-session (the fourteen are not "the eight largest": the 32-cell `MeshRoutedInventoryDeltaTests` sits outside the price) |
| 2 | P7 item 8 carried: tier 2 (backgrounding half; eligibility negative; ARM_AFTER rows) | 2 | 0 | done (timebox 74 / 90 min) | `1017b62` | (a) **PASS** — the pushed foreground leg observed falling and rising with the re-entry naming the leg (first observation on any lane); (b) NOT CROSSED and the documented recipe cannot reach the refusal it names — the fixture is an asymmetric vault (recipe corrected in the runbook); (c) NOT CROSSED — `FERNLET_MESH_ARM_AFTER` does not exist, not built; (d) NOT CROSSED — blocked by **L-4: the sim↔sim QUIC lane discovers nothing at HEAD on this Mac** (six runs, zero `[mesh-quic]`, zero `proximity.transport.quic` at debug), under attribution by a baseline-commit probe at `92f0b8e` (item 3 touched the QUIC browser; `en0` is in CGNAT). Also observed: `mesh.continuation.registered` with the concrete id on a radio |
| 3 | The stop-browsing-keep-links verb; the refused row executed; the token a zero-count wall | 1 | 0 | done | `9687a26` | `holdCommittedLinks()` (12 code lines) + `.holdLinks` + the transport pause/resume pair (protocol requirements; QUIC real, pause-aware republish) + `isAdmittingNewPeers` in all three doors + the re-entry row keyed on facts. Verify → FIX FIRST, nine applied: the give-up clock ran on behind a hold (reproduced: `isSessionLive → false` six minutes in); door 3's excuse admitted ANY slot (`hasSlot` = `slot(for:) != nil`); QUIC republish re-minted through a pause. Founding 29 → 35 cells; mesh-batteries executed 365 (floor raised); walls red once ×3. Tier 2 owes the QUIC hold on real radios — the row is in `Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` |
| 4 | `MeshContinuationCoordinator` as a pure value + the state table; progress arithmetic | 1 | 0 | done | `4296892` | 6 states × 8 events = 48 rows; oracle `completion != nil ⟺ (from == .running && next != .running)` over the whole product + a depth-4 sweep (28 080 walks); 19 cells / 2 suites; red-once on the oracle (3 cells) and on an unpinned row (the mirror reddened). Verify: FIX FIRST → fixed: `requested + taskCancelled → idle` (was `completed`, which absorbed the next commit and stranded the claim); doc corrections F2–F4, F6, F7. **Owed to the close-out sync:** the two keys (`Fernlet mesh`, `%lld friends connected`), the count key's `one`/`other` variations hand-authored in the catalog, THEN its `pluralRuledKeys` row in `LocalizationBoundaryTests` (the wall reads the committed catalog — a row now is red). Item 6 owes a per-session re-submission bound (`running + appForegrounded → requested` re-arms) and must end an ownerless delivered task at once (`taskCancelled`) |
| 5 | The two raises (coordinator only) + the disagreement cell | 1 | 3, 4 | done | `a0495b1` | two public one-line doors on the manager behind a `MeshContinuationRaising` seam; `MeshContinuationDriver` (session-state half; weak manager; no Task/timer/gate/setter/radio); the raise wall (kit 1+1 inside the doors, App 1+1 in the driver, `applySessionEvent(` 0 under App); the disagreement cell on a real founding (all four corners + the never-read-each-other pin); a 258-walk sweep. Verify → SHIP after F1: `reset()` mid-task left the session leg stuck and the handle uncompleted — fixed (goes through `taskDidEnd(.cancelled)`). §4's "two under App/, zero under kit" is met in substance, inverted in letter (the enum is internal). **Owed to item 10:** the six `MeshContinuation*` suites (`CoordinatorTests`, `ProgressTests`, `CardPresentationTests`, `DriverTests`, `RaiseWallTests`, `DisagreementTests`) are on no CI line |
| 6 | Task wiring: register, submit, expire, cancel, complete once; keeps the tunnel; feeds the store; anchor suppressed | 1 + 3 | 4, 5 | done (tier 1; tier 3 rows to item 9) | `4ca9179` | seam + system conformer copied from the probe; `MeshContinuationTaskHost` owns the driver; four pins moved (W8 spelling, `storeEdges` 6, P7 honesty, card occurrence); progress from `sessionContinuationReading` on the tick only. Verify → FIX FIRST: **the raise timing** (a CPT is delivered while the app is still foreground — `begin` now fires on the entry into running-while-dark; sweep 1463 walks) · the anchor ruling (keep the unconditional noop: the shipping coordinator attempted one unrenderable Live Activity per slot) · the poll-reported session end reaches the host · four refusal arms present the card. **Tier 3 (item 9):** registration accepted; a `.fail` submission granted; the launch and expiration handlers firing; the tunnel surviving the whole task; slow progress across the soaks; no proximity activity present; the Control-Centre peek re-submission (device row F12) |
| 7 | Refusal / expiry presentation on the Friends card slot; privacy sentence | 1 + 1b | 4 | done | `da51498` | pure table over 6 × 10 (23 present / 37 silent); `projectedAudit` keeps the ending across the session end; `completed` folds to past tense; `MeshContinuationSlotDecision` (live continuation wins, an ended one yields to a resume card); store projection with no setter; DEBUG hook + UI class (3); §17.3 sentence in all three policy copies, parity-pinned. Verify → FIX FIRST, applied (three rows unreachable; present-tense copy on an ended session; precedence; raw `contains`). **Six keys owed to the close-out sync:** `This session stays on screen` · `iOS didn't give Fernlet time in the background, so this session keeps going while Fernlet is open. Nothing you've kept is affected.` · `Background time ran out` · `iOS ended the background session` · `The session is carrying on now that Fernlet is open again. Nothing that was already saved was lost.` · `That session has since ended. Nothing that was already saved was lost.` (no plural rule) |
| 8 | Tier 3 part one: the device plan A–E | 3 | 0 | todo | | first observation of any P7 behaviour |
| 9 | Tier 3 part two: §15.1–§15.4 → the runbook's Lane B table | 3 | 6, 8 | todo | | the degraded ladder is decided here |
| 10 | P8 acceptance battery + CI lines | 1 | 3–7 | done | `c86e3a0` | six clause suites, 21 cells; the eight `MeshContinuation*` suites + the six batteries on the line (57 → 71); floor **365 → 469 measured**; pin 42 → 48 by declaration; two wall pins added; four red-onces. Verify → SHIP; four follow-ups applied (a third table decomposition, the group key asserted, the 23 cells literal, the verdict asserted) |
| 11 | Close-out: §14 BUILT, §15 filled, §26 handoff, next launcher, memory | 1 | 0–10 | done | `c699289` | draft → adversarial verify → apply from six scratch files; the verify returned 25 binding fixes and all 25 were applied. §14 BUILT (tier 1 + 1b; **tier 3 NOT RUN** — §15's new status table and the runbook's Lane B rows are every row NOT RUN, dated 2026-09-19); §14.1–§14.4 written with both placeholders filled from the one full-suite run (**5 055 / 513, EXIT=0, 910 s**) and item 2's result; §12.3 finding 14 corrected by measurement (+92 % cells, +10 % time — not "roughly double"); §13.3 finding 11 marked resolved at `c758bd9`; §14's "fresh authenticated heartbeats" corrected to branch presence; the degraded ladder stated as undecided; finding **L-4** written up as §14.3 finding 19 (verdict in the owner table below); §26 is the P9/P10 handoff in §24/§25's format; `Docs/Next-Round-Prompt-Mesh-P9-2026-09-19.md` written with every anchor re-grepped at HEAD; the eight catalog keys landed at `e9d3962` (index-only, the held working copy untouched); the runbook's Lane B rows dated; memory note rewritten as CLOSED |

## Item 0 log — every red is its own commit
| Step | Result | SHA | Note |
|---|---|---|---|
| Two scans | 0 violations / 0 undocumented, re-checked after every commit below | — | density 0.778 |
| `build-for-testing` at `292ea0e` | RED — five compile errors, one per build cycle (package → app → tests) | `cb34622` | `nonisolated extension` under `defaultIsolation(MainActor)`; `MeshSessionCeiling.ceilingSeconds` public for the poller; `Hashable` on both presentation enums; `Set<LocalizedStringKey>` (not `Hashable`); `@testable import FernletCrypto` for `DeviceBindingID.$testOverride` |
| Owner device finding (a): the asymmetric-commit founding outage | FIXED — `reannounceToNewbornPeerIfNeeded` on the foreign refusal, once per (sender, meshID) | `4b5a2d8` | red-once cell runs BOTH orderings; higher-first was always green |
| Owner device finding (b): the recipe listener's self-stop with no re-start | FIXED — listeners reconcile against `isListening`, not the last verdict; `PresenceManager` now self-stops on `didNotStart*` | `32b2d5c` | NOT the launcher's level-trigger: presence never self-stopped, and level-triggering re-pinned 13 cells; 13 cells untouched this way |
| Finding (c), new: photos to nobody were silent | FIXED — `photosKeptOnThisPhone` + `mesh.routedShare.skipped` + one sentence in the camera info sheet, never an alert per shot | `a29197b` | one new app string, owed to the catalog sync below |
| Owner retest: still no merge in either ordering | FIXED — finding (d): the yield's "provably empty" guard counted OTHER meshes' custody; now `holdsNoRoutedItem(mintedIn:)` | `90ba678` | reproduced sim↔sim with the audit stream: `droppedUncommittedSlot` → `yieldRefusedRoutedContent` (items=4) → (a)'s reannounce → the peer (items=0) yielded → members=2 |
| `ProximityRunPolicyTests` alone | green (in fix (b)'s batch) | — | |
| Per-item suites (items 1–7) | green in the fix batches: seams, funnel-adjacent, locked-device, founding, photo, drain, P3, P7 six, poll, resume, copy | — | 138 + 96 + 111 + 81 tests across the batches |
| Red-once item 7: one `MeshP7…` selector deleted from the workflow line | RED by name (`everyMeshAcceptanceBatteryIsGated`, 4 tests / 1 issue), GREEN on restore — no rebuild needed, the wall reads the workflow at test time | — | |
| Red-once item 3: `store.presenceManager.stop()` planted in `ContentView.handleTabChange`; then a second `meshNetworkManager.startJoin()` there | RED by verb and RED by name (`everyRadioVerbLivesInTheSeamsFile`, 17 tests / 1 issue each), GREEN on restore | — | the first attempt's edits never landed (an anchor that matched twice) and its green runs proved nothing — re-done with line-number edits and a `git diff --stat` printed before each run |
| Red-once item 2: a second `applyRoutedAccessGate(.closed, now: Date())` under `App/`; then the funnel's gate line removed from the core's body | RED by file name (`theGateHasExactlyOneWriterOutsideProximityKit`, 27 tests / 1 issue); RED on both halves (2 issues) with the line out of the body; GREEN on restore | — | the second negative deleted the line rather than relocating it (the relocation insert did not land), which reddens both halves — the brace-matched half is exercised either way |
| Red-once item 1: `routedGateForeground(for:)` flipped to `phase == .active` | RED on 5 cells (`anInactiveSceneIsAForegroundScene`, the mirror oracle, the listener rows…; 19 tests / 5 issues) after a rebuild, GREEN after the restore and a second rebuild; tree clean | — | `CIGateSelectorBoundaryTests` green (4 tests) against the raised floor |
| Mesh-batteries line as the workflow spells it; floor re-measured | `Scripts/run-gated-suites.sh mesh-batteries 300 <the 56-suite line>`: **316 ran, 0 failed, result=Passed** (the script's own bundle count), 160 s | floor commit below | expected 313 = 300 + 13; measured 316 = 313 + 2 (the founding suite's two new cells: the two-ordering asymmetric cell counts once, and the custody cell) + 1 not attributed per suite from the console log (a suite on the line grew by one since P6's measure) — write 316, the measured number, never the expectation |
| Catalog sync from HEAD's blob, index-only commit | DONE — 16 insertions: P7's 13 (counted against the P7 ledger's list) + (c)'s 3 (`1 photo…`, `%lld photos…`, `Film remaining: %lld. %@`); `--check` exit 0 | `45c8449` | synced in a clean worktree with its own DerivedData; the held working copy is untouched and still shows `M` |
| UI suite serially — first pass | 20 of 25 classes reached before the owner stopped it: 86 cases started, 57 passed, 14 failed, every failure a 30–59 s element wait (timeouts); 10 of the 14 ran while the strict wall build competed for the CPU | — | the 14 re-run alone on an idle machine — row below |
| UI suite — the 14 failed cases re-run alone, idle machine | 6 passed, 8 still fail: five accessibility-audit ratchets with findings not in the 2026-08-27 baselines (Nutrition targets: potentially inaccessible text; Progress photos: `Aug 28` / `Feeling stronger` clipped; Recent bites: two personal-care labels partially unsupported at Dynamic Type; Friends tab: the search field's placeholder clipped, unlabeled; Home tab: an element with no description) and three element waits (the forced Tier-2 cover over the log-period sheet; `Focus tag`; the meal composer's catalog staging) | — | the test Simulator is at content size `large` (the default) and `dark`; attribution per the 08-27 lesson = a baseline-commit probe, in flight against the P6 close-out `82fc4d7` in a second worktree |
| UI suite — attribution probe: the 8 cases on the P6 close-out build `82fc4d7` (second worktree, same Simulator) | all 8 fail there too. Three reproduce the SAME audit findings (Progress photos, Recent bites, Friends tab) — **pre-existing since before P7**, not this round's. The other five failed on the probe for a different reason: `Settings did not open`, `anchor 'screen.home' never appeared — screen failed to render` — the Simulator's app had stopped rendering | — | |
| UI suite — the remaining classes (trainer export ×2, workout location ×3) on the current build | all 5 `no Move tab` — the same non-rendering Simulator, after ~3 h of continuous runs and one memory event | — | environment, not code |
| UI suite — fresh Simulator (erased, rebooted), the 13 cases serially on the current build | **11 passed, 2 failed**: the Friends tab, Home tab, Nutrition targets, all three capture covers, `Focus tag`, the catalog staging, both trainer-export cases and all three workout-location cases pass; Progress photos (`Aug 28` clipped, not in baseline) and Recent bites (three baseline findings no longer reproduce) fail — both reproduced on the P6 close-out build `82fc4d7`, so they predate P7 | — | **owed, not this round's**: two accessibility-ratchet baselines to re-record (`UXScreenProbe.auditBaselines`, last re-recorded 2026-08-27); with the first pass's 57 passes, every other UI case in the 25 classes is green |
| Full suite, one invocation, vs 4 866 / 486 — FIRST run | 4 939 tests in 499 suites, 2 119 s, EXIT=65 with 3 issues, no restart; none a product defect: the wipe-path scan had no pin for `reapplyProximityRunPolicy` (P7 item 3's call, never run under a toolchain), and two founding counts were process-wide (`== N` over a global audit signal, D-6a.10) | `b7606d2` | pin added with its reason; both counts scoped by the line's `held` context to the rig's own mesh id; the refusal now carries `held`. Re-run whole at the end for the EXIT=0 number |
| Full suite, one invocation — second run, after every fix | **no usable total**: the log carries `Restarting after unexpected exit, crash, or test timeout` (the P6 rule: such a log has no total — 1 925 / 249 is the post-restart fragment). Three issues, none a product defect on re-run: `ConnectionInspectorTests.beginSessionCreatesLiveLog` hit its 120 s limit at 306 s (starvation — the P6 probe worktree was compiling beside it) and passed alone in 0.2 s (13 tests); `MeshKeyAdvertisementDeliveryTests.twoDoorsFiringForOnePeerInOneTurnWriteOneFrame` (a suspension-race probe) and `aParkedRowIsDroppedAfterThreeFailedWidenings` (`parkedReoffered` counted 4 not 3 — a process-wide audit count, the D-6a.10 shape) — re-run alone below | — | **the owner directed no further full-suite runs**; the first invocation's 4 939 / 499 stands as the measured size, and its three issues are fixed (`b7606d2`) |
| `MeshKeyAdvertisementDeliveryTests` alone, fresh Simulator | **26 tests green** in 2.2 s — both cells were load effects | — | the `parkedReoffered` count is a process-wide claim (D-6a.10 shape) — scope it like the founding counts; owed, not this round's |
| Strict wall build (`Scripts/spm-wall-check.sh`) | **WALL CHECK PASSED** at `4b46f6b` — `DIAGNOSE_MISSING_TARGET_DEPENDENCIES=YES_ERROR`, `SUPPRESS_WARNINGS=NO`, warnings as errors; run in the clean worktree fast-forwarded to `main` so it had its own DerivedData beside the UI suite | — | |
| Three simulator eyeballs | NOT done here — folded into the owner's device re-run of the four founding fixes | — | Friends tab entry/exit was observed incidentally on two Simulators (entry starts the search; leaving the tab drops the link and a five-minute search with no commit ends the session); Control Centre and delete-all-in-session are unobserved |
| Plan §13 → BUILT with measured numbers in §13.4 | DONE — heading, the history note, the measured block, and the §26 summary line | this commit | index-only blob from HEAD's plan (the working copy is held and still shows `M`) |

## The one full-suite invocation (everything built)
| Run | Result | Note |
|---|---|---|
| `FernletTests` whole, one invocation, iPhone 17, 2026-09-19 00:07, bundle at `c86e3a0` | **5 055 tests in 513 suites passed after 910 s, EXIT=0**, no `Restarting after unexpected exit`, 0 suites failed | vs P6's 4 866 / 486 and item 0's 4 939 / 499: +116 tests / +14 suites are P8's (items 3–7 and 10). The owner's rule held: this is the first and only full run since item 0's |

## Blocked on owner — every line, resolved / taken / written into plan §14.3

*Closed 2026-09-19. Plan §14.3 is the permanent home; this section is the audit that nothing was
lost between the two.*

| Ledger line | Ends as | Where |
|---|---|---|
| Devices in hand from item 8 on; the runbook's Lane B rows are the evidence table; a re-run on the fixed build is owed | **WRITTEN — open, and it is P8's acceptance** | §14.3 findings 1 and 2; plan §15's new status table; the runbook's Lane B rows (all NOT RUN on hardware, 2026-09-19); P9 launcher item 0 |
| `DayRecordRepository` + 18 siblings: `assertionFailure` in a `catch` traps DEBUG on an environmental I/O failure | **WRITTEN — open, scope is the owner's** | §14.3 finding 3; P9 launcher item 1, flagged "do before the phone drawer opens" |
| Carried from plan §25.4: hardware lanes (A report, B double-dial, AWDL, D cable-out); option (b); D-7.30; §18.2 copy; the legacy unsigned removal; transcript `sid`; census/duress; P6's 19 and P7's 13 sentences; §17.3 | **CARRIED — unchanged, except §17.3** | §14.3 finding 17; §26.4. **§17.3's sentence is DONE** (item 7: byte-identical in all three copies, parity-pinned) — only its effective date is open, as finding 7 |
| P6 §12.3's open findings 4–13, 15–17, 19, 20; P7 §13.3's residuals 4–9, 13–14 | **CARRIED — unchanged** | §14.3 finding 18. §13.3's findings 1, 2, 3 and 10 were closed by item 0; **11 is resolved at `c758bd9`** and marked so in §13.3; **12** is P8's own item 2 |
| Item 6 call (1): the 1:1 recipe-share / presence foreground anchors are as unrenderable as the mesh's — ship a widget or retire, a P9 call | **WRITTEN — open, a P9 call** | §14.3 finding 4; §26.1, §26.3 and §26.4; P9 launcher item 5, default **retire** |
| Item 6 call (2): `requiredResources` left at the probe's default | **WRITTEN — open** | §14.3 finding 5 |
| Item 6 call (3): the two R2 caps (8 submissions/session, 8 identifiers/process) unmeasured against the OS | **WRITTEN — open; the measurement is device row F12** | §14.3 finding 6; §15's tier-3 row table |
| Item 6 call (4): a legacy pairwise session with no founded mesh gets no continuation | **TAKEN as a default** (there is no mesh id to register against) | §14.2, "Where §14 was silent" |
| Item 7 call (1): the privacy policy's effective date is not bumped though the sentence changed | **WRITTEN — open; publishing is the owner's act** | §14.3 finding 7 |
| Item 7 call (2): the hook name sits in the `FERNLET_MESH_` family; `FERNLET_UI_TEST_MESH_CONTINUATION` is a three-place rename | **WRITTEN — open, cost none today** | §14.3 finding 8 |
| Item 7 call (3): no UI cell for the past-tense ended copy; a boolean `…_ENDED` hook is the fallback | **WRITTEN — open** | §14.3 finding 9 |
| Obligations handed to items 5/6 (in `MeshContinuationCardPresentation.swift`'s header): assign through `projectedAudit`, no `.cancelled` for a delivery the app could not serve, reset on the hard stop and on a new mesh start, compare cards never construct them | **RESOLVED — all four met** | item 5 (`a0495b1`: the projection assigned through `projectedAudit`; an ownerless delivery ends at `.idle` with no card; `reset()` goes through `taskDidEnd(.cancelled)`) and item 6 (`4ca9179`: reset on delete-all and on a new mesh start). Pinned by `MeshP8PresentationAcceptanceTests` and `MeshP8RaiseAndDisagreementAcceptanceTests` |
| Item 3 call (1): no endpoint memory for a FULL link drop behind a hold | **WRITTEN — open; §15.1's re-dial row measures it** | §14.3 finding 14 |
| Item 3 call (2): the re-invite retry's second guard has no tier-1 cell (a real `Task.sleep`) | **WRITTEN — open; the scheduling-site guard is the pinned half** | §14.3 finding 15 |
| Item 4 call: should the count subtitle be a dotted key (`mesh.continuation.subtitle`) rather than the sentence key `%lld friends connected`? "Decide before the close-out sync" | **WRITTEN — open; the sync took the SENTENCE key** (the reversible choice: changing it later is a source edit + a re-sync + a `pluralRuledKeys` rename) | §14.3 finding 16 |
| **Gate the fourteen?** Priced at 247 cells / 11 s; recommendation: gate them | **WRITTEN — open, the owner's one-line call** | §14.3 finding 10; P9 launcher item 6, default **yes** |
| **Latent, now on CI:** `MeshRoutedDrainTests.swift:673, 718, 752` (`== 1`) and `:792, 822, 853` (`== 0`) count a process-global audit capture unscoped to the rig | **WRITTEN — open; its own small item** | §14.3 finding 11; P9 launcher item 7. `MeshKeyAdvertisementDeliveryTests`' `parkedReoffered` is the same shape — finding 12 |
| Owed from item 0, not a "Blocked on owner" bullet but owed: two accessibility-ratchet baselines (`UXScreenProbe.auditBaselines`) | **WRITTEN — open; both reproduce on `82fc4d7`, so they predate P7** | §14.3 finding 13; P9 launcher item 8 |
| **New at item 2 — finding L-4:** the sim↔sim QUIC lane discovers nothing at HEAD on this Mac (six runs, zero `[mesh-quic]`), which is why rows (b)–(d) did not cross | **WRITTEN — open.** **Verdict:** attributed 2026-09-19 by a baseline-commit probe (four lane runs, same hour, same Simulators, same CGNAT network): NOT a P8 regression — the P6 close-out `82fc4d7` and P2's `596bcf8` discover, the pre-P8 tip `92f0b8e` and P8's tip fail identically; a P7 defect at `df0ce5b` (P7 item 3): the DEBUG matrix harness calls `startJoin()` on the Home tab and the store's first policy apply (previous nil, every radio an edge) resolves discovery `.stop` → `.stopJoin` → the QUIC listener is cancelled ~20 ms after creation, before Bonjour registers; the product path (entry via the Social tab) is unaffected; fix landed as the P7 fix commit `80934b7` — the harness selects the Social tab before `startJoin()`, with the pure-value cell `theMatrixHarnessSurvivesTheFirstRunPolicyVerdict` (no `.stopJoin` in the first verdict over the harness's facts) that would have reddened in `df0ce5b` itself, and a scan pinning every shipping `startJoin()` / `resumeSearchingForPartitionedMesh()` caller under `App/` to the seams file or the harness. | §14.3 finding 19; §26.2 (tier 2 is P9's acceptance lane, so the lane is repaired first); the runbook's Lane C section is the full record; P9 launcher item 0 |

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| The first iteration | item 0 whole, in-session | 2026-09-18 |
| Process deviation | the owner drove the session directly ("build all three in that order"; "do the catalog sync, the ledger lines and the rest of item 0"); no three-agent dispatch — the shape was kept as three read-only verifying agents for the diagnosis, then in-session implement + red-once + regression batch per fix | 2026-09-18 |
| Finding (b)'s design | reconcile against the radio's own `isListening`, not a level-triggered seam | 2026-09-18 |
| Finding (c)'s surface | a count, never an alert per shot; no offline queue (freeze-at-mint stays the design) | 2026-09-18 |
| Finding (d)'s guard | this mesh's minted items only; a parked record never counts; load-state fail-closed unchanged | 2026-09-18 |
| Item 1's shape | one implement agent (build → warm → measure without / with / with-the-fourteen → floor to the measured count → floor red-once), then an adversarial re-read; items 3 and 4 surveyed read-only in parallel (design briefs only, no build beside a timing run) | 2026-09-18 |
| Item 1's three comment clauses | applied in-session after the verify (a `#`-only edit; the selector parser skips `#` lines — re-simulated: `mesh-batteries 359 57 suites`, YAML loads) — process deviation, recorded | 2026-09-18 |
| Item 3's shape | one public verb + the transport pause/resume pair + one admitting flag; `stopJoin()`/`stopSearching()` unchanged; the inverse stays `resumeSearchingForPartitionedMesh()`; the re-entry row lives in the transition; drafted in scratch while item 4 held the build slot (a marker file released it) — never two builds on the shared DerivedData | 2026-09-18 |
| Item 4's events and states | the launcher's six events + `taskStarted` / `taskRefused`; five states + `idle`; exactly-once oracle `completion != nil ⟺ (from == .running && next != .running)`; impossible rows absorbed with a frozen token, never a trap; the value takes scalars (no ProximityKit symbol — the public widenings are items 5/6's); the subtitle count is branch presence (`externalPresentFingerprints`), passed in — §14's "fresh authenticated heartbeats" has no per-member representation, a close-out wording correction | 2026-09-18 |
| Full-suite runs | **OWNER (2026-09-18): no full-suite run until everything is built.** Per-item gates are the touched suites + the routed/wall subsets (§6); the one full invocation is item 10/11's, after the last P8 file compiles | 2026-09-18 |
| How the coordinator reaches the radios | (default: feeds the store; never a verb) | — |
| Who raises `.backgrounded` / `.foregrounded` | (default: the coordinator only) | — |
| The stop-browsing verb | (default: one new public verb; `stopJoin()` unchanged) | — |
| The progress unit | (default: elapsed toward the ceiling) | — |
| The poller under a task | (default: unchanged) | — |
| When the task is submitted | (default: first commit, `.fail`, concrete id) | — |
| What refusal / expiry presents | (default: the Friends card slot) | — |
| The degraded ladder | (decided by §15.3) | — |
| New persisted surface | (default: none) | — |

## Surprises worth not re-deriving
- **An unbuilt round produces four error classes first**: a `extension` under a MainActor-default module silently isolating its members (`nonisolated extension`); app-side reads of internal package symbols; `Set`/`Dictionary` over `LocalizedStringKey` (Equatable, not Hashable); `@TaskLocal` test overrides reached through a non-`@testable` import. One build cycle each, because errors stop per target.
- **Two Simulators, or a Simulator and a phone, simulate NI ranging at 34 cm** — the dwell never fires and the row shows "Move closer"; the Force button (Settings → Advanced → Connection log → Developer tools → "Proximity debug tools") is the only commit path on a Simulator. That toggle needed a DRAG on the switch under the simulator tool; taps missed.
- **The audit stream is readable on a Simulator**: `xcrun simctl spawn <udid> log stream --level info --predicate 'subsystem == "com.fernlet"'` — info level is required (`Logger.info`), and the `.private` context IS shown on a Simulator. The two decisive tokens for a "not connecting" report are `mesh.meshDescriptor.droppedUncommittedSlot` and `mesh.descriptor.yieldRefusedRoutedContent`.
- **Leaving the Friends tab drops the link, and a five-minute search with no commit ends the session** (`mesh.session.endedByDiscoveryTimeout`) even while a peer is visible but uncommitted.
- **`MeshRoutedCustodyFixtures.rig` uses PINNED past dates**: the founding's expiry sweep removes the seeded item, and a yield cell over it is green for nothing. Seed with live dates (`seedLiveForeignCustody`).
- **`Text + Text` is deprecated in iOS 26 and warnings are errors** — `Text("\(a). \(Text(b))")` interpolation instead.
- **The store uses `FileProtectionType.complete`** and nothing in the persistence layer consults protected-data availability; a debounced day save landing after the device locks throws, and 19 `assertionFailure`-in-`catch` sites turn that into a DEBUG crash.
- **A continued-processing task is delivered while the app is still in the foreground** — "at task start" is not "when the scene goes dark". A session-state raise that means "we are continuing in the background" must key on (task in hand ∧ scene dark), or it closes foreground-only ceremonies while the person is in the app.
- **The proximity Live Activity never rendered**: its attributes are kit-internal and `FernletWidgetsBundle` declares no configuration, so every `Activity.request` was doomed — including the 1:1 anchors still shipping.
- **View `.onChange` edges do not fire reliably in the background** — a session end the poller observes must reach its consumer from the poller, not from a view.
- **A `reset()` that clears state without walking the table strands every side effect the table owed**: the pending completion, the `.foregrounded` raise, the heart predicate — reset must go through the exit row first, then clear. The sweep that pins raises == entries / ends == exits is what catches it.
- **A protocol requirement declared in an app file is a second occurrence of a grep-wall's needle** — put the seam in the kit beside its conformer, or the wall reads two.
- **A terminal state erases its cause unless the projection rule keeps it**: item 4's move into `completed` carries its own token, so a presentation reading "the last token" never sees the refusal that preceded the session end — a card over (state, last audit) needs a projection rule (absorbed keeps, the completed move keeps, else record) and a cell that drives the real transition through it.
- **A hold is not a stop, and every clock that assumes browsing must pause with the radios**: the five-minute give-up clock re-armed on a link loss during a hold and would have torn the session down — a verb that keeps links must also keep the clocks that would end them. Grep every `arm…Clock` / timeout when adding a pause-shaped verb.
- **`hasSlot(for:)` means ANY slot** (`slot(for:) != nil`), not a committed one — the committed predicate is the new `hasCommittedSlot(for:)`; a door excuse on `hasSlot` admits the uncommitted.
- **Re-entry rows key on facts, not on `previous`**: a `previous == (run, stop)` test is pushed two verdicts back by a foreground on another tab (discovery `hold`), and the next Friends visit answers nothing.
- **A rename red-once can be satisfied by the rename itself** when the cell's needle literal is the renamed spelling — a pass-2 cell's needles must be independently reddenable (`return [.holdLinks]`, the executor call, the resume arm), and a rename is caught by the row cells failing to compile.
- **Gating cost is not proportional to cell count**: +92 % cells cost +10 % wall time on the mesh-batteries step (tier 1, one process, no radio). Price before deferring a gate.
- **Some suites carry no `@Suite` attribute** — plain structs found by their `@Test` members, so a grep for `@Suite` under-counts. Verified at the close-out: `MeshRoutedItemSealTests` (off the line) and, of the 71 on the mesh-batteries step, `MeshContinuationRaiseWallTests` and `MeshContinuationTaskHostWallTests` (both declared at `c86e3a0`). **`MeshKeyAgreementSchemaTests` is NOT one of them** — it carries `@Suite(.serialized)` (`Tests/FernletTests/MeshKeyAgreementAdvertisementTests.swift:859`) and did at `92f0b8e`; the earlier line here was wrong.
- **P6's "~243 across fourteen suites" itemisation lived in a scratch file that is gone**; the fourteen item 1 chose on a stated rule measure 247. Record what the bundle counted, never the remembered estimate.
- **Three launcher anchors drifted at `92f0b8e`**: `mayCommitRoutedHeartLedgerJudgement` is at `MeshNetworkManager.swift:9014` (not ~8964) with **two** shipping readers (`:7184`, `:7669`); `completeBackgroundTask` `:1400` is exact; the anchor suppression item 6 names may already be done (`MeshNetworkManager.swift:14267` injects `NoopProximityForegroundAnchor()`).
- **Nothing items 5/6 need is public**: `meshID` (`:14391`), `sessionState` (`:8971`), `applySessionEvent(` (`:9115`), `sessionCeiling` (`:9020`) are ProximityKit-internal; item 5 pass 1 cannot compile without a narrow public raise pair, pinned by name.
- **W8 pins the very line item 6 changes** (`funnel.contains("continuation: .notRequested")`, `MeshRoutedLockedDeviceTests.swift:~1195`) and `ProximityRunSeamsTests` pins `storeEdges == 5` (`:~334`) — a third setter makes 6; both move in item 6's commit, each red-once.
- **Item 3's hold is a one-way door unless the transition gets a re-entry row**: after the verb, `FriendsDiscoveryEntry.entry(true, true)` → `.none` (`FriendsDiscoveryEntry.swift:66`), so browsing never returns; `isSearching == false` closes no admission door (`mayLinkToDiscoveredPeers` is `isSessionOpen || currentMesh != nil`) — a new admitting flag is needed in all three doors; `NetworkMeshSession.start` guards `!isRunning`, so the QUIC conformer needs a `resumeDiscovery()` or tier-2 browsing stays dark after a pause (`MeshMultipeerSession` already ships `pauseDiscovery()`/`resumeDiscovery()`, `:238–256`).
- Carried from P7 (all still binding): `stopJoin()` tears the slots down; `.inactive` is foreground for the radios; the three store stop sites were not all teardown; a helper repeating a counted spelling reads one too many; persisting effects on the founding rig need the pinned install binding; the chat age gate is `AgeGate.chat`; `FernletLockState` carries associated values; every P6 lesson (a log with `Restarting after unexpected exit` has no usable total; never chain a build and a run on one DerivedData; `-only-testing:` names the SUITE — one file can hold four, and `Suite/cell` runs 0 tests under a green banner; zsh passes an unquoted `$ARGS` as one argument).
- Closed; do not re-audit: `MeshTunnelConvergence`, the id-vs-endpoint family, the crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10, §11.1–§11.4, §12.1–§12.4 and, as a record, §13.1–§13.4; `Docs/Proximity-Security-Followups-2026-08-18.md` §1.
- **Never round-trip an `.xcstrings` through a JSON encoder**: it reformats every entry and buries the signal (the tool writes empty objects across lines). Hand-author a plural block in the catalog's own byte style and re-run `Scripts/sync-string-catalogs.sh --check`, which keeps it.
- **The localization wall cannot run from a worktree under `/tmp`**: the scanners strip `RepoRoot.url.path`, which is `/tmp/…`, from paths the enumerator resolves as `/private/tmp/…`, so every allowlist row misses. Sync in a clean worktree, but run the wall in the primary checkout.
- Concurrent sessions share the tree; never stage the catalog working copy, `xcuserdata`, or the stray PDF.

## Next item
**none — P8 is closed.** Items 0–7, 10 and 11 are done, and item 2 is done at `1017b62` (timeboxed; one row of four crossed, the other three carried by name into P9). **Items 8 and 9 are NOT RUN and are the owner's devices** — they are P8's acceptance, they gate *shipping* background continuation and nothing else, and they are item 0 of the next round, together with the owner-side rows item 0 left owed (a device re-run of the four founding fixes with the three eyeballs; the two accessibility-ratchet baselines; the DayRecord assert-in-catch family, which will crash P8's lock/background device rows in DEBUG if left). The next launcher is `Docs/Next-Round-Prompt-Mesh-P9-2026-09-19.md`; its ledger is `Docs/Mesh-Migration-Loop-Ledger-P9.md`, created on its iteration 1. Plan §14 is BUILT (tier 1 + 1b), §15's table and the runbook's Lane B rows are NOT RUN on hardware dated 2026-09-19, and §26 is the P9/P10 handoff. The full-suite number of record is **5 055 tests in 513 suites, EXIT=0, 910 s** (iPhone 17, 2026-09-19 00:07, bundle at `c86e3a0`); the mesh-batteries line is **469 over 71 suites at floor 469** and `CIGateSelectorBoundaryTests`' pin is **48**. `main` is **not pushed**.
