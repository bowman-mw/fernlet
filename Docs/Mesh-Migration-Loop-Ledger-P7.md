# Mesh Migration Loop Ledger — P7

**Phase:** P7 (the app-layer run policy, the poller, the resume surface) · **Prompt:** [Next-Round-Prompt-Mesh-P7-2026-09-12.md](Next-Round-Prompt-Mesh-P7-2026-09-12.md)
**Started:** 2026-09-17 · **Iteration:** 7 (next; iterations 1–6 ran 2026-09-17/18) · **Tree at seed:** `main` = `82fc4d7` (the P6 ledger's close-out row; the close-out commit itself is `4bf7f02`); **`origin/main` = `82fc4d7` too — P6 IS pushed** (the launcher's "not pushed / `3a32be0`" line is stale). Work branch: `claude/zen-goodall-wejlxg`.

**Environment of this session (read before trusting any "green"):** a Linux container with **no Xcode and no Swift toolchain** (`swift: command not found`; the swift.org and GitHub toolchain downloads are refused by the proxy). The gauntlet's `xcodebuild` steps, the red-once discipline and the full-suite baseline **cannot run here**. What CAN run: `python3 Scripts/power-of-10-scan.py`, `python3 Scripts/doc-coverage-scan.py`, and grep-level checks. Every Swift commit this session makes is therefore **UNBUILT**; each commit message says so, and the "Owed to a Mac" list below is the exact command list the next Mac session (or the owner) runs before any item is called green.

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 1 | `ProximityRunPolicy` as a pure value + the matrix over the full input product | 1 | — | done (UNBUILT) | `b7e428a` | `App/Fernlet/ProximityRunPolicy.swift` (439 lines) + `Tests/FernletTests/ProximityRunPolicyTests.swift` (526 lines, 16 cells over 23 040 rows) + `FernletTab` made `nonisolated` + FileIndex/DocC rows. Vocabulary: `ProximityRunState` {run, foregroundOnly, **hold**, stop}, `ProximityContinuationState` {notRequested, running, refused, expired}, `ProximitySessionPresence` {absent, meshHeld, peerCommitted} with `folding(isInSession:hasCommittedPeer:)`; `ProximityRunPolicy.Input` (11 facts, `Hashable`), `.Verdict` (mesh / discovery / presence / recipeShare + `routedAccessGate`), `verdict(for:)`, `isForeground(_:)`, `isHardStop(_:)`. Scans: po10 504 files / 0 violations / density 0.780; doc coverage 0. **Nothing built or run** — see "Owed to a Mac". Review pass caught one wrong pinned count (1 920 → 480). |
| 2 | The policy is the single writer of `applyRoutedAccessGate` (6 sites → 1) | 1 | 1 | done (UNBUILT) | `22c5333` | Funnel = `FernletStore.applyProximityRunPolicy(scenePhase:protectedDataAvailable:appLockEngaged:duressSessionActive:now:)` (lock facts as PARAMETERS read at the edge, never the store's mirrors); `FernletApp.pushProximityRunPolicy(_:phase:protectedData:)` replaces the six pushes 1:1; `ContentView` mirrors `selectedTab` into the store; new `ProximityRunPolicy.appLockEngaged(_:)` / `belowMinimumAge(_:)` projections; store gains memory-only `proximityRunVerdict` + `deleteAllInProgress` (raised inside `deleteAllData`'s wipe bracket). Walls: W6 re-aimed (6 edges, scene file writes/assembles no gate), W7 (`routedGateForeground(for:` = 0 in FernletApp, = 1 in the policy), launch-mount order wall follows the helper, **NEW W8** (`applyRoutedAccessGate(` exactly once under `App/`, brace-matched inside the funnel). Tests: 2 projection cells + `ProximityRunPolicyFunnelTests` (2 cells, real store). Scans clean. **Nothing built or run.** |
| 3 | The radios get `apply(_:)` seams; ContentView + FernletStore stop calling them | 1 | 1, 2 | done (UNBUILT) | `df0ce5b` | Both passes in ONE commit (a two-writer transitional commit is worse than none while nothing builds). `App/Fernlet/ProximityRunSeams.swift`: `ProximityRunTransition` (pure: last verdict, new verdict, `MeshFacts{isSearching,isInSession,hasCommittedPeer}` → ordered `[ProximityRunAction]`, edge-triggered), executor on `FernletStore` (the ONE file speaking a radio verb), `PresenceManager.apply(_:)` / `ProximityRecipeShareManager.apply(_:)`. Funnel split: private core `runProximityPolicy(_:now:)` + three entries (scene / view `applyProximityRunPolicy(appLockEngaged:duressSessionActive:now:)` / store `reapplyProximityRunPolicy(now:)`; before any scene edge both assume the most restrictive scene). Setters + wipe bracket run the policy; `discoveryTimeoutTask` moved to the store. ContentView retired (8 members gone; 5 edges through one helper; new age-record observer; scene handler moves no radio). Walls: NEW `ProximityRunSeamsTests` (13 table cells + retirement wall, harness exempted BY NAME), W8 re-aimed to the core, wipe needle + doc row → `reapplyProximityRunPolicy`, ML1 invariant names the fifth task. The one unsupported transition (discovery `stop` ∧ mesh `run`) is REFUSED audibly, never executed. **Nothing built or run.** |
| 4 | The poller — one timer, three consumers (ceiling → idle lapse → partition) | 1 | 1, 3 | done (UNBUILT) | `8ad1476` | ProximityKit `Mesh/MeshSessionPoll.swift`: public `pollSession(now:)` (ceiling → idle lapse → partition, `.skipped` when not live, returns early when the ceiling ended the session) + `MeshSessionPollReport` + `armSessionCeilingFromAdoptedMeshIfNeeded(now:)` called at descriptor adoption AND the admission grant — **P6 §12.3 finding 3 closed** (the `MeshPairwiseFoundingTests` residual assertion flipped). App `ProximitySessionPoller.swift`: pure rule (timer iff `isSessionLive`), 30 s, `maxTicks` = ceiling/30 + 1 as the loop bound, ONE timer on the store synced by the core after the seams and by a new ContentView `isSessionLive` observer; self-stops on `sessionLiveAfter == false`. Tests: `MeshSessionPollTests` (3 rig cells) + `ProximitySessionPollerTests` (table + wall). Five doc sites updated. **Nothing built or run.** |
| 5 | The resume surface over `MeshSessionRestoreOutcome` | 1 + 1b | 1 | done (UNBUILT; 1b owed) | `6161004` | Both passes in one commit. ProximityKit `Mesh/MeshSessionResumePresentation.swift`: public `MeshSessionResumePresentation` {nothing, offerResume, previousSessionEnded(MeshSessionEndingPresentation), previousSessionCouldNotBeReopened} + pure total `presentation(outcome:offersForegroundResume:isInSession:)` + `MeshSessionEndingPresentation` {ended, expired, youLeft, youWereRemoved} folding the 8 frozen reasons + `MeshNetworkManager.sessionResumePresentation` (the one public read). App: `SessionResumeCopy` (cards, `LocalizedStringKey`), `FriendsView` album card (`friends.sessionResume` / `.dismiss`), sampled on appear, dismissable, gone when a session surface is up. Deferral/refusal SILENT; endings named as ended, never failed. Tests: `MeshSessionResumePresentationTests` (6 cells incl. all 14 outcomes) + `SessionResumeCopyTests` (3). **Catalog NOT synced (no Mac); 7 keys owed — see below.** UI suite (1b) owed. **Nothing built or run.** |
| 6 | Gate `MeshRoutedDrainTests` (43) and price P6's ~286 ungated cells | 1 | — | blocked | | needs a Mac to MEASURE the step time (the floor is measured, never inherited) |
| 7 | The P7 acceptance battery + CI gate lines, one commit | 1 | 1–6 | done (UNBUILT) | `402bd29` | `Tests/FernletTests/MeshP7AcceptanceTests.swift`: six serialized suites, one per clause, 13 cells — run policy (the full 23 040-row product re-run whole, §11.4), gate writer (a real `FernletStore` through lock / background / duress, the gate read back, W8 re-walked), radio seams (retirement wall re-walked + the edge table on shipping types), poller (each consumer to a verdict on `MeshFoundingRig`: ceiling reached on the yielder; `evictSlotForTesting` → partition → idle lapse at `idleLapseDeadline`; a dead session `.skipped`), resume (all 14 outcomes swept), honesty (digests keep their one home — spelled split here — and the battery names its own unbuilt state). `MeshP7Acceptance` = shared thin rig. Gated on the mesh-batteries line; `CIGateSelectorBoundaryTests` pin 36 → 42 by declaration count. **Floor LEFT at 300, not computed-raised to 313** (deviation from this ledger's own item 7 note): the workflow comment says the floor is measured, never inferred, and a lower bound cannot go vacuous by staying — the first Mac run re-measures and raises. Also carries the `MeshSessionPollTests` binding fix (polls under `DeviceBindingID.$testOverride`; the ceiling's termination mark persists). Item 6 (a prereq) is blocked, so the battery cites P6's rows rather than the drain suite. **Nothing built or run.** |
| 8 | Tier 2: the backgrounding half; the heart eligibility negative; P6's remaining rows behind `FERNLET_MESH_ARM_AFTER` | 2 | 3, 4 | blocked | | needs a Mac + simulators; timebox 2 iterations |
| 9 | Close-out: §13 IMPLEMENTED-UNBUILT (not BUILT), §25 P8 handoff, P8 launcher, device test plan, memory | 1 | 1–8 | done | close-out `9dcdc14` | Drafts written to scratch (`closeout-s13.md`, `closeout-s25.md`), every `file:line` anchor re-checked against `e8ffb44` by script (one UI claim corrected: the inspector is Settings → Advanced → **Connection log**, DEBUG only; the session ends from the session view's **End session**), then applied. Plan §13 re-headed **IMPLEMENTED, UNBUILT** with the SHA table, §13.1–§13.4 (§13.4 says outright that no test ran; evidence is by declaration — 65 new `@Test`s across seven files, five amended suites); §25 appended (25.1 inheritance with anchors, 25.2 lanes, 25.3 ten defaults, 25.4 owed, 25.5 lessons). `Docs/Next-Round-Prompt-Mesh-P8-2026-09-18.md` written from §25 — **item 0 is the P7 gauntlet**, items 1–2 carry P7's 6 and 8, items 3–11 are P8. `Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` written (the owner's ask): sections A–E are P7 on two–three devices, F is §15 as a checklist, results table blank. FileIndex: rows for the test plan, the plan, the launcher family, the ledger family. Items 6 and 8 recorded as blocked, not done. **No catalog sync (Mac-only); the 13 keys stay listed above.** |

## Owed to a Mac (the gauntlet this session could not run)
Run in this order at the current HEAD of `claude/zen-goodall-wejlxg`, per launcher §6; nothing this session committed is green until these are:
```bash
python3 Scripts/power-of-10-scan.py            # this session: 0 violations (re-check)
python3 Scripts/doc-coverage-scan.py           # this session: 0 undocumented (re-check)
xcodebuild build-for-testing -project App/Fernlet.xcodeproj -scheme Fernlet -destination 'platform=iOS Simulator,name=iPhone 17'
xcodebuild test-without-building -project App/Fernlet.xcodeproj -scheme Fernlet -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:FernletTests/ProximityRunPolicyTests
# then the per-item subset (launcher §6) and the FULL suite; baseline to beat: 4866 / 486, EXIT=0, one invocation
```
- Item 1's red-once: flip `ProximityRunPolicy`'s foreground fact to `phase == .active` (so `.inactive` reads as background) and confirm `ProximityRunPolicyTests` reddens; restore the exact text; rebuild.
- Item 2's suites: `-only-testing:FernletTests/ProximityRunPolicyFunnelTests` and `-only-testing:FernletTests/MeshRoutedLockedDeviceTests` (W6, W7, the launch-mount order wall, W8), plus the per-item subset. Item 2's red-once: add a second `store.meshNetworkManager.applyRoutedAccessGate(.closed, now: Date())` call anywhere under `App/` (e.g. in `ContentView.handleTabChange`) and confirm W8 reddens by file name; then move the funnel's `applyRoutedAccessGate(` line OUT of the funnel body and confirm the brace-matched half reddens; restore, rebuild.
- Item 3's suites: `-only-testing:FernletTests/ProximityRunSeamsTests`, `ProximityRunPolicyFunnelTests`, `MeshRoutedLockedDeviceTests`, `PrivacyWipeCoverageTests`, `PersistedSurfaceWipeBoundaryTests`, `MemoryLifecycleBoundaryTests`, `DeleteAllDataTests`, `DuressDecoyAndWipeTests`, `HeartDropAppWiringTests`, `MeshPairwiseFoundingTests`, plus the app's UI suite serially (the Friends tab's discovery start/stop is now the policy's — `ScreenAppearanceUITests` and any tab-switching UI test are the first real exercise of the seams). Item 3's red-once: add `store.presenceManager.stop()` to `ContentView.handleTabChange` and confirm the retirement wall reddens by verb; add a second `meshNetworkManager.startJoin()` to any App file and confirm the by-name exemption does not cover it. **Behaviour to eyeball on a simulator after the build:** (1) Friends tab entry starts the search and the tab exit stands it down — the seams' first live exercise; (2) Control Center pulled over the Friends tab (`.inactive`) no longer stops the search or presence; (3) delete-all during a live session now ends the session at leg 0 (the roster promotes into `pendingFriendReview` before the wipe's later legs — see residual R-3 below).
- Item 7's suites: `-only-testing:FernletTests/MeshP7RunPolicyAcceptanceTests` … `MeshP7HonestyAcceptanceTests` (six selectors), `CIGateSelectorBoundaryTests`, `MeshSessionPollTests` (its binding fix rides item 7), then the mesh-batteries step exactly as the workflow spells it (`Scripts/run-gated-suites.sh mesh-batteries 300 <the line>`) and **raise the floor to what it executed** (expected 313 = 300 + 13; write the measured number, never this expectation). Item 7's red-once: delete one `MeshP7…` selector from the workflow line and confirm `everyMeshAcceptanceBatteryIsGated` reddens by name; restore. The poller clause's partition cell depends on `evictSlotForTesting` reading as a slot loss to `branchView` — if it reddens on the first run, that cell's fixture is the suspect before the seam is.
- Watch for, on the first build: (a) Release configuration only — `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (`project.pbxproj:761`) is why `FernletTab` was made `nonisolated`; if the synthesized `Hashable` of `ProximityRunPolicy.Input` still complains there, `ScenePhase` is the remaining suspect; (b) the funnel's `duressSessionActive` PARAMETER shadows the store property of the same name — intended, but if the compiler objects, rename the parameter and update the doc; (c) `ProximityRunPolicyFunnelTests` builds a real `FernletStore` per cell in `AgeGateWiringTests`'s shape — if its manager's re-entry pass logs or throws on an empty routed store, that is the shipping launch path doing the same, not the test.

## Catalog keys owed to the close-out's sync (item 5; the working-copy catalog is untouched)
`Got it` · `Pick up your last session` · `Keep this tab open. When someone from that session is nearby again, you'll reconnect automatically, and anything still on its way will finish arriving.` · `Your last session couldn't be reopened` · `Its saved state was set aside. Nothing that was already saved was lost.` · `Your last session ended` · `That session is over, so it can't be reopened. Anything you kept is already saved.` · `Your last session timed out` · `Sessions last up to six hours. Anything you kept is already saved.` · `You left your last session` · `It can't be reopened, but anything you kept is already saved.` · `Your last session ended for you` · `You were removed from that session. Anything you kept is already saved.` — **13 sentences** (one dismiss label, six titles, six messages); sync from `HEAD`'s catalog blob on a Mac (`Scripts/sync-string-catalogs.sh`), never from a held working copy, and check the count against this list before committing.

## Residuals by name (for §13.3)
- **R-1** The executor and the two listener seams are exercised by no tier-1 cell (a real manager starts a real radio); the transition table and the wall are the claims. A `PresenceManager` built over a fake host (`HeartShareTests` shape) could drive `apply(_:)` without a radio if `start()` is safe there — not attempted.
- **R-2** Item 3 does not re-run the policy when the mesh's session presence changes on its own (a slot commits or drops) — exactly as today's chain did not; the next tab / scene / lock edge re-evaluates. The manager's own slot-loss clock (door 3) covers the blip case.
- **R-3** A delete-all during a LIVE session now tears it down at leg 0 (`leaveSession()`), which promotes the roster into `pendingFriendReview` before the wipe's later legs; whether a later leg clears that memory-only batch was not verified here (it is not a persisted surface, and the same promotion happened after the wipe before P7 whenever the session later ended).
- **R-4** The view's five edges and the store's edges reuse the scene facts of the LAST scene edge; `FernletApp` pushes on `.background` and `.active` and not on `.inactive`, which is fine because `.inactive` ≡ `.active` for the policy — but a new phase SwiftUI adds would be retained as whatever `routedGateForeground(for:)` answers for it.
- **R-5** `ProximityRunPolicyFunnelTests` cannot pin that the funnel READS the tab mirror with a distinguishing row (every such row starts a radio); the pure table pins the policy's reading and the funnel forwards `selectedTab` by construction.

## Blocked on owner — final state at the P7 boundary
*Every line below is TAKEN as a default (recorded in plan §13.2), or OWED and written into §13.3 with its cost and into §25.4 as the P8 handoff's list. Nothing is left ambiguous.*
- **A Mac — OWED, and it is the P8 launcher's item 0.** The gauntlet above; the 13-key catalog sync; the UI suite; the floor and pin re-measured; then items 6 and 8 (P8 launcher items 1 and 2). Nothing this session committed is green until then; plan §13's heading says IMPLEMENTED, UNBUILT until item 0 changes it.
- **Physical devices — OWED to P8.** `Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` sections A–E (P7 on hardware) and F (§15).
- Carried unchanged from plan §24.4: option (b) for `handleEncryptedMetadata`; D-7.30 once-per-window; §18.2 copy; the legacy unsigned removal; transcript `sid`; the hardware lanes (A report, B double-dial, AWDL, D with the cable out); the two census/duress questions; the final wording of the routed hold / refusal / heart copy (P6 added 19 sentences, 17 of them new to the catalog); §17.3's privacy paragraph (drafted in §24.4); `browsed peers=` downgraded from `.notice`/`.public`; the `HeartDrop` CloudKit record type still not promoted to the Production schema.
- P6 §12.3's open findings, none of them P7 work: the charged forwarder (4), `ConnectionInspectorTests` (5), the conflicted-member blast radius (6), the un-linked third member (7), item 6's two residuals (8), D-4.5's expiring heart (9), I-13's vanished pair (10), the peer-holdings-shrink shape (11), tier 2's un-run rows (12), L-3's arming race (13), the unpinned audit tokens (15), 1c's sibling wall-clock leg (16), the battery's own named weaknesses (17), the un-taken `MeshRoutedAckStageTable.increment1` alias cleanup (19), and the grouped residuals of finding 20.

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| Where the run policy lives | default: app-target `ProximityRunPolicy` (`App/Fernlet/ProximityRunPolicy.swift`), §13 option A | 2026-09-17 |
| Single writer of the routed access gate | default: yes, six sites → one, seam unmoved (item 2) | 2026-09-17 |
| Policy decides plaintext | default: no — radios only; the gate output is the pure three-fact value and reads no tab, age, wipe or CPT fact | 2026-09-17 |
| Poller ownership and interval | default: policy-owned, one timer, 30 s, ceiling → idle lapse → partition (item 4) | — |
| What the launch restore presents | default: Friends-surface affordance, nothing modal; deferred is silent (item 5) | — |
| Delete-all / age / duress reach the radios | default: as policy inputs producing `stop` for every radio (item 1 states it; item 3 wires it) | 2026-09-17 |
| New persisted surface | default: none | 2026-09-17 |
| **`ProximityRunState` has FOUR values, not §13's three** — `run` / `foregroundOnly` / `hold` / `stop` | `hold` = "keep what you have, start nothing, tear nothing down": today's committed session in the background without a CPT, and a committed session while the user is on another tab. §13's three values cannot say it without overloading `foregroundOnly` inside a background row. Deviation for §13.2. | 2026-09-17 |
| **`.inactive` is foreground for RADIOS too** | the policy's one foreground fact is `FernletApp.routedGateForeground(for:)` (`phase != .background`). Today's `ContentView` stops presence, recipe and discovery on `.inactive` (`scenePhase == .active` guards); item 3's retirement pass changes that behaviour by design — device lock still traverses `.inactive → .background`, so the stop still happens. Policy act for §13.2. | 2026-09-17 |
| **What "below-age" means as a radio input** | `belowMinimumAge` = the system's FINAL `.below` ruling against `AgeGate.chat` (13), or a guardian's communication limits — never "undetermined" (an unasked account keeps its radios; only chat is refused, as today). Today NO radio reads any age fact (only `chatAllowedProvider` does); §13 says below-age → stop, so the table says it and item 3 wires it. Policy act for §13.2. | 2026-09-17 |
| **What "duress" does to the radios** | every radio `stop` + the gate's `duressActive` leg (today duress closes only the gate and the decoy surfaces; §13 says stop + teardown). Policy act for §13.2. | 2026-09-17 |
| The three-dispatch shape, this session | the owner declined the item 1 agent dispatch at iteration 1, so item 1 was implemented and reviewed in-session by the orchestrator (design brief in scratch `item1-design.md`; the review pass re-checked every call against the real declarations and every pinned count by arithmetic — it caught one wrong count, 1 920 → 480). No compiler either way. | 2026-09-17 |

## Surprises worth not re-deriving
- **No toolchain in this container** (see the environment note). Do not spend iterations trying to fetch one: swift.org and GitHub release tarballs are both refused by the proxy (403 on CONNECT).
- **The launcher's three `FernletStore` stop sites are not all teardown.** `:1701` and `:1868` are the two settings opt-outs (`setAllowNearbyRecipeShares(false)` / `setAllowNearbyPresence(false)`); only `:5327` is delete-all (leg 7b). They are still policy INPUTS (`allowNearbyRecipeShares` / `allowNearbyPresence`), not competing owners.
- **`stopJoin()` → `stopSearching()` tears down the SLOTS too** (`MeshNetworkManager.swift:10762`: cancels every slot coordinator, `transport.stop()`, clears group-key state) — there is NO seam today that stops browsing/admission while keeping committed links. That is why `ContentView.stopFriendsDiscovery` guards on `hasCommittedPeer`, why the policy needs `hold`, and why a CPT-continued mesh's "discovery `stop`, mesh `run`" row is a P8 seam the P7 mesh seam must refuse loudly rather than execute (item 3).
- **The mesh chat age gate is `AgeGate.chat` (13, `isInterpersonalCommunication`, no self-attestation), reached only through `MeshNetworkManager.chatAllowedProvider`** (`FernletStore.swift:254`). Nothing else in the app reads an age fact for a radio.
- `FernletLockState` carries associated values (`.locked(cooldownDeadline:)`, `.unlocked(scope:)`); the policy takes the projection `appLockEngaged: Bool` (true iff `.locked`), which is exactly how `shouldRunPresence` / `shouldListenForRecipeShares` read it today.
- `MeshRoutedLockedDeviceTests` W6/W7/the launch-mount wall all read `FernletApp.swift` through `MeshRoutedSourceScan.codeOnly` (whole-line comments stripped, TRAILING comments kept) — a spelling a wall pins at 0 or 1 must not appear in a trailing comment on a code line. Item 2 re-aimed them; a helper whose BODY repeats the counted spelling makes a line-count wall read one too many (caught before commit: `FernletApp`'s helper is `pushProximityRunPolicy`, the store's funnel is `applyProximityRunPolicy`, precisely so W6 counts edges and W8 counts writes).
- `ContentView` already mirrors `lockService.state` into `store.lockState` (`handleLockStateChange`) and `isDuressSessionActive` into `store.duressSessionActive` — the tab mirror is the third of that family, not a new pattern.
- Lessons carried from P6 (unchanged, all paid for): a full log containing `Restarting after unexpected exit, crash, or test timeout` has NO usable total — re-run, never bisect; a red can be a starvation (1749 s for a 1.3 s cell); never chain a build and a test run on one DerivedData; `-only-testing:` names the SUITE (one file can hold four) and `Suite/cell` runs 0 tests under a green banner; `@Test(arguments: [])` is green over nothing — collapse whitespace before `contains` and assert every allowlist entry matches something; a negative control whose "before" text is empty cannot be reverted by a count-1 replace; "inert until P8" rots — measure a predicate's blast radius by grepping its readers; a roster-wide invariant is not key-generic; a process-global audit signal cannot witness a per-cell claim; the first test invocation after a build hangs (~350 s) — warm with a tiny suite; zsh passes an unquoted `$ARGS` as ONE argument; `starts − passes` of a few is a logging artefact; `#expect(_, "literal")` only, bind `allSatisfy` Bools first, never `==` on signed records, `MeshRoutedStorageScope.production` never as a test literal; a headless Simulator satisfies the foreground leg by accident; any harness bypass that skips a shipping door hides a product claim; long agent work can die at the usage-limit reset hour — resume, don't retry.
- Closed; do not re-audit: `MeshTunnelConvergence`, the id-vs-endpoint family, the crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10, §11.1–§11.4 and §12.1–§12.4, `Docs/Proximity-Security-Followups-2026-08-18.md` §1.
- Concurrent sessions may share this tree; `Localizable.xcstrings` + `xcschememanagement.plist` were held by another session at P6 — never stage them; commit with explicit pathspecs.

## Memory note (what landed, what surprised, what not to re-derive)
- **Landed:** one policy value (four run states, eleven facts, the gate as output), one gate writer (W8), one radio-speaking file (the retirement wall), one poller timer over one kit seam (ceiling → idle lapse → partition; finding 3 closed on every joiner), one presentation table over every restore outcome with six cards on the Friends tab, one acceptance battery (six suites, 13 cells) on the CI line. **None of it compiled.**
- **Surprised:** the phase's real finding is a missing ProximityKit verb — `stopSearching()` tears slots down, so "mesh runs, discovery stops" has no seam; the policy's `hold` and the refused transition are two views of that absence, and it is P8's first code. `.inactive` as foreground for the radios is a behaviour change, not a no-op. Two of the launcher's three "store stop sites" were opt-outs, not teardown. The owner declined the agent dispatch; the container had no toolchain.
- **Do not re-derive:** the `.inactive` mapping (`routedGateForeground(for:)` is the one place); the three predicates' jobs; the app's edge list (six view call sites, four store call sites, plus the scene push — the wall counts spellings, declaration included); the catalog keys (13, listed above); every "Owed to a Mac" line; the P6 lessons list above.

## Next item
**P7 is CLOSED as IMPLEMENTED, UNBUILT.** Plan §13 carries §13.1–§13.4 with that heading; §25 is the P8 handoff; the P8 launcher is `Docs/Next-Round-Prompt-Mesh-P8-2026-09-18.md`; the device plan is `Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md`. **Next: a FRESH session on a Mac from the P8 launcher, whose item 0 is this ledger's "Owed to a Mac" list.** Do not reuse this ledger; P8 creates its own. Close-out commit: `9dcdc14`, pushed to `origin/claude/zen-goodall-wejlxg`.
