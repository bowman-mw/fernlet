# Mesh Migration Loop Ledger — P5

**Phase:** P5 (encrypted store-and-forward routing) · **Prompt:** [Next-Round-Prompt-Mesh-P5-2026-09-03.md](Next-Round-Prompt-Mesh-P5-2026-09-03.md)
**Started:** 2026-09-03 · **Iteration:** 14 · **Tree at seed:** main = `b31b7c0` (pushed; launcher commit on top of `6bc98ee`)

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 1 | MeshRoutedManifest + MeshRecipientKeyWrap (§11) | 1 | — | done | `bf31f46` | trio additive, no golden moved; verifier splits departed (ok) vs removed (refused); acceptedTypeTokens is item 11's seam |
| 1a | Pre-existing test-host crash: `unowned let store` in mesh rigs (MeshNetworkManager.swift:182, PresenceManager.swift:141) traps nondeterministically after mesh.merge.askedLateReconnect; since item 8 it reproduces in the 121-suite subset (D-8.42: `displayName.getter ← sendEnvelopeCore ← closure #1 in broadcastCoordinatorBeacon()`, crash report Fernlet-2026-09-04-115712.ips) — every gauntlet now costs a second invocation | 1 | — | done | `e9f2282` | seen in P4-era logs before item 1 existed; rig must own the store for the manager's lifetime; small, file-disjoint |
| 2 | MeshChunk on P2's existing stream lane | 1 | 1 | done | `5019e04` | origin-signed chunks + assembly-time contentHash check; chunkID derived, origin-free; Transport/ docs-only |
| 3 | MeshCustodyReceipt, four-state sidecar + fifth wrinkle | 1 | 2 | done | `64e8c44` | MeshRoutedStore five-state floor; witness-gated receipt (durable-before-acknowledged as a type rule); wipe row + delete-all wired; keychain service com.fernlet.mesh-routed |
| 4 | MeshRecipientReceipt, per-type ack stages | 1 | 3 | done | `fdabe2e` | stage resolved from the type token via ONE table (item 11's value); witness-gated; heart = judgements + ledger proof + itemID==giftID; routed index schema → 2 |
| 5 | Routed content digest, own frozen token | 1 | 1 | done | `df1a48d` | MeshRoutedInventory (no 'Digest' stem), advertiser-signed, exact chunk bitmap + signer index lists; Delta.between → ask/offer/receipts; matched = quiescence |
| 6 | The drain on the one merge path | 1 | 1, 5 | done | `b2a09fd` | rides the three sendInventoryDigest call sites (grep-walled); push-only plan; per-peer SESSION FRAME BUDGET (not a once-per-peer boolean); origin-retains at manifest + chunk doors; routed answer = fernlet.mesh.routed-drain-answer.v1 carrying the quiescence bit |
| 6a | Fixture rot: drain/convergence cells inherit `MeshMergeWire.start`'s fixed `MeshP3Acceptance.base` anchor, so every fixture manifest expires 2027-01-15T14:20:00Z | 1 | — | todo | | test-fixture time bomb (memory: fixed-date fixtures rot); anchor the manifests' expiry to the injected clock, small |
| 1b | Pre-existing load-sensitive flake: `ProximityCoordinatorTests.phase1_unknownPayloadTypeIsParkedWithoutFailingSession` waits for async inbound handling with a FIXED deadline (ProximityCoordinatorTests.swift ~857) — seen once in item 8's 139-suite re-run, green on retry | 1 | — | todo | | memory: wall-clock deadlines vs MainActor starvation — use the deadline + min-poll-floor helper idiom; small |
| 6b | Drain store I/O runs on the MAIN ACTOR (item 10: a rising re-entry pass costs THREE index loads plus conditional claim/mint reads; item 9: 1 load + 1 seal per reclaim batch) (up to a 256 MiB hash in committingCustody; up to 64 sealed-file opens per answer) | 2 | 6 | todo | | bounded per answer; `maxChunksPerAnswer` (64) and `maxChunksInFlightPerPeer` are the two numbers tier 2 re-measures; P6/P7 may move it off-main |
| 7 | Merge-window redesign (2d comes due) | 1 | 6 | done | `97ded8e` | `MeshMergeWindow` value type; closes iff `pending = (asked ∪ answered) ∩ reachable ∖ matched` is empty; `answered ⊆ pending` and a mismatching digest un-matches its sender; `reachable` = every committed slot ∩ roster (NOT `activeSlots`); the occasion is one post-merge proof per distinct digest to the pending set captured BEFORE the fold's re-evaluation; routed quiescence logged, gates nothing; 2d's 4 cells back in the matrix at 80/80 at full strictness |
| 8 | Custody-transfer-on-departure | 1 | 3, 6 | done | `86d57c4` | leaver hands outstanding∩pending legs to custodians it named AND served (origin-served set = the hop bound); authority = SignedDepartureRecord, no new wire; handedOffItemCount real; four idempotent claim doors; D3 cell written |
| 9 | Backpressure at 256 MiB / 1024 items | 1 | 3 | done | `9f52323` | one injectable cap value (MeshRoutedCapacity = MeshRoutedStoreFormat) at all four doors; parked drop rule one clause; sweeps budgeted once per peer per session, spent after load; MeshRoutedDeliveryHold + RoutedDeliveryHoldBanner; chunkCount guard added |
| 10 | Locked-device handling | 1 | 3 | done | `9859817` | MeshRoutedAccessGate: data protection gates plaintext, app lock gates nothing in the mesh, custody ungated; push door applyRoutedAccessGate from six FernletApp sites; five-job re-entry (claims, deferred commits re-derived, suppressed sweeps, heart stage, P6 slot); no keychain class moved |
| 11 | Type-token registry | 1 | 1 | done | `4b5e4ca` | MeshRoutedTypeRegistry.increment1 (3 rows = today's constants); ack table is its projection; entry(for:) == nil is 'unknown' at seven doors; .relayInFlight unregisterable; canonical-store slot = frozen token enum; one-registry + no-per-type-branch walls |
| 12 | MeshFrameReplayWindow wired | 1 | 2 | done | `608f428` | keyed (meshID, author, contentID) at four content doors; verdict() before verify, admit() after the store settled; bounds derived (1056 frames/author, 16 authors); digest family stays out; W1–W8 walls |
| 13 | Retire the three keyEpoch gates with the path | 1 | 1–7 | in-flight | | never loosen in place; re-check line numbers first |
| 14 | P5 acceptance battery | 1 | 1–13 | todo | | extend MeshScheduleGenerator, not a new rig. From item 10: `routedLockWindowEvent(at:closingAfter:now:sampling:)` shipped; whether a locked window becomes a `MeshScheduleEvent` case is a matrix-wide decision (changes every seeded draw). Seams already shipped: `routedCustodyEvent`, `routedOutstanding`, `routedInvariants`, `runRoutedDrainRounds` (item 6); `routedDevelopmentEvent`, `runBranchDrainRounds`, `routedHandoffInvariants` (item 8, one cell on the root seed). Owed: fold item 8 design §6.5's five invariants into `routedInvariants` (departure-free seeded cells otherwise assert them vacuously); run the development across more seeds and shapes; `mergeWindowForTesting` + `lastMergeClosureForTesting` for WHY a window closed (D-7.20); D-7.15's liveness disposition is the input |

## Blocked on owner
- (item 12, D-12.14) three routed door refusals sit BEFORE their store verb and stay replayable without bound (notADestinationOrHandoff, unknownItemNotFromOrigin, unregisteredTypeChunk: 1 Ed25519 verify + 1 hash, or 1 sealed-index load, per replay) — needs a per-sender pre-verification budget, a different mechanism, deliberately not half-built; (D-12.15) a post-repair custody receipt re-mint carries the same receiptID, so peers keep the earlier receipt (staleness); (D-12.12 amended) a `payload.sentAt >= inventorySentAt` monotonicity guard on `recordPeerRoutedInventory` is handed forward, not built.
- (item 11 → P6) in ONE commit with its first narrowed per-type cap, P6 owes the receiver-side per-type size check, its new `MeshRoutedManifestRejection` case, and that case's NON-dropping arm in `MeshRoutedParkedDrop.reason`; the registry's `expiry` column has no discriminating test until a second `MeshRoutedExpiryRule` case exists (D-11.22); deleting the derived `MeshRoutedAckStageTable.increment1` alias and re-pointing item 4's pins is an unowed cleanup; `MeshRoutedTypeRegistry.maxEntries` restates the ack table's 16 (test-pinned).
- **(item 10, F-1 — SERIOUS, outside P5)** `IdentityService.loadExistingDeviceIdentity()` (IdentityService.swift ~574–582) uses the nil-collapsing `KeychainItem.load`: after first unlock a TRANSIENT keychain read error falls through to the mint, and `KeychainItem.store` deletes before adding — destroying the real identity. Fix shape: a `loadDistinguishingAbsence` that refuses to mint on an error. Owner's call; not P5's store.
- (item 10, D-10.9) whether a device that could hold nothing all session should say so, and in what words — answer together with item 9's six blocked display literals.
- (item 10 → P6) `reentryFinishLocalAcks` / `reentryHeartStage` are P6's seams (job 4c is a counted no-op today); P6 must extend wall W2(b) to the first real canonical-mutation verb in the same commit.
- (item 9) the final wording of the six new app display literals in RoutedDeliveryHoldBanner.swift and their catalog sync (Localizable.xcstrings held; sync script not run); whether the copy carries the count (a variations.plural block + a pluralRuledKeys entry).
- (item 9, tier 2) the sealed index file's own size (~5 MB worst case) is bounded but UNCOUNTED by the byte cap; the reclaim's per-exchange I/O (1 load + 1 seal per batch) joins 6b's main-actor measurement.
- (item 9, deliberately not shipped) an item whose destinations have all DEPARTED is not reclaimed and waits for expiry — a departed destination and an unmerged roster are indistinguishable at the reclaim instant.
- (item 1a hand-off, H-1a.3) four app-side types hold their host `unowned` on the same detached-Task pattern, outside ML4's scope: MealResolutionService, HealthSyncCoordinator, JournalSealingCoordinator, OwnPhotoBackupCoordinator — same fix shape, app target; (H-1a.4) `stopSearching()` / `leaveMesh()` still do not cancel the detached fan-out (pinned now, so safe, but not cancelled).
- (close-out flag) `MeshSessionContext.routingInventoryDigest` is provably dead as of item 5 (left nil) — delete at close-out or hand to P6.
- (close-out flag) `CryptoFormatCensusTests` now has TWO sealed at-rest mesh surfaces outside its census (mesh-session from P3, mesh-routed from P5 item 3) — P3's precedent followed; owner decides whether the census grows.
- (close-out flag) the duress pre-draw sweep names neither `com.fernlet.mesh-session` nor `com.fernlet.mesh-routed`; both die in the delete-all funnel — decide whether duress should pre-draw them too.
- §18.2 partition UX copy — default: subtitle count only, no new string (unchanged from P4).
- Legacy unsigned two-party removal's retirement — default: leave frozen (unchanged from P4).
- CI gate lines for MeshP3/P4 AcceptanceTests — still not wired into s3-wall.yml.
- (close-out flag, item 7 / D-7.30) the membership re-gossip budget `reGossipedToFingerprints` is
  per SESSION and is not refunded by `abandonMergeExchange`, so a second heal of the same pair inside
  one session crosses no records and neither window can close. Not a regression — today's rule
  converges no better — but making it once-per-window is a transport decision with a blast radius
  across all 80 property cells and `MeshRoutedDrainConvergenceTests`. Owner's, or item 9's.

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| Relay-retention, increment 1 | origin-only until departure (item 1 mints, no relay hop) | 2026-09-03 |
| Merge window closing rule | **every asked peer matched** — `pending = (asked ∪ answered) ∩ reachable ∖ matched`, closing iff empty; responder side is "answered AND the peer's next digest matched", never "answered" alone | 2026-09-04 |
| D-7.1/7.2 rule + responder clause | encoded as an explicit value (`MeshMergeWindow`), never a counter; `answering(peer)` inserts into `answered` AND removes from `matched`, so one frame can never create and discharge an obligation | 2026-09-04 |
| D-7.3 where the state lives | new file `FernletKit/Sources/ProximityKit/Mesh/MeshMergeWindow.swift` — `MeshMergeWindow` + `Role` + `Closure` + `Verdict`, all `nonisolated`, pure, no clock; the manager only calls transitions | 2026-09-04 |
| D-7.4/7.27 monotonicity | a match is RECORDED, never re-derived locally at close time (local grows, stored evidence does not, and re-gossip is spent) — but that peer's own later MISMATCHING digest un-matches it | 2026-09-04 |
| D-7.5 unasked match | recorded in `matched`, never promoted into `asked`: it can only help a peer asked later. This is the 2d fix — an unasked peer's match closes nothing | 2026-09-04 |
| D-7.6 reachability | derived at read time: **every committed slot** ∩ derived roster. Explicitly NOT `activeSlots` (a UWB distance rank capped at 3 of 5, re-assigned by `rerankSlots()` off ranging) and NOT `reachableRosterFingerprints()`. Evaluated at `removeSlot`/`disconnectSlot`; `rerankSlots()` is deliberately not an evaluation site | 2026-09-04 |
| D-7.7/7.8/7.9 the proof | no new frame/field/purpose/golden — `fernlet.mesh.inventory-digest.v1` unchanged. `readvertiseMergeProof(to:)` is a FOURTH `sendInventoryDigest(` call site, fired from `mergeReconnected` only when the fold moved `localInventoryDigest`; value-gated, capped at `MeshMergeWindow.maxProofs` = 49 = `maxReGossipFrames` (pinned by assertion) | 2026-09-04 |
| D-7.10/7.26 evidence | re-evaluate the window's OWN evidence after every fold; evidence lives on the window and dies with it, never `peerInventoryDigests` ("a hint, never an authority", survives a partition) | 2026-09-04 |
| D-7.11 routed half | membership digests gate the window; routed quiescence is **recorded and logged, and gates nothing** — the capacity-refusal contract leaves a refused pair non-quiescent for the session, so a gated window would never close again (item 9's surface) | 2026-09-04 |
| D-7.12/7.13 vocabulary | `concludeMerge()` → `concludeMergeIfConverged()`; `MeshMergeWindowClosure` = frozen `converged` / `nothingOutstanding`; audit token `mesh.merge.converged` unchanged, context = counts + rawValues only | 2026-09-04 |
| D-7.14/7.29 clearing | `clearMergeWindow()` nils ONLY the window, at exactly three sites; `pendingMergeEntry` keeps its own two (a launch restore arms `.processRestart` with no window); never folded into `clearRoutedDrainState()` | 2026-09-04 |
| D-7.21 paperwork | nothing persisted: no wipe row, no delete-all writer, no `UserDefaults` key, `MeshSessionContext` schema stays **2** | 2026-09-04 |
| D-7.22 observable | `awaitingResumeMerge` becomes computed `mergeWindow != nil`; named delta — an IDLE window closes `.nothingOutstanding` at its first evaluation, reachable only via `removeSlot`/`disconnectSlot`, and it errs toward re-opening | 2026-09-04 |
| D-7.25 wall amended | `theDrainFiresOnlyFromTheMergeDoor` → membership 5 / routed 4, and both counts replaced by per-function brace-matched assertions naming the three ask doors plus the proof door. Hiding the fourth call behind a helper was rejected by name | 2026-09-04 |
| D-7.28 proof ordering | the proof is owed to the pending set captured at ENTRY to `advanceMergeWindowAfterFold`, before the re-evaluation and before the verdict — "only if still open" silences exactly the device that just converged | 2026-09-04 |
| D-7.30 re-gossip budget | `reGossipedToFingerprints` stays PER SESSION and is not refunded by `abandonMergeExchange` — same refund D-6.6/D-7.14 refuse for the drain. Consequence traced and pinned by a wire cell; the fix is a transport decision, flagged to the owner | 2026-09-04 |
| D-7.31 arming point | `mergeWindow = .opened(at: now)` sits exactly where `awaitingResumeMerge = true` stood — BEFORE both guards — so the observable is bit-identical on the verifier-less and empty-recipient paths | 2026-09-04 |
| D-7.15 P4 i11 | **Fixed for the bidirectional-mismatch shape P4 named**, by the post-merge proof door. The residual narrows to three named shapes: (1) an asked, reachable, silent peer — now including a peer re-asked under D-7.32; (2) a peer that grew and stopped speaking after being un-matched (D-7.27); (3) a pair whose per-session re-gossip budget is spent (D-7.30). Each is bounded only by `reachable` shrinking, `abandonMergeExchange` on `.partitioned` (MNM:4275) and `resetSessionStateMachine` (MNM:1312) — **so a window is NOT guaranteed to close**, which is item 14's liveness input. No existing `MeshMergeExchangeTests` assertion flipped, contradicting map R5/Q2's prediction that three would (traced in design §6.7) | 2026-09-04 |
| D-7.16/7.17/7.18 the deferral's retirement | `checkExceptTheHealsRotationCause` deleted and its assertions folded back into `check(_:)`; `MeshConvergenceMatrix.deferred` stays as an **empty symbol** with its `cells(for:)` filter (that is the mechanism "deferred again only by name" rides on) and `deferredSeeds` → `windowRedesignSeeds`, keeping both values as the named fixture; `MeshP4DeferralAcceptanceTests` keeps its suite name, and the parameterized test was **deleted and replaced** by a locally-built four-cell list | 2026-09-04 |
| D-7.19 `maxCommitRounds` | the number stays **2**; both halves of P4's written justification are false in their details after item 7, so the comment was re-justified in the same commit rather than the number moved | 2026-09-04 |
| D-7.20 item 14's observable | `mergeWindowForTesting` + `lastMergeClosureForTesting`, so a cell can assert **why** a window closed, not merely that `awaitingResumeMerge` went false | 2026-09-04 |
| D-7.23/7.24 `pendingMergeEntry` and `openedAt` | the entry is the **pre-window arming slot** (non-nil with no window at launch restore) and keeps its own four write sites — the window never duplicates it; `openedAt` is recorded and **never branched on**, pinned by a unit test and a grep-wall | 2026-09-04 |
| D-7.32 the LATE ask un-matches | *(iteration-7 review)* `askOneReconnectedPeer` calls the new `MeshMergeWindow.reAsking(_:)` — insert into `asked`, **remove from `matched`** — not `asking([peer])`. A late ask exists only for a peer whose link dropped and re-formed, i.e. the one peer that may have been on the other branch in between, so a match from before it left cannot close the window. The **opening** `asking(_:)` is unchanged. Fails toward residual shape 1, one peer wider | 2026-09-04 |
| D-7.33 a joiner's grant reply | *(iteration-7 review)* `attemptLedgerAdoption(ownAdmission:meshID:)` sends **one** `sendInventoryDigest(to:[admitter])` after the durable rebase. A joiner admitted mid-heal replies with a one-record bootstrap digest, so the admitter answers it and holds the obligation, but adoption rebases without `mergeMembershipLedger(_:)` so the proof door never fires there. Fifth `sendInventoryDigest(` site, **second non-ask door**, no routed twin; the wall moved to membership 6 with it. Rejected: carving "answered but not pending" out of the closing law for one message shape | 2026-09-04 |
| D-8.32 | **The hop bound is now measured, not argued.** `aSecondHopHolderClaimsNothing` passed for an unrelated reason (the split destination meant the leaver's record named the custodian only, so `claimHandedOffCustody` exited at `guard !leavers.isEmpty` before `originServedItems` was read). New `MeshCustodyChainScenario` — four nodes, **no partition at the development**, so `branchView == nil` and the record names the whole roster — plus `aSecondHopHolderNamedACustodianStillClaimsNothing`, which asserts every other gate open by name and keeps the first hop's successful claim as the positive control. New read-only seam `originServedItemsForTesting`, because a set nothing can observe is a set a widening can delete silently. **Negative-tested:** dropping `context.sender == manifest.originFingerprint` builds clean and fails both hop cells (`logs/neg-hop-test.log`). |
| D-8.33 | **The property-battery cell develops with work still outstanding.** It had healed and drained to completion first, so the transfer had zero candidates and both invariants held with item 8 removed. New `runBranchDrainRounds(origin:key:)` drains **inside the origin's branch only**; the cell states its own preconditions (origin holds a custody receipt; the far branch is still owed) so the shape cannot rot silently. |
| D-8.34 | **`routedHandoffInvariants` asserts what it documents.** `transferredItemCount == 1` (was `<= 1`, which zero satisfies), a required non-empty courier set, and the hop half made real: every device carrying its **own** courier rung must hold the key in `originServedItemsForTesting`. The `handoffTargets` subset stays as the record half. |
| D-8.35 | **`aDepartingCustodianHandsOffNothing` reaches the enumerator.** It was measuring the termination guard (merged roster two ⇒ `.none` at line one). Moved onto the four-node chain, where the ending is `.departure`, `handoffTargets` is non-empty and the outcome is `.completed`; beside it the first negative case `originatedBy:` has ever had — one index, one instant, one roster, empty under self and non-empty under the real origin. |
| D-8.36 | **The per-evaluation commit deferral is real.** It was permanent, not latent: after a claim no named leg is `pending`, so the planner can never re-offer the overflow and `commitLocalCustody`'s other call sites need the `custodiedAt` only this function writes. New `deferredCustodyCommits` — bounded, deduped, memory-only, session-scoped, drained ahead of the next evaluation's own work, and drained by `mintClaimedCustody([], at:)` when an evaluation plans nothing. A store that cannot vend an index defers the **whole** queue rather than the overflow. No wipe row (memory-only). Measured over `cap + 1` items at a **pure courier**, where `custodiedAt` can come from nothing but the claim. |
| D-8.37 | **The merge door runs the roster verdict FIRST.** `claimHandedOffCustody` sat above `applyMergedRosterVerdict`, so a merge delivering this device's own removal wrote rungs and re-streamed content before ejecting — the opposite of the live-record twin `applyRosterMove`. Now `if moved, !applyMergedRosterVerdict(from: before) { claimHandedOffCustody(now: now); requestRotation(cause: .merge) }`. Measured on a four-node rig, because the merge inserts departures before removals and a roster of three would leave one eligible voter (`quorumNotMet`); walled in both doors by `theMergeDoorClaimsOnlyWhenTheVerdictDeclinedToEndTheSession`. |
| D-8.38 | **`aCustodianNamedByARemovedMembersRecordClaimsNothing` reaches the gate.** Folding the removal first meant the departure of an already-removed member moved no roster, so no door evaluated the claim at all. The cell now asserts both records folded and the item origin-served, then runs the derivation at a new seam, **`claimHandedOffCustodyForTesting(now:)`** — documented as *not* a fifth door: the four shipping doors and their wall are unchanged, and the seam exists so a gate is measured rather than inferred from whichever door a scenario reached. |
| D-8.39 | **`windowExpired` is its own `MeshCustodyHandoffSuppression` case.** An expired window returned `.none`, whose documented meaning is "held nothing" — the exact conflation §19.5 asks the enum to prevent. The outcome and the suppression now agree instead of one standing in for the other. |
| D-8.40 | **A delivery-level refusal keeps its own name.** The batch doors collapsed a `MeshDeliveryRefusal` (nothing applied) into `.unchanged`, so it reached no report list and no audit line. New `MeshRoutedHandoffRefusalReason` (`.store` / `.delivery`, frozen `token`) — `MeshRoutedStoreRefusal` has no spelling for `alreadyDelivered`, and inventing one would give one fact two names; `MeshRoutedHandoffStep` gained `refusedDelivery(_:)` and dropped its unused `String` raw value. Both appliers are now `switch`es over `MeshDeliveryOutcome`, so no default stands in for a refusal. |
| D-8.42 | **The gauntlet subset now takes TWO invocations, and the reason is a pre-existing race, not a broken cell.** `MeshNetworkManager` holds its host store `unowned` (safe in production, where the store is process-lifetime), and `broadcastCoordinatorBeacon` spawns one **un-cancellable** send `Task` per slot; one landing after a rig's store has deallocated aborts the whole test process. Measured: 121 suites with item 8's six review cells skipped = 1199 tests green; skipping *any one* of the six still crashes and skipping *all six* does not, so it is six more mesh developments in one process, not one cell. Green as **two** invocations — 1005 + 194 = 1199 tests, every suite ✔, exit 0. Three mitigations landed anyway (rotation consumed before teardown; `quiesce()`; `dispatch` = `deliver` without its per-frame settle), and the real fix — keeping the fixture's stores alive for the process lifetime, as production does — is a shared-rig change handed to the orchestrator. |
| D-8.41 | **The rig verb's return is load-bearing.** `develop(_:clock:)`'s emissions were discarded at all 20 call sites. `developInsideTheWindow` now asserts `emitted.first == .meshMemberDeparture` and no `.meshTerminated` — stronger than planned, because the hook also fires for the push frames, so `first` pins D-8.27's "the record goes before the bytes" at the seam that signed it. The hook is cleared when `develop` returns. |
| D-8.1 | Custodian's authority = the leaver's **`SignedDepartureRecord`** (`custodyHandoff.custodianFingerprints`). No new frame, purpose, golden or framing case; D-6.15's derived rung sufficed — the missing half was never a signature, it was a **writer**. |
| D-8.2 | Two new store doors, both batch (one load / N updates / one save): `recordingCustodyHandoff(_:now:)` (origin, receipt-backed, item 3's rule verbatim through the now-internal `receiptRefusal`/`advancingAll`/`storing`) and `claimingHandedOffLegs(_:now:)` (custodian, **receipt-free**). `recordingCustodyTransfer` untouched and still with **zero** shipping callers. |
| D-8.3 | The claim gates on **`record.isComplete`**, never `custodiedAt != nil` — the latter re-closes item 6's circle and strands the pure courier. |
| D-8.4 | Eligible custodian = one whose **verified custody receipt this device already stores**; the chosen one is the lexicographically least, matching `MeshDeliveryState.later`. |
| D-8.5 | Legs handed = `outstanding(in: roster)` ∩ `pending`, minus the custodian. Makes all four `MeshDeliveryRefusal` cases unreachable and "exactly one transfer" structural at the origin. |
| D-8.6 | `advancingAll` keeps **refuse-batch**; the caller owes a refusal-free list. Item 4's cell is now the documented contract, with item 8's positive cell (`aTransferOverPendingLegsNeverRefuses`) beside it. |
| D-8.7 | `handedOffItemCount` = distinct items whose rung moved `pending → custodied(by: C)` and whose save succeeded. Not sends, not candidates, not legs. Observable on `lastDevelopmentHandoff`, in the signed record, and in `mesh.development.handoff`'s `items`/`unplaced`/`pushed`. |
| D-8.8 | `custodianFingerprints` **stays** `plan.handoffTargets`, re-documented as "**offered** to". Narrowing was rejected: it strands the pure courier and flips a shipping assertion. |
| D-8.9 | Entitlement source 2 is wired into `offerableKeys` **scoped to a live development** by `developmentHandoff` (armed once, cleared in `resetSessionStateMachine`, i.e. dead by the end of `leaveMesh`) — never off `lastDevelopmentPlan`, which outlives the session on purpose. |
| D-8.10 | The departure push is best-effort, **uncounted**, and rides `sendRoutedBulk(_:to:now:)` (extracted from `sendRoutedDrainBatch`) with its own audit tokens. `sendRoutedInventory(` stays 4 and `sendInventoryDigest(` stays 6; the wall was amended anyway with a **third door class** (`pushCustodyToCustodians`) and a `sendRoutedBulk(` call-site count, because a count that stays green by accident is not a wall. |
| D-8.11 | `itemsAwaitingHandoff` / `handoffCandidateCount` gained a **required, undefaulted** `originatedBy:` — the second hop is unrepresentable at the enumerator. Four test call sites and three DocC symbol links moved in the same commit. |
| D-8.12 | `MeshDevelopmentPlan` gained `handoffSummary(handedOffItemCount:)`; the zero-argument property stays as the nothing-transferred answer. The plan stays a pure value with no I/O. |
| D-8.13 | The claim is one **idempotent derivation** called from **four** doors (live insert, merge, item completion, drain-exchange entry), never four event hooks; `dispatchMembershipEventPayload` / `applyRosterMove` / `mergeMembershipLedger` / `mergeReconnected` gained `now: Date = Date()`. |
| D-8.14 | The custody **commit** at the custodian is capped at `MeshRoutedDrainBounds.increment1.maxItems` per evaluation with a named deferral that is **carried in `deferredCustodyCommits` and drained at the next evaluation** (D-8.36 — routing the retry through the planner would have made it permanent); the rungs themselves are uncapped-but-bounded (one save), because they are cheap and `mayCourier` reads the rung, not `custodiedAt`. |
| D-8.15 | D-6.16's second disjunct is **declined**, in a source comment and in `theReceiveClauseIsStillOneHopFromTheOrigin`. No persisted surface, no wipe row, no schema bump, no wire change. |
| D-8.16 | The development path now reads the clock **three** times plus one per custodian on the push; `SteppedClock` saturation means no existing cell was re-timed. Two "twice" doc comments rewritten (`MNM`'s `- Parameter clock:`, `SteppedClock`'s own). |
| D-8.17 | **The hop bound is the origin-served set, not the departure record.** `custodianFingerprints` is the whole roster − self in every production departure, so the record alone would make the content walk A→B→C→D. A device may claim handed-off legs only for an item whose **manifest it admitted from the origin itself** — memory-only `originServedItems`, one write site (`ingestRoutedManifest`), one read site (the claim), cleared with the session. Residual, named: a restart before claiming forfeits the claim (fail-closed, `mesh.development.handoffClaimNotOriginServed`, item 10's if it should ever be durable). Measured by `aSecondHopHolderNamedACustodianStillClaimsNothing` on the four-node no-partition chain (D-8.32) with `aSecondHopHolderClaimsNothing` beside it, walled by `theOriginServedSetIsWrittenAtOneDoorAndDiesWithTheSession`, and observable through `originServedItemsForTesting`. |
| D-8.18 | **The removed-member gate lives in the manager**, not the verifier and not the pure planner: `MeshMembershipRecordVerifier` accepts a departure "from any ADMITTED fingerprint", and a derived roster cannot separate departed from removed. `claimHandedOffCustody` filters leavers against `ledger.removals` **and** `removedMemberFingerprints` before the planner. `aCustodianNamedByARemovedMembersRecordClaimsNothing` uses a real `SignedRemovalRecord.signed(…)`. |
| D-8.19 | **The departure push is built through the drain's narrowing initializer** (`MeshRoutedInventoryDelta.between` + `MeshRoutedDrainPlan(delta:refused:bounds:frameAllowance:)`), so every bound and the gap computation come from the one place they live. `aPushBatchNeverExceedsTheDrainFrameBound` is the cell that would have caught the memberwise bypass. |
| D-8.20 | **The claim is bounded in authority, not in time.** One signed development authorises it; the write is an idempotent derivation applied at four doors, bounded by `record.isLive(at:)` and the manifest's expiry. The fourth door (the drain-exchange entry) exists because a store that was unreadable when the record folded is reachable by no other event — `aDeferredStoreClaimsAtTheNextDrainExchange`. |
| D-8.21 | **`sendMembershipEvent` answers a `Bool`** (`@discardableResult`, four `return false` exits). Its swallowed exits left rungs written and no custodian ever told; the result now reports the count as **unplaced**, `suppression = .recordNotEmitted`, and skips the push. |
| D-8.22 | **The push is bounded by the plan's own deadline, re-read per custodian.** `scope.admits(peer, at: plan.startedAt)` would be vacuous by construction, and a frame cap is not a time bound. `.windowExpired` and a non-zero signed count legitimately coexist — the count is authoritative for what transferred, the outcome for the clock. `thePushStopsAtTheWindowDeadline`. |
| D-8.23 | **One item, one commit, per evaluation.** `claimHandedOffCustody(now:excluding:)` is inserted **before** `guard routedRungsOutstanding(…)` in `finishLocalRungs`, and excludes the key the caller commits one line later. Receipts minted at the other three doors are **not** transmitted — no peer exists — and peers learn through `custodySigners` in the next inventory. |
| D-8.24 | **`MeshRoutedHandoffReport` carries `incomplete:` as its own list**, because `MeshRoutedStoreRefusal` is a `String`-raw enum with no such case and "not yet" is not a refusal. |
| D-8.25 | **Paperwork the rename and the new files owe, all in this commit:** three DocC symbol links; a fourth moved test call site (`MeshRoutedDeliveryAckTests`); `MeshRoutedCustodyHandoff.swift` added to `theRoutedStoreNamesNoDecryptionSeam`'s file list; the ProximityKit landing page, `Docs/ProximityFunctionIndex.md` (two new file sections + five amended rows) and `Docs/FileIndex.md` (two rows). |
| D-8.26 | **Spellings and observation.** One name per concept (`MeshRoutedHandoffClaim`, `transferredItemKeys`/`transferredItemCount`, `unplacedItemKeys`, `pushedItemKeys`); `MeshCustodyHandoffBounds` deleted rather than defined (no new number invented); all three new manager properties `@ObservationIgnored`. |
| D-8.27 | **The push runs before `applySessionEvent(.departureSent)`.** That event carries `.stopParticipation` → `stopSearching()` → `slots.removeAll()`, so a push after it has no slot to send on — measured, twice, at zero frames. The record still goes first, so a custodian sees the entitling record before or with the bytes; only the local teardown is delayed, by a deadline-capped push. |
| D-8.28 | **`advanced` means the rung actually moved, enforced in the door.** `MeshDeliveryTarget.advancing` treats re-applying a destination's current state as a success (deliberately — that is receipt idempotence), so "the planner only passes `pending` legs" is a fact about one caller, not about the door. Both batch doors now answer `.unchanged` when `updated == target`, which makes `advanced.count` a true statement about movement for every caller of a number nobody can retract. |
| D-8.29 | **H8 drives `.corrupt` and is renamed** `aStoreThatCannotSayWhatItHoldsTransfersNothingAndSaysSo`. A `.deferred` routed store cannot be produced at the manager seam without also failing the session store's ending mark, which would take the `blockedByStore` path and assert nothing about custody. Both states map to `suppression == .storeUnavailable` and make the same claim. |
| D-8.30 | **H25 is a value contract plus a source wall** (`aRecordThatWasNeverEmittedSkipsThePush`), not a manager scenario: every lever that makes the emit fail also fails the ending mark first, and there is no seam for a signing identity that refuses. |
| D-8.31 | **A pre-existing cross-suite audit flake was fixed, not muted.** `aReSentChunkIsLoggedAsADuplicateNotAnAdmission` asserted an exact list over the **process-global** `FernletAuditLog`, whose line carries no item discriminator; item 8's scenario suite made it deterministic. The claim is kept whole (a re-sent chunk is *named* a duplicate; no verdict outside the vocabulary reaches the line) and a deterministic non-global assertion added beside it. Adding an item id to the audit context was rejected — audit context is counts and frozen tokens, never content. Item 8's own audit assertions are negative-only, with `lastDevelopmentHandoff` carrying the positive half. |
| D-9.1 **One cap value** | `MeshRoutedCapacity`, `.production` defined **as** `MeshRoutedStoreFormat` (itself `MeshRoutedManifestFormat.maxContentByteCount` + `MeshChunkFormat.maxChunkCount` + `MeshMembershipBounds.maxRosterMembers`); injected via `MeshRoutedStore.init(scope:capacity:)`, default `.production`, stored on the store as an internal `let` and **read back by everything that accounts** (`MeshRoutedCapacityUsage(index:at:capacity: store.capacity)`), so the doors and the accounting are one model. No `@TaskLocal`, no `static var`, no shipping knob. The at-rest guards keep reading the format constants — a `Codable` init has no injection point and an injected cap is always ≤ production (`injectedBoundsDoNotReclassifyAnAtRestIndex`). | 2026-09-04 |
| D-9.2 **"Own items" is walled, not built** | Exactly two admission doors (`recordAdmitting` / `recordStaging`, one caller each), both carrying `capacityRefusal(`, pinned by `theStoreHasExactlyTwoAdmissionDoors` (2 / 2 / 7 occurrences: three definitions and four capacity-guarded call sites) plus `anOriginAtCapIsRefusedAtTheDoorAMintWillUse`. P6's mint inherits the refusal or fails the wall. | 2026-09-04 |
| D-9.3 **Parked drop rule, ONE clause** | `unknownTypeToken` **and** `sender == manifest.originFingerprint` (checked pre-signature, so bound to the only party the chunk door lets park a set). Every other `MeshRoutedManifestRejection` keeps the bytes, each for a stated reason. **`notADestinationOrHandoff` drops nothing** — its else branch fires whenever the sender is not the origin, so a clause there would let any co-destination delete a hand-off custodian's parked ciphertext with a replayable frame, and binding it to the origin would make it unreachable. Never on a capacity refusal, never on `.unavailable`. Only a **parked** record is dropped, through the existing `dropping(item:reason:)` — no new admission door, and the caller is the guard. | 2026-09-04 |
| D-9.4 **Sweep cadence** | Reclaim at the **drain-exchange entry** (one line from item 8's fourth claim door), budgeted **once per peer per session** in `reGossipedToFingerprints`' idiom, capped at `MeshRoutedDrainBounds.increment1.maxItems` through the new bulk `dropping(items:reason:)` (one load, one seal), plus a **one-item** reclaim per ingested recipient receipt. Expiry needs no roster and therefore gets **three** seams — the drain answer, the next session's ledger arm (`prepareMembershipLedger` / `armJoinerLedger`), and `startSearching()` **while a hold is showing** — because an item expires `hardDeadline + 20 min`, i.e. after the session that could have swept it. Source is `itemsReclaimableAsCustodian` ∩ `everyDestinationDelivered(_:in:)`, never `itemsFullyDelivered` / `isAcknowledgedLocally`; the filter runs BEFORE the prefix. No quarantine caller, no timer, no sweep in `load()`; every seam guards on `case .loaded`, and the drain seam spends its budget only **after** the store has answered (D-9.15). The orphan sweep does get a caller, inside the same budget (D-9.14). **Named residual:** a device that leaves and neither searches nor joins again holds expired ciphertext until it does — item 10's unlock/foreground re-entry is the earlier seam. | 2026-09-04 |
| D-9.5 **Visibility** | One **observed** `MeshRoutedDeliveryHold` (frozen cause + count + instant) on `MeshNetworkManager`, derived from three facts kept apart (`routedHeldBackKeys`, `routedUncompletableCount`, `routedUnplacedKeys`) under a fixed precedence (`.storeFull` > `.notPlaced`), so one count never unions two facts. Both key sets are bounded by `MeshRoutedStoreFormat.maxItems` and **name that bound** in an audit line (`mesh.routedDrain.heldBackSetFull` / `mesh.development.unplacedSetFull`), so `itemCount` saturates rather than lying. Item ids stay in the untouched internal diagnostic seams. Copy is app-side, `LocalizedStringKey`, **one sentence pair per cause**, dismissal keyed to the fact rather than a flag (D-9.16), and **no count is interpolated into any key** — so no plural variation is owed at all; if the owner wants the number in the copy, that key needs a `variations.plural` block plus a `pluralRuledKeys` entry at catalog-sync time. a11y id `friends.deliveryHold`, frozen. `deferred` / seal-`refused` / `corrupt` set no hold. | 2026-09-04 |
| D-9.6 **No refund inside a session** | A refused key stays in `routedRefusedKeys` even after a sweep frees room; reclaimed capacity is spendable on *other* items only. Distinct from the hold **release** (D-9.8b): the observable is a claim about the store's current state, not about the refusal's history — `theHoldClearsWhenTheSweepFreesTheStore` asserts both halves in one cell. | 2026-09-04 |
| D-9.7 **Over-commit made visible, not fixed** | `MeshRoutedCapacityUsage.uncompletableItemCount` warns via the census line and the hold; it never refuses, drops or reserves (a durable reservation would need at-rest state). **Arithmetic stated:** an item counts when its own remaining need exceeds what its siblings have not already spoken for — equivalently `heldBytes + Σ remainingNeed > maxContentBytes` and its own need is non-zero. Exact where a manifest is held, an **upper bound** while parked, which is precisely why it warns rather than refuses. | 2026-09-04 |
| D-9.8 **Clearing, in two halves** | (a) the hold and its key sets clear at `prepareMembershipLedger` and `armJoinerLedger` only — **not** in `clearRoutedDrainState()` / `leaveMesh` — because the Friends tab is the screen a tester reaches *after* leaving the session that produced the refusal; (b) **release**: any sweep recomputes the usage and clears a `.storeFull` hold once `hasRoomToAdmit` is true again, and drops `.notPlaced` keys whose record is gone. The expiry-only seams pass `releaseOnly: true`: they may clear but never set. A surface that keeps asserting a condition the store has escaped is the same defect as an invisible refusal. `routedSweptFingerprints` clears at the same three sites as `reGossipedToFingerprints`. | 2026-09-04 |
| D-9.9 **`chunkCount` guard** | Added as the fourth at-rest sibling (`capacityExceeded("chunkCount")`), schema unchanged, `Int(UInt32)` rather than `UInt32(Int)` so nothing can trap. The memberwise `MeshRoutedIndex(items:)` stays deliberately unguarded — the doors own the caps and the handoff/claim appliers only replace present records — and item 5's cell doc comment was corrected in the same commit, since it asserted in prose that the scalar was unguarded everywhere. | 2026-09-04 |
| D-9.10 **Paperwork** | Nothing persisted: no wipe row, no delete-all writer, no `UserDefaults` key, routed index schema stays 2, `MeshSessionContext` stays 2, no wire change, no purpose, no golden, no framing case. Banner dismissal is `@State`. Banner dismissal stores the dismissed hold, not a `Bool`. Shipped in the same commit: a `Docs/FileIndex.md` row per new file — **all five, the two new test files included**, the two new `Mesh/` files added to `theRoutedStoreNamesNoDecryptionSeam`'s hard-coded list (it asserts `scanned == files.count`, so a wall that finds fewer files has stopped looking), the ProximityKit landing page (narrative + five topic bullets) and `Docs/ProximityFunctionIndex.md` (a new section plus five manager rows and the bulk-drop row). | 2026-09-04 |
| D-9.11 **Unrestorable deliveries: counted, audited, expiry-collected** | Never repaired and never a banner cause. `unrestorableItemCount` joins `MeshRoutedCapacityUsage` and the census line, which is the "cap accounting" half `MNM`'s own comment assigns to item 9. These items are invisible to every enumerator (`deliveryTarget` is nil), so they can never be reclaimed and hold their caps until expiry. Repair stays a separate, token-holding decision. | 2026-09-04 |
| D-9.12 **The reclaim's blast radius reaches TWO existing cells, and both are repaired here** | The origin's own copy becomes reclaimable the instant every destination is positively `delivered` (an origin is never a destination), so a fully delivered item disappears from the origin's index and `outstandingDestinations` answers `[]` for a **missing** record. `MeshRoutedDrainConvergenceTests`' progress claim and `MeshRoutedDrainTests.aHealedLinkDeliversManifestChunksAndBothReceipts` were both satisfiable by a deletion; both now read a three-state `MeshRoutedDeliveryProgressState` and accept `.reclaimed` **only** together with an audited `mesh.routedStore.itemDropped ["reason": "delivered"]`. `routedInvariants` gained the courier disjunct at the same time, so item 14 inherits a battery whose progress half a deletion cannot satisfy. `MeshRoutedDrainWallTests.theDrainStateIsClearedAtExactlyThreeSites` names two declarations by exact source text and both gained `now:`; the wall's strings moved in the same commit and `prepareMembershipLedger`'s signature was kept on one line so the wall still matches a whole declaration rather than a prefix (a prefix would also match a call site). | 2026-09-04 |
| D-9.14 **The release predicate measures what the DOOR measures, and the file cap gets a recovery route** | `MeshRoutedCapacityUsage` carries `directoryFileCount: Int?` and `hasRoomToAdmit`'s file clause is `max(chunkFileCount, directoryFileCount) < maxHeldChunkFiles`, exactly as `MeshRoutedCustody.swift:616` refuses; a nil (unlistable) directory answers **false**, never "room". The value can only be built through `MeshRoutedStore.capacityUsage(of:at:)`, which supplies that store's cap model *and* that store's directory, so the two halves cannot drift again. `sweepingOrphanChunkFiles()` gains its **first production caller**, `sweepRoutedOrphans`, inside the existing once-per-peer budget and gated on evidence (`chunkFilesFailed > 0`, or a directory holding more than the index names), ≤ `routedOrphanSweepPasses` (2) while `sweptToCeiling`. The design's premise for skipping it — "every orphan-making writer removes its own file" — is refuted by `removeChunkFiles` *counting* an unlink failure and by every drop verb saving the index before it unlinks. Still **no** `quarantineCorruptIndex` caller. | 2026-09-04 |
| D-9.15 **A budget is spent after the work is possible, never before** | `sweepRoutedCapacity` loads first and inserts into `routedSweptFingerprints` second. A `deferred` / `corrupt` / seal-`refused` store did no work, so burning that peer's one entry stranded the reclaim — and with it the hold's release — for the rest of the session; on a two-node mesh, for every peer there is. Not the `reGossipRecords` idiom, where the work after the insert is unconditional: here the guarded state is transient and recoverable inside the same session. Item 10 inherits an unspent budget at the unlock/foreground re-entry it owns. | 2026-09-04 |
| D-9.16 **The dismissal is keyed to the FACT, not a flag** | `RoutedDeliveryHoldDismissal.hides(_:dismissed:)` — same cause at a count that has not grown — plus `@State private var dismissedHold`. The banner sits at a fixed position in `FriendsView`'s stack, so a `Bool` would have silenced **every** later routed hold for the process lifetime, a different cause with different copy included: the wall's own failure mode one layer above the manager, and the mirror of the release rule. The instant is deliberately not part of the identity (the manager re-derives the hold on every event). `cacheWarningBanner`'s bare flag stays fine for a single monotone photo count. | 2026-09-04 |
| D-9.17 **Only the three STORE-level caps may raise a user-visible hold** | `routedStoreFullRefusals` = `.capacityItems` / `.capacityBytes` / `.capacityChunkFiles`, a strict subset of the six `routedCapacityRefusals`, pinned by `everyCapacityRefusalIsInTheDeviceIsFullSet`. `.capacityReceipts` and `.capacityRecipientReceipts` are raised only **against an existing record**, so counting them beside `routedUncompletableCount` could count one item twice, and `hasRoomToAdmit` cannot see a per-item cap at all, so a sweep would drop a hold whose door still refuses. All six keep narrowing the entitlement and auditing by name — they are simply not "this device is full" for a reader. The "disjoint by construction" doc comment, false for two of the six, is corrected. | 2026-09-04 |
| D-9.18 **Two cells that could not fail were replaced, not renamed** | The battery's `.reclaimed` disjunct was true for *any* absent record — the same condition that makes `holdsSomething` false — so "nothing lost" passed for a destination's deleted record while its sibling cell already demanded corroboration for the same answer; `routedInvariants` now takes the run's audit capture and accepts `.reclaimed` only with `mesh.routedStore.itemDropped ["reason": "delivered"]`. And `theBannerIsGatedOnTheHold` only read back the memberwise initialiser (the gate could be deleted outright and it stayed green); it is replaced by `theBannerGateAndItsMountAreBothReal`, a source wall over the body's gate, the stored-fact dismissal, the absent `.accessibilityElement(children: .combine)`, the single `ConnectView` mount and its position **above** the `meshPhotos.isEmpty` branch. The `.combine` itself was removed: the card contains the dismiss `Button`, and combining strips its `.isButton` trait, its VoiceOver focus and the `fernletIconButton` label (A1/A7 cover `.ignore` on a Button, not `.combine` over one). | 2026-09-04 |
| D-9.13 **`noteRoutedHeldBack` is `internal`** | The set's bound is 1024 distinct signed refusals away from any wire-driven cell, and a bound asserted only in a comment is not asserted at all. Internal in `receiveRoutedInventory`'s idiom, with production reaching it from exactly one place; no `ForTesting` seam was added and no other member of the family moved. | 2026-09-04 |
| D-10.1 push vs host pull | **Push**: `public func applyRoutedAccessGate(_:now:)` on `MeshNetworkManager`, **six** app call sites in `FernletApp.swift` (the launch mount in the `.ready` branch, the scene `.active`/`.background` branches, both existing `protectedData*` `.onReceive`s, and a new `.onChange(of: lockService.isDuressSessionActive)`). A `ProximityHost` property would be a pull with no edge, and the re-entry is defined by an edge; the push is also P7's `apply(_:)` shape, so `ProximityRunPolicy` becomes its single writer without the seam moving. | 2026-09-04 |
| D-10.2 "may seal custody" | Answered by the **store's five states**, never by the gate: `indexForWriting()` vends a `LoadToken` only from `.loaded`/`.absent`, which is the type-level form of the rule. `MeshRoutedCapability.sealCustody` exists so the question has a named, tested answer (`permits` returns `true` in all 8 fact combinations), and a grep-wall asserts no custody door calls it. | 2026-09-04 |
| D-10.3 which lock gates what | **iOS data protection** gates plaintext (decrypt + canonical-store mutation) and, at the store, readability. **Fernlet's app lock gates nothing in the mesh** — no `FernletLockScope` covers Friends and ProximityKit cannot import `FernletLock` — with ONE clause: a duress session closes the gate. Duress is observed on **its own** `.onChange`, because it is entered at an already-foreground lock screen and cleared by a real-passcode unlock in the same foreground (it survives `lock(reason:)`, `FernletLockService.swift:1229`), so it moves at neither a scene nor a protected-data transition. `ContentView.swift:151` is the precedent. | 2026-09-04 |
| D-10.4 foregroundness (D-4.16) | The pushed `appIsForeground` leg is the enforcement. `sessionState == .activeForeground` is ANDed into **`mayCommitRoutedHeartLedgerJudgement` only** — the one stage whose source demands it (`MeshRoutedAck.swift:218-222`) — and deliberately **not** into the decrypt predicate: `sessionState` is `.idle` after `leaveSession` while routed ciphertext outlives the session (expiry = `hardDeadline + 20 min`, D-9.4), and `durableRecipientStorage` states in its own doc that no decrypt and no foreground are in its condition. Item 10 does NOT fire `.backgrounded`/`.foregrounded` (that asserts a CPT is running — P8's claim), so that leg is documented **inert-until-P8**. | 2026-09-04 |
| D-10.5 the re-entry edge | Either **rising ciphertext leg** (protected data OR foreground) **or the duress falling edge** (`MeshRoutedAccessEdge.runsPass`), not `isOpen`'s edge. Jobs 1–3 and 4a/4b are ciphertext-only and must run on an unlock even while the app is backgrounded; job 4c is what the duress fall owes, because duress is the only fact that closes the gate without any other leg moving — so its clearing is the only moment the deferred heart stage would otherwise wait for an unrelated lock/foreground cycle. | 2026-09-04 |
| D-10.6 claim doors | Item 8's **fourth door stays**; the re-entry is a **fifth caller** of the one idempotent derivation. `retrySessionRestoreIfPending(now:)` — whose own doc says "called on unlock or foreground, never on a timer" and which had **no production caller** — becomes job 1, gated on the protected-data rising leg **and `sessionState == .idle`**: `restoreSessionContextAtLaunch` calls `startSessionCeiling` (re-arming `sessionMonotonicOrigin` and re-deriving the monotonic budget) and re-assigns `knownEpochHeads` **before** `applySessionEvent(.contextRestored)` reaches `restoreOnlyFromIdle`, so a mid-session edge would re-grant the manipulation-resistant half of the ceiling and then be refused. Alternative (leave it to P7) recorded. | 2026-09-04 |
| D-10.7 durable provenance | **Both stay memory-only.** `deferredCustodyCommits` is now re-derivable from durable state — the new pure enumerator `MeshRoutedIndex.itemsWithUncommittedOwnCustody(at:for:)` reads the **rung**, not the plan, so a restart costs latency only. `originServedItems` stays memory-only because it is a friend-graph fact (wipe row + delete-all + duress pre-draw for a hop bound), because a persisted "the origin served me this" makes a restart MORE trusting, and because a new sealed index field cannot be written before first unlock — absent in exactly the window that motivates it. Residual stated plainly: a restart between taking the origin's bytes and claiming forfeits that claim and it is **not** re-derivable once the origin departs. Override cost: index schema 2→3 (older = corrupt), wipe row + delete-all + duress decision in the same commit. | 2026-09-04 |
| D-10.8 routed seal key class | **Unchanged `AfterFirstUnlockThisDeviceOnly`; no migration owed.** Strengthening to `WhenUnlocked*` makes every background custody write unsealable and therefore unacknowledgeable (§3.6) — it deletes the feature; weakening to a syncable class is prohibited and pointless (V3 ciphertext authenticates a `ThisDeviceOnly` `DeviceBindingID`). The answer to a future "background decrypt" is **no**, never a class change. Migration cost, if ever taken: `kSecAttrAccessible` is fixed at `SecItemAdd`, the key is written once at mint, so it is a read-old/rewrite-new pass with read-back verify (the `identity.escrow.promotedLocal` ordering is the safe precedent); population today is one device with test data only — state it, never argue from it. | 2026-09-04 |
| D-10.9 the reserved hold cause | **Declined.** D-9.5/D-9.17 keep the user-visible hold to the three store-level capacity refusals, and six app display literals are already blocked on the owner. Locked-device visibility ships as counts + frozen tokens (`routedAccessGate`, `MeshRoutedReentryReport`, `mesh.routedStore.readSuppressed` / `sweepSuppressed`, `mesh.routedAccess.*`); `MeshRoutedDeliveryHoldCause`'s reserved-case doc comment is updated to record the decision. Owner question, not a silent drop. | 2026-09-04 |
| D-10.10 paperwork | Nothing persisted: no `Docs/PrivacyWipeCoverage.md` row, no delete-all writer, no `UserDefaults` key, routed index schema stays **2**, `MeshSessionContext` stays **2**, no new wire vocabulary/purpose/golden/framing case. Owed regardless: the new `Mesh/` file joins `theRoutedStoreNamesNoDecryptionSeam`'s list (in `MeshRoutedCustodyEvidenceTests`; it asserts `scanned == files.count`), a **`Docs/FileIndex.md` row** (every prior P5 file has one), a `Docs/ProximityFunctionIndex.md` section **plus a rewrite of the existing `routedIndexForAdvertising()` row at `:1033`** (E3 renames the symbol), the ProximityKit DocC landing-page paragraph, `///` on every new type. | 2026-09-04 |
| D-10.11 what the re-entry sweeps | **Only the peers a non-loaded store actually suppressed**, recorded in a new bounded `routedSweepsDeferredFingerprints` (capped at `MeshMembershipBounds.maxRosterMembers`, cleared everywhere `routedSweptFingerprints` is). Sweeping every reachable roster peer would spend D-9.4's once-per-peer budget for the whole session on the **common** case (§0: after first unlock a locked device's store is `loaded`, so nothing suppressed those sweeps) and pre-empt the drain-exchange seam's bulk `dropping(items:reason:"delivered")` **and** `sweepRoutedOrphans`, the file cap's only recovery route. D-9.4's retention residual is closed by the roster-free `sweepRoutedExpiry` — the only half that can run at all after `leaveMesh()` nils `membershipVerifier`. | 2026-09-04 |
| D-10.12 how many predicates | **Three, each defined once and walled to one definition:** `mayDecryptRoutedContent` and `mayMutateCanonicalStoreWithRoutedContent` are the gate at the **same strength** (a plaintext write is not a weaker act than a plaintext read), and `mayCommitRoutedHeartLedgerJudgement` is those two AND `sessionState == .activeForeground`. Named residual: a heart not judged before the mesh ends cannot reach `delivered` and expires at `hardDeadline + 20 min` as `custodied(by: self)` — D-4.5's documented shape, bounded by expiry, handed to P6. | 2026-09-04 |
| D-10.13 the non-heart half of the retry list | **Filed, not counted.** `itemsAwaitingLocalAck(at:for:)` is D-4.19's "a retry list that misses an item strands it forever" and had **no production caller**; map-routed-under-lock §5 R4 hands it to item 10. Job 4 splits three ways: **4a** `deliveredAt != nil` (stamped, receipt unstored via `mesh.routedDrain.receiptNotStored`) → `commitLocalDelivery`, idempotent because `committingDelivery` skips the stage-shortfall check once `deliveredAt` is stamped and re-uses the stored instant; **4b** unstamped, complete, stage `durableRecipientStorage`/`immediate` → commit for real (D-4.4); **4c** hearts → the counted, gated no-op P6 replaces. Whole list capped at `MeshRoutedDrainBounds.increment1.maxItems`; the re-entry sends **no frame** — the next exchange forwards, as `mintClaimedCustody` already documents. | 2026-09-04 |
| D-10.14 how the facts reach the gate | The two protected-data notifications carry the fact **literally** (`false` on will-become-unavailable, `true` on did-become-available); `UIApplication.shared.isProtectedDataAvailable` is read **only** where it is a genuine sample (the two scene branches and the launch mount), because the will-notification is posted *before* lockdown and the property still answers `true` there — reading it would make the falling leg unobservable and the unlock edge impossible. A **launch mount push** in the `.ready` branch is required because `.onChange(of: scenePhase)` carries no `initial:` and the loader typically becomes `.ready` after the `.inactive → .active` edge, so a cold start would otherwise sit on `.closed` all session. Six sites, one grep-wall (W6) keying on the literal values and the duress observer. | 2026-09-04 |
| D-10.15 `MeshRoutedIndexReadReason`'s size | **One case per real caller class** — `routedIndexForAdvertising()` has **seven** call sites (`MNM:1367, 3350, 4237, 4283, 4343, 4421, 4579`), so the vocabulary is `handoffPush` / `advertise` / `rung` / `courier` / `claim` / `commit` / `drainPlan`, with `logsSuppression == false` for `.rung` and `.courier`: those two are per-item predicates inside the drain's loops, and a suppression line each would be one-per-item-per-pass — the noise E3 exists to remove, at a finer grain. `MeshRoutedIndexReadReason` and `MeshRoutedSweepVerb` both live in `MeshRoutedAccessGate.swift` (item 10's pure-vocabulary file), each with `///`. | 2026-09-04 |
| D-10.16 does an `.absent` store suppress a sweep? | **No — and this was a real defect, caught by the cell written for D-10.11.** `loadedIndexForSweep` already documented `.absent` as "nothing to sweep, not a suppression" and logged nothing for it, but `sweepRoutedCapacity`'s non-loaded exit recorded it in `routedSweepsDeferredFingerprints` anyway. On a **fresh device** — the commonest state there is — every peer's exchange would have entered that set, and the first unlock's re-entry would then have spent every one of their once-per-session capacity budgets, pre-empting exactly the drain-exchange seam D-10.11 exists to protect. The helper now answers `(index:suppressed:)`, and only `deferred` / `corrupt` / seal-`refused` are recorded. | 2026-09-05 |
| D-10.17 how does W2 enforce "no ungated plaintext seam" — per-file containment, or a pinned count? | **A pinned count, module-wide, plus containment as the forward rule.** Containment alone (a file naming the seam must also name the predicate) can never fire in `MeshNetworkManager.swift`, which **defines** all three predicates — and W3(b) forbade the token everywhere else under `Mesh/`, so the pair was closed and the tripwire could not fire at all. W2 now sweeps `FernletKit/Sources/ProximityKit` (the whole module — `MeshRoutedContentKeyWrapper.unwrap` is internal to all of it) and pins each seam spelling's occurrence count: `MeshRoutedContentKeyWrapper.unwrap(` = 0, `MeshRoutedHeartAck(` = 0, `.heartLedgerCommit(` = 1 (the stage precondition's `guard case` in `MeshRoutedDeliveryCommit.swift`, which reads evidence and judges nothing). The claim is now the honest one — *no such seam exists yet* — and it fails wherever the first one is written. The old heart needle `MeshRoutedAckEvidence.heartLedgerCommit` matched **nothing** in the tree; every real site uses the leading dot. | 2026-09-05 |
| D-10.18 W2(a) and W3(b) were mutually unsatisfiable — which one gives? | **W3(b) exempts a file that performs a routed decrypt.** W2's containment half *requires* such a file to name `mayDecryptRoutedContent`; W3(b) forbade that token outside the gate file and the manager. Any new `Mesh/` file doing a routed decrypt therefore failed one wall or the other however it was written, silently forcing every routed decrypt into the ~10k-line manager — a concentration no doc, design §7.2 or §11 ever stated. W3(b) now skips a source containing `MeshRoutedContentKeyWrapper.unwrap(`. `routedAccessGate` itself stays confined to the manager and the gate's own file: a decrypting file consults the **predicate**, never the gate value. | 2026-09-05 |
| D-11.1 registry vs table | the registry OWNS the rows; `MeshRoutedAckStageTable` becomes its projection (`registry.ackStages`), `MeshRoutedAckStageTable.increment1 = MeshRoutedTypeRegistry.increment1.ackStages`; `committingDelivery(…stages:)` keeps D-4.7's signature | 2026-09-05 |
| D-11.2 where | new file `Mesh/MeshRoutedTypeRegistry.swift`; the one-table grep-wall's construction site moves to it in the SAME commit with its doc rewritten; `MeshRoutedTypeToken` stays in `MeshRoutedAck.swift` | 2026-09-05 |
| D-11.3 key | `String`-keyed (the token is wire and is stored verbatim); `entry(for:) == nil` IS "unknown", the one answer everywhere; no fourth answer added | 2026-09-05 |
| D-11.4 per-type size cap | **mint only** in item 11, on the COST argument, not on relative reachability: a receiver-side check adds a new `MeshRoutedManifestRejection` case that forces `MeshRoutedParkedDrop`'s exhaustive switch to re-decide D-9.3's drop rule now, for a condition no increment-1 row can produce (every per-type cap equals the wire cap). Receiver check + rejection case + non-dropping drop arm are P6's, in the same commit as its first narrowed cap. Overrides the maps' "both" default. NB the mint has no shipping caller at all, while the verifier runs on every inbound manifest — so "the mint is more reachable" is NOT the reason | 2026-09-05 |
| D-11.5 mint acceptance | the mint does NOT refuse an unregistered token (acceptance is receiver-side by D13, and a mint gate would break the `nobody-registered.v1` cell + 8 fixture files minting `…golden-fixture.v1`, violating "existing tests green unmodified"). Documented asymmetry, asserted by a test | 2026-09-05 |
| D-11.6 injection | `signed(…types: = .increment1)`; verifier keeps `acceptedTypeTokens:`; ONE manager seam `routedTypeRegistryForTesting` (internal, `@testable` only) — what makes the two build-narrowed doors and the fourth-token cell reachable | 2026-09-05 |
| D-11.7 relay-retention increment 2 | `.relayInFlight` exists and is **unregisterable**: `init(entries:)` drops such an entry, so its token answers nil at every door. Zero hop plumbing | 2026-09-05 |
| D-11.8 destination semantics | `.singleRecipient` is registerable but **unmintable** — the mint refuses `unsupportedDestinationSemantics` (P4 withheld the `MeshDeliveryTarget` subset initializer on purpose); P6 flips the row when it lands. The asymmetry with D-11.7 is DERIVED, not asserted: `destinations` is mint-side derivation policy with **no receiver-side reader** — the manifest carries its destination set on the wire and the verifier binds wraps ≡ destinations from those bytes — so a registered `.singleRecipient` row cannot fall through to increment-1 behaviour on receive. Pinned by test, not prose | 2026-09-05 |
| D-11.9 expiry | one rule `.meshHardDeadlinePlusGrace`, read by the mint, delegating to the ONE formula; not editable in increment 1 — D6's exact floored equality is checked WITHOUT a rule lookup at **four** shipping verifiers (`MeshRoutedManifestVerifier:147`, `MeshChunkVerifier:154`, `MeshCustodyReceiptVerifier:139`, `MeshRecipientReceiptVerifier:143`), which is what makes a per-type grace a fleet-wide flag day and names the four sites P6 must change together | 2026-09-05 |
| D-11.10 decrypt gate | **derived**, `entry.requiresForegroundDecryptBeforeFinal == (finalAck == .foregroundDecryptAndLedgerCommit)`; replaces the manager's bare stage compare at `:4492`. The registry file names no gate symbol (W3(b)) | 2026-09-05 |
| D-11.11 canonical store | slot SHIPPED as a frozen token enum (`.friendPhotoWall`/`.sessionTranscript`/`.heartLedger`) — a token, never a closure or a store type; doc says "read by P6's dispatch, by nothing today", pinned by test | 2026-09-05 |
| D-11.12 control | stays reserved and unregistered, through the registry and its derived table | 2026-09-05 |
| D-11.13 at-rest unregistered record | held, never offered, never forwarded, never asked for, never claimed, never acked, collected by expiry — NOT dropped (item 9's clause stays origin-bound, D-9.3) | 2026-09-05 |
| D-11.14 the new doors | drain offer (`mayCourier` + `handoffEntitlement` inside `offerableKeys`, which the departure push INHERITS — `handoffPushBatch` narrows through `offerableKeys(...).intersection(pushable)`, it does not build its own offer set), the answer builder's receipt + ask half (D-11.18), and the hand-off claim (`claimHandedOffCustody` filters the planned claims; `MeshCustodyHandoffPlan` keeps its signature). All UNREACHABLE in one shipping build — reachable only across a narrowed registry — and documented and tested as such | 2026-09-05 |
| D-11.15 value name | `increment1`, matching `MeshRoutedAckStageTable.increment1` / `MeshRoutedDrainBounds.increment1` — "current" invites an edit, "increment1" invites a second value beside it, which is what P6 does | 2026-09-05 |
| D-11.16 the entry cap | `MeshRoutedTypeRegistry` declares its OWN `static let maxEntries = 16`; the tie to `MeshRoutedAckStageTable.maxRows` is asserted from the TEST target, never read across in shipping code. Reading it across would put the fragment `MeshRoutedAckStageTable.maxRows` into shipping ProximityKit source and turn `shippingCodeNamesOneAckStageTable`'s `.increment1`-only member assertion red on item 11's first build; inverting the read trips the new one-registry wall instead. The one constant this item restates, and it says so | 2026-09-05 |
| D-11.17 the declared cap's bound | enforced BY CONSTRUCTION: `init(entries:)` drops any entry whose `maxItemByteCount` is outside `1 … MeshRoutedManifestFormat.maxContentByteCount`, exactly as it drops `.relayInFlight`. A comment would have let a row declare a cap above the wire bound (silently overridden by the mint's surviving global guard) or `0` (refusing every item of its type with no diagnostic) | 2026-09-05 |
| D-11.18 receipts + ask | the offer gate alone does NOT satisfy "never forwarded": `MeshRoutedInventoryDelta.receiptsToForward()` takes no entitlement argument and `MeshRoutedDrainPlan` filters it only by the peer's refusals, so origin-signed custody/recipient receipt refs for an unregistered item would still cross. Closed at the MANAGER by unioning the unregistered-key set into the `refused:` set both `MeshRoutedDrainPlan` construction sites already take (drain answer + departure push) — one line each, no signature change, item 5's/6's pure values and tests untouched. The union also drops those keys from `delta.ask`, deliberately: a build that does not register a type does not spend budget completing it | 2026-09-05 |
| D-11.19 the ack pass's positive control | the re-entry ack pass does NOT commit a destination's own first custody — job 2's `itemsWithUncommittedOwnCustody` recovers only legs already marked `custodied(by: self)` (item 8's handed-off custody) — so every "no ack for an unregistered type" cell commits the node's own custody explicitly first and is paired with `aRegisteredTypeIsAcknowledgedAtTheSameDoor`, which asserts `acksFiled == 1` for the identical setup with a registered type. Without the pair the `== 0` cells hold for a registered type too | 2026-09-05 |
| D-11.20 the offer refusal's count | `mesh.routedDrain.unregisteredTypeNotOffered` counts exactly the refs whose registry lookup answers nil, NOT "complete keys the gates removed" — the latter also counts increment 1's ordinary entitlement line and the peer's capacity refusals, so the line would fire on healthy drains and mean nothing | 2026-09-05 |
| D-11.21 the chunk door | a HELD item's chunk is refused when this build does not register its manifest's token (`ingestRoutedChunk`, one clause after the retention guard, audited `reason: unregisteredTypeChunk`); a PARKED set keeps having no answer, because a chunk carries no token and item 9's origin-bound clause disposes of it. "Holds what it already has" means held, **not grown**: a narrowed build was otherwise staging chunks toward `manifest.size` for an item it can never ack, offer, forward or claim, spending the 256 MiB / 1024-item caps and able to raise item 9's `.storeFull` hold against types that ARE registered. Consistent with D-11.18, which already decided such a build does not spend budget completing the item. Taken at the review round | 2026-09-05 |
| D-11.22 the expiry column's test gap | `MeshRoutedExpiryRule` has ONE case, delegating to the same `MeshRoutedManifest.expiry(afterHardDeadline:)` the pre-item-11 mint called, and the mint falls back to it for an unregistered token — so no cell can discriminate "reads `entry.expiry`" from "ignores the registry" until a second rule exists to inject, which increment 1 deliberately does not add (D-11.9). The cell is renamed `theMintsExpiryIsStillTheOneSharedFormula` (it pins D6, not the column) and the gap is a P6 hand-off rather than a name that overclaims. Taken at the review round | 2026-09-05 |
| D-11.23 the wall's needles are data | wall 3's `allowed` dictionary is hoisted to `MeshRoutedStoreIsolationTests.routedTokenNeedles` and BOTH the wall and its scanner self-test read that one value; each branch fixture is written so exactly one needle catches it, and the member scanner's blind spot is fixtured from both sides (the inferred form is invisible; `MeshNetworkManager.swift` and `MeshRoutedManifest.swift` are asserted to spell `MeshRoutedTypeRegistry.increment1` in full). A fixture that re-types a needle as a literal beside its own assertion cannot fail — four of the first draft's six did exactly that. Taken at the review round | 2026-09-05 |
| D-12.1 what is keyed | `(meshID, AUTHOR, contentID)` at the four content doors: manifest `itemID` → `originFingerprint`; chunk `chunkID` → `chunk.originFingerprint`; custody `receiptID` → `custodianFingerprint`; recipient `receiptID` → `recipientFingerprint`. Never an epoch, group key, branch, partition or roster version, and never the forwarding envelope's sender — `chunkID` omits the origin precisely because the window separates by author (C3/C4), so keying on the envelope sender would both collide two origins' chunks and admit one origin's chunk twice via two custodians | 2026-09-05 |
| D-12.2 probe vs record | **Two calls, not one.** `verdict(…)` (new, non-mutating) is each door's FIRST statement, before the verifier and before the first sealed-index load, run on the unverified frame — safe because it records nothing, so an attacker can neither place an id under another author's axis nor evict anything; `admit(…)` records after the store's door answered, on the author the verifier authenticated. Resolves the maps' contradiction (admit-before-verify vs "never feed it a claimed fingerprint") without weakening either half | 2026-09-05 |
| D-12.3 the record gate | Recorded **iff** the outer outcome is `.completed` AND `settled` — `.unavailable` (deferred/corrupt/seal-refused), every `.refused` (item 9's capacity family included) and an unfinished rung all stay re-offerable, or the peer's honest re-offer would be answered `replayed` and the item could never complete. INNER refusals inside `.completed` (`duplicate`, `conflictingChunk`, `chunkHashMismatch`) ARE recorded — gate on the outer case only, never sub-branch — because the ids are derived from identity, not content | 2026-09-05 |
| D-12.4 `settled` comes from `finishLocalRungs` | It becomes `@discardableResult … -> Bool`: false at "the store could not say / holds no manifest" and at "a commit produced nothing while a rung was outstanding", true at "nothing outstanding". A door that never reaches the completion branch passes `settled: true`. The rejected alternative — re-reading `routedRungsOutstanding` at the door — costs a sealed-index load per chunk (the exact I/O the window exists to avoid) and reads "outstanding" forever for a courier with no honest claim | 2026-09-05 |
| D-12.5 both bounds, both derived | frames = `MeshRoutedDrainBounds.sessionFramesPerPeer` (1056 ≥ 1024 chunks + manifest + both receipt kinds = 1027), the outbound session budget's inbound mirror; authors = `MeshMembershipBounds.maxRecordsPerKind` (16), the **admission set's** capacity, NOT `maxRosterMembers` (8). Correction, not preference: all four routed verifiers resolve the author's key from `ledger.admissions.all` and deliberately do not require current roster membership (a departed origin's content stays valid and keeps moving under item 8's transfer), and at 8 the arithmetic is already saturated by one item on a full roster — the 9th author would answer `senderWindowFull` forever and never be caught on repeat. ≈ 520 KiB worst case | 2026-09-05 |
| D-12.6 `senderWindowFull` is a named degradation, never a refusal | Only `.replayed` is actionable; `expired`, `foreignMesh`, `senderWindowFull` and `admitted` all fall through to the unchanged verify-and-store path. So no legitimate frame can be dropped by this defence at any window size — and the naive wiring's DoS goes with it. Named at the record site as `mesh.routedDrain.replayWindowFull` with a frozen `axis` token (`frames` / `senders`), because honest volume and "the admission set outgrew the cap" are different faults | 2026-09-05 |
| D-12.7 the hop bound rides the replayed path | `ingestRoutedManifest`'s `if context.sender == manifest.originFingerprint { noteOriginServed(key) }` runs on the `.replayed` return too. The probe is sender-blind, so a custodian's copy followed by the origin's own would otherwise drop the origin's frame, leave `originServedItems` without the key and strand that leg at the origin's next departure (`handoffClaimNotOriginServed`). Safe on an unverified frame: a `replayed` verdict means a VERIFIED frame recorded the pair, and `context.sender` is the envelope-authenticated committed slot. Item 8's own wall was amended to a per-DOOR claim in the same commit (count 2→3 manager-wide, exactly 2 inside the door) | 2026-09-05 |
| D-12.8 the un-record | `forget(frameID:from:)` is new and shipped for ONE reason: the store gives a chunk slot back on a repair (`repairing(_:dropping:in:token:)`), the drain makes a peer re-offer that exact chunk under the identical derived id, and without it the slot could never be refilled for the session — no complete item, no witness, no receipt, no delivery. Two observation sites: `sendRoutedChunks`' `slotNotHeld` / `chunkFileMismatch` arms (the exact slot) and `commitLocalCustody`'s repair arms (the item, so the whole derivable id family). Over-forgetting costs replay coverage; under-forgetting costs the delivery | 2026-09-05 |
| D-12.9 `forget(senderFingerprint:)` is never called on the routed window | The type's own doc offers it for departure/removal, which is right for CONTROL frames. Under increment 1 a departed origin's content is exactly what a custodian keeps forwarding after item 8's hand-off, so releasing that axis would hand an attacker a free replay of the content custody transfer exists to keep moving. Affordable only because D-12.5 sized the axis on the admission set. Removal is arguable and also declined — the store refuses a removed origin's content on its own | 2026-09-05 |
| D-12.10 cleared at the three session resets only | `routedReplayWindow = nil` inside `clearRoutedDrainState()`, plus an implicit rebuild when `context.meshID` changes so a stale-mesh window cannot answer `foreignMesh` all session. **Not** from `abandonMergeExchange` (D-6.6), and the argument is stronger here than for the frame budget: clearing on a flap is precisely the eviction this type refuses to do, handed to an attacker as a free primitive | 2026-09-05 |
| D-12.11 nothing persists | No file, keychain account, `UserDefaults` key, routed-index or `MeshSessionContext` field; `MeshSessionContextSchema.current` stays 2 and the routed index schema stays 2. Therefore **no `Docs/PrivacyWipeCoverage.md` row and no delete-all wiring** — adding one for something unpersisted is as wrong as omitting one for something persisted (D-5.11 / D-6.14 / D-7.21 / D-10.10's posture). `PersistedSurfaceWipeBoundaryTests` and `PrivacyWipeCoverageTests` are green unchanged. Named residual: after a force-quit a captured frame replays into an empty window; §3.1's load-bearing case (an item the store already DROPPED) is then unguarded, bounded by item 9's item slot, byte budget and visible hold. Persisting would not close it — invariant 5's foreground resume runs the two clears | 2026-09-05 |
| D-12.12 the digest family stays out (AMENDED by the review round) | `receiveRoutedInventory` (D-5.12) and `receiveRoutedDrainAnswer` (D-6.10) are not admitted, and the inventory's doc sentence was corrected: `MeshRoutedInventoryVerifier` has **no freshness check** — `sentAt` is bound INTO the signature, not checked against a clock, so it stops re-attribution and stops nothing being re-sent. `sentAt` is never cited as a replay defence again. What limits a replayed advertisement is the slot binding `payload.senderFingerprint == senderFingerprint` and `sessionFramesPerPeer` — and those two bound the COST, not the effect: **third fact**, `recordPeerRoutedInventory` overwrites `inventory`/`inventorySentAt` with no `sentAt` monotonicity guard, so the peer's own replayed older digest regresses this device's view of its holdings and `answerRoutedInventory` re-stamps `quiescentLocalAsOf` from that stale instant. Cost = a stale delta (wasted, budget-bounded offers), never a lost or double-counted delivery; the replayer can only ever be that peer itself. Pre-existing (items 5 and 6), named on both functions and on the landing page, and deliberately NOT closed here — a `payload.sentAt >= state.inventorySentAt` guard changes items 5/6's door and the stamp item 7's window rule reads, which is a behaviour change, not a wiring change | 2026-09-05 |
| D-12.13 what the window is NOT | It prevents no double effect — the store was already idempotent and stays exactly so. It adds four things: refusal before the expensive half; memory of what the store deliberately FORGOT (a reclaimed or parked-drop item, whose replay is a fresh admission against the 256 MiB / 1024-item caps — the one place it is load-bearing); a named attack signal; and a bound on a repeat flood of frames that reached the store. It is **not** an expiry gate (guard 2 precedes guard 3, and both store doors refuse an expired item independently) | 2026-09-05 |
| D-12.14 the unbounded channel, named not half-closed | Three door refusals sit BEFORE their store verb and stay replayable without bound: `notADestinationOrHandoff` (1 Ed25519 verify + 1 fingerprint hash per replay), `unknownItemNotFromOrigin` and `unregisteredTypeChunk` (1 sealed-index load each). Two fire before their verifier, where the author is a claim; the third is sender-DEPENDENT, so recording a courier's refused copy would drop the origin's own hand-off copy tomorrow. Closing them needs a per-sender pre-verification budget — a different mechanism with its own DoS question — and is deliberately not started. Stated in `dispatchRoutedContent`'s doc comment | 2026-09-05 |
| D-12.15 post-repair receipt re-mint residual | A custody `receiptID` excludes `custodiedAt`, so the receipt this device re-mints after refilling a repaired slot carries the id its peers already recorded and their windows answer `replayed` — they keep the earlier receipt. Staleness, not a lost delivery (the claim is true again; `repairing`'s own doc calls a sent receipt a point-in-time claim). A cross-device un-record would be a wire change item 12 does not make. Documented on `MeshCustodyReceipt.receiptID` and in the function index | 2026-09-05 |
| D-12.16 the window's mesh id is the SESSION's, and `.foreignMesh` is inert at the routed doors | Both the probe and the record pass `meshID: context.meshID` — `currentMesh.meshID`, gathered once at `dispatchRoutedPayload` — never the frame's claimed `manifest.meshID`/`chunk.meshID`/`receipt.meshID`, and `RoutedReplayRef` carries no mesh field. So the window's `frameMeshID == meshID` guard is a tautology on the routed path and `MeshFrameReplayVerdict.foreignMesh` is structurally unreachable at all four doors. Deliberate, not a gap: the foreign-mesh refusal belongs to each routed verifier (`MeshRoutedManifestVerifier`, `MeshChunkVerifier`, both receipt verifiers), one step later; feeding the window a claimed meshID would move a verifier's refusal into a probe that runs BEFORE the signature. `MeshChunk.chunkID`'s doc had stated the other spelling (`chunk.meshID`, "all four parameters come off the chunk") — corrected there and on the ProximityKit landing page, with the consequence stated rather than left to be inferred | 2026-09-05 |
| D-12.17 B9 asserts its own precondition | `anEpochRotationDoesNotReleaseARoutedFrame` looks the coordinator up (`epochCoordinatorFingerprintForTesting`) instead of assuming node 1, seeds coordinator and receiver on head 2, mints INSIDE the pinned `DeviceBindingID.$testOverride`, `#require`s the successor and that it moved, folds it into the receiver and asserts the receiver is on it — then replays. Without those, the rotation happened in about one run in three (the roster minimum is a random keypair's fingerprint) and never at all without the binding (`persistSessionContext(addingEpochHead:)` abandons the mint), so item 13's stated precondition was a byte-identical duplicate of `aReplayedManifestNeverReachesTheStore`. The rotating device is the UNLINKED third node on purpose: a coordinator with an active slot burns the 10 s `rotationAckWindowSeconds` on a peer no pump answers, and tier 1 spends no wall clock | 2026-09-05 |
| D-12.18 a test seam's `Bool` states what reached the DOOR | `MeshConvergenceRun.routedReplayEvent` resolves the receiver's committed slot in the same `guard` as the manifest and the coordinator (and requires its fingerprint), so `false` covers every way the frame could fail to reach `dispatchRoutedPayload`'s first guard. It previously answered `true` on a nil slot, which would have made B16's `#expect(dispatched, …)` — the one guard between that cell proving item 12 and proving nothing — pass over a frame the door never saw | 2026-09-05 |
| MeshDeliveryTarget persistence | inside MeshRoutedStore: restored from the SIGNED manifest + a sparse progress map (no stored destination list); type stays non-Codable; wipe row + delete-all in the same commit | 2026-09-03 |
| Routed digest wire token | `fernlet.mesh.routed-inventory-digest.v1` = `Signature.meshRoutedInventoryDigestV1` + `PayloadType.meshRoutedInventoryDigest`; no Hash sibling; Swift types `MeshRoutedInventory*` (no 'Digest' stem) | 2026-09-04 |
| Four-state sidecar model | MeshRoutedStore mirrors MeshSessionStore's LoadToken exactly (rawValue sets asserted equal) + §19.5 seal-refused; own schema, own keychain service; MeshSessionContext schema stays 2 | 2026-09-03 |
| Departure delivery ack | (default: still no ack; drain carries custody instead) | — |
| D1 recipient keys | mint takes caller-supplied verified `[fingerprint: X25519 pub]`; refuses by name if a destination lacks one | 2026-09-03 |
| D2 contentHash/size | opaque SHA-256 over the CIPHERTEXT + complete sealed-blob size; item 1 computes neither | 2026-09-03 |
| D3 item seal | not item 1 and not item 2 — the seal is item 6 / P6's; `AEAD.meshRoutedItemV1` stays Reserved; content key from `makeContentKey()` | 2026-09-03 |
| D4/D5 wrap | fresh ephemeral + nonce per wrap; AAD = purpose ‖ meshID ‖ itemID ‖ origin ‖ recipient (transplant-proof) | 2026-09-03 |
| D6 expiry | receiver requires exact floored equality with own hardDeadline + 1200 s | 2026-09-03 |
| D7 wraps ≡ destinations | wire-enforced; "roster − self" is mint policy, never a v2 trigger | 2026-09-03 |
| D8 size bound | 1…256 MiB on `MeshRoutedManifestFormat`; item 9 reuses the constant | 2026-09-03 |
| D9 verify vs unwrap | separate calls: verify = public material; unwrap = private KA key via closure (item 10) | 2026-09-03 |
| D10 frame body | `MeshRoutedManifestPayload { manifest }`, departure-payload idiom | 2026-09-03 |
| D11 store key | manifest is NOT MeshMergeableContent; routed-store union key = signed pair (originFingerprint, itemID) | 2026-09-03 |
| D12 destinations | trusted on origin signature, bounded, distinct, origin-free; NOT checked against ledger.admissions | 2026-09-03 |
| D13 unknown tokens | verifier takes `acceptedTypeTokens` from day one; refused between shape check and origin lookup | 2026-09-03 |
| D14 departed vs removed | departed origin still verifies; quorum-removed origin refused (`originRemoved`) | 2026-09-03 |
| C1/C2 chunk integrity | BOTH: per-chunk origin Ed25519 signature over the transcript AND assembly-time contentHash check; relays forward the origin-signed chunk unchanged, never re-derive the inner signature | 2026-09-03 |
| C3/C4 chunkID | derived, not a wire field: UUID(SHA-256(purpose ‖ itemID ‖ index)[0..<16]); origin-free — the replay window separates by sender | 2026-09-03 |
| C5/C6 digests | domain-tagged: `Hash.meshRoutedContentV1` "fernlet.mesh.routed-content.hash.v1", `Hash.meshRoutedChunkV1` "fernlet.mesh.routed-chunk.hash.v1", + chunk-id purpose; manifest.contentHash meaning frozen = SHA-256(lp(purpose) ‖ blob) | 2026-09-03 |
| C7 chunk frame | `Signature.meshRoutedChunkV1` "fernlet.mesh.routed-chunk.v1" (.lengthPrefixed) + `PayloadType.meshRoutedChunk` | 2026-09-03 |
| C8/C9 wire fields | meshID, itemID, origin, contentHash, chunkIndex/Count (UInt32), chunkHash, expiresAt, payload, signature; `size` stays the manifest's | 2026-09-03 |
| C10 manifest-less chunk | admissible: verified from the chunk alone and PARKED, never silently dropped (items 9/11 own the parked set's fate) | 2026-09-03 |
| C11/C12 blob contract | no item sealer in item 2 (seal = item 6 / P6); blob is self-contained (nonce + tag inside); contentHash + size measure the COMPLETE blob | 2026-09-03 |
| C13 assembler | in-memory bounded `[UInt32: MeshChunk]`; item 3 re-backs bytes with the sealed sidecar; duplicate-vs-conflict decided on transcript + payload, not hedged signature bytes | 2026-09-03 |
| C14 chunker | primitive = one chunk; bounded loop over it; guard chain (incl. full-blob hash) once per item | 2026-09-03 |
| C15 pacing placeholders | `maxChunksInFlightPerPeer = maxConcurrentOutbound − 1` (3) and 256 KiB are tier-2 placeholders; measure with a concurrent photo transfer on the same tunnel | 2026-09-03 |
| C16 Transport/ | no behavioural change; one docs-only amendment in MeshTransferStreamTable | 2026-09-03 |
| D-3.1/3.2 store shape | five-state value with P3's load/save/quarantine/wipe floor; custody verbs a thin layer on top; sweeps are explicit calls, load() read-only | 2026-09-03 |
| D-3.3/3.14 C13 by extraction | `MeshChunkAdmissionRule` per-chunk + binding verdicts shared by MeshChunkAssembly and the store | 2026-09-03 |
| D-3.5 mint refusals | only the reachable ones (notTheCustodian, witnessForAnotherItem, contentHashMismatch, …); isWellFormed does not pre-check origin ≠ custodian | 2026-09-03 |
| D-3.6 bad chunk file | missing/unauthenticated chunk file → item INCOMPLETE (descriptor dropped, index repaired), not a corrupt store | 2026-09-03 |
| D-3.7 witness | `MeshCustodyDurabilityWitness` init fileprivate to MeshRoutedCustodyCommit.swift; LoadToken init fileprivate to MeshRoutedStore.swift — two gates, two files | 2026-09-03 |
| D-3.8 refused | RETRYABLE (as P3); `MeshRoutedUnavailability` is an outcome value, deliberately not `Error` | 2026-09-03 |
| D-3.9 delivered | no `delivered` rung written in item 3 — only the frozen spelling; item 4/6 write it | 2026-09-03 |
| D-3.10/3.11 transfer + idempotence | transfer names destinations, custodian from the receipt; idempotent = re-streams + re-gates, never skips | 2026-09-03 |
| D-3.12/3.13 caps | over-cap at rest = corrupt, never clamped; writer doors refuse by name; file cap counts the directory; an orphan-making writer removes it | 2026-09-03 |
| D-4.1/4.2 receipt | recipient-signed, about the origin's item, forwarded verbatim; the ack STAGE is not on the wire — receipt = "final"; stage resolved from the manifest's type token via the table | 2026-09-03 |
| D-4.3 decrypt-pending | = `custodied(by: self)` on the existing frozen ladder; no fourth rung | 2026-09-03 |
| D-4.4/4.5 locked device | photos/text: durable ciphertext-only storage IS final; hearts: NOT final until a foreground pass (custodied(by: self) across restarts) | 2026-09-03 |
| D-4.6 control | the ACK RECORD must be durable (index write), not the content | 2026-09-03 |
| D-4.7 one table | store door takes the TABLE: `committingDelivery(item:recipient:stages:evidence:now:)`; shipping code names exactly one `MeshRoutedAckStageTable` (grep-wall) | 2026-09-03 |
| D-4.8/4.9/4.10 heart evidence | three fail-closed legs: judged exactly once in this `MeshHeartCommitOutcome` (judgements reused), `ProximityHeartLedger.commitProof(for:)` → `MeshHeartLedgerProof` (additive, read-only, fileprivate init), itemID == giftID frozen for `fernlet.mesh.routed-type.heart.v1` | 2026-09-03 |
| D-4.11 routed index schema | `MeshRoutedIndexSchema.current` = 2; schema 1 corrupt (quarantine + orphan sweep); at-rest token unchanged | 2026-09-03 |
| D-4.12/4.13 own receipt + re-commit | recipient stores its OWN receipt in `recipientReceipts` beside peers' (item 6 re-sends); re-commit re-runs the whole verification | 2026-09-03 |
| D-4.14/4.15 refusals | mint errors reachable only; new store refusals `capacityRecipientReceipts`, `unknownTypeToken` | 2026-09-03 |
| D-4.16 foregroundness | unenforceable from a pure value in ProximityKit — item 10 / P6 wire `MeshSessionState.activeForeground` | 2026-09-03 |
| D-4.17/4.18 delivered rung | split as item 3: `committingDelivery` writes record-level `deliveredAt` + witness; heart stage keeps the held-ciphertext precondition (`isComplete && isCustodied`) — a deliberate deviation from §11's letter | 2026-09-03 |
| D-4.19 awaiting local ack | `itemsAwaitingLocalAck(at:for:)` = live items where this device is a destination and its OWN receipt is absent | 2026-09-03 |
| Type tokens | `MeshRoutedTypeToken` photo / tempMessage / heart = `fernlet.mesh.routed-type.<x>.v1`, frozen and pinned by test | 2026-09-03 |
| D-5.2/5.3 entry summary | two signer INDEX lists (custody / recipient receipt signers), no rollup hash; `==` over the canonical value is set equality | 2026-09-04 |
| D-5.4 member table | `members: [String]` ≤ 16, sorted, distinct, MINIMAL + `UInt8` indices (minimality is load-bearing for canonical bytes) | 2026-09-04 |
| D-5.5/5.6 what is advertised | live-only at an injected `now`; parked / delivered / unrestorable all advertised; custodySigners = stored custodians ∪ self iff custodied and origin ≠ self | 2026-09-04 |
| D-5.7 over-cap | refused by name (`.overCapacity` / `.malformed`), never clamped or re-sorted | 2026-09-04 |
| D-5.8 signed | advertiser-signed, `sentAt` bound in, five-check verifier, key from the admission ledger | 2026-09-04 |
| D-5.9/5.15 matched (item 7) | QUIESCENCE, never equality; `isQuiescent` is strictly local; `converged(local:peerReportsQuiescent:)` needs BOTH sides — the peer's bit rides item 6's routed answer | 2026-09-04 |
| D-5.10 comparison inputs | `Delta.between(local:remote:offerableToPeer:)`; only the OFFER lists take the entitlement set (two origin-signed sources) | 2026-09-04 |
| D-5.11/5.12 no persistence, no replay window | no file/keychain/UserDefaults/wipe row; digest not admitted to `MeshFrameReplayWindow` — its replay defence is the signed `sentAt` | 2026-09-04 |
| D-5.13/5.14 builder + bitmap | `init?` nil only past `maxReferencedMembers`; mint throws `tooManyReferencedMembers`; exact held-chunk bitmap ceil(chunkCount/8), frozen bit order, trailing zeros mandatory | 2026-09-04 |
| D-5.16/5.17 delta safety | foreign-mesh pair → nil, never an empty delta (empty reads as matched = fail-open); signer sets compared as resolved fingerprints, never indices | 2026-09-04 |
| D-5.18/5.19 review round | mint derives "this device" from `identity` alone (no `selfFingerprint` param); `ask`/`offer` are DISTINCT keys in canonical order | 2026-09-04 |
| D-6.1 `.absent` store | advertises an EMPTY inventory; deferred/corrupt/refused advertise nothing | 2026-09-04 |
| D-6.2/6.8 routed answer | `fernlet.mesh.routed-drain-answer.v1` / `MeshRoutedDrainAnswer*` (states a comparison, own stem); sent from its own receive door | 2026-09-04 |
| D-6.3 call sites | `sendRoutedInventory` has exactly the three of `sendInventoryDigest(to:)` — beginMergeExchange, askOneReconnectedPeer, the admission-grant reply; grep-walled | 2026-09-04 |
| D-6.4 push-only | no ask frame; `delta.ask` is diagnostic | 2026-09-04 |
| D-6.5 pacing | per-peer SESSION frame budget (`sessionFramesPerPeer` = 1024 + 32) charged by the plan's frameCount — overturns the once-per-peer boolean; `maxChunksPerAnswer` = 64 | 2026-09-04 |
| D-6.6 state clearing | exactly `peerInventoryDigests`' three sites (leaveMesh, prepareMembershipLedger, armJoinerLedger); NOT abandonMergeExchange | 2026-09-04 |
| D-6.7 custody receipts | forwarded AND ingested: `forwardableCustodyReceipts(item:)` + `recordingCustodyEvidence(item:receipt:now:)` | 2026-09-04 |
| D-6.9/6.10 tokens + binding | `acceptedTypeTokens` = `MeshRoutedAckStageTable.increment1.tokens`; answer binds advertiser == self and advertisedAt == the stored peer inventory's | 2026-09-04 |
| D-6.11/6.12 store + clock | store built per call (`MeshRoutedStore(scope: store.meshRoutedStorage)`); `now: Date = Date()` defaults on entry points + `dispatchRoutedPayload(…now:)` seam (review) | 2026-09-04 |
| D-6.13/6.14 | no item seal (P6); no persistence, no wipe row | 2026-09-04 |
| D-6.15/6.16 origin-retains | non-origin offers iff complete && (origin == self \|\| target.state(of: peer) == .custodied(by: self)) — never `isCustodied`; receive admits only self ∈ destinations \|\| sender == origin, at manifest AND chunk doors | 2026-09-04 |
| D-6.17 hard deadline | `currentMesh.createdAt + MeshSessionCeiling.ceilingSeconds`, never `sessionCeiling?.hardDeadline` (armed lazily) | 2026-09-04 |
| D-6.18 converged halves | `MeshRoutedPeerInventory` records BOTH: localQuiescent/quiescentLocalAsOf beside reportsQuiescent/quiescentAsOf | 2026-09-04 |

## Session notes
- 2026-09-04 21:02: item 9's workflow hit the session limit after the review (implementer green: 1277 tests / 132 suites, wall check passed; 11 confirmed findings unapplied — fix + gauntlet failed at spawn). Resumed from run wf_b743b9a3-8f7 after the 9pm reset. Second occurrence; the limit resets at a fixed local hour — plan the longest workflow of the day to start right after a reset.
- 2026-09-04 00:41: item 5's workflow hit the session usage limit mid-verify (implementer green, full suite 4232; 34 refuter votes + fix + gauntlet failed at spawn). Resumed from run wf_65b84324-d14 after the 12:40am reset — cached stages replay, only verify/fix/gauntlet re-run. If a workflow's agents fail with 'session limit', schedule a wake for after the reset instead of retrying.
- 2026-09-03: Opus 4.8/5 were briefly down (item 1's workflow ran on the session model, Fable 5.1); owner confirmed Opus is back — later items use `model: "opus"` per the launcher. Ultracode on: each item runs as a small Workflow (understand → implement → adversarial verify → fix).

## Surprises worth not re-deriving
- A suites list is only as good as its names: item10/suites.txt carried `AccessibilityBoundaryTests` (real: `LockGateAccessibilityBoundaryTests`) and `MeshEpochModelTests` (a FILE; its suites are MeshEpochRefTests / MeshEpochKeyringTests / MeshEpochAcceptanceTests / MeshFrameReplayWindowTests) — both ran zero tests under a green banner. item12/suites.txt is the corrected 143-name list; use it from item 13 on and count ◇ starts against ✔ passes.
- **The second domino** (item 1a): pinning the host let the send run one frame further into `FakePeerTransport.send`, which held its fabric `unowned` and trapped there. After fixing an `unowned` trap, expect the next `unowned` down the call chain; test fakes must hold fabrics `weak`.
- A wall-clock/load flake surfaces only in a COMPLETE full-suite run — `MeshClothingShopTests` (catalogSends 3 vs 2) was green alone and red under load; fix the wait, keep the assertions.
- `MeshRoutedStorageScope.production` may not appear as a literal in ANY test source (MeshRoutedStoreIsolationTests greps for it) — assemble the string at run time in a wall test.
- A `ProximityPayloadHandling` conformance cannot take a `now:` — inject the clock one call below the conformance (`dispatchRoutedPayload(…now:)`).
- A conflicting-chunk verdict needs a VALID origin signature over altered bytes; `MeshChunker.mint` is private by design, so drive `.duplicate`, not `.conflictingChunk`, from a rig.
- `#expect(_, "comment")` needs a LITERAL comment (`String` → `Comment?` build error); no interpolated/computed comments.
- ColumnCrypto seals with a fresh nonce on every write — never assert sealed-byte equality across two writes; compare the decoded value.
- An EMPTY routed store's pre-first-unlock refusal arrives at `.seal`, not `.open` (a green field answers absent without consulting custody); a corrupt-quarantine test must establish the seal key before planting garbage (custody is classified before content).
- `Result<_, MeshRoutedUnavailability>` will not compile and must not — the unavailability is an outcome value, not an `Error`.
- **CryptoKit Ed25519 signatures are HEDGED (nondeterministic).** Never compare signed records by full `==`; compare canonical bytes + payload and verify both signatures. Two mints differ only in the 64 signature bytes; goldens exclude the signature.
- `CryptographicWallScan` matches bare primitive names in COMMENTS too (`rawCryptographicCallsNameAPurpose`); don't name a sealing primitive in prose.
- A non-existent suite in `-only-testing` matches zero tests and still prints `TEST EXECUTE SUCCEEDED` — always check `Test run with N tests` is non-zero (item 1's verifier suite is `MeshRoutedManifestSigningTests`; there is no `…VerifyTests`).
- **Full-suite host crash is pre-existing and nondeterministic** (item 1a): `Attempted to read an unowned reference … already destroyed` on the main thread after `mesh.merge.askedLateReconnect`; the 60 "failing" tests are the ones interrupted, no assertion failed. Re-run the interrupted suites targeted (54 suites → 680 green) and read the implementer's clean full run before believing the red.
- zsh does not word-split `$SUITES` — `${=SUITES}` or an array; `TEST EXECUTE SUCCEEDED` over "Executed 0 tests" is a green banner over nothing.
- Swift Testing's `passed on 'Clone` counter reads 0 in this Xcode; the proof is `Test run with N tests in M suites passed` + per-suite ✔ lines.
- `#expect(xs.allSatisfy(\.p))` fails to build (macro drops rethrows → "call can throw"); bind the Bool first.
- `MeshRoutedManifestMintError.tooManyDestinations` is unreachable (roster cap 8 → ≤ 7 destinations); documented, not tested.
- `MeshQuorumFixtures` mints placeholder-signed proposals the record verifier refuses; use real `SignedRemovalRecord.signed(...)` on a 3-rig for a removed origin.
- Seeded property test paid for itself first iteration in P4 (found 2c, late-reconnect strand, 2d). Write item 14's extension beside the first increment; every event = one call into an existing seam.
- One `ProximityCoordinator` per link in an N-manager rig.
- `ensureProvisioned()` + roster size as hard precondition; distinct `identity:` per manager.
- Sample rotation-queue/drain state right after a synchronous pump, never after an `await`.
- Heals must be ordered (re-gossip / drain answer fires once per peer per session).
- A healed partition must re-form as a full mesh with a second commit round.
- `MeshNetworkManager` holds its host store `unowned` — inline test store traps; use `MeshDepartureRig.node`.
- `.merge` > `.membership` > `.timer` in the 2 s coalescing window; `seedMembershipLedgerForTesting` seeds without spending one.
- `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` on the test target — fixtures `@MainActor`.
- Widening an existing signed frame moves its golden — new additive frame + full trio; re-implement an existing golden byte-for-byte first, then derive the new one.
- `test runner hung before establishing connection` hits the first invocation after every build; retry is the acceptance (~13 min each).
- Close-out apply steps read inputs from scratch files, not a long inline prompt (529s).
- Closed; do not re-audit: `MeshTunnelConvergence`, id-vs-endpoint family (`96337a3`, `2f273a9`), crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10.
- Concurrent sessions share this tree + sim fleet; `Localizable.xcstrings` + `xcschememanagement.plist` held by another session — never stage them.
- **`activeSlots` is a distance rank, not a reach set** (item 7). `.active` is capped at `maxActiveSlots = 3` of `maxTotalSlots = 5`, a fourth slot is born `.lightweight`, and `rerankSlots()` re-assigns every slot's kind from a ranging sample — while `broadcastMembershipFrame` iterates ALL slots. Deriving the merge window's reach set from it would subtract a peer that is still re-gossiping and restore 2d, triggered by a distance measurement with no membership meaning. `reachableRosterFingerprints()` is the same trap wearing a helpful name. The failure is invisible on `FakePeerNetwork` (no ranging samples), so it needs a source wall, not just a cell.
- **"Send the post-merge proof only if the window is still open" silences the one device that just converged** (item 7, D-7.28). In the commonest heal — one side strictly behind — the behind device's fold both catches it up AND empties its pending set; gating on "still open" means it never tells its peer, and the ahead device (which folded nothing, so its own digest never moved) holds an open window for the rest of the session. Capture the owed set at entry, before the re-evaluation and before the verdict.
- **The once-per-peer-per-session re-gossip budget is the unstated precondition of every merge-liveness argument** (item 7, D-7.30). `reGossipedToFingerprints` clears only at `leaveMesh` / `prepareMembershipLedger` / `armJoinerLedger`. On a SECOND heal of the same pair inside one session no records can cross, so no digest can move, so no proof is emitted and neither window closes. That is not a regression (today's rule converges no better) but it must be written down, not discovered from a hung `settle` — the fix is the budget, never a looser closing rule. **Owner-visible; belongs beside the ungated P3/P4 CI batteries at close-out.**
- **A parameterized `@Test(arguments:)` over an empty array runs zero cases and reports green** — retiring a deferral means DELETING the test that asserted the defect and replacing it with a locally-built cell list, not leaving it pointed at the now-empty `deferred`.
- A `@Suite("display name")` prints its display name in the ✔ line, so a per-suite grep on the struct name silently misses it; count `◇ Suite` against `✔ Suite` instead. Under parallel output a ✔ line can also be lost to interleaving — re-run that one suite rather than believing the miss.
- A settle's `until:` fires **synchronously inside the pump**, so a predicate that becomes true in the same step that schedules a send returns before the frame exists. Assert on the frame count itself (`until: { received(...) >= 2 }`), not on the state change that provoked it.

## Next item
13 (in-flight, iteration 14) → 14 → close-out; 1b and 6a as bundled small commits when convenient. A stray untracked PDF sits in Docs/ (owner's, not P5's — never stage it).