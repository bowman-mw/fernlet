# Mesh Migration Loop Ledger — P6

**Phase:** P6 (feature routing — text, hearts, key advertisement) · **Prompt:** [Next-Round-Prompt-Mesh-P6-2026-09-10.md](Next-Round-Prompt-Mesh-P6-2026-09-10.md)
**Started:** 2026-09-10 · **Iteration:** 1 · **Tree at seed:** main = `3a32be0` (P5 close-out + post-close review corrections)

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 1 | Key-advertisement wire family `fernlet.mesh.key-agreement.v1` | 1 | — | done | pass A `1aece9d`, pass B `0295ac4`, fixes `beef3ed` + `bd72de2` + `193cfd7` | **Pass B done:** `PayloadType.meshKeyAgreement` batch ≤ 16; own mint at `seedFounderAdmission` / `armJoinerLedger` (+ bounded 3/session self-mint repair — the unbounded one tripled a full run); `sendKeyAdvertisements(to:)` at SIX doors (3 ask + `readvertiseMergeProof` + `attemptLedgerAdoption` + `grantAdmission`), committed members only, once per (peer, local-set version); receive door in the membership family outside the refusal budget; `MeshKeyAdvertisementFold.restoring(_:verifiedBy:)` re-proves persisted rows at restore (fence, not discipline); `routedDestinationKeys` → three-case value, `MeshRoutedShareRefusal.keyMismatch` (app copy added; **catalog key NOT synced — owner/close-out**); drain wall counts the send, shown red once; R-16 → real-restart delivery, R-12 → star mint + explicit negative. Fixed pass A's `allDomains` 74→75 pin (b0bf4d1 was red on the full suite — pass A skipped the full run). **Full suite 4692 / 469 green (1227 s) = new baseline.** Named residuals: a brand-new joiner still cannot address a third member that never links it (item 2's audit); item 9's convergence invariant must call `armKeyAdvertisements()` or it is green over nothing (`seedMembershipLedgerForTesting` bypasses both arming doors); sibling `== 0` audit-capture assertions in routed suites are per-cell claims on a process-global handler (D-6a.10). **Third-review fixes landed `193cfd7`** (collapsed-code needles + `(type, declaration)` allowlist that reds an entry exempting nothing; park counts ROSTER MOVES by name; rollback restores the version; 48 pinned; `writeKeyAdvertisementFrame(` counted; double-door observed inside the suspension; park types `nonisolated`; six tokens rowed). ITEM 1 CLOSED — no further review pass (final review of `0665d0c`+`193cfd7`: no P1/P2; two P3s owed to the next MNM touch: `handleAdmissionGrant`'s `adoptKeyAdvertisements(addressingBeforeJoin)` rollback (~:10539) still bumps the version while two doc lines say otherwise — 2 lines; and a blank/comment line between `.init(` and `advertisements:` evades the construction wall — widen the window). **Third review (scratch `item1-fix2-verify.md`, of `bd72de2`): no P1; P2 = the construction wall's needles miss `.init(` with the first label on the NEXT line (the declaring file's own house form at ~:582–590 is invisible to the cell, so its allowlist entry matches nothing) → collapse whitespace before `contains`; P3 = `MeshKeyAdvertisementPark.empty` is a second `static var empty:` allowlist match; five member spellings still inherit an allowlisted func; the drop counts ROSTER MOVES not widenings (three departures evict a genuine early row — audited, sender re-sends); a non-durable rollback adopts twice (+2 versions, unchanged frame re-sent to everyone, charged against their 48) → restore the version with the set; 32→48 pinned by no cell; `writeKeyAdvertisementFrame(` counted by no wall; the two-doors cell passes with or without the interleaving; the four park types are not `nonisolated` (implicitly `@MainActor` under the module default); six park audit tokens in no index/DocC row. **Queued as a THIRD small fix, bundled with the 1c agent (file-disjoint), after item 2's session-end fix.** Fix review (scratch `item1-fix-verify.md`): no P1; 3 P2 queued as a SECOND small fix commit after item 2 pass B:** (a) fix D over-corrected — the self-mint repair is gated on "a member is OWED" not "a member exists", so once every member is latched with the self row still missing (arm-time seal refused) attempts 2–3 are forfeited → `guard !members.isEmpty`; (b) the park is squattable by a misbehaving MEMBER (`signerNotAdmitted` is answered before the signature check, 16 rows fill it, junk for never-admitted fingerprints is re-parked forever, a junk row in M's slot blocks M's genuine relayed row unaudited) → per-sender park share + audit the collision + drop after N re-offers; (c) latching after the `await` removed the in-flight claim → two doors firing for one peer write twice (each costs a receiver frame) → latch optimistically, un-latch what `written` omitted. P3: `keyAdvertisementVersion` invariant doc now false; `framesPerSenderPerSession = capacity*2` derivation broken by the third per-member transition (release ⇒ up to 48 versions vs 32); park doc "earliest wins"/"three resets" (arrival order; four resets); a non-durable commit at the re-offer seam loses rows from set AND park; `clearingConflicts`' re-admission justification impossible by design; wall holes (`enclosingDeclaration` satisfied by a member `let` below an allowlisted func; `.init(` evades both needles); the cap red was collateral. **Review fixes landed `beef3ed`** (parking at the joiner closes the grant-door refusal — a third device that adopts early is RE-parked, found only under the 46-suite run; latch-what-was-written; member-keyed receive budget + the `bufferedForAdoption` exception; repair below the owed check; three bound cells red once; conflict mark released on roster exit; bare-constructor wall with six allowlisted sites). Pass B review (scratch `item1-passB-verify.md`): no P1; P2 (all fixed in `beef3ed`): (1) the send LATCHES `keyAdvertisementsSentVersion[peer]` before the write and `broadcastMembershipFrame` skips slot-less recipients / discards send failure with no door firing on a fold → one lost frame = permanent `destinationNotAddressable` on a stable pair — latch only what was written; (2) `senderBudgetSpent` / `framesPerSenderPerSession` / `selfMintUnavailable` / `keyAgreement.notDurable` have ZERO tests — deleting the receive charge, the 3-attempt cap or the durable-before-counts rollback fails nothing; (3) receive budget keyed by committed SENDER not member (cap 8, never evicted) → eight non-members lock out every real member for the session; (4) self-mint repair spent BEFORE the roster filter → 3 link flaps burn all attempts with `owed` empty; (5) a conflicted member now kills the WHOLE mint durably across restarts — fail-closed is right, the A3c doc understates the blast radius; (6) pass A's grep-wall needle `MeshKeyAgreementAdvertisementSet(advertisements:` is defeated by a line break — grep the bare `MeshKeyAgreementAdvertisementSet(`; (7) `restoreMembershipLedger`'s doc still says the fence was "deliberately not taken". P3: token missing from the DocC page + wire-token table; two symbols missing from the indexes; `writeSessionContext` writes the new field unconditionally; tier B's current-roster fence silent under mutation; a JSON-undecodable frame dropped unaudited. **Pass A done:** `SignedKeyAgreementAdvertisement` (meshID bound, self-signed under the admitted key), `MeshMembershipRecordVerifier.verify(_:)` → `MeshVerifiedKeyAgreementAdvertisement` (fileprivate init = compile fence: verify BEFORE any conflict mark), `MeshKeyAgreementAdvertisementSet` (own type, grow-only, 16 refused by name, conflict-refusing, NO `merging(` — grep-walled), context schema **3** (`keyAdvertisements` added, `routingInventoryDigest` deleted, v2 → corrupt), full trio + a new every-purpose-has-a-framing check (floor 20/28 declared, 10 written exemptions). Design-check REQUIRED applied 2/4/9/13/15/17, partial 1/11, **deferred to pass B: 3/5/6/7/8/10/12/14/16**; declined the protocol split (its reuse IS the fail-open). Handoff: scratch `item1-passA-handoff.md`. Pass A review (scratch `item1-passA-verify.md`): no P1; **P2 for pass B first:** (1) `MeshKeyAgreementAdvertisementSet.init(advertisements:conflictedFingerprints:)` is an unwalled third door — `normalized` caps rows by instant and conflicts alphabetically, so a 34-row input with 17 conflicting pairs drops a mark while its row survives → derive conflicts from the surviving rows, add the constructor spelling to the merge-door wall; (2) `conflictedFingerprints` has no input bound; (3) the idempotence pre-filter compares 3 of 5 fields (ignores meshID + signature) → `held == advertisement`; (4) `CryptographicPurposeBoundaryTests` / `CryptographicDomainSeparationTests` / `MeshMembershipEventGoldenTests` are on no CI line; P3 (5) the bound cell's "nothing evicted" cannot fail — mint `mine` at `secondsIn: 0`. Named residual: a DECODED set's rows re-enter as verified with no marker; safe only while the receive path re-proves against the current admission set (`MeshLedgerAdoption.adopt` can narrow it) — pass B must make that a fence, not a discipline. **Pass B** = doors (COMMITTED slots only), fold + save, resolver third source + `keyMismatch` + conflicted refusal + copy/catalog, drain-wall amendment shown red once, `MeshIntroductionAuthorityTests` emitted-list, R-12/R-16 flips, star/over-cap/resumption rig cells with explicit negatives, full suite |
| 2 | Proximity-join founding — a mesh WITH A LEDGER at the first commit (was "pairwise identity") | 1 | 1 | done | pass A `871e52d`, pass B `921441a`, fixes `66c64d8` | **Pass A done:** founds at the first commit via `foundMesh(_:now:)` (extracted from `startNewMesh`; `seedFounderAdmission` = item 1's mint door) + `noteCommitIntoMesh` (arms the ceiling) + `autoGrantsFoundingAdmission` (ONE grant while members == 1, through `grantAdmission`); election = pure order, yield + `unwindNewbornMesh` (group key + advertisement state), newborn = "one member = self"; `hasCommittedPeer` at 3 hooks + 3 app sites (12 reads classified; `ContentView:1784` kept — partitioned pair can't re-arm radios until End Session); `sessionParticipants` ∪ slots; seam `commitSlotForTesting` drives the REAL path (rigs faked commits; `MeshDepartureRig.pump` attributes off a nil `PeerIdentity` — why P3–P5 never drove descriptor/request/grant at tier 1); `MeshPairwiseFoundingTests` 12 cells, hooks cell red once. **New item-1 defect pinned as a negative:** the founder's grant-door advertisement is ALWAYS refused by the joiner (`signerNotAdmitted`, arrives before the bootstrap ledger names the admitter) → item 1 fix commit (park-until-admitted or re-send after adoption). **Pass B done:** `resumeSearchingForPartitionedMesh()` + three-way `startFriendsDiscovery` (P1 closed, residual deleted); `handleSessionChange` split surface/committed-peer; yielder re-applies its closed mode (`reassertClosedModeAfterYield` — 4th policy act; any member may close, merge is LWW on `modeSetAt`); `promoteToMesh` idempotence guard; `commitSlotForTesting` runs `maySeatVerifiedPeer`; delivery MEASURED at the recipient through the real path with nothing seeded (pair both ways; three-device after a yield; only-peer departure fires hooks once; solo-with-ledger own-wall-only); suite 18 cells on the mesh-batteries CI line (floor 131); plan §11.3 13(i)/§23.2/§23.4 amended BUILT 2026-09-11; runbook dated. Fixed a pass-A red no full run had seen (`unwindNewbornMesh` nilled `lastRotationBlockReason`). **Full suite 4721 / 470 green (1279 s) = new baseline.** Named residual: a yielder ends with a mesh and no ceiling until P7's poller. **Review fixes landed `66c64d8`:** `isSessionLive` (four doors: End Session, terminal state, discovery timeout, slot loss only while ledgerless) gates the three hooks + the review — a blip presents nothing, clears nothing (red once); TXT republished on mode change; sticky `userClosedThisSession` + monotonic `modeSetAt`; a11y latch cleared on every exit; `FriendsDiscoveryEntry` pure table; floor 138. **Full suite 4735 / 470, 3 issues = 1c only.** Item 4 keys projection liveness on `isSessionLive` **plus the mesh leg and a transcript-generation guard** (fix review, below). **Fix review (scratch `item2-fix-verify.md`, of `66c64d8`): 0 P1; 3 P2 bundled into item 4's run as its own file set:** (1) door 3's clock is armed only in `startFriendsDiscovery` (tab entry / scene-active, bails on `isSearching`), single-shot, not re-armed on a blip → a partitioned pair more than 5 min into a visit has NO door 3 and `isSessionLive` stays true for the process (no data loss — `beginDevelop` reviews first — but 'eventually ends' is weaker than documented) → arm on the `hasCommittedPeer → false` edge or move the clock into the manager; (2) the album photo feed is a SECOND `fullScreenCover` (ConnectView ~:103) the heal arm's special-case misses → a commit while a wall photo is open latches `.accessibilityHidden` again; (3) `startSearching` clears the give-up flag AFTER the transcript was cleared while `routedProjectedItems` is wiped only on mesh change and custody outlives the session → `isSessionLive` is reversible, the clear is not. P3: >60 s skew defeats the monotonic stamp (the peer's `min(stamp, now+60)` clamp); the yield-path republish fires BEFORE the re-assert (a closed user briefly advertises the open mesh); `userClosedThisSession` has three writers not one; door 2 fires zero times if `.persistContext` fails; the new wall reads raw source (comments satisfy it); `FriendsDiscoveryEntry`'s `String`/`CaseIterable` unread; two stale index/doc lines. Three suites read `ConnectView.swift` as source; only one re-ran after the post-full-run edit (the other two provably unaffected). Pass B review (scratch `item2-passB-verify.md`): 1 P1 (fixed): a link BLIP now runs the session-end hooks (`removeSlot` fires them on any slot loss) → `handleCommittedPeerChange` presents the photo-review sheet whose BOTH actions call `leaveSessionAfterNotifyingPeers()` → for a pair `ending = .termination` (signed termination + permanent rejoin bar) on a session the commit keeps alive; pre-item-2 the same tap hit a mesh-less pair. **Default fix:** session end = mesh end (End Session / termination / departure completed / the 5-min discovery timeout with no peer), NOT slot loss — the three hooks + the review fire from those doors; slot loss keeps its legacy meaning only when `currentMesh == nil`; a blip must neither present the review nor clear the transcript (cell, red once). P2: (2) the closed-mode re-assert never reaches the winner's TXT — `handleMeshDescriptor` calls no `updateDiscoveryInfo()`, so the winner's model says closed while its TXT keeps publishing meshID/name/memberCount; (3) the mode fix is decided by WALL CLOCKS (LWW on `modeSetAt`, stamp clamped only from above) — forward skew on the winner loses the yielder's close and re-opens it; fix = sticky local "user closed" flag or a monotonic stamp; (4) the heal arm handles the keep prompt but not the review sheet, and a failed cover presentation latches `.accessibilityHidden(true)` on the whole Friends surface; (5) the P1's SHIPPING half (ContentView's three-way arm + `armDiscoveryTimeout`) has no failing mutation — extract a pure decision (precedent `FriendMintingReview.sessionEndReview`). P3: `lastRotationBlockReason` carry fires on the yield path (diagnostics only); the resume's contract stated against `startJoin` while its real predecessor is `stopSearching`; two vacuous `guard let slot else return`s; the trio cell's name reads as a star claim; `resume…` sets `isProximityJoin` unconditionally; the mode rule is "any committed SLOT" not "any member". Pass A review (scratch `item2-passA-verify.md`): founding holds; **P1 for pass B FIRST:** `stopFriendsDiscovery` (ContentView ~:1830) + the 5-min timeout moved to `hasCommittedPeer` while `startFriendsDiscovery` (~:1805) kept `isInSession` — a link drop deletes the slot (no retry), a tab exit / background then runs `stopJoin()` → `stopSearching()`, and on return `!isInSession` is false because the mesh outlived the link, `startSearching()` is private with no other shipping re-arm → a live camera over a session with NO radios, escape = End Session only (before 871e52d the same sequence recovered). Fix: a `resumeSearchingForPartitionedMesh()` seam (startJoin minus the resets) called when `isInSession && !hasCommittedPeer`. **P2:** (1) "fully unwinds the yielder" has no failing mutation — both cells assert epoch 0 / nil group key in a rig where no rotation fires; drive a rotation first; (2) nothing yet asserts DELIVERY (pass B's); (4) a blip runs the session-end ceremony over the live camera, and `handleSessionChange`'s `!wasInSession && nowInSession` arm is dead for a pair (`:85` is two readers, only the camera half classified); (5) the yield inherits the winner's open/closed mode (~:9472) — a user who CLOSED and loses the election is reopened, and an open mesh's TXT publishes meshID/name/memberCount (policy: decide and record). **P3:** no idempotence guard inside `promoteToMesh`; `commitSlotForTesting` skips `maySeatVerifiedPeer`; the item-1 negative asserts the absent row not `signerNotAdmitted`; four docs (incl. `ProximityKit.md:1035`) still name `startNewMesh` as the founder door; `commitSlotForTesting` missing from the index; a yielder ends with a mesh and no ceiling (latent — `enforceSessionCeiling` has no shipping caller: P7's poller). **Pass B** = P1 + P2/P3 above, then end-to-end delivery both ways through the real path, third-device merge, only-peer departure, solo cells, CI gate line, the 14 doc sites, plan §11.3/§23.2, full suite. **RE-SCOPED 2026-09-11 (design + adversarial check, scratch `item2-design.md` / `item2-design-check.md`, 12 REQUIRED changes):** `promoteToMesh()` (`:8380` at b0bf4d1) arms NO ledger — none of `startNewMesh`'s five founding steps — and `recordGrantedAdmission:6291` grants without filing when `membershipVerifier == nil`; `startNewMesh` has ZERO shipping callers; so no `startJoin` device ever holds a derived roster > 1 and the routed path is dead for 2–5 devices alike. Every Lane C run seeded `currentMesh` before `startJoin` (harness) and every rig hand-seeds the ledger. Build: found at one commit (election `local < peer` over verified fingerprints — as an ORDER not a gate, both may found, the newborn-yield rule decides; yield must unwind the group key too), auto-grant ONE admission at `currentMesh.members.count == 1` (dwell = consent; policy act), `isInSession` → `hasCommittedPeer` at the three hooks + SEVEN app sites (incl. `ConnectView.swift:614` `presentDisconnectReviewIfNeeded`), `sessionParticipants` ∪ slots, third-device foreign-descriptor drop named; joiner's roster is 1 after the grant (only F→J filed) so the acceptance cell asserts delivery BOTH ways; `promoteToMesh` at 52 lines must split; the new suite must be CI-gated or it never runs. Two passes |
| 3 | Receiver-side per-type cap + photo row's narrowed cap | 1 | — | done | `f306f4f` | `sizeExceedsTypeCap` at the manager door (after verifier, before store, through the refusal budget); NON-dropping arm; photo cap = `maxIncomingPhotoBytes` + framed-header allowance (8 + 64 KiB) + 33 B seal overhead = 10,551,337 B, defined once; `PrivateMediaStore.maxIncomingPhotoBytes` now public (ProximityKit had the dependency since P2). Full suite **4642 / 465 green, 1278 s** = the P6 baseline. Adversarial diff review (scratch `item3-verify.md`): no P1; P2 = the `<=` boundary at the door is untested (`<` survives the suite; add a `capped(at: size)` admission cell) and the registry doc / landing page read the cap as a resident-byte bound (a parked set is untyped, bounded by 256 MiB); P3s = ordering + budget-charge cells, a tautological registry test, a ratchet pin on `maxIncomingPhotoBytes`, four stale plan lines (`:1542`/`:2800`/`:2802`/`:2984`). **Fixes landed `b0bf4d1`** (boundary admitted; ordering + budget charge measured; parked-set bound stated; receive-policy bytes pinned; shown red once). Residual → owner list: a courier's over-cap re-offer is charged (finding 3) |
| 4 | Temporary text on the routed store | 1 | 1, 2, 3 | done | `5659a0a`, fixes `fb24c79` | **FIX REVIEW APPLIED 2026-09-11 — all 15 findings (1 P1, 5 P2, 9 P3), one commit on top of items 5 and 8.** **P1-1, the composite key:** the route taken is *`MeshContentSet` gains a sender-aware key*, NOT a hashed `contentID`. New `public nonisolated struct MeshContentKey{senderFingerprint, contentID}`, derived on `MeshMergeableContent` as a non-overridable `mergeKey`; `MeshContentSet.normalized` dedups on it, `mergeKeys` is added beside `contentIDs` (which still answers *the ids held* — the inventory question a merge exchange asks, and what four suites assert "none doubled" against). Rejected the hashed-`contentID` branch because it would silently change what `contentIDs` means to every caller AND make §10.3's third order key a hash rather than `messageID` — an order amendment, not a dedup fix. §10.3's order needed NO change: `MeshContentOrder.precedes` already ranks `senderFingerprint` above `contentID`, so two same-id rows from different authors were always totally ordered. `SessionMessageStore`: `seenKeys`/`seenOrder`/`attributionByKey` and `Message.id` (`Identifiable`, so the panel's `ForEach` is unique) all key on the pair; `Message.messageID` is the author's own id. The outgoing echo keys on this device's own fingerprint. **No wire change: the manifest and the body are byte-for-byte unchanged, no golden moves, no persisted surface** — the key is local. Photos and hearts are keyed the same way because they share the hazard structurally (routed index key is `(origin, itemID)` for every family, no verifier refuses a duplicate id from a second origin) but **neither set has a shipping call site yet**, so for them it is latent — closing it at the protocol means item 6 inherits it. **P2-1:** delete-all now runs through the funnel — `MeshNetworkManager.beginPrivacyWipe()` at `deleteAllData` **leg 0** (clears through `clearSessionTranscript()`, so the generation moves) paired with `endPrivacyWipe()` in a `defer`, and `privacyWipeInProgress` refuses every routed projection ABOVE the offer (`refusedForNow`, uncharged — a wipe is the gate's own answer); leg 7b's direct `sessionMessages.clear()` is gone, PWC row + `wipeManifest` token moved to `meshNetworkManager.beginPrivacyWipe`. **P2-2:** `dispatchRoutedPlaintext(` pinned at **2** in `routedPlaintextSeams` (decl + 1 call) and added to the containment half's needle list; the table's stale "`routedCanonicalDispatch(` = 2" doc bullet corrected to 4; plan §23's W2 paragraph now says six spellings. **P2-3:** new `aResumedSessionDoorThreeGaveUpOnProjectsNothingFromTheClearedTranscript` — door 3 ends the session with the mesh RETAINED, `resumeSearchingForPartitionedMesh()` un-ends it, and only the generation leg can refuse. **P2-4:** `allowIncomingRoutedText` made `internal` + `theIncomingTextQuotaRefusesPastItsCapAndIsFreeForAReSend` (cap+1 refused, re-send free, per-origin and per-mesh budgets separate, 1024-key map bound). **P2-5:** `routedProjectedItems` made `private(set)` and both "…AndIsMarkedFinal" cells now OBSERVE the mark through `MeshFoundingRig.isMarkedFinal(at:itemID:)`; the starvation cell is NOT duplicated — item 5's `MeshRoutedRetryAllowanceTests` already covers the projection list and is cited in the cell doc. **P3s, all nine taken:** the 128-byte `senderName` bound is now enforced on the WIRE (a 129-byte name is `malformed` — a FIFTH hostile shape, with an at-the-bound negative control); the five "beneath the compose bar" sentences corrected to *at the top of the compose bar, above the text field* (the notice was never moved); `refreshGates`' doc states the next-INGEST trigger and why the gate's setter does not call it; `boundedText`'s truncation audits `mesh.routedShare.textByteBounded` when it bites; the stale ``routedCanonicalDispatch(_:author:manifest:now:)`` DocC link fixed; the first-pass-after-restart cost written into `noteRoutedItemProjected`'s doc (item 5 closed its starvation half); a gate-filtered row is never unread (`aGateFilteredRowIsHeldButNeverUnread` — and it documents that `MeshContentGates.folding` folds over senders ALREADY held, so a sender's first row is refused by `routedProjectionAuthor`, not by the store's gate). **Walls:** no wire-format change, `theRoutedPathNamesNoEpochSymbol` / `theStoreHasExactlyTwoAdmissionDoors` / the refusal-budget door count (8) / the drain wall / `noShippingCodeBranchesOnARoutedTypeToken` untouched; the `Comment?` rule and `LocalizedStringKey`-only unaffected (no new display strings, so **no new catalog keys** beyond the seven already owed). **W2 pin table now SIX rows** (`unwrap(` 1, `open(` 1, `routedCanonicalDispatch(` 4, `dispatchRoutedPlaintext(` **2, new**, `MeshRoutedHeartAck(` 0, `.heartLedgerCommit(` 1). **CI mesh-batteries floor 167 → 171** (`MeshRoutedTextBodyTests` 15→16, `MeshRoutedTextDeliveryTests` 9→12), MEASURED by `Scripts/run-gated-suites.sh` at exactly 171 (`gated-mesh-02.log`), not inferred. **Gauntlet** (logs `…/scratchpad/logs/item4fix/`): po10 `violations: 0; allowlisted hits: 21` (`po10-03.log`), doc coverage `TOTAL undocumented type declarations: 0` (`doccov-03.log`), `build-final.log` `** TEST BUILD SUCCEEDED **`, touched subset 45 suites → `528 tests in 45 suites` then 12-suite re-check `199 tests in 12 suites passed` (`subset-02.log` / `subset-03.log`), `spm-wall.log` `WALL CHECK PASSED`, **FULL `FernletTests` 4789 / 475, ZERO issues, 1524 s (`full-03.log`)** — baseline 4781 → **4789**, exactly the 8 new cells, no new suites. **Red once:** `neg-a.log` — the composite key (`messages.count → 1) == 2`, `(s.messages.map(\.text) → ["the real message"]) == ["the real message", "impostor"]`) AND the new pin (`(atHome → 3) == (seam.pinned → 2)`); `neg-b2.log` — the generation leg (`(rig.transcript(at: 1) → ["before the give-up"]).isEmpty → false`); `neg-c.log` — the wipe guard (`(rig.wallEntries(at: 1, itemID: photoID) → 1) == 0`, i.e. the PHOTO wall is fed mid-wipe — the text arm alone could not tell the guard from the generation leg). **Two throwaway full runs worth not re-deriving:** `full-01.log` hit `Restarting after unexpected exit, crash, or test timeout`, which SPLITS the summary (it reported `1850 tests in 230 suites` for the second launch only — never read a total from a log containing that line), triggered by two load flakes unrelated to this change (`ConnectionInspectorTests.beginSessionCreatesLiveLog` `Time limit was exceeded: 120 s`, `AppIntentsTests.pendingIntentSheetRoundTripsAndIsConsumedOnce` on the process-global `PendingIntentSheet`); `full-02.log` and `gated-mesh.log` both died on `The test runner hung before establishing connection` — the ~350 s first-invocation hang, re-launched clean both times. Both flakes passed in the clean re-run. **DONE 2026-09-11 (one commit, retirement included):** `MeshRoutedTextHeader{id, sentAt, senderName}` ‖ UTF-8, four hostile shapes (failable UTF-8 decode), cap 9,065 B as a formula; `sendTempMessage` → frozen `MeshTextSendOutcome`, inline outcomes in the chat panel, `appendOutgoing` only on `.staged`; `dispatchRoutedPlaintext` switches on `canonicalStore`, W2 unwrap/open 1/1, `routedCanonicalDispatch(` 4; transcript RE-DERIVED through `visibleTranscript` (the dead §10.3 code); token bucket retired → `allowIncomingRoutedText` (policy act); FINAL mark in `routedProjectedItems` (memory-only) via `MeshRoutedProjectionVerdict.leavesTheRetryList`; `MeshTranscriptLiveness` = `isSessionLive` + mesh id + transcript generation (a blip is never final); `.tempMessage`/`TempMessagePayload` PARKED, two zero-list walls. Item 2 follow-ups in the same commit: give-up clock in the manager (`evaluateSessionGiveUp(now:)`), `aPresentationIsUp` over three presenters, `transcriptGeneration`. **7 catalog keys owed at close-out.** Floor 138 → 167. **Full suite 4771 / 473, zero issues = baseline.** Residuals: the age-gate arm is FINAL not retryable (reasoned in handoff §3.5); `sentAt == createdAt` guard not shipped; header allowance 4× floor + 128-byte name bound. **Fix review (scratch `item4-fix-verify.md`, of `fb24c79`): no P1/P2 — ITEM 4 CLOSED.** Three P3s → item 7's run: the CI floor comment still sums to 167 (say 16 + 3 + 12 = 31, 'five' hostile shapes); `privacyWipeInProgress` is a Bool and two wipe funnels can OVERLAP (the duress hook fires `deleteAllData` with no UI gate; the inner `defer` lowers the flag while the outer still has the routed-store purge ahead) → depth counter + a begin/begin/end cell; the header-allowance doc over-claims (the wire bound measures the NAME; a ~9 KB header still decodes — either guard `maxFramedTextHeaderByteCount` or trim the sentence; photo twin identical). Informational: the four 'none doubled' `contentIDs.count == count` cells would not redden on a revert of the key — `mergeKeys.count` is the honest spelling when those files are next touched; `MeshContentGates.folding` misses a sender's first row but `routedProjectionAuthor`'s pre-unwrap block check covers it synchronously — name it in item 6's heart arm. **Review (scratch `item4-verify.md`): 1 P1 (FIXED in `fb24c79`):** the transcript dedups on the message id ALONE (`SessionMessageStore` `seenIDs` + `MeshContentSet` keyed on `contentID`) while the index key is `(origin, itemID)` and manifests cross in the clear — admitted member B can mint a same-id text and win the race to a partitioned C; A's message lands `alreadyHeld` → final → gone for the session while A saw `.staged` → key on `(senderFingerprint, id)`. P2: (1) delete-all is a THIRD transcript clear bypassing the generation funnel (`FernletStore.swift:~5315`), and the routed-store wipe is two async steps later — a window where just-wiped messages re-project; (2) the hoisted mutation guard sits in `routedProjectionVerdict`, one level above the pinned needle, and `dispatchRoutedPlaintext(` has no row in `routedPlaintextSeams` → a second ungated caller keeps the wall green; (3) the generation leg has NO failing mutation (the session-ended cell leaves the mesh, so leg 2 refuses) → needs a door-3 + `startSearching()` cell; (4) `allowIncomingRoutedText` untested; (5) two '…AndIsMarkedFinal' cells never observe the mark. P3: the 128-byte name bound is sender-side only; the notice renders ABOVE the compose bar while docs say beneath; gate/block flips re-render only at next ingest; `boundedText` truncates silently; echo name unbounded; stale DocC link; the first-pass-after-restart cost missing from §6. Verified: golden by hand, all four hostile shapes, cap formula, W2 pins comment-stripped 4/1/1/0/1, §10.3 order, retirement with zero survivors, `evaluateSessionGiveUp` HAS a shipping caller (its own armed task). **Designed + checked 2026-09-11** (scratch `item4-design.md`, `item4-design-check.md`); implement AFTER item 1's second fix and item 2's session-end fix. Design: `MeshRoutedTextHeader{id, sentAt}` ‖ raw UTF-8 (FOUR hostile shapes — invalid UTF-8 must refuse, `String(decoding:)` substitutes U+FFFD silently); text cap = 16 × 500 chars + 1 KiB header allowance + 33 B = 9,065 B (a `Character` cap is unbounded in bytes); `appendOutgoing` only on `.staged`; `sendTempMessage` RETURNS `MeshTextSendOutcome` shown inline in the chat panel (the camera alert is photos'); `.skipped(.noDestinations)` is a real window on the founding path and must be visible; `receiveIncoming` → `Acceptance{appended, alreadyHeld, emptyAfterSanitizing}`; its live-arrival token bucket retires with the transport → per-origin `allowIncomingRoutedText` twin of the photo quota (policy act); W2 unwrap/open pins stay 1/1 via a shared `openPlaintext`, `routedCanonicalDispatch(` 2→4; `PayloadType.tempMessage` + `TempMessagePayload` PARKED not deleted (item 13 precedent; six suites use `.tempMessage` as a control); overlay field is item 9's; door count + drain wall unchanged. **Check's P1s:** (1) the ledger author (`MeshRosterMember`) has NO `displayName` — default: the body carries the display name as a CLAIM exactly as photos do (D-13.33 binds the identity, not the name); (2) `projectsIntoTheLiveTranscript` keyed on `hasCommittedPeer` + an unconditional `noteRoutedItemProjected` would mark a blip-time message FINAL and lose it once session end = mesh end → key liveness on the MESH being live (not slot presence) and never mark a liveness skip final; (3) §10.3's order already exists unused (`MeshContentLedger.visibleTranscript` / `MeshContentOrder.precedes`, derivation-not-mutation, capacity = `SessionMessageStore.maxMessages`) — default: re-derive through it per §12 rather than clamp `sentAt`; also `manifest.createdAt` not `addedAt`; `boundedText` can empty a message and mint it; the containment wall is a whole-file `contains`. Two passes (A = whole path with the legacy receive handler alive + a cell pass B cannot leave alone; B = retirement + paperwork + full suite) or one commit if the iteration has room |
| 5 | Projection retryable-vs-final | 1 | 4 | done | `5d3a00b`, fixes in `ed66042` | `MeshRoutedRetryPlan` (pure, keys-only) paces BOTH re-entry lists: never-tried first, retry share ≤ 8 of 16 drawn round-robin, unused slots spill; `MeshRoutedRetryRotation` memory-only, bounded 1024/list; first pass after a restart charges pre-armed refs to the retry share; FINAL marks memory-only (index stays schema 2); `ackableNow(_:in:)` = the one line item 6 replaces. Found: `mutationDeferred` is dead; the projection list's retryable population IS reachable in production (`transcriptLiveness == .notLiveRightNow` and a deferred store both pass `isProjectableAtThisPass` — this row, the commit message and the test header had said 'unreachable'; corrected 2026-09-11 per the item 5 review's P2 (3) and the item 6 review's P3-2); the ACK list's 16-heart starvation is the second reachable one. Two item-1 P3s taken (grant rollback restores the version; wall window folds forward). **Review (scratch `item5-8-verify.md`): 1 P1 + 4 P2 → SET A of item 6's run:** P1 = the restart cut compares FLOORED `firstSeenAt` against an UNFLOORED `armedAt` — green only because the fixture base is an integral second; from 2026-12-16 (the fixture clock's crossover) every rig newcomer is charged carried-over and three cells red → `armedAt = MeshRoutedManifest.floored(now)`; P2 (1) `.localAck`'s rotation is never pruned at the live door (`finishLocalRungs` has no `noteRoutedRetryFinal`) → stale keys fill the 1024 bound and pacing silently reverts to D-13.32; (2) `dispatchRoutedPlaintext`'s `.heartLedger` arm returns `.refusedForNow` so every live-door heart is charged into the PROJECTION rotation it can never leave — one fix for both: intersect `attempted`/`order` with the enumerated key set at the top of `routedRetryAllowance`; (3) 'the projection list's retryable population is unreachable in production' is FALSE (`transcriptLiveness == .notLiveRightNow` and a deferred store both pass `isProjectableAtThisPass`) — the commit message, this row and the test header are wrong, the file header is right; (4) neither `MeshRoutedRetryAllowanceTests` nor the new drain cell is on a CI line. `ackableNow` is the right place but a bare `Bool` gives item 6 no channel for the heart COUNT (`heartsPending` → 0 the moment hearts answer false) — item 6 widens it. Item 8 held (P3: one roster-moving cell ends on `defer { teardown() }` where the rig asks for `quiesce()`) |
| 6 | Hearts on the routed store | 1 | 1, 5 | done | `ed66042`, fixes `e573a18` | **Fix commit landed `e573a18` (2026-09-12, session 2; handoff `item6-fix-handoff.md`):** P1-1 = `reassertCommitIntoAdoptedMesh()` (`MNM:9192`, called at `:9144/:9151`) — the yielder re-raises `.peerCommitted` for every already-committed slot after adopting the winner's mesh; blast radius MEASURED: `sessionState == .activeForeground` has exactly ONE shipping reader tree-wide (`mayCommitRoutedHeartLedgerJudgement`), the raise is from `.joining` which `openBlipMergeIfReconnected` excludes by name (one raise, no loop; tail = give-up clock cancel + beacon start, no rotation for the yielder); `MeshFoundingRig.ensureForeground` DELETED with its four call sites; cells `aHeartToTheYieldingFounderIsJudgedWithNoManufacturedCommit` + the founding cell now asserts the yielder's state. P2-1 feed hoisted above the ack guard (`:7539`, `aDirtySidecarFeedsClosenessOnceAndAcksOnTheFlush`); P2-2 swapped + `everyHeartFailureCauseHasItsOwnSentence` per-case pin; P2-3 `@autoclosure` evidence (`MeshRoutedDeliveryCommit.swift:153`, `stageShortfall` calls it last; `aRefusedDeliveryNeverResolvesItsEvidence`); P3 1/3/4/5/6 taken (`mesh.routedShare.recipientIsSelf` token; nil ledger fails closed; FINAL-marked heart → `.unfinishable`; `DisposableCameraView.sessionHeartTransport(...)` + 8-row behaviour table). Residuals by name: the save-failure leg of P2-3 (evidence must precede the stamp — heals on a later pass); `routedAckEvidence`'s `deliveredAt == nil` guard unobservable at tier 1; two new audit tokens in no frozen-vocabulary pin; handoff §5 items 3–8 stand. No catalog keys added (19 total). Five red-once logs. **Full suite 4826 / 478, zero issues, 1643 s = new baseline** (`logs/item6fix/full-01.log`). **Fix review (scratch `item6-fix-verify.md`, of `e573a18`): 0 P1 / 0 P2 — ITEM 6 CLOSED.** All nine claimed fixes CLOSED (blast radius re-verified by grep: one shipping reader of `.activeForeground` at `:8822`; the raise is `.joining`-only; `ensureForeground` gone tree-wide; the evidence closure resolves exactly once on success and zero times on all twelve refusals). Six P3s: (a) `reassertCommitIntoAdoptedMesh` (`:9194`) takes `slots.compactMap(\.fingerprint).first` — an ARBITRARY committed slot, in a 3-phone shape possibly a peer not in the adopted mesh (inert today only because `openBlipMergeIfReconnected` excludes `previous == .joining`) → pick a slot on the derived roster or thread the grant's sender down; (b) `:9151` the re-assert's `.persistContext` failure is unchecked, so `recordVerifiedAdmissionDurably()` returns true after a refused seal (context carries no `sessionState`, so cheap) → return `lastSessionEffectFailure == nil`; (c) `:11338` the fix is bound to the grant door — an adoption with no grant sits at `.idle` (self-heals via descriptor re-broadcast; item 9 keys its invariant on 'admitted', not 'holds a mesh'); (d) `:7539` a process death between a `.dirty` accept and the flush double-feeds closeness (day-capped downstream; residual by name); (e) `DisposableCameraView.swift:1712` `(meshLinked, !presence, !addressable)` yields `.unavailable` while `reachable` is true → button enabled, tap silent (pre-existing, now legible) → `let reachable = transport != .unavailable`; (f) two interpolated `#expect` comments (`MeshRoutedDeliveryAckTests.swift:500/:513`, matching 18 pre-existing in that file) + no exactly-once pin on the evidence closure's success path. **Routing: (a)(b)(e)(f) → item 9's run as its own file-disjoint hunk set (SET A); (c)(d) → residuals, item 9 reads them first.** **Adversarial review DONE 2026-09-11 (session 2; scratch `item6-verify.md`): 1 P1, 3 P2, 6 P3; 13/14 REQUIRED applied, #2 partial; SET A applied except A3's ledger half (row 5 corrected).** P1-1: the ceremony's foreground leg is unreachable for a YIELDING FOUNDER — `.activeForeground` is reached only via `.peerCommitted` (`MeshSessionStateMachine.swift:376`) raised only from `noteCommitIntoMesh` (`MNM:10720`, guarded `currentMesh != nil`); the yielder resets to `.idle` (`:1902`→`:1954`) and re-enters `.joining` via `joinDurably()` (`:9094`) with no further commit → in a steady two-device session a heart to the yielder EXPIRES CUSTODIED while the sender saw Sent and burnt the cooldown; `MeshFoundingRig.ensureForeground` (`MeshRoutedHeartTests.swift:775–778`) manufactures the state (upgrades the builder's owner-eye residual from delayed to lost). P2-1: closeness dropped for good when the first judgement stores the gift but `commitProof` is nil (`.dirty` sidecar, `ProximityHeartLedger:284`) — `:7481` returns before the feed at `:7491`, the re-judgement lands in `refusedGiftIDs`. P2-2: `SessionHeartStatusCopy.swift:80–83` swaps the two store causes' sentences (the exhaustive copy test cannot see it). P2-3: R2 NOT applied as claimed — `evidence:` is an eager argument (`:7338`) so the ceremony runs before every `committingDelivery` guard; the door doc (`:7327–7329`) says otherwise. P3: stale `reentryHeartStage` doc (`:6949`); `heartsPending` conflates FINAL-refused with deferred (`:6767`); `recipientIsSelf` audited as `destinationSemanticsMismatch` (`:6208`); nil `heartLedger` fails OPEN at the sender (`:830–832`); the presence-order pin is a source-text scan (`MeshSessionHeartTests.swift:304`). Held under attack: one `MeshHeartCommit.commit` call site; `proof.giftID == giftID` + `itemID == giftID` enforced; the zero-list genuinely zero tree-wide; R-19 a real 16-heart negative; cap a formula; CI floor 171→240 with all seven added suites declared. **Fix commit IN FLIGHT** (brief `item6-fix-brief.md`: P1-1 = re-raise `noteCommitIntoMesh` for committed slots after the yielder adopts; P2-1 feed closeness above the ack guard; P2-2 swap + per-case key pin; P2-3 autoclosure-or-guard; cheap P3s). **DONE 2026-09-11 (one commit with the 5/8 fixes; full suite 4820 / 478, zero issues = baseline):** row flipped in place (freezing rule amended, allowance spent), cap 553 B; `MeshDeliveryTarget.addressing` + `RoutedOriginationAudience`; `unsupportedDestinationSemantics` narrowed; sender five gates + `hasLiveHeartSlot`, consume-on-stage; `SessionHeartFailure` + `SessionHeartStatusCopy` (localization hole closed); presence order live slot → presence → routed; ONE ceremony door in `commitLocalDelivery` after its guards, closeness on `receivedGiftIDs`, blocked = FINAL + marked, `.heartLedger` not projectable; `.friendHeart` mesh path to a zero-list (`PayloadType.friendHeart` NOT parkable); W2 `homes: Set`, ack pin 0→1. All 14 REQUIRED applied. Handoff: scratch `item6-handoff.md` (11 catalog keys → 19 total; 8 residuals; policy acts). **Adversarial review NOT yet run — first step of the next session.** Owner-eye residual: a yielding founder can hold a mesh at `sessionState == .joining`, where the heart stage (the only routed predicate reading the session) judges nothing until a further commit. **Designed 2026-09-11** (scratch `item6-design.md`, 1030 lines; adversarial check in flight). Anchors at 193cfd7: heart row `MeshRoutedTypeRegistry.swift:316–324`, `sendSessionHeart :795`, `reentryHeartStage :6343`. Flip in place + cap narrowed to header allowance + seal overhead (header-only body `{id, sentAtDayKey, senderName}`, four hostile shapes); `MeshDeliveryTarget.addressing(contentID:recipient:roster:selfFingerprint:)` (recipient = caller arg validated against the roster, never body-derived; `recipientIsSelf` / `recipientNotInRoster`); `RoutedOriginationAudience` sum type is the column's real reader; **`unsupportedDestinationSemantics` narrows, does not retire** (2-member full-roster vs subset are indistinguishable by inspection). Sender: five gates kept, three lines, consume-on-stage (two named costs); `canSendHeart` is fail-closed for an UNLOADED ledger where today's copy lies. **`SessionHeartState.failed(message: String)` is a live localization hole invisible to both scanners** → `SessionHeartFailure` token + app `SessionHeartStatusCopy`. Ceremony: evidence resolved INSIDE `commitLocalDelivery` (`routedAckEvidence`, gated `deliveredAt == nil`) through `MeshHeartCommit.commit` — one call site, three doors; `isHeartEligibleFriend` refactored to a `(signingPublicKey:fingerprint:)` primitive fed the LEDGER's key. **Refuses the launcher on two points:** `.heartLedger` must NOT join `projectableRoutedTypeTokens` (job 5's list never shrinks for hearts → 16-slot starvation + re-stream on every restart; the heart belongs to job 4); `PayloadType.friendHeart` is NOT parkable (presence still uses it) — only the three mesh functions go to the zero-list. **Adds:** R-19's enumeration bound on `itemsAwaitingLocalAck` (16 unjudgeable hearts would starve photo+text receipts, position attacker-choosable). W2 pin table `home` → `homes: Set` (`.heartLedgerCommit(` legitimately in two files). Item 5 remainder: sub-allowance for retryables on BOTH retry lists; FINAL marks memory-only (no index bump); cooldown/foreground/unloaded = retryable, not-a-friend = FINAL; **item 6 does not close item 5 — keep 4 → 5 → 6.** Adversarial check (scratch `item6-design-check.md`): the two hardest calls (no projection-list entry; one-door ceremony) are RIGHT; **14 REQUIRED**, 3 real defects: (1) closeness DOUBLE-FEED — `onHeartReceived` fires on every ack mint and `commitProof` is non-nil for an already-stored gift → gate on `outcome.receivedGiftIDs.contains(itemID)`; (2) the ceremony (unwrap + durable ledger write + hook) must run AFTER `committingDelivery`'s own guards; (3) the `routedProjectionAuthor == nil` leg (locally BLOCKED member — blocking ≠ removal) is never marked → 16 hearts park on job 4's list until expiry: R-19 reintroduced by the edit that closes it; plus: presence never wins again (`DisposableCameraView` `if meshCapable` → order = live `.hearts` slot → presence if reachable → routed-custodied); the W2 table needs 'exactly one row has `homes.count > 1`'; `finishLocalRungs` calls `projectRoutedItemIfPermitted` unfiltered (a judged heart logs `noDispatchArm`); two `heartsPending == 1` gate-open pins flip to 0; the shape fence is one-directional; `mayCommitRoutedHeartLedgerJudgement`'s 'inert until P8' doc is stale; the localization hole is MEASURED (5 of 6 package-composed heart sentences absent from the catalog). Tier 1 proves the ceremony; **item 10's two-session lane is the only honest proof of the product** |
| 7 | sentAt monotonicity guard (+ the bundle: launch restore wiring, item 9 stage 0, item 4's three P3s, item 1's two notes) | 1 | — | done | `896d96a`, fixes `a2df52d` | **Review fixes landed `a2df52d` (2026-09-12; handoff `item7-fix-handoff.md`; full suite 4865 / 486, zero issues, 1846 s = new baseline). **Fix review (scratch `item7-fix-verify.md`): 0 P1 / 2 P2 / 7 P3 — the BEHAVIOUR fix is sound (attacked ten ways: offers are rung-gated via `offerableKeys` → `outstandingItems` so no re-offer loop / frame burn; the peer-side binding holds because the stepped-back digest IS the peer's latest; no hostile ceiling; `==` admitted + recorded; one read + one write of `inventorySentAt` in all of FernletKit). Both P2s are claim-strength, folded into item 10's run as its SET A (third pass on item 7 = stop):** P2-A the new wall sentence ('four spellings pinned so a fifth cannot appear unnoticed') is FALSE today — a fifth digest-door refusal exit `mesh.merge.routedQuiescentUnbound` (`MNM:5470`, the drain-answer binding guard) is pinned nowhere → pin at 1 + 'five', or brace-match both door bodies and pin the `FernletAuditLog.log(` count like `doorCalls == 8`; P2-B I-13 is green over EQUALITY only (`neg-03` inverted strictly-greater arm stayed green ⇒ zero strictly-greater pairs; `neg-04` flipped equality arm reddened 646 ⇒ all 646 pairs `==`) and its new doc claims a tolerance would redden it (it would not) — dropping `later >= stamp` made the claim DEFINED by the door it tests → assert both spellings and say what `neg-03` measured. P3: 'began/ended keep pairing' false in 4 places (`ended` fires only at depth 0; the overflow branch returns first); `recordsQuiescence:false` protects a stamp with no reader + `localQuiescent` (one reader, D-7.11 'gating nothing') while 5 places call it 'item 7's window rule'; the P3-6 order wall pins textual precedence not containment; the overflow cap + `saturated` arm unwalled; a re-aimed cell's frame assertion is a rung coincidence; one message-less `#expect`; 'never a stalled delivery' misses the peer-holdings-shrink shape. Tally 6 CLOSED / 5 PARTIAL (claim-strength or doc) / 0 NOT.** P2-2: the guard now refuses the RECORD and the answer ALWAYS runs (`MNM:5300`; `answerRoutedInventory(…recordsQuiescence:)` `:5372` skips only the quiescence re-stamp; `advertisedAt` on the wire stays the digest's own `sentAt`, so the peer's `advertiserFingerprint`+`advertisedAt` binding holds); cells `aStaleDigestIsRefusedAsARecordAndStillAnswered` + `aPeerWhoseClockSteppedBackStillReceivesANewlyMintedItem` (`MeshRoutedDrainTests.swift:679/:717`); docs: door, `MeshRoutedInventoryStampRule.refuseStale`, DocC, D-12.12 (ii) + §23.3, index. P2-1: I-13 calls `MeshRoutedInventoryStampRule.verdict(inbound:recorded:) == .admit` (`…ConvergenceTests.swift:1212`). P2-3: fourth digest-door spelling pinned at 1 (`MeshRoutedRefusalBudgetTests.swift:205`), `total == 5` / `doorCalls == 8` unmoved. P3 1/2/3/5/6/7/8/10 taken (`privacyWipeOverflow` symmetric counter `MNM:1100/:12525/:12549` + cell at the cap; the launch-mount tautology → an order wall; `count(of:)` with the placeholder context dropped). Residuals: P3-4 (I-13 two-sample, blind only to a VANISHED pair — legal under `clearRoutedDrainState()`); P3-9 → items 10/11. Three negative batches over four builds (`neg-03` a recorded non-result: the rectangle's I-13 pairs are all equal, only the flipped equality arm reds, 646×). Two full runs voided by the `ConnectionInspectorTests` load flake (surprises). **DONE 2026-09-12 (one commit, session 2; handoff `item7-handoff.md`; full suite 4837 / 478, zero issues, 1677 s = new baseline). Review (scratch `item7-verify.md`): 0 P1 / 3 P2 / 10 P3 — no fix needed BEFORE item 9 lands on it; fix commit queued right AFTER item 9 (brief `item7-fix-brief.md`).** P2-1: the 'door and claim cannot drift' wall does not exist — I-13 hand-spells `later >= stamp` (`MeshRoutedDrainConvergenceTests.swift:911`) and never calls `MeshRoutedInventoryStampRule`, while shipping source/DocC/index/plan/commit all say it does → assert `MeshRoutedInventoryStampRule.verdict(inbound:recorded:) == .admit` (one line; item 9 is editing that file → after item 9). **P2-2 (behaviour): a peer whose clock steps BACKWARDS ≥ 1 s is silently unanswerable for the session** — the door guard (`MNM:5289`) suppresses the answer, `answerRoutedInventory` is `sendRoutedDrainBatch`'s only caller, the digest fires only from the three merge doors (no timer), so every delivery to that peer stalls for the step's duration (up to the 6 h ceiling), invisibly; item 12's named cost was 'a stale delta, never a lost delivery' → **fix = the launcher's placement: refuse the RECORD (view + `quiescentLocalAsOf` stamp) by name, still ANSWER from the recorded view (a redundant offer is budget-bounded; delivery never stalls); say it in the door doc + D-12.12.** P2-3: the refusal-budget wall's 'a fourth cannot appear unnoticed' is now false (`MeshRoutedRefusalBudgetTests.swift:194–201` pins one token count; the fourth digest-door refusal's token is unseen; budget itself unmoved — `total == 5`, `doorCalls == 8`) → pin `mesh.routedInventory.staleSentAt` at 1. **Part 2 honesty verdict: materially real, not hollow** — `startJoin()` keeps the restored `membershipVerifier` / `keyAdvertisements` / `epochHeads`, so a re-link into the SAME mesh (descriptor adoption `:11429`, `prepareMembershipLedger`'s same-meshID early return) drains what a relaunched member previously could not, and the rejoin bar is re-derived at launch (claimed since P3, never done on device); but nothing on the fresh-founding path (a new `UUID()` discards the restored ledger), nothing in `App/` reads any restore state (P7's resume UI still owed), the only end-to-end cell sets `currentMesh` by hand, and the handoff's second citation (`MeshRoutedLockedDeviceTests.swift:524`) proves the OPPOSITE claim. Verified independently: both digests byte-identical (the suite lives in `MeshP5AcceptanceTests.swift`, not a file of its own); the 250 comment term-by-term against the result bundle; five hostile shapes = five throw sites; both neg logs; no injection left. Part 1: the guard sits on the DOOR (`receiveRoutedInventory`), not inside `recordPeerRoutedInventory` — a strictly-older `sentAt` is refused by name (`mesh.routedInventory.staleSentAt`), nothing recorded and NOTHING ANSWERED, so `inventory` / `inventorySentAt` / `quiescentLocalAsOf` / the plan all stand; `==` admitted silently; no budget charge, door count unmoved, roster-cap path untouched; decision = pure `MeshRoutedInventoryStampRule` (`MeshRoutedDrainPlan.swift:85`) which **I-13** also reads; I-13 folded into `MeshRoutedRungSnapshot.inventoryStamps` (no 40-cell signature grew a parameter), non-vacuity beside the sample; 4 tier-1 cells; I-13 red under an injected regression (299 issues, `neg-01.log`). Part 2: `restoreSessionContextOncePerLaunch(now:)` (`MNM:9491`, latched on `sessionRestoreAttempts`) called once from `FernletApp.swift:496` (`.onAppear`, one statement after the gate push), pure decision + Lane C bypass keyed on `FERNLET_MESH_MATRIX=1`. **Headline: a restore leaves `currentMesh == nil`, `isInSession` false, the Friends three-way resolves `.fresh`; ZERO app readers of `offersForegroundResume` / `restoredSessionContext` / `lastSessionRestoreOutcome` / `rejoinBar` — nothing is presented for any outcome** (the door is now called; whether that makes 'resumption → delivery' true on device is the review's question — likely still P7's resume wiring). Part 3: stage-0 seams inside the existing file/rig; no overlay field / event case / suite / CI line; **both determinism digests unchanged (schedule `ca898bcc…6930`, overlay `f1cc626d…3ff9` — labels per the source; item 9 moved the overlay one)**. **Finding: §2d's arming-skipped negative is STALE and does not red** — item 1's `repairOwnKeyAdvertisementIfMissing()` self-mints on the heal's first ask; the cell now asserts that mechanism; the load-bearing negative is the fail-closed chat gate (reds) → item 9 drops §2d's negative. Part 4: CI floor comment re-measured per suite (105+4+8+27+32+30+44 = 250 over 41 suites; 'five' hostile shapes) — **floor left at 240 against a measured 250; item 9 re-pins at its own measurement**; `privacyWipeInProgress` derived from a bounded `privacyWipeDepth` (cap 8, saturating + audited); header-allowance doc trimmed (no decode-time guard). Part 5: (a) CLOSED, nothing stale; (b) window CONFIRMED by a probe that reddened `theAdvertisementSetNamesNoSilentMergeDoor`, reverted. Residuals by name: two `<SHA placeholder>` strings in the plan (item 11); the Lane C runbook sentence owed; item 4 fix-review P3-4 not taken; I-13 admits a vanished pair without an excuse; the wipe counter saturates at 8. Catalog keys: none. Four new audit tokens in no frozen-vocabulary pin |
| 8 | Departed-origin custodian-forwarding cell (second rig) | 1 | — | done | `d8822ed`, `quiesce()` fix in `ed66042` | four-node no-partition rig; forwarding leg un-records the repaired slot (count −1), repaired slot `.admitted` vs untouched `.replayed` control, real refill re-records; red once with `forget` disabled; in `MeshRoutedDrainTests` (ungated like its siblings — gating = +56 floor, owner's call) |
| 1c | Load flake, now RECURRING: `MeshP4QuorumAcceptanceTests.aTwoTwoSplitOfAFourRosterRemovesNobodyAtTheManagerSeam` (`:373/:381`) + `MeshQuorumManagerSeamTests.anIncompleteProposalWritesNothingAtTheManagerSeam` (`MeshQuorumPartitionTests.swift:877`) — verdict `.expired` vs `.pending` under a 470-suite load; green isolated; seen in 2 of the last 4 full runs (item 1 second fix, both its full runs) | 1 | — | done | `0665d0c` | Cause: `proposalLifetime` (300 s) is REAL wall clock stamped `firstSeenAt`, and the two cells read the verdict at `Date()` — the only two wall-clock verdict reads in the mesh suites; under load a cell's own elapsed time crossed 5 min. Fix: read at the node's own `firstSeenAt`; terminal settles end on a predicate; assertions kept + a `#require` the proposal is open; red once (7 issues incl. 3 sibling sites), green 3× under a 166-suite load. **Full suite 4736 / 470, ZERO issues, twice = the clean baseline.** Residual (reviewed): the receive dispatch stamps `Date()` with no seam and `issuanceSkewAllowance` (600 s) is a second wall-clock leg on the same seam — the new failure shape would be `pending(counted: 1)`, not `.expired`; needs an injectable `now` at the dispatch. Verdict: FIXED, not weakened (expiry is pinned at the value layer with an injected clock) |
| 9 | P6 acceptance battery + CI gate lines | 1 | 1–8 | done | `ee4c7de`, fixes `0e182bb` | **Review fixes landed `0e182bb` (2026-09-12; handoff `item9-fix-handoff.md`; full suite 4865 / 486 unchanged — every change amends an existing cell; floor stays 300; both digests asserted unchanged inside the run; fix review IN FLIGHT, read-only, parallel with item 10).** P2-1: a refused re-assert now UNWINDS its own raise — `MNM:9355` captures the pre-raise timers, `unwindRefusedReassert(beaconWasRunning:rotationWasScheduled:nextRotationAt:)` at `:9379` restores `.joining` and stands down only the beacon/rotation THIS raise started (the give-up clock needs nothing put back: `armSessionGiveUpClock` refuses to arm while a slot is committed, and this door runs only when one is — the cell pins it un-armed both sides); seam `isBeaconLoopRunningForTesting` `:13857`; the cell (`MeshSessionStateMachineTests.swift:1144`) has three legs on separate managers. **P2-2 was the round's real finding:** probe (b) alone (leg 4, `projectableRoutedTypeTokens`) is GREEN and so is leg 3 alone (the arm's `guard isChatAllowed`) — the row is received and held and `SessionMessageStore`'s VIEW gate hides it; only leg 3 + forcing the view fold open reds the cell (`neg-06`) → the cell walls the VIEW, the three runs written into its doc (`MeshP6AcceptanceTests.swift:306–325`); a fourth observable (`routedProjectedItems` mark) was measured false (delivery-time projection marks either way) and withdrawn. P2-3/P3-10: `try #require` at `…ConvergenceTests.swift:2555`; every audience widening through `routedRosterWideAudience(_:_:)` (`:1186`, registry-checked); no `?? [recipient]` survives. P2-4: complement loop (`MeshP6AcceptanceTests.swift:586–596`, no record / no ledger row / no judgement at any non-destination) reading `MeshRoutedHeartJudgementLog.judged(_:at:)`. P3 1–9 taken. Residuals by name: leg 3 has no wall of its own (its honest wall = the `mesh.routedProjection.transcriptAgeGated` audit line); leg 4 unobservable in this rig; `routedRosterWideAudience` falls back to `.photo` when no record survives anywhere (unreachable today); the complement loop is vacuous on `twoOne`. Process lesson: `neg-01` named `MeshSessionStateMachineTests` for a cell that lives in `MeshSessionLifecycleManagerTests` in the SAME FILE — the probe proved nothing until re-run (a file holds several suites; name the suite the cell is in). Seven negative batches, restored byte-identically. **DONE 2026-09-12 (one commit, session 2; handoff `item9-handoff.md`; full suite 4863 / 486, zero issues, 1727 s = new baseline (+26 cells, +8 suites)). Review (scratch `item9-verify.md`): 0 P1 / 4 P2 / 10 P3 — every headline number re-derived and correct (8 draws, fields 1–8 byte-identical, no new event case, exactly one digest literal moved, floor 300/50, pin 36, ungated 284); small fix commit queued AFTER the item 7 fix commit (brief `item9-fix-brief.md`).** P2-1: `aReAssertWhoseOwnSaveIsRefusedAnswersFalse`'s ACCEPTED leg is vacuous (`MeshSessionStateMachineTests.swift:1131–1156` — `applySessionEvent` moves state before effects, so the refused call already left `.activeForeground` and the second call returns true from the `guard sessionState == .joining` early exit); underneath, SET A (b)'s `false` path leaves the state move, the cancelled give-up clock and the started beacon UN-UNWOUND while `handleAdmissionGrant` rolls back the verifier → second manager for the accepted leg + decide whether the false path unwinds the raise. P2-2: §2c's projection-side 13+ gate probe (`MNM:2941`) was only ever run against the VACUOUS cell — after the `gatedCorners` fix it was never re-run → `neg-04` with probe (b) alone. P2-3: `routedJudgedAudience(...) ?? [recipient]` then `#expect(audience == [recipient])` is a tautology when nil (`MeshRoutedDrainConvergenceTests.swift:2503–2507`) → `try #require`. P2-4: nothing asserts the COMPLEMENT of the `.singleRecipient` flip (`MeshP6AcceptanceTests.swift:544–564` — a heart admitted at a non-destination is invisible; the witness arms origin+recipient only) → loop asserting non-destinations hold no record. P3 ×10: no pin that `oneCellPerShape` holds an asleep cell (1 of 5 today); two new interpolated `#expect` comments; two overlong lines with a lost continuation; `MeshRoutedLockedDeviceTests` 25 vs 26 (26 correct); the batch-counter wall's hand-maintained 2-file scope; R8's cell proves half its invariant; the handoff's neg-03 red list omits two cells; plan §12.4 labels two rows '(j)'; a DocC insert splits a paragraph; `routedJudgedAudience`'s nil fallback re-opens R3's trap where it fires. REQUIRED tally 12/14 clean, R3 with a residual, R8 half; SET A (a)(e)(f) clean, (b) = P2-1. **Digest DECISION (source-authoritative labels — row 7's note had them swapped): `pinnedOverlayDigest` MOVED `f1cc626d…3ff9` → `594b6f77d18703e3b3f3d180473869360999061b6d207206ba314b0896d55765`; `pinnedScheduleDigest` `ca898bcc…6930` UNMOVED (asserted green).** Seven fields appended after field 8, EIGHT draws (R2), all after `let unknown`, each in `description`; old literal shown red once before the re-pin. **CI floor MEASURED 300 over 50 suites** (was 240/41; comment recomposed per suite from the run's own bundle; `gated-mesh` log `300 test(s) ran, 0 failed`); the eight `MeshP6*AcceptanceTests` + `MeshRoutedLockedDeviceTests` added; `CIGateSelectorBoundaryTests` pin 28 → 36 (measured — items 6/7 declared none); `MeshRoutedDrainWallTests` added to the named-walls pin. Measured fact: `totalTestCount` counts `@Test` FUNCTIONS, not parameterized cases (the check's C3 assumed otherwise). All 14 REQUIRED mapped; O1/O2/O4/O6/O7 taken, O3 declined with a reason; **R11 moot and better — no shipping field added: the exactly-once witness rides item 6's existing `onHeartJudgedForTesting`, so no wipe row and no catalog key.** Three design corrections found by MEASUREMENT: (1) SET A (a) 'pick a slot on the derived roster' RE-OPENS P1-1's outage (`armJoinerLedger` bootstraps from this device's own admission alone, so the roster does not know the admitter yet) → the grant's SENDER is threaded down, the roster is the fallback; (2) a deferred (backgrounded) heart files NO receipt of any kind (`ackableNow` skips it before the allowance is planned) so its rung cannot move — `blocked` is type-scoped so rectangle A keeps I-8's non-vacuity; (3) the `.judgements` grep needle matches `.judgementsForGift` — the wall strips the per-gift spelling first and assembles its own needle. 15 mutations in three reverted batches (`neg-01/02/03`); two 'did not red' were real findings and fixed (the age-gate clause was vacuous on corners that drew no gated member → `gatedCorners` selected by arithmetic; a 553-literal cap probe was a no-op); two honest non-results (item 6's `deliveredAt` gate is a genuine second belt; R3's narrowing removes a false green, not a failure). SET A (b)(e)(f) landed. **Ungated, deferred to item 11 with counts: `MeshRoutedDrainTests` 41 (holds item 8's cell) + ~243 more P6-relevant cells, itemised in the handoff.** po10 0 (density 0.780), doccov 0, `WALL CHECK PASSED`, subset 1104/100. **Designed 2026-09-11** (scratch `item9-design.md`; adversarial check in flight). Fields 1–8 = 11 draws at `MeshConvergenceSchedule.swift:787–796`, nothing after them consumes a draw; new fields 9–15 (9 draws: `textOrigin`, `textRound`, `ageGatedMember` 2-draw subject, `heartOrigin`, `heartRecipient` over survivors ∖ origin, `heartRound`, `heartRecipientForegrounded`), all appended to `description` or the digest silently doesn't move. `pinnedScheduleDigest` cannot move (three independent RNGs; new tripwire: re-ordering `MeshPartitionShape.matrix` or `MeshConvergenceSeeds.family` moves BOTH). Text via the REAL `sendTempMessage` (so item 1's residual becomes a failure, not a silence); heart via a fixture mint on item 6's target; arming via `armOwnKeyAdvertisementForTesting` per member + bounded `runKeyAdvertisementRounds()`. **Three tripwires:** `chatAllowedProvider` nil ⇒ fail-closed → the whole text half is green over nothing without `allowChatEverywhere(except:)`; exactly-once needs a new memory-only `routedHeartJudgementsForTesting` (the ledger dedups, so counting hearts measures the dedup) + a `.judgements` zero-count grep-wall + a two-heart-batch negative; a new convergence SUITE would evade `isMeshBattery` → seams go inside `MeshRoutedDrainConvergenceTests.swift`. Pipeline 3 (feature mints after the heal), rectangle G = 12 cells. EIGHT suites (+ Honesty), eleven reverted mutations. CI: append the eight + gate `MeshRoutedLockedDeviceTests` / `MeshRoutedDrainWallTests` / item 5's two suites; floor MEASURED (parameterized cells); selector pin 28 → 36; gate `MeshRoutedDrainWallTests` now, defer `MeshRoutedDrainTests`' 37 cells to item 11. Item 7's `inventorySentAt` assertion = a 13th claim in `routedInvariants` in item 7's own commit. ONE commit; record both digest values (a moved digest with no before-value is indistinguishable from a re-pin). **Adversarial check (scratch `item9-design-check.md`): fields 1–8 = 11 draws VERIFIED byte-identical after an append; `pinnedScheduleDigest` cannot move; survivors ≥ 2 on all 40 cells so every new draw is total; 28 batteries today. 14 REQUIRED, four load-bearing:** (R1) the `.judgements` zero-count wall contradicts the design's own `outcome.judgements == 2` cell in the same file → `allowedTokenLines` idiom; (R3) `routedInvariants` is NOT key-generic for a `.singleRecipient` heart — I-2 loops the whole roster and `routedDeliveryState` answers `.reclaimed` for 'no record', so a heart passes for every non-recipient for the wrong reason (I-8 `blocked` and I-4's photo-only projection arm share the shape); (R4) the asleep quarter must use `applySessionEvent(.backgrounded)` → `.continuingInBackground` (no `.activeBackground` exists) and nothing in shipping raises `.backgrounded` — honesty line owed; (R5) `acks.isEmpty` is an absence claim → held ciphertext + no ledger row + `.foregrounded` re-drain ⇒ exactly one ack. Others: 8 draws not 9; the 36 pin collides with item 6's own suite; item 4's session-end invariant dropped; `Message.sentAt` IS the clamped instant; the ungated list omits item 1's 30 cells, 51 photo-delivery cells and `MeshRoutedDrainTests` (where item 8's cell lives); `MeshRoutedLockedDeviceTests` is 25 behaviour cells, not a wall. **Recommendation taken: a STAGE-0 commit first** (arming/chat/convergence seams + the arming-skipped negative — moves neither digest, answers 'does tier B converge in this rig') bundled into item 7's run; the overlay + rectangle G + eight suites + CI after 6 AND 7 |
| 10 | Tier 2: Lane C .chat re-run + two-session hearts/moderation script | 2 | 4, 6 | todo | | **Planned 2026-09-11** (scratch `item10-plan.md`, all facts read at d8822ed). Three facts reshape it: (1) a ROLE-LESS Lane C run cannot carry any routed flow (empty introduction roster → `.stranger`; seed without `FERNLET_MESH_ROLE` → `membershipVerifier == nil` → `.skipped(.noDestinations)`) — the runbook's 2026-09-11 note (~:1244) is wrong for QUIC (holds only on MC); (2) flows fire ONCE on the first committed tick BEFORE the grant → `.noDestinations` is the only outcome a `FLOWS=chat` run can produce today → new hook `FERNLET_MESH_FLOWS_AFTER=<polls>` (same walled `FERNLET_MESH` family); (3) no routed audit line reaches `--console-pty` → per-sim `log stream --predicate 'subsystem == "com.fernlet"'` + `private_data:on`. Four text runs (founding window; routed path with 10 named pass lines and `grep -c fernlet.message.temp.v1 == 0` as the retirement proof; `.chatAgeGated` three-leg negative; optional app-path founding over MC). Two-session hearts (blocked on item 6): `allowNearbyHearts` defaults FALSE (flip before `startJoin`); hooks `FERNLET_MESH_ALLOW_HEARTS`, `FERNLET_MESH_AUTO_KEEP_FRIENDS` (calls the shipping keep + `completeFriendReview`, in the poll AND after `leave()`), `MeshFlowVerb.heart`, `install(manager:store:)`; session 1 ends `terminated.v1` → permanent rejoin bar → session 2 uses a different mesh id. **Findings:** a headless sim IS foreground (`sessionState` never leaves `.activeForeground`; the lane cannot prove that leg); **`restoreSessionContextAtLaunch` has NO shipping caller** (identities + vault persist across relaunch, the session context does not); moderation is independent of the trust vault (`proposeRemoval` gates on mesh + ledger + target ≠ self) → the vote needs three sims + three DEBUG seams for the SIGNED family; not observable: CPT, the foreground leg, data protection (`simctl` has no lock verb) → owner/Lane B |
| 11 | Close-out: §12 BUILT, §24 P7 handoff, P7 launcher, memory | 1 | 1–10 | todo | | draft → verify → apply from files |

## Blocked on owner
- **READ FIRST (item 2 design check, 2026-09-11): the app's proximity-join path has NEVER exercised the routed content path.** `startJoin()` → `promoteToMesh()` arms no membership ledger at any roster size; `startNewMesh` (which does) is unreachable from any user action; every P3–P5 Lane C run and tier-1 rig seeded the ledger through a harness door. Plan §11.3 item 13(i)'s "until a second peer commits" understated a pairwise outage that is in fact the whole proximity-join content path. Item 2 fixes it; the plan's §11.3/§23.2 lines are amended when it lands. **Three policy acts ride with it:** promotion at one commit; the 15 cm dwell as consent for ONE auto-granted admission (prompt kept for closed meshes / 3rd+ / non-join); and a founder election over durable fingerprints that an adversary can grind (~16 keypairs beat any peer over a 64-bit fingerprint) — founder = admitter = the joiner's ledger root is therefore selectable by whoever cares to; what founder status buys inside a session the user already consented to is the question for the owner.
- **TAKEN in item 7 (`896d96a`, default: launch mount behind the gate) — see row 7's Part 2 headline; whether it is still hollow on device (no app reader of the outcome, `currentMesh` stays nil) is the review's call.** Was: **(item 10 plan) `restoreSessionContextAtLaunch` has no shipping caller** — a relaunch restores identities and the trust vault but NOT the session context, so every resumption path P3 built (restart / idle-lapse / rejoin) is unreachable from the app; likely P7's wiring (the run policy) but it hollows out item 1's 'resumption → delivery' claim on device — decide whether item 6/7 wires it or P7 does.
- **`ConnectionInspectorTests.beginSessionCreatesLiveLog()` (`:33`) runs ~206 s under full-suite load against `.timeLimit(.minutes(2))`** — a 1c-shaped wall-clock flake in a non-mesh suite; three voided full runs this round. Raise the limit or make the cell load-independent (owner's suite).
- (item 3, review finding 3) an over-cap manifest refusal is charged to the RELAYING custodian and the capacity exemption does not cover it — `routedRefusalIsHeldForCapacity` keys "named but never charged" on `routedRefusedKeys` (capacity refusals only), so under the cross-build cap disagreement the non-dropping arm anticipates, an honest custodian re-offering burns one of its 1056 per-peer refusals per exchange. Either record the item key the way a capacity refusal does, or record that charging the forwarder is the chosen policy.
- Option (b) for handleEncryptedMetadata; D-7.30 once-per-window; §18.2 copy; legacy unsigned removal; transcript sid; hardware lanes (Lane A/B/D, AWDL); census/duress for the two mesh keychain services; final wording of the routed hold/refusal copy. (Main `3a32be0` == origin/main; S3 Wall + Power of 10 workflows both green on it, 2026-09-06.)

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| Key advertisement family shape | (default: additive family, own door, not a record kind) | — |
| Key advertisement persistence | (default: MeshSessionContext 2→3, older corrupt) | — |
| Pairwise identity | (default: promote at one commit) | — |
| Heart destinations | (default: .singleRecipient flipped in place + subset init) | — |
| Heart send feedback | (default: consume-on-stage) | — |
| Second expiry rule | (default: not added) | — |
| routingInventoryDigest | (default: retired in item 1's schema bump; §23.4 says owner's — amend §23.4 in that commit) | — |
| Items 1–2 despite §23.4's owner gate | taken under the post-close review's recommendation (P5 ledger: "recorded at the head of P6's list"); reported as policy acts at close-out; launcher verified against HEAD (62 claims, 10 corrected) before commit | 2026-09-10 |

## Surprises worth not re-deriving
- **`ConnectionInspectorTests.beginSessionCreatesLiveLog()` is a RECURRING load flake** (`ConnectionInspectorTests.swift:33`, `@MainActor`, `.timeLimit(.minutes(2))`): it takes ~206 s under a 486-suite load and passes only when the timeout task is not scheduled in time; it voided item 4's `full-01` and item 7-fix's `full-01`/`full-03` (each a `Restarting after unexpected exit` log with no usable total). Not P6's diff; owner list. A voided run costs ~30 min — re-run, do not bisect.
- **A full log containing `Restarting after unexpected exit, crash, or test timeout` has NO usable total.** Its `Test run with N tests` line covers the SECOND launch only (item 4's fix review saw `1850 tests in 230 suites` where the real figure is 4789/475). Grep for that line before believing a count.
- **A `…AndIsMarkedFinal` claim can be unobservable rather than untrue.** `isProjectableAtThisPass` drops an ended-transcript item from the re-entry list BEFORE the verdict runs, so it never reaches `routedProjectedItems` — the honest assertion is the exclusion (stronger: no pass slot is spent at all), and only a refusal made INSIDE the verdict (a blocked author, a spent quota) is observable on the mark.
- **`MeshContentGates.folding` folds `isRefused` over the senders the ledger ALREADY holds**, so a sender's FIRST row is never filtered by `SessionMessageStore`'s own gate — what refuses it is `routedProjectionAuthor`, pre-unwrap. A block-half cell needs two rows from that sender.
- **`-only-testing:Target/Suite/cellName` runs 0 tests under a green banner for Swift Testing suites** even spelled correctly and even with `()` — name the whole suite and read the per-cell line.
- **A negative control whose "before" text is an empty string cannot be reverted by a count-1 replace** (`text.count("")` is the file length) — revert deletions by re-inserting the exact block, and re-grep the needles before rebuilding.
- A `until ! pgrep -f "xcodebuild …"` poll loop matches its OWN command string and never ends — use `pgrep -x xcodebuild` or the log's terminal marker.
- **Every pass runs the FULL suite before its commit.** Two passes that skipped it (item 1 pass A, item 2 pass A) each left a red the next pass had to find and fix — a subset gauntlet is not a commit gate. `full-1.log`-style: a ~350 s runner hang with ZERO tests can follow even a warm subset — re-launch; `error:` is useless as a needle on a full log (CoreData teardown lines), `recorded an issue` is the needle.
- **zsh passes an unquoted `$ARGS` string as ONE argument**: 69 `-only-testing:` flags became one flag → `Executed 0 tests` under `TEST EXECUTE SUCCEEDED` + `EXIT=0` (item 3). Build the flags into an array (`ARGS+=(…)`, `"${ARGS[@]}"`). Only the missing `Test run with N tests` line caught it.
- Under full-suite load, 1–4 `✔ Suite … passed` lines are corrupted by app `[startup]` stdout interleaving inside the suite name — `starts − passes` of a few is a logging artifact; the banner, exit code and zero `✘` are the signals.
- Item 1 design + adversarial check live in the scratchpad (`item1-design.md`, `item1-design-check.md` — 18 REQUIRED changes; biggest: `MeshMembershipRecordSet.merging` silently picks one of two conflicting rows, so the advertisement set needs its own conflict-refusing fold; advertisements go to COMMITTED slots only; verify BEFORE any conflict mark). Item 1 runs as two passes: A = record/verifier/set/schema 3 (additive), B = manager doors/resolver/walls/rig cells + full suite.
- A `while read` loop over a suite file with no trailing newline silently drops the LAST suite (67 flags for 68 names) — only the count caught it; `-only-testing:Suite/cell` without `()` runs 0 tests under a green banner; node-index assertions over random-per-run fixture fingerprints are coin flips — assert roles, not indices.
- Swift Testing's `Comment?` rejects `"a" + "b"` concatenation as well as interpolation — every expectation message is ONE literal. `MeshRoutedManifestTests` is a FILE (suites: `MeshRoutedManifestGoldenTests`, `MeshRoutedManifestSigningTests`); `PowerOfTenBoundaryTests` / `LocalizationBoundaryTests` carry no `@Suite` attribute, so a list regenerated from `@Suite` greps must add them by hand.
- `test-without-building` runs the LAST build — revert, REBUILD, then test.
- A suites list is only as good as its names: regenerate from `@Suite` declarations; count `◇ Suite` starts against `✔ Suite` passes; `Test run with N tests` must be non-zero.
- The FIRST test invocation after a build or an idle gap hangs (~350 s); the retry is the acceptance — warm the runner with a tiny suite.
- A process-global audit signal cannot witness a per-cell claim under concurrent suites; witness per run.
- CryptoKit Ed25519 signatures are hedged — goldens exclude signature bytes; never `==` on signed records. `CryptographicWallScan` matches primitive names in comments.
- `#expect(_, "literal")` only; bind `allSatisfy` Bools first; ColumnCrypto nonces differ per write — compare decoded values.
- A settle's `until:` fires synchronously inside the pump — assert on frame counts.
- After one `unowned` trap expect the next down the chain; fakes hold fabrics `weak`; use the rigs (`MeshDepartureRig.node`, `spawnHostPinned`).
- `activeSlots` is a distance rank, not a reach set; reach = every committed slot ∩ derived roster.
- `@Test(arguments: [])` is green over nothing; `MeshQuorumFixtures` proposals are placeholder-signed; a `ProximityPayloadHandling` conformance cannot take `now:`; `Result<_, MeshRoutedUnavailability>` must not compile; `MeshRoutedStorageScope.production` never as a test literal.
- Long agent work can die of the session usage limit at a fixed local reset hour — resume, don't retry; apply steps read inputs from scratch files.
- Closed; do not re-audit: `MeshTunnelConvergence`, id-vs-endpoint family, crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10 and §11.1–§11.4, Proximity-Security-Followups §1.
- Concurrent sessions share this tree + sim fleet; `Localizable.xcstrings` + `xcschememanagement.plist` held by another session, a stray PDF in `Docs/` — never stage them.

## Next item
**Session 2 started 2026-09-11 23:15 EDT** in worktree `.claude/worktrees/admiring-moser-43ae1d` (branch `claude/youthful-zhukovsky-d27308`, own DerivedData; main is fast-forwarded from it at item boundaries with `git -C <primary> merge --ff-only`). Runner warmed (`warm-02.log`: 4/1 green). **(a) item 6 review DONE (1 P1, 3 P2 — row 6); fix commit `e573a18` LANDED, baseline 4826 / 478; fix review 0 P1/0 P2 → ITEM 6 CLOSED (four P3s routed to item 9's SET A). (b) item 7 bundle DONE `896d96a`, baseline 4837 / 478; its review: 0 P1 / 3 P2 (P2-2 = backwards-clock peer stalls — fix = refuse the record, still answer) → fix commit queued AFTER item 9 (brief `item7-fix-brief.md`). (c) item 9 DONE `ee4c7de`, baseline 4863 / 486, floor 300; its review 0 P1 / 4 P2 → item 7 fix LANDED `a2df52d` (baseline 4865 / 486; fix review 0 P1 / 2 P2 claim-strength → item 10's SET A; ITEM 7 CLOSED after that); item 9 fix LANDED `0e182bb` (fix review read-only in parallel; any P1/P2 → a small pre-close commit after item 10). **(d) item 10 builder IN FLIGHT (`item10-brief.md`, SET A = item 7 fix-review claim-strength findings first, then the lane; timebox two iterations)**, then (e) item 11 (brief `item11-brief.md`).** Then the order below. Order: (a) adversarial review of item 6's commit `ed66042` (read-only; scratch `item6-handoff.md` + `item6-design-check.md`), then its fix commit; (b) item 7 bundle in ONE builder run: the `sentAt` monotonicity guard on `recordPeerRoutedInventory` (own hunk; the battery assertion = a 13th claim in `routedInvariants`), wire `restoreSessionContextAtLaunch` (no shipping caller — see the owner list; default: call it from the app's launch mount behind the existing access gate), item 9 STAGE 0 (arming/chat/convergence seams + the arming-skipped negative; moves neither digest), item 4's three P3s (wipe depth counter, CI floor comment, header-allowance doc), item 1's two owed notes if still open; (c) item 9 (design + check in scratch — apply the 14 REQUIRED; ONE commit; record both digest values); (d) item 10 tier 2 (plan in scratch — needs `FERNLET_MESH_FLOWS_AFTER`, the log stream, `FERNLET_MESH_ALLOW_HEARTS` / `AUTO_KEEP_FRIENDS` / `MeshFlowVerb.heart`; timebox two iterations); (e) item 11 close-out (§12 BUILT + §12.1–§12.4, §24 P7 handoff, P7 launcher, the 19 catalog keys synced from HEAD's blob, memory). Every builder run: full suite before commit, poll loops with deadlines, no background process left alive; one builder at a time; Opus agents; the usage limit resets at a fixed local hour — resume the same agent by id, never restart. **Baseline: 4820 / 478, zero issues. Main is ~45 commits ahead of origin, not pushed.**

Previous: 1 (pass A in flight; pass B after its commit), then 2. Item 3's adversarial review may add a fix commit.
