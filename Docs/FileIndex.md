# Fernlet File Index

This index maps the main project files to their responsibilities. It is intended as a quick orientation guide for app navigation, feature work, tests, and documentation.

Last updated: 2026-09-23 (the owner-approved fixes round, integrated on one branch — HealthKit values kept off iCloud with a device-local residue cache, the HealthKit write gate and the first-workout Health ask, the device-local imported body profile, Tier-2 memories moved to a device-local sidecar with the sensitive-notes sealed backup retired, emotion-only Core Memory, equal journal credit, the 10% deficit and the unwell-day "needs rest" readiness, the Messages extension's walls, icon, 5,000-character limit and wipe, and the post-age-gate intimacy choice with the progress-photo lock nudge — added a row for every file its branches created, the integration pass supplying the five they left out (`OnboardingIntimacyChoiceScreen.swift`, `JournalMemoryCaptureTests.swift`, `MessagesCatalogPublisherTests.swift`, `OnboardingIntimacyChoiceTests.swift`, `ProgressPhotoLockNudgeTests.swift`), and revised the rows whose files changed shape; the 2026-09-22 handoff-docs cleanup took the session material of closed rounds out of the published tree — every launcher prompt, loop ledger and progress log, both superseded trackers, the design round's prompts and review viewer — together with five closed plans and reviews, and dropped their rows along with the local-only live tracker's; the files stay on the owner's machine, and a doc that left the tree is cited as `e6a3b48:Docs/<name>`, the last commit that holds it; the deletion round removed the rows for `MeshMultipeerSession.swift`, `MCPeerIDStore.swift`, `MultipeerPeerTests.swift`, `PeerIDArchiveWipeTests.swift` and `MeshTransportErrorSurfacingTests.swift` with the files, and rewrote the `MeshTransportSelection.swift` row; the 2026-08-20 pass rebuilt the Documentation table from the actual contents of `Docs/`, added the twenty-five test files and three scripts that had no row, and added the Localization section below; the 2026-08-24 pass added the food-catalog runtime/ODR decision record; the 2026-08-27 pass added the Messages Extension section, the `FernletExchange` and `CryptographicPurpose` rows, and the three test suites added by that documentation/test/review pass — all of which had shipped without an index row; this pass adds the `FernletStoreAccess.swift` row for the file P10 item 1 split out, and an `ExchangeIntentService.swift` row, which had never had one; item 2 adds the two rows its commit creates — `CompanionRefresh/CompanionRefreshIdentifier.swift` and `BackgroundRefreshBoundaryTests.swift`; item 3 adds the three its commit creates — `CompanionRefresh/CompanionRefreshScheduling.swift`, `CompanionRefresh/CompanionRefreshCoordinator.swift` and `CompanionRefreshSchedulingTests.swift`; item 3's verify fixes revise those two plus the `BackgroundRefreshBoundaryTests.swift` row, which gained the clock-and-persistence needle family; item 4 adds the four its commit creates — `CompanionRefresh/CompanionRefreshPipeline.swift`, `CompanionRefresh/CompanionRefreshWiring.swift`, `CompanionRefreshPipelineTests.swift` and `WidgetSnapshotContentEqualityTests.swift` — and revises the `WidgetBridge.swift`, `FernletStoreAccess.swift` and `CompanionRefresh/CompanionRefreshCoordinator.swift` rows; item 4's verify fixes revise the two `CompanionRefresh/` rows, `BackgroundRefreshBoundaryTests.swift` and `CompanionRefreshPipelineTests.swift`, and create no file; item 9 adds the five files its commit creates — the P10 acceptance battery's clause suites; the close-out adds the `Docs/Next-Round-Prompt-Device-Round-2026-09-21.md` row beside the per-phase launcher glob, which is deliberately NOT widened to match it, and takes D-10.4.5's home off the `MeshP10DiffRuleAcceptanceTests.swift` row now that the plan's §28.3 carries the decision; the 2026-09-21 cutover round's item 2 revises six rows — the workflow, the CI gate wall, `AppIntentsTests.swift`, the coordinator, its scheduling suite and the P10 scheduling-seam battery — and its item 1 adds the `Docs/Mesh-Stranger-Admission-Design-2026-09-21.md` row beside a row for `Docs/Mesh-P9-Item4-Design-2026-09-20.md`, which had shipped without one; the round's ledger is matched by the existing ledger glob; the round's item 1.3 — the MC→QUIC **flip** — revises the `MCPeerIDStore.swift` and `MeshMultipeerSession.swift` rows and creates four that had never existed at all: `Transport/MeshTransportSelection.swift` (the file `shippingDefault` lives in), `Transport/NetworkMeshSession.swift` (the friend mesh's radio since the flip, and the only one of the three QUIC sessions with no row), `MeshTransportSelectionTests.swift` and `PeerIDArchiveWipeTests.swift` — an index with no row for the file holding the flipped default is the gap the deletion round would have walked into). Paths are written from the directory containing the repo checkout, so `Fernlet/…` is the repo root: `Fernlet/App/Fernlet/…` is the app target folder, `Fernlet/FernletKit/…` is the local Swift package, and `Fernlet/Tests/FernletTests/…` etc. are the sibling test/extension targets.

## FernletKit Package (Module Map)

The on-device source is carved into the `FernletKit` local SPM package (see [SPM-Module-Carveup-Plan.md](SPM-Module-Carveup-Plan.md)). The app links one umbrella product; the layered dependency DAG *is* the S3 privacy wall — the walled `AIProviders` and `CloudKitSync` modules list no `Private*` dependency, so a forbidden `import` is a hard build error. Per-file rows appear in the feature sections below; this table is the module-level map.

| Module | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Package.swift` | Package manifest: the layered target DAG, per-target MainActor isolation choices, and the single external dependency (CryptoSwift, for FernletLock's Scrypt KDF). |
| `FernletFoundation` | Layer 0 — shared utilities: dates, keychain helpers, audit log, storage preferences, monotonic clock, startup timing, backup exclusion. |
| `FernletCrypto` | Layer 0 — pure sealing primitives (`ColumnCrypto`, `DeviceBindingID`, CryptoKit-only) and `CryptographicPurpose` / `FernletCryptoPurpose`, the single registry of every cryptographic domain the app uses. See [Crypto-Domain-Separation.md](Crypto-Domain-Separation.md). |
| `WebScrapingKit` | Layer 0 — zero-dependency web-scraping substrate: shared HTML/JSON-LD helpers for both web importers, plus `EphemeralWebSession`, the no-cookie/no-cache/no-credential "private tab" URLSession every outbound fetch goes through. |
| `FernletDomainModel` | Layer 1 — portable, nonisolated domain value types (nutrition/workout/wellbeing/companion models, settings aggregate, wire DTOs). |
| `FernletScoring` | Layer 1 — scoring/goal-weight/meal-parse/workout-plan engines, abstract period-scoring signal types, and the stress engine. |
| `FoodCatalog` | Layer 1 — USDA food search/scoring; owns the bundled read-only `FoodCatalog.sqlite` resource (loaded via `Bundle.module`). |
| `FernletPersistence` | Layer 2 — persistence contract: `FernletRepository` protocol, `FernletSnapshot` aggregate, and the per-store repositoring protocols. |
| `LocalPersistence` | Layer 2 — Foundation-only local JSON repository, log-record DTOs, and the derived-signal / Tier-2 memory engines. |
| `PrivateStoreCore` | Layer 2.5 — sealed local-only Core Data stack shared by the `Private*` stores (protected side of the S3 wall). |
| `PrivateHealthStore` | Layer 3 — sealed cycle/intimacy store: `PeriodTrackerStore`, cycle prediction, menstrual-narrative and intimacy repositories, raw cycle types. |
| `PrivateMemoryStore` | Layer 3 — sealed journal and worry-box narrative repositories. |
| `PrivateMediaStore` | Layer 3 — at-rest AES-GCM-sealed photo stores (mesh friend photos, meal photos, progress photos). |
| `PeriodContextBridge` | Layer 4 — sanctioned egress converting raw cycle data into the abstract period signals scoring consumes. |
| `AIContext` | Layer 4 — typed AI context payloads (the de-identification contract), the `MemoryAgent` gatekeeper, and the AI audit log. |
| `AIProviders` | Layer 5 — **walled** on-device Foundation-model consumers; structurally cannot import any `Private*` store. |
| `CloudKitSync` | Layer 6 — **walled** iCloud-synced Core Data + CloudKit persistence; must never name a sealed type. |
| `StoreCore` | Layer 7 — store-side services lifted out of the app: derived signals, snapshot saves, AI retry queue, saved recipes, coins/custom items/milestones. |
| `DiaryStore` | Layer 8 — the portable `@Observable` diary slice of `FernletStore` (days, meals, journals, goals, scoring methods); the app-side facade forwards to it. |
| `HealthKitGateway` | Layer 6 — HealthKit platform shim: `HealthKitService`, workout sync, activity-type catalog, authorization seams. |
| `FernletLock` | Layer 6 — app-lock service (Scrypt via CryptoSwift); drains the sealed pending-narrative buffer on unlock. |
| `AppServices` | Layer 6 — assorted platform services: notifications, WeatherKit, nutrition-label OCR, barcode scanning, food image classification, share-extension import queue. |
| `ProximityKit` | Layer 6 — the peer-to-peer subsystem as one black-box shim: mesh transport, identity, trust, ranging, and the recipe/photo/clothing/heart/message/moderation flows. |
| `FernletExchange` | Layer 1 — the transport-agnostic exchange core shared by the app and the Messages extension: recipe/workout packets, the `MSMessage.url` envelope, card metadata, and the App Group catalog and hand-off inboxes. Deliberately holds NO repository — it is the only FernletKit product the Messages appex links, and that is what keeps a Messages-hosted process off Fernlet's persistence stack. |

## Localization And The Token/Display Fork

Phase 0 (locale-correctness fixes) and Phase 1 (string-catalog plumbing + the token/display forks)
merged on 2026-08-19; `en` remains the development language and `es`/`fr`/`de` are the planned first
wave (see [Localization-Plan-2026-07-19.md](Localization-Plan-2026-07-19.md)).

| Path | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/Localizable.xcstrings` | App-target string catalog. |
| `Fernlet/App/Fernlet/InfoPlist.xcstrings` | Localized Info.plist values (permission-purpose strings, display name). |
| `Fernlet/App/Fernlet/AppShortcuts.xcstrings` | Localized App Intents / Siri shortcut phrases. |
| `Fernlet/App/FernletWidgets/Localizable.xcstrings`, `Fernlet/App/FernletShareExtension/Localizable.xcstrings` | Extension-target catalogs. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/Localizable.xcstrings`, `Fernlet/FernletKit/Sources/FernletLockUI/Localizable.xcstrings` | Package catalogs. A `String(localized:)` inside a package MUST pass `bundle: .module` — without it the lookup resolves against `Bundle.main`, finds nothing, and silently returns the English literal. |
| `Fernlet/Scripts/sync-string-catalogs.sh` | Repopulates the catalogs without opening Xcode (`xcodebuild` never writes into an `.xcstrings`; the sync half lives in the IDE, but the same `xcstringstool sync` ships in the toolchain and consumes the `.stringsdata` a build already emits). Run it after adding or changing user-facing strings and commit the catalog diff in the same commit; `--check` for CI. It is additive — keys that disappear are marked stale, never deleted, so a translation is never silently thrown away. |
| `Fernlet/Tests/FernletTests/LocalizationBoundaryTests.swift` | The localization wall (sibling of the S3, no-tracking and Power-of-10 walls). Enforces the rule below. |

**The rule: every string does exactly one job.** A TOKEN is a persisted `rawValue`, a mesh wire byte,
an AI prompt word, a dictionary key, a matching input, an accessibility identifier, or an
export/schema field — tokens are English forever, because they are compared, decoded, signed, and
shipped between processes and devices. DISPLAY is what a person reads, and only display localizes.
Where one string did both jobs it was FORKED: the token keeps its exact characters so already-persisted
data still decodes, and a separate display property sits beside it.

| Forked type | Token (persist / sign / prompt / compare) | Display |
| --- | --- | --- |
| `CareGroup` | `token` | `label` |
| `MealConfidence` | `token`, plus a `legacyTokens` table for the pre-fork English phrases already in users' blobs | `label` |
| `MealType` | `rawValue` — persisted on every meal AND the meal-parsing prompt's vocabulary, round-tripped as `MealType(rawValue:)` | `displayName` |
| `WorkoutType` | `rawValue` — persisted on workout rows, matched by the exercise catalog, part of the trainer export | `displayName` |
| `CompanionState` | `rawValue` — persisted on `DailyHealthScore`, byte-mirrored by `WidgetCompanionState` in a SEPARATE PROCESS, and a Coach-export field | `displayName` |
| `CoachPlanTokens` vocabularies | the token vocabularies the export prompt publishes to plan authors | the app's own copy |
| ProximityKit `PayloadSummary.title` / `subtitle` / `extraDetails` | frozen English — they are written into the canonical signing bytes and rendered in the RECEIVER's Connection Inspector | (none yet; a localized inspector would render `payloadTypeToken` through a receiver-side table) |

Every failure mode here is silent: nothing crashes, no test goes red on its own, and the damage
surfaces as lost rows or wrong behaviour in a language nobody on the team reads. That is why it is a
wall and not a review checklist.

## App Entry And Shell

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/FernletApp.swift` | SwiftUI app entry point. Creates the shared persistence, app store, period tracker, launch preparation service, and lock service objects. Runs the one-shot Phase-6 backup-exclusion launch gate and hosts its one-time existing-install prompt. Since network migration P7 item 2 its six lifecycle edges (the launch mount, the two scene legs, the two protected-data notifications, the duress observer) hand the raw phase and the lock service's two facts to `FernletStore.applyProximityRunPolicy(…)` and decide nothing: the file writes no routed access gate and assembles none (`MeshRoutedLockedDeviceTests` W6–W8). |
| `Fernlet/App/Fernlet/BackupExclusionLaunchGate.swift` | Security-hardening Phase 6: `FernletPriorUseMarker` (device-local first-run latch) + `BackupExclusionLaunchGate`, the launch-time resolver that defaults fresh installs to backup-excluded, records already-excluded existing installs, and requests the one-time honest trade-off prompt for existing included installs — never a silent flip. |
| `Fernlet/App/Fernlet/ContentView.swift` | Main app container and navigation shell. Hosts primary tabs, launch state, lock gate integration, quick sheets, and meal/journal notifications. Mirrors `selectedTab` into the store for the run policy (network migration P7 item 2), as it already mirrors `lockState` and `duressSessionActive`, and since **P7 item 3 speaks no radio verb**: the Friends three-way arm, the tab's five-minute timeout, the `hasCommittedPeer`-guarded stand-down and the presence / recipe listener chains are gone from here (`ProximityRunSeams.swift` executes the policy's verdict); the view hands the funnel its tab, lock-state, presence-opt-in, age-record and launch edges through `applyProximityRunPolicyFromView()`, keeps only the away-hearts CloudKit sync on those edges (`syncAwayHeartsIfActive()`), and its scene handler moves no radio — a scene edge is `FernletApp`'s. |
| `Fernlet/FernletKit/Sources/FernletUI/FernletUIComponents.swift` | Shared UI primitives: adaptive color tokens, headers, chip styles, sheet fields, section pickers, layout helpers, searching pulse, medallion/coin glyphs. |
| `Fernlet/FernletKit/Sources/FernletUI/FernletPrimitives.swift` | `FernletCard`, `SectionLabel`, `EmptyState` — the cross-screen layout primitives extracted from HomeView for package-resident views. |
| `Fernlet/FernletKit/Sources/FernletUI/FernletTheme.swift` | App-wide color palette, theme defaults, custom light/dark background support, and UIKit/SwiftUI color vending. |
| `Fernlet/FernletKit/Sources/FernletUI/FernletDesignSystem.swift` | Design-system foundation: the `FernletTextRole` serif/sans type scale, warm token palette, 8pt spacing/radii/shadow/motion tokens, and bundled-font resolution. |
| `Fernlet/FernletKit/Sources/FernletUI/ModelColors.swift` | SwiftUI `Color` extensions mapping domain enums (`MealType`, `WorkoutSplit`, `WorkoutType`…) to palette colors; split out so domain models stay Foundation-only. |
| `Fernlet/FernletKit/Sources/FernletUI/ActivityShareView.swift` | `ActivityShareView` — the app's single `UIActivityViewController` wrapper, with the `onFinish` completion hook the plaintext exports are deleted at. Shared by the data export (`PrivacyDataSettingsView`), the trainer/nutritionist summary (`TrainerExportView`), and the proximity connection-log export (`ConnectionInspectorHistoryView`); replaced the two private copies and one `ShareLink`. |
| `Fernlet/FernletKit/Sources/FernletUI/OptionalPresenceBinding.swift` | `Binding.isPresent()` — collapses an optional-backed binding into the `Bool` shape SwiftUI's `alert` / `sheet` / `confirmationDialog` / `fullScreenCover` modifiers take (presented while non-nil; setting `false` clears the value, setting `true` is a no-op). The shared home for the "present while this optional state is set" binding previously inlined at every call site (`ActivitiesView`, `ConnectView`, `DisposableCameraView`, `FriendListView`, `FriendShopView`, `PrivacyDataSettingsView`, `FriendPhotoReviewSheet`); sites with a compound getter or a side-effecting setter deliberately keep their hand-rolled binding. |
| `Fernlet/FernletKit/Sources/FernletUI/CaptureProtection.swift` | Screen-capture **friction** (never a security control — Docs/Design-Capture-Protection-2026-08-10.md): `CaptureProtectionState` (injected `@Observable` owning the screenshot pulse + per-window-scene `isCaptured` verdict, conservative live-scene fallback pre-registration) and `CaptureProtectedModifier` / `captureProtected(surface:)` (opaque Tier-2 cover while recorded/mirrored or scene-inactive, resigning keyboard focus on arrival; Tier-1 blur + once-per-session nudge — copy in `CaptureNudgeCopy`, announced once for VoiceOver — after a screenshot, skipped while the surface is occluded). Applied by the app target at the six Private-tab surfaces: the hub root (inner to the lock gate) and the five sensitive sheets. |
| `Fernlet/App/Fernlet/FernletNotificationDelegate.swift` | `UNUserNotificationCenterDelegate` that presents gentle notifications while foregrounded and routes a check-in tap to the journal sheet via a pending-open flag `ContentView` consumes. |
| `Fernlet/App/Fernlet/UITestSupport.swift` | DEBUG-only launch hooks for UI tests: central reader of the `FERNLET_UI_TEST_*` appearance-test flags (seed demo, bypass private lock, jump-to-screen), plus `isTestHarnessActive` (XCTest/UI-test detection that suppresses the backup-exclusion launch gate under automation). |
| `Fernlet/App/Fernlet/Assets.xcassets` | Image and color assets used by the app. |
| `Fernlet/App/Fernlet/Info.plist` | App bundle metadata and platform configuration. |
| `Fernlet/App/Fernlet/Fernlet.entitlements` | App capability entitlements. |
| `Fernlet/App/Fernlet/SettingsSearchIndex.swift` | `SettingsRoute` (every addressable Settings destination, driving the hub's single `navigationDestination`) and `SettingsSearchIndex` (the searchable entry table). Adding a route is a compile-time prompt to give it a view and at least one search entry. |

## Feature Views

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/HomeView.swift` | Home dashboard with companion state, quick logging, signal trends, macro summaries, hygiene, and photo wall UI. |
| `Fernlet/App/Fernlet/FoodView.swift` | Food logging, recipes, imported recipe review, ingredient editing, meal creation, macro display, saved recipe book, and Safari presentation. Also hosts the in-file `RecipeDetailView` (sealed photo, macros, edit/log/share actions, in-app Safari source link). |
| `Fernlet/App/Fernlet/MoveView.swift` | Movement/workout screen with workout logging, suggestions, workout rows, and goal summaries. Hosts `WorkoutExerciseDraft`, the exercise-draft state machine shared by `WorkoutSheet` and `WorkoutPlanSheet`. |
| `Fernlet/App/Fernlet/ActivityPickerSection.swift` | Activity-mode workout picker, recent activity shortcuts, and activity-specific workout fields. |
| `Fernlet/App/Fernlet/JournalView.swift` | Journal calendar, prompts, entry creation/editing, day detail, day nutrition breakdowns, and daily edit sheets. |
| `Fernlet/App/Fernlet/MemoryNoteDisplay.swift` | Display fork for Core memory rows: `MemoryNote.displayText` shows a memory's own text, or builds an emotion-only journal memory's sentence from its `FeelingTag` at render time (the token is stored, the sentence never is). |
| `Fernlet/App/Fernlet/CycleTrackerView.swift` | The Private hub's merged Cycle page: one layered calendar (period flow tints + a distinct intimacy marker), period predictions/trends, and a single plus-menu; each half gates on its own derived visibility. |
| `Fernlet/App/Fernlet/CycleDayDetailView.swift` | Combined day detail for the Cycle calendar: the period half (samples, narrative, edit/delete) and the intimacy half (events/notes), each rendered only when its own gate allows. |
| `Fernlet/App/Fernlet/LogPeriodSheet.swift` | Sheet for logging period events. |
| `Fernlet/App/Fernlet/MonthCalendarCard.swift` | `MonthGridModel` + `MonthCalendarCard` — the shared month-calendar chrome (paging chevrons with future months disabled, weekday row, 7-column grid inside a `FernletCard`) plus its pure layout math, whose day keys are canonicalized through `FernletDate.dayKey` rather than a locale-following formatter. The single home of the previously duplicated month grids, now filled by `JournalView` and the layered cycle calendar in `CycleTrackerView`; each supplies its own day cells and footer. |
| `Fernlet/App/Fernlet/PrivateHubView.swift` | Private hub screen with private-section navigation. |
| `Fernlet/App/Fernlet/SocialHubView.swift` | Social hub entry point for the Friends photo wall and active disposable-camera session flow. |
| `Fernlet/App/Fernlet/SessionHeartStatusCopy.swift` | **P6 item 6**: `SessionHeartStatusCopy`, the app-side fork of `MeshNetworkManager.SessionHeartFailure` into `LocalizedStringKey` copy — one sentence per frozen cause, an exhaustive `switch` so a new cause is a build error until it has a sentence, plus the "Sent …" line. It closes a LIVE hole rather than tidying one: the case it replaced was `failed(message: String)`, composed inside ProximityKit and rendered verbatim in every language — the D-13.15 defect in a seam D-13.15 did not reach, and invisible to both halves of `LocalizationBoundaryTests` (a bare `String` associated value matches neither scanner; five of its six sentences were measured absent from the catalog). `DisposableCameraView` renders this for the mesh path and keeps a separate `String?` property for the presence fallback, deliberately: typing the shared row `LocalizedStringKey` would force `LocalizedStringKey(runtimeString)` on `PresenceManager.heartSendState.failed(message:)` and move the defect instead of closing it (P9's). |
| `Fernlet/App/Fernlet/ProximityRunPolicy.swift` | **P7 item 1** (plan §13 option A): `ProximityRunPolicy`, the app-layer run policy as a pure VALUE — one function from the app's ten lifecycle facts (`ScenePhase`, `FernletTab`, duress, iOS data protection, the final below-minimum-age ruling, a delete-all in flight, P8's `ProximityContinuationState`, and `ProximitySessionPresence` folded from `isInSession` × `hasCommittedPeer`) to a `ProximityRunState` per radio (mesh, discovery, presence, recipe share) plus the `MeshRoutedAccessGate` the app already assembles. Four run states, not §13's three: `hold` names today's committed session in the background without a continuation task (kept, nothing runs, iOS suspends — never torn down) and the same session off-tab, because `stopJoin()` tears the committed slots down too. The one foreground fact is `FernletApp.routedGateForeground(for:)`, so `.inactive` is foreground for radios as well as for the gate. Decides radios, never plaintext: the gate reads no tab, age, wipe or task fact. **Item 2** made it the single writer of `applyRoutedAccessGate(_:now:)` through `FernletStore.applyProximityRunPolicy(…)`, and added the input projections — `appLockEngaged(_:)` (`.locked` only; RETIRED with its fact by P9-3-A's fix, 2026-09-22, so a configured lock no longer parks presence and recipe) and `belowMinimumAge(_:)` (the final `.below` ruling against `AgeGate.chat` or guardian communication limits, never `.undetermined`). **Item 3** made it the single writer of every radio seam too (`ProximityRunSeams.swift`). The matrix over the full 11 520-row input product (23 040 until P9-3-A) is `ProximityRunPolicyTests`. |
| `Fernlet/App/Fernlet/ProximityRunSeams.swift` | **P7 item 3**: the radios' `apply(_:)` seams and the ONE place in the app that speaks a radio verb. `ProximityRunTransition` is the pure half — last verdict applied, new verdict, the mesh manager's three live predicates (`isSearching`, `isInSession`, `hasCommittedPeer`) plus each listener's own `isListening` → an ordered `[ProximityRunAction]` (`startJoin` / `resumeSearch` / `stopJoin` / `leaveSession` / arm or cancel the tab's five-minute timeout / `holdLinks` / `presence(_:)` / `recipeShare(_:)`) — the mesh radio edge-triggered so `stopSearching()`'s once-per-ending hooks never fire on a tab bounce, the two listeners reconciled against their own `isListening` on every run (P8 item 0 (b): a `didNotStart*` self-stop under an unchanged verdict is re-started at the next policy run, not at the next verdict edge), resolving `FriendsDiscoveryEntry` unmoved, picking `leaveSession()` over `stopJoin()` on a hard stop by `isInSession`, and never reading `isSessionLive`. The executor is a `FernletStore` extension with one `switch`; `PresenceManager.apply(_:)` and `ProximityRecipeShareManager.apply(_:)` are the listeners' seams. The one transition P7 could not execute — discovery `stop` while the mesh keeps `run` — is **P8 item 3's `holdLinks`**: `MeshNetworkManager.holdCommittedLinks()` stops browsing and closes the admission doors while keeping every committed slot, its coordinator and the group-key state, and `stopJoin()` is still never run over it. The refusal, its case and its audit token (`proximityRunPolicy.unsupportedTransition`) are gone, held at zero under `App/` by `ProximityRunSeamsTests`; the way back out is an explicit row in `discoveryStart` — radios down with a peer still committed ⇒ `.resumeSearch`, no timeout armed — because `FriendsDiscoveryEntry` answers that shape `.none` and the hold would otherwise be a one-way door. Replaces `ContentView.startFriendsDiscovery()` / `stopFriendsDiscovery()` / `armDiscoveryTimeout()` and the two listener chains, and `FernletStore`'s three direct listener stops. Walled by `ProximityRunSeamsTests`. |
| `Fernlet/App/Fernlet/ProximitySessionPoller.swift` | **P7 item 4**: the app's half of the poller. `ProximitySessionPoller` is the pure decision — a timer if and only if `isSessionLive` (`decision(isSessionLive:isPolling:)` → `start` / `stop` / `keep`), the 30 s `interval`, and `maxTicks` (the 6-hour ceiling in ticks, the loop's R2 bound). The `FernletStore` extension owns the ONE timer: `syncSessionPoller()` is called by the run-policy core after every edge and reached by `ContentView`'s `isSessionLive` observer between edges; the tick drives `MeshNetworkManager.pollSession(now:)` and stops itself when a poll reports the session gone. Deliberately keyed on liveness, never on a radio verdict. Walled by `ProximitySessionPollerTests`. |
| `Fernlet/App/Fernlet/SessionResumeCopy.swift` | **P7 item 5**: `SessionResumeCopy`, the app-side fork of `MeshNetworkManager.sessionResumePresentation` into `LocalizedStringKey` cards (`SessionResumeCard`: title, one sentence, an SF Symbol name) — one card per presented case, none for `.nothing`, and one shared dismiss label — in `RoutedShareRefusalCopy`'s shape: one place, exhaustive `switch`es so a new presentation or ending is a build error until it has a sentence. Every sentence says what is true and no more: an offer asks the person to keep the tab open (the restore arms no radio), an ending is named as ended and never as failed, the set-aside file says nothing already saved was lost. Seven new catalog keys, owed to the close-out's sync. |
| `Fernlet/App/Fernlet/MeshContinuationCoordinator.swift` | **P8 item 4** (plan §14): the continued-processing task's lifecycle as a pure VALUE — `MeshContinuationCoordinator.transition(from:on:)` over six `MeshContinuationState`s (`idle` / `requested` / `running` / `refused` / `expired` / `completed`) × eight `MeshContinuationEvent`s (§14's six plus `taskStarted` and `taskRefused`, without which `running` and `refused` are unreachable) → the next state, the `ProximityContinuationState` `ProximityRunPolicy` is fed (derived from the next state, never a second field), the `MeshContinuationCompletion` the system is owed, and a frozen `MeshContinuationAudit` token for item 6 to emit. The invariant iOS enforces is one line: `completion != nil ⟺ (from == .running && next != .running)` — exactly once per ENTRY INTO `running`, which equals once per delivered task under item 6's one registered identifier and is never once per session; the feasibility probe's `completeBackgroundTask(success:)` idiom generalised. A new mesh resets the claim and completes a stale task `false`; a delivered task is adopted from every state; a request the system never ran is `refused`, not `expired`; a request that is WITHDRAWN resets to `idle`, because `completed` absorbs `firstPeerCommitted` and would leave a re-committing peer with no claim for the life of the mesh; a foreground return completes the task `true` and lands back on `requested`, entry into which IS item 6's instruction to submit (the audit token names the event, never the next move). Registers, submits, drives and completes NOTHING itself (no `BackgroundTasks`, no scheduler, no timer, no radio verb, no store, no clock, no log — item 6 owns all of that), and the completion is a signal, never a teardown (§25.1: the production shutdown keeps the tunnel). The 48-row table is `MeshContinuationCoordinatorTests`. |
| `Fernlet/App/Fernlet/MeshContinuationProgress.swift` | **P8 item 4**, the arithmetic half: `MeshContinuationProgress` is the task's progress as a ratcheted fraction of the session ceiling — §14's "progress must advance monotonically or the system kills the task", held twice over (the unit is monotonic elapsed session time, and `advancing(from:elapsed:budget:)` ratchets with `max(previous, computed)`), clamped finite at construction so `completedUnitCount` converts without a trap and capped one short of `totalUnitCount` (100) exactly as the probe's does; a zero, negative or non-finite budget yields no new progress rather than a division. Takes plain `TimeInterval`s — `MeshSessionCeiling`'s `hardDeadline` and `monotonicBudgetSeconds` are internal to ProximityKit, and the widenings belong to items 5/6 — so the file imports nothing but Foundation. Beside it `MeshContinuationCopy` carries the card's two sentences as `LocalizedStringResource` (`Fernlet mesh`, `%lld friends connected`) for item 6 to render with `String(localized:)` at the request site; the friend count is passed in, and is branch PRESENCE (committed active slots, self excluded), a smaller and honester claim than §14's "fresh authenticated heartbeats" — the session holds one `lastExternalHeartbeatAt` for the whole mesh, not one per member. Two catalog keys owed to the close-out's sync — and at that sync `%lld friends connected` also owes a hand-authored `one`/`other` plural block plus a `LocalizationBoundaryTests.pluralRuledKeys` row, which cannot be added before the key reaches the catalog without reddening that wall. Walled by `MeshContinuationProgressTests`. |
| `Fernlet/App/Fernlet/MeshContinuationCardPresentation.swift` | **P8 item 7** (plan §14, §17.3): what a refusal, an expiry and a system end PRESENT. `MeshContinuationCardPresentation.card(state:lastAudit:)` is a pure table over item 4's claim — six `MeshContinuationState`s × the ten audit tokens (nine plus none) → one `MeshContinuationCard` or nil — and `MeshContinuationCardKind` (`refused` / `expired` / `endedBySystem`) is its frozen token, spelled three times over: the suffix of the card's accessibility identifier, the vocabulary of the DEBUG launch hook `FERNLET_MESH_CONTINUATION`, and the test pin. The state alone cannot decide, which is the whole design: `completed` is item 4's terminal after ANY session end, so the six-hour ceiling would erase a refusal the person was never shown — the ending is carried by the last token that MOVED the claim (`refused` / `expired` / `cancelled` survive it, `absorbed` is not a move and falls back to the state's own meaning). A claim that is idle, asked for or running says nothing. Three sentences and no dismiss button: a refusal is not a failure, and the card describes the session running right now — the session stays open while Fernlet is on screen (refused), or it carries on in the foreground with nothing sealed lost (expired / ended by iOS, one shared sentence). Once that session has itself ended (`completed`) the ending keeps its headline and the message folds to one shared PAST-tense sentence, because every present-tense word about "this session" is false there. Six `LocalizedStringKey` keys, owed to the close-out's sync. Two rules the file states for item 6's driver, one of them as code: `projectedAudit(previous:outcome:)` is how the driver assigns `meshContinuationLastAudit` — an absorbed row and the move INTO `completed` keep the previous token, so the six-hour ceiling cannot erase a refusal nobody was shown — and the header owes it the `.cancelled`-for-an-unservable-delivery and hard-stop-reset obligations. Rendered by `ConnectView`'s card slot, which P7's resume card shares: `slotDecision(continuation:resume:)` is the precedence as a value — a live spent claim wins, an ended one yields to a resume card and takes the slot only when there is none. Walled by `MeshContinuationCardPresentationTests` (the 60-cell product, the observed-projection pin and the surface's single read) and `MeshContinuationCardUITests` (tier 1b, the card by identifier). |
| `Fernlet/App/Fernlet/MeshContinuationDriver.swift` | **P8 item 5** (plan §14, §24.1, §25.1): the app's continuation driver — item 5's SESSION-STATE half. Holds item 4's claim (`MeshContinuationState`, moved through `MeshContinuationCoordinator.transition(from:on:)`) and, on exactly the moves that table makes, speaks the two raises ProximityKit widened for it: `taskDidStart()` adopts a delivered `BGContinuedProcessingTask` and — on the ENTRY into `running`, never on the event — raises `MeshNetworkManager.beginBackgroundContinuation()`; `taskDidEnd(_:)` (expired / cancelled / sessionEnded / appForegrounded) moves the claim and, only when the table produced a completion, records it in `pendingCompletion` for item 6 to take with `consumePendingCompletion()` and raises `endBackgroundContinuation()`. An ending of nothing therefore raises nothing — the one negative that keeps a second code path out of either raise. The PROJECTION is a rule, not a raw token (item 7's verify): `lastAudit` rides beside `state`, because item 4's `completed` is the terminal after ANY session end and the state alone cannot say which ending it was, and it is assigned through `MeshContinuationCardPresentation.projectedAudit(previous:outcome:)` — item 7's rule, in item 7's file, not a second copy — while the audit LOG still records every move including the absorbed ones. `MeshContinuationAdoption` is the caller's instruction for a delivery (`claimed` / `ownerless` / `absorbed`): an ownerless one — item 4's `idle + taskStarted` — is adopted so the handle is completable at all, must be ended AT ONCE with `.cancelled`, and its ending RESETS the projection, because "iOS ended your background session" is a sentence about a session that never existed. `reset()` is that act exposed, for item 6 to wire to the proximity hard stop (delete-all, leg 7b) and to a new mesh start; it never clears `pendingCompletion`, since a wipe does not excuse the app from completing the handle it holds, and — the fix round's F1 — it ENDS a task still in hand first (`taskDidEnd(.cancelled)`, which completes it once and raises the foreground edge once) before the private `resetProjection()` clears the three fields, because a projection cleared out from under a live claim would strand `continuingInBackground` for the life of that mesh with nothing left to raise `.foregrounded` and every routed heart deferring silently behind it. The manager is held **weakly** and a raise that reaches none is audited (`mesh.continuation.raiseWithoutAManager`), never swallowed. **Nothing spins**: no `Task` handle, no timer, no `unowned` host (so ML1/ML4 do not apply) and — until item 6 — no `BackgroundTasks` import at all; it writes no routed access gate, calls no store setter and speaks no radio verb, which `MeshContinuationRaiseWallTests.theDriverIsSessionStateOnly` pins by needle. Item 6 added the remaining claim edges (`meshDidStart()`, `firstPeerDidCommit()`, `taskWasRefused()`, all through one private `offer(_:)` branch) and put the scheduler half in a SIBLING, `MeshContinuationTaskHost`, which owns one of these — so `theDriverIsSessionStateOnly`'s needle list was not shortened by three and every `BackgroundTasks` name stays forbidden here permanently. The manager is held as `ProximityKit.MeshContinuationRaising`, the two-verb seam it conforms to — a `final class` cannot be stood in for, so counting the raises needs a protocol, and behind two verbs the rest of the manager is unspellable from here. Walled by `MeshContinuationRaiseWallTests` (the three counts), `MeshContinuationDriverTests` (the claim arithmetic, the raises over a session-less manager, and the exhaustive sweep: every sequence of driver calls to depth three over item 6's eleven-wide alphabet (the scene is part of the walk), 1463 walks (eleven-wide, depth 3), one begin per entry into running-while-dark and one end per dark exit — and `claimed` reachable at last, because `firstPeerCommitted` is the edge that breaks the circle) and `MeshContinuationDisagreementTests` (plan §24.1's cell on a real founding, plus the mid-task reset and the ownerless round trip on a live session). |
| `Fernlet/App/Fernlet/MeshContinuationTaskHost.swift` | **P8 item 6** (plan §14, §25.1): the wiring — the thin object that owns the mesh's `BGContinuedProcessingTask`. Registers the CONCRETE identifier `MBO.Fernlet.mesh-continuation.<meshID>` when a mesh starts (inside `Info.plist`'s `BGTaskSchedulerPermittedIdentifiers` wildcard; a `UUID`'s characters are legal in a task identifier), submits with `.fail` on the ENTRY into item 4's `requested` — which is both the first committed peer and the re-arm a foreground return produces, so the two edges share one test and a founding that commits in the same transaction cannot submit twice — installs the expiration handler, withdraws a pending request on a session end or a hard stop, and completes the delivered task **exactly once**: `completeHeldTask()` is the probe's `completeBackgroundTask(success:)` idiom with the duplicate guard supplied by `MeshContinuationDriver.consumePendingCompletion()` answering nil on a second read, and the handle is dropped before it is completed. It **keeps the tunnel** — the probe tears its own down and the product must not — so there is no radio verb, no listener, no routed access gate and no teardown anywhere in the file; the one way it reaches a radio is `FernletStore.setMeshContinuation(state:lastAudit:)`, which assigns the projection and re-runs `ProximityRunPolicy` (the store's sixth own edge). It is deliberately never called from inside `runProximityPolicy`, because that setter re-runs the funnel and a re-entrant pass would leave the outer one diffing against a `previous` the inner one had replaced. **Nothing spins**: no `Timer`, no `DispatchQueue`, no stored `Task` handle and no `unowned` host (ML1/ML4 do not apply); the progress bar rides `ProximitySessionPoller`'s existing 30-second tick, reading `MeshNetworkManager.sessionContinuationReading` and ratcheting through `MeshContinuationProgress`. Both bounds are R2 literals — eight submissions per session (item 4's `running + appForegrounded → requested` row re-arms on every foreground return) and eight registered identifiers per process (`BGTaskScheduler.register` treats a duplicate as a programmer error). An ownerless delivery is ended at once and presents nothing; a second delivery is completed `false` and dropped. The card's two sentences are rendered from `MeshContinuationCopy`'s `LocalizedStringResource`s with `String(localized:)` at the submit site — the app target IS `Bundle.main`, so no `bundle:` argument applies. No persisted surface, so the wipe wall is owed nothing. Walled by `MeshContinuationTaskHostTests` (the five acts over two fakes) and `MeshContinuationTaskHostWallTests` (the framework's two homes, the forbidden-needle list, the feed's single call site, the funnel's silence, the mesh's Live-Activity suppression and the plist wildcard). |
| `Fernlet/App/Fernlet/MeshContinuationScheduling.swift` | **P8 item 6**, the `BackgroundTasks` SEAM: `BackgroundContinuationScheduling` (register / submit / cancel), `ContinuationTaskHandle` (progress, copy, expiration handler, complete) and `ContinuationTaskRequest`, plus the two production conformers `SystemContinuationScheduler` and `SystemContinuationTaskHandle`. The seam exists because `BGTaskScheduler` refuses a continued-processing submission on a Simulator outright (error 1) and nothing can make iOS deliver a task to a unit test: without it, item 6's five acts would have zero tier-1 coverage. Outside the `#if DEBUG` feasibility probe this is the ONLY app file that names `BGTaskScheduler` or `BGContinuedProcessingTask`, which `MeshContinuationTaskHostWallTests` pins. What it copies from the probe is named line by line in its header — the pre-register and pre-submit `cancel`, `register(forTaskWithIdentifier:using: nil)` with the wrong-class arm completing `false` and a `Task { @MainActor }` hop, `BGContinuedProcessingTaskRequest(identifier:title:subtitle:)` with `strategy = .fail`, the progress scale, the `expirationHandler` hop and `updateTitle(_:subtitle:)`. What it does NOT copy is `endProbe`'s `stopNetworkOperations()`: §25.1's production shutdown keeps the tunnel. `requiredResources` is left at its default because the probe never set it either, and the default is what ran on hardware (§15.1, 2026-09-02). The conformers are the untested remainder and are item 9's device rows. |
| `Fernlet/App/Fernlet/RoutedShareRefusalCopy.swift` | The P5 review's finding 5: `RoutedShareRefusalCopy`, the app-side fork of `MeshRoutedShareRefusal` into `LocalizedStringKey` copy — one sentence per frozen cause, an exhaustive `switch` so a new cause is a build error until it has a sentence, and the session alert's existing "Session" title. Presented by `DisposableCameraView`'s session alert, which shows the typed refusal ahead of the legacy `meshError` `String`. The wording is the owner's; the shape is not. |
| `Fernlet/App/Fernlet/RoutedDeliveryHoldBanner.swift` | P5 item 9: `RoutedDeliveryHoldCopy` (the testable copy enum — `LocalizedStringKey` only, one sentence pair per cause, no count interpolated into any key, frozen a11y id `friends.deliveryHold`), `RoutedDeliveryHoldDismissal` (the dismissal keyed to the FACT — same cause, count not grown — so one tap cannot silence a later, different hold for the life of the process) and `RoutedDeliveryHoldBanner`, the Friends-tab banner mounted from `ConnectView` and gated on `MeshNetworkManager.routedDeliveryHold`. Dismissal is `@State`, so nothing is persisted and nothing is owed the wipe wall; the card carries no `.accessibilityElement(children: .combine)`, because combining would strip the dismiss `Button`'s trait, focus and label. |
| `Fernlet/App/Fernlet/ConnectView.swift` | Friends tab photo wall, nearby-discovery status, connection-success transition, session photo review, and full-screen saved-photo feed. Presents `DisposableCameraView` while a Friends session is active. The session transition is split in two (P6 item 2): `handleSessionSurfaceChange` on `isInSession` owns the layout swap and camera chrome, `handleCommittedPeerChange` on `hasCommittedPeer` owns the choreography and triggers the review — which presents only once `isSessionLive` is false, so a link blip shows no sheet whose primary action would sign a termination on a live mesh. Its heal arm dismisses EITHER standing sheet and clears `showConnectionAnimation`, because a cover that never presented latches `.accessibilityHidden(true)` on the whole surface. |
| `Fernlet/App/Fernlet/CreationStudioView.swift` | Animal-Crossing-style grid fabric editor for designing custom clothing/accessories (per-slot pixel canvas, palette, live companion preview). Pushed from the customization sheet. |
| `Fernlet/App/Fernlet/WardrobeView.swift` | The closet: owned custom items grouped by slot; equip/unequip, edit, delete, mark shop-shareable. Entry point into the Creation Studio. |
| `Fernlet/App/Fernlet/CustomItemRendering.swift` | Renders a palette-indexed `ItemGridTexture` to a cached `CGImage` (`ItemTextureRenderer`) and the `CustomItemThumbnail` preview tile. |
| `Fernlet/App/Fernlet/CompanionVectorAssets.swift` | Vector companion renderer (`CompanionView` + body/accessory/clothing/side-item layers). Hosts `CompanionCustomItemLayer`, which draws equipped user-designed grid items in per-slot regions. |
| `Fernlet/App/Fernlet/OnboardingView.swift` | First-run profile setup, nutrition profile editing, and nutrition preview UI. |
| `Fernlet/App/Fernlet/SettingsSheet.swift` | Settings sheet and user preferences UI. Hosts the two DEBUG crypto surfaces on the Debug tab: the marker-bytes census section and the pushed Phase 3 gate readout beside it. |
| `Fernlet/App/Fernlet/CryptoFormatCensus.swift` | **DEBUG-only.** `CryptoFormatCensus` — the Phase-0 aggregator (Docs/Plan-Crypto-Standardization-2026-08-27.md) that runs the five per-surface format censuses off the main actor and folds them, plus the one uncountable surface, into six `CryptoFormatCensusRow`s. Counts MARKER BYTES only: nothing decrypts, nothing fetches a key, nothing is written or persisted, and a `nil` count never renders as `0`. |
| `Fernlet/App/Fernlet/Phase3GateReadout.swift` | **DEBUG-only.** The Phase 3 gate vocabulary and the pure fold: `Phase3Gate`, `Phase3GateWitness`, the five-case `Phase3GateVerdict`, the stamped-observation model (`Phase3Stamp`, `SealedColumnPassWitness`, `MediaPassWitness`, `SealedPhotoManifestReading`, `BodyProbeReading`, `Phase3LatchReadings`), `Phase3GateReadoutBuilder` (one static per gate) and `Phase3GateReportBuilder` (the two export routes' plain-text serializer + its redaction contract). Model only — no SwiftUI, no I/O. |
| `Fernlet/App/Fernlet/MediaResidueAudit.swift` | **DEBUG-only.** The media gate's residue audit: names each of the eight swept locations by corpus LABEL (never by path) and does the plan's residue arithmetic over the census table plus a live `MediaAtRestFormatMigrator.performPass()` result. A pure fold — no file I/O of its own — and `unaccountedUnprefixed` is `nil` rather than a computed zero whenever no pass witness exists. |
| `Fernlet/App/Fernlet/Phase3ReadoutSession.swift` | **DEBUG-only.** `Phase3ReadoutSession` — the `@Observable`, process-scoped, NON-persisted home for every reading the sitting purchases (it outlives both the destination push and the Settings sheet's dismissal, which the sealed-column ritual requires) plus the derived `Phase3SittingStep` checklist. Carries two fences: `scanGeneration` (a landed scan whose fence moved is dropped, and it is the view's `.task(id:)` key) and `epoch` (work in flight across a clear cannot re-populate the session). Persists nothing; cleared on a duress engage and before a wipe, through `FernletStore.clearPhase3Evidence()` so the store-side keyed witness goes with it. |
| `Fernlet/App/Fernlet/Phase3GateReadoutView.swift` | **DEBUG-only.** The pushed Phase 3 gate readout screen: risk statement, sitting checklist, environment, six gate rows, five controls (re-take the local scan, fetch manifests, body probe, fund a media pass, reset the sealed-column latch) and two export routes (local-only pasteboard + audit-log chunks). Takes its `FernletStore` as an explicit init parameter — never from the SwiftUI environment, which nothing populates. Renders one refusal and nothing else under a duress session. |
| `Fernlet/App/Fernlet/SharedSheets.swift` | Reusable logging sheets for water, sleep, goals, hygiene, and texture entries. |
| `Fernlet/App/Fernlet/AmbientCards.swift` | `AmbientCardsView` — low-cost ambient Home surfaces: once-a-day gentle offer, look-back journal card, macro-gap meal nudge, forgotten-favorite/workout nudges, and a preventive-care micronutrient bubble. |
| `Fernlet/App/Fernlet/CompanionAmbienceLayer.swift` | Full-bleed environment layer behind the Home companion: local-hour time-of-day tint (`CompanionDayPhase`) plus optional WeatherKit sky accents (clouds/rain/snow). |
| `Fernlet/App/Fernlet/QuickMoodRow.swift` | `QuickMoodRow` — one-tap mood check-in row of `FeelingTag` chips (Home + Journal) logging a tag-only journal entry via `logQuickMood`, updating today's check-in in place. |
| `Fernlet/App/Fernlet/GoalPresetCards.swift` | `GoalPresetCards` — selectable goal preset cards (one per `GoalType`) surfacing the paired nutrition + training setup each goal configures. |
| `Fernlet/App/Fernlet/JournalPromptLibrary.swift` | `JournalPromptLibrary` — static curated journaling prompts with a deterministic per-`dateKey` daily rotation (deliberately non-AI, journal text stays walled from models). |
| `Fernlet/App/Fernlet/MilestonesView.swift` | `MilestonesView` — lifetime-milestones surface: warm cumulative care counts and coin gifts from the append-only ledger, plus a keepsake-medallion shelf. |
| `Fernlet/App/Fernlet/PetInteractionGovernor.swift` | `PetInteractionGovernor` — gentle anti-compulsion pacing state machine for tap-to-pet (rolling window → settling → calm-idle), state kept in device-local `UserDefaults`. |
| `Fernlet/App/Fernlet/PhotoCaptureControl.swift` | `PhotoCaptureControl` — reusable camera-first / library photo-capture control shared by meal capture and progress photos, with raw-`Data` or `UIImage` sinks. |
| `Fernlet/App/Fernlet/LogIntimacySheet.swift` | `LogIntimacySheet` — sealed intimacy-log entry sheet (date, note, protection used) written through the fail-closed decrypt/seam gate. |
| `Fernlet/App/Fernlet/TrainerExportView.swift` | `TrainerExportView` — trainer/nutritionist export review + consent screen: pick optional includes and see exactly what is always/never shared before preparing the file. Reached from the **Move tab**'s header "Share" pill (which replaced "Suggest" — that moved into `WorkoutPlanSheet`), not Settings. With `settings.coachExchangeEnabled` on it also carries the manual coach exchange: copy-for-an-AI and paste-a-plan-back. |
| `Fernlet/App/Fernlet/CoachExportPromptBuilder.swift` | Builds the clipboard blob for the manual coach handoff: instruction preamble + the exact `CoachPlan` JSON schema + Fernlet's enum vocabulary, followed by the windowed `TrainerExportBundle`. No exercise catalog is sent. |
| `Fernlet/App/Fernlet/CoachPlanImporter.swift` | Pasted text → `CoachPlan` → dated `PlannedWorkout` rows. Fail-closed JSON extraction/decode, `WorkoutSafetyFilter` pass, collision policy (spec D4), new-exercise registration, audit event. Also resolves `CoachPlanEdit`s (`ResolvedCoachPlanEdit`) that adjust / replace / delete workouts the user ALREADY planned, targeted by the ids the export echoes — refusing anything logged, missing, or in the past. |
| `Fernlet/App/Fernlet/CoachPlanPasteSheet.swift` | `CoachPlanPasteSheet` — where a plan comes back in: `TextEditor` + system `PasteButton` (never a programmatic `UIPasteboard` read), decode only, no writes. |
| `Fernlet/App/Fernlet/CoachPlanReviewView.swift` | `CoachPlanReviewView` — the review gate: start-date picker, blocking issues, per-exercise safety strikes, new-exercise list, collision choice, day-by-day preview. Nothing is written until Accept. |
| `Fernlet/App/Fernlet/LinkMetadataPrototypeView.swift` | DEBUG-only D11 link-metadata prototype (temporary): tests whether sender-supplied `LPLinkMetadata` survives into a sent iMessage bubble; not wired into any screen. |

## Wellbeing, Stress, And First Aid

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/FirstAidView.swift` | `FirstAidView` / `FirstAidTool` — calm first-aid sheet routing to breathing, 5-4-3-2-1 grounding, and the Worry Box, plus a static 988 support row. |
| `Fernlet/App/Fernlet/BreathingExerciseView.swift` | `BreathingExerciseView` — animated breathing circle (box 4-4-4-4 / relax 4-7-8, 1–3 min, optional haptics); a completed session reports its interval to the caller for Apple Health. |
| `Fernlet/App/Fernlet/GroundingView.swift` | `GroundingView` — 5-4-3-2-1 grounding: a stepped tap-to-advance sensory flow with a per-sense colour wash, ending on a soft affirmation. |
| `Fernlet/App/Fernlet/WorryBoxView.swift` | Worry Box surfaces: `WorryEntryView` write-and-let-go entry flow (hosted in First Aid) and the Private-hub kept-worries list with per-worry release. |
| `Fernlet/App/Fernlet/WorryBoxService.swift` | `WorryBoxService` — app-side owner of sealed, local-only Worry Box notes (user-lock/device-key model, never synced, absent from SealedBackup). |
| `Fernlet/App/Fernlet/StressService.swift` | `StressService` (`StressScoringContextProviding`) — opt-in body-signals stress estimate joining HealthKit HRV/RHR/respiration/temp with diary confounders via `StressEngine`; baselines in a device-local sidecar. |
| `Fernlet/FernletKit/Sources/FernletScoring/StressEngine.swift` | Pure personal-baseline stress estimator (`StressDaySample`) mapping HRV/resting-HR deviation to gentle wellness states. |
| `Fernlet/App/Fernlet/StressExplainerSheet.swift` | `StressExplainerSheet` — gentle "how we estimate this" body-signals explainer (wellness framing, not-medical disclaimer) that can chain into First Aid. |
| `Fernlet/App/Fernlet/GentleOffers.swift` | `GentleOfferKind` + pure gating/rotation logic for the once-per-day ambient gentle-offer card (breathing / worry box / short walk). |

## Workouts And Guided Training

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/WorkoutSetupView.swift` | `WorkoutSetupSheet` — captures durable workout context: split, training location/equipment, weekly frequency, experience, interests, and areas to work around. |
| `Fernlet/App/Fernlet/WorkoutLocationSetupView.swift` | `WorkoutLocationSetupView` — location creator + equipment picker: choose where you train, then check off equipment in a categorized grid mapped to coarse planning capabilities. |
| `Fernlet/App/Fernlet/WorkoutPlanningService.swift` | `WorkoutPlanningService` — split recommendation, day-plan generation, and AI day-plan adjustment extracted from `FernletStore` (owns the FoundationModels workout dependency). |
| `Fernlet/App/Fernlet/GuidedWorkout.swift` | `GuidedWorkoutSheet` — in-app guided workout runner (current exercise, set X of Y, live rest countdown) driven by `store.guidedRunState` and mirrored to the Live Activity. |
| `Fernlet/App/Fernlet/GuidedWorkoutEditorSheet.swift` | `GuidedWorkoutEditorSheet` — manual editor for a suggested session (per-exercise sets/reps/rest override, remove, reorder, add from catalog); saving replaces the session in today's plan. |
| `Fernlet/App/Fernlet/EquipmentIcons.swift` | `EquipmentIconLibrary` + a lightweight SVG renderer — editable stroke-only vector glyphs for gym equipment / locations, tinted to foreground, with SF Symbol fallback. |
| `Fernlet/App/Fernlet/ProgressPhotoTimeline.swift` | `ProgressPhotoSection` — Move-tab gym progress-photo timeline: sealed-at-rest body photos behind the global Fernlet lock, with camera/library add and tap-through detail. Since 2026-09-23 `ProgressPhotoSectionContent` decides the section's content in one place, and `ProgressPhotoLockNudgeCard` — an inline card above the capture control, never a modal — offers a user who skipped the onboarding lock step a lock that grants `.progressPhotos` only. |

## Custom Clothing, Coins, And Friend Shop

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/ZoomablePixelCanvas.swift` | `ZoomablePixelCanvas` — zoomable/pannable `UIScrollView` pixel-painting surface for the clothing editor (pinch-zoom, two-finger pan, one-finger paint), rendering via `ItemTextureRenderer`. |
| `Fernlet/App/Fernlet/FriendShopView.swift` | `FriendShopView` — browse friends' post-session shops and buy items with coins (banned-seller filtered, with per-item report). |
| `Fernlet/App/Fernlet/ClothingShareCodec.swift` | `ClothingShareCodec` — domain⇄wire codec for the in-person clothing shop: builds the device's own-designs `ClothingCatalogPayload` and re-sanitizes every incoming item before use. |
| `Fernlet/App/Fernlet/ShopAlert.swift` | `ShopAlert` + `ShopAlertContext` — the three ways a shop listing can be refused (flagged name, listing cap, store ban) as alert cases, with the per-screen copy keyed by which screen is presenting. The shared home of the refusal alerts previously written out twice, in `CreationStudioView`'s confirmation step and `WardrobeView`'s swipe-to-sell path; in every case the item itself was already saved, just left unlisted. The store-ban case carries the ban's remaining time, and `ShopBanRemainingTime` renders it as one friendly, localized, rounded-UP unit ("30 days", "36 hours") in place of the old "after a while" (tracker §3.5). |
| `Fernlet/FernletKit/Sources/StoreCore/CoinLedgerService.swift` | `@MainActor @Observable CoinLedgerService` owning the coin ledger in memory with debounced append-only persistence. |
| `Fernlet/FernletKit/Sources/StoreCore/CustomItemService.swift` | `@MainActor @Observable CustomItemService` owning the custom-item collection with debounced append/upsert persistence. |
| `Fernlet/FernletKit/Sources/StoreCore/MilestoneLedgerService.swift` | `@MainActor @Observable MilestoneLedgerService` owning the milestone ledger with debounced append-only persistence. |
| `Fernlet/FernletKit/Sources/CloudKitSync/CoinLedgerRepository.swift` | Per-row Core Data + iCloud coin-ledger store (`CoinLedgerRepository`), append-only JSON payload rows. |
| `Fernlet/FernletKit/Sources/CloudKitSync/CustomItemRepository.swift` | Per-row Core Data + iCloud custom-item store (`CustomItemRepository`), append/upsert-only JSON payload rows. |
| `Fernlet/FernletKit/Sources/CloudKitSync/MilestoneLedgerRepository.swift` | Per-row Core Data + iCloud milestone-ledger store (`MilestoneLedgerRepository`); append-only in normal operation, plus the `deleteAll()` delete-everything wipe path (added 2026-08-20; since 2026-08-21 the service follows the delete with a `resetBoundary` marker append). |
| `Fernlet/FernletKit/Sources/CloudKitSync/AppendOnlyRowStore.swift` | `AppendOnlyRowStore` — the shared upsert-only per-row Core Data engine (JSON `payloadData` rows keyed by a stable `idString`, sorted by `createdAt`, encoded via `RowPayloadCoders`) whose load/upsert bodies `CoinLedgerRepository`, `MilestoneLedgerRepository`, and `CustomItemRepository` previously carried as byte-for-byte clones. Upserts only the rows handed to it (a stale in-memory set cannot wipe rows synced in from another device) and deliberately exposes **no** delete path — deletion policy stays per repository. |
| `Fernlet/FernletKit/Sources/ProximityKit/ClothingSharing/MeshClothingShop.swift` | Friend-mesh clothing shop: accumulates pairwise-sealed `.clothingCatalog` payloads from committed slots, then opens a 1-hour memory-only post-session browse window; receive/window state only, never persisted. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/ClothingSharePayloads.swift` | In-person clothing-shop wire models: `ClothingCatalogPayload` carrying a peer's anonymous designer id, display name, and capped deterministically-ordered `CustomizationItem`s. |

## Onboarding

| File | Purpose |
| --- | --- |
| `Fernlet/App/Fernlet/OnboardingCoordinator.swift` | Onboarding state machine, step progression, storage choice, lock setup deferral, iCloud data detection, and `OnboardingDefaults` keys. Since 2026-09-23 also the post-age-gate intimacy choice (`ageCheckFinished()` → the step's second page; `complete()` commits it only when answered, different from the setting, and the 16+ gate is still open) and `DeferredLockSetupNudge`, the one-time progress-photo lock-setup nudge for a user who deferred the lock step — the first reader of `lockSetupDeferredKey`. |
| `Fernlet/App/Fernlet/OnboardingWelcomeView.swift` | First onboarding screen presenting the Fernlet welcome message and entry CTA. |
| `Fernlet/App/Fernlet/OnboardingPermissionsView.swift` | Onboarding step for requesting HealthKit and notification permissions. |
| `Fernlet/App/Fernlet/OnboardingStorageChoiceView.swift` | Onboarding step for choosing local vs. iCloud storage, with existing cloud data detection and migration summary. |
| `Fernlet/App/Fernlet/OnboardingLockSetupView.swift` | Onboarding step for setting up a passcode or biometrics-only lock, with a skip/defer path. |
| `Fernlet/App/Fernlet/OnboardingIntimacyChoiceScreen.swift` | `OnboardingIntimacyChoiceScreen` (2026-09-23) — the personal-details step's second page, shown only to a user the 16+ intimacy gate admits: a neutral "Keep intimacy tracking" (pre-selected, because it is what the setting already says) or "Turn it off", under the step's own "N of 8" caption and swapped in rather than presented as a sheet. Draft state until Continue; the answer lands through `FernletStore.setIntimacyTrackingVisible(_:)`, the Settings toggle's setter, and off hides — never deletes. |

## Lock And Privacy

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/FernletLockUI/FernletLockGate.swift` | View modifier that gates protected UI behind the Fernlet lock state. |
| `Fernlet/App/Fernlet/AgeAssuranceStore.swift` | `AgeAssuranceStore` — owns the device-local age determination behind the intimacy (16+) and mesh-chat (13+) gates. Persists to the never-synced sidecar; fail-closed on a fresh or undecodable record. |
| `Fernlet/App/Fernlet/AgeAssuranceRequest.swift` | The single seam to Apple's `DeclaredAgeRange` framework: one prompt for gates 13/16/18, plus the `.requestsAgeRange` view modifier. Every failure path lands on undetermined. |
| `Fernlet/App/Fernlet/AgeGateNotice.swift` | `AgeGateNotice` — the locked-state row for an age-gated feature: states the true reason, and offers a re-check plus the manual confirmation only when the system never ruled. |
| `Fernlet/FernletKit/Sources/FernletLockUI/FernletLockView.swift` | Lock setup, unlock UI, numeric pad, passcode entry, and related lock interaction views. |
| `Fernlet/FernletKit/Sources/FernletLock/FernletLockService.swift` | Lock service protocols, lock state, credential types, crypto provider, keychain item handling, audit logging, and lock/unlock orchestration. |
| `Fernlet/FernletKit/Sources/FernletFoundation/KeychainHelpers.swift` | Low-level Keychain read/write/delete helpers shared by lock and identity services. |
| `Fernlet/FernletKit/Sources/FernletFoundation/FernletLockError.swift` | `FernletLockError` error enum for app-lock/passcode/biometric failures with localized descriptions. |
| `Fernlet/FernletKit/Sources/FernletCrypto/ColumnCrypto.swift` | Shared ChaChaPoly column-encryption helper (`ColumnCrypto`) sealing/opening per-column values under a per-label derived key. Also the single home of the HKDF-SHA256 column-key derivation (`deriveColumnKey`, module-internal so production code reaches it only through the sealing methods) since the duplicate `FernletLockCrypto.deriveColumnKey` was deleted — the label and the derivation are part of the at-rest format, so changing either orphans every sealed column. |
| `Fernlet/FernletKit/Sources/FernletCrypto/CryptographicPurpose.swift` | `CryptographicPurpose` (an opaque, unforgeable domain value whose initializer is `fileprivate`) and `FernletCryptoPurpose`, the 47-entry registry of every signature, KDF, HMAC, AEAD and hash domain in the app. Purpose spellings are FROZEN protocol data: each is an input to a shipped key, digest, or signature, so renaming one orphans what it protects. Also owns `TranscriptFraming` and `signingBytes(_:)`, the positional check that a signed transcript really begins with its reviewed domain. Full inventory and change procedure: [Crypto-Domain-Separation.md](Crypto-Domain-Separation.md). |
| `Fernlet/FernletKit/Sources/FernletCrypto/DeviceBindingID.swift` | Per-install random ID (ThisDeviceOnly keychain row) passed by `ColumnCrypto` as AEAD associated data on new sealed-column writes (device-bound v2 format); fail-open to the legacy format when unavailable. |
| `Fernlet/FernletKit/Sources/FernletLock/SecureEnclaveContentKeyWrap.swift` | ECIES wrap of the lock content key under a non-exportable Secure Enclave P-256 key. Additive while `LockKeychainKey.wrappedContentKey` exists (LEGACY / SE-less hardware); AUTHORITATIVE and the only recoverable copy once the service has proven a round-trip and deleted the scrypt item (HARD-BOUND — Docs/Verifiability.md §4/§6.1, done). `unwrap` fails soft to nil for the maintenance paths; the hard-bound recovery path uses `unwrapResult` instead, so a destroyed enclave key (`FernletLockError.contentKeyUnrecoverable`) is never confused with a keychain that would not answer (`.contentKeyTemporarilyUnavailable`). |
| `Fernlet/App/Fernlet/JournalSealingCoordinator.swift` | `JournalSealingCoordinator` (`JournalSealingContext`) — sealed journal management (content key, device fallback key, sealed-ID set, narrative repository) extracted from `FernletStore`; `isSealed` drives the snapshot text-strip. |
| `Fernlet/App/Fernlet/SealedBackupService.swift` | `SealedBackupCrypto` — AES-GCM seal/open of `SealedBackupRecord`s bound to the iCloud-Keychain escrow signing key. |
| `Fernlet/App/Fernlet/SealedBackupCoordinator.swift` | `SealedBackupCoordinator` (`SealedBackupContext`) — sealed iCloud backup/restore of Tier-2 memory, journals, and period narratives under the escrow key, extracted from `FernletStore`. |
| `Fernlet/App/Fernlet/SealedPhotoBackupService.swift` | `SealedPhotoCrypto` (AAD **v3**, per-photo) + `SealedPhotoBackupService` — the own-photo escrow route's mechanism half (hardening Phase 5, step 5b): seal one record per photo id, write the per-corpus manifest LAST as the commit marker, restore manifest-first (authenticate → generation high-water → fetch bodies), incremental add, delete, corpus teardown. Derives on the same v2 salted escrow key as the chunked route; a **distinct namespace**, never a `SealedBackupPayloadType` case. |
| `Fernlet/App/Fernlet/OwnPhotoBackupCoordinator.swift` | `OwnPhotoBackupCoordinator` (`OwnPhotoBackupContext`) — the own-photo escrow route's policy half: the single opt-in preference, the per-corpus FILE-PRESENCE no-clobber gate, restore-before-reupload ordering, the progress corpus's sealed-index sidecar, the delete-all teardown, and `OwnPhotoUploadLedger` (the device-local record of which ids THIS device uploaded — the prune scope that keeps one device's pass from deleting another device's photos). Owns its own `MealPhotoStore`/`ProgressPhotoStore` instances over `OwnPhotoCorpusLayout`. |
| `Fernlet/App/Fernlet/DuressRecoveryCoordinator.swift` | `DuressRecoveryCoordinator` (+ `DuressRecoveryRequest` / `DuressRecoveryReply` / `DuressRecoveryTranscript` / `DuressRecoveryDecision` / `DuressRecoveryOutcome` / `DuressRecoveryError`) — the app-side half of `DuressMode.recoveryLock` (security-hardening Phase 7, step 8): the in-person QR mutual-auth ceremonies that enroll the user's own second device as a recovery custodian and later get the content key back from it. Lives in the app target because it needs BOTH `FernletLock` and `ProximityKit` and the lock must gain no ProximityKit edge — the content key is released into `IdentityService.seal` through a closure, never returned. One type plays both roles (custodian displays, primary scans). Transport-free: every step returns the bytes to send. **No cloud route of any kind** — recovery is in-person mesh + QR only. |
| `Fernlet/App/Fernlet/DuressPINSetupView.swift` | `DuressPINSetupView` (+ `DuressSetupAvailability` / `DuressModeCopy`) — Settings → App lock → **Duress code** (security-hardening Phase 7, step 9): choose one `DuressMode`, set/change the code, enrol or remove the recovery device, remove the code. Real-PIN-gated **by construction** (the page it is pushed from is `.appLockSettings`-gated), so it takes no `current:` passcode. `DuressSetupAvailability` is the view-free gating policy — `.recoveryLock` unselectable without an enrolled custodian, the recovery device unremovable while `.recoveryLock` is armed — split out so those two rules are unit-testable. `DuressModeCopy` is the destructive-path copy, reviewed as a unit; service rejections are surfaced verbatim, never restated. |
| `Fernlet/App/Fernlet/DuressRecoveryCeremonyViews.swift` | `DuressRecoveryEnrollmentSheet` / `DuressRecoveryReturnSheet` (+ `DuressCeremonyMessage` / `DuressCeremonyQR`) — the in-person screens that drive `DuressRecoveryCoordinator`, for both roles on both phones. Transport is a **QR relay**, not the mesh: the coordinator is transport-free, so the bytes ride codes on screen and ProximityKit gains no new `PayloadType`. The relay adds no trust — the small hops carry only public nonces and public keys (a substituted key produces a response the real scanner rejects), and the large hops are already sealed and signed by the coordinator. |
| `Fernlet/App/Fernlet/DeleteAllDataConfirmation.swift` | `DeleteAllDataConfirmation` — the single shared destructive confirm dialog for "delete everything", funneling both Settings entry points to `FernletStore.deleteAllData`. |
| `Fernlet/App/Fernlet/DeleteEverythingFlow.swift` | `DeleteEverythingFlow` — per-screen "delete everything" wipe state (busy/success/failure) + confirmation glue and the shared `.deleteEverythingAlerts` outcome alerts, instantiated once per entry point (`SettingsSheet`, `PrivacyDataSettingsView`); buttons/overlays/dismiss guards stay in the views. |
| `Fernlet/App/Fernlet/PrivacyPolicyView.swift` | `PrivacyPolicyView` — in-app Privacy Policy screen rendering the policy in the app's voice/type, with a DEBUG-only draft banner. |
| `Fernlet/App/Fernlet/PrivacyDataSettingsView.swift` | Privacy and data settings screen for managing storage preferences, HealthKit toggles, and sealed backup options. Routes every destructive/OFF toggle through a pre-commit warning; surfaces the sealed-backup restore status + escrow-conflict banner (retry / re-link). Also carries the own-photo escrow switch (with its size disclosure) and the step-5c "Lock photos to this device" consent ceremony — the branch that lets a user device-bind their own photos without turning the iCloud photo backup on. Holds the "Forget corrected searches" row (research §26 fix 1.10's escape hatch — clears the local correction memory and nothing else, behind the same `DestructiveConfirmation`, hidden when nothing is remembered). |
| `Fernlet/App/Fernlet/DestructiveConfirmation.swift` | Reusable destructive-action confirmation (`DestructiveConfirmation` + `.destructiveConfirmation` modifier): the mutation runs only on an explicit destructive-role confirm, so no destructive settings toggle can ship without a warning. |

## Data, Persistence, And State

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/FernletDomainModel/` | Core domain models (formerly the app's `Models.swift`), split per area — see the Domain Models section. |
| `Fernlet/App/Fernlet/FernletStore.swift` | Main observable app store that coordinates repository data and app-level state changes. Holds the ordered `deleteAllData` wipe funnel, whose **leg 0** (P6 item 4's fix review) calls `MeshNetworkManager.beginPrivacyWipe()` — the manager's one transcript-clear funnel, which also bumps `transcriptGeneration` and switches the routed projection off for the rest of the wipe — paired with `endPrivacyWipe()` in a `defer`. **P7 item 2:** owns `applyProximityRunPolicy(scenePhase:protectedDataAvailable:duressSessionActive:now:)` (the `appLockEngaged:` label retired by P9-3-A, 2026-09-22) — THE run-policy funnel and the only writer of `MeshNetworkManager.applyRoutedAccessGate(_:now:)` outside ProximityKit (the lock facts are parameters, read at the edge, never the lagging mirrors) — plus the `selectedTab` mirror, the memory-only `deleteAllInProgress` bracket flag raised inside the wipe funnel, and the kept `proximityRunVerdict`. **P7 item 3** split the funnel into a private core (`runProximityPolicy(_:now:)` — input, verdict, the gate write, then the seams' executor) behind three entries: the scene edge (as before, retaining its three facts), the view edge (`applyProximityRunPolicy(duressSessionActive:now:)`, a fresh duress fact over the retained scene facts) and the store edge (`reapplyProximityRunPolicy(now:)`, everything retained; before any scene edge both assume the most restrictive scene). The two nearby opt-in setters and the wipe funnel's raise and lower of `deleteAllInProgress` run the policy instead of calling a manager; `discoveryTimeoutTask` (the tab's five-minute timeout) lives here for the seams. |
| `Fernlet/FernletKit/Sources/StoreCore/AIRetryQueueService.swift` | AI analysis retry queue; stores pending retry records and calls `onChange` on each mutation. Extracted sub-service of `FernletStore`. |
| `Fernlet/FernletKit/Sources/StoreCore/DerivedSignalsService.swift` | Derived signal computation and deferred post-launch rebuild scheduling. Extracted sub-service of `FernletStore`. |
| `Fernlet/FernletKit/Sources/StoreCore/DerivedSignalsRebuilder.swift` | Pure helper that rebuilds derived signals from a historical day window; used by `DerivedSignalsService`. |
| `Fernlet/FernletKit/Sources/StoreCore/SavedRecipeService.swift` | Observable saved recipe list manager; loads, adds, deletes, and persists saved recipes. Extracted sub-service of `FernletStore`. |
| `Fernlet/FernletKit/Sources/StoreCore/SnapshotSaveCoordinator.swift` | Debounced snapshot persistence coordinator and remote-change reload handler. Extracted sub-service of `FernletStore`. |
| `Fernlet/FernletKit/Sources/StoreCore/PendingWriteBuffer.swift` | `DebouncedRowBuffer` / `DebouncedAppendBuffer` — the queue-then-debounce-then-flush write machinery, and its durability contract (the pending queue is the sole un-persisted copy, so it is cleared only after a confirmed write and a failed write keeps it for retry instead of trapping), shared by `SavedRecipeService` / `CustomItemService` (upsert+delete rows) and `CoinLedgerService` / `MilestoneLedgerService` (append-only rows). The one home of what those four services previously carried as byte-identical copies; row meaning, load-time dedup, and reset semantics stay in each service. |
| `Fernlet/App/Fernlet/FernletStoreLoader.swift` | Async store bootstrap coordinator that manages `preparing → ready → failed` phase transitions at launch; its once-per-process `startIfNeeded()` first runs `ProximityLiveActivityReaper.endOrphans()` (before any proximity manager can exist), then loads the store. |
| `Fernlet/App/Fernlet/FernletStoreAccess.swift` | `FernletStoreAccess` — the one process-global store-acquisition cache, shared by the scene loader (via `ExchangeIntentService.loadStoreForUI`) and the background App Intents. Main-actor isolated and single-in-flight: a cold background launch and the scene loader coalesce onto one `load()` rather than building competing `FernletStore`s over the same repositories. Lifted out of `ExchangeIntentService.swift` by the P10 mesh round so the companion background-refresh handler can share it. Its missing `deinit` is allowlisted by `MemoryLifecycleBoundaryTests` (ML1) with the invariant that makes it safe. **P10 item 4** adds the LATE ATTACH (decision D-10.4.1): the cache returns its store whatever arguments a later `load()` passes, and the scene loader passes the app's one long-lived `HealthKitService` while every background caller passes nil — so a cold background wake that built the store first left the foreground on a store with no gateway for the rest of the process, its workout sync silently falling back to a second instance of its own. `load()` now attaches a caller's gateway to a cached or coalesced store that has none, through `FernletStore.attachHealthKitServiceIfMissing(_:)`, and audits when it does. The repair lives here rather than in the refresh handler because the handler may not speak a HealthKit spelling at all (§16.4's wall), and because the background App Intents have had the same defect since they were written — the refresh only moved it from a rare Shortcut to a fifteen-minute cadence. |
| `Fernlet/App/Fernlet/DataExportBuilder.swift` | `FernletDataExport` + `FernletStore` builder — assembles the user's non-sealed data into one human-readable JSON export file (allowlist projection; sealed/sensitive data excluded by construction). |
| `Fernlet/App/Fernlet/TrainerExportBuilder.swift` | `TrainerExportBundle` / `TrainerExportOptions` / `TrainerExportWindow` + `FernletStore` builder — curated fail-closed-allowlist workout + nutrition export for a trainer/nutritionist, with opt-in extras. Also carries macro targets, training setup (location/equipment/split), and the per-exercise progression rollup (`ExerciseLineParser`). The window defaults to unlimited (file/mesh) and narrows for the clipboard. |
| `Fernlet/App/Fernlet/FernletStore+DemoSeed.swift` | DEBUG-only `FernletStore` extension that seeds today's diary with representative demo content (day-idempotent) so every tab renders populated cards for the UX appearance UI tests. |
| `Fernlet/FernletKit/Sources/LocalPersistence/LocalFernletRepository.swift` | Local JSON-style repository contract and implementation, snapshot/database records, derived signal creation, limits, and memory engine logic. |
| `Fernlet/FernletKit/Sources/CloudKitSync/CoreDataFernletRepository.swift` | Core Data-backed repository implementation. |
| `Fernlet/FernletKit/Sources/CloudKitSync/Persistence.swift` | Core Data persistence controller setup for runtime and previews/tests. |
| `Fernlet/FernletKit/Sources/FernletFoundation/CoreDataModelBuilding.swift` | `CoreDataModelBuilding.makeAttribute` — the shared `NSAttributeDescription` factory for the two programmatic (no `.xcdatamodeld`) model builders, `PersistenceController` (CloudKitSync) and `PrivatePersistenceController` (PrivateStoreCore), each of which previously held a private byte-identical copy. Pure in-memory construction that names no sealed or synced entity, which is what lets it sit at layer 0 and be reused from both sides of the S3 wall. |
| `Fernlet/FernletKit/Sources/CloudKitSync/CloudKitDataService.swift` | CloudKit record read/write service and `ExistingDataSummary` detection used during onboarding migration. |
| `Fernlet/FernletKit/Sources/FernletFoundation/StoragePreferences.swift` | Codable model for user storage preferences: iCloud sync, local backup exclusion, HealthKit capability toggles, and sealed backup flags. |
| `Fernlet/FernletKit/Sources/PrivateHealthStore/PeriodTrackerStore.swift` | Period tracker domain state, cycle events, symptom/flow models, cycle phase logic, HealthKit integration contract, and store behavior. |
| `Fernlet/FernletKit/Sources/PrivateHealthStore/MenstrualNarrativeRepository.swift` | Persistence for menstrual narrative entries. |
| `Fernlet/FernletKit/Sources/PrivateStoreCore/PendingNarrativeBuffer.swift` | Codable buffer for pending menstrual narrative payloads. |
| `Fernlet/FernletKit/Sources/CloudKitSync/SavedRecipe.swift` | Saved recipe model and repository implementations, including legacy JSON migration support. |
| `Fernlet/FernletKit/Sources/HealthKitGateway/ActivityTypeCatalog.swift` | Workout activity type search and HealthKit activity type mapping. |
| `Fernlet/FernletKit/Sources/FernletPersistence/FernletRepository.swift` | The abstract `FernletRepository` persistence contract for snapshot/day/tier-two load and save. |
| `Fernlet/FernletKit/Sources/FernletPersistence/FernletSnapshot.swift` | `FernletSnapshot` persisted aggregate value type serialized to/from storage. |
| `Fernlet/FernletKit/Sources/FernletPersistence/HealthKitStorageStrip.swift` | The HealthKit half of the storage strip (HealthKit information is never stored in iCloud, 2026-09-23): `FernletDay.strippingHealthKitValues()` / `healthKitResidue` / `overlayingHealthKitResidue(_:)`, the device-local `DeviceHealthResidue` value, and the `DeviceHealthResidueStoring` cache contract (which since the body-profile follow-up also carries the imported body profile). |
| `Fernlet/FernletKit/Sources/FernletPersistence/DeviceHealthBodyProfile.swift` | `DeviceHealthBodyProfile` — the age, sex, height and weight THIS device imported from HealthKit, kept in the device cache instead of the synced `settings.userProfile` (2026-09-23): `applied(to:)` (the overlay, re-clamped), `removingFieldsEdited(from:to:)` and `typedProfile(_:editedFrom:to:)` (a profile-screen edit writes only the changed fields to settings). |
| `Fernlet/FernletKit/Sources/FernletPersistence/RemoteChangePublishingRepository.swift` | `RemoteChangePublishingRepository` refinement exposing an iCloud remote-change `AnyPublisher`. |
| `Fernlet/FernletKit/Sources/FernletPersistence/DayRecordRepositoring.swift` | Per-day persistence protocol (`DayRecordRepositoring`) plus the sanitize-barrier `DayRecordUpsert` mint. |
| `Fernlet/FernletKit/Sources/FernletPersistence/SavedRecipeRepositoring.swift` | `@MainActor` append/upsert-only saved-recipe persistence protocol (`SavedRecipeRepositoring`). |
| `Fernlet/FernletKit/Sources/FernletPersistence/CoinLedgerRepositoring.swift` | `@MainActor` append-only coin-ledger persistence protocol (`CoinLedgerRepositoring`). |
| `Fernlet/FernletKit/Sources/FernletPersistence/CustomItemRepositoring.swift` | `@MainActor` append/upsert-only custom-item persistence protocol (`CustomItemRepositoring`). |
| `Fernlet/FernletKit/Sources/FernletPersistence/MilestoneLedgerRepositoring.swift` | `@MainActor` milestone-ledger persistence protocol (`MilestoneLedgerRepositoring`); append-only, deliberately no delete on the protocol — the wipe's `deleteAll()` lives only on the CloudKitSync conformer. |
| `Fernlet/FernletKit/Sources/LocalPersistence/DayContentSummary.swift` | `DayContentSummary` precomputed content-count roll-up carried in the aggregate blob for single-read existing-data detection. |
| `Fernlet/FernletKit/Sources/LocalPersistence/DerivedSignalFactory.swift` | `DerivedSignalFactory` computing the rolling derived-signal window (mood/energy/eating/progression/micronutrient trends) from stored day pairs. |
| `Fernlet/FernletKit/Sources/LocalPersistence/LogRecords.swift` | Derived-table DTO records (`DailyLogRecord`) persisted alongside the local database. |
| `Fernlet/FernletKit/Sources/LocalPersistence/TierTwoMemoryEngine.swift` | `TierTwoMemoryEngine` deriving capped tier-two behavioral memories from the rolling 14-day window. |
| `Fernlet/FernletKit/Sources/LocalPersistence/TierTwoMemoryStore.swift` | `TierTwoMemoryStore` — the device-local, never-synced, always-backup-excluded JSON sidecar the tier-two memories live in (owner decision 2026-09-23); refreshed by both repositories after a successful save, purged by delete everything. |
| `Fernlet/FernletKit/Sources/LocalPersistence/FeelingTagMoodScale.swift` | `FeelingTag.moodScore` — the fixed 0.2–1.0 mood scale shared by the deterministic derived engines, and the single home of the two previously duplicated private copies in `DerivedSignalFactory` and `TierTwoMemoryEngine`. |
| `Fernlet/FernletKit/Sources/CloudKitSync/DayRecordRepository.swift` | Per-row Core Data + iCloud day-history store (`DayRecordRepository`) with duplicate-row dedup by `dateKey`. |
| `Fernlet/FernletKit/Sources/FernletFoundation/RowPayloadCoders.swift` | `RowPayloadCoders`: single source of truth for the persisted-JSON encoder/decoder config (sorted keys + ISO-8601 dates, with opt-in pretty-printing for the human-readable on-disk files). Moved down to layer 0 from `CloudKitSync` so the local-only blob file in `LocalPersistence` shares it too; it replaced the per-store private `makeEncoder`/`makeDecoder` pairs that had drifted (the coin-ledger and custom-item row stores once wrote bare numeric dates). ISO-8601 truncates to whole seconds — callers comparing dates across representations must account for that. |
| `Fernlet/FernletKit/Sources/CloudKitSync/MultiDeviceSyncWarning.swift` | Pure `MultiDeviceSyncWarning` classifier for the "devices will diverge without iCloud" warning state. |
| `Fernlet/FernletKit/Sources/CloudKitSync/SealedBackupRecord.swift` | Sealed-backup transport DTOs (`SealedBackupRecord`, `SealedBackupPayloadType`): the opaque encrypted CloudKit record shape. |
| `Fernlet/FernletKit/Sources/CloudKitSync/SealedPhotoRecord.swift` | Own-photo escrow transport DTOs (`SealedPhotoCorpus`, `SealedPhotoSlot`, `SealedPhotoManifest`, `SealedPhotoRecord`): one encrypted record per photo id plus the sealed per-corpus manifest (id set + per-id content hash + the progress corpus's index sidecar). Born at record format 2 — no legacy shape to tolerate. |
| `Fernlet/FernletKit/Sources/DiaryStore/DiaryStore.swift` | `@MainActor @Observable DiaryStore`: the portable diary-state slice (scoring, meal/recipe/workout/journal/settings) carved from `FernletStore`. |
| `Fernlet/FernletKit/Sources/PrivateStoreCore/PrivatePersistenceController.swift` | `PrivatePersistenceController`: dedicated local-only (never-iCloud) Core Data store for sealed, encrypted entities. |
| `Fernlet/FernletKit/Sources/PrivateStoreCore/PrivateRowPlumbing.swift` | `PrivateRowPlumbing.deleteRows` — the shared keyless fetch → guard → delete → save → history-prune sequence that the sealed repositories' `deleteAll()` methods (`JournalNarrativeRepository`, `WorryNarrativeRepository`, `IntimacyLogRepository`, `MenstrualNarrativeRepository`) each repeated inline. Rows are deleted without decrypting them, so deletion stays available while the app is locked or the feature is hidden, and the (rethrowing) persistent-history prune is part of the sequence. Deliberately not used by `purgeEncryptedEntities()`, whose wipe must stay atomic across all four entities under one save. |
| `Fernlet/FernletKit/Sources/PrivateHealthStore/IntimacyLogRepository.swift` | Sealed Core Data repository for `IntimacyLog` records (ChaChaPoly column-encrypted intimacy notes). |
| `Fernlet/FernletKit/Sources/PrivateHealthStore/IntimacyLogStore.swift` | `@MainActor IntimacyLogStore`: the fail-closed, visibility-gated funnel for every intimacy sealed-note read/write. |
| `Fernlet/FernletKit/Sources/PrivateMemoryStore/JournalNarrativeRepository.swift` | Sealed journal-narrative store: `JournalNarrative` model, `JournalNarrativeStoring` protocol, and its encrypted repository. |
| `Fernlet/FernletKit/Sources/PrivateMemoryStore/WorryNarrativeRepository.swift` | Sealed, device-only Worry Box store: `WorryNarrative` model, `WorryStoring` protocol, and its encrypted repository. |
| `Fernlet/FernletKit/Sources/PeriodContextBridge/PeriodContextBridge.swift` | Period-module egress bridge: raw→abstract cycle conversions (`PeriodPhaseBand`) exporting only coarse enums past the S3 wall. |
| `Fernlet/FernletKit/Sources/PeriodContextBridge/PeriodPhaseTrendEngine.swift` | `PeriodHealthTrend` device-sealed per-phase wellbeing trend (coarse direction + confidence band) from the user's own history. |
| `Fernlet/FernletKit/Sources/CloudKitSync/HeartDropCloudTransport.swift` | CloudKit **public**-database ferry for heart drops (the app's only public-DB use): upload one sealed drop per tag, fetch a friend's tag window with per-chunk anti-starvation budgeting. Sees tags and ciphertext only. |
| `Fernlet/App/Fernlet/SealedBackupGenerationStore.swift` | Device-local, never-synced high-water mark of the highest sealed-backup generation this device has written or accepted, per payload type **and per own-photo corpus** (a separate key namespace, since the two routes have independent write histories) — the state half of the rollback defense. The crypto half is the generation binding in `SealedBackupCrypto.authenticatedData`. Cleared by the delete-all path. |
| `Fernlet/FernletKit/Sources/CloudKitSync/CloudKitSchemaDeploy.swift` | The schema-deploy launch flag. `isRequested(arguments:)` is pure and testable; the caller that actually pushes the schema is `#if DEBUG`-gated so it cannot fire in Release. See [CloudKit-Schema-Deploy.md](CloudKit-Schema-Deploy.md). |

## Domain Models (FernletDomainModel)

The portable value-type layer that replaced the app's old `Models.swift`, split per area.

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/FernletDomainModel/WellbeingModels.swift` | Day, health-context, journal, sleep, hygiene, goals, and daily-score value models (`FernletDay`, …). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/NutritionModels.swift` | Nutrition, food, meal, recipe, and macro/micronutrient value models (`UserNutritionProfile`, …). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/WorkoutModels.swift` | Workout, exercise, muscle, and equipment value models (`Workout`, …). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/WorkoutProgram.swift` | Workout program/training model value types: `ExperienceLevel`, `WorkoutProfile`, training-split selection. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/WorkoutRestGuidance.swift` | Evidence-based default rest-between-sets table (`WorkoutRestGuidance`) keyed by movement `Demand` and training goal. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/CompanionModels.swift` | Companion appearance, workshop, and texture value models (`WorkshopData`, `TextureEntry`). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/SettingsModel.swift` | `FernletSettings` app-settings serialization aggregate (hydration, companion, equipped items, designer provenance). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/AgeAssurance.swift` | `AgeGate` (13/16/18), `AgeGateVerdict`, `AgeAssuranceProvenance`, and `AgeAssuranceRecord` — the pure age-gate rules: fail-closed, a below-gate verdict is unappealable, and provenance is asymmetric (it can close a gate but not open one). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/NavigationEnums.swift` | Screen/widget/shortcut navigation enums (`FernletScreen`, `ConnectionInspectorMode`). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ScoringValueTypes.swift` | Pure scoring value types (`ScoringWeights`) carved out of the app-layer scoring logic. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/FoodItemSearch.swift` | Pure relevance-search value logic over `FoodItem` plus a restaurant-chain brand lexicon (`FoodItemSearch`, `FoodBrandLexicon`). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/FoodSearchHistory.swift` | Research §26 fix 1.9's history-first ranking input (`FoodSearchHistory`): the foods this user has logged, weighted `log(1 + count)` × exponential recency decay (τ = 21 days), read as the TOP key of `FoodItemSearch`'s comparator on the typed-query surface only. DERIVED from `DiaryStore.recentMeals` on every write and at init — no new persisted surface, so the diary's own wipe is the feature's wipe. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/RecipeSourceURLMatcher.swift` | The one source-URL normalization rule (scheme/host case-insensitive, fragment stripped, query kept) behind the zero-network duplicate skip, the supersede-on-re-import match, the superseded-photo cleanup, and the mesh already-saved decision. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/EnumDecodeCompat.swift` | Forward-compatible decode helpers (`EnumDecodeCompat`) for synced raw-value enums: park and re-adopt unknown tokens via side channels. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/AIAnalysisRetryRecord.swift` | Codable retry-queue record (`AIAnalysisRetryRecord`) for pending AI-analysis payloads; element type of `FernletSnapshot.retryQueue`. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/AIDestination.swift` | `AIDestination` enum naming where an AI call is routed (on-device FoundationModels or web nutrition lookup). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/TierTwoMemoryRecord.swift` | Codable tier-two behavioral memory record (`TierTwoMemoryRecord`) referenced by the repository and memory agent. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/DiagnosticLanguage.swift` | Pure best-effort classifier (`DiagnosticLanguage`) flagging clinical/diagnostic language to screen out of AI prompts and stored memories. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/JournalMemoryCapture.swift` | Core Memory's journal capture (owner decision 2026-09-23): `MemoryNote.emotionOnly(for:)` mints a journal entry's memory as its `FeelingTag` token with NO text, and `JournalMemorySummaryPolicy` is the test an AI summary must pass to become that memory's text (bounded, no diagnostic language, not verbatim / a prefix / an excerpt). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/CustomItemModels.swift` | User-designed custom clothing/accessory models: `ItemSlot` taxonomy, palette-indexed grid textures, `CustomizationItem`. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ItemDesigner.swift` | `ItemDesigner` provenance value carrying only an anonymous, stable designer id for "designed by …" attribution. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ItemNameModeration.swift` | Pure profanity/slur name gate (`ItemNameModeration`) for items listed for sale in the clothing shop. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ClothingModeration.swift` | Value types and pure verdict math for the in-person clothing-shop report/ban moderation ledger (`ReportReason`). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ModerationBanRecovery.swift` | `BanEvidence` + `ModerationBanRecovery` — the pure half of lifting a store ban when its reporters withdraw (2026-09-24): which recorded evidence a `retract` positively withdrew (absence never counts), the bounded canonical merge, and the monotone uncapped threshold test. Reporter identities arrive as opaque tags through a closure, so the module stays crypto-free. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ClothingShopLimits.swift` | Shared constants and a wire-boundary sanitizer (`ClothingShopLimits`) clamping untrusted peer-received shop items. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/CoinEconomy.swift` | Append-only coin-ledger model (`CoinLedgerEntry`, `CoinLedgerKind` earn/spend) and balance-aggregation math with structural idempotency. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/MilestoneLedger.swift` | Append-only cumulative care-milestone ledger model (`MilestoneEventKind`, `MilestoneLedgerEntry`); lifetime counts, monotonic between wipes (since 2026-08-20 "delete everything" clears the ledger, reversing the earlier survive-reset rule; since 2026-08-21 the wipe also appends a `resetBoundary` marker and aggregation counts only rows whose day is at/after the marker's day AND created strictly after its instant, so neither re-synced pre-wipe rows nor rows re-derived from re-synced days can resurrect the trail). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/IdentityDedup.swift` | `Array.deduplicatedByID()` — the application-level union-merge that collapses duplicate-id rows (first seen wins, order preserved), needed because `NSPersistentCloudKitContainer` mirrors by record identity and does not honor an id attribute as unique. The one implementation now behind `CoinEconomy.deduplicatedByID`, `MilestoneEconomy.deduplicatedByID`, and the StoreCore per-row services' load paths, which each held their own copy of the loop. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/Closeness.swift` | Deterministic closeness math and close-friend slot assignment: `FriendInteractionDayCounts` and capped daily warmth points. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/FriendState.swift` | Friend-facing fuzzy wellbeing state (`FriendFuzzyState`) and its sealed shareable payload, derived from the state enum, never the score. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/HeartSharing.swift` | Wire model (`HeartPayload`) and presentation math for proximity "send good vibes" hearts between friends. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ActivityModels.swift` | Pure value types for proximity group activities: host-signed versioned rosters, join tokens, and `ActivityLimits`. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ProximityCoordinatorEnums.swift` | Top-level proximity enums (`ProximityRole`, `ProximityMode`, `ProximityRangingMode`) hoisted out of `ProximityCoordinator`. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ProximityPersistenceRecords.swift` | Codable proximity trust/audit DTOs (`ProximityTrustedPeerRecord`) for the persisted, synced trust vault. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/FDADailyValues.swift` | The single FDA Daily Value reference table shared by the gap analyzer and the label scanner, verified against 21 CFR 101.9. Flat label-comparison reference — no age/sex adjustment, no upper intake levels. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/GroceryAggregation.swift` | Pure, catalog-free shopping-list aggregation (F3, §11.3): merges several recipes' ingredients into one consolidated list. Below the wall, no I/O — `FoodItem` resolution stays the caller's job. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/RecipeScaling.swift` | Pure proportional recipe scaling (F4, §11.4). A non-persisted view/share-time transform returning scaled *copies* — it never mutates a stored recipe, resolves no `FoodItem`, and touches no catalog. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/RecipeSubstitution.swift` | A bound substitution suggestion: a catalog `FoodItem` proposed to replace one ingredient, plus an optional display-only reason. The model picks a candidate *number*; code binds it back to the catalog. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/HeartDropTransport.swift` | The `HeartDropRecord` (rotating pairwise day tag + sealed blob) and the `HeartDropTransporting` seam. The transport neither knows nor can learn who either endpoint is — this is why the wall holds across CloudKit. |

## Food, Nutrition, And Recipe Services

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/FoodCatalog/FoodDataCatalog.swift` | USDA food item record decoder (compact + raw FDC), source-JSON loading for the DB generator, `FoodItemSearch` scorer, and recipe search helpers. |
| `Fernlet/FernletKit/Sources/FoodCatalog/FoodCatalog.swift` | `FoodCatalog` service: merges the SQLite bundled food source with the in-memory user-item snapshot; search via FTS candidate prefilter → `FoodItemSearch` scorer, plus ID/exact-name resolution. Replaces the old in-memory `allFoodItems` array. Also holds the fix-1.10 correction-alias snapshot (`setSearchAliases`), which promotes the user's own remembered pick to rank 1 on `results(for:)` — never on the bind-confidence `scoredResults`. |
| `Fernlet/FernletKit/Sources/FoodCatalog/BundledFoodStore.swift` | `BundledFoodSource` protocol + read-only SQLite-backed `SQLiteBundledFoodSource` (FTS5 candidate query, id/exact lookups, hydration) and `InMemoryBundledFoodSource` for tests; shared `FoodCatalogSchema`. |
| `Fernlet/App/Fernlet/FoodCatalogDatabaseBuilder.swift` | Build-time generator that converts the `FoodDataSource/*.json` foods into the shipped read-only `FoodCatalog.sqlite` (run via the gated `FoodCatalogGenerationTests`). Not used at runtime. |
| `Fernlet/Scripts/food-catalog/` | Offline, standard-library preservation pipeline for the approved FDC/FNDDS/curated-branded/CoFID source set; builds only ignored validation artifacts and never replaces the shipping catalog. Its README is the reproducibility and corrective-workflow contract. |
| `Fernlet/FoodDataSource/FoodCatalogSourceManifest.json` | Immutable source/version/hash/license/attribution contract, strict FDC allowlist, and explicit CNF/NEVO exclusions for the offline preservation pipeline. |
| `Fernlet/FoodDataSource/FoodCatalogGenerationEvidence.json` | Compact measured validation, FTS, preservation, size/ODR arithmetic, and committed-catalog checksum evidence for the historical schema-v3 local generation; current schema-v4 reproducibility results are recorded in the round ledger. Contains no raw data or SQLite payload. |
| `Fernlet/Docs/Food-Catalog-Runtime-ODR-Options-2026-08-24.md` | Measured base/ODR/preservation distribution options, P0/P1/P2 projection boundary, and the owner decision required before any runtime catalog or ODR integration. |
| `Fernlet/FoodDataSource/USDAFoodItems.json` | Source USDA food item dataset consumed by the build-time DB generator (not bundled at runtime). |
| `Fernlet/FernletKit/Sources/FoodCatalog/Resources/FoodCatalog.sqlite` | The shipped read-only SQLite food catalog (FTS5), generated from `FoodDataSource/` and loaded via `Bundle.module`. |
| `Fernlet/App/Fernlet/WorkoutExercises.json` | Bundled exercise dataset consumed by the workout system. |
| `Fernlet/FernletKit/Sources/AIProviders/FoundationFoodSelection.swift` | Foundation model availability and food selection model helpers. |
| `Fernlet/FernletKit/Sources/AppServices/NutritionLabelScanner.swift` | Nutrition label scan result model, scan errors, and scanner implementation. |
| `Fernlet/App/Fernlet/NutritionLabelCameraSheet.swift` | Camera and image picker UI for capturing nutrition labels and reviewing scan results. |
| `Fernlet/FernletKit/Sources/AIProviders/RecipeWebImporter.swift` | Recipe import pipeline for extracting structured recipes from web content and metadata. |
| `Fernlet/App/Fernlet/RecipeShareCodec.swift` | Encodes and decodes recipes into a shareable text format for peer-to-peer recipe sharing. |
| `Fernlet/App/Fernlet/MealBuilder.swift` | Converts a `FoodSelectionPlan` and food candidates into structured `Meal` records and inline recipe definitions, with good-protein threshold logic. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/CustomIngredientUpsert.swift` | Resolves manual recipe ingredient inputs into `FoodItem` records, creating or updating custom food entries in the catalog. |
| `Fernlet/App/Fernlet/MealResolutionService.swift` | `MealResolutionService` + `MealPlausibility` — the quick-log meal-resolution cascade (cold whole-description probe → AI decomposition → AI selection → lexicon → plan → heuristic) plus the micronutrient fallback and implausible-total gate. |
| `Fernlet/App/Fernlet/WholeDescriptionFoodProbe.swift` | Cold pre-decomposition whole-description resolver probe: requires the corpus-calibrated high score floor plus a typed-unit `FoodPortion`, computes its grams, emits only a validated food-serving basis, and conservatively surfaces stripped brand/retailer text. |
| `Fernlet/App/Fernlet/FoundationDishDecomposition.swift` | `FoundationDishDecompositionModel` — on-device Foundation Models decomposition of a meal description into catalog-resolved, scaled `Meal` components with a match confidence. |
| `Fernlet/App/Fernlet/DishTemplateLexicon.swift` | `DishTemplateLexicon` + JSON template types — loads `DishTemplates.json` for deterministic, count-aware dish-template lookup (the decomposition fallback path). |
| `Fernlet/App/Fernlet/FoodCaptureRouter.swift` | `FoodCaptureRoute` + router — auto-detects a captured photo (barcode / nutrition label / meal) and routes to the matching existing flow, or `.ambiguous` for a gentle chooser. |
| `Fernlet/App/Fernlet/MealPhotoRecognizer.swift` | `MealPhotoRecognizer` (+ host protocol) — on-device Vision food-photo classification that composes a text description into the existing `resolveMeals` cascade, always pausing at review. |
| `Fernlet/App/Fernlet/MealPhotoPolaroid.swift` | `MealPhotoPolaroid` — polaroid-framed meal-photo tile with lazy sealed-byte load and `MealPhotoPresence` classification (here / on other device / unreadable). |
| `Fernlet/App/Fernlet/RecentBites.swift` | `RecentBite` + `MealPhotoPresence` — pure 7-day windowing behind Home's "Recent bites" photo strip (resolves which photographed meals to show without reading sealed bytes). |
| `Fernlet/App/Fernlet/BarcodeScanView.swift` | `BarcodeResolveFlowView` / `BarcodeScanView` — scan a product barcode and resolve it (user items → catalog → gentle not-found that creates a food item with the barcode remembered). |
| `Fernlet/App/Fernlet/BrandedCatalogResourceLoader.swift` | `BrandedCatalogResourceLoader` — loads the ~364k-product branded food DB (`FoodCatalogBranded.sqlite`) via bundle-first or On-Demand Resource and attaches it as a secondary `FoodCatalog` source. |
| `Fernlet/App/Fernlet/FoodProductWebImporter.swift` | `FoodProductWebImporter` / `FoodProductWebSearch` / `ImportedFoodProduct` — on-device Foundation Models import of a branded product's nutrition from a web product page. |
| `Fernlet/App/Fernlet/NutritionTargetsEditor.swift` | `NutritionTargetsEditor` — Settings card to edit macro targets (calories/protein/fat overridable, carbs shown as the live rebalancing residual). |
| `Fernlet/FernletKit/Sources/FernletScoring/Scoring.swift` | Health scoring, goal weights, meal parsing, workout planning, workout suggestions, and date helpers. |
| `Fernlet/FernletKit/Sources/FernletScoring/PeriodScoringSignals.swift` | Abstract period-scoring vocabulary (`PeriodSignalStrength`, `PeriodPhaseSignal`, `PeriodScoringAdjustment`) consumed by the scoring engine. |
| `Fernlet/FernletKit/Sources/AppServices/BarcodeScanner.swift` | Vision still-photo product-barcode detection (`BarcodePayloadDetecting` seam + `VisionBarcodeDetector`/`BarcodeScanner`) over EAN-13/EAN-8/UPC-E, the fallback for devices lacking live `DataScannerViewController`. |
| `Fernlet/FernletKit/Sources/AppServices/FoodImageClassifier.swift` | On-device meal-photo image classification behind the `FoodImageClassifying` seam (`VisionFoodImageClassifier` via `VNClassifyImageRequest`), returning raw taxonomy labels; the photo never leaves the device. |
| `Fernlet/FernletKit/Sources/WebScrapingKit/EphemeralWebSession.swift` | `EphemeralWebSession` — the single private-browsing `URLSession` (ephemeral + cookies/cache/credentials explicitly disabled) that both web importers fetch through. The per-setting rationale, including which knobs are redundant under `.ephemeral` and why they are set anyway, lives here (see [No-Tracking-Wall.md](No-Tracking-Wall.md) §2a). |
| `Fernlet/FernletKit/Sources/WebScrapingKit/HTMLScraper.swift` | `HTMLScraper` — shared regex capture reads, HTML-entity decoding (`decodingNumericEntities:` preserves both importers' differing policies), and the body-to-plain-text pass; returns `nil` on an empty page so each caller throws its own error. |
| `Fernlet/FernletKit/Sources/WebScrapingKit/JSONLDScraper.swift` | `JSONLDScraper` — `application/ld+json` script extraction, `@type` reading, and the recursive `@graph`/`itemListElement` search that finds a schema.org `Product` or `Recipe`. |
| `Fernlet/FernletKit/Sources/FoodCatalog/CuratedNutrientSources.swift` | Hand-authored "good source" foods per tracked micronutrient — the F2 gap-filling nudge's payload, pinned to real bundled-catalog ids so the card can bind the food and offer "add it". |
| `Fernlet/App/Fernlet/GroceryListComposer.swift` | F3 app-target glue between the recipe stores and the pure `GroceryAggregation` engine: catalog resolution the engine refuses to do, F4 "cook for N" scaling, and web-import lines through `DataExportBuilder.recipeIngredientLines`. The list is one-shot share text; only the weekly plan persists. |
| `Fernlet/App/Fernlet/GroceryPlannerView.swift` | The Phase A shopping-list builder: multi-select recipes with an optional per-recipe yield, preview the aggregated list, share as plain text. Nothing here persists. |
| `Fernlet/App/Fernlet/IngredientSubstitutionSheet.swift` | F4 substitution UI (§11.4): replace ONE structured ingredient. Applying forks a new recipe only on an explicit "Save as new recipe" tap — cancelling forks nothing and never mutates the source. |
| `Fernlet/App/Fernlet/CookingMode.swift` | `CookingModeAvailability` — the single rule deciding whether a recipe gets a "Cook" action (authored steps, structured ingredients, or a web import's free-text lines). UIKit-free. |
| `Fernlet/App/Fernlet/CookingLiveActivityController.swift` | App-target-only requester of the cooking Live Activity (starting one is the only thing solely the app process can do). Updates/ends go through the shared `CookingActivityBridge`. |
| `Fernlet/App/Fernlet/BarcodeServingStepView.swift` | The quick serving-count step after a barcode scan, plus its device-local last-serving memory keyed by normalized GTIN — a `UserDefaults` sidecar deliberately kept off the synced/sealed blob path. |
| `Fernlet/App/Fernlet/RecipeWebImageAttemptMemory.swift` | Device-local `UserDefaults` sidecar recording which recipes' one automatic web-image download THIS device already attempted — the per-device half of the “one attempt per device, suppression syncs” contract (the synced half is `RecipeWebImport.webImageSuppressed`). |
| `Fernlet/App/Fernlet/FoodSearchCorrectionMemory.swift` | Research §26 fix 1.10's local correction memory: a device-local `UserDefaults` sidecar (never synced between devices, capped at 200, oldest-out) pairing the text a user searched in "Adjust meal" with the food they picked, republished into `FoodCatalog` so the same search self-heals thereafter. |

## AI Context And Providers

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/AIContext/AIContextPayload.swift` | `AIContextPayload` marker protocol and concrete payload types (food-selection, meal-decomposition, etc.) defining the exact allowlisted fields that may enter each AI prompt. |
| `Fernlet/FernletKit/Sources/AIContext/MemoryAgent.swift` | Gate routing Tier-2 memory into AI prompts through a destination allowlist, recency filter, confidence filter, and diagnostic-language post-classifier, returning a char-capped context string. |
| `Fernlet/FernletKit/Sources/AIContext/AIAuditLog.swift` | `AIAuditEntry` metadata record plus the in-session `AIAuditLog` actor logging each AI call's payload kind, destination, included field names, and memory char count — metadata only, never persisted. |
| `Fernlet/FernletKit/Sources/AIProviders/FoundationWorkoutAdjustment.swift` | On-device workout adjuster: `WorkoutAdjustmentCandidateBuilder` builds the equipment/injury-filtered, ranked, capped candidate pool and `FoundationWorkoutAdjustmentModel` maps a natural-language request to substitutions within it via FoundationModels. |
| `Fernlet/FernletKit/Sources/AIContext/AICapabilityTier.swift` | The minimum capability an AI task declares, plus its ordered `escalationLadder`. The router picks the *cheapest available* destination meeting the tier, never above device capability or the user's ceiling (Ladder §3.1). |
| `Fernlet/FernletKit/Sources/AIContext/AIDeviceCapability.swift` | Snapshot of which AI rungs the *device* can physically reach, independent of user settings; obtained through the `AIDeviceCapabilityProviding` seam so tests can inject a fixed value. `.webNutritionLookup` always reports false by design (settings-gated web path, not an LLM rung). |
| `Fernlet/FernletKit/Sources/AIContext/AICallQuota.swift` | The daily AI-call budget with day-key rollover (Ladder §3.2). A pure value the *caller* stores device-locally — the derived `.sleepy`/`.resting` states must never reach synced `FernletSettings`, or one device's usage would throttle another. |
| `Fernlet/FernletKit/Sources/AIContext/FernletModelRouter.swift` | Capability-capped route resolution and its `AIRouteResolution`/fallback-reason vocabulary, letting deferred tasks tell a transient budget fallback from a persistent one before spending a retry. |
| `Fernlet/FernletKit/Sources/AIContext/FernletAIGate.swift` | The single routing entry point every AI call site funnels through — router + stored user intent + device-local quota in one value. `dispatch`/`resolveRoute` are the only places the daily quota is charged. |
| `Fernlet/FernletKit/Sources/AIProviders/SystemLanguageModelCapabilityProvider.swift` | Production `AIDeviceCapabilityProviding`: wraps `SystemLanguageModel.default.availability`. The PCC and BYOK rungs report false here — their symbols are iOS 27 APIs. |
| `Fernlet/FernletKit/Sources/AIProviders/FoundationIngredientSubstitution.swift` | On-device stage for F4 substitution: the model contributes world knowledge only (substitute food *names* plus an optional reason) — never a quantity, macro, or binding. Code rebinds each name through the local catalog. |
| `Fernlet/FernletKit/Sources/AIProviders/FoundationJournalSummary.swift` | On-device stage for the journal → Core Memory summary: `light` tier (never leaves the device), ambient (yields to the sleepy band), fed only the entry via `JournalSummaryPayload`; the reply must pass `JournalMemorySummaryPolicy` or it is audited as a fallback and dropped. No retry by design. |
| `Fernlet/App/Fernlet/JournalMemorySummarizer.swift` | `JournalMemorySummarizing`, the seam `FernletStore` asks for a journal memory's summary, and its production conformer over the walled stage; a test double stands in because the model never runs in the simulator. |
| `Fernlet/App/Fernlet/FernletAIComposition.swift` | Composition-root factory for the AI provider seam. Lives in its own file on purpose: naming `SystemLanguageModel…` matches the S3 grep-wall's AI-facing marker, keeping the type out of `FernletStore.swift`. |
| `Fernlet/App/Fernlet/AICallQuotaStore.swift` | The device-local, non-synced daily AI-call counter backed by `UserDefaults`. Deliberately app-target-only: the walled AI module reaches it solely through the `AICallQuotaStore` protocol declared in `AIContext`. |
| `Fernlet/App/Fernlet/FileAIAuditLogStore.swift` | Device-local, non-synced persistence sink for the AI audit log (Ladder §7.2) — one JSON file in Application Support, reached only through the `AIAuditLogPersisting` protocol. |

## Health And Launch Services

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/HealthKitGateway/HealthKitService.swift` | HealthKit service protocol and implementation, capability metadata, authorization snapshots, body/activity/cycle/mindfulness context, and authorization view model. Since 2026-09-23 also THE write gate: every write into Apple Health passes `requireWriteSharing(_:)` (master switch + that kind's switch) and reaches the store only through `save(_:)` or the `HealthKitStoreControlling.saveWorkout` seam, where the workout builder now lives. |
| `Fernlet/FernletKit/Sources/HealthKitGateway/WorkoutHealthKitSync.swift` | HealthKit workout import pipeline; reads `HKWorkout` samples and upserts matching `Workout` records via a `WorkoutSyncContext`. |
| `Fernlet/App/Fernlet/HealthSyncCoordinator.swift` | `HealthSyncCoordinator` (`HealthSyncContext`) — HealthKit ingestion (daily-context merge, derived sleep, workout import/backfill/observe) extracted from `FernletStore`. |
| `Fernlet/App/Fernlet/CoreDataHealthKitCacheCleaner.swift` | `CoreDataHealthKitCacheCleaner` — concrete `HealthKitCacheClearing` that scrubs HealthKit values an older build left in the synced rows / blob and empties the device-local HealthKit residue cache, fail-closed so opt-out never silently "succeeds"; its capture mode is the one-time launch scrub. |
| `Fernlet/App/Fernlet/DeviceHealthResidueStore.swift` | `FileDeviceHealthResidueStore` (the device-local, backup-excluded, bounded JSON cache of this device's HealthKit readings; one `production` instance) and `InMemoryDeviceHealthResidueStore` (the synchronous initializer's hermetic default). |
| `Fernlet/App/Fernlet/FernletStore+HealthBodyProfile.swift` | `FernletStore` extension: this device's HealthKit body-profile import (age, sex, height, weight) recorded device-locally instead of into the synced `settings.userProfile` (2026-09-23), `effectiveUserProfile`/`effectiveSettings`, the profile-screen edit split (only changed fields reach settings), the opt-out reload, and the `DeviceHealthBodyProfile(imported:)` bridge from the gateway type. |
| `Fernlet/App/Fernlet/FernletStore+HealthKitResidue.swift` | `FernletStore` extension: records today's HealthKit residue at the ingestion points, and the once-per-device launch scrub of HealthKit values out of rows older builds synced. |
| `Fernlet/App/Fernlet/WorkoutHealthAccessOffer.swift` | `WorkoutHealthAccessOffer` + `WorkoutHealthAccessOutcome` — the one contextual Apple Health ask at the first workout logged or started (onboarding's "Asked the first time you log a workout…"): mirrors "Give access" (master + workout switches, prompt, ledger), restores the switches on a decline, and never asks twice, after an explicit off in Settings, or when HealthKit would show no sheet. Its durable fact `fernlet.healthkit.workoutAccessOfferResolved` lives in the store's sensitive-surface suite and is cleared by "delete everything". |
| `Fernlet/App/Fernlet/HealthAccessGrant.swift` | `HealthAccessGrant.requestInContext` — asking for one kind of Health data where a feature first needs it (the period sheet and Cycle page) with the effect Settings' "Give access" has: the kind's Fernlet switch goes on, the prompt runs, the ask is ledgered, and a declined prompt puts the switch back. Never turns the master switch on. Exists because the gateway's write gate (2026-09-23) reads that switch. |
| `Fernlet/App/Fernlet/WorkoutTombstoneStore.swift` | `WorkoutTombstoneStore` — persisted capped FIFO ring of removed workout ids so a resurrected orphan HealthKit sample is deleted-and-skipped rather than re-imported. |
| `Fernlet/FernletKit/Sources/PrivateHealthStore/CyclePredictionEngine.swift` | Statistical cycle prediction engine using historical period data to forecast next period start, flow patterns, confidence level, and predicted flow days. |
| `Fernlet/App/Fernlet/LaunchPreparationService.swift` | Launch preparation state and photo wall seed loading. |
| `Fernlet/FernletKit/Sources/FernletFoundation/StartupTiming.swift` | `os_signpost`-based startup timing probes for measuring and instrumenting app launch phases. |
| `Fernlet/FernletKit/Sources/AppServices/NotificationService.swift` | Thin `UNUserNotificationCenter` wrapper for the opt-in gentle daily check-in local reminder: authorization, status check, and scheduling. |
| `Fernlet/FernletKit/Sources/AppServices/WeatherKitService.swift` | Opt-in coarse-location WeatherKit service producing tiny derived snapshots (`WeatherComfort` walk-friendliness, `AmbientSky` sky bucket) for mood-recovery prompts and Home ambience; degrades to nil, leaking no raw data. |
| `Fernlet/FernletKit/Sources/FernletFoundation/FernletDate.swift` | Layer-0 date helpers (`FernletDate`): canonical `yyyy-MM-dd` day-key formatting, enumeration, and display. |
| `Fernlet/FernletKit/Sources/FernletFoundation/MonotonicClock.swift` | `MonotonicClock` protocol plus sleep-counting `SystemMonotonicClock` (`mach_continuous_time`) for the store-ban clock. |
| `Fernlet/FernletKit/Sources/FernletFoundation/FernletAuditLog.swift` | Privacy audit-log facade (`FernletAuditLog`) emitting os_log events with a test-observable capture-handler registry. |
| `Fernlet/FernletKit/Sources/FernletFoundation/PersistenceFailureAudit.swift` | `PersistenceFailureAudit`: the one seam every ENVIRONMENTAL persistence failure records through — a Core Data fetch/save/delete, a file write/remove, or the payload encode that feeds one. Replaced the `assertionFailure`-in-`catch` those sites carried (a DEBUG trap on a locked-device write). Records the frozen dotted token plus the error's `NSError` domain and code only — never `localizedDescription` or `userInfo`. |
| `Fernlet/FernletKit/Sources/FernletFoundation/BackupExclusion.swift` | Layer-0 helper (`BackupExclusion`) toggling `isExcludedFromBackupKey` across a Core Data store file and its sidecars. |
| `Fernlet/FernletKit/Sources/FernletFoundation/FernletFoundation.swift` | Overview/placeholder source standing up the layer-0 `FernletFoundation` module. |

## Social And Mesh Networking

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshNetworkManager.swift` | Observable mesh network coordinator managing active/lightweight peer slots, proximity-join (15 cm commit gate), **the founding at the first commit** (P6 item 2: the descriptor, the membership ledger, the founder admission, the ceiling and the state machine, with `unwindNewbornMesh()`/`yieldsNewbornMesh(_:to:from:)` repairing the double mint, `reassertLocallyClosedMode()` keeping this user's own closed choice against any descriptor — a sticky local policy plus a monotonic `modeSetAt`, because LWW on wall clocks lost it both ways — `resumeSearchingForPartitionedMesh()` re-arming a mesh that outlived its links, `endSessionAfterDiscoveryTimeout()`, and `isSessionLive` carrying the session-end lifecycle that neither `isInSession` nor `hasCommittedPeer` can: session end means MESH end, so a link blip runs no part of the ceremony), session open/closed state, member removal proposals, admission requests, friend-of-friend vouchers, photo session metadata, encrypted mesh payloads, coordinator beacons, and key rotation. Its observation loop explicitly finishes cancelled streams so repeated Friends-tab discovery sessions do not retain suspended observers. |
| `Fernlet/App/Fernlet/Proximity/Feasibility/NetworkMeshFeasibilityProbe.swift` | DEBUG-only Bonjour/QUIC probe with a Simulator infrastructure lane and a physical-device peer-to-peer/background gate; verifies the signed TLS-channel binding. |
| `Fernlet/App/Fernlet/Proximity/Feasibility/MeshRejectionMatrixHarness.swift` | DEBUG-only launch hooks for the runbook's Lane C (two Simulators, the PRODUCTION mesh over QUIC): `MeshMatrixDebugOptions` reads `FERNLET_MESH_MATRIX*` once per process, and the harness seeds a memory-only `MeshDescriptor` roster, echoes this device's signing identity, and starts the radios so each named `MeshIntroductionRejection` can be observed on a real radio. Off when absent; compiled out in release. |
| `Fernlet/App/Fernlet/Proximity/Feasibility/MeshFlowDriver.swift` | DEBUG-only Lane C app-flow driver: `FERNLET_MESH_FLOWS=<csv>` commits each device's own slot at either proximity gate and then drives chat, photos and the clothing shop through the same public entry points the UI uses, echoing both sides' observations under `[mesh-flow]`. Off when absent; compiled out in release. |
| `Fernlet/App/Fernlet/DisposableCameraView.swift` | Active Friends-session disposable-camera UI: `CameraCaptureController` (`@Observable`, queued `AVCaptureSession` lifecycle, rotation handling, and arm/wind gate), `CameraPreviewView`, and the full-screen camera. The session-info sheet exposes the 10-shot film count, open/closed access, mesh rename, participant options, friend-of-friend labels, diagnostics, removal requests, and end-session flow; admission requests and mesh errors are presented as popups. |
| `Fernlet/App/Fernlet/JoinPromptSheet.swift` | `JoinPromptSheet` — the shared "someone wants to join" confirmation for both closed-group gatekeepers: mesh admission requests (presented by the disposable-camera session flow) and Group Activity join requests (presented by `ActivitiesView`). Generic over the request payload via display-name/fingerprint extractor closures; swipe-to-dismiss declines everything still pending (fail-closed). Replaced the former `MeshAdmissionPromptSheet.swift` and `ActivityJoinPromptSheet.swift`. |
| `Fernlet/App/Fernlet/FriendListView.swift` | Friend list screen for browsing trusted proximity peers with all/friends/blocked filters and block management. |
| `Fernlet/App/Fernlet/ActivitiesView.swift` | `ActivitiesView` — Group Activities screen (host form / nearby invites / hosting roster / joined states) off the Friends tab, driven by `ProximityActivityManager`. |
| `Fernlet/App/Fernlet/SessionChatPanel.swift` | `SessionChatPanel` — live-session chat panel over `MeshNetworkManager`: the memory-only transcript (cleared at session end) plus, since **P6 item 4**, an INLINE notice for a send that did not stage, shown at the TOP of the compose bar (above the text field, below the transcript — five docs said "beneath", corrected at item 4's fix review) with the draft kept. Reads `sendTempMessage(_:)`'s returned `MeshTextSendOutcome` rather than `routedShareRefusal`, whose one consumer is a `.alert` on the `DisposableCameraView` this panel is presented over. |
| `Fernlet/App/Fernlet/SafetyReportingView.swift` | `SafetyReportingView` — always-available report/block explainer and no-tolerance policy for shared shop items and people (App Store UGC compliance). |

## Proximity

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/ProximityKit/Engine/ProximityCoordinator.swift` | High-level coordinator for proximity sessions; orchestrates the per-peer transport channel its radio hands it (`PeerTransport`, never a framework type), the signed identity/ranging-token handshake, UWB startup, heartbeat RTT, payload dispatch, and inspector recording. |
| `Fernlet/FernletKit/Sources/ProximityKit/Engine/ObservationLoop.swift` | `ObservationLoop.start` — the shared `withObservationTracking` re-arm loop behind the proximity managers' coordinator-state observers (`MeshNetworkManager.startObserving`, `ProximityRecipeShareManager.startObserving`, `PresenceManager.startHeartObserving`), which each hand-rolled the same machinery until its leak-fix comment had drifted to only one copy. Holds the owner weakly and finishes the `AsyncStream` explicitly, so a cancelled discovery session leaves no suspended observer task behind. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/PeerHandle.swift` | Transport-neutral peer value (`PeerHandle`) and the transport's stable endpoint identity (`PeerEndpointKey`), whose `isSameEndpoint(as:)` is the sanctioned "same device?" test. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/PeerTransport.swift` | Framework-free transport protocol and its value family: `PeerTransport`, `PeerTransportState`, `PeerPendingInvite`, `InboundPeerFrame`, `PeerTransportError`, `PeerDeliveryMode`, plus the reserved trainer-service constant. |
| `Fernlet/FernletKit/Sources/ProximityKit/Engine/ProximityCommitDetector.swift` | Rolling distance-window detector used by proximity commit and trainer tap gates. |
| `Fernlet/FernletKit/Sources/ProximityKit/Ranging/RangingProvider.swift` | Ranging provider contract plus shared distance and state models. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/MeshTransportSelection.swift` | The friend mesh's radio seam: `MeshPeerChannel` (a per-peer channel that knows its peer — `NetworkPeerChannel` and `DetachedPeerChannel` conform), `DetachedPeerChannel` (the radio-less channel the manager's test seams use), and the `MeshTransportSession` conformance of `NetworkMeshSession` — **the only radio a shipping or a test build constructs since the deletion round (2026-09-22)**, which took `MeshTransportKind`, `MeshTransportFactory`, the DEBUG `FERNLET_MESH_TRANSPORT` bisect door and the MultipeerConnectivity conformance out with `MeshMultipeerSession.swift`. There is no selection left to make: `MeshNetworkManager.init`'s default is `NetworkMeshSession()` directly, pinned by `MeshTransportSelectionTests.theAppsInitializerRunsOnTheQUICRadio` and `MeshP9McRetirementAcceptanceTests`. The seam protocol stays because the suite injects a fake through it. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/NetworkMeshSession.swift` | **The friend mesh's radio** since the cutover: the Network.framework/QUIC surface on `_fernlet-mesh2._udp` — listener, Bonjour browse, the signed channel introduction and its roster verdicts (including D-4.3's provisional stranger admission), the per-peer `NetworkPeerChannel` tunnels, the heartbeat, the re-propose sweep and the eviction-cause seam. Its decisions are factored out so tier 1 can enumerate them with no radio and no wall clock (`MeshLinkTable`, `MeshHeartbeatSchedule`, `MeshLinkAdvertisement`, `MeshSessionIdentityMap`, `NetworkMeshWire`, `MeshTransferStreamTable`, `EphemeralMeshTLSIdentity`); `Tests/FernletTests/NetworkMeshTransportTests.swift` is the battery and the runbook's device lanes cover the actor. The browse transcript logs its COUNT publicly and peer instance NAMES privately (plan §8.7 finding 1, taken at the cutover). |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/NetworkPresenceSession.swift` | **P9 item 2 pass 2** (plan §17.1): the presence radio's Network.framework/QUIC surface on `_fernlet-near2._udp` — one listener registered under the current `PresenceEpochPosture`'s instance name and presenting its TLS identity, one browser, and up to four short-lived pairwise tunnels multiplexed into `NetworkPeerChannel`s. A separate, much smaller session than `NetworkMeshSession` on purpose (the posture rotates here and must never rotate there; there is no signed channel introduction, because `ProximityCoordinator`'s SEALED introduction is the presence handshake; there is no roster, dial budget, heartbeat or transfer stream) while reusing its building blocks by symbol: `ProximityQUICParameters`, `NetworkMeshWire`, `MeshSessionIdentityMap`, `MeshLinkKey`, `NetworkPeerChannel`. Owns **no timer**, so presence keeps its one-timer rule. An inbound dialer is resolved by the `PresenceDialHello` it writes first and refused unless the owner's `resolveDialer` answers with an already-browsed, tag-matched peer. Audits `presence.quic.advertised` / `.rotated` / `.sighted` / `.connected` / `.redundantTunnelClosed` / `.stopped`. Pinned by `PresenceOverQUICTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/NetworkRecipeShareSession.swift` | **P9 item 3 pass 2** (plan §17.1): the recipe-share radio's Network.framework/QUIC surface on `_fernlet-recipe2._udp` (ALPN `fernlet-recipe-v1`) — one listener registered under a `RecipeSharePosture` minted fresh at every `start()` and every resume (random instance name + `EphemeralMeshTLSIdentity` + `sid`, no rotation timer), one browser, and the ONE pairwise tunnel the hard 2-device cap allows, multiplexed into a `NetworkPeerChannel`. `RecipeShareRadioSession` is the seam `ProximityRecipeShareManager` drives it through. What it adds that the presence radio deliberately has none of is a **per-transfer-stream acceptor**: a recipe crosses as ONE sealed frame, and a recipe carrying a picture clears `MeshTransferStreamTable.bulkFloorBytes` and earns a stream of its own (ack byte, bounded budget, refused for any connection this radio never admitted) while a text recipe stays in order on the control stream. `pauseDiscovery()` stands the listener AND the browser down for as long as a pairing is held — the shipped "recipe sharing closes once two devices connect" — and `stop()` clears the pause flag, which is what makes `RecipeShareDiscoveryGate`'s three `.unchanged` rows true. An inbound dialer is resolved by the `RecipeShareDialHello` it writes first and refused unless it is not our own `sid`, the owner's `resolveDialer` names a browsed peer, and the owner's `shouldAcceptDialer` admits it. Every `report` is a START failure or a *listener* failure; a dial miss (including a peer whose identity aged out), a refused hello and a failed transfer are audited and nothing more, and a BROWSER failure is stood down and audited (`recipe.quic.browserFailed`) but reaches the owner's door only when nothing is held and nothing is mid-connect. A withdrawn Bonjour registration REPUBLISHES under a fresh posture (`recipe.quic.registrationWithdrawn`) rather than leaving the radio dark with `isListening` still true — P8 item 0's device finding (b). The `PeerHandle` it mints carries NO `displayHint`: the only tokens it holds for a peer are the random instance name and the endpoint key containing it, so the name a user reads comes from the TXT `name` or from the picker's existing placeholder. Audits `recipe.quic.advertised` / `.paused` / `.resumed` / `.sighted` / `.connected` / `.dialRefused` / `.helloRefused` / `.redundantTunnelClosed` / `.transferStream` / `.transferStreamRefused` / `.transferStreamFailed` / `.browserFailed` / `.registrationWithdrawn` / `.stopped`, every peer named by an opaque salted session label. Pinned by `RecipeShareOverQUICTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Transport/MeshTransferStreamTable.swift` | The per-transfer stream policy for the QUIC radio: `MeshTransferRoute` (control stream vs a stream of its own), the 64 KiB bulk floor, the per-direction concurrency budgets, and the one-byte transfer ack. Pure and clock-free, so the whole policy is settled at tier 1. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshSessionTypes.swift` | Mesh session slot, group-key, ranking-window sample, participant, and encryption-error support models extracted from the manager. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshKeyAgreementAdvertisement.swift` | P6 item 1 (plan §11.3 item 13(ii)): `SignedKeyAgreementAdvertisement` (a member's own signed key-agreement public key, `fernlet.mesh.key-agreement.v1`, self-signed and with `meshID` bound into the transcript), `MeshKeyAgreementAdvertisementSet` (bounded at the admission cap, grow-only, and **conflict-refusing** — deliberately NOT a `MeshMembershipRecordSet`, whose earliest-wins merge would silently pick one of two keys), `MeshKeyAdvertisementFoldOutcome` / `MeshKeyAdvertisementFoldResult` / `MeshKeyAdvertisementFold` (the one pure fold door: the idempotence pre-filter, then verification, then the three-way decision) the `@MainActor` `signed(...)` mint, `MeshKeyAdvertisementFold.restoring(_:verifiedBy:)` (the restore door: every persisted row re-proved against the ledger adoption proved, drops named), `MeshKeyAgreementPayload` (the additive unsigned wire frame, a batch clamped to the set's capacity on both doors) `MeshKeyAdvertisementReceiveBounds` (the per-sender frame bound the membership family otherwise has none of — `capacity × transitionsPerMember`, recomputed by the second fix review once the conflict-mark release added a third per-member transition), the park's own four types (`MeshKeyAdvertisementPark`, `MeshParkedKeyAdvertisement`, `MeshParkedKeyAdvertisementOffer`, `MeshKeyAdvertisementParkOutcome`) under `MeshKeyAdvertisementParkBounds` — the per-SENDER side container for rows refused `signerNotAdmitted`, which a flat member-keyed map let one misbehaving member fill with a single frame — and `MeshKeyAdvertisementSendBounds` (`selfMintAttemptsPerSession` — the bound on the sender's repair of its own row; unbounded, it tripled a full test run). `MeshKeyAgreementAdvertisementSet.clearingConflicts(outside:)` is the bounded relief for the conflict mark's blast radius: a mark is dropped once the derived roster no longer names its member. **Addressing, not membership**: it verifies against `ledger.admissions` and can never create, end or revoke a membership, and it is not a fifth `MeshMembershipRecordKind` — so the membership digest's golden and the pinned re-gossip budget cannot move. Persisted inside `MeshSessionContext` at schema 3. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedManifest.swift` | P5 item 1 (plan §11): `MeshRoutedManifestFormat`, `MeshRecipientKeyWrap`, `MeshRoutedManifest` (the origin-signed routed-item record with its immutable destination set and per-recipient wraps, clamped on both doors), `MeshRoutedManifestPayload` (the `fernlet.mesh.routed-manifest.v1` frame), `MeshRoutedManifestMintError`, and the `@MainActor` `signed(...)` mint over a `MeshDeliveryTarget`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedManifestVerifier.swift` | P5 item 1: `MeshRoutedManifestVerifier` — the one receive-side door (mesh, shape, accepted type token, origin key from the ADMISSION ledger, origin not in the REMOVAL set, signature, wraps ≡ destinations, distinct set, expiry equality) — and its frozen `MeshRoutedManifestRejection` tokens. Public material only; departures never consulted (a departed origin verifies), quorum removals refused by name, no destination lookup. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedContentKeyWrapper.swift` | P5 item 1: `MeshRoutedContentKeyWrapper` — the per-recipient content-key wrap (ephemeral X25519 → HKDF → AES-GCM with the manifest binding and recipient in the AAD) and its inverse via a static-agreement closure, plus `MeshRoutedWrapBinding` and `MeshRoutedKeyWrapError`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshChunk.swift` | P5 item 2 (plan §11): `MeshChunkFormat` (256 KiB payload cap, the 1024-chunk cap derived from the 256 MiB content cap, the in-flight headroom constant, and `chunkCount(forSize:)`), `MeshRoutedContentDigest` (the item hash, the per-chunk hash and the derived chunk id, each under its own registered `Hash` purpose), `MeshChunk` (the origin-signed slice whose payload is EXCLUDED from the signed transcript and bound through `chunkHash`), and `MeshChunkPayload` (the `fernlet.mesh.routed-chunk.v1` frame, signed and unsealed). |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshChunkVerifier.swift` | P5 item 2: `MeshChunkVerifier` — the receive-side door (mesh, shape, origin key from the ADMISSION ledger, origin not in the REMOVAL set, signature, chunk hash AFTER the signature, expiry equality, then the manifest cross-checks) — and its frozen `MeshChunkRejection` tokens. Public material only, so it verifies on a locked device; the manifest is OPTIONAL, because a chunk may arrive before it. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshChunker.swift` | P5 item 2: `MeshChunker` — the origin's mint (`chunk(of:at:for:identity:)` is the primitive so item 6 can stream at `blob + one chunk`; `chunks(of:for:identity:)` is the bounded run) and `MeshChunkMintError`. Refuses to mint for a manifest this device did not originate; the blob is opaque and no key is ever handled here. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshChunkAssembly.swift` | P5 item 2: `MeshChunkAssembly` — the order-independent reassembler with `MeshChunkRefusal`, `MeshChunkAdmission`, `MeshChunkBinding` and `MeshChunkCompletion`. Every input returns a verdict, a retransmission is a no-op, completion is impossible while unbound, and `.complete(blob:)` is the whole-item hash check — necessary but never sufficient for item 3's custody receipt, which needs durability first. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshChunkAdmissionRule.swift` | P5 item 3 (C13): `MeshChunkDescriptor`, `MeshChunkSetShape` and `MeshChunkAdmissionRule` — the per-chunk verdict and the manifest-binding verdict, extracted so the in-memory reassembler and the durable routed store reach each through ONE pure function. Item 2's assembly tests pass unmodified beside the extraction. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshContentMerge.swift` | P4 item 7 (plan §10.3): content merge — ordering and dedup on ingestion. `MeshContentKey`, `MeshMergeableContent`, `MeshContentOrder`, `MeshContentSet`, the three projections (`MeshMergedPhoto` / `MeshMergedMessage` / `MeshMergedHeart`) and `MeshContentLedger`. Pure values with a pure union, so reconnect, post-partition merge and reload-after-death are one code path; nothing here is persisted and nothing is wire. Since **P6 item 4's fix review** the union keys on `MeshContentKey` = `(author, id)` rather than the id alone, for every kind — a routed id is its origin's own choice and its signed manifest publishes it to the whole roster before the content arrives, so an id-only key let one admitted member spend another's dedup slot. §10.3's order is unchanged. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedItemSeal.swift` | P5 item 13: `MeshRoutedItemSealFormat`, `MeshRoutedItemSealError` and `MeshRoutedItemSealer` — the routed ITEM seal (AES-256-GCM under `AEAD.meshRoutedItemV1`, keyed by the manifest's single-use content key, with mesh/item/origin/type token authenticated) and its inverse. The `FMRI1` marker rides in the clear so a foreign or retired blob is refused BY NAME; the seal bound and the open bound are one derived number, and since **P6 item 3** that number is a FORMULA — `PrivateMediaStore.maxIncomingPhotoBytes` + the body's framed-header allowance + the seal overhead — which is also the registry's photo-row cap, so the seam bound and the per-type cap cannot drift. Pure — no actor, clock, I/O or identity; the one `PrivateMediaStore` import reaches a single `Int`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedItemBody.swift` | P5 item 13 + **P6 items 4 and 6**: `MeshRoutedItemBodyFormat` and THREE body families — `MeshRoutedPhotoHeader`/`MeshRoutedPhotoBody`, `MeshRoutedTextHeader`/`MeshRoutedTextBody`, and `MeshRoutedHeartHeader`/`MeshRoutedHeartBody` (item 6: header-only, so its cap formula has a ZERO payload term; FOUR hostile shapes, the fourth being a non-empty remainder, because a heart body ends at its header and unread bytes inside an authenticated blob are a malleability seam; plus a day-key shape check and the same 128-byte name bound refused on the wire) — the sealed plaintext of a routed photo, framed as a length-prefixed JSON header plus the image RAW (never base64, which would inflate every byte bound in the phase by a third). **P6 item 3** adds the header allowance (`maxHeaderJSONByteCount` / `maxFramedHeaderByteCount`) a routed type's ciphertext cap reserves — an allowance the cap formula reads, never a new refusal. The text body has FIVE hostile shapes on the one `malformed` token since item 4's fix review: the fifth is a decoded `senderName` above the 128-byte bound, refused on the WIRE and not only bounded at the sender, because the allowance's whole arithmetic is over that number. No `keyEpoch`, no sender-supplied identity. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedOrigination.swift` | P5 item 13: `MeshRoutedShareSkip`, `MeshRoutedShareRefusal` (public, `CaseIterable` since the P5 review's finding 5) and `MeshRoutedOriginationOutcome` — what the routed SENDER door answers. "Nobody to send to" is a silent skip; "the mint was attempted and failed" is a visible refusal, published as a frozen token on `MeshNetworkManager.routedShareRefusal` and forked into localized copy by the app's `RoutedShareRefusalCopy`. Frozen English tokens, never user copy. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedRefusalBudget.swift` | The P5 review's finding 3 (closing D-12.14): `MeshRoutedRefusalBudget`, the per-**sender** budget for routed content frames refused BEFORE any store verb — a verifier rejection, `notADestinationOrHandoff`, `unknownItemNotFromOrigin`, `unregisteredTypeChunk`, an undecodable frame — which the replay window cannot record. Counts refusals (never frames) per authenticated envelope sender, capped at `MeshRoutedDrainBounds.sessionFramesPerPeer` (an honest peer cannot reach it: that is already the most frames the drain lets it make this device serve; frames for an item this device already capacity-refused from that sender are exempt, or a courier facing a full receiver would be), bounded on both axes (sender axis = the admission set's 16), fail-closed on a full sender axis, reset with the drain state only. A spent sender's content is dropped at `dispatchRoutedContent` unread after one `mesh.routedDrain.refusalBudgetSpent` line. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedItemDelivery.swift` | P5 item 13: `MeshRoutedDeliveryError`, `MeshRoutedOriginQuotaKey` and `MeshRoutedItemDelivery` — the ONE routed plaintext seam (unwrap the content key, open the item blob, decode the body), in its own file because the locked-device walls pin the unwrap and open to one home and require it to GUARD on `mayDecryptRoutedContent`. Takes the predicate as a parameter and refuses on it first. **P6 items 4 and 6** add `openTextBody` and `openHeartBody` beside `openPhotoBody` and funnel all three through one private `openPlaintext` whose first line is the guard — structure rather than a wall, because W2's containment half is a whole-FILE `contains` and cannot see a second entry point skipping it. Also holds `MeshRoutedProjectionVerdict` (handed on / refused for good / refused for now — the retry-list decision) and `MeshTranscriptLiveness`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedRetryPlan.swift` | P6 item 5 (D-13.32): the re-entry pass's allowance discipline, as values — `MeshRoutedRetryList` (the two retry lists as frozen audit tokens), `MeshRoutedRetryPlan` (pure, `nonisolated`, deterministic: never-attempted keys first, the retry share capped at `maxItems / retryShareDivisor`, unused slots spilling either way, the share ordered by a round-robin queue so its own tail cannot starve) and `MeshRoutedRetryRotation` (the memory-only per-list tried set + queue + session cut, bounded by `MeshRoutedStoreFormat.maxItems`, cleared with the drain state, and **pruned to the pass's own enumeration** by `retain(_:)` since item 6's run — a key that left its list by some other door costs no slot but does fill the 1024 bound, and at the bound the pacing silently reverts to the head-of-list prefix D-13.32 exists to stop). Its session cut is armed with a **floored** instant, because the record's `firstSeenAt` is floored at every admission door and comparing the two unfloored charged a genuinely new item to the retry share. It names no routed type token, no registry, no store and no manifest — deliberately, and walled by `MeshRoutedRetryPlanTests.thePlannerNamesNoRoutedTypeNoRegistryAndNoStore`: the per-type opinion belongs to the manager's filter-before-plan seam (`isProjectableAtThisPass`, `ackableNow`), which is where item 6 adds the heart predicate. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshCustodyReceipt.swift` | P5 item 3: `MeshCustodyReceiptFormat`, `MeshCustodyReceipt`, `MeshCustodyReceiptPayload`, `MeshCustodyReceiptMintError` and the mint — a custodian's signed statement that it durably holds one item's COMPLETE ciphertext, signed by the custodian ABOUT the origin's item. The mint takes a `MeshCustodyDurabilityWitness`, so the forbidden order (receipt first, durability later) is unwritable. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshCustodyReceiptVerifier.swift` | P5 item 3: `MeshCustodyReceiptRejection` + `MeshCustodyReceiptVerifier` — the receive-side door, resolving the key by the CUSTODIAN fingerprint from the admission ledger. D14: a departed custodian's receipt still verifies, a quorum-removed one's does not. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedStoreKey.swift` | P5 item 3: `MeshRoutedStorageScope` (directory + its OWN `com.fernlet.mesh-routed` keychain service), `MeshRoutedSealKeyOutcome` and `MeshRoutedSealKey` — the key row that seals the routed index and every chunk file, minted only on a definitive absence and read-back-verified. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedIndex.swift` | P5 item 3: the sealed catalogue's value types — `MeshRoutedIndexSchema`, `MeshRoutedStoreFormat`, `MeshRoutedIndexDecodingError`, `MeshRoutedItemKey` (the SIGNED PAIR, D11), `MeshRoutedChunkDescriptor`, `MeshRoutedDeliveryProgress`/`Record`, `MeshRoutedItemRecord`, `MeshRoutedItemRef` and `MeshRoutedIndex`. Caps are refused at rest, never clamped. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedStore.swift` | P5 item 3: the sealed sidecar's floor — `MeshRoutedSealRefusal`/`Deferral`/`Corruption`, the five-state `MeshRoutedLoad`, `MeshRoutedSaveError`, `MeshRoutedChunkFileRead` and `MeshRoutedStore` (with the `fileprivate`-minted `LoadToken`). The only file in the routed store that names `ColumnCrypto`; the store has no decrypt door at all. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedCustody.swift` | P5 item 3: the custody verbs — `admittingManifest`, `stagingChunk`, `recordingCustodyTransfer`, the two forwarding doors, the two sweeps and `dropping` — plus `MeshRoutedUnavailability`, `MeshRoutedStoreRefusal`, `MeshRoutedOutcome`, `MeshRoutedRetryBounds`, `MeshRoutedManifestAdmission`, `MeshRoutedCustodyOutcome` and `MeshRoutedSweepReport`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedCustodyCommit.swift` | P5 item 3: `MeshCustodyDurabilityWitness` (`fileprivate` init) and `MeshRoutedStore.committingCustody(item:custodian:now:)` — **one type, one function, nothing else**, so the witness has exactly one construction site and this file cannot mint a write token either. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedAck.swift` | P5 item 4: the ack-stage vocabulary as VALUES — `MeshRoutedAckStage` (plan §11's three clauses), `MeshRoutedTypeToken` (the frozen `routed-type.*` spellings, control reserved-not-registered), `MeshRoutedAckStageRow`/`MeshRoutedAckStageTable` (+ `.increment1`, the one table shipping code names — since P5 item 11 a **projection**, `MeshRoutedTypeRegistry.increment1.ackStages`, so the accepted-token set and the stage column cannot drift), `MeshRoutedHeartAck`, `MeshRoutedAckEvidence`, `MeshRoutedAckShortfall` and `MeshRoutedDeliveryCommitOutcome`. Still the one source of the frozen token SPELLINGS, and still no store extension and no decrypt. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRecipientReceipt.swift` | P5 item 4: `MeshRecipientReceiptFormat`, `MeshRecipientReceipt`, `MeshRecipientReceiptPayload`, `MeshRecipientReceiptMintError` and the mint — a DESTINATION's signed statement that one routed item reached it, finally, signed by the recipient ABOUT the origin's item. The mint takes a `MeshRecipientDeliveryWitness`, so a receipt for an acknowledgement no durable write returned is unwritable. The ack stage is never a field. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRecipientReceiptVerifier.swift` | P5 item 4: `MeshRecipientReceiptRejection` + `MeshRecipientReceiptVerifier` — the receive-side door, resolving the key by the RECIPIENT fingerprint from the admission ledger, with the one leg custody has no analogue for (`notADestination`). D14: a departed recipient's receipt still verifies, a quorum-removed one's does not. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDeliveryCommit.swift` | P5 item 4: `MeshRecipientDeliveryWitness` (`fileprivate` init) and `MeshRoutedStore.committingDelivery(item:recipient:stages:evidence:now:)` — **one type, one function, nothing else**. It writes only the record-level ack instant and advances NO delivery rung, which is what makes every `delivered` receipt-backed. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDeliveryIngest.swift` | P5 item 4: `MeshRoutedStore.recordingRecipientReceipt(item:receipt:now:)` — the **only** writer of a `delivered` rung, and it advances only the receipt's own signer — plus `forwardableRecipientReceipts(item:)`, which hands on the stored bytes verbatim (this device's own receipt included, because a heart's ledger judgement cannot be repeated). |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedInventory.swift` | P5 item 5: the ROUTED CONTENT digest — `MeshRoutedInventoryFormat` (the caps, including the two DIFFERENT signer caps), `MeshRoutedInventoryEntry` (the signed pair, the parked flag, the chunk count, the exact held-chunk bitmap and its frozen bit order, the two signer index lists), `MeshRoutedInventory` (the minimal member table, `isWithinCaps` → `.overCapacity` and the five `isWellFormed` clauses → `.malformed`, refused and never repaired), `MeshRoutedInventoryPayload` (+ its own shape check and `@MainActor signed(...)`, which takes NO `selfFingerprint` — the signing identity is the sole spelling of "this device") and `MeshRoutedInventoryMintError`. NOT `MeshInventoryDigest` — that is P4's membership summary under a different token. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedInventoryBuilder.swift` | P5 item 5: `MeshRoutedInventory.init?(meshID:index:selfFingerprint:at:)` — deriving the digest from a LOADED index, read-only and clock-injected, with the custody self-rule (this device appears iff it holds custody of somebody ELSE's item) and the one filter (`isLive(at:)`, so parked, delivered and unrestorable-delivery items are all advertised). Bounds the at-rest `chunkCount` before it becomes a bitmap allocation size — an out-of-range count yields an empty map, so the built digest refuses its own shape check rather than sizing a `Data` off a planted `UInt32`. Adds no store verb and no index enumerator. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedInventoryVerifier.swift` | P5 item 5: `MeshRoutedInventoryRejection` + `MeshRoutedInventoryVerifier` — the receive-side door, resolving the key by the ADVERTISER's fingerprint from the admission ledger, caps and shape first so an over-cap digest costs no signature verify. D14: a departed advertiser's digest still verifies, a quorum-removed one's does not. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedInventoryDelta.swift` | P5 item 5: the pure comparison — `MeshRoutedInventoryReceiptKind`, `MeshRoutedInventoryReceiptRef` (signers as resolved FINGERPRINTS, never indices), `MeshRoutedChunkGap` (exact missing indices) and `MeshRoutedInventoryDelta` with `between(local:remote:offerableToPeer:)` (nil for a foreign-mesh pair, never an empty delta), `isQuiescent` (strictly LOCAL), `converged(local:peerReportsQuiescent:)` (item 7's predicate) and the `ask`/`offer` rollups, which are DISTINCT keys in canonical order rather than the two lists concatenated. All six drain rules live here so item 6 implements no policy of its own. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDrainAnswer.swift` | P5 item 6: the drain's quiescence frame — `MeshRoutedDrainAnswerFormat`, `MeshRoutedDrainAnswer` (mesh, WHOSE advertisement, WHICH one, the single bit), `MeshRoutedDrainAnswerPayload` (+ its own shape check and `@MainActor signed(...)`, which takes no `selfFingerprint`) and `MeshRoutedDrainAnswerMintError`. Token `fernlet.mesh.routed-drain-answer.v1` — its own stem, because the frame states the result of a COMPARISON rather than what a disk holds. Signed, because the bit is a **recorded fact** rather than a gate: item 7's window closes on the membership digest (D-7.11), while this bit is stored per peer, counted onto the merge's audit line and is item 9's capacity input. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDrainAnswerVerifier.swift` | P5 item 6: `MeshRoutedDrainAnswerRejection` + `MeshRoutedDrainAnswerVerifier` — mesh, shape, admitted key by the payload's own fingerprint, quorum-removal, key agreement, signature. D14: a departed answerer still verifies. The two BINDING checks (this device is the advertiser; the instant is one it really advertised) live at the manager, which is the only place that knows what was advertised. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshMergeWindow.swift` | P5 item 7: the merge exchange in flight, as an explicit value — `MeshMergeWindow` (three bounded per-peer sets **asked**/**answered**/**matched**, the window's own evidence map, `openedAt` recorded and never compared, and the proof budget), plus `MeshMergeWindowRole`, `MeshMergeWindowClosure` and `MeshMergeWindowVerdict`. The closing law is `pending = (asked ∪ answered) ∩ reachable ∖ matched`; "answered" sits inside `pending`, so answering never closes anything; a peer's later mismatching digest un-matches it, and so does the late re-ask a re-committing peer gets (`reAsking(_:)`, D-7.32) — the opening `asking(_:)` un-matches nobody. Pure, no clock, no I/O — the manager only calls transitions. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDrainPlan.swift` | P5 item 6: the drain's pure half — `MeshRoutedPeerInventory` (per-peer state, BOTH quiescence halves), `MeshRoutedDrainRefusalNote` (item 9's visible-failure seam), `MeshRoutedDrainBounds` (64 chunks per answer = 16 MiB, and a per-peer SESSION budget sized for exactly one maximal item), `MeshRoutedDrainChunkSend` and `MeshRoutedDrainPlan`, which re-decides no policy: it filters by the peer's refused set, truncates to the bounds and to the remaining allowance, and keeps the delta's canonical order. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshCustodyHandoffPlan.swift` | P5 item 8 (plan §10.6, §11): custody-transfer-on-departure as pure values — `MeshCustodyHandoffScope` (the run-scoped entitlement a live development opens), `MeshCustodyHandoffSuppression`, `MeshCustodyHandoffResult` (what actually transferred, what could not be placed, what the push offered) and `MeshCustodyHandoffPlan` itself: which items go to which custodian over which legs, plus the custodian-side claim planner and the push batch. No store, no clock, no isolation; every bound is an existing constant. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedCustodyHandoff.swift` | P5 item 8: the two BATCH custody doors — `recordingCustodyHandoff(_:now:)` (the departing origin, receipt-backed) and `claimingHandedOffLegs(_:now:)` (the custodian, receipt-free) — each one load, N bounded updates and ONE save, plus `MeshRoutedCustodyHandoff`, `MeshRoutedHandoffClaim`, `MeshRoutedHandoffRefusalReason` (the store's vocabulary or the delivery ladder's, so a refusal keeps the name the door that raised it gave it), `MeshRoutedHandoffRefusal`, `MeshRoutedHandoffStep` and `MeshRoutedHandoffReport`. Applies the single-item transfer door's rules through its own helpers, so the rule has one implementation. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedCapacity.swift` | P5 item 9 (backpressure): the routed store's caps as ONE injectable value — `MeshRoutedCapacity` (`.production` defined **as** `MeshRoutedStoreFormat`, so no number is written twice; injected via `MeshRoutedStore.init(scope:capacity:)` and read back off the store by everything that accounts), `MeshRoutedCapacityUsage` (the accounting rule stated once — items with parked ones included, staged bytes, files, the over-commit `uncompletableItemCount`, `unrestorableItemCount`, and `hasRoomToAdmit`, the release predicate for a `.storeFull` hold) and `MeshRoutedParkedDrop`, the ONE-clause terminal-refusal rule: `unknownTypeToken` **and** the sender is the set's own claimed origin. Every other manifest rejection keeps the bytes. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedDeliveryHold.swift` | P5 item 9: the bounded observable behind the user-visible backpressure failure — `MeshRoutedDeliveryHoldCause` (frozen `storeFull` / `notPlaced`; `deferred`, seal-`refused` and `corrupt` are three other answers and set NO hold) and `MeshRoutedDeliveryHold` (cause + count + instant). Counts and tokens only: no display text crosses the module boundary, and no item id reaches the visible surface. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedAccessGate.swift` | P5 item 10 (locked-device handling, plan §11 + §19.5): item 10's whole pure vocabulary — `MeshRoutedAccessGate` (the three app-level facts that decide whether PLAINTEXT may exist right now: iOS data protection, the app scene, and the app lock's duress session), `MeshRoutedCapability` (`sealCustody` is answered `true` **deliberately** — custody's authority is the store's five states), `MeshRoutedAccessEdge` (the two ciphertext legs rise, duress falls), `MeshRoutedReentryReport` (counts and legs only) and the two frozen token enums the store's suppression lines are named with, `MeshRoutedIndexReadReason` and `MeshRoutedSweepVerb`. No store, no clock, no UIKit, no decrypt seam. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedTypeRegistry.swift` | P5 item 11 (plan §11's "declared at registration"): the routed type-token registry as ONE value — `MeshRoutedTypeEntry` (the five §11 columns plus the derived foreground-decrypt requirement and the declared canonical-store slot), the four frozen column enums `MeshRoutedDestinationSemantics` / `MeshRoutedRelayRetention` / `MeshRoutedExpiryRule` / `MeshRoutedCanonicalStore`, and `MeshRoutedTypeRegistry` itself (+ `.increment1`, the one registry shipping code names). `entry(for:) == nil` **is** "unknown", the one answer at every door; `init(entries:)` DROPS a row declaring increment 2's relay-retention or a cap outside the wire bound, so an unimplemented policy is unreachable by construction rather than by comment. **P6 item 3** narrowed the first cap — the photo row now declares `MeshRoutedItemSealFormat.maxResidentBlobByteCount`, which is what makes the manifest door's per-type check reachable. Deliberately holds no gate vocabulary, no store, no clock, no persistence and no raw token literal — the spellings stay in `MeshRoutedAck.swift`, one source. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshRoutedContentHasher.swift` | P5 item 3: `MeshRoutedContentHasher` — the streaming sibling of `MeshRoutedContentDigest.contentHash(of:)`, so a 256 MiB item's whole-item hash is measured one chunk file at a time under the same domain. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshNameGenerator.swift` | Generates random human-readable two-word mesh names from adjective/noun word lists. |
| `Fernlet/FernletKit/Sources/ProximityKit/Ranging/NIRangingSession.swift` | NearbyInteraction UWB ranging session for measuring real-time distance and direction between Fernlet devices. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/FernletIdentityEnvelope.swift` | Ed25519-signed wire envelope for all peer-to-peer transfers, including canonical JSON encoding and verification. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/PayloadType.swift` | Peer-to-peer payload classification, envelope encryption mode, and payload summary models. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/MeshPayloads.swift` | Mesh descriptor, admission, removal, voucher, group-encryption, and coordinator-beacon wire models plus admission-token signing and verification. |
| `Fernlet/FernletKit/Sources/FernletDomainModel/FriendPhotoPayloads.swift` | Friend-photo payload, session metadata, manifest, and missing-photo request wire models. |
| `Fernlet/FernletKit/Sources/ProximityKit/Identity/IdentityService.swift` | Per-device Ed25519 signing identity and X25519 key-agreement, with keys stored in Keychain under `AfterFirstUnlockThisDeviceOnly`. Since 2026-09-06 (F-1) `ensureProvisioned()` fails CLOSED on an unreadable identity row: the two identity-row reads and Case 3's legacy read distinguish absence (`KeychainItem.loadDistinguishingAbsence`), the pure `classifyDeviceIdentityRows` decides, and any status other than not-found throws `IdentityError.keychainReadFailed` rather than minting over — and deleting — a live identity. |
| `Fernlet/FernletKit/Sources/ProximityKit/Identity/ReplayCache.swift` | Rolling 24-hour envelope-ID cache for replay-attack prevention; injectable clock for deterministic testing. |
| `Fernlet/App/Fernlet/Proximity/Audit/ConnectionInspector.swift` | Live and historical proximity session logging, ranging mode/distance samples, RTT samples, event subsampling, and session lifecycle recording (`ProximityInspectorRecording`). |
| `Fernlet/FernletKit/Sources/FernletDomainModel/ConnectionSessionLog.swift` | Structured session log model capturing peer info, ranging state, transport counters, envelope records, and error events. |
| `Fernlet/App/Fernlet/Proximity/UI/ConnectionInspectorView.swift` | On-demand live connection inspector UI for real-time proximity diagnostics, distance, RTT, and fallback status. |
| `Fernlet/App/Fernlet/Proximity/UI/ConnectionInspectorHistoryView.swift` | View for browsing and reviewing historical proximity connection session logs. |
| `Fernlet/FernletKit/Sources/ProximityKit/UI/KeepFriendsPromptSheet.swift` | `KeepFriendsSection` + prompt sheet — session-end one-sided "keep as a friend?" affordance that mints a local-only trust-vault record (peer never notified). |
| `Fernlet/App/Fernlet/Proximity/UI/ProximityRecipeShareSheet.swift` | `ProximityRecipeShareSheet` + `ProximityRecipeShareDraft` — outbound sheet to share a recipe with a nearby Fernlet over the proximity channel. |
| `Fernlet/App/Fernlet/Proximity/UI/ProximityRecipeShareReviewSheet.swift` | `ProximityRecipeShareReviewSheet` — review an incoming proximity-shared recipe (title, servings, macros) before saving it. |
| `Fernlet/App/Fernlet/ProximityHostAdapter.swift` | Conforms `FernletStore` to the Proximity subsystem's `ProximityHost` seam (proximity display name, nearby-hearts opt-in). |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/TrainerAuditLog.swift` | Trusted peer records, `ProximityTrustPolicy`, and trainer-named audit event vocabulary shared by proximity sessions. |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/FriendSessionTrustPolicy.swift` | Friend-session trust-policy wrapper over `ProximityTrustVault`; blocked, revoked, and audit operations delegate to the vault while proximity-gated sessions remain permissive for remembered-trust checks. |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/ProximityTrustVault.swift` | Trusted proximity peer records and trainer audit event vault; implements `ProximityTrustPolicy`. Extracted sub-service of `FernletStore`. |
| `Fernlet/FernletKit/Sources/ProximityKit/UI/FriendPhotoReviewSheet.swift` | Review tile, review sheet, and photo-library saver for shared friend photos. |
| `Fernlet/FernletKit/Sources/ProximityKit/ForegroundAnchor/ProximityForegroundAnchor.swift` | The `ProximityForegroundAnchoring` seam and its one shipping conformer, `NoopProximityForegroundAnchor` — `ProximityCoordinator`'s unconditional default since P9 item 5 retired the Live Activity anchor (no widget ever declared an `ActivityConfiguration` for the proximity attributes, so every request was doomed). Plus `ProximityLiveActivityReaper` — the launch-time reaper (called once per process from `FernletStoreLoader.startIfNeeded()` before the store exists) that ends proximity activities a killed previous process stranded; it is the only remaining reader of the attributes type. |
| `Fernlet/FernletKit/Sources/ProximityKit/ProximityHost.swift` | `@MainActor` host-protocol seam letting the mesh/recipe-share managers reach app state (display name, trusted peers, trust vault, block/fingerprint checks, nearby-hearts opt-in) without naming concrete `FernletStore`. |
| `Fernlet/FernletKit/Sources/ProximityKit/PeerDisplayNames.swift` | The subsystem's two shared display-name coercions: `ProximityHost.resolvedProximityDisplayName` (the LOCAL advertised name — host preference trimmed, device name as fallback), the one home of the three identical private `displayName` vars in `MeshNetworkManager`, `ProximityRecipeShareManager`, and `PresenceManager`; and `ItemNameModeration.moderatedPeerDisplayName` (the wire-boundary sanitize-or-"A friend" coercion for a PEER-supplied name), the one home of that idiom in the heart-receive, vouch-list, session-chat, and keep-as-friend paths. `FernletStore.shopDisplayName` and the heart-drop name paths differ on purpose and deliberately do not adopt it. |
| `Fernlet/FernletKit/Sources/ProximityKit/Support/JSONSidecarFile.swift` | `JSONSidecarFile` — the shared best-effort load/save/remove plumbing for the device-local Application Support JSON sidecars (`.completeFileProtection`, never synced) that `FriendStateCache`, `ClosenessLedger`, `ModerationLedger`, `ProximityActivityManager`, and `MeshNetworkManager`'s photo-wall preferences each repeated verbatim; it replaced the private `FriendPhotoWallPreferencesStore` outright. Deliberately the naive idiom — every read failure (including a locked-device read) returns nil and reads as "no data", so anything that is data of record must use `ProtectedSidecar` instead. |
| `Fernlet/FernletKit/Sources/ProximityKit/Activities/ProximityActivityManager.swift` | State-and-logic brain for Group Activities riding the friend mesh: roster state, host-authoritative join-token minting/verification, and a device-local never-synced sidecar surviving relaunch until expiry. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/ProximityHeartLedger.swift` | Device-local JSON sidecar for received hearts (Home bubble + 24h health-bar glow) plus the rolling 1-per-friend-per-5-minute rate limit enforced both directions; never synced. |
| `Fernlet/FernletKit/Sources/ProximityKit/Messaging/SessionMessageStore.swift` | Deliberately non-`Codable`, memory-only holder of the current friend-session chat transcript; sanitizes, caps and dedupes messages and clears them at every session end and formation. **P6 item 4** makes the visible transcript a DERIVATION over a held `MeshContentSet<MeshMergedMessage>` — plan §12's own word — so it reads in §10.3's total order rather than arrival order (a routed backlog drains by origin fingerprint), the 500-row cap evicts in that order, and a gate that closes hides rows without destroying them. `receiveIncoming` answers a three-way `Acceptance` and takes a required `seenAt`; the per-sender token bucket retired with the live transport it belonged to. Every dedup — the seen set, the held `MeshContentSet`, the attribution table and `Message.id` itself — keys on `MeshContentKey` = `(senderFingerprint, id)` since item 4's **fix review**, because the id is the origin's own choice and its manifest crosses in the clear first. A gate flip re-renders at the next INGEST, not at the flip, which the type's own doc now states. |
| `Fernlet/FernletKit/Sources/ProximityKit/Presence/PresenceEpochPosture.swift` | **P9 item 2 pass 1** (plan §17.1): `PresenceEpochPosture`, the presence radio's ephemeral posture as a pure VALUE — for any instant, the epoch (`IdentityService.presenceEpoch(at:)`, the one presence clock), the service instance name to advertise (a frozen `fn-` service token plus 64 bits of fresh entropy as lowercase hex, fixed length on every device at every epoch) and the TLS identity to present (one `EphemeralMeshTLSIdentity.Minted`, the module's single certificate path — no new cryptographic purpose). `rotated(at:)` returns `self` inside the epoch and an entirely fresh posture across a boundary, so no name and no certificate survives 900 s. The certificate is minted at the EPOCH'S START (`IdentityService.presenceEpochStart(at:)`), never at the mint instant: its `notBefore`/`notAfter` are readable by anyone in range, so an instant-anchored window would stamp the posture with the second that radio came up. Wall-clock anchored on purpose: the tag epoch must be absolute for pairwise derivation to be mutual, a per-launch phase would itself be a rotation-surviving fingerprint, and a global rotation instant maximises the anonymity set. No radio, no task, no timer, no persistence; the clock and the entropy are injected. `PresencePostureError` is its one diagnostic error (never a `LocalizedError`). Pinned by `PresenceEpochPostureTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Presence/PresenceAdvertisement.swift` | **P9 item 2 pass 2**: the presence radio's Bonjour TXT vocabulary as pure functions (`v` plus the pairwise-DH tags, chunked across `t`/`t1` so no entry exceeds DNS-SD's 255-byte ceiling — 24 tags are 311 bytes and would have been refused or truncated on the air), plus `PresenceDialHello`, the one frame a dialer writes so the responder can resolve an inbound QUIC connection back to the browsed peer it matched. Pinned by `PresenceAdvertisementTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Presence/PresenceManager.swift` | Standing presence radio on `_fernlet-near2._udp` (QUIC, since P9 item 2 pass 2) broadcasting only rotating pairwise-DH tags for nearby-friend recognition under a per-epoch `PresenceEpochPosture`, and driving the on-demand short-lived pairwise heart send/receive handshake; memory-only. |
| `Fernlet/FernletKit/Sources/ProximityKit/Presence/ClosenessLedger.swift` | Device-local day-granularity per-friend interaction counters feeding the deterministic closeness score and close-slot assignment (with persisted hysteresis); capped, never synced. |
| `Fernlet/FernletKit/Sources/ProximityKit/Presence/FriendStateCache.swift` | Device-local cache of a friend's fuzzy wellbeing state + avatar appearance captured at the last in-person meet (`CachedFriendState`), with 30-day staleness treatment; never synced. |
| `Fernlet/FernletKit/Sources/ProximityKit/RecipeSharing/RecipeShareAdvertisement.swift` | **P9 item 3 pass 2**: the recipe radio's Bonjour TXT vocabulary as pure functions (`v`, `sid`, `mode`, an optional byte-bounded `name`; the owner contributes all but the `sid`, which belongs to the radio and is re-minted with its posture, and every entry is held under DNS-SD's 255-byte ceiling), plus `RecipeShareDialHello` — the one canonical frame a dialer writes so the responder can resolve an inbound QUIC connection back to a browsed peer, which the retired transport gave for free. Pinned by `RecipeShareOverQUICTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/RecipeSharing/RecipeShareTransfer.swift` | **P9 item 3 pass 1** (plan §17.1): the recipe-share radio's two transport-losable decisions as pure values, plus the advertised name's bound. `RecipeShareDiscoveryGate` is the pause/resume contract as a total table over (`isRunning`, `isPaused`, `connectionCount`) × five events — discovery moves at exactly two moments (pause on `connectionRegistered`, resume on `connectionsEvicted` with nothing left held, over a RUNNING radio) and at no others; `refreshRequested`, `transportErrorWhileListening` and `stopped` all resolve through the radio's own `stop()`/`start()` rather than through the gate, so the paused flag never has two owners. `RecipeShareTransfer` is one share's state machine — `connecting → verified → sending → sent/failed/cancelled`, `peerVerified` idempotent and `sendBegan` once-only, `completionCount` an oracle (0 or 1, and 1 exactly when `.sent`), and the load-bearing row that a `discoveryPaused`/`discoveryResumed` is accepted in EVERY phase and moves none of them. Its `route` projection reads `MeshTransferStreamTable.route(reliableByteCount:)` off the encoded plaintext (an estimate, NOT a floor — wire2 deflates a body before sealing it, so a compressible plaintext over the floor can still ride the control stream; nothing acts on the projection, the radio routes on the sealed frame's real size): a text-only recipe stays on the control stream, a recipe carrying a picture clears the bulk floor — which is why the pass-2 recipe radio needs the per-transfer-stream acceptor `NetworkPresenceSession` deliberately has none of. `radioIsQuiet` is SEEDED from the radio at the mint, not defaulted — a second share to an already-paired peer is minted between gate transitions and would otherwise claim an open radio for its whole life. `RecipeShareAdvertisedName` caps the advertised name at `MeshLinkAdvertisement.maxFieldValueLength` (64) BYTES on grapheme boundaries, because `publishedFields` drops an over-long value rather than cutting it and 24 CJK Characters are 72 bytes; `publishable(_:)` answers "" for the one input it cannot publish (a single grapheme wider than the bound — combining marks survive sanitizing), which the caller OMITS rather than advertising, and `received(_:hint:)` is the matching receive-side rule that falls back to the peer hint on an absent *and* an empty name. No radio, no clock, no task. Pinned by `RecipeShareTransferTests`. |
| `Fernlet/FernletKit/Sources/ProximityKit/RecipeSharing/ProximityRecipeShareManager.swift` | `@MainActor` `@Observable` manager for one-at-a-time in-person recipe sharing: nearby-recipient discovery, a single verified sealed connection, send-state machine, pending inbound shares, and diagnostics. Since **P9 item 3 pass 1** its two transport-losable decisions are tier-1 values it consults rather than inline guard chains: `applyDiscoveryGate(_:)` is the ONLY caller of `pauseDiscovery()`/`resumeDiscovery()` (one each, walled by `RecipeShareTransferTests`), and `applyTransfer(_:)` drives the `RecipeShareTransfer` record — minted in `sendRecipeShare` (seeded from `isDiscoveryPaused`), gating the once-only send start in `sendPendingPayload`, cancelled by the pre-connect timeout, and DISCARDED outright (`transfer = nil`, no `.cancelled`) by `stop()`/`refreshDiscovery()`. The advertised `name` is bounded in bytes by `RecipeShareAdvertisedName` and omitted entirely when it cannot be published; an absent or empty inbound name falls back to the picker's existing "A friend" placeholder, never to a transport token — `displayName(for peer:)` is the ONE rule for naming a peer in anything a user reads (the picker row, the four "Connection details" lines and the cap's status line all go through it). **P9 item 3 pass 2** put it behind the `RecipeShareRadioSession` seam and onto QUIC: no MultipeerConnectivity type is left in the file, `invite` became `dial(_:helloSID:)`, the inbound invitation gate became `resolveDialer` + `shouldAcceptDialer`, `disconnectPeer` became `endTunnel`, and `isListening` stays the radio's own LEVEL account. A failed `start()` goes through the one `handleTransportError(_:)` stand-down door and nothing else does. Each send's outcome is attributed by a **per-send token**, so two overlapping shares to one already-paired peer cannot credit each other's completion. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/FriendsDiscoveryEntry.swift` | P6 item 2 fix: the Friends tab's discovery entry as a pure three-way value (`fresh` / `resume` / `none`) over `isInSession × hasCommittedPeer`, plus whether that entry arms the five-minute timeout (an exhaustive `switch`, never `!= .none`, and no `String` rawValue / `CaseIterable` — nothing read either and a rawValue is a frozen-token obligation; fix review P3-9). Extracted from `ContentView.startFriendsDiscovery()`, where both halves were `private` in the app target and reddened nothing when deleted while being item 2's whole user-facing P1 claim. Same precedent and shape as `FriendMintingReview.sessionEndReview`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshSessionPoll.swift` | **P7 item 4**: `MeshNetworkManager.pollSession(now:)` — the ONE public seam that runs the three on-demand consumers in their one order (ceiling → idle lapse → partition) for a live session and nothing for a dead one, returning `MeshSessionPollReport` (flags only; `sessionLiveAfter` is the app timer's stop condition) — and `armSessionCeilingFromAdoptedMeshIfNeeded(now:)`, the joiner-side ceiling arm called at descriptor adoption and at the admission grant, which closes P6 §12.3 finding 3 (a yielding founder, and every proximity joiner, held a mesh with no ceiling to enforce). No timer, no clock, no plaintext, nothing routed. |
| `Fernlet/FernletKit/Sources/ProximityKit/Mesh/MeshSessionResumePresentation.swift` | **P7 item 5**: the decision half of the resume surface. `MeshSessionResumePresentation` (public: `nothing` / `offerResume` / `previousSessionEnded(_:)` / `previousSessionCouldNotBeReopened`) with the pure, total `presentation(outcome:offersForegroundResume:isInSession:)` over every `MeshSessionRestoreOutcome` case, `MeshSessionEndingPresentation` (the four-way fold of the eight frozen termination reasons — ended, never failed), and `MeshNetworkManager.sessionResumePresentation`, the one public read over the four observation-ignored restore surfaces the app could not see. A deferral or refusal is deliberately silent; a session surface up presents nothing. |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/FriendMintingReview.swift` | Pure view-free decision logic for the post-session keep-as-friend prompt: chooses the session-end review mode and filters roster candidates for eligibility against the trust vault at presentation time. |
| `Fernlet/FernletKit/Sources/ProximityKit/Moderation/ModerationBanStore.swift` | Tamper-resistant 30-day designer store-ban clock: reinstall-proof via a dedicated Keychain service and clock-tamper-proof via a `mach_continuous_time` countdown plus a wall-clock high-water ratchet. Reconciles in both directions: applies warranted bans with their salted-tag evidence, and lifts an active ban whose reporters positively withdrew enough of it. |
| `Fernlet/FernletKit/Sources/ProximityKit/Moderation/ModerationContentHash.swift` | Stable content key an abuse report binds to: order-stable SHA-256 over a sanitized item's artwork (texture grid + slot), so relisting under a new id/name cannot evade a report. |
| `Fernlet/FernletKit/Sources/ProximityKit/Moderation/ModerationLedger.swift` | `@MainActor` append-only JSON sidecar of moderation report/retract rows keyed for idempotent de-dupe and supersede-by-`reporterSeq`; bounded, never synced. |
| `Fernlet/FernletKit/Sources/ProximityKit/Moderation/ModerationReportRelay.swift` | One-hop report wire: `SignedModerationReport`/`ModerationReportPayload` models plus verification of each row's Ed25519 signature against the transport-verified sender key (no transitive relay — the Sybil defense). |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/ActivityPayloads.swift` | Group Activities wire envelopes plus `schemaVersion`-gated join-token/roster-snapshot signing and verification and the `ActivityParamsHash` descriptor hash. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/CanonicalSignatureSerializer.swift` | Deterministic, cross-platform-stable positional length-prefixed binary canonical-byte serializer for Ed25519 signing, replacing JSON `.sortedKeys` to prevent cross-stack `signatureInvalid`. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/MessagePayloads.swift` | `TempMessagePayload` wire value type for a single sealed live-session chat message (id/text/sentAt); receiver sanitizes and length-caps before use. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/RecipeSharePayloads.swift` | `ProximityRecipeSharePayload` wire envelope wrapping a `ProximitySharedRecipe` for in-person recipe sharing, plus the optional 512 KB `imageJPEGData` wire image (cap enforced at the receive door via `droppingOversizeImage()`) and the share-notes / include-picture strips. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/SealedIntroductionEnvelope.swift` | Transport wrapper carrying only the ciphertext of a KA-sealed identity intro/ack for presence-heart handshakes, so a tag-replay forger cannot deanonymize the sender's identity. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/TrainerPayloads.swift` | `TrainerExportPayload` wire envelope carrying the opaque, size-bounded curated Trainer/Nutritionist export bundle; sealing-required so an unsealed send fails closed at verify. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/SealedPayloadFraming.swift` | wire2 sealed-body framing (compress + pad inside the AEAD). Threaded through `IdentityService.seal/open` and envelope verify; derived from the peer's advertised capabilities, never from the bytes alone. |
| `Fernlet/FernletKit/Sources/ProximityKit/Wire/ProximityVerification.swift` | The QR verification ceremony that upgrades a non-UWB `awaitingManualCommit` slot to ceremony grade: the QR proves the signing key is physically on screen, the sealed challenge/response proves the live peer holds it. |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/CoachVerificationCeremony.swift` | Slot-independent verification ceremony for the in-person coach session (Increment 10). Carries the nonce-binding rules — per-peer nonce, sign-after-check, wrong-peer drop must not clear. **No production callers yet:** the coach session manager is unbuilt. |
| `Fernlet/FernletKit/Sources/ProximityKit/Trust/CoachSessionTrustPolicy.swift` | `CoachSessionContract` (the written-down coach/trainee role split) and the coach trust policy — a coach is NOT a friend, so only an unrevoked `.trainer` vault record with `unknownModeToken == nil` auto-confirms. **No production callers yet.** |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropService.swift` | Offline "away" hearts over the CloudKit public-DB dead-drop. All crypto happens here on the sealed side of the wall; the injected transport only ever sees rotating day tags plus ciphertext. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropSealer.swift` | The outer seal for heart drops: versioned wire form with a prekey id (all-zeros = sealed to the static key). Gates payload size *before* key agreement. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartPrekeyStore.swift` | One-time X25519 prekeys for forward-secret drops, plus the X3DH-style signed prekey. Private halves live in one keychain blob (`AfterFirstUnlockThisDeviceOnly`, never synchronizable). |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropPeerBundleCache.swift` | Cached prekey bundles gossiped by friends plus per-friend consumed-prekey marking. A bundle is only ever stored from a verified, signed identity intro, keyed by the sender's full signing key. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropOutbox.swift` | Persisted sender-side queue for offline drops plus the durable receive dedup — two sidecars beside `HeartLedger.json`, deliberately outside the synced snapshot. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/ProtectedSidecar.swift` | The `ProtectedSidecar` state machine (absent / deferred / corrupt / loaded) and `SidecarSeal`. Fixes the class of data loss where any read failure read as "no data" and the next persist overwrote the real file. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropSidecarKey.swift` | Keychain-backed ChaChaPoly seal for the heart-drop sidecars at rest — the plaintext versions were a timestamped log of who the user sent affection to. One-way plaintext→sealed migration. |
| `Fernlet/FernletKit/Sources/ProximityKit/HeartSharing/HeartDropStorageScope.swift` | The storage identity of one device's heart-drop state — sidecar directory + the keychain service holding the seal key and prekey blob. One value because `wipeForDeleteAll()` destroys both, so isolating either alone isolates nothing. |
| `Fernlet/App/Fernlet/VerifyQRViews.swift` | `QRCodeRenderer` plus the verify-QR display/scan sheets — renders this device's signed `fernlet://verify` URL for the ceremony above. |

## Private Media Stores

| File | Purpose |
| --- | --- |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/PrivateMediaStore.swift` | Disk-backed mesh photo index with **AES-256-GCM at-rest encryption** of image/thumbnail bytes and of the index itself (`MeshPhotoCache.sealed` under the friend-wall media key; a legacy plaintext `MeshPhotoCache.json` is read once, resealed, then deleted), thumbnail generation, hydration, FIFO eviction (1000 cap / 900 warn), and orphan cleanup. (Formerly `MeshPhotoCacheStore`.) `maxIncomingPhotoBytes` is `public` since **P6 item 3** so the routed mesh path derives its per-type ciphertext cap from this one plaintext bound instead of restating 10 MB. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/PrivateMediaKeyStore.swift` | `PrivateMediaKeyProviding` + the keychain-backed AES key provider, split per `Role` since hardening Phase 5: `.friendWall` = the original backup-restorable `AfterFirstUnlock` row (`…contentKey`, unchanged), `.ownPhotos` = a second row (`…ownContentKey`) for the user's own meal/recipe/progress media, whose device binding is a one-line policy flip in `defaultDeviceBinding(for:)`. Fails closed (no mint) when the keychain read is *unreadable* rather than definitively absent. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/OwnPhotoCorpusLayout.swift` | The one home of the own photo corpora's on-disk names (`OwnPhotoCorpusLayout`) and the location set that walks them (`OwnPhotoSealedLocations`). Was `OwnPhotoKeyMigration.swift` and also held `OwnPhotoKeyMigrator` + `OwnPhotoMigrationLatch`; both were retired at the close of the crypto standardization round (owner decision 2026-08-29) once Phase 3's deletion of the unmarked at-rest read left the re-seal pass with no reachable input, and `MediaAtRestFormatMigrationLatch` inherited its half of `OwnPhotoKeyBinder`'s binding gate. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/OwnPhotoKeyBinding.swift` | The own-photo key's device-binding gate (Phase 5, step 5c): `OwnPhotoKeyBinder` (refuses unless the migration latch is set AND the user has a cross-device route, then re-binds the row in place with `SecItemUpdate` — never delete-then-add, which would open a window with no own-photos key at all), `OwnPhotoDeviceBindingConsent` (the recorded, one-way user acceptance for the escrow-off branch), and `OwnPhotoKeyBindingOutcome`. Also the keychain accessors that read and set the row's accessibility class. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/MediaAtRestCrypto.swift` | Shared AES-256-GCM at-rest helpers on `PrivateMediaKeyProviding` (`gcmSeal`, `gcmOpen`, `sealAndWrite` — the last writes nothing at all on a nil key or seal failure, so plaintext never reaches disk). The one home of the seal / open / seal-then-write idiom `PrivateMediaStore`, `MealPhotoStore`, and `ProgressPhotoStore` each hand-rolled; deliberately policy-free, so each store keeps its own fail-closed decision (and its legacy-plaintext branch) at the call site. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/FriendPhotoImageHelpers.swift` | `UIImage` resizing helper for friend-photo sharing. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/MealPhotoStore.swift` | `MealPhotoStore`: on-device AES-256-GCM-sealed store for meal and other private photos with bounded downscale. |
| `Fernlet/FernletKit/Sources/PrivateMediaStore/ProgressPhotoStore.swift` | `ProgressPhotoStore`: sealed gym progress-photo timeline over `MealPhotoStore` bytes plus a GCM-sealed dated index. |

## Widgets, Live Activities, And App Intents

| File | Purpose |
| --- | --- |
| `Fernlet/App/FernletWidgets/FernletWidgetsBundle.swift` | `@main` `WidgetBundle` registering the companion widget (mood glyph + water) and the guided-workout Live Activity. |
| `Fernlet/App/FernletWidgets/WidgetSharedModels.swift` | Deliberately duplicated app-group models (`WidgetSnapshot`, `PendingWidgetAction`, `WidgetCompanionState`, day-key/JSON helpers) so the standalone widget extension avoids linking FernletKit. |
| `Fernlet/App/FernletWidgets/WaterPlusOneIntent.swift` | `AppIntent` behind the companion widget's interactive "+1 water" button; queues a pending action and reloads the widget timeline. |
| `Fernlet/App/FernletWidgets/WorkoutLiveActivity.swift` | Widget-target `Widget` rendering the Lock Screen card and Dynamic Island for the guided-workout rest-timer Live Activity. |
| `Fernlet/App/FernletWidgets/WorkoutActivityAttributes.swift` | `ActivityAttributes`/`ContentState` contract for the guided-workout Live Activity, compiled into both the app and widget targets. |
| `Fernlet/App/FernletWidgets/GuidedWorkoutRunState.swift` | Codable/Hashable value type for an in-progress guided workout, mirrored into the app-group container so Lock Screen intents can advance it. |
| `Fernlet/App/FernletWidgets/AppGroupRunStateStore.swift` | `AppGroupRunStateStore` + `AppGroupRunStatePersistable` — the generic `NSFileCoordinator`-guarded app-group reader/writer behind both run-state files (ISO-8601 + sorted-keys codecs, atomic protected writes, nil-tolerant reads that treat a missing/corrupt/still-protected file as "no active run", and the `updatedAt` re-stamp the app's reconcile ages out on). The single home of the plumbing `GuidedWorkoutRunStateStore` and `CookingRunStateStore` each carried; self-contained (literal app-group id) so it compiles identically into both targets. |
| `Fernlet/App/FernletWidgets/GuidedWorkoutRunStateStore.swift` | The guided-workout specialization of that store — now a `typealias` for `AppGroupRunStateStore<GuidedWorkoutRunState>`; the persistence seam between the app's in-app transitions and the Lock Screen intent runner. |
| `Fernlet/App/FernletWidgets/GuidedWorkoutActivityBridge.swift` | Shared bridge (app + widget targets) that syncs a `GuidedWorkoutRunState` onto the live workout Activity, updating or ending it. Remains the documented seam, now delegating the loop to `LiveActivityReflector`. |
| `Fernlet/App/FernletWidgets/LiveActivityRunReflecting.swift` | `LiveActivityRunReflectable` + `LiveActivityReflector` — the one generic update/end engine (update while the run is live, end on terminal) that `GuidedWorkoutActivityBridge` and `CookingActivityBridge` delegate to instead of each carrying the loop. Every call is parameterized by its own `ActivityAttributes` type, so ending one activity kind can never enumerate the other's instances. Compiled into both the app and widget targets. |
| `Fernlet/App/FernletWidgets/GuidedWorkoutLiveActivityIntents.swift` | `LiveActivityIntent`s for the guided-workout Live Activity's "Done set" and "Skip rest" Lock Screen buttons. |
| `Fernlet/App/Fernlet/WidgetBridge.swift` | `WidgetSnapshot` + bridge — app side of the FernletWidgets app-group JSON bridge: outbound benign snapshot and inbound pending widget actions (deliberately S3-walled twin types). **P10 item 4** adds the CONTENT/METADATA split the background refresh's "reload timelines only on change" rests on: `WidgetSnapshot.contentEquals(_:)` compares the six fields that decide what the widget DRAWS and ignores `computedAt`, which is stamped `Date()` at every construction — so the synthesised `==` is false between two snapshots of an unchanged day and a diff built on it would reload the timelines forever. The doc comment carries the per-field table; `WidgetSnapshotContentEqualityTests` pins it, including a `Mirror` field-count pin that reds when a field is added without a classification. `WidgetSnapshotMirror.publishIfContentChanged(_:)` is the handler's publish door — it always WRITES (the stamp moves) and reloads only on a content change, answering `WidgetSnapshotPublication`. `publish(_:)` is untouched and still reloads unconditionally: that scoping is the foreground's, every one of whose callers is a change by construction, and a cell pins it. |
| `Fernlet/App/Fernlet/CompanionRefresh/CompanionRefreshIdentifier.swift` | **P10 item 2** (plan §16.4, §17.2): `CompanionRefresh` — the companion background refresh's namespace, holding its one frozen `BGAppRefreshTask` identifier `MBO.Fernlet.companion-refresh` and nothing else (item 3 registers it and adds the `Info.plist` entries; item 4 is the handler). `nonisolated`, because the app target defaults to MainActor isolation and an identifier that needs an `await` to read is one the registration site cannot use — the same reason `MeshContinuationTaskHost.identifierPrefix` is spelled that way. It exists in its own directory on purpose: `App/Fernlet/CompanionRefresh/` is the unit `BackgroundRefreshBoundaryTests` walks, so items 3 and 4 are inside §16.4's import wall from the moment their files land, and this literal is that wall's positive needle — an emptied directory reds it instead of passing vacuously. Writes nothing, so the wipe wall is owed no disposition row. |
| `Fernlet/App/Fernlet/CompanionRefresh/CompanionRefreshScheduling.swift` | **P10 item 3** (plan §17.2, §27.1): the companion refresh's OWN `BackgroundTasks` seam — `CompanionRefreshScheduling` (register / submit), `CompanionRefreshTaskHandle` (expiration handler, complete) and the `CompanionRefreshRequest` value, plus the production conformers `SystemCompanionRefreshScheduler` and `SystemCompanionRefreshTaskHandle`. A SECOND seam rather than a widened `MeshContinuationScheduling`, deliberately: two tasks through one protocol is how a fake stops modelling either, and the mesh's members (progress, a two-sentence card, four ending reasons) have no meaning for an app refresh. Three places the mesh's shape did NOT transfer, each with its reason in the header: no pre-register cancel (the refresh identifier is process-stable, so a request from a previous launch is exactly what this one wants delivered — cancelling would break the chain every cold start, silently), no pre-submit cancel (the pending request IS the fallback when a submission throws), and no `strategy` / `progress` / `updateTitle` (`BGAppRefreshTaskRequest` has none of them; `earliestBeginDate` is the only thing there is to ask for). It is the app's only file that names `BGAppRefreshTask`, which `MeshContinuationTaskHostTests` cell (a) pins in both directions alongside the mesh's `BGContinuedProcessingTask`. The conformers are the untested remainder and are item 9's device row — including §27.2's open measurement of what a Simulator does with an app-refresh submission at all. |
| `Fernlet/App/Fernlet/CompanionRefresh/CompanionRefreshCoordinator.swift` | **P10 item 3**: `CompanionRefreshCoordinator` — the process-global (`.shared`, an immutable `static let` in `FernletStoreAccess.shared`'s idiom) that owns the companion `BGAppRefreshTask`: one registration at launch, the two submission triggers, and exactly-once completion of whatever the system delivers. **The schedule policy is at handle + background, never on a timer** (plan §26.3/§27.3): trigger (a) is the tail of every handler run, which submits the successor BEFORE it completes the task, and trigger (b) is the `.background` scene edge — there is no third, and no clock at all (no `Timer`, no `DispatchQueue`, no `Task.sleep`; the app owns exactly one timer and it is `ProximitySessionPoller`'s). **The edge asks only when nothing is pending**, held in an in-memory `pendingRequest` slot: `BGTaskScheduler` REPLACES a same-identifier request, so an unconditional edge submission slid the floor fifteen minutes further out on every switch away and a person who opened the app more often than that would never be delivered one. A delivery empties the slot, a refusal leaves it empty, and the tail is unconditional because it runs after a delivery; the accepted cost of an in-memory slot is one slide per process launch. The R2 cap (`maxEdgeSubmissionsPerLaunch`) bounds the EDGE alone — the tail is exempt, because a never-reset lifetime budget shared with it would end the chain permanently on a process iOS keeps suspended for days. `earliestBeginInterval` is 15 minutes: a floor the app states, not a cadence it gets, and a constant so a soak can move it with a measurement. **Item 3's handler is a placeholder by design** — adopt, submit the successor, complete once — so §17.2's seven steps land in item 4 as an insertion between two existing lines rather than a rewrite, and so §16.4's import wall and the exactly-once contract are both already true of the directory before any work runs inside it. Writes nothing: no `UserDefaults`, no "last refreshed at", nothing on disk, so the wipe wall is owed no disposition row. Speaks the seam's protocols and never `BackgroundTasks` itself. **2026-09-21 (item 2c):** the edge budget is charged AFTER `submitNext(trigger:)` reports the ask reached the seam, so a launch whose registration was refused spends none of it and its audit trail keeps naming the refusal instead of the cap (plan §17.2.3 finding 3); `edgeSubmissions <= submissions` is now an invariant. |
| `Fernlet/App/Fernlet/CompanionRefresh/CompanionRefreshPipeline.swift` | **P10 item 4** (plan §17.2, §27.3): the companion refresh HANDLER, written as a pipeline over VALUES — `CompanionRefreshStep` (the trace), `CompanionRefreshOutcome` (what the system is told), `CompanionRefreshRun` (trace + verdict + the two facts worth reporting), `CompanionRefreshSteps` (the five post-acquisition steps as `@MainActor` closures) and `CompanionRefreshPipeline` itself. Closures rather than a `FernletStore` parameter because a handler that named the store could be driven in a unit test only by BUILDING one, which is the single thing §17.2 forbids outright; acquire is two-phase (it returns the remaining steps rather than a store) so the store appears in no type here at all. The order is the claim: acquire → inspect the widget action queue → roll the day → recompute the companion → publish through the diff → reload only on a change. A queued widget action ends the run at the inspection with a named outcome and NO publish (decision D-10.4.2: the widget's own "+1 water" App Intent has already written a provisional count into the same file, and republishing the app's lower one would make the count go backwards; §17.2 forbids draining here, and the roll is skipped with the publish because a warm process's roll publishes on its own). Cancellation is checked at the two places it can land — before the acquisition and immediately after it, the pipeline's one suspension point — and nowhere else, because the tail is synchronous main-actor work a cancellation cannot cut into. Never throws and never traps: a background handler that threw would leave the coordinator owing the system a completion, so every failure is a value. Writes nothing and reads no clock; it builds no store of its own, and the acquisition it calls builds the PROCESS's first one on a cold wake, which is what §17.2 allows and what the item-4 verify corrected three places for saying otherwise. **Item 4's verify fixes added the two refusals and the roll's own reload.** The queue skip now yields to a new day (D-10.4.8: one undrained row used to suspend every background publish until the next foreground, and across midnight a snapshot left on yesterday's `dateKey` makes `WidgetDayGate` blank the companion — a widget a tap behind beats a blank one, reported as `publishedDespitePendingActions`). A scoring adjustment switched on without its bridge ends the run at a new `inspectScoringContext` step (D-10.4.6: the bridges are attached only by `ContentView`, so a COLD wake recomputes a score missing the period and stress adjustments, and the handler refuses to publish a number the app itself disagrees with — `scoringContextUnavailable`). And the snapshot file is read either side of the roll, because `refreshCurrentDayIfNeeded()` publishes through the store's own mirror on an advance and that write used to satisfy the handler's diff before it ran, leaving `dayAdvanced` true with no reload in the trace on the most consequential refresh of the day. |
| `Fernlet/App/Fernlet/CompanionRefresh/CompanionRefreshWiring.swift` | **P10 item 4**: the ONE place the pipeline's abstract steps are bound to the real app, and therefore the only file in the refresh directory that names anything of the app's. Eight one-expression bindings — `FernletStoreAccess.shared.load()` (the sanctioned acquisition, which refuses before first unlock, hands back the CACHED store, and on a COLD wake builds the process's first one inside the grant), `hasUndrainedWidgetActions`, `publishedWidgetSnapshotIsForCurrentDay`, `hasCompleteScoringContext`, `publishedWidgetSnapshot()`, `refreshCurrentDayIfNeeded()`, `companionState.rawValue`, `currentWidgetSnapshot()` and `ensureWidgetSnapshotMirror().publishIfContentChanged(_:)` — each on `BackgroundRefreshBoundaryTests`' PERMITTED list, so a reviewer can read the body in one screen and decide whether the handler reaches anything it must not. `steps(for:)` is split out of `productionPipeline()` so those five expressions are reachable from a test over an ordinary test store rather than only through the process-global cache. The header records what the day roll and the recompute were TRACED to touch, since the wall cannot see inside them: repository reads and writes only — no HealthKit call, no radio, no forced CloudKit sync (the Core Data stack's own mirroring to the user's private database is ambient, not a sync this handler asks for), and `day.healthContext` is values HealthKit wrote into the diary on an earlier foreground. Imports `FernletDomainModel` for one member, `CompanionState.rawValue`, which member-import visibility makes explicit. Item 4's verify fixes write down what ensuring the mirror COSTS (decision D-10.4.7, accepted): in a process woken purely for background work the mirror outlives the refresh, so any later save there — a Siri water intent — now reloads the widget unconditionally, which was impossible before and is exactly what the foreground already does. |
| `Fernlet/App/Fernlet/WorkoutLiveActivityController.swift` | `WorkoutLiveActivityController` — app-side requester for the guided-workout Live Activity (starts one, ends any stale one; degrades silently when Live Activities are disabled). |
| `Fernlet/App/Fernlet/LiveActivityStarter.swift` | `LiveActivityStarter.start` — the shared generic requester behind `WorkoutLiveActivityController` and `CookingLiveActivityController`: ends any already-live activity of the same attributes type, then requests one, and no-ops when the user hasn't enabled Live Activities (request failures degrade silently). App-target only and deliberately outside the dual-membership set, because only the app process can start an activity — unlike the update/end engine (`LiveActivityRunReflecting.swift`), which the Lock Screen intents also need. |
| `Fernlet/App/Fernlet/FernletNavigation.swift` | App navigation enums (`FernletTab`, `FernletSheet`) — split out of the design system when it moved into the FernletUI package target (`FernletSheet` references app-resident `FirstAidTool`). |
| `Fernlet/App/Fernlet/FernletAppIntents.swift` | App Intents — `LogWaterIntent` (background app-group queue append, no app launch), `LogMealIntent` / `OpenJournalIntent` (open the app to the matching sheet via a persisted deep-link). |
| `Fernlet/App/Fernlet/FernletShortcuts.swift` | `FernletShortcuts` (`AppShortcutsProvider`) — surfaces the log-water/log-meal/open-journal App Intents to Siri and Spotlight with natural phrases. |
| `Fernlet/App/Fernlet/ExchangeIntentService.swift` | `ExchangeIntentService` — the main-process dependency behind the recipe / workout-plan exchange App Intents, plus the entities those intents speak in (`RecipeEntity`, `PlannedWorkoutEntity`, their `EntityQuery`s, `WorkoutPlanImportPreview`, the duplicate/collision policy enums, `ExchangeIntentLimits`, and `ExchangeIntentServiceError`). It creates no repositories and owns no store of its own: the scene loader (via `loadStoreForUI`) and every background intent coalesce through `FernletStoreAccess`. A workout-plan preview carries a calendar-revision fingerprint and an expiry, so an approved plan cannot be applied against a calendar that changed underneath it, and both import paths consult `ExchangeImportLedger` so a retried automation replays its result instead of importing a second copy. |
| `Fernlet/App/FernletWidgets/CookingActivityAttributes.swift` | ActivityKit contract for the cooking Live Activity (recipe name + per-step `ContentState`), compiled into both targets. |
| `Fernlet/App/FernletWidgets/CookingLiveActivity.swift` | The cooking Live Activity widget: Lock Screen card plus every Dynamic Island presentation. Registered in `FernletWidgetsBundle` alongside the workout activity. |
| `Fernlet/App/FernletWidgets/CookingActivityBridge.swift` | The single seam reflecting a `CookingRunState` onto the live activity — shared by the in-app walker and the Lock Screen intents. Delegates its loop to `LiveActivityReflector`. |
| `Fernlet/App/FernletWidgets/CookingLiveActivityIntents.swift` | `LiveActivityIntent`s for the cooking activity's "Next"/repeat-step buttons and the matching Siri phrases; `CookingIntentRunner` performs them in the app's process and posts the notification `FernletStore` reconciles on. |
| `Fernlet/App/FernletWidgets/CookingRunState.swift` | A cooking session as a flat value that survives process death in the app group, carrying the full ordered `steps` so a cold-launched intent can render the next step with no live app state. |
| `Fernlet/App/FernletWidgets/CookingRunStateStore.swift` | Coordinated app-group reader/writer for `CookingRunState.json` — the persistence seam between `FernletStore` and the Lock Screen/Siri intent runner. |

## Messages Extension

The only UIKit target Fernlet ships, and a two-process boundary: the appex reads an App Group
catalog the app publishes and writes hand-off inboxes the app drains. It links exactly one
FernletKit product (`FernletExchange`) and no repository — `MessagesExtensionBoundaryTests` scans
the whole directory to keep it that way.

| File | Purpose |
| --- | --- |
| `Fernlet/App/FernletMessagesExtension/FernletMessagesViewController.swift` | `FernletMessagesViewController` — the `MSMessagesAppViewController` composer and received-item viewer. Reads only the bounded App Group catalog, and independently decodes every received envelope rather than trusting the visible message card. Draws its own 1200×630 card artwork locally, so Messages never fetches or exposes a private food photo. Persists nothing of its own since 2026-09-23 (the last-picked-recipe key it kept in its own `UserDefaults.standard`, which the wipe could not reach, was removed). |
| `Fernlet/App/FernletMessagesExtension/FernletMessagesCopy.swift` | `FernletMessagesCopy` — the target's copy vault (the `FernletUICopy`/`FernletLockCopy` model). Every sentence the extension shows, keyed and catalogued, with computed members so the lookup happens under the locale in force at use time. NO `bundle:` argument: an appex's `Bundle.main` is its own. |
| `Fernlet/App/FernletMessagesExtension/Localizable.xcstrings` | The target's string catalog, added 2026-08-27 — the target shipped a round without one, so `String(localized:)` there would have harvested nothing. Synced by `Scripts/sync-string-catalogs.sh`; the four count keys carry hand-authored plural variations. |
| `Fernlet/App/FernletMessagesExtension/Assets.xcassets` | The `iMessage App Icon` set App Store Connect requires of an iMessage app (all twelve Xcode slots, including the 1024×768 Messages App Store image), wired through `ASSETCATALOG_COMPILER_APPICON_NAME`. **Placeholder art** (2026-09-23): the app icon scaled and padded with `sips`; replace before release. |
| `Fernlet/App/FernletMessagesExtension/PrivacyInfo.xcprivacy` | The appex's own privacy manifest (App Store Connect checks each executable against its own bundle's manifest). Declares no tracking and no required-reason API — held to the code compiled into the binary by `MessagesExtensionBoundaryTests`, and pinned for tracking flags by `NoTrackingBoundaryTests`. |
| `Fernlet/App/FernletMessagesExtension/MessageTransportProbe.swift` | Phase-0 physical-device probe proving `MSMessage.url` can carry opaque bytes. DEAD CODE — zero call sites anywhere, including tests. Delete it or wire it up. |
| `Fernlet/FernletKit/Sources/FernletExchange/FernletExchange.swift` | The exchange core: `RecipeExchangePacket`, `WorkoutPlanExchangePacket`, `ExchangeCardMetadata`, `ExchangeLimits`, and `ExchangeMessageEnvelope` — the `data:` URL payload carried by `MSMessage.url`. The envelope's `validate()` re-derives the card from the payload and requires equality, which is what makes a tampered visible card unusable. |
| `Fernlet/FernletKit/Sources/FernletExchange/FernletMessagesCatalog.swift` | `FernletMessagesCatalog` — the versioned App Group document the app publishes for the extension: prebuilt packets plus picker metadata, capped at 100 recipes / 100 workouts / 1 MiB. Also `FernletMessagesRecipePicker` / `FernletMessagesWorkoutPicker` (the compact/search rules) and `FernletMessagesCatalogFileStore`. Bounded by construction: browsing it can never widen into repository access. |
| `Fernlet/FernletKit/Sources/FernletExchange/FernletMessagesInbox.swift` | `FernletMessagesInboxStore` and `FernletMessagesInboxLink` — the recipe hand-off inbox. The extension enqueues an opaque record and opens a deep link; the app decodes and presents it for review. Records expire and the queue refuses overflow, so a post-wipe message cannot reopen old content. Cleared by `FernletStore.clearMessagesImportInboxes` — see [PrivacyWipeCoverage.md](PrivacyWipeCoverage.md). |
| `Fernlet/FernletKit/Sources/FernletExchange/FernletMessagesWorkoutInbox.swift` | `FernletMessagesWorkoutInboxStore` / `FernletMessagesWorkoutInboxRecord` — the workout-plan twin of the recipe inbox, carrying the sender's suggested start day for revalidation against the recipient's calendar. |
| `Fernlet/App/Fernlet/ExchangeImportLedger.swift` | `ExchangeImportLedger` — the app-side replay ledger keyed on packet ID + content hash, so the same shared item forwarded twice is recognized as one import rather than duplicated. |

## Share Extension

| File | Purpose |
| --- | --- |
| `Fernlet/App/FernletShareExtension/ShareViewController.swift` | Share-extension entry `UIViewController` that extracts the shared URL and enqueues it via `SharedRecipeImportQueueWriter`. |
| `Fernlet/App/FernletShareExtension/SharedRecipeImportQueueWriter.swift` | App-group JSON queue writer that records shared recipe URLs (`SharedRecipeImportRecord`) for the app to import later. A deliberate hand-copied twin of `AppServices/SharedRecipeImportQueue.swift` (the extension links no FernletKit products); field list, coder config, container fallback chain, and `NSFileCoordinator` coordination must stay in sync — enforced by `SharedRecipeImportQueueMirrorTests`. |
| `Fernlet/FernletKit/Sources/AppServices/SharedRecipeImportQueue.swift` | App-group-backed queue of shared recipe-URL import records (`SharedRecipeImportRecord` with attempt/error tracking) handed off from the share extension for later import. |

## Tests

### Unit Tests

| File | Purpose |
| --- | --- |
| `Fernlet/Tests/FernletTests/FernletTests.swift` | Core app/domain tests. |
| `Fernlet/Tests/FernletTests/Phase3GateReadoutTests.swift` | The Phase 3 gate readout's fold, driven as pure functions over hand-built fixtures: every way a row could manufacture a pass (an empty or truncated corpus, a stale keyed witness, a converting media pass, a manifest behind the device high-water mark, an untaken body probe, an invalid run scheme, a duress session), the media residue arithmetic and label mapping, and the redaction contract on BOTH export routes. |
| `Fernlet/Tests/FernletTests/Phase3ReadoutSessionTests.swift` | `Phase3ReadoutSession`'s stateful half — the seven-step checklist's derived done-states, the scan and session fences, the probe cap's keep-probe-#1 eviction, and `clear()` — plus the store-side `clearPhase3Evidence()` / `clearSealedColumnPassWitness()` that stand between a wipe (or a duress engage) and a manufactured discharge. |
| `Fernlet/Tests/FernletUITests/Phase3GateReadoutUITests.swift` | The navigation smoke test for the Phase 3 gate readout: Settings → Connection log → Developer tools → the readout, asserting the page actually RENDERS. It exists because the view shipped reading its store from an environment nothing populates, which trapped on the push and which no unit test could have caught. |
| `Fernlet/Tests/FernletTests/FernletPersistenceTests.swift` | Persistence behavior tests. |
| `Fernlet/Tests/FernletTests/FernletLockTests.swift` | Lock-related integration or behavior tests. |
| `Fernlet/Tests/FernletTests/FernletLockCryptoTests.swift` | Lock crypto tests: `FernletLockCrypto`'s scrypt passcode derivation, content-key wrapping, and verifier digest, plus the column-key HKDF derivation — which now lives in `ColumnCrypto` (FernletCrypto), characterized here via `@testable import FernletCrypto` including a pinned known-answer vector. |
| `Fernlet/Tests/FernletTests/FernletLockServiceTests.swift` | Lock service tests plus local fake providers and harnesses. |
| `Fernlet/Tests/FernletTests/ColumnCryptoDeviceBindingTests.swift` | Pins the device-bound sealed-column **v3** format: round trip, cross-install refusal, and — since the crypto standardization round's Phase 3 — the two retired generations as REFUSALS BY NAME rather than as compatibility (`SealedColumnOpenError.retiredFormat(_:)`, kept distinct from `emptyBlob` and `installBindingMissing`), the write side pinned CLOSED (`SealedColumnStrictSealError.bindingUnavailable` where it used to fall open), the retryable keychain-outage split (`DeviceBindingID.ReadError`, never an authentication failure), and the one resolution the round gave up — `aCollidedLegacyBlobCanNoLongerBeResolvedByOpen`, the ~1-in-256 blob whose first nonce byte collides with a marker. |
| `Fernlet/Tests/FernletTests/SecureEnclaveWrapTests.swift` | Secure-Enclave content-key wrap tests: round trip, key-death on delete, lock-service maintain/repair without ever blocking unlock, reset removing the enclave key; both SE-present and SE-absent branches. |
| `Fernlet/Tests/FernletTests/KeyCustodyBoundaryTests.swift` | Key-custody tripwire: reads real keychain rows' accessibility/synchronizable attributes back per store, plus grep-walls confining `synchronizable: true` and bare non-ThisDeviceOnly accessibility classes to the two sanctioned files. |
| `Fernlet/Tests/FernletTests/SealedBackupFormatPinTests.swift` | Pins the sealed-backup at-rest format end-to-end: escrow HKDF known-answer via a planted key, the AAD v2 byte layout opened independently, and the restore-needs-only-the-escrow-key guard. |
| `Fernlet/Tests/FernletTests/FernletTestHelpers.swift` | Shared test helper utilities. |
| `Fernlet/Tests/FernletTests/ActivityTypeCatalogTests.swift` | Activity type taxonomy, search, and HealthKit mapping tests. |
| `Fernlet/Tests/FernletTests/MoveRefactorTests.swift` | Move refactor taxonomy tests for workout modes, muscle groups, body regions, and equipment. |
| `Fernlet/Tests/FernletTests/HealthKitWorkoutTests.swift` | HealthKit workout logging configuration, duration default, and metadata helper tests. |
| `Fernlet/Tests/FernletTests/HealthKitDisableTests.swift` | Tests for disabling individual HealthKit capabilities via `StoragePreferences`. |
| `Fernlet/Tests/FernletTests/NutritionLabelScannerTests.swift` | Nutrition label scanner tests. |
| `Fernlet/Tests/FernletTests/PeriodTrackerTests.swift` | Period tracker domain tests. |
| `Fernlet/Tests/FernletTests/CyclePredictionEngineTests.swift` | Cycle prediction accuracy, edge cases, and confidence scoring tests. |
| `Fernlet/Tests/FernletTests/CloudKitDataServiceTests.swift` | CloudKit data service and existing-data detection tests. |
| `Fernlet/Tests/FernletTests/StoragePreferencesTests.swift` | Storage preferences model encoding, defaults, and mutation tests. |
| `Fernlet/Tests/FernletTests/StoragePrivacyIntegrationTests.swift` | Integration tests for storage preference changes propagating through the app. |
| `Fernlet/Tests/FernletTests/IdentityServiceTests.swift` | Ed25519/X25519 identity provisioning, signing, and key-agreement tests. |
| `Fernlet/Tests/FernletTests/IdentityProvisioningReadTests.swift` | F-1 (2026-09-06): the fail-closed identity read — `classifyDeviceIdentityRows`'s decision table (an unreadable row wins over everything, including the Case-3 trap of signing absent + key-agreement unreadable), the one keychain-drivable path (a present-but-unparseable row is minted over and named first), the adopt-not-remint common case, and the wall that neither identity private-key row is ever read nil-collapsing again. |
| `Fernlet/Tests/FernletTests/CIGateSelectorBoundaryTests.swift` | The static half of the CI non-vacuity guard (P5 review finding 4): every suite named on a `Scripts/run-gated-suites.sh` line in `s3-wall.yml` must be a declared top-level type in `Tests/FernletTests`, every `MeshP<n>*AcceptanceTests` / convergence suite declared in the tree must be named there, no step may bypass the floor script, and the suite gates itself. Wall-load-bearing; in CODEOWNERS. **2026-09-21 (item 2a):** gains `theSimulatorDestinationIsPinnedWithNoFallback` and the `stepBody(named:in:)` parser it reads the workflow's destination step with — the one device literal, the `FERNLET_DESTINATION` export, a non-zero exit, no `::warning::`/fallback in the step's live lines, and the same canonical destination string in the two scripts' defaults and the UI probe's failure text. |
| `Fernlet/Tests/FernletTests/BackgroundRefreshBoundaryTests.swift` | **P10 item 2** (plan §16.4): the background-refresh import wall — the mechanical half of §17.2's "never: mesh, HealthKit, CloudKit force-sync, Foundation Models, store creation". Walks `App/Fernlet/CompanionRefresh/` by DIRECTORY rather than by file name (P9's lesson: an anchor needle over four hand-listed files is a wall over four files), so items 3 and 4 are covered before their files exist. Its claims: a module allowlist of six names, with every one of the 25 FernletKit targets classified forbidden-or-permitted-with-a-reason and a walk of `Package.swift` that reds when a new module has no disposition; an 84-spelling call zero-list — the mesh/HealthKit/CloudKit/FoundationModels types, the ten radio verbs `ProximityRunSeamsTests` counts, the store-creation spellings, the `internal` `FernletStore` doors a second file in the app target can actually speak, and (added by item 3's verify) the 15-row clock-and-persistence family, which is where "schedule at handle + background, never on a timer" and "no new persisted surface" are actually enforced: the scheduling suite had carried those needles over three hand-named paths, which is a wall over three files rather than over the directory item 4's handler lands in; the identifier as a positive needle, exactly once, as code; a floor on the walk; and two drift cells — the ten copied radio verbs against the spellings that file still speaks, and all 37 app-declared needles against the declarations `App/Fernlet/` actually makes — so a rename cannot leave a needle nothing can contain. Item 4's verify fixes RETIRED one: `attachedHealthKitService` had zero production callers (four test assertions and nothing else), so a door that hands out the HealthKit gateway to any file able to name the store is gone from `FernletStore` and `HealthSyncCoordinator` together with its needle, and the three read-only doors the handler gained in its place — `hasCompleteScoringContext`, `publishedWidgetSnapshot()`, `publishedWidgetSnapshotIsForCurrentDay` — are on the PERMITTED list with the argument for each. The call list is the half that holds: the app target is one module, so `store.applyProximityRunPolicy(…)`, `store.meshNetworkManager`, `store.refreshWorkoutsFromHealth()` and `FernletStore.load(…)` all reach the prohibited thing with no import line at all, and an import-only wall is green over every one of them. ACCESS LEVEL is the second gate: the list's first shape named only the PRIVATE funnels (`runProximityPolicy`, `applyProximityRunPolicyFromView`, `healthSyncCoordinator`), which no second file can speak, and the verify caught it passing a probe that spoke the reachable ones — those three are kept as belt behind the internal braces. The line matcher keeps string INTERPOLATION bodies, because `"policy: \(store.reapplyProximityRunPolicy())"` runs the funnel exactly as the bare call does, and a table cell pins the stripper input-by-input. Reuses `S3BoundaryTests.importedModules` / `.containsAtIdentifierBoundary` so the two walls cannot disagree. Wall-load-bearing; gated on the `s3-grep` CI step. |
| `Fernlet/Tests/FernletTests/CompanionRefreshSchedulingTests.swift` | **P10 item 3**: the refresh scheduling seam driven end to end through two fakes, because nothing can make iOS deliver a `BGAppRefreshTask` to a unit test and plan §27.2 is explicit that the Simulator's answer to an app-refresh SUBMISSION is an open measurement rather than the continued-processing path's known `BGTaskSchedulerErrorDomain` 1. Claims the app's half: one registration with exactly the frozen identifier (a drift from `Info.plist` is an error nowhere — iOS just stops delivering); the request built to the 15-minute policy; the chain re-submitting on every handler run, read AT the completion so the order is pinned and not just the count; a refused registration and a refused submission each REPORTED, never swallowed, because an audit line is the only observable a dead chain ever produces; the pending-slot rule in both directions (three edges with no delivery ask ONCE, at the first edge's floor; the tail's request silences the next edge; a refused tail leaves nothing pending and the next edge asks again); the R2 EDGE bound, driven through refusals because with the slot rule that is the only shape an edge storm has, and its twin — the tail re-submitting through 65 deliveries with the edge budget spent; and **exactly-once completion as a five-row table** in P8's oracle shape, covering the expiry door and the system's own installed handler each once and twice, with the sixth order (an expiration landing while the task is still HELD, before the tail) as its own cell because it needs the re-entrant fake. Plus the boundary pins: the mesh's two protocols gained no member — their brace-matched bodies are FROZEN as normalised strings, because the first shape counted the word `func` and stayed green over a `var` requirement — `BGAppRefreshTask` has one home and `BGContinuedProcessingTask` is not it, the clock-and-persistence family still exists on `BackgroundRefreshBoundaryTests` and that wall's walk really reaches this seam's files, and the parsed `Info.plist` carries both the identifier and the `fetch` mode without having replaced the mesh's wildcard. The frozen identifier is asserted against the LITERAL `MBO.Fernlet.companion-refresh` rather than against `CompanionRefresh.taskIdentifier`, which put the constant on both sides of an `==` and could not fail. The `deliveryAbsorbed` arm is unreachable through the ordinary item-3 path (the handler completes synchronously) and is driven re-entrantly rather than shipped untested. What only a device can prove is named and claimed nowhere. Gated on the `s3-grep` CI step. **2026-09-21 (item 2c):** gains `aRefusedRegistrationSpendsNoEdgeBudget`, driven one past the cap so the cell is about the sixty-fifth audit line, not the count. |
| `Fernlet/Tests/FernletTests/CompanionRefreshPipelineTests.swift` | **P10 item 4**: the refresh handler driven end to end — the step order as a whole trace (a publish that ran before the day roll would publish yesterday under today's key, and per-step booleans cannot tell that story), the reload decision, every failure arm, the outcome→`setTaskCompleted(success:)` table derived from `CaseIterable` so a ninth outcome cannot ship without a stated answer, and the two cancellation boundaries driven from INSIDE a chosen step rather than by racing a yield against one. The last section runs `CompanionRefreshWiring.steps(for:)` — the five production expressions, byte for byte — against a real test store, because a suite of fakes would be equally green if `rollDay` were bound to something that did not roll the day; what is left untested there is `FernletStoreAccess.shared.load()` itself. It also carries the ACQUIRE path's own cells (decision D-10.4.1): a store built without a gateway takes one later, a store that has one refuses a second however often it is asked, and an attach arriving after the workout sync was built is refused rather than leaving the sync on one gateway while the store claims another — all three asserted through `attachHealthKitServiceIfMissing(_:)`'s own `Bool` since item 4's verify retired the test-only `attachedHealthKitService` reader. That verify also added the refusal and day-boundary cells: either adjustment switched on without its bridge ends the run before the roll (D-10.4.6), both off publishes because the identity IS the app's number, an undrained widget row on a NEW day runs the whole tail (D-10.4.8), and a warm midnight roll that republished through the store's own mirror still names that reload in its trace. Installs no `FernletAuditLog` capture anywhere: the registry is process-global and accumulates across parallel suites, and `CompanionRefreshSchedulingTests` already captures the same prefix — everything here is asserted as a VALUE instead, which is why the pipeline returns one. Gated on the `s3-grep` CI step. |
| `Fernlet/Tests/FernletTests/WidgetSnapshotContentEqualityTests.swift` | **P10 item 4**: the content/metadata split "reload timelines only on change" rests on. The trap it exists for: `WidgetSnapshot` is `Equatable` by synthesis and the synthesis includes `computedAt`, stamped `Date()` at every construction — so `==` is FALSE between two snapshots of an unchanged day a millisecond apart, and a handler that diffed with `==` would reload the widget every fifteen minutes forever with a green suite behind it. One cell proves the two notions are different in both directions at once; a flip table covers every content field and a separate cell pins that the table is WHOLE; a `Mirror` field-count pin reds when a field is added to the struct without being classified — the failure a hand-written comparison always has and never announces. The metadata claim is made MECHANICALLY rather than in prose: `computedAt` is counted as code under `App/FernletWidgets` (three sites, all writes, all in the shared-model twin), so a family that starts drawing it reds here instead of leaving the refresh silently skipping reloads a person can see. The mirror's own decision is counted through the `reloadTimelines` closure the type already injects, and a last cell pins that the unconditional `publish(_:)` path still reloads every time. Gated on the `s3-grep` CI step. |
| `Fernlet/Tests/FernletTests/MeshRoutedRefusalBudgetTests.swift` | P5 review finding 3: `MeshRoutedRefusalBudget`'s decision table (reported once at the cap, fail-closed full axis, floored arguments, derived defaults), one drain cell through the real dispatch door (a courier repeating `unknownItemNotFromOrigin` is charged per refusal, reported once, then dropped unread — even its valid manifest — while another member still lands), the under-cap honest case, and the wall that every content-door refusal exit goes through the one charging door while the digest doors stay outside by name. |
| `Fernlet/Tests/FernletTests/MeshRoutedHeartTests.swift` | **P6 item 6**: three suites — `MeshRoutedHeartBodyTests` (the header-only framing, its four hostile shapes, the day-key and name refusals, the cap formula and its measured 2x header allowance), `MeshRoutedHeartTargetTests` (the `.singleRecipient` flip, the subset capture door's two named refusals, the frozen refusal vocabulary, a one-destination map restoring) and `MeshRoutedHeartCeremonyTests` (the ack ceremony end to end on a real two-node `MeshFoundingRig`: delivered and judged exactly once by the per-gift witness, closeness fed on first acceptance only, custodied-then-delivered with no live slot, a backgrounded recipient deferring, hearts-off healing on a flip, the receive cooldown healing after its window, not-a-friend and blocked FINAL and marked, the heart token absent from the projection list, and the batch-counter-vs-per-gift separation). The file header is explicit that tier 1 proves the CEREMONY while item 10's two-session lane is the only honest proof of the FEATURE — the mutual trust-vault rows a mesh heart needs cannot exist inside the session that produced it. |
| `Fernlet/Tests/FernletTests/RoutedShareRefusalCopyTests.swift` | P5 review finding 5: every `MeshRoutedShareRefusal` case has `LocalizedStringKey` copy, the causes a user can act on differently read differently, the tokens are frozen, and the package composes no sentence while the camera forks the token. |
| `Fernlet/Tests/FernletTests/FernletIdentityEnvelopeTests.swift` | Envelope signing, verification, canonical encoding, and payload type tests. |
| `Fernlet/Tests/FernletTests/NearbyRangingSessionTests.swift` | NearbyInteraction ranging session state machine and distance update tests. |
| `Fernlet/Tests/FernletTests/ConnectionInspectorTests.swift` | Connection inspector session recording, subsampling, and purge-old logic tests. |
| `Fernlet/Tests/FernletTests/ProximityCoordinatorTests.swift` | Proximity coordinator handshake, payload dispatch, and error recovery tests. |
| `Fernlet/Tests/FernletTests/TrainerProximityServiceTests.swift` | Trainer proximity service disclosure card and audit event tests. |
| `Fernlet/Tests/FernletTests/FriendPhotoManifestPayloadTests.swift` | Friend-photo manifest payload round-trip and blocked-sender request filtering tests. |
| `Fernlet/Tests/FernletTests/RecipeShareCodecTests.swift` | Recipe share codec encoding and round-trip tests. |
| `Fernlet/Tests/FernletTests/RecipeShareOverQUICTests.swift` | **P9 item 3 pass 2**: the recipe radio on QUIC — the posture that goes on the air (fresh name, certificate and `sid` per radio; the owner's fields beside the radio's own `sid`; every TXT entry inside DNS-SD's ceiling), the dial hello as a golden canonical frame plus its whole rejection matrix in both halves (the decoder's malformed shapes; the radio's unwired-owner, self-`sid`, unknown-`sid` and owner-declined refusals), the pause/resume contract (a resume re-mints the WHOLE posture; a pause is refused on a radio that never started; **`stop()` clears the radio's own pause flag**, which is what makes the gate's three `.unchanged` rows true), the dial miss that refuses the dial and not the radio, glare collapsing identically on both devices and the cap refusing a second device, the connecting window, the per-transfer stream (a picture recipe earns one and gives it back, a text recipe does not, the inbound budget is bounded, an unknown tunnel is refused, an oversized frame never leaves), the channel round-trip and its weak back-edge, the manager over the seam (a start failure stands it down; a dial claims the radio's CURRENT `sid`; a dialer resolves by `sid` and our own echo never reaches the picker), the per-send token attributing a completion to the send that began it, and the two rule-7 gates — the cancellation guard's source order, and the retirement scan that proves pass 2 ran. Carries `FakeRecipeShareRadioSession`, the in-memory seam conformer the other recipe suites drive the manager through. |
| `Fernlet/Tests/FernletTests/RecipeShareTransferTests.swift` | **P9 item 3 pass 1**: the recipe radio's pause/resume table over the whole cross-product of (`isRunning`, `isPaused`, `connectionCount`) × five events against an independently written expectation, the two rules it exists to freeze on their own (a stopped radio never resumes; a second pairing held keeps it closed), the exchange's transition table cell by cell including every refusal, every terminal phase refusing every phase event, **the pass-1 row** (a `discoveryPaused`/`discoveryResumed` in each of six phases leaves phase, byte count and completion count untouched), the exactly-once completion oracle checked after EVERY event of all 2 401 four-event walks (so every sequence of length ≤ 4 is covered, not just the end state), the route projection splitting at `MeshTransferStreamTable.bulkFloorBytes`, the advertised name's byte bound (24 CJK Characters overflow 64 bytes; the trim is grapheme-safe and UTF-8 round-trips), the one name that cannot be published at all (a single grapheme under 40 combining marks — the key is omitted, and the receiver falls back to the peer hint on absent and empty alike) beside an ordinary name that still rides the wire, the record's seed from the radio at the mint, the manager's wiring through the production helpers with no radio started, and two rule-7 source needles for pass 2 — `session.pauseDiscovery()` and `session.resumeDiscovery()` appear exactly once each AND only inside `applyDiscoveryGate`'s own brace-matched body (containment, so a relocation reddens), and no Character cap has come back beside the advertised name. |
| `Fernlet/Tests/FernletTests/FernletSnapshotRoundTripTests.swift` | Snapshot serialization round-trip integrity tests. |
| `Fernlet/Tests/FernletTests/ProximityTrustVaultTests.swift` | ProximityTrustVault trust/revoke idempotency, audit ring-buffer cap, onChange callback, and apply atomicity tests. |
| `Fernlet/Tests/FernletTests/AIRetryQueueServiceTests.swift` | AIRetryQueueService queue, clear, apply, reset, and onChange callback tests. |
| `Fernlet/Tests/FernletTests/DerivedSignalsServiceTests.swift` | DerivedSignalsService rebuild accuracy and deferred-rebuild single-run guard tests. |
| `Fernlet/Tests/FernletTests/DerivedSignalsRebuilderTests.swift` | Derived signals rebuilder accuracy and day-window boundary tests. |
| `Fernlet/Tests/FernletTests/MeshNetworkManagerTests.swift` | Mesh network manager discovery visibility, session open/closed transitions, slot allocation, peer admission, lifecycle, session-photo retention, and film-quota tests (10-shot limit, `filmRemaining`, `leaveSession` reset). |
| `Fernlet/Tests/FernletTests/DisposableCameraControllerTests.swift` | Pure state-machine unit tests for `CameraCaptureController`: initial armed state, disarm, wind progress, arm-on-full-wind, re-arm cycle, and no-op guards. |
| `Fernlet/Tests/FernletTests/MealBuilderTests.swift` | Meal builder food plan to meal/recipe conversion tests. |
| `Fernlet/Tests/FernletTests/WholeDescriptionFoodProbeTests.swift` | Whole-description probe floor calibration, full corpus/resolver-bank fired-population pins, portion/count boundaries, retailer disclosure, and real quick-log short-circuit tests. |
| `Fernlet/Tests/FernletTests/SavedRecipeServiceTests.swift` | Saved recipe service load, add, delete, and persistence tests. |
| `Fernlet/Tests/FernletTests/SnapshotSaveCoordinatorTests.swift` | Snapshot save coordinator debounce and remote-reload tests. |
| `Fernlet/Tests/FernletTests/WorkoutHealthKitSyncTests.swift` | HealthKit workout import and upsert logic tests. |
| `Fernlet/Tests/FernletTests/FoodCatalogTests.swift` | SQLite food catalog round-trip/hydration, search parity with the in-memory scorer, user-item merging, and the gated `FoodCatalog.sqlite` regeneration test. |
| `Fernlet/Tests/FernletTests/CustomIngredientUpsertTests.swift` | Custom ingredient resolution and food item creation tests. |
| `Fernlet/Tests/FernletTests/AIContextPayloadTests.swift` | AI context payload de-identification: forbidden and allowlisted field tests. |
| `Fernlet/Tests/FernletTests/ActivityTests.swift` | Proximity group-activity token/snapshot crypto and join-token verification tests. |
| `Fernlet/Tests/FernletTests/AppIntentsTests.swift` | App Intents pending-sheet deep-link round-trip, consume-once, and expiry-window tests. `@Suite(.serialized)` since 2026-09-21 (plan §17.2.3 finding 5): the eleven cells share one process-global `UserDefaults.standard` token, and `init`/`deinit` draining is hermetic against the cell before, not the cell beside. |
| `Fernlet/Tests/FernletTests/BarcodeScanTests.swift` | Barcode scan GTIN normalization, schema-tolerance, and resolution-cascade tests. |
| `Fernlet/Tests/FernletTests/BrandedODRCatalogTests.swift` | Branded On-Demand-Resource food catalog attach, resolve, and detach-fallback integration tests. |
| `Fernlet/Tests/FernletTests/ClosenessTests.swift` | Friend closeness day-point weighting and closeness-formula tests. |
| `Fernlet/Tests/FernletTests/ClothingShareCodecTests.swift` | Clothing-share codec catalog-building and untrusted-catalog sanitization tests. |
| `Fernlet/Tests/FernletTests/CoinEconomyTests.swift` | Coin-economy ledger aggregation, idempotent earn/spend, and store-wiring tests. |
| `Fernlet/Tests/FernletTests/CommitResolutionPersistenceTests.swift` | Regression test for meal-resolution recipe creation persisting without accompanying meals. |
| `Fernlet/Tests/FernletTests/CreationStudioEditorTests.swift` | Creation Studio item-editor texture reprojection and dimension round-trip tests. |
| `Fernlet/Tests/FernletTests/CustomItemModelTests.swift` | Custom clothing-item texture helpers, Codable round-trip, and CRUD persistence tests. |
| `Fernlet/Tests/FernletTests/CyclePhaseResolverTests.swift` | Cycle-phase resolver tests deriving phase from entries, predictions, and calendar math. |
| `Fernlet/Tests/FernletTests/DataExportTests.swift` | Data-export shape round-trip and sensitive-vocabulary exclusion tests. |
| `Fernlet/Tests/FernletTests/DayDecodeCompatTests.swift` | Forward-compatibility tests for unknown enum tokens in day-resident fields. |
| `Fernlet/Tests/FernletTests/DayHistoryUncappedTests.swift` | Day-history uncapped per-row storage and read-cache invalidation tests. |
| `Fernlet/Tests/FernletTests/DayRecordRepositoryTests.swift` | DayRecordRepository upsert/load round-trip and non-destructive-upsert tests. |
| `Fernlet/Tests/FernletTests/PersistenceFailureAuditTests.swift` | One cell per environmental persistence-failure site (day rows, the append-only row engine, coin/milestone/custom-item, saved recipes and their legacy JSON file, the Core Data blob, the local blob, the past-day write): the real code path is driven against a store that genuinely throws — a seeded store re-attached read-only, a database file overwritten underneath the open connection, a read-only directory, a non-finite `Double` — and each cell asserts the call ran to completion and the audit token was emitted. |
| `Fernlet/Tests/FernletTests/DayRowMigrationTests.swift` | Day-row migration tests moving legacy blob days into the per-row store. |
| `Fernlet/Tests/FernletTests/DeleteAllDataTests.swift` | "Delete all data" reset tests verifying day history and inbox are fully purged — and, since P6 item 4's fix review, that the live chat transcript goes through the MANAGER's clear funnel rather than around it: the generation moves with it and the routed projection is switched back on by the funnel's `defer`. An empty transcript alone is what BOTH shapes produce, so those two are the load-bearing assertions. |
| `Fernlet/Tests/FernletTests/DestructiveConfirmationTests.swift` | Destructive-action confirmation tests verifying mutation is deferred until confirm. |
| `Fernlet/Tests/FernletTests/EquipmentIconTests.swift` | Equipment/location SVG icon markup parsing and path-bounds sanity tests. |
| `Fernlet/Tests/FernletTests/FernletFontRegistrationTests.swift` | Bundled font registration and text-role-to-font resolution tests. |
| `Fernlet/Tests/FernletTests/FoodProductWebImportTests.swift` | Food-product web-import structured-data (schema.org) nutrition extraction tests. |
| `Fernlet/Tests/FernletTests/FoodSearchLabelAndFallbackTests.swift` | Food-search data-source labeling and micronutrient-fallback tests. |
| `Fernlet/Tests/FernletTests/FriendMintingTests.swift` | One-sided friend-minting session-roster, review-batch, and vouch-list-gate tests. |
| `Fernlet/Tests/FernletTests/FriendShopTests.swift` | In-person friend-shop buy-flow, listing-cap, and ephemeral-catalog tests. |
| `Fernlet/Tests/FernletTests/FriendStateTests.swift` | Friend companion-state fuzzy-fold and constant-length wire-payload contract tests. |
| `Fernlet/Tests/FernletTests/GentleOffersTests.swift` | Gentle-intervention offer-engine gating/rotation, weather-comfort, and HealthKit write-gating tests. |
| `Fernlet/Tests/FernletTests/GoalSelectionPersistenceTests.swift` | Goal-selection persistence regression test (`setSelectedGoal` schedules a save and survives reload). |
| `Fernlet/Tests/FernletTests/GuidedWorkoutRunStoreTests.swift` | Guided-workout-run store approval-gate, start/finish/log, and Live-Activity mirroring tests. |
| `Fernlet/Tests/FernletTests/HealthKitCacheCleanerTests.swift` | HealthKit cache-cleaner opt-out purge tests preserving user-authored sleep entries; the device-cache clear (and its fail-closed failure) and the launch scrub's capture mode. |
| `Fernlet/Tests/FernletTests/HealthKitCloudBoundaryTests.swift` | "HealthKit information is not stored in iCloud": field-classification pins for the health-bearing types, sentinel checks that the sanitize boundary's output carries no HealthKit value (and keeps what the user typed), and the device-local residue round trip through relaunch, past-day scoring, past-day edits, Apple Health imports, the reset, and the legacy scrub. |
| `Fernlet/Tests/FernletTests/DeviceHealthResidueStoreTests.swift` | The file-backed HealthKit residue cache: persistence across instances, the day bound, backup exclusion, clear, undecodable-file replacement, and residue normalization. |
| `Fernlet/Tests/FernletTests/HealthImportedBodyProfileTests.swift` | The HealthKit body-profile import stays on the device: never in the in-memory or persisted synced settings; a typed edit still syncs without carrying a merely-shown Health value; the overlay feeds targets, the profile screen and the period-visibility default; it survives a relaunch on this device only; the wipe and the opt-out clear it; the file cache keeps it (and still decodes a pre-profile file); the bridge keeps only supplied fields within bounds. |
| `Fernlet/Tests/FernletTests/WorkoutHealthAccessOfferTests.swift` | The first-workout Health offer end to end through `FernletStore` over the real `HealthKitService` and a recording seam: the first log asks once and its workout reaches Health on a grant, the second never asks; a decline stays off, writes nothing and is never re-asked; an explicit Settings off, an answer HealthKit already holds, and an earlier ledgered ask are never prompted; guided start, planned completion and a batch log all route through it; the wipe clears the fact; an unwired store never asks. |
| `Fernlet/Tests/FernletTests/HealthKitWriteGateTests.swift` | "With Fernlet's Health switch off, Fernlet writes nothing to Apple Health" (2026-09-23, DPA-118): every write kind (workout, height/weight, mindful, intimacy, cycle, generic save) × each closed switch through the REAL `HealthKitService` over a recording store seam, the positive controls, mixed and never-shared batches, the type→capability map, the deliberately ungated own-sample delete, the period-edit pre-check, the store/sync/view-model quiet checks, and a source scan proving the two gated doors are the only way into HealthKit. |
| `Fernlet/Tests/FernletTests/HealthKitScoringTests.swift` | HealthKit-aware scoring-engine tests verifying backward-compatible score computation. |
| `Fernlet/Tests/FernletTests/HeartShareTests.swift` | Presence-layer heart-share codec, trust-gate, rate-limit, and glow-decay tests. |
| `Fernlet/Tests/FernletTests/IdentityServiceEscrowTests.swift` | Identity-service backup-escrow-key provisioning-race and conflict-reconciliation tests. |
| `Fernlet/Tests/FernletTests/IslandViewfinderMetricsTests.swift` | Dynamic Island viewfinder geometry and device-classification tests. |
| `Fernlet/Tests/FernletTests/ItemNameModerationTests.swift` | Clothing-listing item-name moderation and sanitization tests. |
| `Fernlet/Tests/FernletTests/JournalNarrativeRepositoryTests.swift` | Sealed journal-narrative repository insert/fetch encryption round-trip tests. |
| `Fernlet/Tests/FernletTests/JournalQuickMoodTests.swift` | Quick-mood tag-only journal-entry and prompt-rotation tests. |
| `Fernlet/Tests/FernletTests/KnownDesignerNamesBoundTests.swift` | Known-designer-names map cardinality-bound regression test against hostile peer cycling. |
| `Fernlet/Tests/FernletTests/LogRecordsDecodeCompatTests.swift` | Forward-compatibility tests for derived log-record unknown-enum decoding. |
| `Fernlet/Tests/FernletTests/MacroRingProgressTests.swift` | Macro-ring progress NaN/Infinity guard tests for zero or degenerate goals. |
| `Fernlet/Tests/FernletTests/MealPhotoRecognitionTests.swift` | Meal-photo recognition taxonomy-filtering and resolveMeals-pipeline tests. |
| `Fernlet/Tests/FernletTests/MealPhotoStoreTests.swift` | Meal-photo store at-rest encryption, downscaling, and fail-closed-without-key tests. |
| `Fernlet/Tests/FernletTests/MemoryStorageScreeningTests.swift` | Memory-storage diagnostic-language screening classifier (storage-time gate) tests. |
| `Fernlet/Tests/FernletTests/MeshClothingShopTests.swift` | Mesh friend-clothing-shop catalog lifecycle, exchange, and hostile-input guard tests. |
| `Fernlet/Tests/FernletTests/MeshEncryptionTests.swift` | Mesh group symmetric-encryption (group key) unit tests. |
| `Fernlet/Tests/FernletTests/MilestoneLedgerTests.swift` | Milestone-ledger aggregation, exactly-once coin-award, and reset tests (since 2026-08-20 `resetAll` clears the ledger; the old reset-survival rule is reversed). |
| `Fernlet/Tests/FernletTests/MilestoneResetBoundaryTests.swift` | The milestone `resetBoundary` marker (2026-08-21): re-synced pre-wipe rows raise no count and re-mint no award; the marker is never counted, displayed or awarded; frozen-rawValue round-trip + per-row unknown-kind drop; service seam (record refusal, failed-append retry). |
| `Fernlet/Tests/FernletTests/ModerationBanTests.swift` | Moderation self-ban store default-state and reinstall-persistence tests. |
| `Fernlet/Tests/FernletTests/ModerationTests.swift` | Moderation content-hash stability and local report-ledger idempotency tests. |
| `Fernlet/Tests/FernletTests/MultiDeviceSyncWarningTests.swift` | Multi-device sync-warning classification tests across iCloud/sync-state combinations. |
| `Fernlet/Tests/FernletTests/NutritionTargetOverrideTests.swift` | Nutrition-target-override derivation and settings round-trip tests. |
| `Fernlet/Tests/FernletTests/PastDayJournalScrubMigrationTests.swift` | Past-day journal scrub-migration retry regression test for failed sealing passes. |
| `Fernlet/Tests/FernletTests/PastDayJournalSealingTests.swift` | Past-day journal sealing tests ensuring no plaintext leaks into the synced blob. |
| `Fernlet/Tests/FernletTests/PeriodAwareScoringTests.swift` | Period-aware scoring adjustment and backward-compatibility tests. |
| `Fernlet/Tests/FernletTests/PeriodContextBridgeTests.swift` | Period-context bridge phase-signal derivation and 3-cycle-gate tests. |
| `Fernlet/Tests/FernletTests/PeriodPhaseTrendEngineTests.swift` | Period phase-trend correlation-engine minimum-cycle-gate and trend-detection tests. |
| `Fernlet/Tests/FernletTests/PeriodPredictionUITests.swift` | Period-prediction UI visibility-gating (`hidePredictions` setting) tests. |
| `Fernlet/Tests/FernletTests/PeriodTestSupport.swift` | Shared deterministic fixtures and scoring-context stub used by the period-aware test suites. |
| `Fernlet/Tests/FernletTests/PetInteractionGovernorTests.swift` | Pet-interaction time-lock governor and ambience pure-function tests. |
| `Fernlet/Tests/FernletTests/PhotowallPhotoSelectorTests.swift` | Photowall photo-selector next-launch dedup and ranking-strategy tests. |
| `Fernlet/Tests/FernletTests/PresenceEpochPostureTests.swift` | **P9 item 2 pass 1**: `PresenceEpochPosture`'s rotation table (epoch start, mid, 899.999, 900, 900.001, 1800, a wrap 100 000 epochs ahead) — name and certificate stable inside an epoch, both replaced at every boundary — plus the exact non-derivability pair (fixed entropy across epochs gives the SAME name, so nothing in it comes from the epoch; the same epoch with different entropy gives different names), the name's frozen-token-plus-stated-entropy shape, the refusal of a short entropy draw, the certificate's shared `fernlet-mesh` subject, the epoch-start anchoring of the certificate's validity window (two postures minted at different instants inside one epoch carry byte-identical validity DER), and two source grep-walls: no second 900 and no clock of its own, and no device byte (`UIDevice`, hostname, fingerprint, keychain, MCPeerID, …) or disk writer anywhere in the value. |
| `Fernlet/Tests/FernletTests/PresenceHeartsTests.swift` | Presence-layer heart reachability, invitation-gate, and opt-out tests. |
| `Fernlet/Tests/FernletTests/PresenceManagerTests.swift` | Presence-manager radio state-machine roster, advertise-cap, and epoch-rotation tests, plus the posture seam (the epoch tick and a boundary-crossing roster refresh rotate name + identity together; `stop()` drops the posture entirely; and a failing mint costs ONE attempt and ONE audit row per epoch, not one per `refreshRoster()`). |
| `Fernlet/Tests/FernletTests/PresenceTagTests.swift` | Presence-tag Diffie-Hellman primitive mutual-derivation and epoch-rotation tests. |
| `Fernlet/Tests/FernletTests/PrivateEncryptedRepositoryRecoveryTests.swift` | Private encrypted-repository recovery tests skipping rows keyed by a destroyed key. |
| `Fernlet/Tests/FernletTests/PrivateHistoryPruningTests.swift` | Private-history deletion/pruning tests verifying no residual record remains. |
| `Fernlet/Tests/FernletTests/PrivateMediaStoreTests.swift` | Private media-store (mesh photo cache) decompression-bomb defense and at-rest encryption tests. |
| `Fernlet/Tests/FernletTests/ProgressPhotoStoreTests.swift` | Progress-photo store at-rest encryption and fail-closed-without-key tests. |
| `Fernlet/Tests/FernletTests/ProgressPhotoLockNudgeTests.swift` | The progress-photo lock-setup nudge (2026-09-23): appears once for a deferred, no-lock user on first use ("Not now" retires it across a relaunch), honors the deferral key, never shows while the real `FernletLockService` has a lock, opens a setup that grants `.progressPhotos` only and clears the deferral, and never takes the capture control's place. |
| `Fernlet/Tests/FernletTests/ProximityRecipeShareCapTests.swift` | Proximity recipe-share two-device cap and radio pause/resume lifecycle tests. |
| `Fernlet/Tests/FernletTests/ProximityRecipeShareDiagnosticsTests.swift` | Proximity recipe-share diagnostics capped ring-buffer append/ordering tests. |
| `Fernlet/Tests/FernletTests/ProximityRecordDecodeCompatTests.swift` | Forward-compatibility tests for persisted proximity records (trust vault, audit, session logs). |
| `Fernlet/Tests/FernletTests/ProximityWireOffMainDecodeTests.swift` | Proximity wire-type Sendable conformance and off-main decode/signature-verification tests. |
| `Fernlet/Tests/FernletTests/RecentBitesTests.swift` | Home "Recent bites" 7-day photographed-meal window tests. |
| `Fernlet/Tests/FernletTests/RecipeWebImporterTests.swift` | Recipe web-importer SSRF/URL-safety allow- and block-list tests (including encoded IP-literal spellings), page-image extraction/normalization, and the image-download MIME/sniff/size guards. |
| `Fernlet/Tests/FernletTests/RecipeWebImageTests.swift` | Recipe web-image feature tests: tolerant `RecipeWebImport` decode, the per-device attempt + synced suppression gates (cancellation, photo-wins, deletion), mesh receive neutralization and the already-saved keep decision, and wire image compatibility/strips. |
| `Fernlet/Tests/FernletTests/RecipeSourceURLTests.swift` | `RecipeSourceURLMatcher` normalization table plus re-import contract tests (live-row merge, deleted-row nil, attempt re-arm, drain duplicate skip). |
| `Fernlet/Tests/FernletTests/ReplayCacheTests.swift` | Replay-cache oldest-eviction and envelope-replay-detection tests. |
| `Fernlet/Tests/FernletTests/SharedRecipeImportQueueMirrorTests.swift` | Drift guard for the share extension's hand-copied `SharedRecipeImportRecord` mirror: parses both source files and requires identical field lists (the `budgetDeferredDayKey` stripping regression), plus wire-format round-trip, `NSFileCoordinator` coordination, and container-fallback-order parity. |
| `Fernlet/Tests/FernletTests/CoreDataStagedBlobLoadTests.swift` | Parity coverage for `CoreDataFernletRepository`'s two aggregate-blob entry points: async/sync agreement, first-launch legacy migration, and read-only-recovery latching on fetch failure and corrupt payload via `loadSnapshotAsync`. |
| `Fernlet/Tests/FernletTests/JournalAppendPathTests.swift` | Pins the single journal-append path (`addJournal` overloads + `logQuickMood`): today updates day/`previousJournals`/memories, a back-dated entry touches none of the today-scoped state but persists and hydrates on its own day. |
| `Fernlet/Tests/FernletTests/JournalMemoryCaptureTests.swift` | Core Memory never holds journal text (owner decision 2026-09-23): every entry mints an emotion-only memory (its `FeelingTag` token, no text), and AI off, sleepy, resting, unavailable, failed or filtered leaves it at that; an on-device summary replaces the empty text only when `JournalMemorySummaryPolicy` accepts it against the full entry (never verbatim, a prefix or an excerpt; bounded; no diagnostic language) and the memory is still there, untouched, with AI still on; the persisted blob is checked too. The summarizer double routes through the store's real `FernletAIGate`. |
| `Fernlet/Tests/FernletTests/NoTrackingBoundaryTests.swift` | No-tracking wall: banned advertising/analytics SDKs and symbols across every target, exact SPM dependency + hardcoded-destination allowlists, pinned HTTP-client files, and PrivacyInfo.xcprivacy/entitlements assertions. See [No-Tracking-Wall.md](No-Tracking-Wall.md). |
| `Fernlet/Tests/FernletTests/S3BoundaryTests.swift` | S3 privacy-wall grep-based backstop tests scanning AI-facing sources for sealed-store access. |
| `Fernlet/Tests/FernletTests/MemoryLifecycleTests.swift` | Runtime half of the memory-lifecycle wall (2026-08-17 leak review): pins that `ObservationLoop` never pins its owner across the suspension, that the three proximity managers deallocate with or without `stop()`, that a store-owned mesh manager still releases with its beacon loop armed (HP2 — a host pin on a STORED task handle would be a cycle), that mesh slot eviction frees the evicted peer's transport link, that presence/recipe record drops run the coordinator's teardown (ranging + Live Activity anchor), that photo-wall preferences shrink with the cache, and that a local-only synced store prunes persistent history. See [Memory-Leak-Review-2026-08-17.md](Memory-Leak-Review-2026-08-17.md). |
| `Fernlet/Tests/FernletTests/MemoryLifecycleBoundaryTests.swift` | Static half of the memory-lifecycle wall — grep-wall over the five shipping roots (the Messages extension joined 2026-09-23): ML1 a file storing a `Task<…>` handle cancels it in a `deinit`/`isolated deinit` or carries an allowlisted invariant; ML2 a block-based `addObserver(forName:)` file removes its token or is allowlisted; ML3 `withObservationTracking` lives only in `ObservationLoop.swift`; ML4 a file holding an `unowned ProximityHost` may not contain an UNMARKED `Task` construction (every marker is `helper`/`timer — <reason>`/`exempt — <reason>` on the `Task` line, or the off-`Task` `scoped — <reason>` of an async callee's pin); ML5 a test may not build a proximity manager over an inline host expression (`MeshNetworkManager(store: makeTestStore(), …)` is a dangling `unowned` from birth). Floors + fixtures + fully-used-allowlist contract, like the other walls. |
| `Fernlet/Tests/FernletTests/PowerOfTenBoundaryTests.swift` | Power-of-10 grep-wall: the Swift port of `Scripts/power-of-10-scan.py` (function length, force unwraps/traps, swallowed `try?`, unbounded loops, mutable globals, `#if` policy, unsafe seams, density floor) plus the rule-10 build-setting pins. Reads the same allowlist. See [Power-of-10-Swift.md](Power-of-10-Swift.md). |
| `Fernlet/Tests/FernletTests/SavedRecipeMigrationTests.swift` | Legacy SavedRecipe-to-RecipeDefinition migration round-trip tests. |
| `Fernlet/Tests/FernletTests/SealedBackupChunkTests.swift` | Sealed-backup chunked-export paging and multi-chunk restore-writeback tests. |
| `Fernlet/Tests/FernletTests/SealedBackupRestoreOutcomeTests.swift` | Sealed-backup restore-outcome classification and status-recording tests. |
| `Fernlet/Tests/FernletTests/SealedBackupRestoreTests.swift` | Sealed-backup restore-into-stores tests (empty-store guard, the retired Tier-2 payload writing nothing back, locked-key path). |
| `Fernlet/Tests/FernletTests/SensitiveNotesRetirementTests.swift` | The retired sensitive-notes (Tier-2) sealed-backup payload (2026-09-23): enable refused before any key or upload, the launch sweep deleting a surviving iCloud copy quietly and idempotently (and keeping its marker when the delete fails), no restore, no toggle, and a coordinator that can no longer name Tier-2. |
| `Fernlet/Tests/FernletTests/TierTwoDeviceLocalTests.swift` | Tier-2 memories never in the CloudKit-mirrored blob or the local day-blob file (asserted on the written bytes, including the scrub of a pre-2026-09-23 record), living only in the backup-excluded device-local sidecar with first-seen dates and inactive history intact, and wiped by both purges and delete everything. |
| `Fernlet/Tests/FernletTests/SealedBackupTests.swift` | Sealed-backup crypto round-trip and escrow-key provisioning tests. |
| `Fernlet/Tests/FernletTests/SealedIntroductionTests.swift` | Sealed-introduction mesh handshake tests guarding against unsealed identity leakage. |
| `Fernlet/Tests/FernletTests/SealedStoreConfigTests.swift` | Sealed-store/CloudKit-model isolation tests keeping sensitive entities out of the synced model. |
| `Fernlet/Tests/FernletTests/AgeAssuranceTests.swift` | Age-gate tests: the pure record rules, the device-local store's persistence and fail-closed decode, and the wiring into the intimacy visibility gate, the mesh chat seams, and `resetAll`. |
| `Fernlet/Tests/FernletTests/OnboardingIntimacyChoiceTests.swift` | The intimacy opt-out after the onboarding age gate (2026-09-23), against `OnboardingCoordinatorModel`: the choice lands in the Settings setting through the Settings setter and survives a reload; the default is unchanged and a kept default is never written; all five closed-gate outcomes never see the choice or get written; an answer does not outlive a gate that closed; off hides (the gated store reads nothing) without deleting the sealed row. |
| `Fernlet/Tests/FernletTests/SensitiveSurfaceGateTests.swift` | Sensitive-surface (period/intimacy) hard hide-gate visibility tests. |
| `Fernlet/Tests/FernletTests/CaptureProtectionTests.swift` | Capture-friction tests: `CaptureProtectionState` transitions off the real notifications (screenshot pulse, per-screen capture re-read, once-per-session nudge claim + single VoiceOver announcement, pre-registration live-scene fallback, teardown without a retain cycle) plus view-level hosting of `captureProtected(surface:)` (opaque cover renders while captured / scene-inactive and clears, pixel-sampled; frontmost AND occlusion gating of the pulse; keyboard resign on cover arrival) and the `FernletLockGateOcclusion` truth table. |
| `Fernlet/Tests/FernletTests/SessionMessageTests.swift` | Live-session ephemeral message codec, dispatch-gate, and transcript-clearing tests. |
| `Fernlet/Tests/FernletTests/SettingsDecodeCompatTests.swift` | Forward-compatibility tests for unknown enum tokens in synced `FernletSettings` fields. |
| `Fernlet/Tests/FernletTests/SnapshotModelsDecodeCompatTests.swift` | Forward-compatibility tests for unknown enum tokens across remaining synced-blob model types. |
| `Fernlet/Tests/FernletTests/StressEngineTests.swift` | Stress-engine HRV z-score classification and scoring-modifier tests. |
| `Fernlet/Tests/FernletTests/TabBarCompactionTests.swift` | Tab-bar compaction hysteresis decision-logic tests. |
| `Fernlet/Tests/FernletTests/TrainerExportTests.swift` | Trainer/nutritionist export bundle fail-closed category-exclusion tests. |
| `Fernlet/Tests/FernletTests/WidgetBridgeTests.swift` | Widget-bridge snapshot mirroring and pending-action-queue codec tests. |
| `Fernlet/Tests/FernletTests/WorkoutProgramTests.swift` | Workout-program location identity-stability and template-ID collision tests. |
| `Fernlet/Tests/FernletTests/WorkoutRecoveryTests.swift` | Logged-workout removal/edit recoverability and bookkeeping-reversal tests. |
| `Fernlet/Tests/FernletTests/WorryBoxTests.swift` | Worry Box sealed repository round-trip, deletion, and key-migration tests. |
| `Fernlet/Tests/FernletTests/FernletModelRouterTests.swift` | Router resolution: capability caps, tier escalation ladders, and fallback-reason classification. |
| `Fernlet/Tests/FernletTests/FernletAIGateTests.swift` | Gate dispatch: the single quota-charge point, user intent, and route resolution. |
| `Fernlet/Tests/FernletTests/AICallQuotaTests.swift` | Daily AI-call budget: day-key rollover and the derived `.sleepy`/`.resting` states. |
| `Fernlet/Tests/FernletTests/AIAuditLogTests.swift` | AI audit entries: `modelIdentifier`/outcome fields and device-local persistence. |
| `Fernlet/Tests/FernletTests/HeartDropTests.swift` | Heart-drop core: sealing, prekey selection and consumption, tag rotation, outbox and dedup behavior. |
| `Fernlet/Tests/FernletTests/HeartDropAppWiringTests.swift` | App-side heart-drop wiring: opt-in gating, delivery paths, and wipe coverage. |
| `Fernlet/Tests/FernletTests/ProtectedSidecarTests.swift` | The sidecar state machine: absent/deferred/corrupt/loaded classification and the fail-closed persist policy. |
| `Fernlet/Tests/FernletTests/SealedPayloadFramingTests.swift` | wire2 compress+pad framing round-trips and capability gating. |
| `Fernlet/Tests/FernletTests/ProximityVerificationTests.swift` | QR ceremony: nonce binding, sign-after-check ordering, and wrong-peer drops that must not clear state. |
| `Fernlet/Tests/FernletTests/CoachSessionHardeningTests.swift` | Coach-path hardening as an executable contract: the role split, coach-vs-friend trust policy, the freeze-default `.trainer` guard, and the pre-decrypt wire-size gate. |
| `Fernlet/Tests/FernletTests/SealedBackupRollbackTests.swift` | Rollback defense: AAD binding (editing `generation`/`updatedAt` breaks authentication, sub-second timestamps still open) and the high-water mark (monotonic minting, per-payload independence, forward-only accept, wipe reset, and the authentic-older-generation rejection). |
| `Fernlet/Tests/FernletTests/PrivacyWipeCoverageTests.swift` | The enforced delete-all coverage checklist — identity and sidecar keys must die with the wipe. See [PrivacyWipeCoverage.md](PrivacyWipeCoverage.md). |
| `Fernlet/Tests/FernletTests/GroceryAggregationTests.swift` | Pure aggregation engine: unit merging, duplicate collapse, and unresolvable-line passthrough. |
| `Fernlet/Tests/FernletTests/GroceryListComposerTests.swift` | App-side composition: catalog resolution, "cook for N" scaling, and web-import line unification. |
| `Fernlet/Tests/FernletTests/RecipeScalingTests.swift` | Proportional scaling arithmetic and the non-persistence guarantee. |
| `Fernlet/Tests/FernletTests/IngredientSubstitutionTests.swift` | Substitution: candidate-number binding, gram-equivalence quantities, and fork-only-on-save. |
| `Fernlet/Tests/FernletTests/RecipeStepsTests.swift` | `RecipeStep` schema, web-import step preservation, and mesh wire compatibility. |
| `Fernlet/Tests/FernletTests/CookingRunStateTests.swift` | Cooking run value semantics: step numbering, last-step detection, and advance/finish. |
| `Fernlet/Tests/FernletTests/CookingRunStoreTests.swift` | App-group cooking-run persistence, including the injectable directory seam. |
| `Fernlet/Tests/FernletTests/SavedRecipePayloadMigrationTests.swift` | The `SavedRecipeRecord` typed-column → payload-blob migration that unblocked F3/F4/F5. |
| `Fernlet/Tests/FernletTests/MealDecompositionRecipeWireTests.swift` | Meal-decomposition recipe payloads over the wire. |
| `Fernlet/Tests/FernletTests/FDADailyValuesTests.swift` | Daily Value table values and their single-source coupling to the gap analyzer. |
| `Fernlet/Tests/FernletTests/NutrientSuggestionTests.swift` | The gap-filling nutrient nudge: curated-source binding and suppression rules. |
| `Fernlet/Tests/FernletTests/BarcodeServingStepTests.swift` | Serving-step prefill memory keyed by normalized GTIN, and its wipe. |
| `Fernlet/Tests/FernletTests/CloudKitSchemaDeployTests.swift` | Purity of the schema-deploy launch-flag parse. |
| `Fernlet/Tests/FernletTests/SettingsSearchIndexTests.swift` | Asserts every `SettingsRoute` case has at least one search entry and a destination. |
| `Fernlet/Tests/FernletTests/AdaptiveColorIsolationTests.swift` | Theme-token resolution isolation across trait changes. |
| `Fernlet/Tests/FernletTests/DisposableCameraOrientationTests.swift` | Camera landscape/rotation and Dynamic-Island-merge geometry. |
| `Fernlet/Tests/FernletTests/MeshSessionHeartTests.swift` | In-session hearts on the ROUTED store since **P6 item 6** — the sender half: the five gates it keeps (the opt-out, active-record-only, the ledger's loaded-ness as its OWN refusal, the cooldown, the in-flight claim), the three origination lines with a single-recipient audience, consume-on-stage, the frozen `SessionHeartFailure` vocabulary, addressability vs liveness as two questions, and the app's three-way presence ordering. Its whole legacy receive half went with the `.friendHeart` transport (`MeshRoutedDrainWallTests.theRetiredMeshHeartTransportIsGone` pins the symbols at zero); the ceremony that replaced it lives in `MeshRoutedHeartTests.swift`. |
| `Fernlet/Tests/FernletTests/SessionMessageUnreadTests.swift` | Mesh session message unread signalling — including that a row a GATE is filtering is HELD but never unread (item 4's fix review: an unread badge is an invitation to read something, and a filtered row is not something the user can open the panel and see). |
| `Fernlet/Tests/FernletTests/MeshFavoriteObservationTests.swift` | Favorite-peer observation and its effect on home weighting. |
| `Fernlet/Tests/FernletTests/MeshRoutedManifestTests.swift` | P5 item 1: `MeshRoutedManifestFixtures` + the two routed goldens (manifest canonical bytes and the wrap AAD, independently derived after reproducing the inventory and epoch-heads goldens), the signed-bytes-survive-the-wire and signature-excluded claims, the shape faults and both clamps, and the signing suite — mint from a `MeshDeliveryTarget`, verify through the one door, every tamper and refusal by name, relay forwards verbatim, departed origin still verifies while a quorum-removed origin and a forged admission key are refused by name, both doors floor the instants, hostile expiry never traps, signed-not-sealed with a `.tempMessage` control, replay-window smoke. |
| `Fernlet/Tests/FernletTests/MeshRecipientKeyWrapTests.swift` | P5 item 1: the per-recipient wrap's two directions with real keys — the addressed recipient opens, nobody else does, a relabelled wrap opens for neither party (the AAD at the key layer), no other binding opens it, fresh ephemeral + nonce per wrap, fixed widths, every refusal by name, and an unprovisioned identity surfacing `notProvisioned` rather than a failed open. |
| `Fernlet/Tests/FernletTests/MeshChunkTests.swift` | P5 item 2: `MeshChunkFixtures` and the four chunk goldens (the 185-byte transcript, the per-chunk hash, the item hash and the derived chunk id — all independently derived after reproducing the inventory, epoch-heads and routed-manifest goldens), the payload- and signature-exclusion claims, the courier round trip, the two digest domains disagreeing on identical bytes, every shape fault, the unclamped-wire refusal, the expiry floor on both doors, and the derived-cap / in-flight-headroom / boundary-rule pins. |
| `Fernlet/Tests/FernletTests/MeshChunkerTests.swift` | P5 item 2: mint and verify with real identities — boundary-exact chunking, every signed-field tamper as `signatureInvalid`, a tampered payload as `chunkHashMismatch`, foreign mesh / unadmitted / departed / removed origins by name, expiry, count and length refusals, all three legs of the manifest triple (including the squatting case), a chunk verifying without its manifest, the chunker refusing to mint for another origin, the transfer-lane fit computed from the real encoders, signed-not-sealed with a `.tempMessage` control, reverse-order delivery across the fake fabric, and the replay-window shape. |
| `Fernlet/Tests/FernletTests/MeshChunkAssemblyTests.swift` | P5 item 2: the reassembler's verdicts — any admit order reassembles identically, a duplicate is a no-op and a conflicting chunk is refused, bounded growth as `sizeOverflow`, the bound and unbound length rules, binding cross-checks and re-validates already-held chunks without mutating, an unbound assembly never completes, an incomplete one never yields a partial blob, a tampered blob fails the final content hash, and a complete one returns the exact original bytes. |
| `Fernlet/Tests/FernletTests/MeshCustodyReceiptTests.swift` | P5 item 3: `MeshCustodyReceiptFixtures` and the two receipt goldens (the 153-byte transcript and the derived receipt id, both independently derived after reproducing the inventory, epoch-heads, routed-manifest and routed-chunk goldens), the signature- and re-mint-stability claims, every shape fault, every mint refusal, every rejection, and D14 in both directions. |
| `Fernlet/Tests/FernletTests/MeshRoutedStoreTests.swift` | P5 item 3: the sealed routed store's five states, the write token no writer-less state can reach, the vocabulary parity with P3's, the at-rest cap refusals, and the durability ladder — a receipt survives a new store instance on the same scope, a failed index write mints no witness, a refused seal acknowledges nothing, orphans sweep across bounded calls, and a lost or swapped chunk file clears `custodiedAt` rather than handing out a receipt. |
| `Fernlet/Tests/FernletTests/MeshRoutedCustodyTests.swift` | P5 item 3: the C13 differential (both doors, value for value, including a refused bind mutating nothing on either side), the delivery map's round trip through the ORIGIN'S SIGNED destination set with `departed` refused on decode, and the evidence claims — every `custodied` rung backed by a stored receipt written in the same index write, the recorded custodian being the signer, and every cap refusing by its own name. |
| `Fernlet/Tests/FernletTests/MeshRoutedStoreIsolationTests.swift` | P5 item 3: the shared-disk-root grep-wall for the routed scope (every test store names its own directory AND keychain service, the app scope stays derived from the already-walled seams) plus the wall over `MeshCustodyDurabilityWitness`'s single construction site — the thing that notices if the `fileprivate` gate is widened by moving the type. P5 item 4 added six more source-scan walls to the same file: `MeshRecipientDeliveryWitness`'s single construction site (the delivery twin of the same `fileprivate` gate), the one-type-one-verb property of `MeshRoutedDeliveryCommit.swift`, the commit verb writing no delivery rung (only the receipt door moves one), `MeshHeartLedgerProof`'s mint being confined to `ProximityHeartLedger.swift`, shipping code naming exactly one ack-stage table — the ONLY thing stopping a second, weaker heart policy, since the table has no type-system gate — with its paren-matching scanner fixtured both ways, and the ack path naming no decrypt seam. |
| `Fernlet/Tests/FernletTests/MeshRecipientReceiptTests.swift` | P5 item 4: `MeshRecipientReceiptFixtures` and the two recipient-receipt goldens (the 155-byte transcript and the derived receipt id, both independently derived after reproducing all six prior vectors), the signature- and re-mint-stability claims, every shape fault, every mint refusal (including the planted self-authored record that makes `recipientIsOrigin` reachable), every rejection, the `notADestination` leg, D14 in both directions, and the unsealed-frame stance. |
| `Fernlet/Tests/FernletTests/MeshRoutedDeliveryAckTests.swift` | P5 item 4: the per-type ack matrix — a photo final on durable ciphertext with no decrypt in the path, a heart not final until the foreground ledger commit (per-GIFT evidence, so a two-heart batch receipts both), control immediate with no held chunks, an unknown token refused; the durability half (the ack instant survives a new store instance, a failed write mints nothing, before first unlock every path refuses by name, the crash window is enumerated and recoverable); and the ingest half (one signer per receipt, a second arrival grows nothing, fully-delivered is not reclaimable at a recipient, `delivered` never regresses). |
| `Fernlet/Tests/FernletTests/MeshRoutedInventoryTests.swift` | P5 item 5: `MeshRoutedInventoryFixtures`, the 229-byte routed-inventory golden (independently derived after reproducing all EIGHT prior vectors), the collision wall between the two digest families asserted over Swift value-type stems rather than a grep, every shape fault as `.malformed` and every cap fault as `.overCapacity` **by name**, the mint's one named refusal, the mint taking "this device" from the signer and nowhere else, re-mint stability, every rejection, D14 in both directions, and the unsealed-frame stance. |
| `Fernlet/Tests/FernletTests/MeshRoutedInventoryBuilderTests.swift` | P5 item 5: the builder against the real store doors on a per-instance scope — both halves of the custody self-rule (self appears for another origin's item even though its own receipt is never stored; never for its own, with `MeshCustodyReceiptMintError.originIsSelf` asserted as the reason), the two evidence doors, parked and unrestorable-delivery items advertised rather than dropped, expired ones filtered, the minimal member table, index order, determinism, the read-only spy, the exact held-chunk bitmap including a repaired hole whose COUNT still ties with a peer's, and a planted over-cap `chunkCount` that never becomes an allocation. |
| `Fernlet/Tests/FernletTests/MeshRoutedInventoryDeltaTests.swift` | P5 item 5: the six drain rules, the chunk-set claim (equal counts over different held sets are NOT quiescent), the overlap-only rule for a disagreeing chunk count, the three receipt deadlocks (unknown item, parked peer, parked self), fingerprint-not-index signer comparison, the entitlement gate and its custody-handoff source, the scoped offer/ask mirror plus the receipt mirror over a pair where both rules can fire, the ask/offer rollups as distinct canonically-ordered keys, canonical order and bounds, the foreign-mesh refusal, and the one-sided-quiescence claim that stops item 7 spending `isQuiescent` alone. |
| `Fernlet/Tests/FernletTests/MeshRoutedDrainAnswerTests.swift` | P5 item 6: `MeshRoutedDrainAnswerFixtures`, the 102-byte drain-answer golden (independently derived after reproducing all NINE prior vectors), the collision wall against both digest families in both directions, the four shape faults, the flooring rule that makes the receiver's exact-equality binding possible at all, the mint's two named refusals, every rejection, D14 in both directions, and the unsealed-frame stance. |
| `Fernlet/Tests/FernletTests/MeshMergeWindowTests.swift` | P5 item 7: `MeshMergeWindowStateTests` (the closing law as pure values — one match of two does not close, answering alone never closes, a mismatching digest un-matches its sender, an unasked peer's match discharges nothing, an unreachable peer is not waited for, every set roster-bounded, the proof once per distinct digest and its derived cap, and `openedAt` provably never branched on) and `MeshMergeWindowWireTests` (the same rule over real signed frames: 2d at the wire seam on a three-manager star, the departure and dropped-link exits, the responder clause, the post-merge proof, the re-evaluation rescue, the lightweight-slot regression, and §6.9's spent re-gossip budget pinned as the behaviour it is). |
| `Fernlet/Tests/FernletTests/MeshRoutedDrainPlanTests.swift` | P5 item 6: the plan's narrowing rules with no manager and no store — the refused set removing keys from both directions AND from the ask, the bounds and the session allowance truncating rather than growing, the delta's canonical prefix surviving, determinism, an empty delta planning nothing, and the pacing regression (one item may spend the whole 64-chunk answer allowance). |
| `Fernlet/Tests/FernletTests/MeshPairwiseFoundingTests.swift` | P6 item 2: `MeshFoundingRig` (N proximity-join managers on `FakePeerNetwork` with **no** seeded mesh and **no** seeded ledger — the state the app is in when two phones meet, driven through `commitSlotForTesting` and a pump that carries each sender's verified `PeerIdentity`, which the membership family attributes off and every previous rig passed as nil) and the founding end to end: one commit arms a ledger, a ceiling and this device's key advertisement; a refused seal leaves no mesh; a symmetric pair converges on one meshID with the yielder fully unwound; a roster of two never yields; both the yield and the formerly silent foreign-descriptor drop are named; a third commit merges into the founded mesh and is PROMPTED; the founding pair's one admission is auto-granted while a closed session, a non-join founding and an uncommitted requester are not; `hasCommittedPeer` tracks the slots and not the mesh; and a committed peer is a session participant before it is admitted. **Pass B's headline is delivery, measured at the recipient:** a pair's photo reaches the other side's WALL in both directions and a three-device founded mesh delivers to all three after the yield, all through `addPhoto` with nothing seeded — a staged own item plus no refusal being what excludes `.skipped(.noDestinations)`, which is what every proximity session answered before item 2. Also pass B: a partitioned pair re-arms its radios without re-founding (`resumeSearchingForPartitionedMesh()`, shown red against the old `!isInSession` guard), the blip/heal edge that only `hasCommittedPeer` raises, a second founding for one session refused by name, and a yielder that had CLOSED its session staying closed after adopting. **The session-end fix adds the four doors of `isSessionLive` as four cells** — End Session fires the three hooks exactly once, a termination record fires them with the mesh still held, the five-minute timeout fires them and refuses while a peer is committed, and a ledgerless pair keeps its legacy slot-loss ceremony — plus the two blip cells (a blip presents nothing and clears nothing; a blip, a resume and a re-link keep the pre-blip transcript), `FriendsDiscoveryEntry`'s truth table, and a session the user closed that no skewed descriptor re-opens. Item 1's grant-door row is now a positive: the joiner PARKS it and proves it at the adoption, asserted with the `mesh.keyAgreement.parked` line so the mechanism is named and not inferred. |
| `Fernlet/Tests/FernletTests/MeshRoutedTextBodyTests.swift` | P6 item 4: the routed TEXT body — its frozen framing golden (an INDEPENDENT new pin beside the photo family's, never an edit to it), all FIVE hostile shapes on the one `malformed` token including **payload bytes that are not valid UTF-8** and, since item 4's fix review, a decoded `senderName` one byte over the 128-byte bound (paired with one exactly at it, so a decoder that refused every long name would not pass) (with a replacement-character negative control, so the cell cannot be satisfied by refusing non-ASCII), the cap formula and its registration (the registry init silently DROPS a row whose cap is outside the wire bound), the header allowance's measured 4× floor, and `boundedText`'s Character alignment — including that it can EMPTY a message the 500-`Character` cap admitted. |
| `Fernlet/Tests/FernletTests/MeshRoutedTextDeliveryTests.swift` | P6 item 4: `MeshRoutedTextSenderTests` + `MeshRoutedTextDeliveryTests`, over `MeshFoundingRig` with nothing seeded. The sender's outcome table on the shape that produces it — `.noDestinations` in the founding window paired with `.staged` after the second admission, which is the non-vacuity — and text DELIVERED and PROJECTED into both transcripts in §10.3's order with two members interleaved and delivered out of order. Plus the six-message burst that makes the token bucket's retirement load-bearing, the blip that must NOT be final, the ended session that must, the age gate that projects nothing now or later, the blocked-after-custody origin, and the closed access gate that projects nothing and then everything. Item 4's **fix review** adds: door 3's give-up ending a session with the mesh RETAINED and then `resumeSearchingForPartitionedMesh()` un-ending it, which is the only shape the transcript-GENERATION leg is the refusal on (the session-ended cell leaves the mesh, so leg 2 answered first); a delete-all wipe in progress refusing every projection retryably and **uncharged**; the per-origin text quota's three legs at the door; and the two "…AndIsMarkedFinal" cells now OBSERVING the mark instead of inferring it from an empty transcript. |
| `Fernlet/Tests/FernletTests/MeshRoutedDrainTests.swift` | P5 item 6: `MeshRoutedDrainRig` (an N-manager facade over `MeshDepartureRig` with a per-node sealed routed store) and `MeshRoutedDrainItem`, then the drain end to end — it runs when the membership digest already MATCHES, a link delivers manifest/chunks/both receipts, a heart stops at custody, an ABSENT store advertises and is then offered, a second heal moves nothing, a five-chunk item completes, an item minted after a spent batch still drains, a destination is not a courier for its co-destinations, a third party's manifest is refused, an unknown type token never binds, the quiescence bit round-trips and an unbound one is dropped, a non-loaded store sends nothing, the state clears at the session resets, and the drain raises no rotation. P5 item 12 adds the replay cells: a replayed manifest answered before the verifier and one that never reaches the store, a replayed chunk answered before its two hashes, both receipt doors, one origin's chunk forwarded by two custodians as ONE window row, an epoch rotation that releases nothing, the clear at a session reset, a full axis whose frame still lands, a deferred and a capacity-refused frame that stay re-offerable, a repaired slot that is re-admittable, and the settled gate asserted as a pair. Plus `MeshRoutedDrainWallTests`: the three-call-site grep-wall, the clear-site count, no epoch symbol on the routed path, and item 12's W1–W8 — the window wired at every content door and nowhere else, the probe before the verifier and the store, the digest family not admitted, cleared only at the session resets, both bounds derived (frames from `sessionFramesPerPeer`, authors from `maxRecordsPerKind`), never forgetting an author, and item 8's hop bound surviving a replayed manifest. |
| `Fernlet/Tests/FernletTests/MeshRoutedDrainConvergenceTests.swift` | P5 items 6 and 14: the routed half of §16.2's property, as an EXTENSION on `MeshConvergenceRun` rather than a parallel rig. **D-14.1 taken here: no new `MeshScheduleEvent` case** — the routed vocabulary is `MeshRoutedScheduleOverlay`, a salted side-plan over the same seeds (adding a case would re-phase every one of P4's 80 membership cells and void their named regression fixtures). Item 14 widens the rectangle to **five partition shapes × the eight fixed seeds = 40 cells**, plus a 12-cell lock-window rectangle, the development cells derived from the overlays (their own pipeline, so the hand-off is never vacuous), and one sealed photo per shape. `routedInvariants(_:_:audited:overlay:before:judged:)` is the one entry point over **thirteen** named claims (the thirteenth is P6 item 7's inventory-stamp monotonicity, and P6 item 9 added the `judged:` AUDIENCE so a `.singleRecipient` heart's roster-wide claims are narrowed to its own destination set — nil keeps the pre-P6 reading, so every earlier cell is byte-identical): progress per destination, nothing lost, the rung is real, the bytes are recoverable at both populations that claim them, no receipt double-counted, all five load states distinct, the hand-off and hop bounds, rung monotonicity on `MeshDeliveryState.rank`, a visible capacity hold, a replay that moved nothing, and why a merge window closed. **P6 item 9** added the file's third pipeline: `MeshRoutedFeaturePipeline.featureRouting` and rectangle G (`MeshRoutedConvergenceMatrix.featureTree`, 12 cells), which converge the advertised-key set and then mint BOTH of P6's rows — a text through the real `sendTempMessage(_:)` and a heart through the real `sendSessionHeart(to:)` — inside a bounded two-round window the overlay's own draws interleave, with `MeshRoutedHeartJudgementLog` collecting item 6's `onHeartJudgedForTesting` firings per gift as the exactly-once witness. |
| `Fernlet/Tests/FernletTests/MeshP6AcceptanceTests.swift` | **P6 item 9**: the phase's acceptance battery — eight serialized suites, one per clause, in `MeshP5AcceptanceTests.swift`'s shape (`MeshP6KeyAdvertisementAcceptanceTests`, `MeshP6PairwiseIdentityAcceptanceTests`, `MeshP6PerTypeCapAcceptanceTests`, `MeshP6TextRoutingAcceptanceTests`, `MeshP6ProjectionRetryAcceptanceTests`, `MeshP6HeartCeremonyAcceptanceTests`, `MeshP6HonestyAcceptanceTests`, `MeshP6DeterminismAcceptanceTests`). Each runs its clause END TO END on the shipping seams and CITES the exhaustive space rather than re-running it, so CI gating one line fails on this battery's own assertions. The feature pipeline, its seams and rectangle G live in `MeshRoutedDrainConvergenceTests.swift` on purpose: a new `Mesh…ConvergenceTests` suite would not match `CIGateSelectorBoundaryTests.isMeshBattery`'s `MeshP<n>…AcceptanceTests` shape and could sit ungated the day it was written. The two determinism digests keep their one home in `MeshP5DeterminismAcceptanceTests`; this file's determinism clause asserts the facts that make P6's move legitimate and re-pins neither. It is on the grep-wall's scanned list, so it spells no `Date()` and no RNG. |
| `Fernlet/Tests/FernletTests/ProximityRunPolicyTests.swift` | **P7 item 1**: `ProximityRunPolicy`'s matrix over the FULL input product — `ProximityRunPolicyProduct.rows()` enumerates all 11 520 rows (3 phases × 5 tabs × 4 continuation states × 3 presences × 2⁶ Bool facts; 2⁷ and 23 040 until P9-3-A retired the lock fact, 2026-09-22) from the enums' own `allCases`, pinned by count and by distinctness. Over that product each claim the policy makes is a cell: the gate reads only its three facts (blind to the wipe, age, lock and task facts), `.inactive` equals `.active` on every row while `.background` differs, only a mesh continued by a running task ever claims the background and it always does, discovery never runs in the background (invariant 5) and stops outright under a live task, a hard stop stops every radio while nothing else ever tears the mesh down, nothing outside the opt-in, the scene, the tab and the hard stops parks a listener (the P9-3-A replacement for the retired "the app lock moves presence and recipe only"), the three non-running continuation states are one row and under `.notRequested` nothing claims the background ("inert until P8" stated positively), the listener rules are today's `ContentView` chains flat, and a flat re-statement of every radio's rule agrees on every row (the named expectation per row — a guard-order catch, not a proof). Plan §13's load-bearing rows are pinned by hand, `ProximitySessionPresence.folding` is shown total over its unrepresentable row, and the honesty cell names what the table does not claim (no scene, no manager, no seam, no task). **Item 2** added the projection tables (`appLockEngaged`, retired by P9-3-A; `belowMinimumAge` — a ruling, never an absence) and a SECOND suite in this file, `ProximityRunPolicyFunnelTests` (serialized, main-actor, a real `FernletStore` per cell): the funnel writes exactly the gate the policy decided on every edge, keeps the verdict, and — since item 3, when the funnel also executes the radios — uses only rows that keep every radio stopped or held (the Private tab — the app lock's job until P9-3-A — the background scene, duress), so no real transport is ever started; it also drives the three entries (scene edge, view edge reusing the retained scene facts, store edge reusing everything) and the before-any-edge fallback. Tier 1: no rig, no clock, no RNG, no simulator. |
| `Fernlet/Tests/FernletTests/ProximityRunSeamsTests.swift` | **P7 item 3**: `ProximityRunTransition`'s table — the first application counts every radio as an edge; an unchanged verdict is silent; a tab exit keeps a committed link and only cancels the timeout; a tab exit stands a blipped search down without tearing the mesh down; a tab entry over a live session arms nothing; a background exit holds the link and stops the listeners; a foreground return after a blip resumes rather than founds; a hard stop tears a live session down through `leaveSession()`, stands a mere search down, and only cancels over nothing; a cleared hard stop starts afresh; the one transition P7 cannot execute is refused by name; a discovery `run` is answered, not trapped — and **the retirement wall**: every mesh radio verb under `App/` lives in `ProximityRunSeams.swift` exactly once (plus the DEBUG Lane C harness's `startJoin()`, exempted by name), no listener is started or stopped by a qualified call anywhere, `ContentView` names none of the retired chain and hands the funnel its five edges through one helper, and the store's own edges (the two opt-in setters, the wipe's raise and lower) run the policy rather than reaching around it. The honesty cell says the executor and the listener seams are not driven here, because a real manager starts a real radio. |
| `Fernlet/Tests/FernletTests/MeshSessionPollTests.swift` | **P7 item 4**: the poll seam and the joiner-side ceiling arm on the founding rig — the yielding half of a symmetric founding now holds the winner's own signed deadline, an hour-early poll ends nothing, an hour-late poll reaches the ceiling and ends the session with idle lapse and partition unjudged after it (the order), and a poll over the ended session is `.skipped`; a device with no session polls nothing and the arm refuses with no mesh; the arm keeps an already-armed ceiling. Stated instants only, no clock, no timer. |
| `Fernlet/Tests/FernletTests/ProximitySessionPollerTests.swift` | **P7 item 4**: the app poller's decision table (a timer iff a live session), the interval (30 s) and the bound (721 ticks, derived from the ceiling), and the wall — `pollSession(` driven from exactly one app file, the one timer constructed there once, its handle on the store, the loop bounded by the ceiling and self-stopping, the run-policy core syncing it, and `ContentView` observing `isSessionLive`. |
| `Fernlet/Tests/FernletTests/MeshSessionResumePresentationTests.swift` | **P7 item 5**: the presentation table over every `MeshSessionRestoreOutcome` case — resumable with and without the raised offer, each of the eight termination reasons folded to its ending, the passed ceiling, the green field, a deferral and a refusal (silent), the set-aside file — and a session surface up presenting nothing whatever the outcome; plus the manager's derived read on a fresh manager. Tier 1, stated instants. |
| `Fernlet/Tests/FernletTests/SessionResumeCopyTests.swift` | **P7 item 5**: every presented case has its own card and `.nothing` none, no two cards repeat, no sentence says "failed", the dismiss label is one shared key — and the surface wall: `ConnectView` samples `manager.sessionResumePresentation` exactly once, only through `SessionResumeCopy.card(for:)`, exposes `friends.sessionResume` and `friends.sessionResume.dismiss` by identifier, and reads none of the manager's internal restore surfaces. |
| `Fernlet/Tests/FernletTests/MeshP7AcceptanceTests.swift` | **P7 item 7**: the phase's acceptance battery — six serialized suites, one per clause, in the P5/P6 batteries' shape (`MeshP7RunPolicyAcceptanceTests`, `MeshP7GateWriterAcceptanceTests`, `MeshP7RadioSeamsAcceptanceTests`, `MeshP7PollerAcceptanceTests`, `MeshP7ResumeAcceptanceTests`, `MeshP7HonestyAcceptanceTests`; 13 cells). The run-policy clause re-runs the full 11 520-row product (23 040 until P9-3-A) rather than sampling it; the gate-writer clause drives a real `FernletStore` through lock, background and duress and reads the gate back; the radio-seams clause re-walks the retirement wall; the poller clause drives each of the three consumers to a verdict on the founding rig (a ceiling reached, a partition then an idle lapse, a dead session skipped); the resume clause sweeps every restore outcome; the honesty suite states that the two determinism digests keep their one home and that this battery's first Mac run is its first execution. `MeshP7Acceptance` is the shared thin rig (source walker, pinned binding, store). Gated on the mesh-batteries step; `CIGateSelectorBoundaryTests`' battery pin moved 36 → 42. **Committed unbuilt** — the session that wrote it had no toolchain. |
| `Fernlet/Tests/FernletTests/MeshP8AcceptanceTests.swift` | **P8 item 10**: the phase's acceptance battery — six serialized clause suites in the P5/P6/P7 batteries' shape (`MeshP8CoordinatorTableAcceptanceTests`, `MeshP8HoldVerbAcceptanceTests`, `MeshP8RaiseAndDisagreementAcceptanceTests`, `MeshP8PresentationAcceptanceTests`, `MeshP8TaskWiringAcceptanceTests`, `MeshP8HonestyAcceptanceTests`; 21 cells, measured). The coordinator clause re-walks all 48 rows of item 4's table against the battery's OWN expectation of plan §14, spelled EVENT-outermost with all six states as literal rows per event — a third decomposition, neither the shipped table's state-first helpers nor `MeshContinuationProduct`'s event-first predicates, so a sweep that rewrites a production helper has no identically-shaped twin here and a table and an oracle edited together still redden — plus the exactly-once biconditional over the whole product (five completing rows) and the progress ratchet. The verb clause drives item 3's `holdCommittedLinks()` on the real founding rig: the committed link, its coordinator and the seeded group key kept — with the peer's own `stopJoin()` at the end of the same cell nilling that same observable, so the claim has teeth — `pauseDiscovery()` once and `stop()` never, all three admission doors shut with the uncommitted slot dropped, the five-minute give-up clock stood down and restarted by the resume, and the policy row P7 could only refuse now emitting `.holdLinks` with `proximityRunPolicy.unsupportedTransition` at zero under `App/`. The raise clause re-asserts item 5's three wall counts and walks the four corners of the two legs on a real founding through item 6's scene-dark rule (a task delivered while the app is on screen raises nothing), plus the mid-task `reset()` that gives the session leg back. The presentation clause runs item 7's whole 6 × 10 product (the 23 presenting cells pinned by IDENTITY against a literal set, not by a count a swapped pair would keep; 37 silent, three past-tense), `projectedAudit` across a real session end, and the slot decision's six rows. The wiring clause drives item 6 end to end through its own two fakes — register, submit once, a `.fail` refusal reaching the card, a delivery completed exactly once at expiry, the hard stop paying the debt — and pins the feed as the host's only route to a radio. The honesty suite names what no Simulator can run: plan §15.1–§15.4 by name, the device runbook's Lane B rows, `BGTaskScheduler`'s error 1, item 3's device-only QUIC hold row, and the two determinism digests' single home in `MeshP5AcceptanceTests` (spelled split here, as P7 does). `MeshP8Acceptance` is the shared thin rig — the battery-local event-outermost 48-row expectation, the literal 23-cell presenting set, the founded heart pair and the pinned-binding gate push — reusing `MeshP7Acceptance`'s source walker rather than copying it. The same commit gates this battery AND the eight `MeshContinuation*` suites items 4/5/6/7 shipped, which were on no CI line at all: the mesh-batteries floor moved 365 → **469** (measured, 71 suites) and `CIGateSelectorBoundaryTests`' battery pin 42 → 48, with `MeshContinuationRaiseWallTests` and `MeshContinuationTaskHostWallTests` pinned by name as walls with no compiler half. |
| `Fernlet/Tests/FernletTests/MeshTransportSelectionTests.swift` | The radio the app's own initializer builds. There is no selection left to assert: `MeshTransportKind`, `MeshTransportFactory.shippingDefault` and `resolvedKind(environment:)` retired with the MultipeerConnectivity radio in the deletion round (2026-09-22), and every cell that pinned them went with them (`MeshP9McRetirementAcceptanceTests` asserts the retirement from the battery side). Eight cells: the initializer pinned to `NetworkMeshSession` as a VALUE, a radio nobody wired holding neither authority nor gate, the `MeshIntroductionAuthority` wiring, its signing domain and its refusal to sign outside that domain, the full callback wiring, the discovery-failure banner and the closed-session admission gate. The suite keeps its name so the CI mesh line and `CIGateSelectorBoundaryTests` are untouched by the retirement. On the CI mesh-batteries line and pinned there by NAME. |
| `Fernlet/Tests/FernletTests/MeshP9AcceptanceTests.swift` | **P9 item 9**: the phase's acceptance battery — five serialized clause suites in the P5–P8 batteries' shape (`MeshP9EphemeralPostureAcceptanceTests`, `MeshP9PresenceSwapAcceptanceTests`, `MeshP9RecipeSwapAcceptanceTests`, `MeshP9McRetirementAcceptanceTests`, `MeshP9HonestyAcceptanceTests`; 20 cells, measured). The posture clause walks four epoch boundaries as VALUES (no name, certificate or epoch repeats) and reaches the certificate's epoch-START anchor through the injected mint seam rather than the unit suite's DER arithmetic, pins the two posture types apart (900 s epoch vs. per-`start()`/`resume()`) and pins neither persisted. The presence clause runs item 2 pass 2 end to end on the shipping `PresenceManager` over the seam's conformer — a mid-epoch start wearing the posture's own name AND certificate, a tick inside the epoch that republishes nothing, a boundary that replaces all of it and withdraws the old name — plus the service type/ALPN declared while the retired pair is gone, every `presence.quic.*` line naming its peer only by a salted per-session label read SCOPED to this session, and the MC path gone from the manager over a whole-package walk. The recipe clause re-walks item 3's whole 5 × 12 discovery-gate product against the battery's OWN event-outermost literal table (a battery-local three-valued `Held` makes it exhaustive without a `default`; exactly five of sixty readings move discovery), mints a transfer UNDER the pause it mirrors, pins the exactly-once completion oracle, the per-`resume()` fresh posture and the `stop()` that keeps no pause flag, and pairs the discovery verbs' whole-file count with the count inside `applyDiscoveryGate`'s brace-matched body. The MC clause is an **HONESTY row, not a zero-list**: at this commit `MeshTransportFactory.shippingDefault` is `.multipeer` and first-meeting stranger admission has no QUIC path, so it asserts the CURRENT truth in both directions — both MC files exist BECAUSE of those two facts, the framework import has exactly two homes, the four dead Bonjour strings are gone while the LIVE friend pair is still declared, and the owner's D-4.1/D-4.3 record survives — which makes every cell one the cutover commit must edit. The honesty suite names what no CI machine can run (the two tier-2 Lane C rows, the three branches nothing has ever observed, plan §15.1–§15.4, P9-3-A, 9.4-LATER) as assertions over the docs that record them, and re-pins the two determinism digests' single home in `MeshP5AcceptanceTests` (spelled split here, as P7 and P8 do). `MeshP9Acceptance` is the shared thin rig, reusing `MeshP7Acceptance`'s source walker and both pass-2 fakes rather than forking them. The same commit gates this battery AND the seven suites items 2, 3 and 4 left on no CI line at all: the mesh-batteries floor moved 719 → **860** (measured, 97 suites), `measuredSuiteNameCounts` 85 → 97 and the battery pin 48 → 53, with `TransportNeutralityBoundaryTests` pinned by NAME as a source grep-wall with no compiler half. |
| `Fernlet/Tests/FernletTests/MeshP10SchedulingSeamAcceptanceTests.swift` | **P10 item 9**, clause (1): the companion refresh's scheduling seam as an acceptance clause, and the home of `MeshP10Acceptance` — the thin rig P10's five clause suites share (the five document paths, the five frozen suite names, the frozen instant, and a REGISTERED coordinator over `CompanionRefreshSchedulingTests`' fakes, which it reuses rather than forking). Five cells: the frozen identifier as ONE literal shared by four places (the app's constant, the PARSED `Info.plist` array, the import wall's un-linked copy, and the cell itself — a drift between any two is an error nowhere, because iOS simply stops delivering the task, for the life of the install); registration once, and a refused registration closing the chain with nothing submitted, read as values rather than through audit capture; the background edge asking only when nothing is pending, driven over a MOVING clock because three requests built from one frozen instant are indistinguishable from one; the R2 edge cap bounding the edge alone while the handler's tail keeps the chain alive through it; and the chain unbroken over three deliveries with every successor read AT its own completion instant, because both orders finish with the same count in hand. The rig deliberately carries NO ledger path — P10's ledger was not committed until the close-out (and has been out of the tree since 2026-09-22, `e6a3b48:Docs/Mesh-Migration-Loop-Ledger-P10.md`), so a cell that read it would pass only in the worktree that wrote it. Suite name chosen so `CIGateSelectorBoundaryTests.isMeshBattery` demands it. Gated on the mesh-batteries CI step. 2026-09-21 (item 2c): the refused-registration arm also pins `edgeSubmissions == 0` as a value. |
| `Fernlet/Tests/FernletTests/MeshP10HandlerPipelineAcceptanceTests.swift` | **P10 item 9**, clause (2): §17.2's handler as an acceptance clause, every cell a JOIN neither unit suite makes. Six cells: the seven-step order asserted against the enum's own `allCases` — not against a literal, which is what `CompanionRefreshPipelineTests` pins and which stays green if two cases are reordered in the declaration, since the pipeline appends in code order — plus the raw values `companionRefresh.runFinished` carries and `reload` pinned as the step a publication REPORTS rather than one anything calls; the `.deviceLocked` acquire refusal carried out to `setTaskCompleted(success: false)` and to a successor that already exists; the missing-scoring-bridge refusal (D-10.4.6) over a REAL store through a real handle, publishing nothing and still reporting SUCCESS; both arms of the widget-queue decision in one cell (D-10.4.2 skips on today's snapshot, D-10.4.8 runs the whole tail across midnight) with both reporting success; an expiration landing inside the run — placed from inside the acquire step rather than by racing a yield — completing exactly once and leaving the successor standing; and a whole refresh over a gateway-less store that leaves the cold-wake repair (D-10.4.1) UNSPENT, which is §16.4's HealthKit prohibition restated behaviourally: a handler that had reached any workout path would have built the sync, shut the attach door, and left the foreground on a store whose sync holds a gateway of its own. Gated on the mesh-batteries CI step. |
| `Fernlet/Tests/FernletTests/MeshP10DiffRuleAcceptanceTests.swift` | **P10 item 9**, clause (3): "reload timelines only on change", from the field classification out to a `CompanionRefreshOutcome`. Five cells: the content/metadata table proved a PARTITION of the struct's stored fields through `Mirror` and then made behavioural (the one metadata field flipped leaves the pair content-equal and NOT `==`, which is why the handler could not have used the synthesised equality); a stamp-only difference written, not reloaded, ending `unchanged` and telling the system it SUCCEEDED; every content flip walked off the unit suite's own table — reused, not copied — each over a fresh mirror, reloading and ending `reloaded`; the foreground `publish(_:)` still reloading unconditionally, pinned as the CURRENT truth while decision **D-10.4.5** is open; and, on `CompanionRefreshWiring.steps(for:)` over a real store, the half no unit cell carries — a dull refresh WRITES anyway, proved by backdating the published snapshot an hour so the re-write clears the encoder's second-resolution ISO-8601 floor, with the content still identical and nothing poking WidgetKit. Gated on the mesh-batteries CI step. |
| `Fernlet/Tests/FernletTests/MeshP10ImportWallAcceptanceTests.swift` | **P10 item 9**, clause (4): §16.4's background-refresh wall as an acceptance clause, with every pin read as a VALUE off `BackgroundRefreshBoundaryTests` rather than re-spelled. Five cells: the wall still aimed at `App/Fernlet/CompanionRefresh/` with a non-vacuous floor, checked against this file's OWN `FileManager` walk so a broken scanner cannot make the wall and its acceptance clause green together; the zero-list no smaller than its measured 84, its clock-and-persistence family no smaller than its own 15, the 37 app-declared needles no fewer, and §17.2's five prohibitions each re-derived from the SPECIFICATION (a needle dropped for one of the five leaves the count pin satisfied by whatever replaced it); the module classification proved a partition with the permitted set DERIVED from its reasons map and held to eight names; the identifier as the wall's positive needle with exactly one code home, plus `import BackgroundTasks` once and `BGAppRefreshTask` in one file; and the wall and the three refresh unit suites name-pinned on the `s3-grep` step on the `MeshRoutedDrainWallTests` argument — none is a `MeshP<n>…AcceptanceTests`, so nothing else demands any of them. Gated on the mesh-batteries CI step. |
| `Fernlet/Tests/FernletTests/MeshP10HonestyAcceptanceTests.swift` | **P10 item 9**, clause (5): the honesty suite, in P8's and P9's shape — every unrun claim asserted over the record that carries it, so deleting the record reddens. Five cells: the five clause suites declared and named exactly once on the mesh step (a duplicate selector runs its tests twice and inflates the floor); both determinism digests unmoved and each still with its single home in `MeshP5AcceptanceTests`, spelled split here and PROVED split; **what only a device can show, made mechanical** — nothing in the whole test target constructs `SystemCompanionRefreshScheduler` or `SystemCompanionRefreshTaskHandle`, so no test has ever seen a registration accepted, a submission granted, a task launched or an expiration handler fired, and plan §27.2's tier-3 sentence still records that; **item 8's Lane E as a MEASURED verdict**, scoped to that section of the runbook (registration accepted — a positive observation, so the lane does not read as "nothing worked"; the submission refused with `BGTaskSchedulerErrorDomain` code 1; `companionRefresh.runFinished` never emitted on any machine) together with the six rows only a phone can give and the two audit events that fall with them, each pinned by NAME; and plan §15.1–§15.4, item 0's `blocked (owner)`, 9.4-LATER, P9-3-A and D-10.4.5 all still recorded as owed. Reads TRACKED records only — deliberately not the phase ledger, which is uncommitted until the close-out and would make the battery pass only where it was authored. Gated on the mesh-batteries CI step. |
| `Fernlet/Tests/FernletTests/MeshRoutedCapacityTests.swift` | P5 item 9, the STORE half: every cap refused by its own name at every door it guards under INJECTED small bounds (`MeshRoutedStore(scope:capacity:)`), the production model proved to be exactly `MeshRoutedStoreFormat`, the at-rest guards left the format's (including the new `capacityExceeded("chunkCount")`), the release predicate measured against `max(index, directory)` with an unreadable directory reading as full, the ten-case parked-drop table (`MeshRoutedParkedDropTests`), the reclaim/expiry/orphan verbs (`MeshRoutedSweepTests`), and the two census walls — every `.upsert(` growth site answers a cap, and every `capacity*` refusal is in the manager's "this device is full" set with the narrower store-full subset pinned beside it. |
| `Fernlet/Tests/FernletTests/MeshRoutedBackpressureTests.swift` | P5 item 9, the MANAGER half on the existing `MeshRoutedDrainRig`: a capacity refusal refused by name, remembered, published as a bounded `MeshRoutedDeliveryHold` and readable as copy — with nothing lost at the origin; the heal that hangs on "the content is here" rather than "the door ran"; corrupt is not full; the hold surviving `leaveMesh` and clearing on a new ledger; the release rule and its budget (once per peer per session, spent only after the store answers); the orphan sweep; the held-back set naming its own bound; `unplacedItemKeys` reaching the observable; `MeshRoutedParkedDropDoorTests` (the drop rule at the door, and the delete lever closed); and `RoutedDeliveryHoldCopyTests` — the app-side copy, the dismissal rule that cannot silence a different hold, and the render gate + `ConnectView` mount as source walls. |
| `Fernlet/Tests/FernletTests/WeightedPhotowallOrderingTests.swift` | Weighted photo-wall ordering. |
| `Fernlet/Tests/FernletTests/MeshHostPinTests.swift` | P5 item 1a: the ONE-SHOT, sleepless reproduction of the `unowned` host trap. `MeshHostPinProbe` owns a mesh manager's host separably from the manager, so a cell can release the host in the SAME synchronous run as `broadcastCoordinatorBeacon()`'s per-slot spawn and prove the in-flight send pins it (HP1). It is a reproduction, not a canary — on an unpinned tree it aborts the test host rather than going red — so the durable guards are ML4 in `MemoryLifecycleBoundaryTests` and the per-run `Attempted to read an unowned reference` grep. |
| `Fernlet/Tests/FernletTests/RepoRoot.swift` | Locates the repository root for every grep-wall suite, anchored on marker files rather than a hardcoded `deletingLastPathComponent()` depth. It exists because the App/+Tests/ restructure made every hand-counted depth resolve one directory short — and a wall suite handed a wrong root enumerates *nothing*, finds no violations, and **passes vacuously**. Marker anchoring either still resolves or fails loudly. |
| `Fernlet/Tests/FernletTests/LocalizationBoundaryTests.swift` | The localization wall: pins the frozen raw values of every enum whose tokens are persisted/sealed/signed/prompted, grep-walls `String(localized:)` inside `FernletKit/Sources` for `bundle: .module`, pins the app↔widget cross-process companion-state contract no type system spans, pins the derived-signal phrases six call sites string-compare, refuses a bare sentence in a `LocalizedError` member (G), and — added 2026-08-27 — refuses a bare literal assigned to a UIKit display/accessibility property (H1) and any uncatalogued letter-bearing literal in the one UIKit target (H2). Every scan discovers its inputs via `RepoRoot` and carries a hard floor, and every matcher is fixtured BOTH ways. |
| `Fernlet/Tests/FernletTests/CryptographicDomainSeparationTests.swift` | The domain CONTRACT, as a property of the whole 47-entry registry: every test is a negative that requires a cross-domain operation to FAIL. Spelling uniqueness, all-pairs key/tag/digest separation, AAD separation with the key held constant, the production v3 seal's own AAD binding, transcript discrimination, and prefix-freedom (`signingBytes` matches by `starts(with:)`, and several call sites build AAD by bare concatenation). The inventory is checked against `CryptographicPurpose.swift` on disk, not trusted. |
| `Fernlet/Tests/FernletTests/HealthKitLifecycleBoundaryTests.swift` | The two `269003c` lifecycle invariants no behavioural test can reach: the observed-change refresh still coalesces (pending set + newest-wins `cancel()` + the named `healthChangeCoalescingWindow`), and every `HealthKitService` construction in the tree is inventoried with its role — one long-lived, three transient per-write, three injection fallbacks, one `#Preview`. A source scan, and says so. |
| `Fernlet/Tests/FernletTests/PasteboardBoundaryTests.swift` | Pasteboard grep-wall: every shipping write to `UIPasteboard.general` must go through `setItems(_:options:)` with `.localOnly: true`. A plain `UIPasteboard.general.string = …` is Handoff-synced to every device on the account, and there is no API to read an item's options back — this static check is the whole enforcement. |
| `Fernlet/Tests/FernletTests/TestHookBoundaryTests.swift` | Debug/test-hook grep-wall: every shipping line reading a `FERNLET_UI_TEST_`/`FERNLET_SKIP_` launch flag must sit inside an open `#if DEBUG`, and `UITestSupport`'s `#else` block must define a no-op for every `static var` its DEBUG half declares. |
| `Fernlet/Tests/FernletTests/PhotoDirectoryIsolationTests.swift` | Grep-wall keeping every test `FernletStore` on its own own-photo root. The three own-photo corpora are shared on-disk state that `resetAll`/`deleteAllData`/the duress wipe all clear, and parallel suites share one process — a store on the process-wide root has its photos deleted out from under it by any concurrent wipe. |
| `Fernlet/Tests/FernletTests/PrivacyPolicyParityTests.swift` | Pins the privacy-policy triple in sync (in-app view, `Docs/Privacy-Policy.md`, `Site/privacy/index.html`): same effective date, same load-bearing clauses. Exists because the in-app copy once shipped without the verifiability paragraph the other two had. |
| `Fernlet/Tests/FernletTests/FernletLockScopeTests.swift` | Per-surface unlock scopes: one unlock covers ONE locked surface. Before scoping, unlocking the progress-photo strip also opened the Private tab, with only an `onDisappear` re-lock in between — which a covering sheet or scene transition legitimately suppresses. |
| `Fernlet/Tests/FernletTests/DuressLockTests.swift` | Duress PIN steps 1–4: the duress verifier's own salt, the unconditional derivation that closes the timing oracle, duress-FIRST ordering in every credential entry point, and the keyless decoy session. The property under test is indistinguishability from a benign unlock — same state, same audit line, no attempt/cooldown residue — while never handing over the real content key. |
| `Fernlet/Tests/FernletTests/DuressDecoyAndWipeTests.swift` | Duress steps 5–6: the decoy must change NOTHING on disk (an in-memory flag riding the existing hide machinery, so the real passcode restores every surface exactly), and the silent wipe's crypto-erase must actually destroy. |
| `Fernlet/Tests/FernletTests/DuressRecoveryTests.swift` | Duress steps 7–8: `DuressMode.recoveryLock` destroys every local way into the content key while KEEPING the sealed corpus and a recovery blob only the user's own second device can open, plus the two in-person ceremonies that enroll that device and get the key back. |
| `Fernlet/Tests/FernletTests/DuressHardeningTests.swift` | The properties the duress PIN was supposed to have and did not — each suite pins one confirmed defect from the adversarial review (e.g. a duress session can never hold the `.appLockSettings` scope, the one screen that announces a duress code exists). |
| `Fernlet/Tests/FernletTests/DuressSetupUITests.swift` | The duress setup screen's testable half: `DuressSetupAvailability` (which response may be armed, whether the recovery device may be un-enrolled) and the rejection copy the view surfaces rather than restates. Unit-level despite the name. |
| `Fernlet/Tests/FernletTests/BackupExclusionLaunchGateTests.swift` | The default-on backup-exclusion launch gate — fresh installs default excluded, existing installs get a one-time honest prompt, nobody is silently flipped — plus the `LocalFernletRepository` day-blob exclusion that closes the last local Fernlet-data file the Privacy & Data toggle did not reach. |
| `Fernlet/Tests/FernletTests/OwnPhotoKeyBindingTests.swift` | The follow-on flip: the own-photos at-rest key becomes device-bound and the dual-open fallback goes away. Runs against REAL keychain rows (isolated `UserDefaults` for the latch/consent) because the thing under test is a keychain attribute. Since the crypto standardization round retired `OwnPhotoKeyMigrator`, the gate's first half reads `MediaAtRestFormatMigrationLatch`, and `aWallKeySealedOwnFileNoLongerBlocksTheGateAndIsLostAcrossTheFlip` pins what that swap COST — a recorded trade, not a surprise. |
| `Fernlet/Tests/FernletTests/SealedPhotoBackupTests.swift` | The own-photo escrow route: one sealed CloudKit record per photo id plus a sealed manifest written LAST as the commit marker, driven over a mock record database with a planted escrow identity. Pins incrementality — sealing photo B leaves A's record byte-identical, which a chunked scheme cannot do. |
| `Fernlet/Tests/FernletTests/SealedBackupPayloadCoverageTests.swift` | Journal narratives and intimacy logs as first-class sealed-backup payloads, driven through `SealedBackupCoordinator` with a fake context: insert-into-empty, the one-way divergence latch, the locked-key deferral, and the empty-store-clobber guard, all against isolated sealed stores. |
| `Fernlet/Tests/FernletTests/IntimacyLogRepositoryTests.swift` | The sealed-backup surface on `IntimacyLogRepository`: keyless row count, paged reader in a TOTAL order, all-or-nothing restore insert, and the one-way "this device has diverged" latch that stops a stale cloud backup resurrecting deliberately-deleted logs. |
| `Fernlet/Tests/FernletTests/CoachPlanExchangeTests.swift` | The manual coach exchange (clipboard export → pasted `CoachPlan` → dated `PlannedWorkout` rows). The paste path takes untrusted text, so most of the suite is what the importer REFUSES: oversize blobs, truncated JSON, plans over the day cap, exercises with no safety metadata. |
| `Fernlet/Tests/FernletTests/MessagesCatalogPublisherTests.swift` | The app's Messages catalog publisher over a library it cannot share whole (2026-09-23): a recipe or plan the exchange rules refuse (a web import with more than 24 servings, a plan name past the card bound) is left out while every shareable item still publishes — one refusal used to throw out of the whole publish and leave the extension on a stale catalog. |
| `Fernlet/Tests/FernletTests/LocaleCorrectnessTests.swift` | Locale Phase 0: the month grid must start its week where `Calendar.firstWeekday` says. `veryShortWeekdaySymbols` is always Sunday-indexed, so rendering it unrotated over `firstWeekday - 1` blanks put every day cell under the wrong letter across most of Europe. |
| `Fernlet/Tests/FernletTests/LocaleTolerantNumberTests.swift` | The shared free-text number parser behind every decimal field: iOS shows `.decimalPad` with the locale's separator, so es/fr/de users type `"2,5"` and bare `Double("2,5")` is nil — the value was silently dropped. Pins both spellings, grouped forms, the ambiguous `"1,500"` split, and the charset guard that keeps hex/inf/nan spellings out of clinical samples. |
| `Fernlet/Tests/FernletTests/MicronutrientBoundsTests.swift` | Micronutrients arrive from two untrusted sources (a peer's recipe payload, a web page's JSON-LD) and were carried into the synced snapshot unbounded. Covers the wire half (`sanitizedForImport()` drops an implausible amount to "not measured" rather than fabricating a clamp) and the presentation half, which must be TOTAL because `Int(_: Double)` traps and already-poisoned days are synced. |
| `Fernlet/Tests/FernletTests/WebScrapingKitTests.swift` | `WebScrapingKit`'s HTML/JSON-LD extraction primitives — net-new in the 2026-08-18 security round. The old `scriptContents(from:)` regex retried from every `<script` position, so a page with a few hundred thousand unclosed tags cost ~N²/2 comparisons for zero matches and could stall the main actor into a watchdog kill. Flood cases prove linearity; equivalence cases pin the rewrite's output against the regex it replaced. |
| `Fernlet/Tests/FernletTests/JournalScoringParityTests.swift` | Journaling scores the same whatever kind of day it was (2026-09-23): every `FeelingTag` earns the same journal component, above no entry, never below its retired value; the overall score no longer depends on the tag; the tag survives as the unweighted `mood` reading the period bridge reads (with the legacy-row fallback). |
| `Fernlet/Tests/FernletTests/WeightManagementDeficitTests.swift` | Weight Management's gentle starting deficit (2026-09-23, OWNER SIGN-OFF NEEDED): the research note's worked examples, the 1,200/1,500 kcal + RMR floor, "no deficit rather than a surplus" under the floor, a 1,000-profile invariant grid, and the goal card's "up to 10%" copy. |
| `Fernlet/Tests/FernletTests/SickReadinessTests.swift` | Marking today unwell forces readiness to `needs rest` (spec §6a), at once and across the save rebuild; the recommendation is `.light`, never nil; Home's and the launch thought's rest line, the companion prompt's rest-first ordering, the Trends display fork; Move's light-only chips, never-push copy (`MoveRestDayCopy`) and the un-guided rest plan. |

### Test Mocks

| File | Purpose |
| --- | --- |
| `Fernlet/Tests/FernletTests/Mocks/MockMultipeerTransport.swift` | Single-peer scripted `PeerTransport` for coordinator handshake tests. |
| `Fernlet/Tests/FernletTests/Mocks/FakePeerTransport.swift` | Deterministic multi-endpoint transport fabric: `VirtualClock`, `FakePeerNetwork` (links, latency, partition/heal), `FakePeerTransport`. No wall-clock sleeps — the foundation of the P4 partition suite. |
| `Fernlet/Tests/FernletTests/Mocks/MockRangingProvider.swift` | Controllable mock for NearbyInteraction ranging used in coordinator and session tests. |

### UI Tests

| File | Purpose |
| --- | --- |
| `Fernlet/Tests/FernletUITests/FernletUITests.swift` | UI automation tests for core app workflows. |
| `Fernlet/Tests/FernletUITests/FernletUITestsLaunchTests.swift` | UI launch and performance tests. |
| `Fernlet/Tests/FernletUITests/OnboardingFlowUITests.swift` | End-to-end UI tests for the onboarding flow. |
| `Fernlet/Tests/FernletUITests/PrivacyDataSettingsUITests.swift` | UI tests for the privacy and data settings screen. |
| `Fernlet/Tests/FernletUITests/StoragePrivacyUITests.swift` | UI tests for storage and privacy preference flows. |
| `Fernlet/Tests/FernletUITests/MeshNetworkUITests.swift` | Legacy mesh-lobby UI automation suite. Currently skipped pending replacement with coverage for the active Friends-session disposable-camera flow. |
| `Fernlet/Tests/FernletUITests/UXScreenProbe.swift` | Test harness (`UXTestApp` launcher + chainable `UXScreenProbe` geometry assertions/screenshot capture) backing the appearance UI-test suites. |
| `Fernlet/Tests/FernletUITests/ScreenAppearanceUITests.swift` | Visual/layout regression suite screenshotting every primary tab, private hub, and logging sheet, asserting nothing is clipped or blank. |
| `Fernlet/Tests/FernletUITests/CaptureProtectionUITests.swift` | Asserts the forced Tier-2 capture cover (`FERNLET_UI_TEST_FORCE_CAPTURE`) and its accessibility label render over each of the six capture-protected surfaces; the two JournalView-owned sheets are auto-presented via their DEBUG launch hooks because the cover blocks navigation taps. |
| `Fernlet/Tests/FernletUITests/OnboardingAppearanceUITests.swift` | Walks the 8-screen onboarding flow, screenshotting each screen and asserting its title and primary action button render on-screen. |
| `Fernlet/Tests/FernletUITests/SettingsAppearanceUITests.swift` | Walks each Settings sub-screen (pushed destination), screenshotting and asserting its nav bar renders on-screen. |
| `Fernlet/Tests/FernletUITests/HomeCardsRedesignUITests.swift` | UI test guarding the redesigned First Aid and Milestones home cards render and open their destinations. |
| `Fernlet/Tests/FernletUITests/GoalPresetCardsUITests.swift` | UI test for the Goal & Nutrition preset cards: renders and verifies tapping a card updates the selection. |
| `Fernlet/Tests/FernletUITests/NutritionTargetsEditorUITests.swift` | End-to-end UI test of the Settings nutrition targets editor, covering residual rebalancing and the fat placeholder tracking calorie overrides. |
| `Fernlet/Tests/FernletUITests/RecentBitesUITests.swift` | UI test verifying the Home "Recent bites" polaroid strip of photographed meals renders. |
| `Fernlet/Tests/FernletUITests/RecipeDetailUITests.swift` | UI test for the read-only recipe detail view, opened from both the recipe book and the Food page's Recipes section. |
| `Fernlet/Tests/FernletUITests/ProgressPhotoUITests.swift` | UI test for the Move tab's progress-photo timeline: strip renders and its detail view (caption + delete) is reachable. |
| `Fernlet/Tests/FernletUITests/WorkoutLocationUITests.swift` | UI test for gym-location delete/rename, asserting a deletion persists after closing and reopening the sheet. |
| `Fernlet/Tests/FernletUITests/ItemCreationFlowUITests.swift` | UI test for the split item-creation flow: the drawing-only editor leading to a separate naming/shop-listing confirmation step. |
| `Fernlet/Tests/FernletUITests/DeleteAllDataUITests.swift` | End-to-end UI test proving "Delete everything" empties the app and stays deleted across a relaunch. |
| `Fernlet/Tests/FernletUITests/DeleteAllDialogAppearanceUITests.swift` | Screenshots the "Delete everything?" confirm dialog and logs its rendered copy/buttons for review. |
| `Fernlet/Tests/FernletUITests/TrainerExportShareUITests.swift` | Trainer-summary share flow, including the delete-on-share-completion path. |
| `Fernlet/Tests/FernletUITests/CoachExchangeUITests.swift` | The manual coach exchange from the Move tab: that the gate gates, that both coach cards are reachable (the screen moved off Settings → Privacy & Data on 2026-08-12), and that the paste sheet refuses junk with a visible error. The copy button is only checked for existence and hittability — tapping it presents a data-leaves-the-device confirmation, and a UI test should not be what teaches anyone that dialog is skippable. |
| `Fernlet/Tests/FernletUITests/MoveHeaderActionsUITests.swift` | The Move header after Suggest moved into the plan sheet and Share took its slot — pins BOTH ends, since Suggest missing from the header *and* the plan sheet would strand the whole suggestion engine behind a working-looking app. Deliberately contains no `XCTSkip`: an earlier draft's skips let both plan-sheet tests pass while verifying nothing. |

## Documentation

DocC landing pages (one per SPM module, plus the app and extension targets) are the orientation
layer and live in-source at `Documentation.docc/`; the tables below are the planning-doc layer.

### Orientation

| File | Purpose |
| --- | --- |
| `Docs/FernletSpecificationV3.md` | Canonical product & architecture specification, reconciled with the current implementation on 2026-09-14. Explicit current-status blocks override retained historical prototype notes. |
| `Docs/ImplementationPlan.md` | Historical phase definitions and rationale, plus the reconciled 2026-09-14 status table and current release/mesh ordering. |
| `Docs/FileIndex.md` | This file index. |
| `Docs/ProximityFunctionIndex.md` | Function-level map for proximity, mesh, transport, identity, trust, audit, friend-photo, recipe-share, presence, activities, clothing shop, messaging, and moderation code. Read before adding proximity behavior. |
| `Docs/StoreRepositoryFunctionIndex.md` | Function-level map for `FernletStore`, repositories, persistence, storage preferences, snapshot saves, and the extracted store services. Read before adding data-mutation or save/load behavior. |

### Privacy commitments, walls, and process

| File | Purpose |
| --- | --- |
| `Docs/Privacy-Policy.md` | The published privacy policy text, mirrored by the in-app view and the hosted page; the triple is kept in sync by `PrivacyPolicyParityTests`. |
| `Docs/Verifiability.md` | Standing commitment: the bridge between the promises in the privacy policy and the machinery that backs them. Every guarantee names the exact command, test, or file an outside auditor can run, so "trust us" can be replaced with "run this". The repo is public and `fernlet.com` is deployed. |
| `Docs/Release-Process.md` | Standing process, and the governance half of `Verifiability.md`: branch protection, code-owner review, signed release tags, checksum publication, and the two one-time publish steps. The walls make a privacy regression loud; this makes shipping one attributable and gated. |
| `Docs/No-Tracking-Wall.md` | The enforced no-tracking boundary: permitted network destinations and why each exists, mechanical vs. policy enforcement, and how to add a destination. Backed by `NoTrackingBoundaryTests`. |
| `Docs/PrivacyWipeCoverage.md` | The enforced delete-all coverage checklist, backed by `PrivacyWipeCoverageTests`. |
| `Docs/Crypto-Domain-Separation.md` | The cryptographic domain registry: the full 47-entry inventory across five families, the rule that purpose spellings are FROZEN protocol data (never localized, never assembled from input, never casually renamed), transcript framing and the `91c3956` mismatch it records, which formats are version-marked and the fact that NOTHING reads an older one any more, the procedure and review checklist for adding a domain, and — at length — what the two boundary suites can and cannot catch, including the 6 remaining `// cryptographic-domain:` escape-hatch sites — all annotations, no reader left — and how the count reached them. Backed by `CryptographicPurposeBoundaryTests` (a purpose is USED) and `CryptographicDomainSeparationTests` (the registry's values are USABLE for separation). |
| `Docs/Power-of-10-Swift.md` | The Power-of-10 wall: NASA's ten rules adapted to Swift/SwiftUI, rule by rule — what is enforced by `Scripts/power-of-10-scan.py` + `PowerOfTenBoundaryTests` (zero baseline + reasoned allowlist) and what is enforced by review. Read before writing any function. |
| `Docs/SPM-Module-Carveup-Plan.md` | The `FernletKit` carve-up: the S3 compile-time privacy wall and cross-platform layering. One optional item remains (§14 AI-file inversions). |
| `Docs/App-Privacy-Nutrition-Labels.md` | Draft answers for the App Store Connect App Privacy questionnaire (owner-entered). |
| `Docs/Export-Compliance-Encryption.md` | Encryption export compliance: mass-market, ECCN 5D992.c, self-classified under License Exception ENC with no CCATS. Materially revised 2026-08-19 — the determination held, its *filing consequences* did not: §740.17(e)(3) was rewritten in 2021 to cover only encryption components and "executable software", so the annual February 1 report is almost certainly not owed, and the earlier text of this document asserted a recurring legal obligation that does not exist. |
| `Docs/Export-Self-Classification-Memo.md` | The self-classification memorandum itself — the record §740.17(b)(1) obliges the developer to have made and be able to produce. Five-year retention past the last export. Engineering and regulatory analysis, explicitly not legal advice; §8 lists the two questions that warrant counsel. |

### Reviews and audits (records, not plans)

| File | Purpose |
| --- | --- |
| `Docs/Security-Review-External-Surfaces-2026-08-18.md` | Read-only review of every surface where data or control enters from outside the user's own in-app actions (mesh, CloudKit dead-drop, web import, share extension, camera, bundled SQLite, the AI ladder, HealthKit). **Resolved:** 30 of 35 findings fixed, 5 deliberately not. The original review is preserved as written; where it and the *Disposition* section disagree, the Disposition is correct. |
| `Docs/Proximity-Security-Followups-2026-08-18.md` | The two items deliberately kept OUT of that round's guard commits because they change wire or key-exchange surface, recorded with their deadlines. Also stands in for the referenced `Proximity-Mesh-Redesign-2026-07-10.md`, which does not exist in this tree. |
| `Docs/Memory-Leak-Review-2026-08-17.md` | Whole-codebase leak review (366 files, ~136k lines) plus a verification pass, fixes for every confirmed defect, and the two-part memory-lifecycle wall (`MemoryLifecycleTests` + the grep half) that stops the confirmed classes coming back. |
| `Docs/Doc-Pass-Anomalies-2026-08-04.md` | Bugs, dead code, and smells logged during the DocC pass. **Never triaged** — several entries are real defects, not smells. |

### Security-hardening track (2026-08-10)

| File | Purpose |
| --- | --- |
| `Docs/Design-Capture-Protection-2026-08-10.md` | Screenshot / screen-capture protection for the Private tab — **shipped** as runbook phase P8. Now the design record of a built feature (`FernletUI/CaptureProtection.swift`, six agreed surfaces), including the follow-ups deliberately not built and the manual device matrix still owed. |

### Feature and design plans

| File | Purpose |
| --- | --- |
| `Docs/Calorie-Deficit-Research-2026-09-23.md` | Evidence for a gentler Weight Management starting deficit (CDC, NHLBI, AHA/ACC/TOS, NICE NG246, DGA, NIDDK/Hall, small-changes literature, CALERIE 2, safety floors, AAP/weight-inclusive sources), worked examples, six options, and the shipped recommendation (10% + 1,200/1,500 kcal/RMR floor). **OWNER SIGN-OFF NEEDED** on the value; flags the spec's "no deficit" conflict and minors. |
| `Docs/Companion-Emotions-Scoping-2026-09-23.md` | Scoping only (nothing built): how `CompanionState` is derived, every surface that renders it, the frozen-token / widget / friend-fold / export constraints, presentation-only accents (Route A, recommended) vs new cases (Route B), sizes, and existing-art interim options for "a happy companion on a hard day". |
| `Docs/FernletCoach-Specification-2026-07-19.md` | The Fernlet Coach track (P0→P4), both apps. **§3.3/§3.6/§6/§8 need revising** — they still describe the iMessage+CloudKit hybrid as primary, reversed by the owner on 2026-07-26. |
| `Docs/Plan-Prekeys-ProtectedLoad-CoachMesh-2026-07-26.md` | Sidecar durability, signed prekey, and the coach path. Tracks A and B shipped; Track C stopped at Increment 10's hardening subset — the coach session manager/UI is the outstanding work. |
| `Docs/Data-Provenance-Coach-Trust-2026-07-12.md` | The coach trust model the coach spec builds on: origin class, trust basis, and per-source revocation. |
| `Docs/D11-LinkMetadata-Prototype.md` | Harness for the coach P0 `LPLinkMetadata` device test. |
| `Docs/AI-Provider-Ladder-2026-07-23.md` | The routed provider ladder. Provider seam shipped; the cloud/PCC, BYOK, and iOS-27 tracks remain gated. |
| `Docs/AI-Feature-Expansion-2026-07-23.md` | Seven features riding the ladder, and the canonical build order for both AI docs. F3/F4/F5 shipped; F6/F7 deferred by decision D-C. |
| `Docs/Meal-Estimation-Overhaul-Plan.md` | Meal-estimation overhaul. Partially shipped; the chain-restaurant importer and dynamic product-image discovery were not re-audited item-by-item. |
| `Docs/Multi-Device-Without-iCloud-Design-2026-06-29.md` | Multi-device without iCloud. Phase 1 shipped; Phases 2–3 (owned-device pairing, mesh backup transfer, offline escrow, live merge) unstarted. |
| `Docs/Localization-Plan-2026-07-19.md` | es/fr/de localization. **Phase 0 (locale-correctness fixes) and Phase 1 (string-catalog plumbing + the token/display forks) shipped 2026-08-19**; the Phase 1 bulk string conversion and Phases 2–5 remain planned only. See the Localization section above for the token/display rule the forks established. |
| `Docs/Sealed-Backup-Escrow-Recovery-FollowUp-2026-06-28.md` | Sealed-backup escrow recovery (including BIP39 recovery codes) and the "nothing destructive happens silently" settings contract. **SHIPPED** — all five workstreams landed; the doc's own header once read "not started" long after the work was done. |

### Runbooks and validation scripts

| File | Purpose |
| --- | --- |
| `Docs/CloudKit-Schema-Deploy.md` | Runbook for pushing the Core Data model to CloudKit's server-side schema — a manual, developer-run ritual. Record types added since the last deploy are listed in the persistence sections of the store index. |
| `Docs/Friend-Shop-Real-Device-Validation.md` | Two-device validation script for the in-person friend shop. |
| `Docs/Recipe-P2P-Real-Device-Validation.md` | Two-device validation script for recipe sharing. |
| `Docs/Mesh-Network-Feasibility-Runbook.md` | Required physical-device validation gate before production background mesh transport work. |
| `Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` | Two-to-three-device checklist for network migration P7 — the run policy's tab/scene edges and hard stops, the nearby-setting toggles, the poller's three verdicts (partition, the 30-minute idle stop, the six-hour ceiling), and the resume card for every reachable restore outcome — plus plan §15's hardware gates as section F, so the phone drawer opens once. **Cannot be run until P7's Mac gauntlet is green** (P7 was written without a toolchain); its results table is blank on purpose. |
| `Docs/Plan-ProximityKit-Network-Migration-2026-08-27.md` | The authority for the ProximityKit network migration (QUIC transport, durable sessions, partition + merge, the routed store, feature routing, the app-layer run policy, background continuation): one section per phase, each marked BUILT with landing SHAs and its §n.1–§n.4 close-out (what landed / deviations / findings not fixed / acceptance evidence), and one handoff section per boundary (§19–§25). **§13 (P7) is IMPLEMENTED, UNBUILT** — the one phase whose heading is not BUILT, because nothing in it has compiled. |
| `Docs/Mesh-P9-Item4-Design-2026-09-20.md` | P9 item 4's survey of the MultipeerConnectivity retirement, written before any code landed: why the launcher's row was wrong about HEAD (the friend mesh still ships on MC; the QUIC radio is members-only, so a first meeting between two phones that have never met cannot found a mesh on it), the D-4.1/D-4.2/D-4.3 decision with D-4.4, the acceptance criterion, the walls that bite, and seven appendices — the rule-7 cell verbatim and six anchored patches (`Info.plist`, `MeshNetworkManager.swift`, `MeshTransportSelection.swift`, `TransportNeutralityBoundaryTests.swift`, docs, tests), every hunk marked `[SPLIT: NOW]` (landed as 9.4-NOW) or `[SPLIT: LATER]` (the cutover). *Row added 2026-09-21; the file had shipped without one.* |
| `Docs/Mesh-Stranger-Admission-Design-2026-09-21.md` | The design D-4.3 was waiting for: how the QUIC radio admits a stranger at a first meeting so the MC transport can be retired. Compares the two radios' admission stages side by side, names two owed items the survey did not (the meshID check precedes the roster check, so a mesh-less joiner dialing an open mesh needs a target-mesh rule; the `browsed peers=` log level), and prices three options — provisional admission while the join doors are open (recommended: MC's own posture, key proven, no screen, no wire field, no persisted surface; changes only the survey's `MeshNetworkManager.swift` edits 5 and 6), a two-scan QR pre-admission (strongest, needs a pre-session screen), and keeping MC for admission (ruled out) — with trust consequence, UI cost, tests owed and appendix patches changed for each. Ends with the one-sentence D-4.3 decision, D-4.4 riding with it as the legacy sweep. |
| `Docs/Mesh-Migration-Loop-Ledger-P9.md`, `…-Cutover-2026-09-21.md`, `…-Device-2026-09-22.md`, `…-Owner-Calls-2026-09-22.md` | The loop ledgers still in the tree. P9's and the cutover round's are read from disk by `MeshP9AcceptanceTests` — the record of item 0's owner block, 9.4-LATER, P9-3-A and the owner's D-4.3/D-4.4 answers — so they stay. The device round's and the owner-calls round's (with its launcher, `Docs/Next-Round-Prompt-Owner-Calls-2026-09-22.md`) carry the owed soak read-out and leave once it is read. Every other round's launcher, ledger and progress log is local session material — out of the tree since 2026-09-22; `e6a3b48` holds the last copies. |
| `Docs/design-refs/*.html` | Design mockups the shipped UI was ported from (widget, first aid, companion moments, home ambiance, milestones, good vibes, barcode handoff). Cited from `FernletWidgetsBundle.swift`. |
| `Docs/design-refs/ux-review-2026-08-16/` | The 2026-08-21 redesign canvas (`Fernlet-Redesign-2026-08-21.dc.html`) and its text extraction (`design-spec-2026-08-21.md`, cited from `FernletTheme.swift` and `FernletUIComponents.swift`). The 2026-08-16 review's screenshot sweep, its `review.html` index and the round's prompts are local-only; the findings report is in history at `e6a3b48:Docs/UI-UX-Review-2026-08-16.md`. |


## Scripts And CI

| Path | Purpose |
| --- | --- |
| `Fernlet/Scripts/spm-wall-check.sh` | The ONE strict build: `DIAGNOSE_MISSING_TARGET_DEPENDENCIES=YES_ERROR` (S3 wall — a forbidden cross-wall `import` fails the build) plus `SUPPRESS_WARNINGS=NO SWIFT_TREAT_WARNINGS_AS_ERRORS=YES GCC_TREAT_WARNINGS_AS_ERRORS=YES` (Power-of-10 rule 10 — every warning, including the package warnings Xcode otherwise hides, is an error). Run pre-merge and in CI. |
| `Fernlet/Scripts/power-of-10-scan.py` | Power-of-10 mechanical scan (rules 2/4/5/6/7/8/9 + assertion-density floor + allowlist hygiene); zero-violation baseline; `--advisory` lists the review-only signals; `--json` for tooling. |
| `Fernlet/Scripts/power-of-10-allowlist.json` | The single reasoned allowlist read by both the scanner and `PowerOfTenBoundaryTests`; every entry names the invariant that makes the exemption safe. |
| `Fernlet/Scripts/doc-coverage-scan.py` | Scans Swift sources for type declarations with no adjacent `///` doc comment. Zero undocumented is the enforced baseline; runs in the Power-of-10 CI workflow. |
| `Fernlet/Scripts/sync-string-catalogs.sh` | Repopulates the `.xcstrings` catalogs outside Xcode via `xcstringstool sync` over the `.stringsdata` a build emits. Run after changing user-facing strings and commit the catalog diff with the code; `--check` for CI. Additive — vanished keys are marked stale, never deleted. |
| `Fernlet/Scripts/run-gated-suites.sh` | Every CI test step's runner (P5 review finding 4): runs the named suites against the already-built bundle with `-resultBundlePath`, then reads the bundle's own `totalTestCount` and refuses a green run under the step's floor — because `-only-testing:` matches suite names exactly, matches nothing on a misspelling or a deleted suite, and then prints `TEST EXECUTE SUCCEEDED` over zero tests. `CIGateSelectorBoundaryTests` is the static half. Usable locally: `Scripts/run-gated-suites.sh <label> <min> <Suite>…`. |
| `Fernlet/Scripts/run-gated-suites-selftest.sh` | Negative test proving the restart guard in `run-gated-suites.sh` is load-bearing on its LIVE branch, not only at its `--check-log` seam (P10 item 5): stubs `xcodebuild` and `xcrun` on `PATH` so the gate reads a green-looking run whose count clears the floor, and asserts the script still exits non-zero when the run output carries `Restarting after unexpected exit, crash, or test timeout` — a restart re-runs suites, so the bundle's `totalTestCount` is inflated and every floor above it stops proving anything. Four assertions: live negative/positive and seam negative/positive. Needs no Simulator, no build and no Xcode; runs on the enforcement self-test step of `s3-wall.yml`. |
| `Fernlet/Scripts/release-checksum.sh` | Builds a release archive, emits SHA-256 checksums, and cuts a signed tag ([Release-Process.md](Release-Process.md), [Verifiability.md](Verifiability.md) §2). Honest limit, stated in the script itself: byte-exact verification of an App Store binary is impossible — Apple re-signs and re-encrypts every submission. What this gives is the self-build baseline: the signed tag pins the source, the checksums let two independent builders compare for drift, and a user can sideload their own build of the tag. |
| `Fernlet/Scripts/spm-wall-selftest.sh` | Negative test proving the wall is load-bearing: plants a forbidden `import PrivateHealthStore` in `AIProviders`, asserts the build fails, then reverts. |
| `Fernlet/Scripts/install-git-hooks.sh` | Once per clone: points `core.hooksPath` at the versioned `Scripts/git-hooks` so pushes run the wall check. |
| `Fernlet/Scripts/git-hooks/pre-push` | Versioned pre-push hook: runs `power-of-10-scan.py` on every push and `spm-wall-check.sh` when a push touches any Swift/manifest/project file. |
| `Fernlet/Scripts/branded-catalog/` | Python pipeline converting the USDA branded dataset into the curated and On-Demand-Resource SQLite catalogs. |
| `Fernlet/.github/workflows/s3-wall.yml` | CI workflow running the wall enforcement self-test, the seven wall suites and — since 2026-09-06 — the three mesh acceptance batteries (28 suites) on push/PR to `main`; every test step goes through `Scripts/run-gated-suites.sh`, so a step that ran zero tests cannot pass. **2026-09-21 (the cutover round, item 2a):** the "Resolve a simulator destination" step is a hard pin — `iPhone 17` or the job fails in seconds, no fallback to another device (plan §17.2.3 finding 6); read by `CIGateSelectorBoundaryTests.theSimulatorDestinationIsPinnedWithNoFallback`. |
| `Fernlet/.github/workflows/power-of-10.yml` | CI workflow running the Power-of-10 mechanical scan and the doc-coverage scan on push/PR to `main` (Ubuntu, no Xcode). |

## Dependency

| Path | Purpose |
| --- | --- |
| `Fernlet/FernletKit/` | The local Swift package holding the carved-up on-device modules (see the FernletKit Package module map above). |
| CryptoSwift | Remote SPM dependency (`.upToNextMinor` from 1.10.0) supplying the Scrypt KDF used by `FernletLock`; no longer checked into the repo. |
