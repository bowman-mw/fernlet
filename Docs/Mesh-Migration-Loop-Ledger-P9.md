# Mesh Migration Loop Ledger — P9

**Phase:** P9 (the remaining radios, MC retirement), with P8's device gate still open · **Prompt:** [Next-Round-Prompt-Mesh-P9-2026-09-19.md](Next-Round-Prompt-Mesh-P9-2026-09-19.md)
**Started:** 2026-09-19 · **Iteration:** 5 (item 2 tier 2 + its fixes, item 3 pass 1 Phase A; closed 2026-09-20 — next session resumes at item 3 Phase B) · **Tree at seed:** `main` = the P8 close-out (`bb454fe`); P8 BUILT at tier 1/1b, §15 NOT RUN; the P7 lane fix `80934b7` is on `main` (HEAD~1 at seed)

**Environment of this session:** a Mac with Xcode 26 and the iOS 26.5 SDK, working in the worktree `.claude/worktrees/wizardly-haslett-ddce10` on branch `claude/loving-bell-296321` (its own DerivedData); `main` is fast-forwarded from it in the primary checkout after each iteration. The primary holds `App/Fernlet/Localizable.xcstrings`, `xcuserdata/**`, the plan and several `Docs/*.md` uncommitted — every commit is by explicit pathspec, plan edits land as index-only blobs. **Devices:** exactly ONE physical device connected (`iPhone 17 Pro Max`, iOS 26.6.1, `xcrun devicectl` state `connected`); the owner is not at the keyboard. Item 0's rows need two to four phones in the owner's hands (lock, Control Centre, walks, 3 h / 6 h soaks), so they are `blocked (owner)` — P9 is NOT gated by them.

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 0 | P8's device lanes (§15.1–§15.4, device plan A–F) + the two owed re-runs; the ladder decided | 3 | — | blocked (owner) | — | one device connected, two to four needed plus the owner's hands. **Device-free half DONE 2026-09-19:** this ledger, item 1 (`dad86e9`) and its tier-1 walls. **Lane C confirmation DONE 2026-09-19** (`2af5767`, runbook § "Lane C — P9 item 0"): the lane **DISCOVERS at HEAD** — the pair (13 `[mesh-quic]` lines per node, `slots total=1 committed=1` on both, 2.6 s), THREE nodes (3 tunnels, a full mesh, 3.1 s), FOUR nodes (6 tunnels, a full mesh, ~3 s), a pair repeat (2.0 s); zero `stopJoin` in any audit stream; `80934b7` confirmed. **Tier 2 may be believed for items 2/3.** Owner asked for the 3/4-node runs; both passed |
| 1 | The `assertionFailure`-in-`catch` family | 1 | — | done | `dad86e9` | 40 traps enumerated (not 19): **27 environmental sites FIXED** (26 through the new `PersistenceFailureAudit.record` in `FernletFoundation`, token + `NSError` domain/code only; DiaryStore's existing token kept, its day-key context dropped), **12 programmer-error guards LEFT**, 1 DEBUG demo-seed site reverted after the verify doubted its class; 27 cells one-to-one in `PersistenceFailureAuditTests`. Verify → **FIX FIRST**, 5 fixes + 2 notes applied: **a failed recipe payload encode left the previous `payloadData` on the record and the read path served the pre-edit recipe back — a silent edit revert the removed assert was the only signal for** (would have shipped; the catch now clears the blob); purge cells were clearing `UserDefaults.standard`; two docs claimed a rollback the encode path does not do; the day key in an audit; a duplicate import. Gates: scans 0 / 0, build green, **392 tests / 26 suites green**, no restart, `spm-wall-check` PASSED; three red-onces kept (`…/scratchpad/item1/redonce-RED.log`, `…/item1-fix/test-pfa-RED.log`). No full-suite run |
| 2 | Presence over QUIC — posture first | 1 + 2 | — | done | `9f78111` (pass 2) · `d7342f3` (pass 1) | **Pass 1 DONE:** `PresenceEpochPosture` (pure value; `fn-` + 8 CSPRNG bytes hex; identity via `EphemeralMeshTLSIdentity.mint` at the **epoch START**), wall-clock-anchored epochs, the seam `PresenceManager.presencePosture` on the existing tick; 9 + 4 cells; nothing over the air. Verify → FIX FIRST (2 fixes, 7 notes, all applied): **the certificate's validity window encoded the mint SECOND** — a mid-epoch start was singled out of the anonymity set (fixed: minted at `presenceEpochStart`); docs overstated the shipping property; mint retry budgeted once per epoch. Gates: 293 / 21 green, wall PASSED, three red-onces. **Pass 2 DONE (`9f78111`):** `NetworkPresenceSession` on `_fernlet-near2._udp` (plist + no-tracking permit + §4c row, one commit), `PresenceManager` MC-free behind a `PresenceRadioSession` seam, `ProximityQUICParameters` extracted (mesh values byte-identical), TXT chunked `t`/`t1` (24 tags were 311 B > 255 under MC). Verify → **FIX FIRST** (1 BLOCKER + 3 FIX + 6 notes, all applied): **a dial miss after a friend's own boundary stopped the WHOLE radio**; task cancellation reported as a start failure (every 900 s); peer names in `sighted`; **glare refused both sides**; the mint budget lost on `stop()`. Gates: 509 / 41 green, wall PASSED, five red-onces. **Tier 2 PASS 2026-09-19** (`09f09f6`, runbook § "Lane C — P9 item 2"): both sides rotated +0.8 s after 21:45:00 and re-sighted each other 1.3 s later, no byte in common; a heart across the new epoch connected both ways; glare not reached (taps 1.35 s apart — inconclusive); a second rotation correct but **51 s late**. **Tier-2 findings fixed (`b3f9de9`):** P9-2-A the "opaque" peer key WAS the Bonjour service name → salted per-session digest labels, five peer-naming log lines swept; P9-2-C the boundary is a deadline awaited in ≤ 30 s steps (device drift measure owed). P9-2-B (a sole-friend pair never surfaces — layer-3 self-exclusion) is an accepted residual: **every presence lane run must seed a third friend** |
| 3 | Recipe share over QUIC — pause/resume preserved | 1 + 2 | 2 | in-flight (pass 1 Phase A: designed + drafted, UNBUILT) | — | **Premise corrected** (design note `Docs/Mesh-P9-Item3-Design-2026-09-19.md`, `09f09f6`): the share is ONE sealed frame — pause/resume is the RADIO's discovery pause (`pauseDiscovery`/`resumeDiscovery` while paired; 5 cells in `ProximityRecipeShareCapTests`); the recipe radio used the PERSISTENT device-name `MCPeerID`, so pass 2's posture is an improvement. Pass 1 = `RecipeShareDiscoveryGate` (total table) + `RecipeShareTransfer` (exactly-once oracle) + the advertised-name BYTE bound (a naive QUIC binding drops a 24-CJK name) + 2 rule-7 needles; 16 cells drafted. Drafts at `<session scratchpad>/item3p1/drafts/` (a `/private/tmp` path — may not survive a reboot; the design note is the durable record). **Phase B next:** re-read every target at HEAD, apply, build, red-once ×2, then verify → fix → commit. Pass 2 needs a per-transfer-stream acceptor `NetworkPresenceSession` has none of |
| 4 | Retire MultipeerConnectivity (2 files, 6 or 8 plist strings, the wipe row, the permit list) | 1 | 2, 3 | todo | | one commit; the coach pair is §18 decision 4 |
| 5 | The 1:1 foreground anchors — widget or retire | 1 | — | todo | | default: retire |
| 6 | Gate the fourteen (247 cells / 11 s) | 1 | — | todo | | floor re-measured |
| 7 | The six process-global drain counts + `parkedReoffered` | 1 | — | todo | | D-6a.10 shape |
| 8 | The two accessibility-ratchet baselines | 1b | — | todo | | fresh Simulator; **needs no device** |
| 9 | P9 acceptance battery + CI lines | 1 | 2–4 | todo | | pin and floor measured (48 / 469 at the P8 boundary) |
| 10 | Close-out: §17.1 BUILT, §15 updated, the P10 handoff, the next launcher, memory | 1 | 0–9 | todo | | draft → verify → apply |

## Blocked on owner
- **Devices for item 0** — two minimum (rows A–E, F1–F6, F9, F11, F12), three for E3 / F8, four for F7. The runbook's Lane B table (`Docs/Mesh-Network-Feasibility-Runbook.md` § "Lane B", line ~1939) is the evidence table; the device plan's Results table (`Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` § Results) takes A–E. **Every row below is NOT RUN as of 2026-09-19:**
  - Device plan **A1–A8, B1–B4, C1–C2, D1–D5, E1–E4, E7** (E5, E6 are tier-1-only by design).
  - **F1–F12** (plan §15.1–§15.4; F10 is the owner's call; F11 = Lane D phone ↔ Simulator, cable out, the cheapest first run).
  - The **tier-3 rows P8 item 6 named**: registration accepted; a `.fail` submission **granted** (no grant observed anywhere); the launch and expiration handlers firing on the real `SystemContinuationScheduler` conformer; the tunnel surviving the whole task; slow progress across the 3 h / 6 h soaks; no proximity Live Activity present; the Control-Centre peek re-submission (F12).
  - The **re-run of P8 item 0's four founding fixes (a)–(d)** on the fixed build, with the **three eyeballs**: Friends tab entry/exit (observed incidentally on Simulators only), Control Centre over the Friends tab, delete-all during a live session.
  - **§15.3 decides the degraded ladder** — unchosen until the soak runs; plan §14's scope unchanged.
- **Device measure owed for P9-2-C**: does a real phone's boundary wake drift like the Simulators' (+51 s over a 767 s arm)? The 30 s step bounds it either way; a phone number decides whether the step can widen.
- **Owner follow-up:** `NetworkMeshSession`'s listener/browser task bodies report their own cancellation through `onTransportError` (`discoveryError`, a banner) — left alone in item 2; a chip was filed. Starting it is safe from git's standpoint (it branches from `main`), but it edits `NetworkMeshSession.swift`, which item 3 pass 2 and item 4 also touch — expect a rebase — and its build competes for CPU with the P9 build slot.
- **The drawer is now safe to open in DEBUG**: item 1 removed every environmental trap a lock or background row could fire (`dad86e9`).
- Carried from plan §26.4 (read it; do not re-list it here).

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| The first iteration | item 0's device-free half (this ledger, item 1, its tier-1 walls); item 0's device rows `blocked (owner)` | 2026-09-19 |
| Item 1's scope | **the whole family** — every `assertionFailure` inside a `catch` (or a `guard let … try? … else { assertionFailure }`) that fires on an environmental Core Data / file I/O / encode failure, in every module: the acceptance is per site, any one of them crashes a DEBUG lock row. Programmer-error asserts stay (12, each justified in the commit). Owner may narrow. | 2026-09-19 |
| Item 1's demo-seed site (`FernletStore+DemoSeed.swift`) | **reverted to its `assertionFailure`** — `#if DEBUG` demo code that never runs on a locked device, and `seeded == nil` from a bundled image is as likely bad seed data as an I/O failure; no seam for a cell | 2026-09-19 |
| Item 1's `upsert` return after a failed recipe payload encode | **stays `true`** — the fresh legacy columns are durable and a `false` would retry-loop on a non-finite value; the structured half is dropped (`payloadData` cleared), audited (`savedRecipe.payloadEncode.failed`) and documented at `apply`, `upsert` and the landing page | 2026-09-19 |
| Presence posture | **reproduced before the framing** (pass 1 `d7342f3`): epochs anchored to wall-clock multiples (the tag epoch is already absolute; a per-launch phase is a rotation-surviving fingerprint; synchronized rotation maximizes the anonymity set); the certificate minted at the epoch START, never at `now`; eager mint (one keygen per 900 s); a failed mint retried once per epoch | 2026-09-19 |
| Presence glare | inbound for a held/dialing key collapses through `MeshTunnelConvergence` ranked over the two instance names (the mesh's `sid` pattern); `presence.quic.redundantTunnelClosed` | 2026-09-19 |
| Presence tag-replay squatting | ACCEPTED residual (option b): an unauthenticated dialer replaying a sighted tag occupies the friend's `MeshLinkKey` slot for ≤ 2 000 frames / the QUIC idle timeout; the sealed introduction still refuses everything; a name-ranked glare can evict a live tunnel (one failed heart send, retried). Option (a) — file under the connection id until the introduction succeeds — needs a new seam for the coordinator's verdict; deferred | 2026-09-19 |
| Presence audit lines | our own posture (`advertised`/`rotated`: epoch, name, certificate digest, `.private`); peers by opaque session key + tag COUNT only | 2026-09-19 |
| Recipe share | **pause/resume = the radio's discovery gate** (there is no transfer to pause — one sealed frame); pass 1 pins it as a total table; pass 2 binds `_fernlet-recipe2._udp` in a new `NetworkRecipeShareSession.swift` with a per-transfer-stream acceptor | 2026-09-20 |
| Recipe-share posture | per-`start()` fresh name + TLS identity via `MeshLinkAdvertisement.randomInstanceName()` + `EphemeralMeshTLSIdentity.mint()` (the mesh's own shape), NOT `PresenceEpochPosture` (its 900 s epoch and epoch-start certificate would be a silent mismatch on a per-tab-visit radio); no rotation timer; bound = one name per Food-tab visit; `sid` minted per start in pass 2 | 2026-09-20 |
| Presence peer labels | salted per-session SHA-256 digest, 12 hex (`peerLabel(for:)`); never the key, never a plain hash of a public name | 2026-09-20 |
| Presence boundary arm | a deadline awaited in ≤ 30 s steps on the one existing task (`maxEpochRotationStepSeconds`) | 2026-09-20 |
| When MC is deleted | (default: same phase, after both radios cross) | — |
| The coach radio (plan §18 decision 4) | (default: hold the two `_fernlet-coach` strings; drop the other six) | — |
| The 1:1 anchors | (default: retire) | — |
| Gate the fourteen | (default: yes) | — |
| New persisted surface | (default: none) — item 1 added none | — |
| The degraded ladder | (decided by §15.3 in item 0) | — |

## Surprises worth not re-deriving
- **The assert family was 40 traps, not 19.** The plan's 19 counted the literal `catch` shape; eight siblings were `guard let x = try? … else { assertionFailure }` on the same environmental failures (a store fetch, a payload encode of a non-finite `Double`). Enumerate by failure class, not by syntax.
- **Removing an `assertionFailure` can remove the only signal for a silent data revert.** `SavedRecipe.apply`'s encode `catch` left the previous `payloadData` on the record; the read path prefers the blob whenever the legacy projection has not diverged, so an edit to a structured-only field vanished on the next load. Every converted `catch` must leave the row in the state the *read path* expects, not merely "not trap".
- **A controller whose store never loaded is NOT a throwing rig**: zero stores → empty fetch, no-op save, nothing throws (and an insert crashed the runner). To make saves throw, re-attach the SAME store with `NSReadOnlyPersistentStoreOption`; to make fetches throw, overwrite the database file (and WAL) under the open connection — SQLite re-reads page 1 per read transaction.
- **A test that builds `LocalFernletRepository` without `legacyDefaults:` purges `UserDefaults.standard`** when it drives `purgeAllPersistedData()` — the seam's own doc comment names the hazard; pass `isolatedDefaults()` and `backupExclusionPreference: { false }` on every construction.
- **`power-of-10-scan.py`'s assertion-density floor counts `assertionFailure` as a check**: 27 removals moved 0.775 → 0.770 against a 0.68 floor. A future sweep of this shape moves it further.
- **A continued-processing task is delivered while the app is still in the foreground.** Any state meaning "we are continuing in the background" keys on (task in hand ∧ scene dark).
- **Gating cost is not proportional to cell count**: +92 % cells cost +10 % wall time on a tier-1 step. Price a gate before deferring it.
- **A hold is not a stop**: every clock that assumes browsing must pause with the radios — grep every `arm…Clock` when adding a pause-shaped verb.
- **A terminal state erases its cause unless a projection rule keeps it**; and **a reset that clears state without walking the table strands every side effect the table owed**.
- **`hasSlot(for:)` means ANY slot** — read the body of every predicate a new door consults; a **re-entry row keys on facts, not on `previous`**.
- **A protocol requirement declared in an app file is a second occurrence of a grep-wall's needle**; **a grep for `@Suite` under-counts the suites** (some are plain structs found by their `@Test`s).
- **A background build's notified exit code is the last command's**; **a Simulator that has run ~3 h stops rendering** — erase and reboot before believing a UI red.
- **Every P8 adversarial verify found something real.** Seven for seven; five would have shipped. **P9 item 1's did too** (the stale blob).
- **Every P6 and P7 lesson still binds**: a log with `Restarting after unexpected exit` has no usable total; never chain a build and a run on one DerivedData; `-only-testing:` names the SUITE; `@Test(arguments: [])` is green over nothing; a process-global audit signal cannot witness a per-cell claim; `.inactive` is foreground for the radios; `FernletLockState` carries associated values; the chat age gate is `AgeGate.chat`; zsh passes an unquoted `$ARGS` as one argument.
- **Closed; do not re-audit:** `MeshTunnelConvergence`, the id-vs-endpoint family, the crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10, §11.1–§11.4, §12.1–§12.4, §13.1–§13.4 and §14.1–§14.4; `Docs/Proximity-Security-Followups-2026-08-18.md` §1. Concurrent sessions share the tree; never stage the catalog working copy, `xcuserdata`, or the stray PDF.
- **A worktree has its own DerivedData** (the path hash differs from the primary's): the first build there is cold (~20 min here), and "one `xcodebuild` at a time" is a CPU rule as much as a DerivedData rule — `pgrep -x xcodebuild` still gates every start.

- **Rebuild the app before a lane run, always**: the "warm" app binary was stamped one minute BEFORE the last code commit, so it could not have contained it — a build log's date is not the tree's date.
- **Kill audit streams by saved PID, never `pkill -f "log stream"`**: the pattern matched a concurrent session's `backboardd` orientation stream on a Simulator (spawned through Claude.app's helper) and killed it.
- **Four Simulators are as cheap as two**: boot-to-usable ~25 s on the fourth, and the four-node full mesh converged in ~3 s — the same as the pair. At N ≥ 4 two `tunnel activated, tunnels=2` lines can land in one instant (three distinct sids, final `tunnels=3`) — a log-ordering artefact, not a duplicate.
- **A seeded harness run converges the descriptor roster only** (`membership ledger=absent derived=0`) — correct per the 2026-09-12 correction; do not read it as a derived-roster proof.

- **A rotation test can only see fields that CHANGE.** The certificate's validity window is the one part of the posture that is supposed to be identical across devices, so the raw-`now` leak survived the whole rotation table; the cell that catches it asserts two mints at different instants in one epoch are byte-identical. When a value must be unlinkable, pin the fields that must match as hard as the ones that must differ.
- **The MC presence radio wears one name for its whole life while its tags rotate** (`MeshMultipeerSession` mints the `MCPeerID` in `init`; `updateDiscoveryInfo` restarts only the advertiser). Presence must not reach testers over MC; item 2 pass 2 / item 4 remove the path.

- **A transport-error door that was start-failure-only under MC becomes any-error under QUIC.** `handleTransportError → stop()` was reachable only from `didNotStart*` under MC; a QUIC session that `report()`s a per-dial miss or its own task cancellation through the same door stands the whole radio down. Every `report`/`onTransportError` call in a new session must be classified: start failure (stand down) vs. per-operation refusal (log and return) vs. cancellation (silent).
- **24 advertised presence tags were already 311 bytes — over DNS-SD's 255-byte TXT entry limit.** MC's behaviour on an oversize `discoveryInfo` value was never established; `NWTXTRecord` refuses or truncates. The TXT is now chunked `t`/`t1` (capacity 36) with a cell pinning `maxAdvertisedTags <= capacity`.
- **Owner follow-up, not fixed:** `NetworkMeshSession`'s listener/browser task bodies (`:~834`, `:~877`) have the same unguarded `catch { report(…) }` shape; the mesh's owner only sets `discoveryError` (a banner), so a real cancel may show a spurious banner — left alone because guarding it changes what the mesh records.

- **Two foreground agents cannot hand a build slot to each other through the orchestrator.** The item 3 drafter polled for the marker file for 100 minutes while the orchestrator was blocked waiting on both it and the lane runner; the marker could only be created after both returned. Either run the drafter in the BACKGROUND, or let the runner create the marker itself when it is done with the tree.
- **`-only-testing:FernletTests/<FileName>` matches nothing and reports nothing** — the run proceeds with the other suites and the file's suites silently contribute zero. Name the struct; count the suites in `Test run with N tests in M suites` against the list.
- **A sole-friend pair never surfaces in presence**: layer-3 self-exclusion drops an ad whose token set is a subset of our own, and two devices whose only friend is each other advertise identical sets. Accepted residual; seed a third friend on every presence lane run, and remember the diagnostic is in-memory only.
- **The launcher's item 3 premise was wrong twice** (verified by the pass-1 survey): the recipe share is one sealed frame with no chunks or offsets, and the recipe radio used the persistent device-name `MCPeerID`. Read the file before believing a launcher row's description of it.
- **A `MeshLinkKey` built from a Bonjour endpoint id IS the advertised name.** "Opaque" must be proven by a cell over the audit capture, not asserted in a comment.
- **A long single `Task.sleep` to a wall-clock deadline wakes late in proportion to its length on a loaded host** (+0.8 s at 300 s, +51 s at 767 s, both Simulators together). Await deadlines in bounded steps and re-read the clock on every wake.

## Next item
**3, Phase B** — apply item 3's pass-1 drafts (re-read every target at HEAD first), build, red-once ×2, then verify → fix → commit; then pass 2 (`NetworkRecipeShareSession`, the transfer-stream acceptor, the tier-2 paused-and-resumed share on two Simulators with a picture recipe and a THIRD friend seeded). Item 2 is done; its tier-2 row was against `9f78111`; then item 3. Pass 1 (the posture) is at `d7342f3`. The Lane C lane is confirmed healthy (four runs, 2026-09-19, `2af5767`); pass 2's tier-2 row may be believed. The recipe and UDIDs are in the runbook's new subsection. Item 8 needs no device and may be taken whenever the build slot is free. Item 0's device rows stay `blocked (owner)` until two phones are in hand.
