# Mesh Migration Loop Ledger — P9

**Phase:** P9 (the remaining radios, MC retirement), with P8's device gate still open · **Prompt:** [Next-Round-Prompt-Mesh-P9-2026-09-19.md](Next-Round-Prompt-Mesh-P9-2026-09-19.md)
**Started:** 2026-09-19 · **Closed:** 2026-09-20 · **Iterations:** 10 (iteration 9 ran 9.3 through 9.7 back to back with their verify fixes; iteration 10 is 9.8, 9.9, their verify fixes and this close-out; notation `9.<item>.<tier>`) · **Tree at seed:** `main` = the P8 close-out (`bb454fe`); P8 BUILT at tier 1/1b, §15 NOT RUN; the P7 lane fix `80934b7` is on `main` (HEAD~1 at seed) · **Tree at close:** `main` carries this close-out's two commits — this ledger + the P10 launcher here in the worktree, and plan §17.1/§15/§26.1/§27 as an index-only blob in the primary — and is **NOT pushed**

**Outcome:** plan **§17.1 is BUILT** (tier 1 + 1b + **tier 2**, both acceptance rows crossed) with
**three exceptions recorded there and here**: MultipeerConnectivity was **not** deleted (item 4 SPLIT;
the LATER half is the friend mesh's MC→QUIC cutover and waits on the owner), item 0's device rows
**never ran** (one phone, two to four needed), and **no full suite ran this phase** (the owner's
standing instruction since P8 item 0 — the last measured full suite is P8's 5 055 / 513). Every gate
number below is a measured subset. **Ten adversarial verifies ran and every one found something
real.** **Next phase: P10**, `Docs/Next-Round-Prompt-Mesh-P10-2026-09-20.md`.

**Environment of this session:** a Mac with Xcode 26 and the iOS 26.5 SDK, working in the worktree `.claude/worktrees/wizardly-haslett-ddce10` on branch `claude/loving-bell-296321` (its own DerivedData); `main` is fast-forwarded from it in the primary checkout after each iteration. The primary holds `App/Fernlet/Localizable.xcstrings`, `xcuserdata/**`, the plan and several `Docs/*.md` uncommitted — every commit is by explicit pathspec, plan edits land as index-only blobs. **Devices:** exactly ONE physical device connected (`iPhone 17 Pro Max`, iOS 26.6.1, `xcrun devicectl` state `connected`); the owner is not at the keyboard. Item 0's rows need two to four phones in the owner's hands (lock, Control Centre, walks, 3 h / 6 h soaks), so they are `blocked (owner)` — P9 is NOT gated by them.

## Items
States: `todo` / `in-flight` / `done` / `blocked` / `skipped (reason)`. Tier per §2.
| # | Item | Tier | Prereq | State | SHA | Note |
|---|---|---|---|---|---|---|
| 0 | P8's device lanes (§15.1–§15.4, device plan A–F) + the two owed re-runs; the ladder decided | 3 | — | blocked (owner) | — | one device connected, two to four needed plus the owner's hands. **Device-free half DONE 2026-09-19:** this ledger, item 1 (`dad86e9`) and its tier-1 walls. **Lane C confirmation DONE 2026-09-19** (`2af5767`, runbook § "Lane C — P9 item 0"): the lane **DISCOVERS at HEAD** — the pair (13 `[mesh-quic]` lines per node, `slots total=1 committed=1` on both, 2.6 s), THREE nodes (3 tunnels, a full mesh, 3.1 s), FOUR nodes (6 tunnels, a full mesh, ~3 s), a pair repeat (2.0 s); zero `stopJoin` in any audit stream; `80934b7` confirmed. **Tier 2 may be believed for items 2/3.** Owner asked for the 3/4-node runs; both passed |
| 1 | The `assertionFailure`-in-`catch` family | 1 | — | done | `dad86e9` | 40 traps enumerated (not 19): **27 environmental sites FIXED** (26 through the new `PersistenceFailureAudit.record` in `FernletFoundation`, token + `NSError` domain/code only; DiaryStore's existing token kept, its day-key context dropped), **12 programmer-error guards LEFT**, 1 DEBUG demo-seed site reverted after the verify doubted its class; 27 cells one-to-one in `PersistenceFailureAuditTests`. Verify → **FIX FIRST**, 5 fixes + 2 notes applied: **a failed recipe payload encode left the previous `payloadData` on the record and the read path served the pre-edit recipe back — a silent edit revert the removed assert was the only signal for** (would have shipped; the catch now clears the blob); purge cells were clearing `UserDefaults.standard`; two docs claimed a rollback the encode path does not do; the day key in an audit; a duplicate import. Gates: scans 0 / 0, build green, **392 tests / 26 suites green**, no restart, `spm-wall-check` PASSED; three red-onces kept (`…/scratchpad/item1/redonce-RED.log`, `…/item1-fix/test-pfa-RED.log`). No full-suite run |
| 2 | Presence over QUIC — posture first | 1 + 2 | — | done | `9f78111` (pass 2) · `d7342f3` (pass 1) | **Pass 1 DONE:** `PresenceEpochPosture` (pure value; `fn-` + 8 CSPRNG bytes hex; identity via `EphemeralMeshTLSIdentity.mint` at the **epoch START**), wall-clock-anchored epochs, the seam `PresenceManager.presencePosture` on the existing tick; 9 + 4 cells; nothing over the air. Verify → FIX FIRST (2 fixes, 7 notes, all applied): **the certificate's validity window encoded the mint SECOND** — a mid-epoch start was singled out of the anonymity set (fixed: minted at `presenceEpochStart`); docs overstated the shipping property; mint retry budgeted once per epoch. Gates: 293 / 21 green, wall PASSED, three red-onces. **Pass 2 DONE (`9f78111`):** `NetworkPresenceSession` on `_fernlet-near2._udp` (plist + no-tracking permit + §4c row, one commit), `PresenceManager` MC-free behind a `PresenceRadioSession` seam, `ProximityQUICParameters` extracted (mesh values byte-identical), TXT chunked `t`/`t1` (24 tags were 311 B > 255 under MC). Verify → **FIX FIRST** (1 BLOCKER + 3 FIX + 6 notes, all applied): **a dial miss after a friend's own boundary stopped the WHOLE radio**; task cancellation reported as a start failure (every 900 s); peer names in `sighted`; **glare refused both sides**; the mint budget lost on `stop()`. Gates: 509 / 41 green, wall PASSED, five red-onces. **Tier 2 PASS 2026-09-19** (`09f09f6`, runbook § "Lane C — P9 item 2"): both sides rotated +0.8 s after 21:45:00 and re-sighted each other 1.3 s later, no byte in common; a heart across the new epoch connected both ways; glare not reached (taps 1.35 s apart — inconclusive); a second rotation correct but **51 s late**. **Tier-2 findings fixed (`b3f9de9`):** P9-2-A the "opaque" peer key WAS the Bonjour service name → salted per-session digest labels, five peer-naming log lines swept; P9-2-C the boundary is a deadline awaited in ≤ 30 s steps (device drift measure owed). P9-2-B (a sole-friend pair never surfaces — layer-3 self-exclusion) is an accepted residual: **every presence lane run must seed a third friend** |
| 3 | Recipe share over QUIC — pause/resume preserved | 1 + 2 | 2 | done | `79f6b15` (pass 1) · `ba34491` (pass 2) · `8f1ab3d` + `036d0a3` (9.3.2 lane) | **Premise corrected** (design note `Docs/Mesh-P9-Item3-Design-2026-09-19.md`): ONE sealed frame; pause/resume = the RADIO's discovery pause. **Pass 1 (`79f6b15`):** `RecipeShareDiscoveryGate` / `RecipeShareTransfer` / `RecipeShareAdvertisedName`, 20 cells, two needles; verify → FIX FIRST (2 FIX: `radioIsQuiet` born wrong; empty name vs `??`). **Pass 2 (`ba34491`):** `NetworkRecipeShareSession` on `_fernlet-recipe2._udp` (ALPN `fernlet-recipe-v1`, fresh name + TLS identity + `sid` per `start()` AND per `resume()`), `RecipeShareDialHello` + design §4's rejection matrix as golden cells, pause = listener AND browser down with the tunnel up, `stop()` clears the session's OWN pause flag, `.remove` republishes at the level, the per-transfer-stream acceptor on `MeshTransferStreamTable`'s route, every `report` classified, the manager MC-free behind `RecipeShareRadioSession` + `makeSession`, per-send token, plist + no-tracking permit + §4c row in one commit, rule-7 cell over the whole `RecipeSharing/` subtree. Verify → **FIX FIRST (1 BLOCKER, 3 FIX, 9 NOTE):** **every inbound dial was refused — the pending entry was booked BEFORE the owner's gate ran, so `!hasConnectingPeers(besides:)` was false for the dialer's own connection; the fake modelled no pending inbound** (now `adjudicatingInbound` excludes it, the fake books-before-gate, `theOnlyInboundDialInFlightIsAdmitted` red on the pre-fix ordering); the display hint was the random instance name and reached the picker; a browser failure during connecting cancelled the dial; a withdrawn registration left `isListening` true. Gates: 1210 / 117 green, wall PASSED. **9.3.2 PASS 2026-09-20** (`036d0a3`, runbook § "Lane C — P9 item 3", harness `FERNLET_RECIPE_LANE` at `8f1ab3d`, DEBUG-only): R1 inbound dial 3.7 ms; R2 the third Fernlet lost both 1.3 ms after `paused` (picker 2 → 0); R3 `resumed` +44 ms with a new name + certificate, re-sighted in 1.17 s; R4 a 490 732-byte JPEG on a transfer stream (`bytes=898715` sealed) byte-identical, a text recipe no stream; R5 TRUE glare 17 µs apart → one tunnel each side, both recipes delivered; R6 a share at 200 s idle arrived; R7 no `fernlet-mesh-` token in any UI line; R8 `.remove` fired 0 times in 5 min 27 s; R9 `stopped` silent, fresh name on return. **Findings:** P9-3-A BLOCKER, PRE-EXISTING, OWNER (below); P9-3-B/C/D NOTE (glare loser re-mints a whole posture mid-collapse; `received` may timestamp < 2 ms before `sent`; a paused radio keeps its own picker rows). **Unproven:** the `.remove` → republish branch (never fires on a Simulator — Lane D), `openTransferCount` back to 0, a share in flight DURING a glare collapse |
| 4 | Retire MultipeerConnectivity (2 files, 6 or 8 plist strings, the wipe row, the permit list) | 1 | 2, 3 | SPLIT: 9.4-NOW done · 9.4-LATER blocked (owner) | `df37afb` (design) · `db62de5` (NOW) | **The launcher's row is wrong about HEAD** (`Docs/Mesh-P9-Item4-Design-2026-09-20.md`, `df37afb`): the friend mesh still SHIPS on MC — `MeshTransportFactory.shippingDefault` is `.multipeer`, `MeshNetworkManager.init` resolves `transport ?? MeshTransportFactory.makeSession(MeshTransportFactory.resolvedKind())`, and the factory in `MeshTransportSelection.swift` is the ONE site that constructs `MeshMultipeerSession()` — on every shipping launch; the QUIC mesh is members-only by construction and **first-meeting stranger admission over QUIC has no path** (plan §8.7 finding 3: "MC remains the first-meeting admission path … a design decision, not a port detail"). Deleting `MeshMultipeerSession.swift` is the mesh's MC→QUIC CUTOVER and ships a build where two phones that have never met cannot found a mesh. The coach radio is type-and-string only (no live MC instantiation). 32 test files name MC (the permit list never scanned `Tests/`). **9.4-NOW DONE (`db62de5`)**: `_fernlet-near._{tcp,udp}` and `_fernlet-recipe._{tcp,udp}` out of `Info.plist` (nothing advertises or browses them — neither manager names a service type at all), §4c rewritten, cell `NoTrackingBoundaryTests.theRetiredRadiosBonjourTypesAreGoneFromThePlist` (parses `NSBonjourServices`; the four absent AND the QUIC three present), red-once. Verify (0 BLOCKER, 2 FIX, 5 NOTE — `…/scratchpad/items4-7/verify-45/findings.md`): **F1 the live set omitted `_fernlet-friend._{tcp,udp}` — the ONLY Bonjour types a shipping radio uses today; deleting them would leave every wall green and kill friend-mesh discovery on device** — FIXED `b63aeaf`: live / held / retired partition of every declared type (the friend pair LIVE with the cutover rule; the coach pair HELD — `MultipeerServiceType.trainer`'s only reader is reachable only via `begin(mode: .trainer)`, which no shipping call passes; not pinned present), red-once. **9.4-LATER** (the two files, `_fernlet-friend.*`, the coach pair, the wipe row as a legacy sweep D-4.4, `permittedFiles` emptied, the 32 test files, the rule-7 cell in the note's Appendix A): waits for the owner's decision D-4.1 / D-4.3 — gated on stranger admission (plan §8) and §15's dates |
| 5 | The 1:1 foreground anchors — widget or retire | 1 | — | done | `01e92b9` · `b63aeaf` (fix) | **Retired** (drafts `…/scratchpad/items4-7/item5/`): `ActivityKitProximityForegroundAnchor` deleted; `ProximityCoordinator`'s default is `foregroundAnchor ?? NoopProximityForegroundAnchor()` unconditionally (no call site edited — six shipping constructions, three passed an anchor); the protocol, `ProximityConnectionActivityAttributes` (stored shape unchanged — **the orphan reaper's body IS `Activity<ProximityConnectionActivityAttributes>.activities`; read literally, "retire the anchors" would have deleted the reaper**) and the reaper kept; the R9-UNSAFE allowlist reason rewritten to the reaper's invariant; cell `theOneToOneForegroundAnchorsAreRetired` (4 needles) landed in the struct `MeshContinuationTaskHostWallTests` (inside `MeshContinuationTaskHostTests.swift`; the draft named the sibling struct; wall suite 6 → 7, so the mesh-batteries floor moved 469 → 470 — measured); red-once. **Launcher wrong ×3**: `ProximityRecipeShareManager.swift:932` was a test seam (production `:669` passes no anchor); nothing reddened on the deletion; the allowlist entry narrated deleted code. Verify F2: needle (2) checked four hand-listed files — FIXED `b63aeaf`: a walk of `FernletKit/Sources/ProximityKit` through the suite's existing `codeSources` (floor 120 files; 143 today), needle (1) brace-matches the reaper body; scoped to ProximityKit because the attributes type is internal and `App/Fernlet/LiveActivityStarter.swift` legitimately calls `Activity.request`; a planted compiling `Activity.request` file reddened 4 needles where the old list stayed green |
| 6 | Gate the fourteen (247 cells / 11 s) | 1 | — | done | `b97e34e` · `85b7c4b` (fix) | Fourteen STRUCT names on the mesh-batteries step (71 → 85; two are attribute-less structs), **floor MEASURED 470 → 718** (`Scripts/run-gated-suites.sh mesh-batteries 1 <85 names>` → `718 tests in 85 suites`, 165 s; arithmetic 470 + 247 + 1 for 9.7's proof cell), comments outside the `run:` continuation (a `#` inside truncates the shell list at exit 127 while Swift sees the full one). `CIGateSelectorBoundaryTests`' numbers did not move — its pins are `>= 48` / `>= 6` / `>= 2`; the 469/71 were workflow COMMENTS. **FINDING: the selector wall did NOT catch a declared-but-not-named suite among the fourteen** (none is a `MeshP<n>*AcceptanceTests`, none was name-pinned) — needle added for `MeshRoutedStoreIsolationTests` (a grep wall with no compiler half); the other 13 are floor-protected only. Three red-onces (pin removed → RED; 84 names at floor 718 → `only 692 ran`). Determinism digests unmoved. 11 sibling suites (83 cells) in the fourteen's files stay ungated, named in the workflow block. **Verify (0 BLOCKER, 1 FIX, 4 NOTE; `…/scratchpad/items4-7/verify-67/findings.md`) → FIXED `85b7c4b`:** the selector wall now pins EVERY step's suite-name count (`measuredSuiteNameCounts`, mesh-batteries 85, parsed from the workflow; a step with no entry fails; 84 vs 85 → RED in 0.14 s, no Simulator) — the generic rule, no hand list; `Scripts/run-gated-suites.sh` fails on `Restarting after unexpected exit` (a restart re-runs suites and inflates the count) with a `--check-log <file>` seam (12 lines) — the live restart branch is proved only at the seam; **floor re-measured 718 → 719** (`Scripts/run-gated-suites.sh mesh-batteries 1 <85 names>` → `719 tests in 85 suites`, 165.8 s) |
| 7 | The six process-global drain counts + `parkedReoffered` | 1 | — | done | `08b8d45` · `85b7c4b` (fix) | **Eight cells, not seven** (two `parkedReoffered` sites; `MeshKeyAdvertisementDeliveryTests` is a struct inside `MeshRoutedPhotoDeliveryTests.swift`; a third file, `MeshRoutedBackpressureTests`, holds that suite's capture class). **A production change was unavoidable — neither audit line carried any context key**; both now carry `held = currentMesh?.meshID.uuidString` (P8 item 0's exact spelling; nil omits the key), readers are the existing `count(of:where:)` idiom; `MeshRoutedRefusalBudgetTests`' `log(` count (6 in three bodies) untouched because the calls were extended, not added. Proof cell: emits the token from OUTSIDE the rig and shows the unscoped count would have moved (`>= 3` + `everyRig > mine`, NOT `== 3` — an `==` over a process-global count would be a seventh instance of the defect once 9.6 put 84 suites beside it). Red-once: 3 mutations → 6 cells red, restored → 97 / 4 green. Gates: 257 / 12, subset 1213 / 117, wall PASSED. **Verify FIX-1 → CLOSED `85b7c4b`:** 9.6 had put ~20 MORE unscoped counts in `MeshRoutedPhotoDeliveryTests.swift` (park ×3, fold outcomes, `notDurable`, `selfMintUnavailable`, `senderBudgetSpent` ×2, `rejected`, `openFailed` ×2, `routedShare.skipped`, `droppedUncommittedSlot`) on the CI line beside suites that demonstrably park rows (`parkedReoffered == 3` had already read 4 under a full-suite run) — one production door `MeshNetworkManager.heldMeshAuditContext(_:)` adds `held` at all 14 emission sites (every `log(` EXTENDED, none added; the 6-in-3-bodies count unmoved), 18 cells scoped through one predicate `heldBy`, a sibling proof cell; `foldParkedKeyAdvertisements` takes `verifier.meshID` unconditionally (the nil-mesh comment described an unreachable state). **Residual, process-wide `>= 1` counts:** `routedQuiescent`, `blockedOrigin`, `originUnresolvable`, `originRemoved` (tokens carry no `held`) and `keyAgreement.rejected` at the restore path (`currentMesh` not proven set); `droppedUncommittedSlot` is the one genuinely nil-reachable site (refuses before the mesh guard, by design). The three scoped `== 0` drain cells are NOT vacuous (the verifier pins `payload.meshID == mesh.meshID` synchronously) and go vacuous only if `held` leaves production, which the `== 1` cells catch |
| 8 | The two accessibility-ratchet baselines | 1b | — | done | `c5ff754` · `a5f8bcf` (fix) | Re-recorded on a freshly erased, pinned `Fernlet-A11y` (`BBB73DF2…`, iPhone-17 type, 402×874 portrait), appearance and content size SET and READ BACK before every run — **dark/large then light/large**, the two probe classes serially in their own `xcodebuild` invocation. Both deltas reproduced **byte-for-byte in both appearances**, so there are **no appearance-dependent residuals**. `Home · Recent bites` 16 → 13 (three `Text clipped` personal-care lines deleted; two of the three are ALSO frozen on `Sheet · Hygiene`, which was NOT re-recorded — the deletion is entry-scoped, and a file-wide search-and-delete would have silently stripped that screen's wall). `Move · Progress photos` 6 → 7: the date chip clipped as TWO identities (`Aug #` AND `Sep #`) because the seed's three relative dates straddle a month boundary — **the `Aug 28` time bomb caught mid-detonation**; month abbreviations were appended to `volatileDateWords` and to `baselinePinsAVolatileLiteral`'s `months` rather than the literal being pasted in. Gates: both probe suites green in dark AND light, `AuditRatchetBoundaryTests` + `PowerOfTenBoundaryTests` + `LocalizationBoundaryTests` = 73 / 3, `UXScreenProbeIdentityTests` 8/8, two red-onces. **Verify → 1 BLOCKER + 3 FIX, ALL FIXED in `a5f8bcf`:** (1) **the sentence justifying the three deletions was refuted by the commit's own four `.xcresult` bundles** — the six `Dynamic Type` siblings under the same screen were *excused* by `unreportedCategories` on every run, so `UXScreenProbe.swift:858`–`:863` froze six lines that matched nothing and **could never red**; a fifth evidence run on `4dbd8d3` reproduced that green, the six were **DELETED** (13 → 7 lines) and the hole was closed by `absentFromScreen(_:)`, which takes an entry back out of the under-reporting excuse when the element it names is **not on the audited screen at all** (bounded: 24 entries per screen, 8 matches per label; every uncertainty resolves to "still excused", so it can only ever ADD a failure naming a specific absent element). (2) `:833`'s "the audited viewport is the same every run" was false — rewritten to say what IS pinned and that scrolling pins the bottom EDGE, not the contents, with a wall-clock-dated demo seed above it. (3) the baseline-device guard compared only the window frame (iPhone 17 / 17 Pro / 16 Pro all report 402×874) — `isOnBaselineEnvironment` now names mismatches from **four** inputs: `SIMULATOR_MODEL_IDENTIFIER` vs `iPhone18,3`, content size `.large`, `en_US`, and the window, under the existing bypass. (4) "this app renders no abbreviated weekday beside a numeral" was false (`CoachPlanReviewView.swift:317`/`:408` render "Sat, Sep 20") — weekday abbreviations were **added** and both halves of the wall tightened to **whole-word** matching, so "Fri" in "Friends" and "Sun" in "Sunscreen" are untouched; **0 of the 82 frozen labels key differently**, measured over the committed file. Three red-onces in the fix (a planted frozen line that never reproduces — GREEN before the change, RED after; the device pin on a temporary iPhone 17 Pro, `iPhone18,1` named; the content-size pin at XL). **Residuals:** the locale leg has no red-once; `absentFromScreen` is validated on 3 of 14 screens (59 excusable entries remain across the other 11, and the first full `ScreenAppearanceUITests` run is where that is tested); this whole wall runs on **no CI line** (no workflow names `FernletUITests` or `AuditRatchetBoundaryTests`); `Home · Recent bites` stays viewport-unstable by construction, now stated in the entry rather than denied by it. The "last re-recorded 2026-08-27" prose in plan §14.3 finding 13 is corrected by the close-out; the same sentence in the P9 launcher is that launcher's own task text and stays as history. No full-suite run |
| 9 | P9 acceptance battery + CI lines | 1 | 2–4 | done | `4dbd8d3` · `4f52e0a` (fix) | One file `Tests/FernletTests/MeshP9AcceptanceTests.swift` (the P5–P8 convention), **five serialized clause suites / 20 cells**: `MeshP9EphemeralPostureAcceptanceTests` 3 (four epoch boundaries walked as VALUES, plus the half a rotation table cannot see — two mints 27 s and 613 s into ONE epoch hand the certificate path the SAME instant), `…PresenceSwap…` 4, `…RecipeSwap…` 5 (the whole 5 × 12 discovery-gate product against this battery's OWN event-outermost literal table with no `default` — exactly five of sixty readings move discovery), `MeshP9McRetirementAcceptanceTests` 4 (**item 4 as an HONESTY row, not a zero-list**: both MC files exist BECAUSE the friend mesh still ships on MC and stranger admission has no QUIC path, `import MultipeerConnectivity` has exactly two homes, the four dead Bonjour strings are gone while the LIVE friend pair is still declared — **every cell is one the cutover commit MUST edit; that is the handoff**), `MeshP9HonestyAcceptanceTests` 4 (never the launcher's `MeshP9CIHonestyTests` — that name matches neither half of `isMeshBattery` and would have been demanded by nothing). Five test-target declarations widened `private` → `internal`, each carrying a sentence naming this battery as the second reader. Three pins moved, **all MEASURED at the commit, none carried**: mesh-batteries floor 719 → **860** (the exact 97-name line run at floor 1 → "Test run with 860 tests in 97 suites passed", 165.9 s, no restart line; the arithmetic 719 + 141 is a check, never the source), `measuredSuiteNameCounts["mesh-batteries"]` 85 → **97**, the battery pin 48 → **53** (a count of DECLARATIONS). The line gained the five clause suites **and the seven suites items 2/3/4 had left on NO CI line** (`PresenceEpochPostureTests`, `PresenceAdvertisementTests`, `PresenceManagerTests`, `PresenceOverQUICTests`, `RecipeShareTransferTests`, `RecipeShareOverQUICTests`, `TransportNeutralityBoundaryTests` — the last ALSO pinned by NAME, on the `MeshRoutedDrainWallTests` argument: a source grep-wall with no compiler half). **Ledger correction carried in the commit:** the 9.4 row's "`MeshNetworkManager.init` constructs the MC session" — `MeshMultipeerSession(` occurs ZERO times in that file; the one construction site is `MeshTransportSelection.swift:297`. Ten red-onces, each planted, built, run, RED via its own assertion, restored byte-exact (sha256 verified) and re-built green — R9 reddened three pins together and R10 ran the line with 96 names at the measured floor ("only 857 test(s) ran but the floor is 860"). Gates: scans 0 (523 files) / 0, the P9 gate subset + the five new suites **1 233 / 122** (EXIT=0, no restart line), the wall group 150 / 10; `MeshP5AcceptanceTests.swift` untouched, so neither determinism digest moved. **Verify → 3 BLOCKER + 6 FIX + 6 NOTE, fixed in `4f52e0a`:** (1) the "every P9 suite is on the mesh step" cell checked a **hand-list of seven** while `ProximityRecipeShareCapTests` (31 cells, rewritten by `ba34491`) ran on **no CI line at all**, with six more P9-touched suites ungated and unnamed (`NetworkMeshTransportTests` 119 cells, `MeshTransportSelectionTests`, `PresenceHeartsTests`, `PresenceTagTests`, `PeerTransportNeutralityTests`, `ProximityRecipeShareDiagnosticsTests`) — each is now **gated or named as an honesty row**, and the cell is derived from a frozen P9-touched list rather than a hand-list; (2) clause (d) pinned `shippingDefault`'s **source text**, so flipping `resolvedKind(environment:)`'s `#else` branch to `.quic` left all four cells green while the friend mesh shipped on QUIC — both are now asserted as **VALUES**; (3) the audit-line cell filtered the records to those already carrying the peer label and then walked only those — **its own filter was the blacklist it claimed not to be** — so the needle loop now walks `everyRig`; plus the `everyRig.count >= mine.count` tautology deleted, a real `.pause` driven over a `.sending` record, the two `RecipeShareTransfer` mints funnelled through one helper with a containment pin (nothing had been exercising the shipping `sendRecipeShare` birth seed), the stranger-admission rationale added as an honesty row, `PresenceOverQUICTests`' two `first {}` reads scoped against a cross-suite audit-capture collision this commit created, item 8's artefacts and the ~43 unscoped `.count(of:)` reads added as honesty rows, and the anti-vacuity floors raised. Three pins re-measured at the fix: floor **`1048`**, `measuredSuiteNameCounts` **`118`**, battery pin **`53`**. No production display string, no persisted surface, no network destination and no Bonjour type added |
| 10 | Close-out: §17.1 BUILT, §15 updated, the P10 handoff, the next launcher, memory | 1 | 0–9 | done | — (this commit, plus the plan blob in the primary) | draft → adversarial verify → revise → apply. The verify returned **4 BLOCKER + 13 FIX + 9 NOTE against the draft text** — item 8 written up as clean when its own verify was still open; "the item-4 design note mis-cites §11 finding 3" (it does not — **this ledger's item 4 row and `df37afb`'s message are the only homes**, corrected here); "adding a suite name without raising `measuredSuiteNameCounts` is a red" (it is not — the pin is `>=`, only REMOVAL reds); a stale `MEMORY.md` row quoted as the replace target; and a dozen carried numbers that had moved (the phase diffstat, the new-file counts, the +suites/+cells, the gate subset, R1's "3.7 ms" for a 3.5 ms gap 3.68 s after launch, a SHA table missing `507972e` and `f86becf`, a catalog grep returning seven false positives, and "ten catalogs" for fifteen). Plan §17.1 is BUILT with its own What landed / Deviations / Findings / Acceptance evidence (§13/§14's format) and §17.1.2 stating the SPLIT; §15's status paragraph gains a P9 line (**still NOT RUN**, no row's date moved); plan **§26.1 is dated superseded in part** (three facts P9 falsified: seven plist Bonjour types not eight, the moved plist anchors, the CI shape) and **§14.3 finding 13 is marked CLOSED by item 8**; a new plan **§27** is the P10 handoff; `Docs/Next-Round-Prompt-Mesh-P10-2026-09-20.md` is written from it with every anchor re-grepped at HEAD. Runbook Lane B unchanged (no devices); the Lane C rows for 9.2.2 and 9.3.2 were already dated at their items. **Catalog: P9 added ZERO display keys** — no added `LocalizedStringKey` / `String(localized:` / `Text("…")` / `Label("…")` line in `bb454fe..a5f8bcf -- '*.swift'`, and no `.xcstrings` is in the diff; `Scripts/sync-string-catalogs.sh --check` is a no-op. Scans 0 / 0 |

## Blocked on owner
- **Devices for item 0** — two minimum (rows A–E, F1–F6, F9, F11, F12), three for E3 / F8, four for F7. The runbook's Lane B table (`Docs/Mesh-Network-Feasibility-Runbook.md` § "Lane B", line ~1939) is the evidence table; the device plan's Results table (`Docs/Mesh-P7-Physical-Device-Test-Plan-2026-09-18.md` § Results) takes A–E. **Every row below is NOT RUN as of 2026-09-19:**
  - Device plan **A1–A8, B1–B4, C1–C2, D1–D5, E1–E4, E7** (E5, E6 are tier-1-only by design).
  - **F1–F12** (plan §15.1–§15.4; F10 is the owner's call; F11 = Lane D phone ↔ Simulator, cable out, the cheapest first run).
  - The **tier-3 rows P8 item 6 named**: registration accepted; a `.fail` submission **granted** (no grant observed anywhere); the launch and expiration handlers firing on the real `SystemContinuationScheduler` conformer; the tunnel surviving the whole task; slow progress across the 3 h / 6 h soaks; no proximity Live Activity present; the Control-Centre peek re-submission (F12).
  - The **re-run of P8 item 0's four founding fixes (a)–(d)** on the fixed build, with the **three eyeballs**: Friends tab entry/exit (observed incidentally on Simulators only), Control Centre over the Friends tab, delete-all during a live session.
  - **§15.3 decides the degraded ladder** — unchosen until the soak runs; plan §14's scope unchanged.
- **DECISION OWED — 9.4-LATER, the MC→QUIC cutover of the friend mesh** (`Docs/Mesh-P9-Item4-Design-2026-09-20.md` § "The decision"): D-4.1 hold until stranger admission exists on QUIC and §15 has dates (recommended by the drafter); D-4.2 split (TAKEN as the working default — the NOW half lands, nothing else); D-4.3 cut over anyway (ships broken first-meeting founding on hardware — not recommended). Sub-decision D-4.4: the `MCPeerIDStore` wipe row becomes a legacy `FileManager` sweep, not a pure retire (`FernletPeerID.archive` survives on any pre-P9 install). Until decided, 9.9's "MC retirement as a zero-list" clause is an HONESTY row, and §17.1 cannot be marked fully BUILT.
- **P9-3-A — a configured Fernlet Lock stops the recipe-share and presence radios PERMANENTLY** (pre-existing, not pass 2; observed on the 9.3.2 lane: a locked Simulator read `policy=stop` with zero radio records for 200 s of usable foreground app). `recipeShareState` / `presenceState` stop on `appLockEngaged`, and `FernletLockState.locked` is the RESTING state of a configured lock; every `.unlocked(scope:)` is a private surface where both radios stop anyway. Nothing tells the user why. Product decision: which lock states may run the 1:1 radios (the run policy is P7's 23 040-row table — a policy row change is a P7 bug fix, re-run the whole product). **FIXED 2026-09-22** on the owner-calls launcher, item 1: the owner's answer was "make it work", and the fact was RETIRED rather than re-projected — no `appLockEngaged` input, no projection, no feed, no `FernletLock` import in the policy; the product is now 11 520 rows. Runbook *Lane C — P9 item 3* findings and plan §17.1.3 finding 1 carry the record.
- **Device measure owed for P9-2-C**: does a real phone's boundary wake drift like the Simulators' (+51 s over a 767 s arm)? The 30 s step bounds it either way; a phone number decides whether the step can widen.
- **Owner follow-up:** `NetworkMeshSession`'s listener/browser task bodies reported their own
  cancellation through `onTransportError`. **RESOLVED 2026-09-20 in `507972e`**, in its own session
  off the P9 slot: both bodies guarded, cell
  `NetworkMeshSessionTests.theListenerAndBrowserTasksDoNotReportTheirOwnCancellation`
  (`Tests/FernletTests/NetworkMeshTransportTests.swift`, the struct at `:936`, the cell at `:1153`);
  the decision is recorded at the item 2 surprise entry below.
- **The drawer is now safe to open in DEBUG**: item 1 removed every environmental trap a lock or
  background row could fire (`dad86e9`). **RESOLVED** — nothing further owed here.
- Carried from plan §26.4 (read it; do not re-list it here) — and now §27.4, which adds only what P9
  itself left open.

### Disposition at close (every line above ends here)

| Blocked line | How it ends |
|---|---|
| Devices for item 0 (§15.1–§15.4, device plan A–F, the two owed re-runs, the tier-3 rows P8 item 6 named, §15.3's ladder) | **CARRIED to P10 item 0** with its cost stated: §15 is unrun for a second phase and it blocks **shipping**, not the loop. §15's table and the runbook's Lane B are unchanged and undated |
| **D-4.1 / D-4.3** — the MC→QUIC cutover (and D-4.4, the wipe row as a legacy sweep) | **TAKEN as D-4.2 (split) for the NOW half** (`db62de5`), the rest **CARRIED to P10 item 6** as a finding with its cost: §17.1 cannot be read as MC retired, `permittedFiles` still names two files, 32 test files still name MC, and `MeshP9McRetirementAcceptanceTests` pins the current truth — as VALUES after the 9.9 fix — so the cutover commit must edit it |
| **P9-3-A** — a configured lock parks the 1:1 radios permanently | **CARRIED to P10 item 7** and written into plan §17.1.3 finding 1 with its cost: two shipped radios are dead for any user who has configured the app lock, and nothing tells them why. **Answered and FIXED 2026-09-22** (owner: *make it work*; the input is retired, 23 040 → 11 520 rows) |
| **P9-2-C** — the device measure of the boundary-wake drift | **CARRIED to P10 item 0** (a device row) and to §17.1.3 finding 3; the ≤ 30 s step bounds it meanwhile |
| Owner follow-up — the mesh session's cancellation reports | **RESOLVED** 2026-09-20 in `507972e`, in its own session |
| The DEBUG drawer's traps | **RESOLVED** by item 1 (`dad86e9`) |
| Plan §26.4's carried list | **CARRIED unchanged** to §27.4 |

## Decisions taken (defaults from §3 unless the owner overrides)
| Decision | Choice | Taken on |
|---|---|---|
| The first iteration | item 0's device-free half (this ledger, item 1, its tier-1 walls); item 0's device rows `blocked (owner)` | 2026-09-19 |
| Item 1's scope | **the whole family** — every `assertionFailure` inside a `catch` (or a `guard let … try? … else { assertionFailure }`) that fires on an environmental Core Data / file I/O / encode failure, in every module: the acceptance is per site, any one of them crashes a DEBUG lock row. Programmer-error asserts stay (12, each justified in the commit). Owner may narrow. | 2026-09-19 |
| Item 1's demo-seed site (`FernletStore+DemoSeed.swift`) | **reverted to its `assertionFailure`** — `#if DEBUG` demo code that never runs on a locked device, and `seeded == nil` from a bundled image is as likely bad seed data as an I/O failure; no seam for a cell | 2026-09-19 |
| Item 1's `upsert` return after a failed recipe payload encode | **stays `true`** — the fresh legacy columns are durable and a `false` would retry-loop on a non-finite value; the structured half is dropped (`payloadData` cleared), audited (`savedRecipe.payloadEncode.failed`) and documented at `apply`, `upsert` and the landing page | 2026-09-19 |
| Presence posture | **reproduced before the framing** (pass 1 `d7342f3`): epochs anchored to wall-clock multiples (the tag epoch is already absolute; a per-launch phase is a rotation-surviving fingerprint; synchronized rotation maximizes the anonymity set); the certificate minted at the epoch START, never at `now`; eager mint (one keygen per 900 s); a failed mint retried once per epoch | 2026-09-19 |
| Presence glare | inbound for a held/dialing key collapses through `MeshTunnelConvergence` ranked over the two instance names (the mesh's `sid` pattern); `presence.quic.redundantTunnelClosed` | 2026-09-19 |
| Presence tag-replay squatting | ACCEPTED residual (option b): an unauthenticated dialer replaying a sighted tag occupies the friend's `MeshLinkKey` slot for ≤ 2 000 frames / the QUIC idle timeout; the sealed introduction still refuses everything; a name-ranked glare can evict a live tunnel (one failed heart send, retried). Option (a) — file under the connection id until the introduction succeeds — needs a new seam for the coordinator's verdict; deferred | 2026-09-19 |
| Presence audit lines | our own posture (`advertised`/`rotated`: epoch, name, certificate digest, `.private`); peers by opaque session key + tag COUNT only | 2026-09-19 |
| Recipe share | **pause/resume = the radio's discovery gate** (there is no transfer to pause — one sealed frame); pass 1 pins it as a total table; pass 2 binds `_fernlet-recipe2._udp` in a new `NetworkRecipeShareSession.swift` with a per-transfer-stream acceptor | 2026-09-20 |
| Recipe-share posture | per-`start()` fresh name + TLS identity via `MeshLinkAdvertisement.randomInstanceName()` + `EphemeralMeshTLSIdentity.mint()` (the mesh's own shape), NOT `PresenceEpochPosture` (its 900 s epoch and epoch-start certificate would be a silent mismatch on a per-tab-visit radio); no rotation timer; bound = one name per Food-tab visit; `sid` minted per start in pass 2 | 2026-09-20 |
| Presence peer labels | salted per-session SHA-256 digest, 12 hex (`peerLabel(for:)`); never the key, never a plain hash of a public name | 2026-09-20 |
| Presence boundary arm | a deadline awaited in ≤ 30 s steps on the one existing task (`maxEpochRotationStepSeconds`) | 2026-09-20 |
| Recipe-share advertised name over the byte bound | trailing Characters dropped to ≤ 64 UTF-8 bytes; a single grapheme wider than that → the `name` key is OMITTED (never `""`, matching `publishedFields`) and the receiver falls back to the peer hint on absent OR empty; the 32→24-Character narrowing is stated at `discoveryInfo()` | 2026-09-20 |
| Exchange fields that mirror a radio LEVEL | seeded from the level at the mint (`radioIsQuiet` ← `session.isDiscoveryPaused`), never left to transitions alone | 2026-09-20 |
| Overlapping shares to one peer (completion attributed to the wrong record) | recorded, NOT fixed in pass 1 — needs a per-send token (`recipientID` is the same for both); pass 2 owes it or states it | 2026-09-20 |
| Recipe-share `sid` ownership | the SESSION's, re-minted with name + identity at `start()` and every `resume()`; the manager's `discoveryInfo()` carries `v`/`mode`/`name` only | 2026-09-20 |
| Recipe-share browser failure | audit only (`recipe.quic.browserFailed`); never stands the radio down; the owner's stand-down guard covers the connecting window; a listener failure still stands down | 2026-09-20 |
| Recipe-share display hint | never the instance name — the picker row, then the TXT `name`, then the existing placeholder; `displayHint` forbidden by the rule-7 scan | 2026-09-20 |
| Overlapping shares (resolved in pass 2) | per-send token on `RecipeShareTransfer`; a superseded completion cannot clobber `sendState` | 2026-09-20 |
| 9.3.2 lane harness | `FERNLET_RECIPE_LANE` (`8f1ab3d`), DEBUG-only, reaches the radio through the run seams; kept for Lane D | 2026-09-20 |
| Notation | `9.<item>.<tier>` (e.g. 9.3.2 = phase 9, item 3, tier 2); passes read `9.3 pass 2` | 2026-09-20 |
| Selector-wall name pins for the fourteen | one needle for `MeshRoutedStoreIsolationTests` (the one that earns it on the existing argument: a grep wall with no compiler half); the 13 behaviour suites stay floor-protected — a generic rule is 9.6's verify's call | 2026-09-20 |
| The friend-mesh Bonjour strings | `_fernlet-friend._{tcp,udp}` are LIVE (the shipping MC mesh) and pinned present by the plist cell; the cutover commit moves them to the retired set in the same breath | 2026-09-20 |
| The coach Bonjour strings | HELD (declared, no advertiser, not pinned present): `MultipeerServiceType.trainer` is reachable only through `begin(mode: .trainer)`, which no shipping call passes and only a test-only service uses; every declared type must be classified live / held / retired (a new obligation on any commit adding a Bonjour type) | 2026-09-20 |
| Selector wall — the generic rule | every gated step pins its suite-NAME count (`measuredSuiteNameCounts`, parsed from the workflow) beside its test-count floor; a name leaving the line reds with no Simulator; the `MeshRoutedStoreIsolationTests` needle kept as the documented reason | 2026-09-20 |
| 9.7's second sweep | close, not record: one production door adds `held` at every routed-inventory / park emission; the five tokens with no mesh identity stay process-wide `>= 1` and are named | 2026-09-20 |
| 9.9 honesty suite name | `MeshP9HonestyAcceptanceTests` (the selector wall's prefix + suffix), never the launcher's `MeshP9CIHonestyTests` | 2026-09-20 |
| 9.8 appearance | record in dark AND light; freeze only findings that reproduce in both; appearance-dependent ones are residuals with their identity line | 2026-09-20 |
| When MC is deleted | **SPLIT (D-4.2)**: the dead `_fernlet-near`/`_fernlet-recipe` strings now (9.4-NOW); the two files, the friend/coach strings, the wipe row and the permit list ONLY after the owner decides the friend mesh's MC→QUIC cutover (stranger admission has no QUIC path) | 2026-09-20 |
| The coach radio (plan §18 decision 4) | default held (keep the two `_fernlet-coach` strings) — the survey found them type-and-string only, no live MC instantiation; they leave with 9.4-LATER unless the owner retires the coach types | 2026-09-20 |
| The 1:1 anchors | **retired** (`01e92b9`): the conformer deleted, the noop default unconditional, protocol + attributes + reaper kept | 2026-09-20 |
| Gate the fourteen | **yes** (`b97e34e`): 85 suites, floor 718 measured | 2026-09-20 |
| New persisted surface | (default: none) — item 1 added none | — |
| The degraded ladder | (decided by §15.3 in item 0) | — |

## Surprises worth not re-deriving
- **The assert family was 40 traps, not 19.** The plan's 19 counted the literal `catch` shape; eight siblings were `guard let x = try? … else { assertionFailure }` on the same environmental failures (a store fetch, a payload encode of a non-finite `Double`). Enumerate by failure class, not by syntax.
- **Removing an `assertionFailure` can remove the only signal for a silent data revert.** `SavedRecipe.apply`'s encode `catch` left the previous `payloadData` on the record; the read path prefers the blob whenever the legacy projection has not diverged, so an edit to a structured-only field vanished on the next load. Every converted `catch` must leave the row in the state the *read path* expects, not merely "not trap".
- **A controller whose store never loaded is NOT a throwing rig**: zero stores → empty fetch, no-op save, nothing throws (and an insert crashed the runner). To make saves throw, re-attach the SAME store with `NSReadOnlyPersistentStoreOption`; to make fetches throw, overwrite the database file (and WAL) under the open connection — SQLite re-reads page 1 per read transaction.
- **A test that builds `LocalFernletRepository` without `legacyDefaults:` purges `UserDefaults.standard`** when it drives `purgeAllPersistedData()` — the seam's own doc comment names the hazard; pass `isolatedDefaults()` and `backupExclusionPreference: { false }` on every construction.
- **`power-of-10-scan.py`'s assertion-density floor counts `assertionFailure` as a check**: 27 removals moved 0.775 → 0.770 against a 0.68 floor. A future sweep of this shape moves it further.
- **A continued-processing task is delivered while the app is still in the foreground.** Any state meaning "we are continuing in the background" keys on (task in hand ∧ scene dark).
- **Gating cost is not proportional to cell count**: +92 % cells cost +10 % wall time on a tier-1 step. Price a gate before deferring it.
- **A hold is not a stop**: every clock that assumes browsing must pause with the radios — grep every `arm…Clock` when adding a pause-shaped verb.
- **A terminal state erases its cause unless a projection rule keeps it**; and **a reset that clears state without walking the table strands every side effect the table owed**.
- **`hasSlot(for:)` means ANY slot** — read the body of every predicate a new door consults; a **re-entry row keys on facts, not on `previous`**.
- **A protocol requirement declared in an app file is a second occurrence of a grep-wall's needle**; **a grep for `@Suite` under-counts the suites** (some are plain structs found by their `@Test`s).
- **A background build's notified exit code is the last command's**; **a Simulator that has run ~3 h stops rendering** — erase and reboot before believing a UI red.
- **Every P8 adversarial verify found something real.** Seven for seven; five would have shipped. **P9 item 1's did too** (the stale blob).
- **Every P6 and P7 lesson still binds**: a log with `Restarting after unexpected exit` has no usable total; never chain a build and a run on one DerivedData; `-only-testing:` names the SUITE; `@Test(arguments: [])` is green over nothing; a process-global audit signal cannot witness a per-cell claim; `.inactive` is foreground for the radios; `FernletLockState` carries associated values; the chat age gate is `AgeGate.chat`; zsh passes an unquoted `$ARGS` as one argument.
- **Closed; do not re-audit:** `MeshTunnelConvergence`, the id-vs-endpoint family, the crypto-purpose / `PayloadType` / record-kind spellings, plan §10.7–§10.10, §11.1–§11.4, §12.1–§12.4, §13.1–§13.4 and §14.1–§14.4; `Docs/Proximity-Security-Followups-2026-08-18.md` §1. Concurrent sessions share the tree; never stage the catalog working copy, `xcuserdata`, or the stray PDF.
- **A worktree has its own DerivedData** (the path hash differs from the primary's): the first build there is cold (~20 min here), and "one `xcodebuild` at a time" is a CPU rule as much as a DerivedData rule — `pgrep -x xcodebuild` still gates every start.

- **Rebuild the app before a lane run, always**: the "warm" app binary was stamped one minute BEFORE the last code commit, so it could not have contained it — a build log's date is not the tree's date.
- **Kill audit streams by saved PID, never `pkill -f "log stream"`**: the pattern matched a concurrent session's `backboardd` orientation stream on a Simulator (spawned through Claude.app's helper) and killed it.
- **Four Simulators are as cheap as two**: boot-to-usable ~25 s on the fourth, and the four-node full mesh converged in ~3 s — the same as the pair. At N ≥ 4 two `tunnel activated, tunnels=2` lines can land in one instant (three distinct sids, final `tunnels=3`) — a log-ordering artefact, not a duplicate.
- **A seeded harness run converges the descriptor roster only** (`membership ledger=absent derived=0`) — correct per the 2026-09-12 correction; do not read it as a derived-roster proof.

- **A rotation test can only see fields that CHANGE.** The certificate's validity window is the one part of the posture that is supposed to be identical across devices, so the raw-`now` leak survived the whole rotation table; the cell that catches it asserts two mints at different instants in one epoch are byte-identical. When a value must be unlinkable, pin the fields that must match as hard as the ones that must differ.
- **The MC presence radio wears one name for its whole life while its tags rotate** (`MeshMultipeerSession` mints the `MCPeerID` in `init`; `updateDiscoveryInfo` restarts only the advertiser). Presence must not reach testers over MC; item 2 pass 2 / item 4 remove the path.

- **A transport-error door that was start-failure-only under MC becomes any-error under QUIC.** `handleTransportError → stop()` was reachable only from `didNotStart*` under MC; a QUIC session that `report()`s a per-dial miss or its own task cancellation through the same door stands the whole radio down. Every `report`/`onTransportError` call in a new session must be classified: start failure (stand down) vs. per-operation refusal (log and return) vs. cancellation (silent).
- **24 advertised presence tags were already 311 bytes — over DNS-SD's 255-byte TXT entry limit.** MC's behaviour on an oversize `discoveryInfo` value was never established; `NWTXTRecord` refuses or truncates. The TXT is now chunked `t`/`t1` (capacity 36) with a cell pinning `maxAdvertisedTags <= capacity`.
- **Owner follow-up, not fixed:** `NetworkMeshSession`'s listener/browser task bodies (`:~834`, `:~877`) have the same unguarded `catch { report(…) }` shape; the mesh's owner only sets `discoveryError` (a banner), so a real cancel may show a spurious banner — left alone because guarding it changes what the mesh records. **Decided and fixed 2026-09-20:** guard both bodies. The republish path (`updateDiscoveryInfo(_:)` — every mesh-mode change, merge and member-count change) never clears `discoveryError` and runs on a live search with the banner's own screen up, so a reported cancel is a "discovery failed" banner pinned until the user leaves the tab; the hold and stop paths clear it BEFORE the cancel lands, so their clear never wins and the banner is hidden there only by `isSearching == false`. The `.cancelled` state callback was already silent, so the task body now agrees with it. What the mesh records on a real cancel is nothing — which is `discoveryError`'s documented contract (a start-up failure, and a cancel is not one). Cell: `NetworkMeshSessionTests.theListenerAndBrowserTasksDoNotReportTheirOwnCancellation`, a source scan for the same reason the presence cell is one.

- **Two foreground agents cannot hand a build slot to each other through the orchestrator.** The item 3 drafter polled for the marker file for 100 minutes while the orchestrator was blocked waiting on both it and the lane runner; the marker could only be created after both returned. Either run the drafter in the BACKGROUND, or let the runner create the marker itself when it is done with the tree.
- **`-only-testing:FernletTests/<FileName>` matches nothing and reports nothing** — the run proceeds with the other suites and the file's suites silently contribute zero. Name the struct; count the suites in `Test run with N tests in M suites` against the list.
- **A sole-friend pair never surfaces in presence**: layer-3 self-exclusion drops an ad whose token set is a subset of our own, and two devices whose only friend is each other advertise identical sets. Accepted residual; seed a third friend on every presence lane run, and remember the diagnostic is in-memory only.
- **The launcher's item 3 premise was wrong twice** (verified by the pass-1 survey): the recipe share is one sealed frame with no chunks or offsets, and the recipe radio used the persistent device-name `MCPeerID`. Read the file before believing a launcher row's description of it.
- **A `MeshLinkKey` built from a Bonjour endpoint id IS the advertised name.** "Opaque" must be proven by a cell over the audit capture, not asserted in a comment.
- **A long single `Task.sleep` to a wall-clock deadline wakes late in proportion to its length on a loaded host** (+0.8 s at 300 s, +51 s at 767 s, both Simulators together). Await deadlines in bounded steps and re-read the clock on every wake.

- **A field that only TRANSITIONS write is born wrong whenever the level was already set.** `radioIsQuiet` mirrored the radio's pause, but only the gate's pause/resume verdicts wrote it, so a transfer minted while the radio was already paused carried `false` for its whole life — and the cell meant to catch it drove the session directly and asserted the birth value. Seed every mirrored field from the level at construction, and make the cell mint UNDER the level it claims to mirror.
- **`??` is a nil-coalesce, not an empty-coalesce.** A publisher that can emit `""` for a key whose receiver only falls back on `nil` renders the placeholder where the old path rendered the user's text. Either never emit the empty value (omit the key) or fall back on both — pass 1 did both.
- **`ML5` (`MemoryLifecycleBoundaryTests`, HP0) is a tree-wide grep wall the compiler and the new suite cannot see**: five inline `ProximityRecipeShareManager(store: SomeHost())` test hosts built clean and every recipe-share cell passed; only that suite named them. Every new proximity-manager test file runs `MemoryLifecycleBoundaryTests` before it is believed.
- **A needle that counts occurrences survives relocation.** `occurrences("session.pauseDiscovery()") == 1` was true with the call moved out of `applyDiscoveryGate(`; the needle now pairs the whole-file count with the count inside the function's brace-matched body.
- **`Scripts/spm-wall-check.sh` leaves NO `FernletTests.xctest` in DerivedData** (it rebuilds under the enforcement flag): a verify agent that runs after it cannot `test-without-building`; run the wall check LAST and expect a `build-for-testing` before the next test run.
- **`ItemNameModeration.sanitizedName` strips ZWJ** — a family-emoji name cell passes for a different reason than a byte bound; pass 2's TXT publisher must not assume ZWJ sequences survive the sanitizer. It KEEPS combining marks, which is how a single grapheme reaches 64+ bytes.
- **An unbuilt draft compiled first try bar one class of error** (`#expect` cannot hold a mutating call — `'$0' is immutable`); the ML5 hosts and the two FIXes were the real cost. Phase A's anchoring by surrounding text (not line numbers) held across one intervening commit.
- **A fake that does not model the real session's ORDERING hides a total outage.** The QUIC recipe session booked the pending-inbound entry before the owner's gate ran; the gate's `!hasConnectingPeers(besides:)` saw the dialer's own connection and refused EVERY inbound dial, while 25 cells over a fake with no pending inbound stayed green. A seam's fake must book-before-gate (or whatever the real one does) at the same instant; a lane row ("an inbound dial completes at all") is the only cell that cannot lie.
- **The launcher's item 4 premise was wrong**: "MC now stops at two files" (P0/P1) meant two files CONTAIN it, not that it is dead. The friend mesh ships on MC by default and QUIC has no stranger-admission path. Grep `shippingDefault` before believing any "retire X" row.
- **`Docs/PrivacyWipeCoverage.md`'s permit-style lists never scanned `Tests/`**: 32 test files name MC. A deletion's blast radius is the whole tree, not the scan roots.
- **A lane finding can be pre-existing and still a blocker**: P9-3-A (a configured lock parks the 1:1 radios forever) was found by the 9.3.2 harness, not caused by it; record the class (pre-existing / regression) on every lane finding.
- **True glare IS reachable on Simulators** with a harness that dials on sight (17 µs apart) — do not mark glare inconclusive without trying an auto-dial.
- **zsh does not word-split an unquoted `$ARGS`** — the second time this bit: 117 `-only-testing:` flags passed as one argument reported `TEST EXECUTE SUCCEEDED / EXIT=0 / Executed 0 tests`. Build argv as an array; always read `Test run with N tests in M suites`.
- **A drafter and a lane runner can share a tree** as long as the drafter is read-only and returns without waiting on a marker.
- **A floor written in a draft is stale the moment another item lands a cell in a gated suite**: every 469 anchor in the 9.6 drafts was wrong by the time they were applied (9.5's cell had moved it to 470). Measure the floor at the commit that moves it; never carry a number across commits.
- **A cell whose home suite is a wall suite moves the CI floor**: 9.5's rule-7 cell landed in `MeshContinuationTaskHostWallTests` (the draft named the sibling suite) and the mesh-batteries floor had to move with it, in the same commit.
- **The selector wall protected only `MeshP<n>*AcceptanceTests` by name.** Any other gated suite could be dropped from the workflow line with every wall green; the floor is the only guard for the rest. Price a name pin before believing "gated".
- **An audit line with NO context key cannot be scoped by a test** — the fix is production, however small, and it must extend the existing call (a `log(` count wall stands beside it).
- **"Retire the anchors" read literally deletes the reaper**: the attributes type the doomed conformer used is the same one the orphan reaper enumerates. Before deleting a type, list every reader, not every writer.
- **A retirement cell that pins what must be ABSENT is half a wall** — 9.4-NOW's cell pinned the four dead strings absent and three QUIC strings present but forgot the two the shipping mesh still uses; a deletion of a live string would have been green. Pin the live set as hard as the dead one.
- **Scoping two counts in a file that holds twenty is not a fix; it is a map of the other eighteen.** 9.7 closed the two `parkedReoffered` sites and 9.6 then gated the file — the verify found ~20 siblings with no key at all. Sweep the FILE for the token family, not the launcher's list.
- **A suite name that matches neither half of the selector's predicate is demanded by nothing** — `MeshP9CIHonestyTests` would have been a battery CI never asked for. Check a new suite's name against `isMeshBattery` before writing it.
- **A gate script that cannot see `Restarting after unexpected exit` will report an inflated green**: a restart re-runs suites and the total climbs past the floor. The check now lives in `run-gated-suites.sh` (proved at a `--check-log` seam only).
- **The verify agent could not write its findings file** (a harness block on that path) and returned them inline; the orchestrator transcribed them. Do not assume a scratch path is writable by every agent.
- **A ledger row can be wrong about a mechanism while right about the fact**: "`MeshNetworkManager.init` constructs `MeshMultipeerSession()`" — it resolves the factory; the ONE construction site is `MeshTransportSelection.swift`. A drafter grepping the ledger's claim found zero hits. Name the site, not the caller.

## Next item
**None — P9 is CLOSED.** The phase ends on `main` (**not pushed**) with plan §17.1 BUILT, §15
untouched, §26.1 dated superseded in part, §27 written, and three owner items carried by name (the
device gate, the MC→QUIC cutover, P9-3-A).

**The next session is P10, from `Docs/Next-Round-Prompt-Mesh-P10-2026-09-20.md`, in a FRESH session** —
a `/loop` does not compact between phases, so resuming this one just re-reads this context. P10 is the
companion `BGAppRefreshTask` (plan §17.2), the §16.4 import wall, and the cheap P9 residuals: the 11
sibling suites (83 cells) still off the CI line plus whichever P9-touched suites the 9.9 fix left as
honesty rows, the five process-wide `>= 1` audit counts and the ~43 unscoped `.count(of:)` reads
beside them, the live restart branch of `run-gated-suites.sh`, and item 8's residuals (the UI ratchet
runs on no CI line at all). Plan **§27** is the handoff; this ledger is a finished record — read its
"Blocked on owner" and "Surprises" sections once, then do not reuse it.

**Gates as P9 leaves them:** both scans **0**; mesh-batteries **`1048` over `118`
suites**; battery pin **`53`**; the P9 gate subset was 1 275 / 119 at `85b7c4b`, then
1 233 / 122 at `4dbd8d3`, then **`1 577 / 164`** at `4f52e0a` — three different lists, none of them
a full suite; determinism digests unmoved; `spm-wall-check` PASSED. **No full-suite run in P9**; the
last measured one is P8's 5 055 / 513 at `c86e3a0`.
