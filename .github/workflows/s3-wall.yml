# S3 privacy-wall enforcement (Docs/Security-Hardening-Plan-2026-06-27.md, WI-3).
#
# The carve-up's security feature — AIProviders/CloudKitSync being *structurally incapable* of
# reaching the sealed Private* stores — is only load-bearing when the build runs under
# DIAGNOSE_MISSING_TARGET_DEPENDENCIES=YES_ERROR (the flag must be on the build command; it does
# not reach the synthesized SwiftPM targets from the pbxproj). This workflow runs that check on
# every push/PR so a forbidden cross-wall `import` can never land green.
#
# RUNNER REQUIREMENT: macOS with an **iOS 26.5 SDK or newer** (any Xcode that ships one). Two
# separate floors are in play and both are checked below, in seconds, rather than surfacing as a
# confusing build failure minutes in:
#   * Swift tools 6.2 — FernletKit/Package.swift's tools version. An older toolchain cannot even
#     resolve the package ("using Swift tools version 6.2.0 but the installed version is 6.1.0").
#   * the iOS 26.5 SDK — the app uses API introduced there (AgeRangeService.AgeRangeDeclaration
#     .confirmed, in App/Fernlet/AgeAssuranceRequest.swift).
# The job selects the NEWEST Xcode 26+ on the image rather than pinning a minor version; pinning
# 26.5 is how this workflow silently fell back to Xcode 16.4 and failed on every run. If hosted
# images ever fall behind the SDK floor again, point `runs-on` at a self-hosted label. The
# pre-push hook (Scripts/install-git-hooks.sh) is the always-available, no-CI complement.
#
# Keep IPHONEOS_DEPLOYMENT_TARGET at the floor the product actually claims (26.0). A deployment
# target above the runner's SDK is a hard build error, so raising it to the latest point release
# silently makes this workflow unrunnable on hosted images.
#
# Since 2026-08-16 the same build is also the Power-of-10 rule-10 build (Docs/Power-of-10-Swift.md
# §R10): Scripts/spm-wall-check.sh passes SUPPRESS_WARNINGS=NO SWIFT_TREAT_WARNINGS_AS_ERRORS=YES
# GCC_TREAT_WARNINGS_AS_ERRORS=YES, so every warning — including the package warnings Xcode hides
# behind -suppress-warnings for local packages — fails the build. The Swift grep-wall port of the
# Power-of-10 mechanical checks (PowerOfTenBoundaryTests) runs below next to the other grep-walls;
# the Python scanner itself runs in power-of-10.yml on a cheap runner.
#
# Every test step runs through Scripts/run-gated-suites.sh, which writes a result bundle and
# refuses a green run that executed fewer tests than the step's floor: `-only-testing:` matches
# suite names exactly and matches NOTHING on a misspelling, so a bare xcodebuild line can name a
# suite that no longer exists and stay green forever. Tests/FernletTests/CIGateSelectorBoundaryTests
# is the static half (every named suite is declared; every mesh battery is named).
#
# Make this a REQUIRED status check in branch protection so the wall cannot be merged around.

name: S3 Wall

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

# One in-flight run per ref; newer pushes cancel older ones.
concurrency:
  group: s3-wall-${{ github.ref }}
  cancel-in-progress: true

jobs:
  s3-wall:
    name: Compiler wall + grep-wall
    # macos-26, not macos-15: the macos-15 image tops out at Xcode 26.3 / the iOS 26.3 SDK, which
    # is too old to compile this app (see the SDK floor check below). macos-26 carries Xcode
    # 26.4.1 through 26.6. Swap for a self-hosted label if hosted images ever fall behind again.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      # Picks the newest Xcode 26+ and exports it for every later step. Deliberately a *floor*,
      # not a pin: the exact minor version does not matter, being able to read Swift tools 6.2
      # and the iOS 26 SDK does. Missing toolchain = hard failure, never a silent fallback.
      - name: Select the newest Xcode 26+
        run: |
          set -euo pipefail
          shopt -s nullglob
          best_version=""; best_dir=""
          for app in /Applications/Xcode*.app; do
            version="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' \
              "$app/Contents/Info.plist" 2>/dev/null)" || continue
            [ -n "$version" ] || continue
            major="${version%%.*}"
            case "$major" in ''|*[!0-9]*) continue ;; esac
            [ "$major" -ge 26 ] || continue
            if [ -z "$best_version" ] || \
               [ "$(printf '%s\n%s\n' "$best_version" "$version" | sort -V | tail -1)" = "$version" ]; then
              best_version="$version"; best_dir="$app/Contents/Developer"
            fi
          done
          if [ -z "$best_dir" ]; then
            echo "::error::No Xcode 26 or newer on this runner. FernletKit needs Swift tools 6.2+ and the iOS 26 SDK."
            ls -d /Applications/Xcode*.app || true
            exit 1
          fi
          echo "Selected Xcode $best_version ($best_dir)"
          echo "DEVELOPER_DIR=$best_dir" >> "$GITHUB_ENV"
          DEVELOPER_DIR="$best_dir" xcodebuild -version
          DEVELOPER_DIR="$best_dir" swift --version

          # The Xcode version is not the real requirement — the iOS SDK it ships is. The app uses
          # API that only exists from the 26.5 SDK on (AgeRangeService.AgeRangeDeclaration
          # .confirmed, in App/Fernlet/AgeAssuranceRequest.swift), so an older SDK fails three minutes
          # in with "has no member 'confirmed'", which looks nothing like a toolchain problem.
          # Fail here instead, in seconds, saying exactly what is missing.
          required_sdk="26.5"
          sdk="$(DEVELOPER_DIR="$best_dir" xcodebuild -version -sdk iphonesimulator SDKVersion | head -1)"
          echo "iOS Simulator SDK: $sdk (require >= $required_sdk)"
          if [ "$(printf '%s\n%s\n' "$required_sdk" "$sdk" | sort -V | head -1)" != "$required_sdk" ]; then
            echo "::error::iOS SDK $sdk is older than the required $required_sdk. Fernlet uses API added in the $required_sdk SDK (AgeRangeService.AgeRangeDeclaration.confirmed). Use a runner image with a newer Xcode, or a self-hosted runner."
            exit 1
          fi

      # iPhone 17 is the ONE device every test line runs on — the scripts' defaults
      # (Scripts/run-gated-suites.sh, Scripts/spm-wall-check.sh and, through the latter, the
      # pre-push hook) all name it — and it is a hard requirement, not a preference. Until
      # 2026-09-21 this step fell back to the newest available iPhone with a `::warning::`, which
      # is precisely the silent device change the appearance baselines cannot survive:
      # `UXScreenProbe.auditBaselines` (Tests/FernletUITests) is pinned four ways to an iPhone 17
      # in portrait; on any other device — the 17 Pro has identical point geometry — the probe's
      # environment guard refuses to run the ratchet at all, and without that guard the deltas read
      # like a regression that never happened (the misdiagnosis its doc records).
      # This workflow does not run that suite yet, and the unpinned device was the unnamed
      # prerequisite under all three of the UI residuals that would put it here (plan §17.2.3
      # finding 6, §28.3). So: if the image drops the device, fail in seconds, naming the fix.
      # `CIGateSelectorBoundaryTests.theSimulatorDestinationIsPinnedWithNoFallback` reads this step
      # and the two scripts' defaults, so the three cannot drift apart or grow a fallback quietly.
      - name: Resolve a simulator destination
        run: |
          set -euo pipefail
          name="iPhone 17"
          if ! xcrun simctl list devices available | grep -qE "^ *${name} \("; then
            echo "::error::The '${name}' simulator is not available on this runner, and every test line is pinned to it (the appearance baselines are recorded on it and fail on any other device — plan §28.3). Use a runner image that ships it, or a self-hosted runner. Do not substitute another device here."
            xcrun simctl list devices available || true
            exit 1
          fi
          echo "Using simulator: $name"
          echo "FERNLET_DESTINATION=platform=iOS Simulator,name=$name" >> "$GITHUB_ENV"

      - name: Enforcement self-tests (prove the wall AND the test gate are load-bearing) + clean-tree check
        # Two negative self-tests. Each plants a defect, requires the wall to refuse it, and then
        # requires the clean case to pass — a negative alone proves nothing about the mechanism.
        #   * spm-wall-selftest.sh plants a forbidden cross-wall import into the walled AIProviders
        #     target and requires the compiler wall to reject it (WI-3), then re-runs the wall check
        #     on the clean tree.
        #   * run-gated-suites-selftest.sh (P10 item 5 (c)) stubs `xcodebuild` and `xcrun` on PATH
        #     and requires run-gated-suites.sh to refuse a green-LOOKING run whose output carries
        #     `Restarting after unexpected exit, crash, or test timeout`. Until this existed the
        #     restart guard was executed only at its `--check-log` seam — a SECOND code path — so a
        #     refactor that dropped the `tee`, renamed `$RUN_LOG` or moved the grep would have left
        #     the seam green and every floor on this workflow inflated by re-run suites. It needs no
        #     Simulator and no build: the stubs are what the gate reads.
        # Neither line names Scripts/run-gated-suites.sh as a word, so CIGateSelectorBoundaryTests'
        # `gatedSteps` parses no step here and no measured suite-name count moves.
        run: |
          set -euo pipefail
          Scripts/spm-wall-selftest.sh
          Scripts/run-gated-suites-selftest.sh

      - name: Grep-wall (S3BoundaryTests) + the companion-refresh seam
        # `AuditRatchetBoundaryTests` joins the two at P9 item 9's fix review (2026-09-20). It is a
        # pure SOURCE scan — every frozen `UXScreenProbe.auditBaselines` entry still names a probed
        # screen, and the probe inventory cannot silently shrink — so it belongs with the walls
        # rather than on the simulator-heavy mesh line, and it needs no floor of its own beyond the
        # shared 1. Item 8 re-recorded those baselines and the suite ran on NO CI line at all; the
        # ratchet's other half lives in Tests/FernletUITests, which this workflow never runs, and
        # `MeshP9HonestyAcceptanceTests` says so out loud rather than leaving it to be inferred.
        #
        # `BackgroundRefreshBoundaryTests` joins them at P10 item 2 (plan §16.4): the
        # background-refresh import wall. Another pure SOURCE scan with no compiler half — it walks
        # `App/Fernlet/CompanionRefresh/` and holds every file there to a module allowlist and a
        # call-spelling zero-list, so the companion `BGAppRefreshTask` cannot reach the mesh,
        # HealthKit, a CloudKit force-sync, Foundation Models, or store creation. It belongs on this
        # line for the same reason the other two do: nothing else runs it, and §16.4 requires it to
        # be a gate from the commit that creates the directory, not from the commit that fills it.
        #
        # `CompanionRefreshSchedulingTests` joins them at P10 item 3. It is NOT a source scan — it is
        # the tier-1 behaviour suite over the refresh scheduling seam's two fakes (one registration
        # with the frozen identifier, the request's earliest-begin policy, the chain's re-submission
        # before the completion, a refusal reported rather than swallowed, and exactly-once
        # completion over a five-row table) plus the pins that keep the refresh seam and the mesh's
        # apart. It rides THIS line rather than the mesh one for two reasons: it is not a
        # `MeshP<n>…AcceptanceTests`, so `everyMeshAcceptanceBatteryIsGated` does not demand it
        # anywhere; and this step's floor is the shared `1`, so adding a name here needs no Mac to
        # re-measure, whereas the mesh step's floor is a measured test count that a draft must not
        # guess at. Item 9 may move it when it measures its own battery line.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh s3-grep 1 S3BoundaryTests CIGateSelectorBoundaryTests \
            AuditRatchetBoundaryTests BackgroundRefreshBoundaryTests \
            CompanionRefreshSchedulingTests CompanionRefreshPipelineTests \
            WidgetSnapshotContentEqualityTests

      - name: No-tracking wall (NoTrackingBoundaryTests)
        # Independent of the S3 wall: this one has no compiler half, because a tracking SDK is an
        # honest new dependency that compiles clean. The test IS the enforcement, so it must run
        # even when the S3 steps above pass. See Docs/No-Tracking-Wall.md.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh no-tracking 1 NoTrackingBoundaryTests

      - name: Power-of-10 grep-wall (PowerOfTenBoundaryTests)
        # The Swift port of Scripts/power-of-10-scan.py (Docs/Power-of-10-Swift.md): function
        # length, force unwraps / traps, swallowed `try?`, unbounded loops, mutable globals, `#if`
        # policy, unsafe seams, the assertion-density floor, and the rule-10 build-setting pins.
        # Reads the same allowlist as the scanner, so the two cannot disagree about an exemption.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh power-of-10 1 PowerOfTenBoundaryTests

      - name: Localization wall
        # Also a no-compiler-half wall, and every failure it catches is SILENT in ordinary
        # development: a package string that resolves against the wrong bundle renders English
        # forever with a clean build; a changed sealed-column rawValue drops symptoms out of an
        # encrypted history with no error; a drifted widget token leaves the widget on the
        # neutral companion in a separate process no app test observes. Nothing here is visible
        # in an English simulator run, which is why it has to run on every push.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh localization 1 LocalizationBoundaryTests

      - name: Key-custody + at-rest-format tripwires
        # Same shape as the no-tracking wall: no compiler half, so these suites ARE the
        # enforcement (Docs/Verifiability.md §2, §4) and must run here for the "cannot merge
        # green" claim in Docs/Release-Process.md §1 to be true. Simulator-safe by design:
        # KeyCustodyBoundaryTests drives the production key stores against per-test UUID
        # keychain services (plus the simulator's own data-protection keychain for the two
        # well-known rows) and pins the one passcode-gated class in source, so no device
        # passcode is needed. The remaining suites are pure crypto format pins, plus
        # IdentityProvisioningReadTests (F-1, 2026-09-06): the fail-closed identity read whose
        # source wall keeps a transient keychain error from minting over a live identity.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh key-custody 1 \
            KeyCustodyBoundaryTests \
            ColumnCryptoDeviceBindingTests \
            SealedBackupFormatPinTests \
            IdentityProvisioningReadTests

      - name: Crypto domain + framing + membership-golden tripwires
        # The third wall with no compiler half. `CryptographicPurposeBoundaryTests` carries the
        # framing trio — including `everySignaturePurposeIsHeldToADeclaredFraming`, which reads the
        # Signature block off disk and fails any purpose not held to a declared framing, the check
        # P6 item 1's new signature domain is the first to rely on. `CryptographicDomainSeparationTests`
        # is the auto-failing inventory (every declared purpose has a row, no purpose is a prefix of
        # another) and `MeshMembershipEventGoldenTests` pins the membership wire vectors, which must
        # never be re-pinned. All three ran only when somebody remembered to run the full suite
        # locally until 2026-09-11. Floor = the measured count at that date; RAISE it when a vector
        # or a purpose is added.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh crypto-goldens 38 \
            CryptographicPurposeBoundaryTests \
            CryptographicDomainSeparationTests \
            MeshMembershipEventGoldenTests

      - name: Mesh acceptance batteries (P3 – P9) + the routed suites + the CI selector wall
        # The three phases' acceptance batteries (Docs/Plan-ProximityKit-Network-Migration-2026-08-27.md
        # §11.4, §23.4) ran only by hand until 2026-09-06. Tier 1 throughout: FakePeerNetwork, an
        # injected clock, no radio, no wall-clock sleeps — ~2 minutes on a hosted runner.
        #
        # The decomposition below was RE-MEASURED per suite at P6 item 9, from that item's own
        # gated run (`Scripts/run-gated-suites.sh mesh-batteries 1 <this exact list>` at floor 1,
        # read off the result bundle). Terms:
        #   108  the P3/P4/P5 acceptance batteries + the two convergence generators
        #        (MeshConvergenceScheduleTests, MeshConvergencePropertyTests) + the routed
        #        convergence battery. 105 at P6 item 7; item 9 added rectangle G's property cell
        #        and two rectangle pins to MeshRoutedDrainConvergenceTests (now 19).
        #  +  4  CIGateSelectorBoundaryTests' own — the static half of this guard.
        #  +  8  MeshRoutedRefusalBudgetTests — the per-sender pre-store refusal bound the P5
        #        post-close review added.
        #  + 27  MeshPairwiseFoundingTests (P6 item 2: the proximity-join founding — the app's ONLY
        #        session entry, and the only suite that drives the real commit/founding/admission/
        #        delivery path with nothing seeded).
        #  + 32  P6 item 4's three text suites (16 + 3 + 13): the routed text body's framing, its
        #        FIVE hostile shapes and its cap formula; the sender's outcome table; and text
        #        delivered and PROJECTED end to end through the real founding path.
        #  + 30  P6 item 5's two allowance suites and item 8's drain-wall home (6 + 5 + 19): the
        #        retry planner, the two lists on a rig, and the retirement/parking walls — an
        #        AVAILABILITY wall against an attacker-choosable list position.
        #  + 44  P6 item 6's four heart suites (7 + 6 + 16 + 15): the header-only body and its four
        #        hostile shapes, the `.singleRecipient` flip with the shape fence in both
        #        directions, the ack ceremony end to end on a real two-node founding, and the
        #        sender's five gates + consume-on-stage.
        #  + 26  MeshRoutedLockedDeviceTests, gated by P6 item 9 and priced on its OWN argument
        #        rather than riding the drain wall's: it is 26 behaviour cells over five store
        #        states and two independent knobs, not a zero-list wall, and it is where item 7's
        #        launch-restore decision table and item 6's W2 rows live.
        #  + 21  P6 item 9's eight acceptance suites (2 + 2 + 2 + 3 + 2 + 3 + 2 + 5): key
        #        advertisement, pairwise identity, per-type caps, text routing, projection retry,
        #        the heart ceremony, honesty and determinism.
        #  = 300 tests in 50 suites.
        # `MeshRoutedDrainTests` is GATED from P8 item 1 (2026-09-18): 43 behaviour cells (the block used to say 37 — a number never measured; the suite already held 43 at the P6 close-out `82fc4d7`) — one of
        # them P6 item 8's handed-over departed-origin custodian cell — that ran on no CI line until
        # then. Like `MeshRoutedLockedDeviceTests` it is deliberately NOT pinned by name in
        # CIGateSelectorBoundaryTests: it is a behaviour suite, not a zero-list wall, and THIS floor
        # is what stops it being dropped from the line.
        # MEASURED at P8 item 1 on a Mac, 2026-09-18, three runs back to back on one warm bundle:
        #   without `MeshRoutedDrainTests` (the 56-suite list)  — 316 ran, 0 failed, 153 s
        #   with    `MeshRoutedDrainTests` (57 suites)          — 359 ran, 0 failed, 157 s  (+43, +4 s;
        #     the same 57-suite list measured 157 / 159 / 160 s across three runs, so +4 s is inside noise
        #     and +11 s for the fourteen is three to four times it)
        #   plus the fourteen named below (that hypothetical list was 57 + 14 = 71 suites)
        #                                                       — 606 ran, 0 failed, 168 s  (+247, +11 s)
        #     NOTE the collision: that "71 suites" is NOT today's 71-suite line (57 + P8 item 10's
        #     OWN, different fourteen), and neither "fourteen" is the other.
        # GATED at P9 item 6 (2026-09-20), the fourteen the owner's call had deferred; 247 cells for
        # a measured +11 s. Per suite, as re-counted at P9 item 6 — the selector names the STRUCT,
        # never the file, and seven of the fourteen do not share their file's name:
        #   item 1's three key-agreement suites (30), all in MeshKeyAgreementAdvertisementTests.swift:
        #     MeshKeyAgreementFoldTests 14, MeshKeyAgreementVerificationTests 11,
        #     MeshKeyAgreementSchemaTests 5;
        #   the 51 photo-delivery cells the key-advertisement clause promotes by citation, all three
        #     in MeshRoutedPhotoDeliveryTests.swift:
        #     MeshKeyAdvertisementDeliveryTests 26, MeshRoutedPhotoDeliveryTests 17,
        #     MeshRoutedPhotoSenderTests 8;
        #   eight of the larger remaining ungated routed suites (166) — NOT the eight largest: the
        #     32-cell MeshRoutedInventoryDeltaTests is deliberately outside this price — MeshRoutedManifestSigningTests 27,
        #     MeshRoutedCustodyHandoffTests 26, MeshRoutedCapacityTests 22, MeshRoutedItemSealTests 21,
        #     MeshRoutedTypeRegistryTests 19, MeshRoutedInventoryBuilderTests 18,
        #     MeshRoutedCustodyEvidenceTests 17, MeshRoutedStoreIsolationTests 16.
        # P6 item 9's handoff said "~243 across fourteen suites" and its itemisation lived in a scratch
        # file that is gone; these fourteen MEASURE 247. The measured delta is what is recorded here,
        # never the remembered estimate.
        # Two of the fourteen carry NO @Suite attribute — MeshRoutedItemSealTests and
        # MeshRoutedStoreIsolationTests are plain structs found by their @Tests; the other twelve
        # carry @Suite(.serialized) on the line ABOVE the struct. A grep for @Suite under-counts
        # both ways, which is why this list is maintained against DECLARATIONS, not against a grep.
        # Gating the fourteen does not gate their FILES: 11 sibling suites (83 cells) in the same
        # files stay off the line — MeshRoutedManifestGoldenTests 16,
        # MeshRoutedTypeRegistryConsumerTests 10, MeshRoutedCustodyHandoffWallTests 9,
        # MeshRoutedItemSealGoldenTests 9, and seven smaller. Named here rather than silently
        # widened into the item.
        # RE-MEASURED at P8 item 3 (2026-09-18): the same 57-suite line executed 365 (359 + the six
        # founding cells item 3 added to MeshPairwiseFoundingTests, now 35: the hold's doors, its clock,
        # its uncommitted slots, the committed heal). The floor is the measured 365.
        # The floor is MEASURED, never inferred: 300 is what this exact step list executed at P6
        # item 9. Note the count is over `@Test` FUNCTIONS, not parameterized cases — measured, not
        # assumed: MeshRoutedDrainConvergenceTests contributes 19 while running a 40-cell and a
        # 12-cell rectangle. RAISE this as suites grow; never lower it in passing.
        # P7 item 7 added the six MeshP7*AcceptanceTests suites (13 `@Test` functions by count)
        # without raising the floor (no toolchain to measure with). RE-MEASURED at P8 item 0 on
        # 2026-09-18, on a Mac, with this exact list at floor 300: **316 ran, 0 failed** (the
        # script's own bundle count). 316 = 300 + 13 (P7) + 2 (P8 item 0 (a) and (d): the
        # two-ordering asymmetric-commit cell — one @Test function — and the custody-yield cell in
        # MeshPairwiseFoundingTests, now 29) + 1 that the console total does not attribute to a
        # suite; the floor is the measured number, never the arithmetic. RAISED to **359** at P8
        # item 1 (2026-09-18) by gating `MeshRoutedDrainTests`: 316 + 43, measured on the same bundle
        # (`mesh-with` run, "359 test(s) ran, 0 failed"), and shown red once at 359 with the suite
        # removed before it was committed.
        # P8 item 10 (2026-09-18) gates the rest of P8 — its own acceptance battery AND the eight
        # `MeshContinuation*` suites items 4/5/6/7 shipped, which ran on NO CI line until now: none
        # is a `MeshP<n>…AcceptanceTests`, so `everyMeshAcceptanceBatteryIsGated` never demanded
        # them and nothing in the tree would have noticed their deletion. Fourteen suites join the
        # line. MEASURED on a Mac, 2026-09-18, on one warm bundle, with this exact list run at the
        # OLD floor 365: **469 ran, 0 failed** (the script's own bundle count), 186 s test time.
        # Per suite, read off that run's own result bundle — measured, never inferred:
        #   +  3  MeshP8CoordinatorTableAcceptanceTests — item 4's 48-row table re-walked against
        #         the battery's OWN expectation, spelled event-outermost: a THIRD decomposition,
        #         neither the shipped table's state-first helpers nor the unit suite's oracle. Plus
        #         the exactly-once biconditional over the whole product, and the progress ratchet.
        #   +  4  MeshP8HoldVerbAcceptanceTests — item 3's verb on the founding rig: the committed
        #         link and its coordinator kept, `pauseDiscovery()` once and `stop()` never, all
        #         three doors shut and the uncommitted slot dropped, the give-up clock stood down
        #         and restarted by the resume, and the policy row executing with the refusal's
        #         token at zero under `App/`.
        #   +  3  MeshP8RaiseAndDisagreementAcceptanceTests — item 5's three raise-wall counts, the
        #         four corners of the two legs on a real founding (through item 6's scene-dark
        #         rule), and the mid-task reset that gives the session leg back.
        #   +  3  MeshP8PresentationAcceptanceTests — item 7's 6 × 10 product (the 23 presenting
        #         cells pinned by identity, 37 silent), `projectedAudit` keeping an ending across
        #         the session end, and the slot decision's six rows.
        #   +  5  MeshP8TaskWiringAcceptanceTests — item 6 through its own two fakes: register and
        #         submit once, a `.fail` refusal reaching the card, a delivery completed exactly
        #         once at expiry, the hard stop paying the debt, and the feed with the kept tunnel.
        #   +  3  MeshP8HonestyAcceptanceTests — plan §15.1–§15.4 by name, the runbook's Lane B
        #         rows, the Simulator's own `BGTaskScheduler` refusal, item 3's device-only QUIC
        #         hold row, and the two determinism digests' single home.
        #   + 13  MeshContinuationCoordinatorTests  + 6  MeshContinuationProgressTests   (item 4)
        #   + 13  MeshContinuationCardPresentationTests                                   (item 7)
        #   + 12  MeshContinuationDriverTests + 5 MeshContinuationRaiseWallTests
        #     + 7  MeshContinuationDisagreementTests                                      (item 5)
        #   + 21  MeshContinuationTaskHostTests + 7 MeshContinuationTaskHostWallTests     (item 6,
        #         the wall suite +1 in P9 item 5: the 1:1 foreground anchors' retirement)
        #   = 105 added; 365 + 105 = 470, which is also what the run counted. The floor is the
        #     MEASURED number; the arithmetic agreeing with it is a check, never the source.
        # RAISED 365 → 469, and shown red once at 469 with one suite removed from the line before
        # it was committed; 469 → 470 in P9 item 5, re-MEASURED over this exact list
        # (`Test run with 470 tests in 71 suites passed`) rather than incremented. `MeshContinuationRaiseWallTests` and `MeshContinuationTaskHostWallTests`
        # are ALSO pinned by name in CIGateSelectorBoundaryTests — both are walls with no compiler
        # half, the `MeshRoutedDrainWallTests` argument. The five behaviour suites beside them are
        # protected by THIS floor and not by a name, the `MeshRoutedLockedDeviceTests` precedent.
        # CIGateSelectorBoundaryTests is the static half of this guard — every suite named on any
        # run-gated-suites.sh line must be a declared suite, and every MeshP<n>*AcceptanceTests /
        # convergence battery declared in the tree must be named here — and it also runs on the first
        # wall step, so editing this step cannot silence it.
        # RAISED 470 → 718 at P9 item 6 (2026-09-20) by gating the fourteen above, MEASURED on
        # a Mac on one warm bundle with this exact 85-suite list run at floor 1 (the script's own
        # result-bundle count; 470 + 247 + item 7's one new drain cell = 718 is the arithmetic
        # check, never the source), and shown red once with one suite removed from the line before
        # it was committed. Of the fourteen only MeshRoutedStoreIsolationTests is ALSO pinned by
        # name in CIGateSelectorBoundaryTests — it is a source grep-wall with no compiler half, the
        # MeshRoutedDrainWallTests argument; the other thirteen are behaviour suites and THIS floor
        # is what stops any of them being dropped from the line.
        # 718 → 719 at item 6's FIX REVIEW (2026-09-20), RE-MEASURED over this same 85-suite list
        # ("Test run with 719 tests in 85 suites passed", 165.8 s): +1 for the sibling of item 7's
        # proof cell, MeshKeyAdvertisementDeliveryTests.aSiblingRigsParkRefusalIsNotCountedAgainstThisOne,
        # which is what the twenty newly scoped audit counts in that file owe. The NAME count is
        # unchanged at 85 and is now pinned itself, in CIGateSelectorBoundaryTests'
        # `measuredSuiteNameCounts` — so a name leaving this line reds with no simulator, which is
        # the half the thirteen behaviour suites above did not have.
        # RAISED 719 -> 860 at P9 item 9 (2026-09-20) by adding P9's own acceptance battery and
        # the seven suites items 2, 3 and 4 left on no CI line at all. 85 -> 97 suite names,
        # re-pinned in CIGateSelectorBoundaryTests' `measuredSuiteNameCounts` in this same commit,
        # and the battery pin there moves 48 -> 53. Terms:
        #   +  3  MeshP9EphemeralPostureAcceptanceTests - the posture both radios wear: four epoch
        #         boundaries walked as VALUES (no name, no certificate, no epoch repeats), the
        #         certificate anchored to the epoch START through the injected mint seam rather
        #         than through the unit suite's DER arithmetic, the two posture types pinned apart
        #         (900 s epoch vs. per-start()/resume()), and neither persisted.
        #   +  4  MeshP9PresenceSwapAcceptanceTests - item 2 pass 2 end to end on the shipping
        #         manager over the seam's conformer: a mid-epoch start wearing the posture's own
        #         name AND certificate, a tick inside the epoch that republishes nothing, a
        #         boundary that replaces all of it and withdraws the old name; the service type and
        #         ALPN distinct and declared while the retired pair is gone; every presence audit
        #         line naming its peer only by a salted, per-session, fixed-width label, read
        #         SCOPED to this session (item 7's rule); and the MC path gone from the manager,
        #         walked over the whole package rather than one file.
        #   +  5  MeshP9RecipeSwapAcceptanceTests - item 3: the whole 5 x 12 discovery-gate product
        #         against this battery's OWN event-outermost literal table (five of sixty readings
        #         move discovery, and no more), a pause that leaves a share in flight alone with a
        #         record minted UNDER the pause it mirrors, the exactly-once completion oracle, the
        #         per-resume() fresh posture and the stop() that keeps no pause flag, the service
        #         type and ALPN, and the containment pairing that keeps discovery behind one door.
        #   +  4  MeshP9McRetirementAcceptanceTests - item 4 as an HONESTY row, not a zero-list:
        #         both MC files still exist BECAUSE MeshTransportFactory.shippingDefault is
        #         .multipeer and stranger admission has no QUIC path, the framework import has
        #         exactly two homes, the four dead Bonjour strings are gone while the LIVE friend
        #         pair is still declared, and the owner's D-4.1/D-4.3 record survives. Every cell
        #         here is one the cutover commit must edit - that is the handoff.
        #   +  4  MeshP9HonestyAcceptanceTests - the two tier-2 lane rows, the three branches
        #         nothing has ever observed, item 0's device rows, P9-3-A and 9.4-LATER, each as an
        #         assertion over the doc that records it; the two determinism digests' single home;
        #         and this battery's own gating. (+2 at the fix review: the total accounting over
        #         the forty-nine P9-touched suites, and the two coverage holes of P9's own walls —
        #         the ratchet half CI never runs and the unscoped audit counts - forty-two when
        #         that cell was written, twenty since P10 item 5.)
        #   +  9  PresenceEpochPostureTests   + 11  PresenceAdvertisementTests        (item 2 pass 1)
        #   + 24  PresenceManagerTests        + 20  PresenceOverQUICTests             (item 2)
        #   + 20  RecipeShareTransferTests    + 34  RecipeShareOverQUICTests          (item 3)
        #   +  3  TransportNeutralityBoundaryTests - the MultipeerConnectivity permit wall. It ran
        #         on NO CI line at all, and it is the whole-identifier scan clause (d) defers to for
        #         the app target (where "MCSession" is a legitimate inspector row label and
        #         FileMCPeerIDStore is Fernlet's own name). ALSO pinned by NAME in
        #         CIGateSelectorBoundaryTests: a source grep-wall with no compiler half, the
        #         MeshRoutedDrainWallTests argument, and the one of the seven that earns it.
        #   = 141 added (the twelve terms above: 20 clause cells + 121 promoted); 719 + 141 = 860
        #     is the ARITHMETIC CHECK, never the source. The floor is the MEASURED number, read off
        #     the result bundle over this exact list ("Test run with 860 tests in 97 suites passed",
        #     165.9 s, no `Restarting after unexpected exit` line), and it is shown red once with
        #     one suite removed from the line before it is committed.
        # RAISED AGAIN at item 9's FIX REVIEW (2026-09-20). Item 9's own verify found the accounting
        # above was a hand-list of seven: `ProximityRecipeShareCapTests` — 31 cells, rewritten by
        # item 3 pass 2, holding the pause/resume behaviour clause (c) leans on — was on NO CI line
        # and nothing in the tree demanded it. The list is now DERIVED in
        # MeshP9HonestyAcceptanceTests.p9TouchedSuites (every suite declared in a file
        # `git diff --name-only bb454fe..HEAD -- Tests/FernletTests` names), and every one of those
        # forty-nine must be gated or carry a reason in that suite's `ungatedByDesign` map.
        # Twenty-one names join the line here; three cell counts are added on their own argument:
        #   + 31  ProximityRecipeShareCapTests - the 2-device cap and all three reopen paths. The
        #         cells clause (c) cites by name (establishingConnectionPausesDiscovery,
        #         peerDisconnectEvictionResumesDiscovery, aStartFailureStandsTheListenerDownByItsOwn
        #         Account, sendRefusedVisiblyWhilePairedWithAnotherPeer) live HERE.
        #   +  3  ProximityRecipeShareDiagnosticsTests - the recipe diagnostics ring.
        #   + 12  MeshTransportSelectionTests - the radio the app's own initializer builds and the
        #         pure selection function. Clause (d) asserts the two factory VALUES, but a DEBUG
        #         test build cannot execute the Release arm of resolvedKind(environment:) at all;
        #         this suite is the other half, and it is pinned by NAME in the selector wall.
        #   + 13  PresenceHeartsTests    + 8  PresenceTagTests - the presence radio's payload: the
        #         per-epoch pair tags and the heart ledger the swapped radio carries.
        #   +119  the SIXTEEN suites of NetworkMeshTransportTests.swift (MeshLinkTableTests 19,
        #         MeshTransferStreamTableTests 19, MeshChannelIntroductionTests 15,
        #         MeshTunnelConvergenceTests 11, NetworkMeshSessionTests 10, MeshLinkAdvertisementTests 7,
        #         MeshHeartbeatScheduleTests 5, MeshHeartbeatLivenessTests 5,
        #         EphemeralMeshTLSIdentityTests 5, MeshInboundRankingTests 4, NetworkMeshWireTests 4,
        #         MeshDialPreferenceTests 3, MeshTunnelEndReasonTests 3,
        #         MeshChannelIntroductionTranscriptTests 3, MeshIntroductionRosterTests 3,
        #         MeshIntroductionNonceCacheTests 3). P9 item 2/3 edited this file; the selector
        #         names a STRUCT and there is no type called `NetworkMeshTransportTests` at all, so
        #         a line naming the FILE would have matched nothing and passed having run zero
        #         tests. Gated because the file is radio-free and deterministic: the sessions it
        #         constructs are never started, NetworkPeerChannel's advertise/browse doors are
        #         no-ops, and there is not one sleep or wall-clock wait in its 2565 lines.
        # EIGHT P9-touched suites are deliberately NOT gated and are named with their reasons in
        # MeshP9HonestyAcceptanceTests.ungatedByDesign: MemoryLifecycleTests' seven timed weak-ref
        # polls (a floor cannot price a deallocation observed by waiting) and
        # PersistenceFailureAuditTests (item 1's wall, not a mesh suite). It was eleven until P10
        # item 5: the three behaviour suites in MeshRoutedBackpressureTests.swift that item 7 only
        # re-scoped are on THIS line now, and their three rows left that map in the same commit.
        # RAISED 860 -> 1048, MEASURED over this exact 118-name list on a Mac (2026-09-20), floor 1,
        # read off the result bundle: "Test run with 1048 tests in 118 suites passed after 192.745
        # seconds", 0 failed, no `Restarting after unexpected exit` line. 860 + 186 (the six terms
        # above) + 2 (the battery's own two new honesty cells) = 1048 is the ARITHMETIC CHECK, never
        # the source, and the floor is shown red once with a suite removed from the line before it
        # is committed. The NAME count moves 97 -> 118 in CIGateSelectorBoundaryTests'
        # `measuredSuiteNameCounts` in this same commit, and `s3-grep` 2 -> 3.
        # P10 item 5 (a) (2026-09-21) gates SEVENTEEN more names in two tiers.
        # TIER 1+2, fourteen names / 105 cells: the ELEVEN sibling suites the block above named as
        # deliberately left off (83 cells in six files whose OTHER suite was already on this line)
        # plus the three suites of MeshRoutedBackpressureTests.swift that
        # MeshP9HonestyAcceptanceTests.ungatedByDesign said were P10's to price (22 cells). Their
        # three `ungatedByDesign` rows are DELETED in this same commit - that map reds for a suite
        # CI does run, which is the opposite failure and the one that teaches a reader to distrust
        # the rest.
        # Of the seven P9-touched suites the P10 launcher lists as possibly-still-honesty-rows, SIX
        # are already on this line (ProximityRecipeShareCapTests, ProximityRecipeShareDiagnostics-
        # Tests, MeshTransportSelectionTests, PresenceHeartsTests, PresenceTagTests and all sixteen
        # suites of NetworkMeshTransportTests.swift) - item 9's fix review gated them. The seventh,
        # `PeerTransportNeutralityTests`, names no declared type at all: that FILE declares
        # PeerHandleIdentityTests, MeshMultipeerSessionIdentityTests and FakePeerTransportTests, and
        # it is not in `git diff bb454fe..HEAD -- Tests/FernletTests`, so P9 never touched it and
        # `p9TouchedSuites` correctly omits it. It is TIER 3 below, on its own argument.
        # Per suite, counted over `@Test` FUNCTIONS inside each DECLARATION - a grep for @Suite
        # under-counts, and a grep for @Test over a FILE over-counts across its siblings:
        #   + 16  MeshRoutedManifestGoldenTests          (MeshRoutedManifestTests.swift)
        #   + 10  MeshRoutedTypeRegistryConsumerTests    (MeshRoutedTypeRegistryTests.swift)
        #   +  9  MeshRoutedCustodyHandoffWallTests      (MeshRoutedCustodyHandoffTests.swift)
        #   +  9  MeshRoutedItemSealGoldenTests          (MeshRoutedItemSealTests.swift)
        #   +  8  MeshRoutedCustodyHandoffDoorTests      (MeshRoutedCustodyHandoffTests.swift)
        #   +  8  MeshRoutedTypeRegistryDoorTests        (MeshRoutedTypeRegistryTests.swift)
        #   +  7  MeshRoutedDeliveryPersistenceTests     (MeshRoutedCustodyTests.swift)
        #   +  6  MeshRoutedSweepTests                   (MeshRoutedCapacityTests.swift)
        #   +  4  MeshRoutedCustodyParityTests           (MeshRoutedCustodyTests.swift)
        #   +  4  MeshRoutedParkedDropTests              (MeshRoutedCapacityTests.swift)
        #   +  2  MeshRoutedTypeRegistryFourthTypeTests  (MeshRoutedTypeRegistryTests.swift)
        #   = 83, the number the block above named, RE-COUNTED at this commit rather than carried.
        #   + 12  MeshRoutedBackpressureTests  +  7  MeshRoutedParkedDropDoorTests
        #   +  3  RoutedDeliveryHoldCopyTests   (all three in MeshRoutedBackpressureTests.swift)
        #   = 22.
        # All fourteen are @Suite structs (thirteen .serialized; RoutedDeliveryHoldCopyTests is a
        # plain @Suite over value copies), and all seven files are radio-free and sleep-free - no
        # NWListener/NWConnection/NWBrowser, no Task.sleep/Thread.sleep/usleep/asyncAfter, no
        # NotificationCenter, nothing .disabled or .timeLimit-ed. The one `Date()` among them
        # (MeshRoutedBackpressureTests.swift, aDismissalNeverSilencesADifferentHold) is an instant
        # handed to a value initializer, not a deadline anything waits on. So: one step, no
        # isolation. Each already runs in THIS process beside its gated sibling in any local
        # full-suite run, so none of them is a new interaction.
        # TIER 3, three names / 19 cells: PeerTransportNeutralityTests.swift's
        #   +  10  FakePeerTransportTests   +  5  PeerHandleIdentityTests
        #   +   4  MeshMultipeerSessionIdentityTests
        # gated on its OWN argument, not on P9's: it holds the `PeerHandle.isSameEndpoint(as:)` rule
        # the mesh call sites depend on, the plan's endpoint->identity split, and the FakePeerNetwork
        # fabric every acceptance battery on this line is built on - a fake nobody checks is a source
        # of false confidence. It is the only addition here that waits on wall clock, and it waits
        # the way this repository requires: `waitUntil` pairs a 5 s deadline with a 200-poll FLOOR,
        # because a deadline alone measures wall time that keeps advancing while a @MainActor suite
        # is starved by every other @MainActor suite in a loaded run. No radio is ever started - the
        # MCNearbyServiceBrowser / MCNearbyServiceAdvertiser it builds are delegate ARGUMENTS,
        # neither startBrowsingForPeers nor startAdvertisingPeer is called, and every session takes
        # `usesEphemeralPeerID: true`, so the archived FileMCPeerIDStore the shipping radios share is
        # never touched. If the MC->QUIC cutover empties that file, these three names leave the line
        # deliberately - which is the point of pinning the NAME count beside the floor.
        # RAISED 1048 -> 1172, MEASURED over this exact 135-name list on a Mac (2026-09-21),
        # floor 1, read off the result bundle: "Test run with 1172 tests in 135 suites passed after
        # 193.995 seconds", 0 failed, 0 skipped, no `Restarting after unexpected exit` line. The
        # line was then re-run AT 1172 to prove it passes at its own floor, and shown red once with
        # a name removed. 1048 + 105 + 19 = 1172 is the ARITHMETIC CHECK, never the source - it
        # happens to agree here, which is what a measurement is for and not a substitute for one.
        # PRICE, MEASURED on the same warm bundle: 192.745 s -> 193.995 s, +1.25 s for +124 cells.
        # That is an order of magnitude under the two deltas this step already records (+247 cells
        # cost +11 s at P8 item 1, +329 cost +27 s at P9 items 6 and 9): those runs re-measured a
        # cold bundle, and these 124 cells are pure in-process table and golden work.
        # P10 item 9 adds the phase's ACCEPTANCE BATTERY: five serialized clause suites - the
        # scheduling seam, the handler pipeline, the diff rule, the import wall, and an honesty
        # suite naming what only a phone can show (and what item 8's Lane E measured on a
        # Simulator: registration accepted, everything after it refused with BGTaskScheduler
        # ErrorDomain code 1). They are named MeshP10<Clause>AcceptanceTests because
        # CIGateSelectorBoundaryTests.isMeshBattery matches only that shape: a battery called
        # CompanionRefreshAcceptanceTests would be demanded by nothing and would sit ungated
        # exactly as P3/P4/P5's did for three phases. The four refresh UNIT suites stay on the
        # s3-grep step (items 2, 3 and 4 put them there; this battery's clause (4) pins all four
        # by name) - this line gains the five batteries and nothing else. The NAME count moves
        # 135 -> 140 in CIGateSelectorBoundaryTests' measuredSuiteNameCounts and the battery pin
        # 53 -> 58 in the same commit; both are MEASURED at this commit, never arithmetic.
        # RAISED 1172 -> 1198, MEASURED over this exact 140-name list on a Mac (2026-09-21) after a
        # CLEAN build-for-testing, read off the result bundle: "Test run with 1198 tests in 140
        # suites", 0 skipped, no `Restarting after unexpected exit` line. The line was then re-run
        # AT 1198 and shown red once at 1199. 1172 + 26 = 1198 is the ARITHMETIC CHECK, never the
        # source. The battery's 26 cells are 5 + 6 + 5 + 5 + 5 across the five clause files.
        # THE RED THAT BLOCK RECORDED WAS NOT ENVIRONMENTAL: MeshRoutedHeartCeremonyTests'
        # everyHeartFailureCauseHasItsOwnSentence carried a latent `LocalizedStringKey ==` defect - not a
        # value comparison for an interpolated key - that codegen exposed when item 9 added five files to
        # the test module; the cell compares `String(describing:)` now and this line is green at 1198/140.
        # RAISED 1198 -> 1199 (2026-09-21, the cutover round's item 2a verify fixes): CIGateSelectorBoundaryTests,
        # which is on this line, gained one cell (the Simulator-device pin). MEASURED over this exact
        # 140-name list on a Mac after a build-for-testing, read off the result bundle: "Test run with
        # 1199 tests in 140 suites passed", no restart. 1198 + 1 = 1199 is the ARITHMETIC CHECK, never
        # the number. (The decomposition's "+ 4 CIGateSelectorBoundaryTests' own" term is P6's count and
        # is now 6; the RAISED lines carry the running total, the decomposition is not re-summed.)
        # RAISED 1199 -> 1217 (2026-09-21, the cutover round's item 1.2 — D-4.3's provisional stranger
        # admission). Four suites already on this line gained cells and a fifth joined it:
        # MeshChannelIntroductionTests +7 (doors shut/open, barred beats provisional, and the four
        # mesh-id rows the target-mesh rule owes), MeshIntroductionRosterTests +1 (the posture flag
        # defaults shut and moves no verdict), MeshLinkTableTests +1 (a pre-commit timeout refunds its
        # re-proposal, an owner refusal does not), MeshPairwiseFoundingTests +4 (the unseeded manager
        # half, Option 1b's frame gate, the eviction-cause table, the hold's cause), and
        # MeshIntroductionAuthorityRosterTests joins the line with 5 (3 existing + 2 for the join-door
        # predicate) — so the NAME count moves 140 -> 141 in CIGateSelectorBoundaryTests in this same
        # commit. MEASURED over this exact 141-name list on a Mac after a build-for-testing, read off
        # the result bundle: "Test run with 1217 tests in 141 suites", no restart. 1199 + 18 = 1217 is
        # the ARITHMETIC CHECK, never the number.
        # RAISED 1217 -> 1220 (2026-09-21, the blind verify's fixes on item 1.2). Three suites
        # already on this line gained one cell each and no suite joined, so the NAME count stays
        # 141 and CIGateSelectorBoundaryTests is untouched: MeshLinkTableTests +1 (an all-timeout
        # endpoint's refunds are capped, so the sweep stops re-offering it), NetworkMeshSessionTests
        # +1 (the .preCommitTimeout arm of disconnectPeer(_:cause:) driven end to end — finding 5's
        # missing driver), MeshIntroductionAuthorityRosterTests +1 (a CLOSED session with no mesh
        # shuts the join door — finding 3's regression). MEASURED over this exact 141-name list on a
        # Mac after a build-for-testing, read off the result bundle: "Test run with 1220 tests in
        # 141 suites", no restart. 1217 + 3 = 1220 is the ARITHMETIC CHECK, never the number.
        # RAISED 1220 -> 1221 (2026-09-21, the cutover round's item 1.3 - the MC->QUIC FLIP,
        # MeshTransportFactory.shippingDefault .multipeer -> .quic). ONE suite already on this line
        # gained ONE cell and no suite joined or left, so the NAME count stays 141 and
        # CIGateSelectorBoundaryTests' `measuredSuiteNameCounts` is untouched: NetworkMeshSessionTests
        # +1 (theBrowseTranscriptLogsItsCountPubliclyAndItsPeerNamesPrivately - plan section 8.7
        # finding 1's owed log downgrade, taken now because the flip is what made this radio the
        # shipping one: the `browsed peers=` COUNT stays privacy: .public and the nearby Bonjour
        # instance NAMES go privacy: .private). Nothing was retired. The flip RENAMED three cells in
        # MeshTransportSelectionTests and one in MeshP9McRetirementAcceptanceTests and turned their
        # VALUES over, which moves no count; D-4.4's pure retire deleted a production leg that no
        # cell in the repository ever drove. MEASURED over this exact 141-name list on a Mac after a
        # build-for-testing, read off the result bundle: "Test run with 1221 tests in 141 suites
        # passed after 171.894 seconds", 0 failed, 0 skipped, no `Restarting after unexpected exit`
        # line. 1220 + 1 = 1221 is the ARITHMETIC CHECK, never the number.
        # RAISED 1221 -> 1222 (2026-09-21, the cutover verify's finding 1 - peer-derived values at
        # privacy: .public on the now-shipping QUIC mesh radio). ONE suite already on this line
        # gained ONE cell and no suite joined or left, so the NAME count stays 141 and
        # CIGateSelectorBoundaryTests' `measuredSuiteNameCounts` is untouched: NetworkMeshSessionTests
        # +1 (noPublicDiagnosticOnTheShippingRadioCarriesAPeerDerivedValue - the source wall over
        # every `privacy: .public` interpolation in NetworkMeshSession.swift, after the radio gained
        # the two sibling radios' salted session-scoped peerLabel(for:) and the tunnelEnded line's
        # STABLE identity fingerprint moved to privacy: .private). Nothing was retired; findings 2-5
        # and 7-9 of the same verify are doc, comment and label edits that move no count. MEASURED
        # over this exact 141-name list on a Mac after a build-for-testing, read off the result
        # bundle: "Test run with 1222 tests in 141 suites passed after 174.749 seconds", 0 failed,
        # 0 skipped, no `Restarting after unexpected exit` line. 1221 + 1 = 1222 is the ARITHMETIC
        # CHECK, never the number.
        # LOWERED 1222 -> 1214 (2026-09-22, the deletion round - MultipeerConnectivity leaves the
        # tree, one round after the flip and after the unseeded Lane C pair observed the provisional
        # path founding a mesh on a real tunnel). ONE name LEFT this line, deliberately:
        # MeshMultipeerSessionIdentityTests (4 cells) drove the retired radio's real delegate
        # callbacks and cannot exist without it; its invariant - a stable PeerHandle.id for the life
        # of a session - is carried on the QUIC radio by NetworkMeshSessionTests and
        # MeshLinkTableTests, both still here. So the NAME count moves 141 -> 140 in
        # CIGateSelectorBoundaryTests' `measuredSuiteNameCounts` in this same commit, with the
        # retirement argued there (the one way that number may go down). Also on this line:
        # MeshTransportSelectionTests lost its 4 `.multipeer` cells (the DEBUG opt-in, the per-kind
        # factory, the MC conformer ignoring the authority, the env-key resolver) with the seam they
        # pinned; NetworkMeshWireTests replaced bothTransportsShareOneInboundCeiling 1:1 with
        # theInboundCeilingIsTheFramingsInflateCap (the cap-identity #expect ported from the deleted
        # MeshTransportErrorSurfacingTests, which was on no CI line); MeshP9McRetirementAcceptanceTests
        # turned its cells over to the zero-list (files ABSENT, homes.isEmpty, the friend pair
        # RETIRED) and moved no count. MEASURED over this exact 140-name list on a Mac after a
        # build-for-testing, read off the result bundle: "Test run with 1214 tests in 140 suites
        # passed after 181.673 seconds", 0 failed, 0 skipped, no `Restarting after unexpected exit`
        # line. 1222 - 4 - 4 = 1214 is the ARITHMETIC CHECK, never the number.
        # RAISED 1214 -> 1216 (2026-09-22, the deletion round's blind verify, finding 1). ONE suite
        # already on this line gained TWO cells and no suite joined or left, so the NAME count stays
        # 140 and CIGateSelectorBoundaryTests is untouched: NetworkMeshWireTests +2
        # (theRadioAdmitsAFrameAtExactlyTheCeilingAndRefusesOneByteOver and
        # bothWireDirectionsAskThePredicateAtTheirGuard - the QUIC radio's OWN oversized-datagram
        # drop and outbound over-cap refusal, unpinned once the MC suite that drove them by delegate
        # was deleted; now one predicate, NetworkMeshSession.withinWireCeiling(_:), pinned as a value
        # with both call sites pinned by source needle). MEASURED over this exact 140-name list on a
        # Mac after a build-for-testing, read off the result bundle: "Test run with 1216 tests in
        # 140 suites passed after 180.646 seconds", 0 failed, 0 skipped, no `Restarting after
        # unexpected exit` line. 1214 + 2 = 1216 is the ARITHMETIC CHECK, never the number.
        run: |
          set -euo pipefail
          Scripts/run-gated-suites.sh mesh-batteries 1216 \
            CIGateSelectorBoundaryTests \
            MeshRoutedRefusalBudgetTests \
            MeshPairwiseFoundingTests \
            MeshRoutedTextBodyTests \
            MeshRoutedTextSenderTests \
            MeshRoutedTextDeliveryTests \
            MeshRoutedRetryPlanTests \
            MeshRoutedRetryAllowanceTests \
            MeshRoutedDrainWallTests \
            MeshRoutedDrainTests \
            MeshRoutedHeartBodyTests \
            MeshRoutedHeartTargetTests \
            MeshRoutedHeartCeremonyTests \
            MeshSessionHeartTests \
            MeshP3SessionAcceptanceTests \
            MeshP3RotationAcceptanceTests \
            MeshP3RestoreMatrixAcceptanceTests \
            MeshP3InteropAcceptanceTests \
            MeshConvergenceScheduleTests \
            MeshConvergencePropertyTests \
            MeshP4ScenarioMatrixAcceptanceTests \
            MeshP4ConvergencePropertyAcceptanceTests \
            MeshP4QuorumAcceptanceTests \
            MeshP4WorkedExampleAcceptanceTests \
            MeshP4EpochAcceptanceTests \
            MeshP4ContentAcceptanceTests \
            MeshP4LedgerCommitAcceptanceTests \
            MeshP4DeferralAcceptanceTests \
            MeshP4DeterminismAcceptanceTests \
            MeshRoutedDrainConvergenceTests \
            MeshP5ManifestAcceptanceTests \
            MeshP5CustodyAndReceiptAcceptanceTests \
            MeshP5AckStageAcceptanceTests \
            MeshP5BackpressureAcceptanceTests \
            MeshP5LockedDeviceAcceptanceTests \
            MeshP5PartitionDrainAcceptanceTests \
            MeshP5TypeRegistryAcceptanceTests \
            MeshP5RelayScopeAcceptanceTests \
            MeshP5ReplayAcceptanceTests \
            MeshP5OtherBranchDeliveryAcceptanceTests \
            MeshP5HonestyAcceptanceTests \
            MeshP5DeterminismAcceptanceTests \
            MeshRoutedLockedDeviceTests \
            MeshP6KeyAdvertisementAcceptanceTests \
            MeshP6PairwiseIdentityAcceptanceTests \
            MeshP6PerTypeCapAcceptanceTests \
            MeshP6TextRoutingAcceptanceTests \
            MeshP6ProjectionRetryAcceptanceTests \
            MeshP6HeartCeremonyAcceptanceTests \
            MeshP6HonestyAcceptanceTests \
            MeshP6DeterminismAcceptanceTests \
            MeshP7RunPolicyAcceptanceTests \
            MeshP7GateWriterAcceptanceTests \
            MeshP7RadioSeamsAcceptanceTests \
            MeshP7PollerAcceptanceTests \
            MeshP7ResumeAcceptanceTests \
            MeshP7HonestyAcceptanceTests \
            MeshP8CoordinatorTableAcceptanceTests \
            MeshP8HoldVerbAcceptanceTests \
            MeshP8RaiseAndDisagreementAcceptanceTests \
            MeshP8PresentationAcceptanceTests \
            MeshP8TaskWiringAcceptanceTests \
            MeshP8HonestyAcceptanceTests \
            MeshContinuationCoordinatorTests \
            MeshContinuationProgressTests \
            MeshContinuationCardPresentationTests \
            MeshContinuationDriverTests \
            MeshContinuationRaiseWallTests \
            MeshContinuationDisagreementTests \
            MeshContinuationTaskHostTests \
            MeshContinuationTaskHostWallTests \
            MeshKeyAgreementVerificationTests \
            MeshKeyAgreementFoldTests \
            MeshKeyAgreementSchemaTests \
            MeshKeyAdvertisementDeliveryTests \
            MeshRoutedPhotoSenderTests \
            MeshRoutedPhotoDeliveryTests \
            MeshRoutedManifestSigningTests \
            MeshRoutedCustodyHandoffTests \
            MeshRoutedCapacityTests \
            MeshRoutedItemSealTests \
            MeshRoutedTypeRegistryTests \
            MeshRoutedInventoryBuilderTests \
            MeshRoutedCustodyEvidenceTests \
            MeshRoutedStoreIsolationTests \
            MeshP9EphemeralPostureAcceptanceTests \
            MeshP9PresenceSwapAcceptanceTests \
            MeshP9RecipeSwapAcceptanceTests \
            MeshP9McRetirementAcceptanceTests \
            MeshP9HonestyAcceptanceTests \
            PresenceEpochPostureTests \
            PresenceAdvertisementTests \
            PresenceManagerTests \
            PresenceOverQUICTests \
            RecipeShareTransferTests \
            RecipeShareOverQUICTests \
            TransportNeutralityBoundaryTests \
            ProximityRecipeShareCapTests \
            ProximityRecipeShareDiagnosticsTests \
            MeshTransportSelectionTests \
            PresenceHeartsTests \
            PresenceTagTests \
            MeshLinkTableTests \
            MeshDialPreferenceTests \
            MeshHeartbeatScheduleTests \
            MeshHeartbeatLivenessTests \
            MeshTunnelEndReasonTests \
            MeshLinkAdvertisementTests \
            NetworkMeshWireTests \
            EphemeralMeshTLSIdentityTests \
            NetworkMeshSessionTests \
            MeshChannelIntroductionTests \
            MeshChannelIntroductionTranscriptTests \
            MeshIntroductionRosterTests \
            MeshIntroductionAuthorityRosterTests \
            MeshIntroductionNonceCacheTests \
            MeshInboundRankingTests \
            MeshTunnelConvergenceTests \
            MeshTransferStreamTableTests \
            MeshRoutedManifestGoldenTests \
            MeshRoutedTypeRegistryConsumerTests \
            MeshRoutedTypeRegistryDoorTests \
            MeshRoutedTypeRegistryFourthTypeTests \
            MeshRoutedCustodyHandoffWallTests \
            MeshRoutedCustodyHandoffDoorTests \
            MeshRoutedItemSealGoldenTests \
            MeshRoutedDeliveryPersistenceTests \
            MeshRoutedCustodyParityTests \
            MeshRoutedSweepTests \
            MeshRoutedParkedDropTests \
            MeshRoutedBackpressureTests \
            MeshRoutedParkedDropDoorTests \
            RoutedDeliveryHoldCopyTests \
            PeerHandleIdentityTests \
            FakePeerTransportTests \
            MeshP10SchedulingSeamAcceptanceTests \
            MeshP10HandlerPipelineAcceptanceTests \
            MeshP10DiffRuleAcceptanceTests \
            MeshP10ImportWallAcceptanceTests \
            MeshP10HonestyAcceptanceTests
